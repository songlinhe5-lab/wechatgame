# 《拼豆填色消除》(beads) 冒烟测试清单 · Smoke Test Suite

- 任务号：WXG-T-011 / WXG-T-028 / **WXG-T-084** ｜ 作者：严守真 ｜ 版本 **v1.2** ｜ 日期 **2026-09-14**
- 判据来源：`games/beads/design/gdd/*.md` §8、`systems-index §3`（含 §3.10/§3.11）、`concept.md §7`（MVP 验收线）；v1.1 新增冲刺段：`score-combo.md §8` + `ux-spec §5`；**v1.2 新增可感知段依据**：`ux-spec §6.1`、`assets-spec §1.2`、`audio-events §4`。

**图例（v1.2 扩展）**：`[Node]` vitest ｜ `[Probe]` **G4 探针**（`g4-probe.mjs`）｜ `[Harness]` 浏览器（**当前污染，BD-07/BD-08**）｜ `[Cocos]` 构建产物（**当前阻塞，BD-20**）｜ `[DevTools]` 微信开发者工具（**未装**）｜ `[Device]` 真机（**本轮不可执行**）。

> **v1.0 原文（已过期，保留作谱系）**：「beads 实现 0 行 → 本清单全部为“就绪待执行”」。
>
> **v1.2 实跑口径**：beads S1–S9 **已落码**（20 文件 / 184 vitest 全绿），本清单**已首次实跑**。自动化冒烟（`pnpm run harness:smoke`）双游戏 **✅ OK**（`evidence/g3-harness-smoke.log`）；但**人工链路的画面/音效半边全部不可执行或 FAIL**（详下表）。**任一步 FAIL 即停并开 Bug** 纪律沿用；本轮 FAIL 项已开 **BD-01..BD-26**（详 `g4-regression-report.md §5`）。

## 0. 本轮实跑结论汇总（v1.2 新增）

| ID | 链路 | 环境 | v1.0 状态 | **v1.2 本轮实跑结果** |
|---|---|---|---|---|
| SC-01 | 冷启动无菜单 | Node 分流 + DevTools 画面 | 就绪待执行 | ⚠️ **逻辑半边 ✅ / 画面半边 ⛔**：无存档 → 直接进 L1 `phase=playing`、**无主菜单** ✓；HUD 四带文本实测存在（`05:00` / `LV 1/8` / `×1` / 三道具标签）✓；但**托盘 12 槽全空**（**BD-02**）且**空槽无目标色**（**BD-01**）⇒ 「布局四带不重叠」的像素级判定 ⛔（`[Cocos]` 被 BD-20 阻塞） |
| SC-02 | 供料心跳 | Node | 就绪待执行 | ❌ **FAIL（BD-02）**：周期半边 ✓（对照组注入 `SPAWN_INTERVAL_DEFAULT` → 首颗珠第 241 帧 = 4.02s；L1 关卡值 6s → 第 361 帧 = 6.02s，均符合 `interval`）；但**首供缺失** ⇒ t=0/1.5s/3.0s 持有 **0/0/0** 颗，违反 `ux-spec §6.1`「1.5s 首颗珠已在托盘」 |
| SC-03 | 选珠→落子→拒绝 | Node + DevTools | 就绪待执行 | ⚠️ **事件半边 ✅ / 动效半边 ❌**：`bead:placed` 恰 1 次、珠离槽、格 `filled` ✓；`bead:rejected` 恰 1 次、珠留原槽、格保持 `empty` ✓（P4/P10/P12）；但**抖动+描边闪完全不存在**（拒绝后 12 帧指令增量 `[8×12]` 恒定，**BD-04**） |
| SC-04 | 暂停冻结 | Node + DevTools | 就绪待执行 | ⚠️ **逻辑半边 ✅ / 面板半边 ⛔**：PAUSED 下点托盘/网格/道具卡 ⇒ 事件总数 **0**、相位仍 `paused` ✓（P11，`input-control §8-8`）；面板像素与遮罩覆盖判定 ⛔（`[Cocos]` 阻塞）；「恢复后无 3-2-1」待执行（`pause-settings §8-2` 本轮未跑） |
| SC-05 | 满槽 → 清槽恢复 | Node | 就绪待执行 | ✅ **PASS**（P12）：满槽推 3 个间隔 `tray:full` **恰 1 次**（去重生效）、满槽期 `tray:spawned=0`、**不判负**（D7）✓；白盒排水后 1 个间隔内供料恢复（`=2`）✓ |
| SC-06 | 道具×3 + 扩展占位 | Node + DevTools | 就绪待执行 | ⚠️ **部分 ✅ / 入口 ❌**：三道具首轮均 `powerup:used` ✓、**第二轮全超限且零事件零扣次**、`powerupHint=「即将开放」`（**占位不误拉起** ✓，P6）；但**扩展入口在 UI 层不存在**（`btn_expand`/`_hitExpand` src 命中 0，全盘扫描命中 `tray:expanded`=**0 个点**，**BD-15**）⇒ 角标视觉项 ⛔ |
| SC-07 | 扩展行重置 | Node | 就绪待执行 | ⚠️ **方法层 ✅ / 玩家不可达 ❌**：`expandTray()` → 12 → **24** 槽 ✓（P12，`tray-spawner §8-5`）；重开/换关重置待执行；⚠️ **因 BD-15 无入口 ⇒ 此链路玩家端不存在** |
| SC-08 | 过关 LEVEL_CLEAR | Node + DevTools | 就绪待执行 | ⚠️ **事件半边 ✅ / 庆祝表现 ❌**：填满最后一格 → `level:cleared` 恰 1 次、结算帧写档恰 1 次 ✓（P18）；面板**等按钮不自动推进**已由 §F F1–F5 自动化覆盖 ✓；但「波浪庆祝」**无代码路径**（`vfx_complete_wave` 缺失，**BD-04**） |
| SC-09 | 通关 FINISH | Node | 就绪待执行 | 待执行（本轮未跑；8 关表已确认存在、`goToLevel(index)` 可用，**复验轮补**） |
| SC-10 | 归零 GAME_OVER + 五项重置 | Node | 就绪待执行 | ⚠️ **归零半边 ✅ / 五项重置待执行**：归零 → `phase=game-over`、`level:failed` 恰 1 次 ✓（P6/P18）；五项重置（`timer §8-6` / `pause §8-3`）本轮未跑。⚠️ **实测登记**：`game-over.onEnter`（`beads-game.ts:874-884`）**不调 `_persistProgress()`** ⇒ 失败结算帧零写档（不判缺陷，仅登记事实供策划确认 `save-progress §8-9`「结算帧」语义） |
| SC-11 | 杀进程续进 | Device | DEFERRED | ⛔ **不可测**：微信开发者工具未装、无 AppID、无真机。**解除条件**：工具 + AppID + 真机（M1-6） |
| SC-12 | 存档降级 | Node | 就绪待执行 | ⚠️ **写档已验 / 降级矩阵待执行**：实测存档内容 `{version:1, runs:1, maxUnlockedLevel:4, currentLevel:4, sprintBestScore:0, sprintBestStage:0, starsByLevel:[3,3,3,0,0,0,0,0], settings:{bgmMuted:false, sfxMuted:false}}`（P17）；`unlockedLevel=99`/非法 stars/损坏 JSON 三条降级路径（`save-progress §8-3`）本轮未跑。❌ **附带发现 BD-12**：§2.3 ❄️冻结的 `meta.winStreak*`/`lastPlayDate`/`signin` **无字段位** |
| SC-13 | 后台切换 | Device | DEFERRED | ⛔ **不可测**（同 SC-11）。**解除条件**：真机 |
| SC-SP-01 | 冲刺单局全链路 | Node + Harness + DevTools | 就绪待执行 | ⛔ **部分受阻**：入口半边 ✅（§F F3「▶ 去冲刺」路由已自动化验证）；但 ① **升档特效三档全不可观察**（`[DevTools]` 未装 + Lv2 伪震屏已登记平台缺口 WXG-T-074）；② **`[Harness]` 人工操作段全部污染**（BD-07 点不中）；③ stage 切换/断连/破纪录三段本轮未跑。**解除条件**：T-087 + 真机/DevTools |
| **SC-PER-01**（v1.2 新增） | **可感知冒烟：首屏 1.5s 三锚点** | `[Probe]` + `[Cocos]` | — | ❌ **FAIL**（= TC-PER-20）：1.5s 锚点 **0** 颗珠、无槽脉冲、无 `hint` 目标格；5s 锚点仍 **0** 颗 ⇒ **首屏零可操作对象**。解除：M0-2 + M0-3 + M1-1 |
| **SC-PER-02**（v1.2 新增） | **可感知冒烟：一次操作一个反馈** | `[Probe]` + `[Cocos]` | — | ❌ **FAIL**（= TC-PER-04/05/15）：落子后 12 帧指令恒定（零 VFX）、音频 clip 去重仅 **2** 种 vs 要求 **19**。解除：M0-4 + M1-2 |
| **SC-PER-03**（v1.2 新增） | **可感知冒烟：死局有出口** | `[Probe]` | — | ❌ **FAIL**（= TC-PER-06）：灌满 12/12 + 道具耗尽 → 60s 仍满槽 → 归零判负，**零非「整关重置」出口**。解除：**用户拍板 U8** + M0-6 |

> **v1.2 本轮可执行口径**：17 条中 **1 条全 PASS**（SC-05）、**7 条部分 PASS**（逻辑半边成立、表现半边 FAIL 或 ⛔）、**1 条 FAIL**（SC-02）、**2 条 ⛔ 不可测**（SC-11/SC-13）、**1 条部分受阻**（SC-SP-01）、**3 条待执行**（SC-09/SC-12 降级矩阵/SC-04 恢复半边）、**3 条 v1.2 新增可感知冒烟全 FAIL**。
>
> **结论**：自动化冒烟（`harness:smoke`）✅ 绿，但**人工链路冒烟不绿**——这正是 **BD-14（GAP-14）** 的具体体现：旧清单 14 条中 **12 条判定逻辑可由 vitest 覆盖**，而 vitest 全绿与「玩家能不能玩」**零相关**。v1.2 因此新增 **SC-PER-01..03** 三条可感知冒烟作为**人工链路的必要门**。

## 冒烟主链路

### SC-01 冷启动（首次，无存档）`[Node]`（分流逻辑）/ `[DevTools]`（画面）
- **步骤**：清存档启动。
- **预期**：直接进**第 1 关 PLAYING**，无主菜单（`core-loop §2.1`）；HUD 带（左设置齿轮/中倒计时胶囊/右胶囊避让）、拼图区、托盘 12 槽、道具栏 3 卡就位（`§3.1` 布局带）。
- **判定**：无菜单、无白屏、布局四带不重叠。

### SC-02 供料心跳 `[Node]`
- **预期**：默认间隔下每 4.0s ±0.1s 入 1 颗随机空闲槽，`tray:spawned` 逐次广播（`tray-spawner §2.2/§8.1`）。

### SC-03 选珠 → 落子 → 拒绝（微循环闭环）`[Node]` / `[DevTools]`（动画）
- **步骤**：点托盘珠（选中）→ 点匹配空格 → 再点不匹配空格。
- **预期**：匹配→珠离槽、格 `filled`、`bead:placed` 恰 1 次；不匹配→珠留槽、格 `empty`、`bead:rejected` 恰 1 次（抖动+描边闪 ≤2 次/秒）（`core-loop §2.2`、`bead-grid §2.3`）。

### SC-04 暂停冻结（计时 + 供料）`[Node]` / `[DevTools]`（面板）
- **预期**：PAUSED 期间 remaining 不变、零供料（`core-loop §8.7`、`tray-spawner §8.8`）；面板含「继续/重玩本关/音量开关」；玩法区点击全忽略（`input-control §2.3`）。
- **判定**：恢复后从暂停值继续，无 3-2-1（沿用 breakout 低摩擦判例）。

### SC-05 满槽 → 清槽恢复 `[Node]`
- **预期**：12 槽满→跳过供料 + `tray:full` 恰 1 次，**不判负**（D7）；用"区域消除"清 6 槽后 ≤1 间隔恢复供料（`core-loop §8.3`、`tray-spawner §8.4`）。

### SC-06 道具三种 + 扩展占位 `[Node]` / `[DevTools]`（角标）
- **预期**：region 清连续 6 槽 / clearAll 清全槽 / random 清 5 颗，均仅作用托盘、`powerup:used.affectedSlots` 一致；每道具免费 1 次，第二次点击出现激励视频**角标占位**且**不误拉起**（`§3.6`、AD `[待用户确认]`）。

### SC-07 扩展行 `[Node]`
- **预期**：解锁后 12+12 槽；重开/换关重置为未扩展（A1）；满槽期间解锁不补发跳过的供料（`tray-spawner §6`）。

### SC-08 过关 LEVEL_CLEAR `[Node]` / `[DevTools]`（庆祝表现）
- **预期**：最后一格填满瞬间 → LEVEL_CLEAR（即使剩余 0.01s，cleared 优先）；下一关 1.2s 级自动或点击进入（`core-loop §2.1`）；波浪庆祝 ≤3Hz（`§3.8`）。

### SC-09 通关 FINISH `[Node]`
- **预期**：第 8 关通过 → FINISH 通关画面；「重玩第 1 关」→ 整关重置回第 1 关（`core-loop §8.8`）。

### SC-10 倒计时归零 GAME_OVER + 整关重置 `[Node]`
- **预期**：归零未满图 → GAME_OVER（温和提示，不红闪不屏震）；重试后**五项重置**逐一生效（`timer-gameover §2.4/§8.6`）。

### SC-11 杀进程重启续进 `[Device]`
- **预期**：L3 进行中杀进程 → 重启直接续进已解锁最远关，无菜单（`core-loop §8.1`、S8 判例沿用 breakout）。

### SC-12 存档降级 `[Node]`
- **预期**：`unlockedLevel=99`/stars 非法/损坏 JSON → 降级进第 1 关，不崩溃、不中断（`core-loop §8.10`）；存档键名归代码（`wxgame.beads.*`，`systems-index §3` 刻意例外）。

### SC-13 后台切换 `[Device]`
- **预期**：`onHide` 自动暂停，`onShow` 停留 PAUSED 不自动继续（F1 冻结补偿，沿用 breakout 判例）。

## 冲刺模式冒烟段（v1.1 新增，WXG-T-028）

### SC-SP-01 冲刺单局最短通关链路 `[Node]`（逻辑断言归 TC-SPRINT-01..11）/ `[Harness]`（人工操作）/ `[DevTools]`（帧检/特效）

**前置**：清存档或预置已通关 1 关；DevTools 可用注入面板控制 `mode` 与计时。

**步骤（[Harness] 操作 → 预期观察点）**：
1. **进 sprint**：完成 L1 → LEVEL_CLEAR 面板点「去冲刺」（`ux-spec §4` U1 入口）→ 起局。
   - 观察：倒计时自 **120s** 起跳（`systems-index §3.10` `SPRINT_TIME_DEFAULT`）；HUD 出现 分数+倍率+连击数（`score-combo §2.1`）；stage 1 图案已装载；倍率角标此刻**不显示**（streak<2 零噪声，`score-combo §4`）。
2. **起连击升档**：连续正确落子 2 次 → 倍率 ×2 角标出现 + Lv1 星光粒子（ux-spec §5 200ms）；streak=4 → ×3 伪震屏（scale 1.00→1.015→1.00 / 150ms）；streak=7 → ×5 全屏爆发（350ms）。
   - 观察：每档切换恰 1 次；HUD 分数增量 = **10 × 当前倍率**（`§3.10` C4）。
3. **stage 切换不断连**：保持 ×3+ 连击填满 stage 1 图案。
   - 观察：立即换 stage 2、**不弹结算窗**（`score-combo §2.3`）；连击数/倍率不变（C8：stage 切换不断连）；倒计时 **+15s**（`§3.10` C5）；分数跳增 **200**（200+50×0）。
4. **故意超窗断连**：×3 档下停手 >**5.0s**（`§3.10` `COMBO_WINDOW_S`）。
   - 观察：倍率角标灰缩消失（150ms）、streak 清零回 ×1；分数**不清零不扣分**（`score-combo §2.4`）。
5. **单局结束 + 破纪录判定**：等倒计时归零（或 DevTools 快进）。
   - 观察：进结算 → 显示 单局分 + 最佳梯位 + `sprint:ended{score, bestStage, settleScore}` 广播（`systems-index §4`）；本次分 > S8 存档最佳 → **NEW BEST** 角标且存档写入；随后再开一局故意低分 → 无 NEW BEST、存档值不变（`score-combo §8.11`）。

**判定**：任一观察点 FAIL 即停、按复现步骤开 Bug（严重度参照 qa 计划分级）。逻辑层断言（事件恰次/公式复算/同帧裁决）由 §C 用例 `[Node]` 自动化，本段为人工链路验收。

## 冒烟汇总表（v1.0 原表，**状态列已过期**——真实结果见上方 §0）

| ID | 链路 | 环境 | 本轮（v1.0 原标注） |
|---|---|---|---|
| SC-01 | 冷启动无菜单 | Node 分流 + DevTools 画面 | 就绪待执行 |
| SC-02 | 供料心跳 | Node | 就绪待执行 |
| SC-03 | 选珠→落子→拒绝 | Node + DevTools | 就绪待执行 |
| SC-04 | 暂停冻结 | Node + DevTools | 就绪待执行 |
| SC-05 | 满槽→清槽恢复 | Node | 就绪待执行 |
| SC-06 | 道具×3 + 扩展占位 | Node + DevTools | 就绪待执行 |
| SC-07 | 扩展行重置 | Node | 就绪待执行 |
| SC-08 | 过关 | Node + DevTools | 就绪待执行 |
| SC-09 | FINISH | Node | 就绪待执行 |
| SC-10 | 归零 + 五项重置 | Node | 就绪待执行 |
| SC-11 | 杀进程续进 | Device | DEFERRED |
| SC-12 | 存档降级 | Node | 就绪待执行 |
| SC-13 | 后台切换 | Device | DEFERRED |
| SC-SP-01 | 冲刺单局全链路（进局→升档→跨 stage→断连→结算/破纪录） | Node + Harness + DevTools | 就绪待执行 |

> ~~**本轮可执行口径**：14 条中 12 条的判定逻辑可由 vitest 覆盖（实现落地后）；SC-11/SC-13 及各条的画面断言、SC-SP-01 的特效帧检需 Cocos 编辑器/真机。~~
>
> **v1.2 注（重要）**：上引口径是 **BD-14 的直接成因**——把「12/14 可由 vitest 覆盖」读作「冒烟已绿」，而 vitest **结构上无法发现** BD-01/02/03/04/05/06/10。v1.2 已新增 **SC-PER-01..03** 三条可感知冒烟（见 §0），**它们不可由 vitest 代替**，必须走 `[Probe]` 渲染指令流差分或 `[Cocos]` 产物截图。

---

## 风格与豆径族冒烟（**v1.3 / WXG-T-211-F 新增**）

> **依据**：`bead-visual-style-spec §12.2 C10`（验证口径原文：「每风格交付跑全量 `pnpm run verify` + `harness:smoke`（beads）；风格池全量交付后再跑一次满盘 A/B 对照（沿用复刻·四棱的三档缩放口径：0.75×/1.5×/3×）」）+ `C6`（新基线判据全绿 / 旧 1587 作废）+ `§12.9 步 1/3 验收列` + `ADR-0024 J-5` 方法 + `pause-settings v1.7 §8-14~19` + `save-progress v1.1 §8-11~13`。
> **效力边界（与 `test-cases.md §K.0a` 同文，不得删）**：① 命令数 / α 数下降**只算纸面口径**，不得写「性能改善」（`render-perf-device-baseline.md §10.1 禁令 3`）；② 屏幕层结论一律标 `[Cocos]` / `[Device]`，**本批全部未执行**；SVG 逐字节等值 = **指令流序列化**道次（`[Node]`），**不等于像素/真机屏一致**（K-037）；③ **落码前只拿红基线，不得预写绿**（K-035）；④ **SKIP ≠ 测**（K-036）。

### SC-ST-01 每风格全量门 `[Node]`（`[CI]` 腿）
- **命令**：`pnpm run verify`（18 项**逐项不短路**）＋（收严复跑）`pnpm run verify:strict`。
- **步骤**：任一风格（复刻·四棱 / `13` / `18`）落码后各跑**全量一次**（`C10`：逐风格，**不是"跑一次代表三套"**）。
- **预期**：**PASS 18 ｜ WARN 0 ｜ SKIP 0 ｜ FAIL 0**（`--strict` 下 SKIP 判失败）；结论须**逐项列出**（K-036），并**单列**本轮 SKIP 项与其自报原因。
- **判红条件**：① 任何 FAIL；② 出现 SKIP 却被汇总为"全绿"；③ 只报退出码、不报逐项结论。
- **本批基线（2026-09-26 00:14 实跑，非风格臂）**：`PASS 17 ｜ SKIP 1（`check:size`，未覆盖 ≠ 通过）｜ FAIL 0`；test 腿 = framework **330/330**、beads **592/592**、breakout **239/239**；`harness:smoke` = breakout OK ｜ **beads OK（5943 draw commands/帧，14×13/182 格/24 槽）**。⚠ 该 5943 与 §11.2 基线 1587 **夹具不同源，不得互校**。

### SC-ST-02 四棱转正零漂移（`legacy-ten` 对照臂）`[Node]`（离线 SVG，**非 `[Cocos]`**）
- **命令**：`pnpm run preview:frames:beads -- --out=<dirBefore>` → 转正批落码 → `pnpm run preview:frames:beads -- --out=<dirAfter>` → `diff -r <dirBefore> <dirAfter>`（或逐文件 `cmp`）。
- **对照对象（不得写错）**：**`legacy-ten` 臂 = 「什么都没变」**（`§12.9 步 3` 验收原话）——十层原样封成 `legacy-ten` 后其 SVG 输出**逐字节等值**；**四棱新基线臂本来就必须变**（新层集），拿"四棱零漂移"当门是**判据方向错**。
- **预期**：`legacy-ten` 臂逐字节 diff **为空**；方法先例 = `ADR-0024 J-5`（十层盘 8 关 + FX 夹具 24 文件 / 936 枚 polygon 逐字节等值，A 单证据 `temp/wxg-t-211/03-svg-byte-diff.txt`）。
- **判红条件**：diff 非空且无法归因到"新风格臂"；或产物陈旧（改源码未重建 ⇒ `harness:build` 须先于本步，锚 `§10.1 禁令 4`）。
- **效力**：等值只证**指令流未漂移**，**不证观感** ⇒ 观感面归 `playtest-plan.md` 皮肤族 `[待真机]`。

### SC-ST-03 行4 两钮回路（最短可玩回路）`[Node]`（逻辑半边）+ `[Cocos]`/`[Device]`（画面半边 ⛔）
- **步骤**：PLAYING 点齿轮进 PAUSED → 点「珠子风格」钮 1 次 → 点「豆子尺寸」钮 1 次 → 点「继续」。
- **预期（逐字引 `S9 §8-14/15/16/17` + `ux §4` 行4「切档后点『继续』」）**：相位全程 PAUSED 未切（面板内**不发 `game:resumed`**、不推 S5/S4 计时）；两字段**各写各的、恰写各 1 次、末值落档**；出栈后**新档已在画面**、**不重开 BOOT**、**棋局状态零写入**（L5）。
- **判红条件**：面板内出现 `game:resumed`；一次点按连带改写另一字段；出栈伴随关卡重载或 `filled` 计数变化；写档次数 ≠ 末值一次。
- **状态**：⛔ **需等落码**（`§12.9 步 5`；现码 `view-model.ts:196/200` 只有「性能信息 / 回主菜单」，**行4 两钮未实现**）⇒ 本行**登记为待执行，禁止预写绿**。

### SC-ST-04 `06` 珐琅出池引用行（**不造判据，仅登记门**）
- **口径**：本批 **Q1 = 甲**（可即刻三套）⇒ `06` 属**步 6**，其门 = `assets-spec §1.9.7 7.13` 的 **P-1…P-4 全绿 + A/B 判据 ①–⑤ 全绿 + 用户拍板进 registry**；三系数全 `[TODO]` **禁预填**。
- **冒烟义务**：若任一批次在未齐四条前置时把 `06` 塞进 `registry`（⇒ 钮循环出现第 4 档），SC-ST-01 与 `test-cases TC-STY-11` 同时判红（后者验的是"注册数与呈现一致"）。

> **v1.3 本轮结论（2026-09-26）**：SC-ST-01 **已跑**（非风格臂，基线如上，含 1 项 SKIP 如实登记）；SC-ST-02 方法已具备（A 单 J-5 先例）但**四棱转正批未进场 ⇒ 未跑**；SC-ST-03 / SC-ST-04 **待执行 / 引用行**。族内**零条判绿**。

## 变更记录

| 版本 | 日期 | 变更 | 作者 |
|---|---|---|---|
| v1.0 | 2026-09-11 | 初稿：SC-01..SC-13 主链路冒烟 + 汇总表 | 严守真 |
| v1.1 | 2026-09-12 | 增冲刺段 SC-SP-01（WXG-T-028）：进局→升档→跨 stage→断连→结算/破纪录五步观察点 | 严守真 |
| **v1.2** | **2026-09-14** | **WXG-T-084（首次实跑）**：① 新增 **§0 本轮实跑结论汇总表**（17 行，逐条标 ✅/⚠️/❌/⛔ + 探针编号 P1–P19 + 缺陷编号 BD-nn + **解除条件**）；② 图例扩展 `[Probe]`/`[Cocos]` 并标注 harness 污染与 Cocos 阻塞；③ **新增 3 条可感知冒烟 SC-PER-01..03**（首屏 1.5s 三锚点 / 一次操作一个反馈 / 死局有出口）——**本轮全 FAIL**，作为人工链路的必要门；④ 原汇总表**保留但标「状态列已过期」**，并注记「12/14 可由 vitest 覆盖」口径是 **BD-14 直接成因**；⑤ 登记实测事实：`game-over.onEnter` 不调 `_persistProgress()`（不判缺陷，供策划确认 §8-9「结算帧」语义） | 严守真 |
| **v1.3** | 2026-09-26 | **WXG-T-211-F（风格与豆径族冒烟）**：新增 **SC-ST-01..04**（逐风格全量 `verify`+`harness:smoke`／`legacy-ten` SVG 逐字节零漂移（含"对照臂不得写错为四棱"的纠偏）／行4 两钮最短回路／06 出池门引用行）。依据 = `§12.2 C10/C6` + `§12.9 步 1/3/5` + `ADR-0024 J-5` + `S9 v1.7 §8-14~19` + `S8 v1.1 §8-11~13`。**四条效力边界随文**（纸面口径不得写性能改善／屏幕层待执行／落码前只拿红基线／SKIP≠测）。基线读数：`verify` PASS 17｜**SKIP 1（`check:size`）**｜FAIL 0；beads 592、framework 330、breakout 239；`harness:smoke` beads OK（**5943** commands/帧，**与 1587 不同夹具**）。**本族零条判绿** | 严守真（主对话代落盘） |
