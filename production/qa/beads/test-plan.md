# 《拼豆填色消除》(beads) 测试计划 · Test Plan

- 项目：`games/beads`（第二款）｜ 任务号：**WXG-T-011**（P1）/ **WXG-T-084**（P0）｜ 作者：严守真
- 版本 **v1.1** ｜ 日期 **2026-09-14** ｜ 形态对齐 `production/qa/`（breakout 判例）
- **判据纪律**：全部判据从 `games/beads/design/gdd/*.md` §8 与 `systems-index.md §3` 导出，**禁止自造数值**；可感知层判据（v1.1 新增）从 `ux-spec §5/§6`、`assets-spec §1.2`、`accessibility`、`audio-events §4` 导出；本文不改任何设计文档。
- **v1.1 定位变更**：v1.0 是「判据先行轮」（beads 实现 0 行）；v1.1 是**首次实跑轮**——G1–G3 已全量实跑、G4 已执行（现状基线），详证见 **`g4-regression-report.md`**（v1.0）与 **`evidence/`**（6 份日志）。

## 0. 本轮环境事实（v1.1 全量更新）

| 项 | 状态 | 影响 |
|---|---|---|
| Node + vitest | ✅ 可用 | beads **20 文件 / 184 用例全绿**（1.40–2.15s）；breakout 12 / 239 全绿 |
| **beads 实现** | ✅ **已落码**（S1–S9，`src/**` + `tests/**`） | v1.0 的「NOT-RUN」全部解除；G1–G4 **本轮已实跑** |
| **beads 关卡数据** | ✅ 已产出（**8** 关，`levels-spec.md`） | L6（12×9，`locked=2`，7 色）/ L7（13×10，8 色）/ L8（13×12，`void=91`，8 色）**可走 Node 注入**（`goToLevel(index)`）；harness 侧仅暴露 L1–L5（**BD-13**） |
| **G4 探针** | ✅ 可用（`production/qa/beads/g4-probe.mjs` v3，19 组） | 可装配真实 `BeadsGame` + `NodePlatform`，读渲染指令流 / 事件流 / 存档；debug hooks `tapDesign()` / `giveTrayBead()` / `goToLevel()` |
| Cocos Creator 3.8.8 | ⚠️ **已装、工程已建，但取证路径阻塞** | `build:cocos:web`（beads）**被前置检查 FAIL 阻塞**（**BD-20**：`framework:sync` 拷贝件 4 处漂移）⇒ **所有 `[Cocos]` 道次本轮 ⛔ 不可测** |
| harness（浏览器） | ⚠️ **可用但污染** | **BD-07**（DPR/视口换算 y 轴折叠 ⇒ 点击全打飞）+ **BD-08**（文字镜像）⇒ T-087 修好前 **`[Harness]` 用例结论不可信，一律显式标「⛔ 污染」** |
| 微信开发者工具 / AppID / 真机 | ⛔ **未装、无 AppID** | 全部 `[DevTools]/[Device]` 用例本轮 **⛔ 不可测**（必须如实标注，**禁止跳过或标绿**——breakout D-01 教训） |
| 音频后端 | ⛔ **三平台均 `NullAudioBackend`**（`node.ts:57` / `web.ts:60` / `weapp.ts:139`） | 音频判据本轮不可测；⚠️ **真机到位也不解除此阻塞**（**BD-05b**） |
| 激励视频 | ⚠️ `MockRewardedAdProvider('complete')` | 续时**必然成功** ⇒ **不可作真机行为证据**；「未看完」分支必须靠替身注入 |
| UX spec / audio spec | ✅ **已产出**（v1.0 的 R3 已关） | `ux-spec.md`（WXG-T-081 重写：§5 动效毫秒表 21 行、§6.1 首 10 秒时间轴、§8 U6/U7/U8 裁决）+ `audio-events.md`（WXG-T-083，§4 A05-01..27 **明文「供 WXG-T-084 引用」**）⇒ **动效毫秒级断言现有权威值** |

## 1. 测试目标与质量门（G1–G4 在 beads 的适用口径 + **v1.1 实跑结果**）

| 门 | breakout 口径 | beads 口径 | **v1.1 实跑结果（WXG-T-084）** |
|---|---|---|---|
| G1 静态门 | tsc 零错 + check-architecture | `pnpm run verify` **13 项**逐项 | ❌ **FAIL**：第 8/13 项 `framework:sync:check` 失败（4 处拷贝件漂移，**BD-20**）⇒ `&&` 短路，**第 9–13 项从未执行**（**BD-17**）；本 QA 已手动补跑 9–13，均 ✅ |
| G2 单测门 | vitest 全绿 | vitest 全绿 + **判据↔测试 1:1 可追溯** | ✅ **PASS**（beads 20/184、breakout 12/239）；⚠️ **有覆盖缺口**：S2 `input-control §8` 全 10 条**零自动化测试文件**、§8 判据映射率仅 71/93（**BD-21**） |
| G3 冒烟门 | 本轮可执行冒烟全过 | `harness:smoke` + Cocos 构建 + 帧预览 | ⚠️ **部分**：`harness:smoke` 双游戏 ✅；`build:cocos:web`（beads）❌（BD-20）；`preview:frames` **不支持 beads**（**BD-19**）；`check:size` 对 beads **零覆盖却报绿**（**BD-18** 假绿风险） |
| G4 硬判据门 | §A 全过（含边界） | **§8 全 93 条 + §G 可感知层 20 条** | ❌ **FAIL**：19 组探针中 **11 FAIL / 5 PASS\* / 2 PASS / 1 ⛔**；6 项 P0 可感知判据全灭 ⇒ **建议裁定 FAIL**（详 `g4-regression-report.md`） |
| G5 真机门 | 真机签收 | 沿用 breakout 真机矩阵 + beads 追加：托盘/道具卡热区（C1）、三重编码灰度（A3） | ⛔ **未执行**（无工具、无 AppID、无真机） |

> 门禁纪律沿用 skill：未过 G1–G3 不进真机；环境阻塞显式登记「阻塞原因 + 解除条件」（见 §0 + `g4-regression-report.md §4.2`），**不静默跳过、不标绿**。PASS/CONCERNS/FAIL 裁定归编排层，本文只产判据与证据。
>
> **v1.1 新增门禁纪律（BD-14 根治）**：G4 不得只统计 `[Node]` 逻辑判据——**必须同时报「可感知层通过率」**（§G 20 条）。若逻辑层全绿而可感知层全灭，结论应为 **FAIL** 而非 PASS（本轮即此情形）。

## 2. 测试分层（v1.1 扩展）

| 层 | 环境 | 覆盖 | 本轮 |
|---|---|---|---|
| L1 单元 | `[Node]` | S3 落子裁决/格子状态机、S4 供料节律/加权/满槽、S5 计时/告急/冻结、S1 状态机合法边 | ✅ 184 用例全绿 |
| **L1.5 G4 探针**（v1.1 新增） | `[Probe]` | 装配真实 `BeadsGame`，读**渲染指令流 / 事件流 / 存档 / 快照**；可全盘扫描可点区域、可做帧间指令签名差分 | ✅ 19 组已跑（`g4-probe.mjs` v3） |
| L2 集成 | `[Node]` | 微循环闭环、同帧裁决（cleared 优先）、五项重置、事件契约 §4 | ✅ |
| **L2.5 可感知层**（v1.1 新增） | `[Cocos]` / `[Harness]` | §G 20 条：画面（空槽目标色/四类 VFX/告急脉冲/hint）、音效（19 clip + 同帧）、手感（首屏 1.5s 可操作、尾盘出口） | ⛔ `[Cocos]` 被 BD-20 阻塞；`[Harness]` 被 BD-07/08 污染 |
| L3 冒烟 | `[Harness]`→`[DevTools]` | 全链路（见 smoke-tests） | ⚠️ harness 可跑但结论污染 |
| L4 真机 | `[Device]` | 热区（C1 132×88）、灰度三重编码、杀进程续进、onHide 冻结、多点触控 | ⛔ 不可 |

## 3. 测试范围

**In（MVP 验收线，来源 `concept.md §7`）**：8 关；图案矩阵填色；托盘 12 槽+供料；倒计时+告急；3 道具（含激励视频**角标占位**）；扩展行（占位）；暂停/设置（音乐/音效开关）；本地存档（进度+星级+设置）；通关画面。

**Out（本轮）**：激励视频**实际拉起与发奖**（`systems-index §3.11` 已冻结失败页续时为唯一 live 位，但 `MockRewardedAdProvider('complete')` ⇒ **真机广告行为本轮不可取证**）；排行榜/云存档/内购（Won't 层）；**元游戏钩子（连胜礼盒/签到）**——`save-progress §2.3` 已冻结 v1.1 `meta` 字段但 src **零实现**（**BD-12**，且属 `concept §7` MVP 验收线之外）。

## 4. 入口 / 退出条件

- **入口**：GDD §8 判据就绪（✅ **v1.1：93 条全部就绪且 100% 映射**）；实现落地（✅ **v1.1：已满足**）⇒ **本轮已不再是「判据先行轮」，而是首次实跑轮**。
- **退出**：G1–G4 全 PASS 或 FAIL 项有缺陷单（✅ 本轮已出 **BD-01..BD-26**）；判据↔用例↔测试三方可追溯（⚠️ 部分：22 条§8 判据无自动化测试，BD-21）；阻塞项清单移交（✅ `g4-regression-report.md §4.2 + §10`）。
- **挂起（v1.0 的 R1 已关）**：S6/S7/S8/S9 四份 GDD **均已交付并冻结** ⇒ G4 判定现已可完整。
- **v1.1 新增退出条件（防假绿）**：`art/accessibility.md` 矩阵中每一个「✅ 落地」**必须附代码符号锚点或证据链接**，否则不得计入 G4 通过（防再犯依据：`g4-regression-report.md §6.4` 四轮谱系 + §6.5 F-1..F-5）。

## 5. 机型与基础库矩阵

沿用 `../test-plan.md §5` 全部维度；beads 追加两条：
- **触控热区**：`btn_expand` 热区 132×88（视觉 132×48，`accessibility.md C1` 修正项）须在窄屏/大屏各验一次；
- **三重编码**：色觉障碍/灰度下 10 色 + 6 状态可辨（`accessibility.md A1/A2/A3`），覆盖暗色珠（炭黑）与亮色珠（奶白）两端。

## 6. ⚠️ 风险登记（风险驱动优先级，**v1.1 全量重算**）

| # | 发现 | 证据 | 影响 | 处置 / **v1.1 状态** |
|---|---|---|---|---|
| **R1** | ~~S6/S7/S8/S9 四份 GDD 未写~~ | — | — | ✅ **已关**（`powerups` / `score-combo` / `save-progress` / `pause-settings` 均已交付并冻结） |
| **R2** | ~~beads 关卡数据未产出~~ | — | — | ✅ **已关**（8 关已产出）；⚠️ **残留**：逐关三旋钮档位的 **Playtest 主观判定线**仍需策划确认（见 `playtest-plan.md`） |
| **R3** | ~~UX spec 未写 ⇒ 动效毫秒无权威值~~ | — | — | ✅ **已关**：`ux-spec §5` 动效毫秒表 **21 行权威值** + `audio-events §4` A05-01..27 ⇒ **§G 可感知判据现有源可引** |
| **R4** | ~~任务书称 §8 共 40 条，实测 50 条~~ | — | — | ✅ **已关并升级**：v1.1 实测为 **93 条**（9 份 GDD），v1.2 用例只映射 71 条 ⇒ **BD-21**，已由 `test-cases.md` v1.3 §H 补齐 |
| **R5** | ~~激励视频接口存在性未定~~ | `systems-index §3.11`（失败续时已用户拍板冻结） | — | ✅ **已关**；⚠️ **新约束**：`MockRewardedAdProvider('complete')` 使「看完」分支必然成功 ⇒ **不可作真机证据**（已写进 TC-TIMER-12 取证纪律） |
| **R6**（v1.1 新增，**P0**） | ~~表现层零反馈~~ → **波次 2 已落码，v1.2 复验轮已取探针他证**：GAP-01/02/03/04/06/10/11 主体已全部存在（空槽目标色 / 第 1 帧首供 / 引导三通道 / 落座+拒绝 VFX / A′ 不变量 / 告急三通道 / hint 载体） | `g4-probe-v1.1.mjs` P1–P7/P19/P21 + `evidence/g4-reverify-v1.1.log §1` | 「完全无可玩性」的直接成因已除；§G 20 条中 **12 条「取证主体不存在」降为 0** | 🔻 **降级为 P1 残留**：① TC-PER-11/12（溶解/波浪两行未实现，BD-04 降级）② TC-PER-18 满槽告警描边呼吸零通道（BD-10 半边）③ 屏幕层像素取证仍未完成（→ **R11**） |
| **R7**（v1.1 新增，**P0 阻塞**） | ~~G1 FAIL + Cocos 取证全链路阻塞（`framework:sync` 4 处漂移）~~ | `pnpm run verify`：`framework:sync:check ✅`（beads+breakout 拷贝件一致）+ `cocos:check ✅`，14 项 **EXIT=0** ⇒ **BD-20 关闭（v1.2 实测）** | 原「阻塞 10 条 `[Cocos]` 道次」的**构建侧已解除**（web-mobile 产物存在，时间戳 2026-09-15 08:48） | ✅ **已关**；⚠️ **拆出新 R11**（无浏览器截图/滤镜通路 ⇒ `[Cocos]` 像素道次仍 ⛔，**阻塞原因已从「构建」换为「取证手段」**，不得混读为已解除） |
| **R8**（v1.1 新增，**P1**） | **三处结构性假绿风险**：① `verify` 用 `&&` 串联（v1.2 实测已增至 **14 项**，仍 `&&`）⇒ 任一项失败则其后各项**从未执行**且无提示（**BD-17 仍开放**）；② `check:size` 对 beads 无产物时 `status:'skipped'` 静默通过、总行仍打 ✅（**BD-18 仍开放**）；③ ~~`accessibility.md` 5 项假绿~~ → **BD-09 部分关闭**（D1/E2/A2 已实测转真；A2b/A3 仍 ⛔、C1 仍无对象） | `package.json:51`、`tools/scripts/check-bundle-size.mjs`、报告 v1.1 §14 | 「工程完成度高」的表象来源；使 G1/G3 **不具备发现能力**（v1.2 本轮 EXIT=0 只因无失败项，**不代表链路可信**） | ①② 交 T-082（`verify:all` 逐项收集 + `check:size` 遍历 `games/*` 且 skipped 至少 WARN）；③ 剩余项随 BD-01/04 与真机 |
| **R9**（v1.1 新增，**P1**） | ~~harness 取证环境不可信~~ → **v1.2 大幅解除**：BD-07 **关闭**（三视口往返误差 ≤2.3e-13 + **端到端 CSS-px 点击命中期望槽**，ADR-0011）；BD-13 **部分关闭**（harness `goToLevel(5/6/7)` 全部进 PLAYING，不再只暴露 L1–L5） | `g4-probe-v1.1.mjs` P13/P14；`dev/harness/main.ts:90-102` | `[Harness]` 用例不再属「⛔ 污染」，**本轮已可直接驱动真实 `InputManager` 取证（P11 因此升 PASS）** | ✅ 主体已关；**残留**：BD-08 文字镜像**仍不可验**（无像素通路，**文档称已修不作证据**）、BD-19 `preview:frames` 仍硬编码 breakout（`render-harness-frame.mjs:37/56/77` 实测）⇒ 归 R11 / T-082 |
| **R10**（v1.1 新增，**P1**） | **音频双通道零**：三平台 `NullAudioBackend` + `tuning.ts` 仅 3 个 clip vs 要求 19 | `packages/framework/src/platform/*.ts`；`g4-probe.mjs` P5 | 所有 `[B]/[P]/[R]` 音频判据**不因真机到位而解除**；Playtest「解压/治愈」维度必记 **BLOCKED**（`audio-spec §5` 明文） | 框架侧 + T-085；本轮 R6 需求已登记排期信号 |
| **R11**（v1.2 新增，**P1**） | **屏幕层取证通路缺失**：沙箱禁监听 socket、无无头截图与色盲/灰度滤镜脚本 | 报告 v1.1 §18.2 **B1**；evidence §5 未执行表 | §G 10 条的像素半边 + TC-PER-13/14 整条 + P15 真实栅格化 + BD-08（文字镜像）**因此仍不可验，相关条目不得改判** | **T-082 / 主理人**：提供截图脚本 + 命名约定 `evidence/shots/<case-id>-<frame>.png`（= v1.0 §10.2 R2/R3） |
| **R12**（v1.2 新增，**P1 流程**） | **任务书 R6 需求列表中的「R1/R2/R3 取证支持」尚未闭环**：R1（framework:sync）已满足✓；**R2（无头截图与坐标校验）/ R3（色盲与滤镜）本轮仍未交付** | 报告 v1.1 §18.2 B1；v1.0 §10.2 | 所有像素级可感知判据（TC-PER-13/14 整条、其余十条的像素半边）无法闭证 ⇒ 直接卡住 G4 升 PASS | **T-082 交付 R2/R3 手段 + 主理人授权 beads `build:cocos`（wechatgame）以闭 G3/BD-18** |

### 6.1 优先级排序（v1.1 重排：**可感知层升至首位**；v1.2 逐项更新状态）
1. **P0 · 可感知层六项**（§G TC-PER-01..07，= BD-01/02/03/04/05/06/10）：v1.0 **全部 FAIL** → **v1.2 复验：6/7 项转 PASS 或 PASS\***（唯 TC-PER-05 音频仍 FAIL，属 R10）；波次 2 目标已达成。
2. **P0 · 落子裁决与格子状态机**（S3 §8 全 10 条）：玩法真源，`filled` 终态不可回退是进度资产（✅ v1.2 复跑无回归，beads 198 测全绿）。
3. **P0 · 同帧裁决 cleared 优先**（`core-loop §8-5` / `timer §8-4`）：generosity 判例，违者 P0（✅ 无回归）。
4. **P0 · G1/Cocos 取证阻塞解除**（R7）：✅ **已关**（`framework:sync:check` ✅）；取证阻塞重心已移至 **R11（屏幕层通路）**。
5. **P1 · 暂停冻结**（`timer §8-5` / `core-loop §8-7` / `pause-settings §8-2`）：F1 可访问性承诺的补偿机制（✅ P11 实测 PAUSED 事件增量 0，经真实 `InputManager`）。
6. **P1 · 供料节律与满槽**（S4 §8-1/§8-4）：支柱 2 压力源的节拍器（✅ 期望已改 **16（±0）**且实跑成立；⚠️ 唯窗口口径待裁 = **BD-27**）。
7. **P1 · 输入门禁与热区**（S2 §8）：错点无害原则（✅ BD-23 已裁并回写、P9 升 PASS；⚠️ **BD-15 扩展入口 / BD-16 轻提示仍开放**）。
8. **P2 · 表现层与可访问性**（D1/D2/C1）：v1.2 更新——**D1/E2 已落码并实测（P22/P23）**；A2 目标色通道已实（P1）；⚠️ **A2b/A3 卡在 R11 像素通路**、**C1 卡在 BD-15（无对象）**。

## 7. 证据留存要求

沿用 `../test-plan.md §7`；beads 追加：
- 加权统计类用例（TC-TRAY-02/03/09）须附样本量与统计口径（脚本 + 分布直方图）；
- 事件计数断言（"恰发 1 次"/"=0"）须贴监听计数器输出；
- 每条 Bug 附 `rngSeed`（供料随机槽/随机消除均走 `services.rng`，控制清单 L4）。

## 8. 本轮可执行 / 需真机 占比（v1.1 重算，**v1.2 按复验实测修正道次可用性**）

> 明细见 `beads/test-cases.md` v1.3 合计表。汇总：**§8 判据 93 条（100% 映射）** ⇒ 用例 **137 条**（§A 50 + §B 12 + §C 11 + §D 10 + §E 4 + §F 8 + **§G 20** + **§H 22**）。
>
> **道次分布（v1.2 修正）**：`[Node]`/`[Probe]` 可覆盖 **约 118 条（86%）**——且**本轮全部可实跑**（v1.1 时因 BD-07/BD-20 部分路径不可信）；`[Harness]` **约 9 条（7%）**⇒ **坐标污染已解除（BD-07 关），仅余 `preview:frames` 与像素取证（R11）**；`[Cocos]` **10 条（7%）**——阻塞原因由「构建（BD-20）」换为「**无截图/滤镜通路（R11）**」，仍全部 ⛔；`[DevTools]/[Device]` **约 10 条（7%，无真机不可执行）**。
>
> **v1.2 本轮（复验轮 WXG-T-092）实际执行量**：**26 组探针**（P1–P19 全量重判 + P20–P26 补测，覆盖 §G 主判据 8/8 + 细化子条 8/12）+ **677 vitest**（framework 240 / breakout 239 / **beads 198**）+ G1–G3 **14 项实跑 EXIT=0** + 2 份夹具诊断脚本。**未执行项逐条登记于报告 v1.1 §18.2 与 evidence §5（含原因 + 解除条件），无一项静默跳过或标绿**。
>
> **v1.1 原口径（保留作谱系）**：19 组探针（覆盖 16 行用例）+ 184 vitest + G1–G3 13 项实跑；当时 `[Cocos]` 全部被 BD-20 阻塞、`[Harness]` 全部被 BD-07/08 污染。
>
> **v1.0 原文（已过期，保留作谱系）**：「判据 50 条（§A）+ 派生 12 条（§B）= 62 条用例；`[Node]` 56（90%）、`[Harness]/[DevTools]` 4（6%）、`[Device]` 2（3%）；但 beads 实现未开工，全部处于“判据就绪、待实现回归”状态」。

## 9. 变更记录

| 版本 | 日期 | 变更 | 作者 |
|---|---|---|---|
| v1.0 | 2026-09-11 | 初稿：判据先行轮（S1–S5 共 50 条 §8 判据全覆盖 + 派生 12 条）；R1–R5 风险登记 | 严守真 |
| **v1.1** | **2026-09-14** | **WXG-T-084（首次实跑轮）**：① §0 环境事实表**全量重写**（10 行：beads 已落码 / 8 关已产出 / 探针可用 / Cocos 取证阻塞 / harness 污染 / 无真机 / 三平台 NullAudio / Mock 广告 / ux+audio spec 已产出）；② §1 G1–G4 口径由「NOT-RUN」改为**实跑结论**（G1 ❌ / G2 ✅ / G3 ⚠️ / G4 ❌ / G5 ⛔）并新增**「可感知层通过率必报」门禁纪律**；③ §2 新增 **L1.5 `[Probe]`** 与 **L2.5 可感知层** 两层；④ §4 入口/退出条件更新（R1 已关）+ 新增**「✅ 必附代码锚点」防假绿退出条件**；⑤ §6 R1–R5 **全部标已关**（含残留项），新增 **R6–R10**（表现层零反馈 / G1+Cocos 阻塞 / 三处结构性假绿 / harness 不可信 / 音频双通道零）；⑥ §6.1 优先级**重排：可感知层升至首位**；⑦ §8 占比重算（137 用例 / 93 判据 / 四道次分布 / 本轮实际执行量）。证据均落盘 `evidence/`（6 份日志） | 严守真 |
| **v1.2** | **2026-09-15** | **WXG-T-092（复验轮，只动 QA 五件套本件 + 新增探针/证据）**：① §6 风险表 **R6–R10 逐行改状态**（R6 表现层零反馈 → 降级 P1 残留；R7 G1/Cocos 阻塞 → **已关**；R8 → ①② 仍开放、③ 部分关闭；R9 harness → 主体已关、残留 BD-08/BD-19；R10 维持）；② 新增 **R11（屏幕层取证通路缺失）** 与 **R12（v1.0 需求 R2/R3 未闭环）**——把「阻塞原因」从「构建不通」纠正为「取证手段不通」，防止被误读为已解除；③ §6.1 优先级 8 项逐项回填 v1.2 实测状态；④ §8 占比重算：`[Harness]` 污染已解除、`[Cocos]` 阻塞改性、本轮实际执行量（26 组探针 / 677 测 / 14 项门链）；⑤ 可感知层 20 条逐条「本轮可验道次」结论集中写于 `g4-regression-report.md` **v1.1 §17**（本件不重复列表，避免两处真相）。**建议裁定（只供主理人裁决）：G4 = CONCERNS**。 | 严守真 |
