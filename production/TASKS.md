# WXG 任务台账（SSOT）

> **格式（WXG-T-064 起）：标题制 + 详情分片。** 本表**只留标题**（名称列 ≤ 60 字符，由 `pnpm run check:tasks` 机械强制）；任务正文在 `production/TASKS-DETAIL.md` 的 `## WXG-T-0NN` 小节。**读取协议**：领号/看状态只读本表（≈ 2.2k tok）；要某任务详情时，从 `ctx/index.json` 取该小节的 `startLine`/`endLine`，`read_file(path, offset, limit)` **只读那一节**（中位 ≈ 290 tok），**不要整读详情文件**。归档时**行与小节成对搬走**。
> 单号递增不回收。任何会话（WorkBuddy/Cursor/Qoder/CodeBuddy）开工前**先读本文件领号**，完成后回填状态——根治跨 IDE 撞号（教训：2026-09-12 CodeBuddy 独立会话撞用 T-013/T-014）。
> 建档：2026-09-12，主理人游承峰。当前已分配至 **WXG-T-066**，下一可用号 **WXG-T-067**。ADR-0008 编号已为「每日挑战本地确定性派生 ADR」预约（未落盘，每日挑战专项立项时使用，跳空合规）；ADR-0009 = Cocos MCP 编辑器接入（已落盘）。
> 勘误（2026-09-13）：本注此前长期停在「已分配至 T-032」而表内已排到 T-035，曾致撞号（教训见 memory 记录）；WXG-T-040 归档落地后改为——**领号认本注（下一可用号），本注由 `tasks:archive` 校准**为「主表 ∪ 归档全局最大号 + 1」（归档会把老行移出主表，只看主表最大号必重号）；手工领号后请顺手核对本注（可运行 `pnpm run tasks:archive` 校准，0 行时不落盘）。

| Task ID | 名称 | 负责 | 状态 | 产出 |
|---|---|---|---|---|
| WXG-T-058 | beads Mock 续时实现 | 程基岩 | ✅ 完成 | 见详情 |
| WXG-T-055 | beads 打断留存 | 程基岩 | ✅ 完成（快照未落码） | 见详情 |
| WXG-T-056 | G4 复核：星级可达性用例缺口（注入 ratio 假绿） | 严守真 | ✅ 完成（readonly） | 见详情 |
| WXG-T-059 | beads 局内崩溃快照落码 | 主理人(CodeBuddy) | 🔄 进行中 | 见详情 |
| WXG-T-060 | beads S6 道具系统落码 | 主理人(CodeBuddy) | ✅ 完成 | 见详情 |
| WXG-T-061 | 「到达序」判据清理 | 主理人(CodeBuddy) | ✅ 完成 | 见详情 |
| WXG-T-062 | 三项待裁定落地 | 主理人(CodeBuddy) | ✅ 完成 | 见详情 |
| WXG-T-063 | EP-07 结算·过关面板落地 | 主理人(CodeBuddy) | 🔄 进行中（EP07-S1 关面完成） | 见详情 |
| WXG-T-064 | 台账标题制 + 详情分片（治膨胀） | 主理人(CodeBuddy) | ✅ 完成 | 见详情 |
| WXG-T-065 | 归档器成对搬运（行+详情节） | 主理人(CodeBuddy) | ✅ 完成 | 见详情 |
| WXG-T-066 | EP-07 通关画面（FINISH） | 主理人(CodeBuddy) | ✅ 完成 | 见详情 |
| WXG-T-067 | 冲刺结算面板（§3.5 左列） | 主理人(CodeBuddy) | ✅ 完成 | 见详情 |
| WXG-T-068 | memory 日志分级加载（摘要层） | 主理人(CodeBuddy) | ✅ 完成 | 见详情 |

> 注 1：T-016/T-017 的产出实际由 CodeBuddy 侧会话完成（用户授权路径），本台账为跨 IDE 统一追认。
> 注 2（第二次撞号追认）：CodeBuddy 会话后续又将 CI/CD 与 ADR-0009 误编为 016/017，已按 T-021/T-022 归位；其提交信息与任务单内嵌旧号不回改，以本台账为准。
> 注 3：多会话并行时领号纪律——开工前读本文件取「下一可用号」并立即回填占位，完成后再更新状态行。

## 待排（backlog，无号，立项时领取）

| 事项 | 来源 | 说明 |
| **台账随任务数线性膨胀（结构性；上轮误判为「已结」）** | WXG-T-053 曾以「体积驱动归档」结项，**本轮按用户反馈回检，判定该结论不足** | 实测 `production/TASKS.md` 的 **81% 体积是任务行详情**：16 行合计 ≈ **5334 tok**、中位行 **389**、最重 **613**，而非任务行部分仅 ≈1207。归档只清**已完成**行，**每新增一个任务仍带入 ~400–600 tok 详情** ⇒ 必然反复撞 B 项 8000（当前 7035，余量 965）。**解法方向已明确**：① 主表行改**标题制**（id ｜ 短标题 ｜ 负责 ｜ 状态 ｜ 产出）；② 详情移入 `production/TASKS-DETAIL.md` 的 `## WXG-T-0NN` **小节**；③ **装置无需新增机器**——`ctx/index.json` **已按小节**输出 `anchor/level/startLine/endLine/tokens/summary/keywords` ⇒ agent 先读标题表，命中后按该小节的 `startLine/endLine` 精确 `read_file`（＝用户要的「命中标题后再读详情」）；④ 配套三件：归档器**成对搬运**（行 + 小节）、新增**行长门**（主表单元格超限即 FAIL，进 pre-commit/verify）、`ctx/ROUTES.md` 增详情文件入口与读取协议。**风险**：双文件漂移 ⇒ 由「同 id 成对」门禁兜住 |
| **`epics-beads.md` 贴 B 门（结构性）** | WXG-T-066 提交时被 ctx 兜底门拦下 | 该文档 **8063 > 8000**（B 项单文件上限），本轮已收紧自己的新增文字压回 ~7900，**余量仅两位数** ⇒ 下一次任何实质性增补都会再撞。它不是「追加式日志」也不是 vendor 文档，**不适用 B 门豁免**（豁免只对那两类）。结构性解法：把顶部「状态时效声明」（纯**状态**、非设计，约占千余 token）拆到独立文件（如 `production/epics/epics-beads-status.md`），或按 Epic 拆文档；**不得靠删信息凑体积** |
| **星级表持久化（S8 字段）** | WXG-T-066 派生项 | 通关画面的「星级总览」现取**局内累计**（`_starsByLevel`，每关历史最好）——`BeadsSave` **无星级表字段**，冷启动即清空 ⇒ 重启后再通关，总览显示的是本次运行的成绩 ✗。补法三步（不在本轮夹带）：① `save-progress` GDD 增「星级表」字段并冻结；② `normalizeBeadsSave` 逐关钳 `0..3`、过关时随 `_persistProgress()` 写盘（版本 +1 或按字段降级）；③ 补「重启后总览保持」的判据 |
| **归档器缺「同号」守卫（结构问题）** | WXG-T-061 归档复核发现 | `tools/scripts/archive-tasks.mjs` 把主表行**原样搬入**归档，从不检查同号：实测 `WXG-T-032` 在归档里有**两行**（根因 = 两个会话各建一行同号任务，归档一并搬入；残行仅一句「并入下方同号完整行」）。本轮已手工合并为一行并补注记，但**守卫缺失仍在** ⇒ 下一个撞号会再次留下同号多行，而任何「id → 行」映射型工具会**静默丢行**。建议：写盘前扫「主表 ∪ 归档」的同号集合并**警告**（保守做法只警告不自动合并——合并语义需人判） |
|---|---|---|
| **净收益口径** | WXG-T-026 复验提出（严守真） | 现节省率是**毛节省**——未计入装置自身开销（`ctx/ROUTES.md` ≈3352 估算 tokens 常驻 + `ctx/index.json`）。需补「净节省」口径再下结论 |
| **压缩整文件读** | WXG-T-026 实测结论 | 整文件读占 **259/457（56.7%）**、大文件（≥3000 估算 tokens）整文件读 **65 次** → 拉低 P10（30.0%）。手段：`ROUTES.md §0` 增设「大文件禁止整文件读」硬约束 + 采新样本复测 P10（阶段目标 ≥40%） |
| **IDE 埋点接入** | `docs/agent/context-instrumentation-survey.md` | 四 IDE 均有 read 埋点能力（Cursor `beforeReadFile` / 其余 `PreToolUse`），**offset/limit 均 `[待实测]`**；建议先对 Cursor 写 log-only hook 实测粒度，再评估推广 |
| **X-01 裁剪后实玩验收** | WXG-T-051 | 引擎裁剪已执行（22→10 模块，主包 3008.3→**1815.1 KB**，内部目标首次达标），但**运行时实玩验收未完成**——类型检查抓不到禁模块的运行时行为。需在裁剪后的 `web-mobile` 产物上复玩：发球 / 挡板跟手 / 撞砖得分 / 生命与失败 / 通关流转 / 文字渲染。**任一异常即回加对应模块并重测**（诊断法：浏览器 console 报错里的缺失类名直接指向被裁模块）。 |
| **G3 遗留：Label 真实宽度替换** | WXG-T-050 回填（`VERSION.md` G3） | `_anchorForText()` 仍用 `字符数 × 字号 × 0.55` **估算**文本宽度（`packages/framework/src/adapters/cocos/cocos-renderer.ts:171`）；`VERSION.md` G3 明确要求「**必须**用真实 `Label` 尺寸替换（建议用 `UITransform.width` 回填）」。当前**目视通过**（预览验收文字未跑偏），但换文本长度 / 字号 / 字体时存在**跑偏风险**。修法：文本绘制后读 `UITransform.width` 回填锚点偏移；须补一条「长文本不越界」的回归判据 |
