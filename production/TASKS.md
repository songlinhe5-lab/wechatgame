# WXG 任务台账（SSOT）

> **格式（WXG-T-064 起）：标题制 + 详情分片。** 本表**只留标题**（名称列 ≤ 60 字符，由 `pnpm run check:tasks` 机械强制）；任务正文在 `production/TASKS-DETAIL.md` 的 `## WXG-T-0NN` 小节。**读取协议**：领号/看状态只读本表（≈ 2.2k tok）；要某任务详情时，从 `ctx/index.json` 取该小节的 `startLine`/`endLine`，`read_file(path, offset, limit)` **只读那一节**（中位 ≈ 290 tok），**不要整读详情文件**。归档时**行与小节成对搬走**。
> 单号递增不回收。任何会话（WorkBuddy/Cursor/Qoder/CodeBuddy）开工前**先读本文件领号**，完成后回填状态——根治跨 IDE 撞号（教训：2026-09-12 CodeBuddy 独立会话撞用 T-013/T-014）。
> 建档：2026-09-12，主理人游承峰。当前已分配至 **WXG-T-151**（⚠️ T-148 系并发会话在提交 `667d2c5` 中已使用但未登记，见注 7；T-149 = 头注预警所指的 v1.25 在途会话，**主表行与详情节已自登**，见 `## WXG-T-149`；T-150 = 本会话（Qoder）G2′ 解环器归位落码单，**主表行与详情节已自登**，见 `## WXG-T-150`；120 未启用，跳空合规），下一可用号 **WXG-T-152**。ADR-0011 = 屏幕坐标空间契约（已落盘，T-082）；ADR-0012 = G10 ES5 转译修法（T-090 待裁）。ADR-0008 编号已为「每日挑战本地确定性派生 ADR」预约（未落盘，每日挑战专项立项时使用，跳空合规）；ADR-0009 = Cocos MCP 编辑器接入（已落盘）。
> 勘误（2026-09-13）：本注此前长期停在「已分配至 T-032」而表内已排到 T-035，曾致撞号（教训见 memory 记录）；WXG-T-040 归档落地后改为——**领号认本注（下一可用号），本注由 `tasks:archive` 校准**为「主表 ∪ 归档全局最大号 + 1」（归档会把老行移出主表，只看主表最大号必重号）；手工领号后请顺手核对本注（可运行 `pnpm run tasks:archive` 校准，0 行时不落盘）。

| Task ID | 名称 | 负责 | 状态 | 产出 |
|---|---|---|---|---|
| WXG-T-077 | Cocos G3：Label 真实宽度替换 | 主理人(Qoder) | ✅ 完成（两半码齐 + 预览目视 PASS，G3 关闭；截图存证） | 见详情 |
| WXG-T-097 | beads·P1反馈缺口(BD-15/16+BD-04余类+BD-32) | 主理人(Qoder) | ✅ 完成（四项全关：BD-15/16/10/32/04；探针复跑归 QA） | 见详情 |
| WXG-T-099 | beads·取证通路补全(像素滤镜+frames+§H缺口) | 程基岩+严守真 | 📋 已立项（待施工） | 见详情 |
| WXG-T-128 | beads 美术 v1.4「动态质感章」风格单(G1-G9 动效欠账清偿) | 林绘澄(art)+主理人(Qoder) | 🔄 进行中（规格层定稿并叠至 **v1.5-r6**；7 项裁定已落笔；**落码 5 单：①G1 ✅ T-145、②G3/G4 ✅ T-146、②b G2′ 代码+判据 ✅ T-150（待提交）**，余 ③G7 原子批 ④G6 ⑤G5=T-132 待领） | 见详情 |
| WXG-T-129 | 微信真机触摸坐标归一化错误(BD-48) 可玩性 P0 | 程基岩(eng) | 📋 已立项（待施工） | 见详情 |
| WXG-T-127 | beads 可玩性实测差距修复(BD-43 热区错位 P1 等) | 程基岩(eng) | 📋 已立项（待施工） | 见详情 |
| WXG-T-132 | G5案B：渲染管线全局变换通道（跨 framework） | 程基岩(eng) | 📋 已立项（待施工） | 见详情 |
| WXG-T-133 | beads·Epic：错位归位工程实现（E1–E7 拆分） | 主理人(Qoder) | 🔄 进行中（E1 已派 T-134） | 见详情 |
| WXG-T-144 | beads·E7 QA 判据迁移（四族⛔+新玩法用例+G4 收口） | 严守真(qa) | ✅ 完成（test-cases §J + 报告 v1.9；探针 v2 数据下 ⛔ 待重写；**Epic T-133 收官 7/7**） | 见详情 |
| WXG-T-145 | beads·G1 落座回弹落码（vfx_fill_pop，T-128 落码①） | 主理人(Qoder) | ✅ 完成（328/328 绿；代码随并发会话 `dbb3c1c` 进 HEAD，**未经真机验证**） | 见详情 |
| WXG-T-146 | beads·落码②G3/G4 VFX+24槽托盘适配 | 并发会话+主理人收口 | ✅ 完成（369/369 全绿；G2′ 拆 T-149） | 见详情 |
| WXG-T-147 | beads·连通选取+整组收进（错位玩法组语义，用户裁定） | 主理人(Qoder) | ✅ 完成（335/335 全绿；8 邻接 flood fill 锚 + 组化取回；规格回填待 GDD 批。⚠️ **全绿串经复核不成立于其自身提交**，见详情节「收口后复核注」） | 见详情 |
| WXG-T-148 | beads·错位珠恒亮白环+锚珠抬起（用户反馈①②） | 并发会话(Qoder)，本会话代登记 | ✅ 完成（随 `667d2c5` 落码；⚠️ **领号未登记**——主表/头注均无此号，本行为事后补登） | 见详情 |
| WXG-T-149 | beads·失败续时 60→180 落码 + ref-video §10.7 勘误（用户 2026-09-17 拍板） | WorkBuddy 主会话 | ✅ 完成（systems-index v1.25 + tuning.ts src/cocos 双落 + 勘误；revive/timer 15/15 绿；⚠️ 全包另有 4 例存量红与本次无关，见详情） | 见详情 |
| WXG-T-150 | beads·G2′ 解环器归位落码（solver 归位动效三相） | 主理人(Qoder) | ✅ 完成（代码随 3ab5457 入库；369/369 绿；三裁定追认） | 见详情 |
| WXG-T-151 | beads·G4 探针 v2.0 适配复跑（满盘/24槽/组语义 54 组） | 严守真(qa) | 📋 已立项（待施工） | 见详情 |

> 注 1：T-016/T-017 的产出实际由 CodeBuddy 侧会话完成（用户授权路径），本台账为跨 IDE 统一追认。
> 注 2（第二次撞号追认）：CodeBuddy 会话后续又将 CI/CD 与 ADR-0009 误编为 016/017，已按 T-021/T-022 归位；其提交信息与任务单内嵌旧号不回改，以本台账为准。
> 注 3：多会话并行时领号纪律——开工前读本文件取「下一可用号」并立即回填占位，完成后再更新状态行。
> 注 4（撞号预警，2026-09-14 Qoder）：阶段0复盘期间 CodeBuddy 并发登记 T-079（Cocos MCP 接入），主理人子代理据过时头注拟从 T-079 起派单 → 险撞号；已按当前头注从 **T-080** 重编波次1（T-080~084）并立即回填占位。教训：**spawn/领号前必以主表当前头注为准复核号段，不采信子代理或记忆中的旧号**。
> 注 5（波次2 立项，2026-09-14 Qoder）：波次1（T-080~084）全部 ✅ 完成并核实落盘（G4=FAIL/G1 经 framework:sync 转绿）。用户拍板 **U8=A′+D / G10=先spike再裁ADR / 波次2全派P0 / R1=甲 / R2 BD-04升P0·BD-12维P2 / R4全按QA改 / R5+R6都做** 后，立项波次2 **T-085~090**（T-090 = T-082 取证撞到的 G10 新 P0）。systems-index 拟增常量已串行落盘 **v1.16**（EMPTY_*/AUDIO_*）；U8(DECOY_COLORS_MAX 2→0)+R1甲(D1/E2) 随 T-085/086/088 落码登记 **v1.17**。波次2 落码主体 = 主对话（可跑 verify 把关，避 spawn 回传丢失）。
> 注 6（波次3/4，2026-09-15 Qoder）：用户两度拍板「**甲**」（阶段0 推荐路线）→ 波次3 = T-092（严守真 G4 复验）+ T-093（文档漂移回写）；复验结果 **FAIL 11→4、零新回归** ⇒ 主理人于 T-094 正式裁决 **G4 = CONCERNS**（报告 §18.5：放行波次4 与降级 Playtest；不放行阶段7 发布与每日挑战）。波次4 一次性占位 **T-095~099**（顺序：T-095 门禁可信度 → T-096 音频后端 ∥ T-098 裁定包 → T-097 反馈缺口 → T-099 取证通路）。**事实修正**：G3 包体红线实测**不能**靠「授权代跑 build:cocos」解除——`wechatgame` 平台需有效 **AppID**（`build-cocos.mjs` 头注 09-14 实测），该阻塞归用户侧 B4。
> 注 7（**跨会话登记遗漏・2026-09-17**，WXG-T-146 代补）：同一工作树多会话并行时发生两类遗漏——① **用了号不登记**：`667d2c5` 提交消息写 **WXG-T-148** 但主表/头注/详情三处均无（已补行与节）；② **连带提交他人产出**：`dbb3c1c`（T-139 补遗）扫走 T-145 的 G1 五件、`3359c25`（T-147）扫走 T-146 的 G3/G4 五件与文档登记，两笔提交消息均未提这些。⇒ 头注「下一可用号」**不足以防撞**（并发会话可在头注未及时更新时直接占号），收口方必须跑 `git log --oneline -6` + `git status` 双向校对。另：`3359c25` 当时全包 **10 例红**（被扫走的 G4 面板延迟门撞既有面板判据）⇒ 详情内的全绿串必须按提交内容可重现，否则记为待核。

## 待排（backlog，无号，立项时领取）

> 纪律（WXG-T-076，由 `check:tasks` G 项机械强制）：**结项即移除**（不留删除线占位）；
> 每行 ≤160 字符——只写「是什么 / 谁发现 / 下一步动作」，长分析开任务再写。

| 事项 | 来源 | 说明 |
|---|---|---|
| **epics-beads.md 贴 B 门（结构性）** | WXG-T-066 | 现 ≈7900/8000 余量两位数：把顶部「状态时效声明」拆独立文件或按 Epic 拆文档；不适用豁免、不得删信息凑体积 |
| **净收益口径** | WXG-T-026 复验 | 现节省率是毛节省，未扣装置自身开销；补「净=毛−开销」口径再下结论（E4 已并列三行，待归因数据） |
| **压缩整文件读** | WXG-T-026 实测（整读 56.7%） | 手段：ROUTES 大文件禁整读硬约束 + hot-files 扩覆盖 + 采新样本复测 P10（阶段目标 ≥40%） |
| **IDE 埋点接入** | context-instrumentation-survey.md | 四 IDE 均有 read 埋点能力（offset/limit 粒度待实测）；先对 Cursor 写 log-only hook，再评估推广 |
| **X-01 裁剪后实玩验收** | WXG-T-051 | 裁剪已执行（主包 3008→1815 KB 达标）但运行时实玩验收未完成——裁剪后产物复玩走查，任一异常即回加模块 |
| **check:a11y 机械守卫（待立项）** | K-035 / WXG-T-091 | accessibility 矩阵每行增设「代码符号锚点」列，状态含「落地」者须在 games/*/src/** 命中，零命中即 FAIL；唯一能结构性阻止第 5 轮假绿的措施 |
| **Playtest M2 降级轮（波次4尾）** | WXG-T-092 §18.3-5 / T-094 | 音频后端 T-096 落地后领号开跑；在此之前音频维度**必记 BLOCKED**，不得对「解压/治愈」下 PASS（`audio-spec §5`） |
| **镜像脚本缺 `.ts.meta`（BD-33）** | 同上 | `cocos:check` 不覆盖；meta 须编辑器生成 ⇒ 无编辑器即阻塞，不伪造 |
| **包体分项表与实测差 16 KB（BD-36）** | 林绘澄 / WXG-T-098 | `assets-spec §5` 分项 1500+452+0+0=1952 ≠ 实测合计 1968；同源 `audio-spec §4.2` 同表述 ⇒ 下次产物重建时对齐口径，不动数值不改产物 |
| **A05-09 探针同步（T-103 残留）** | 严守真 §19 / WXG-T-103 | 判据主体已改 `sfx_combo_t2`（+新增 A05-09b）⇒ 探针与 `test-cases` 须同改后复跑，现仍记 ⛔；与 T-102 的 P4 复跑同批，不单独起一轮 |
| **ux-spec 预算豁免到期条** | WXG-T-098 | `ux-spec.md` 8683>8000 tok，本轮登记豁免；下次增幅 >500 tok 先评 §3 线框外移，不删信息凑体积 |
| **BD-37 扩展出口判据待改（随解锁上线）** | 主理人 / WXG-T-097 | `input-control §8-1`「点扩展→S4 收请求」与 `powerups §2.6` 布局 A 冲突；现按占位实装记 ⛔，解锁落地时同批改 §8-1+`ux-spec §4` 并复跑 P8 |
| **BD-39 装置自测未挂 verify** | WXG-T-111 → WXG-T-121 | 修桩后由 `selftests.mjs` 分两档挂 CI（fast 7 件 ≈6.5s／heavy 5 件 ≈25s）；余项 = `verify` STEPS 加 `selftest:fast`，待 T-110 |
| **BD-40 `ctx:check` 未挂 verify** | WXG-T-112 → WXG-T-121 | 已并入 fast 档（CI 另有 ctx job）⇒ CI 侧闭合；本地半边与 BD-39 同一次改动即闭合，执行器每次点名该缺口 |
| **BD-41 宿主双驱动（仿真 ≈2× 墙钟）** | 严守真 / WXG-T-119 | App 自驱 + schedule 同推 `app.tick` ⇒ 比值 2.003 稳定复现；真机同源 ⇒ 首验 P0，仍 ≈2× 升 P1（成员原记 BD-40 系撞号，正本以此号为准） |
| **BD-42 BD-40/41 撞号残留订正** | 主理人 / WXG-T-119 | §WXG-T-122 背景段「T-121 已挂 BD-40 门禁」失真（挂的是 ctx:check 装置门）；探针 CLK-01 行仍写 BD-40 ⇒ 正本号 BD-41；diag 脚本移出 evidence |

（注：「台账随任务数线性膨胀」已由 WXG-T-064/065/068 结项移除，不再占位。）
