# WXG 任务台账（SSOT）

> **格式（WXG-T-064 起）：标题制 + 详情分片。** 本表**只留标题**（名称列 ≤ 60 字符，由 `pnpm run check:tasks` 机械强制）；任务正文在 `production/TASKS-DETAIL.md` 的 `## WXG-T-0NN` 小节。**读取协议**：领号/看状态只读本表（≈ 2.2k tok）；要某任务详情时，从 `ctx/index.json` 取该小节的 `startLine`/`endLine`，`read_file(path, offset, limit)` **只读那一节**（中位 ≈ 290 tok），**不要整读详情文件**。归档时**行与小节成对搬走**。
> 单号递增不回收。任何会话（WorkBuddy/Cursor/Qoder/CodeBuddy）开工前**先读本文件领号**，完成后回填状态——根治跨 IDE 撞号（教训：2026-09-12 CodeBuddy 独立会话撞用 T-013/T-014）。
> 建档：2026-09-12，主理人游承峰。当前已分配至 **WXG-T-183**（⚠️ T-148 系并发会话在提交 `667d2c5` 中已使用但未登记，见注 7；T-149 = 头注预警所指的 v1.25 在途会话，**主表行与详情节已自登**，见 `## WXG-T-149`；T-150 = 本会话（Qoder）G2′ 解环器归位落码单，**主表行与详情节已自登**，见 `## WXG-T-150`；T-151 = 并发会话随 `adb471a` 登记的 G4 探针复跑单（严守真）；T-152 = 本会话（Qoder）G7 轻压原子批，**初误占 151 与 QA 表撞号后改领**，见 `## WXG-T-152`；T-153 = 本会话（Qoder）G6 结算彩带落码单，**领号前已核 HEAD 至 3c1470e 无此号**，见 `## WXG-T-153`；T-154 = 本会话（Qoder）BD-49 道具音频断链修复单（T-151 移交项，backlog 按优排序首项），见 `## WXG-T-154`；T-155 = 本会话（Qoder）check:a11y 机械守卫立项单（backlog 次项，K-035）；T-156 = 本会话（Qoder）BD-50 Cocos rgba 色彩解析修复单（裁定②排障），见 `## WXG-T-156`；120 未启用，跳空合规），T-157 = CodeBuddy 会话组选收窄+board 锚直填落码单（用户三项裁定），见 `## WXG-T-157`（领号前已核工作树全局最大号 = 157，grep 无 158 引用）；T-158 = 本会话（Qoder）托盘同色归类+组选+批量填充落码单（用户四项裁定 2026-09-18），见 `## WXG-T-158`；T-159 = 本会话（CodeBuddy）breakout 配色双轨漂移定性与处置单（backlog 项，领号前已核工作树无此号），见 `## WXG-T-159`；T-160 = 本会话（CodeBuddy）BD-39/BD-40 装置自测挂 verify 单（backlog 项，领号前已核工作树无此号），见 `## WXG-T-160`；T-161 = 本会话（CodeBuddy）微信宿主触摸逆变换公式勘误单（BD-48 真因，用户 2026-09-18「无法点击任何珠子」；领号前已核工作树无此号），见 `## WXG-T-161`；T-162 = 本会话（Qoder）beads+框架真机五项修复单（SFX 离线渲染/组选全连通/直填放距/乙档缝宽/同心圆角，用户 2026-09-18 真机五项裁定；领号前已核工作树无此号），见 `## WXG-T-162`；T-163 = 本会话（Qoder）主菜单+元游戏页族程序结构设计稿（程基岩委草、零落盘；用户四项结构拍板：B 壳双对象 / 分批 0-1-2 / 本地榜+好友置灰 / 装体力+广告+钱包），见 `## WXG-T-163`；T-164 = 本会话（Qoder）元游戏页族反转冻结回写 + 批0 落码单（用户八项拍板；六文回写 systems-index v1.28 / ux-spec v1.7 / concept v1.4 / meta-framework v1.1 / pause-settings v1.3 + changelog），见 `## WXG-T-164`；T-165 = 本会话（Qoder）真机首验反馈修复批（白环移除 + 回主菜单弃本局棋盘、§3.14 反转），见 `## WXG-T-165`；T-166 = 本会话（Qoder）棋盘区双指缩放+单指拖拽立项（框架单点输入前置、当时待 ADR；**2026-09-19 已核销**——ADR-0015 Accepted、代码交付在 T-169），见 `## WXG-T-166`；**⚠️ WXG-T-167 = 并发会话（ADR-0015 C-3 判据翻转「棋盘区抬起才提交」，design-strategist）已占用但漏登主表——本会话领号时由 `games/beads/design/gdd/input-control.md` v2.5 头注发现，故本会话改领 168**（判例 K-046 的反向面：只 grep 主表仍会撞号，须查工作树 GDD/ADR 头注）；T-168 = 本会话（CodeBuddy）取回落槽口径改写单，见 `## WXG-T-168`；T-169 = 本会话（Qoder）棋盘缩放平移落码 Z0–Z6（ADR-0015 甲′+丁-3+C-3(a) 实施），见 `## WXG-T-169`；T-170 = 本会话（Qoder）F1 同固定步 down+up 丢 tap 修复单（T-169 §A4c.4 移交、用户 2026-09-18 授权执行；领号前已核工作树无此号），见 `## WXG-T-170`；T-171 = 本会话（Qoder）F2 棋盘 tap/drag 位移度量形态裁决单（T-169 §A4c.4-F2 移交；GDD v2.5 写切比雪夫 vs 实现为曼哈顿；四选一待用户拍板；领号前已核工作树无此号），见 `## WXG-T-171`；T-172 = 本会话（Qoder→studio-orchestrator）F3 相机复位口径回写单（GDD/ADR「恒等」 vs 实现 fitCamera 漂移；T-169 §A4c.4-F3 移交；跨三成员 spawn，推荐以实现为准）；T-173 = 本会话（Qoder→studio-orchestrator）U11 扫光链起算帧归属裁决单（core-loop v2.1 注② vs 道具卡属区外两上位口径张力；跨三成员 spawn，需用户先裁）；T-176 = 本会话（Qoder）ctx 生成器 `SKIP_DIRS` 跳过 vendor `_repos` 单（索引产物瘦身；并发会话已把 175 登记为外来 skill 集成并把头注顶到 176，本会话按 K-046 先扫工作树后占 176），见 `## WXG-T-176`；T-178 = 本会话（Qoder）真机「能缩放不能拖动」修复单（框架输入层 prev 结算时序，根因见详情节；沉淀 K-062），见 `## WXG-T-178`；T-179 / T-180 / T-181 = CodeBuddy 会话（照片转拼豆 spike / 29×29 盘面落档 / zoom LOD 立项，见主表）；⚠️ **WXG-T-182 = 工作树已占用未登主表**（`games/beads/design/forensics/wxg-t-182-g3-derangement-deadlock.md`，程基岩 2026-09-19 G-3 取证留档，结论已采纳）——本会话（Qoder）领号时按 K-046 发现，用户裁定改领 183（T-167 同型判例），本注已校准；T-183 = 本会话（Qoder）board 锚直填消费序改写单（用户 2026-09-20 裁定：选豆点固定+同序配对；领号前已核工作树全局最大号 = 182），见 `## WXG-T-183`；下一可用号 **WXG-T-184**。ADR-0011 = 屏幕坐标空间契约（已落盘，T-082）；ADR-0012 = G10 ES5 转译修法（T-090 待裁）。ADR-0008 编号已为「每日挑战本地确定性派生 ADR」预约（未落盘，每日挑战专项立项时使用，跳空合规）；ADR-0009 = Cocos MCP 编辑器接入（已落盘）；ADR-0014 = 渲染管线全局变换通道（已落盘，WXG-T-132）。
> 勘误（2026-09-13）：本注此前长期停在「已分配至 T-032」而表内已排到 T-035，曾致撞号（教训见 memory 记录）；WXG-T-040 归档落地后改为——**领号认本注（下一可用号），本注由 `tasks:archive` 校准**为「主表 ∪ 归档全局最大号 + 1」（归档会把老行移出主表，只看主表最大号必重号）；手工领号后请顺手核对本注（可运行 `pnpm run tasks:archive` 校准，0 行时不落盘）。

| Task ID | 名称 | 负责 | 状态 | 产出 |
|---|---|---|---|---|
| WXG-T-128 | beads 美术 v1.4「动态质感章」风格单(G1-G9 动效欠账清偿) | 林绘澄(art)+主理人(Qoder) | ✅ 完成（2026-09-19 收口 by CodeBuddy：**待裁③ 已裁 = 甲** —— 扫光「臂起后恒播满、不因状态切换中断」写入 `assets-spec §1.6.3`「状态无关性」+ `ux-spec v1.13`，判据 = `scene-vfx.test.ts` 新增 1 例（含 K-036 变异自检）；**QA 两处漏回写补记** —— TC-PER-21/26 的 `[Cocos]` 由「待执行」转「已验」（2026-09-18 人眼判读，附非真机屏效力边界）+ TC-PER-26 差异②闭合回填 ⇒ `test-cases v1.16`；落码 5 单全入库；规格侧待裁项**归零**——差异① scratch=B 已执行（409+295 绿）、差异② 2026-09-17 用户认可闭合（系 y 轴登记误读，公式/规格零改动）、G9 a11y D1 已改关停项、P0-4 澄清达标（release 1927.9KB）；**G6/G3 `[Cocos]` 连拍 v2.1 复跑 14/14 + 人眼判读两项均 ✅**（2026-09-18 收口：G6 飘落/自清/禁飞带干净、BD-50 不复现；G3 斜带扫过全程、顶部 HUD 零侵入，底部道具带被扫经核 §1.6.3 属规格内）；**真机首验包 P0-1..P1-7 全 ✅（2026-09-18 用户扫码，SHOW_ALL 重开）**；**真机侧唯一余项 = P1-8 集中走查轮**（2026-09-19 新挂、跨 T-164/165/168/169，依赖 AppID 扫码 ⇒ **移交真机轨、不阻塞本单验收**） | 见详情 |
| WXG-T-164 | beads·元游戏页族反转冻结回写+批0 落码（体力/钱包/振动，用户八项拍板） | 主理人(Qoder) | ✅ 收口（2026-09-19：六文回写+批0 落码随 `79f217a` 入库；真机首验 P0-D 元游戏 OK，菜单路由一项由 T-165 当场裁定反转并已落码 `3f79f6d`；**收口批补 T-165 的语义漏回写**：ux-spec v1.11（四）/ concept v1.5（D10）/ meta-framework v1.2（§7）/ systems-index v1.31（S10 指针）；余项 = 批1（`gdd/meta-ui.md` + ui-kit，已挂 backlog 无号）+ 设备面并入下次真机复验批） | 见详情 |
| WXG-T-166 | beads·棋盘区双指缩放+单指拖拽（立项，框架单点输入前置） | 程基岩(eng)+文策渊(design)·主理人核销 | ✅ 核销（2026-09-19：本单只到「立项 + 待 ADR」；范围已由 `ADR-0015`（Accepted）应答、代码交付在 T-169 Z1–Z6（甲′ 第二指针槽 + `board-camera.ts` + `gridLayoutFor(camera?)`），判据翻转在 T-167，缺陷批 F1/F2/F3 在 T-170/171/172；Z0 经真机反证解除。余项已分流（C-5→art 轨、三占位值→§3 变更单、zoom≠1 命中→真机首验包）；ADR 状态行 F3-c 同批校正） | 见详情 |
| WXG-T-167 | beads·缩放设计轨（C-3 判据翻转 + 图鉴/扩容盘面 concept 提案） | 文策渊(design)·主理人编排 | ✅ 两笔交付均入库（(i) 判据翻转已随 `e95a55f`/`1fefe3b`；(ii) 2026-09-19 用户裁 **Q1=甲 / Q2=逐格推进 / Q4=甲 / Q5=甲** ⇒ 新建 `gdd/meta-ui.md` v0.2（185 行、7,992 tok、**零 §3 变更零新常量**）随 `bcd1dcb` 入库；S10 行改记 = systems-index v1.33；顺带清 `concept` 托盘 24/12 漂移（v1.6）。未决：Q3/Q6/Q7/Q7-b/Q8–Q11）→ **v0.3 已裁完 Q7=甲（逐格扣，§3.14 现文与数值零改动）/ Q7-b=甲（退回即弃本局，与 v1.29 同构）**⇒ §8-7/§8-9 转正、§10 CH 表外移为 `meta-ui-changelog.md`（先例 WXG-T-062，不再欠豁免）；**未决仅余 Q3/Q6/Q8/Q9/Q10/Q11**）| 见详情 |
| WXG-T-169 | beads·棋盘缩放平移落码（Z0–Z6） | 程基岩(eng)·主理人收口 | ✅ 收口（2026-09-19：Z1–Z6 全落、Z0 真机反证解除、F1/F2/F3/U11 已由 T-170~173 关闭；**收口批补 QA §A4c.2 无落点欠账**：新落 4 条 `[Node]` 用例 + F5 护栏锁，beads **501 例绿**、**五轮变异自检**均按预期转红；F7 注释误称已改、F8 已闭合、F6 不并入⇒挂 backlog。余项 = 三占位值 §3 变更单、C-5 art 轨、zoom≠1 真机 P0（首验包 P1-8）、TC-SUB-05 构造性阻塞） | 见详情 |
| WXG-T-170 | beads·F1 同固定步 down+up 丢 tap 修复 | 主理人(Qoder) | ✅ 完成（`_readInput` onBoard 分支同帧 justUp 就地提交；beads 490 例绿 +1 回归例；`TC-SUB-06` 升为已验） | 见详情 |
| WXG-T-171 | beads·F2 棋盘 tap/drag 位移度量形态裁决 | 主理人(Qoder)→文策渊+程基岩+严守真 | ✅ 完成（用户 2026-09-18 拍板**甲 · 切比雪夫 L∞**；代码改 `sum`→`max` 1 行；beads 491 例绿 +1 反验；TC-SUB-06 同级新增 F2 形态回归例）| 见详情 |
| WXG-T-172 | beads·F3 相机复位口径回写（GDD/ADR vs fitCamera） | studio-orchestrator→文策渊+程基岩+严守真 | ✅ 完成（用户 2026-09-18 拍板**甲 + 附裁 5→2**；GDD v2.7 + ADR §3.4 回写、代码仅注释、TC-CAM-08 新 5 例；beads **496 例绿**、verify 17/17）| 见详情 |
| WXG-T-173 | beads·U11 扫光链起算帧归属裁决 | studio-orchestrator→文策渊+程基岩+严守真 | ✅ 完成（用户 2026-09-18 拍板**丁** · 摘出注②）；`core-loop v2.2` + `ux-spec v1.10` + `test-cases v1.14` 回写，代码仅注释；**零行为改动**、零判据迁移；beads **496 例绿**、verify 17/17 | 见详情 |
| WXG-T-175 | 外来 skill 集成·A 档（CCGS 点菜式吸收三件套） | 主理人(CodeBuddy) | ✅ 落码（三 skill 正本 + 12 条四 IDE 链接 + 上游留档 5.4M；verify 17/17 PASS；**待真跑一个 story 验证价值**） | 见详情 |
| WXG-T-176 | ctx 生成器 SKIP_DIRS 跳过 vendor `_repos`（索引产物瘦身） | 主理人(Qoder) | ✅ 完成（`my-skills/_repos/**` 早被 `.gitignore` 忽略，但生成器只走盘不读 gitignore ⇒ 补 `SKIP_DIRS` 一项；`ctx/index.json` 136,369→60,271 行、3.68→1.7 MB；verify 17/17）| 见详情 |
| WXG-T-177 | beads·冲刺入口隐藏（去冲刺三处全隐藏，U1 反转） | 主理人(CodeBuddy) | ✅ 落码（三面板隐藏 + 单钮居中；beads 498 例绿、verify 17/17 PASS；待真机复验） | 见详情 |
| WXG-T-178 | framework·真机拖不动修复（输入位移帧末结算）+ 调试读回口 | 主理人(Qoder)·程基岩复核 | ✅ 落码待真机复扫（根因：`beginFrame` 拷 prev ⇒ 事件驱动宿主帧间到达的位移被吞成 0；修法移到 `endFrame`，新契约用例先红后绿；framework 307/beads 500/breakout 239 零回归，探针实测 offsetX 走满夹取 ±12.5；release 1955.0 KB） | 见详情 |
| WXG-T-179 | beads·照片转拼豆生成器 spike（调色板/限色/聚集 调优 + ADR-0016） | 主理人(CodeBuddy)·续作 Qoder | 🔄 待用户裁定 ADR-0016（**续作已交**：beads-studio 服务+CI+小游戏在线导入；**已部署到腾讯云 VPS 且 CI 发布链跑通**（PR #4 合并 → 自动 deploy ✓），仅待安全组放行 8787（隧道实测可用）；修两处漂移 K-064） | 见详情 |
| WXG-T-180 | beads·盘面规格落档（29×29 标准盘）+ MVP 三关草案 | 主理人(CodeBuddy) | ✅ 主体完成（GRID_MAX v1.34 + 生成器转正 + ADR-0018 两档 8 关已入库 `89f28e0`；29×29 转设计档；留边模型搁置=保留 24px，用户 2026-09-20） | 见详情 |
| WXG-T-181 | beads·大盘 zoom 自适应 LOD 立项（ADR-0017，风险 1） | 主理人(CodeBuddy) | ✅ 收口（ADR-0017 Proposed 留档；MVP 两档无 LOD 需求不阻塞，LOD 转 29×29 解锁前置另立项；初始取景口径维持现状，用户 2026-09-20） | 见详情 |
| WXG-T-183 | beads·board 锚直填消费序改写（选豆点固定+同序配对，用户 2026-09-20 裁定） | 主理人(Qoder) | ✅ 完成（`_boardSelected` 增 anchorRow/Col 恒定拾取锚；直填/部分收纳消费序基准改选豆点；bead-grid v2.4；判据 1 例；beads 511 例绿、verify 17/17；沉淀 K-063） | 见详情 |

> 注 1：T-016/T-017 的产出实际由 CodeBuddy 侧会话完成（用户授权路径），本台账为跨 IDE 统一追认。
> 注 2（第二次撞号追认）：CodeBuddy 会话后续又将 CI/CD 与 ADR-0009 误编为 016/017，已按 T-021/T-022 归位；其提交信息与任务单内嵌旧号不回改，以本台账为准。
> 注 3：多会话并行时领号纪律——开工前读本文件取「下一可用号」并立即回填占位，完成后再更新状态行。
> 注 4（撞号预警，2026-09-14 Qoder）：阶段0复盘期间 CodeBuddy 并发登记 T-079（Cocos MCP 接入），主理人子代理据过时头注拟从 T-079 起派单 → 险撞号；已按当前头注从 **T-080** 重编波次1（T-080~084）并立即回填占位。教训：**spawn/领号前必以主表当前头注为准复核号段，不采信子代理或记忆中的旧号**。
> 注 5（波次2 立项，2026-09-14 Qoder）：波次1（T-080~084）全部 ✅ 完成并核实落盘（G4=FAIL/G1 经 framework:sync 转绿）。用户拍板 **U8=A′+D / G10=先spike再裁ADR / 波次2全派P0 / R1=甲 / R2 BD-04升P0·BD-12维P2 / R4全按QA改 / R5+R6都做** 后，立项波次2 **T-085~090**（T-090 = T-082 取证撞到的 G10 新 P0）。systems-index 拟增常量已串行落盘 **v1.16**（EMPTY_*/AUDIO_*）；U8(DECOY_COLORS_MAX 2→0)+R1甲(D1/E2) 随 T-085/086/088 落码登记 **v1.17**。波次2 落码主体 = 主对话（可跑 verify 把关，避 spawn 回传丢失）。
> 注 6（波次3/4，2026-09-15 Qoder）：用户两度拍板「**甲**」（阶段0 推荐路线）→ 波次3 = T-092（严守真 G4 复验）+ T-093（文档漂移回写）；复验结果 **FAIL 11→4、零新回归** ⇒ 主理人于 T-094 正式裁决 **G4 = CONCERNS**（报告 §18.5：放行波次4 与降级 Playtest；不放行阶段7 发布与每日挑战）。波次4 一次性占位 **T-095~099**（顺序：T-095 门禁可信度 → T-096 音频后端 ∥ T-098 裁定包 → T-097 反馈缺口 → T-099 取证通路）。**事实修正**：G3 包体红线实测**不能**靠「授权代跑 build:cocos」解除——`wechatgame` 平台需有效 **AppID**（`build-cocos.mjs` 头注 09-14 实测），该阻塞归用户侧 B4。
> 注 7（**跨会话登记遗漏・2026-09-17**，WXG-T-146 代补）：同一工作树多会话并行时发生两类遗漏——① **用了号不登记**：`667d2c5` 提交消息写 **WXG-T-148** 但主表/头注/详情三处均无（已补行与节）；② **连带提交他人产出**：`dbb3c1c`（T-139 补遗）扫走 T-145 的 G1 五件、`3359c25`（T-147）扫走 T-146 的 G3/G4 五件与文档登记，两笔提交消息均未提这些。⇒ 头注「下一可用号」**不足以防撞**（并发会话可在头注未及时更新时直接占号），收口方必须跑 `git log --oneline -6` + `git status` 双向校对。另：`3359c25` 当时全包 **10 例红**（被扫走的 G4 面板延迟门撞既有面板判据）⇒ 详情内的全绿串必须按提交内容可重现，否则记为待核。

## 待排（backlog，无号，立项时领取）

> 纪律（WXG-T-076，由 `check:tasks` G 项机械强制）：**结项即移除**（不留删除线占位）；
> 每行 ≤160 字符——只写「是什么 / 谁发现 / 下一步动作」，长分析开任务再写。

| 事项 | 来源 | 说明 |
|---|---|---|
| **净收益口径** | WXG-T-026 复验 | 现节省率是毛节省，未扣装置自身开销；补「净=毛−开销」口径再下结论（E4 已并列三行，待归因数据） |
| **压缩整文件读** | WXG-T-026 实测（整读 56.7%） | 手段：ROUTES 大文件禁整读硬约束 + hot-files 扩覆盖 + 采新样本复测 P10（阶段目标 ≥40%） |
| **IDE 埋点接入** | context-instrumentation-survey.md | 四 IDE 均有 read 埋点能力（offset/limit 粒度待实测）；先对 Cursor 写 log-only hook，再评估推广 |
| **X-01 裁剪后实玩验收** | WXG-T-051 | 裁剪已执行（主包 3008→1815 KB 达标）但运行时实玩验收未完成——裁剪后产物复玩走查，任一异常即回加模块 |
| **breakout 配色双轨漂移（B2 项·定性已订正）** | WXG-T-155 → **WXG-T-159** | 定性已订正（漂移面仅 `palette.ts` UI 场景色，砖块色全对齐）；用户裁定 **C 维持登记 ⛔**：两侧均不动 |
| **Playtest M2 降级轮（波次4尾）** | WXG-T-092 §18.3-5 / T-094 | 音频后端 T-096 落地后领号开跑；在此之前音频维度**必记 BLOCKED**，不得对「解压/治愈」下 PASS（`audio-spec §5`） |
| **镜像脚本缺 `.ts.meta`（BD-33）** | 同上 | `cocos:check` 不覆盖；meta 须编辑器生成 ⇒ 无编辑器即阻塞，不伪造 |
| **包体分项表与实测差 16 KB（BD-36）** | 林绘澄 / WXG-T-098 | `assets-spec §5` 分项 1500+452+0+0=1952 ≠ 实测合计 1968；同源 `audio-spec §4.2` 同表述 ⇒ 下次产物重建时对齐口径，不动数值不改产物 |
| **ux-spec 预算豁免到期条** | WXG-T-098 / WXG-T-164 收口复测 | 实测 19587 tok（豁免 note 里的 8683 已过期）；下次触 ux-spec 先评 §3 线框外移，不删信息凑体积 |
| **BD-37 扩展出口判据待改（随解锁上线）** | 主理人 / WXG-T-097 | `input-control §8-1`「点扩展→S4 收请求」与 `powerups §2.6` 布局 A 冲突；现按占位实装记 ⛔，解锁落地时同批改 §8-1+`ux-spec §4` 并复跑 P8 |
| **BD-41 宿主双驱动（仿真 ≈2× 墙钟）** | 严守真 / WXG-T-119 | App 自驱 + schedule 同推 `app.tick` ⇒ 比值 2.003 稳定复现；真机同源 ⇒ 首验 P0，仍 ≈2× 升 P1（原记 BD-40 系撞号，正本以此号为准；订正见报告 §27.8） |
| **`build:cocos:wx` 脚本名漂移** | WXG-T-129 指引实测 | 仓库无此脚本，实际入口 = `build:cocos`（wechatgame 默认平台，测包体加 `--release`）；QA 文档 20+ 处待订正（历史报告不回改） |
| **beads F6：G4 探针未随 v2.5 时基翻转升级** | QA §A4c.4-F6 / T-169 收口 | `g4-probe tapChain` 仍单固定步 down+up；改两帧形后需复跑 §A4b/§H/§G 族，历史 `[Probe]` 读数待复核 |
| **`meta:overlay` 字段名不一致：§4 写 `id`、现码 emit `name`** | meta-ui CH-6 / WXG-T-167 | 需主理人定是 §4 文本还是码侧命名（`beads-shell.ts:257`），同批补 `album` 值域 |
| **beads C-5：缩放后描边/缝隙是否随缩** | ADR-0015 C-5 / T-166 核销 | 现按「不随缩」占位并标 `[R]/[P]`；需 art 轨拍板，改后按 K-051 差分复算图元与包体 |
| **beads §3 变更单：相机三占位值 + F3-b 底线** | T-169/172 移交 / T-166 核销 | fit 边距 / 缩放上限 / 位移阈值 + z<1×`TOUCH_MIN` 底线；须 playtest 或用户给值 |
| **beads 元游戏批1（ui-kit 图元层 + 图鉴/本地榜/名片页；余 GDD 半边已由 T-167 交草案）** | WXG-T-164 收口 / T-167 执行批 | 前置 = 裁完 Q7/Q7-b 后 meta-ui 转正；ui-kit 仍无人领 |
| **save-progress §8-9 规格回写待裁** | 严守真 / WXG-T-151 | 规格「通关写档」与 E1 onboarded/局内快照语义互斥；需设计域裁定（区分快照写档/业务写档）后改文 |

（注：「台账随任务数线性膨胀」已由 WXG-T-064/065/068 结项移除，不再占位。）
