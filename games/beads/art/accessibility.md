# 《拼豆》可访问性规范（Accessibility Spec）

> 项目：微信小游戏矩阵 · 第二款《拼豆填色消除》
> 版本：**v1.5**（demo 阶段；本批增量登记为 **v1.5-r2**）｜ 作者：林绘澄（可访问性）
> **v1.5（WXG-T-130 第二批，2026-09-16）**：**可访问性降档登记**——用户裁定「幽灵符号（E4）移除」并给出裁定原文（= **用户签字**，见 A2b/A3 行内登记）；A2b 由「Basic+Standard 落地」降档为「**部分降级（有可恢复路径）**」、A3 降档范围收窄至 `empty` 空槽；**降级范围 = 黑白（灰度）模式下同明度档空槽不可分**；**可恢复路径 = E4 绘制逻辑保留为关闭的开关/死路径（不删码），重开零成本**（改回 `EMPTY_GHOST_ALPHA` + 开关置开）。A1（珠子三重编码）**不受影响**。本文件由文策渊代为登记（授权落盘），美术/质感侧归 WXG-T-131。
> **v1.5-r2（WXG-T-131 交叉收口，2026-09-16，林绘澄）**：**修订对象 = 上行 v1.5（文策渊代登记），与 v1.4 / v1.4-r2 无涉**。v1.5 已完成「降档事实 + 用户裁定原文 + 可恢复路径」的**登记**；本 r2 补它缺的四件事：① **量化核算**（v1.5 的 A2b/A3 为定性描述，现给 10 色实测：空槽主读面 S2 坑底在灰度下仅 **6 个可分离明度档**、**4 色（丁香紫/玫红/湖蓝/赭棕）无独立明度位**、4 组相邻对 ΔL < 本仓自判据 4% ⇒ v1.5 原句「Standard 层（色相可辨）维持」**不成立**，实际可达 = **Basic 的弱化形态**）；② **规格态 vs 代码态分离 +「落码即假绿」窗口显式化**（`bead-render.ts:200/232-240` E4 仍在画 ⇒ A2b 此前的 ✅ 描述的是**代码现状、不是假绿**；但 `assets-spec §1.2` 的禁画令一旦落码，empty 侧档位在同一提交内立即消失）；③ **A6 论证精确化 + 六条逐条复核**（G1 原论证「色+符号+凸起形态三通道」在 empty 侧不成立，改为「符号的**出现**即事件」⇒ 结论仍真且更强）；④ **D1 受影响的诚实登记**（v1.5 本以为与动效无关，实测 **D1 受影响**：G1 保留项「L0a α 单峰」的地色由容器板 `#FFFFFF` 改判为 S1 暗缘框 ⇒ 峰 ΔL 由 **0.156 塌到 0.097～0.003**（降 38%～98%），**深棕/炭黑任何 α ≤1 都不可恢复**；缓解 = D1 保留清单增列「S1 咬合显影环」，零 `ux-spec §5` 值变更）。另修 A2 的 `empty`/`hint` 两条（**`ux-spec §1` L16「hint grep 零命中、A2 属假绿」经复核为误报**，但 v1.22 下 hint 的**触发前提断裂**）、§3 清单口径与 **E1 同名不同物消歧**、A5 追加 ⑧⑨。**D2 已复核、不受影响**（显式给结论）。**毫秒值零改动**（真源 `ux-spec §5`；需变更处一律列清单交主理人串行落笔，见 §4 第 7 条）、**色值零新增**、**冻结常量零新增**。
> v1.4：随 art-bible v1.4「**动态质感章**」（**WXG-T-128**；台账已被并发会话改号，`WXG-T-127` 现为程基岩「beads 可玩性实测差距修复」工程单）做 **G9 连带回写**——D1 行「落座回弹 / 消除溶解缩放」由 **N/A 改为关停项**（该 N/A 的前提「本作无对应动画通道」在 G1/G2 落码后自动失效，不回写即下一次假绿的种子）；D2 / A5 追加六条新动效的频率与三重编码核算；**新增 A6「动画不承载独占信息」**（D1 敢下「整条关停」的合法性前提）；§2 追加本轮复核记录；§4 追加 G7 → ux 矩阵中转条。**毫秒值零改动**（真源 = `ux-spec §5`）、**色值零新增**、**冻结常量零新增**。
> **v1.4-r2（三方复核后修订，同批未提交）**：① **修缺陷**——A5 ⑦ 间隙 **4px → 3px**（只被点一颗缩到 48 ⇒ `52−24−25 = 3`）、A5 ① **`27+25=52` → `26.5+25=51.5 < 52`**（与 ④ 精确算法统一，结论仍为零重叠）、全文改号 **127 → 128**（4 处）；② **回写用户 7 项裁定**——A5 ⑤ / D1 / §2 复核 ⑥ 的 **G5「两案待裁」→「案 B 定案、案 A 作废」**（**a11y 口径不变：D1 整条关停**）、D2 / A6 补 **G7 新增音频通道 `sfx_denied`**（第 20 剪辑，与视觉**同门** 0.25s ⇒ 不新增视觉往复）、§2 复核 ⑤ 的 G7 跨域连带由「仅登记」改为 **已闭合**（主理人串行落笔）、§4 第 6 条由「中转登记」改为 **已落地**（`ux-spec §5` 新增「不可填格轻压」行，毫秒有源、未决项 3 清除）。**毫秒值零改动、色值零新增、冻结常量零新增**（唯一跨域例外：裁定 3 连带 `AUDIO_CLIP_TOTAL` 19→20 属音频/GDD 域冻结变更单，不在本美术单）。
> v1.3（承前）：丙案新色板/新层次回查 + F7 a11y 假绿根治（B1/E1 口径与实装重新对齐）；新增 A5 丙案层次回查行。
> 配套：`art-bible.md`、`assets-spec.md`。方法论与分级体系：`my-skills/wxgame-art-spec-programmatic/SKILL.md`
> 设计分辨率 750×1334 / `FIXED_WIDTH` → **1 设计 px ≈ 0.5 pt**，88 设计 px ≈ 44pt。

---

## 1. 分级选择

三级承诺：Basic / Standard / Comprehensive。

| 分级 | 本 demo 目标 |
|---|---|
| Basic | ✅ 必须全部满足 |
| **Standard** | ✅ **本 demo 目标等级** |
| Comprehensive | 🔶 部分落地，其余延后（见 §3） |

> **结论：本 demo 承诺达到 Standard，并落地 Comprehensive 中成本低的若干项。**
> 与 breakout 一致：微信小游戏平台不支持屏幕阅读器，属平台限制，不计入本团队承诺范围。

---

## 2. 特性矩阵

| # | 特性 | 具体要求（本 demo 具体值） | 落地状态 |
|---|---|---|---|
| A1 | **色觉障碍友好 · 珠子颜色** | 「颜色 + 符号 + 明度」**三重编码**：10 色各绑唯一矢量符号（○★●■♥◐▽▲◆✚，path 绘制不依赖字体），明度分 5 档 | ✅ **落地** |
| A2 | **色觉障碍友好 · 珠子状态** | `locked` X 斜纹 / `empty` 目标色底 / `hint` 蓝描边呼吸 / `wrong` 抖动+红描边 / `selected` 上浮+圆点——**每个状态都有非颜色通道**。**`[v1.5]` empty 态的 E4 幽灵符号已移除**（用户裁定，见 A2b）⇒ empty 态非颜色通道收窄为：纯色底色相 + 明度 +（WXG-T-131）凹陷质感形态。<br>**`[v1.5-r2]` 逐项代码复核（本行五条状态各自核实，不整行沿用 ✅）**：<br>· `locked` X 斜纹 ⇒ ✅ **已实装且已接入**（`view-model.ts:692` `drawLockedBead(builder, bx, cy, palette)`；撤销此前口头流传的「有导出零调用者」疑点，**该疑点为误报**）。<br>· `empty` 目标色底 ⇒ ⚠️ 代码态仍为 **E1 混色底 + E4 幽灵符号**（`view-model.ts:699` 对网格空槽无条件传 `cell.colorIdx`），规格态已改**纯色凹陷坑四层**（`assets-spec §1.2`）⇒ 非颜色通道由「符号」收窄为「**明度 6 档 + 凹凸形态 1 bit**」（量化见 A2b ②/A3 ②）。<br>· `hint` 蓝描边呼吸 ⇒ ⚠️ **已实装但 v1.22 下触发前提断裂**：渲染在 `view-model.ts:701-703`（`drawStateRing(palette.hintBlue, hintAlpha(pulseClock, reduceMotion))`）、托盘槽脉冲在 `:783`（`guideSlot`）、`onboarding` 在 `beads-game.ts:2228` 接通 ⇒ **`ux-spec §1` L16 的「`hint` grep 零命中、A2 标 ✅ 属假绿」为误报**（详见 §4 第 7③ 条）。但引导目标派生自「**托盘当前持有的最前一颗**」（`beads-game.ts:2230-2241`），而 v1.22 **定时供料关停、托盘初始为空** ⇒ `guideSlot = −1`、`firstColor = −1` ⇒ `hintRow/hintCol` 恒 `−1` ⇒ **开局引导环永不渲染**；而「取回」又要求玩家先认出错位珠 ⇒ **循环依赖**。<br>· `wrong` 抖动+红描边 ⇒ ✅ 已实装（`view-model.ts:682-708`，含 D1 静态红描边分支）。<br>· `selected` 上浮+圆点 ⇒ 本 r2 未复核（v1.5 未改其规格）。 | ⚠️ **部分落地（`[v1.5-r2]` 由 ✅ 降档）**：`locked`/`wrong` ✅；`empty` 非颜色通道收窄（见 A2b/A3 量化）；`hint` **已实装但 v1.22 下触发前提断裂**（引导链开局即断）；`selected` 未复核。`[v1.5]` 降档登记维持（E4 移除，可恢复路径见 A2b ⑤） |
| A2b | **色觉障碍友好 · 空槽目标色识别**（WXG-T-080 新增；**`[v1.5]` 降档登记**） | empty 态现显示目标色（E1 纯色直填），色觉障碍玩家须在**未填**状态辨识「这个槽要放哪个色」。三级方案：<br>**Basic**：E4 幽灵符号（与 L5 同矢量 path）提供 **10 色 ↔ 10 符号** 的一一映射。<br>**Standard（本 demo 目标）**：Basic 全满足 + E1 色底色相差异在 deuteranopia/protanopia 模拟下仍可区分同关使用的 3–8 色。<br>**Comprehensive**：Standard + 「空槽目标色显示开关」。<br><br>**⛔→⚠️ 降档登记（WXG-T-130，用户签字 2026-09-16）**：用户裁定原文——**「暂时不需要支持色盲玩家，我需要的是珠子和托盘，棋盘都有质感」** ⇒ **E4 幽灵符号移除、E1 由混色改纯色直填**（`systems-index §3.8` 两常量作废标注）。**降级范围**：Basic 层符号映射通道消失——**灰度（黑白）模式下同明度档的空槽互相不可分、且不再有符号辅助定位匹配槽**；色相通道（Standard 层）在彩色显示下维持（10 色色相仍须拉开，归 WXG-T-131 色板兼容项）。**可恢复路径**：E4 绘制逻辑**保留为关闭的开关/死路径（不删码）**，重开零成本（恢复 `EMPTY_GHOST_ALPHA=0.32` + 开关置开即可回到 Basic+Standard）；触发条件 = 用户推翻本裁定（须走 `systems-index §6` 变更）。<br><br>**`[v1.5-r2]` 量化核算（WXG-T-131，替代 v1.5 的定性描述；口径：`palette.ts::luminance` = Rec.601，与代码同函数；不可辨阈值取**本仓自己的判据**：`art-bible §3.1` / `assets-spec §1.8`「背景 ΔL ≤4% = 刻意极低、不可辨」⇒ **10.2/255**）**：<br>**① 规格态 vs 代码态（必须先分清，否则会误判假绿）**：· **代码态（当下事实）**：`src/view/bead-render.ts:200` `GHOST_SYMBOL_SCALE = 0.8`、`:232-240` E4 幽灵符号 emit 分支**仍在执行**；`src/view/palette.ts:203/209` `EMPTY_TINT_MIX = 0.42` / `EMPTY_GHOST_ALPHA = 0.32` **仍在被消费**（`view-model.ts:699`）⇒ **A2b 此前标 ✅ 描述的是代码现状，不是假绿**（T-131 禁改 `src/**`，本 r2 未动代码）。· **规格态（v1.5 已生效）**：`assets-spec §1.2` 明写「**禁画** …L5/**E4** 符号（E4 幽灵符号移除）」、E1 改纯色直填。· ⚠️ **「落码即假绿」窗口（显式声明、不得含糊）**：`drawEmptySocket` 的 E4 分支**一旦被工程单删除或置为关闭**，A2b 的 Basic 档**在同一提交内立即消失**，而本行落地状态列若仍为 ✅ 即成为**假绿**⇒ 落码纪律：**E4 关闭的提交必须同批改本行状态**（已列入 §4 第 8 条）。<br>**② Basic 档丢失后的剩余通道实测**：· **色相**（目标色纯色直填）——对 CVD **定义性失效**，不计入。· **明度**——空槽主读面 = **S2 坑底**（内缩 6% ⇒ 占格面积 `(44/50)² = 77.4%`，为四层中唯一大面积面；S1 为 3px 环、S3/S4 为 2～2.5px 线）。10 色 S2 = `mix(base,#000,0.14)` 的 luma×255 降序与相邻差：奶白 212.4｜柠黄 177.9（Δ34.5）｜活力橙 144.6（Δ33.3）｜草绿 122.9（Δ21.7）｜**丁香紫 113.6（Δ9.3❌）**｜**玫红 103.9（Δ9.7❌）**｜**湖蓝 101.8（Δ2.1❌）**｜**赭棕 97.9（Δ4.0❌）**｜深棕 61.6（Δ36.3）｜炭黑 44.9（Δ16.7）⇒ 单链聚类得 **6 个可分离档**（{奶白}{柠黄}{活力橙}{草绿,丁香紫,玫红,湖蓝,赭棕}{深棕}{炭黑}），**4 色（丁香紫/玫红/湖蓝/赭棕）无独立明度位**、**4 组相邻对 ΔL < 4%**。同法核 S1（−0.30）与 S4（+0.20）：**×0.70 / ×1.20 的等比压缩使相邻差同比收紧**（S1 最小相邻差仅 **1.5**），可分离档同为 **6**、落榜同为该 4 色。· **凹凸形态**（v1.5 新增通道）——只承载「**空 vs 珠**」**1 bit**：同色下坑底 S2 与珠主体 L1 的 ΔL = `0.14 × L_base`（活力橙实测 **23.7/255**，恒可辨）；但**形态不携带色序号**（10 色的坑几何完全同构：S 内缩比、线宽公式、mix 系数全为常量，唯一变量 = base）⇒ **不能区分「哪个色的空槽」**。<br>**③ 实际可达档位（不放宽核算的结论）**：**Basic 消失、Standard 不成立**，实际可达 = **Basic 的弱化形态**。逐条理由：Standard 判据原文要求「Basic 全满足 + 色相差异在 deuteranopia/protanopia 模拟下仍可区分同关使用的 **3–8 色**」——(a)「Basic 全满足」已为假；(b) 明度通道实测只有 **6 档 < 8**（`BEAD_COLOR_MAX = 8`，`systems-index §3.2`），且关卡 JSON 自选色 ⇒ **无法保证**同关 3 色落在 6 个可分离档内（丁香紫/玫红/湖蓝/赭棕 任两色同关即塌）；(c) 形态通道零色序信息。⇒ **v1.5 原句「Standard 层（色相可辨）维持」不得再写**，改为：「**色相通道在彩色显示下对色觉正常玩家维持；对 CVD 玩家，Standard 层不成立**」。（本 r2 **未做 CVD 色相模拟**——仓内无模拟工具、不伪造数据；上述结论**不依赖**色相模拟，仅靠 (a)(b)(c) 三条即已足以否定 Standard。）<br>**④ 判据对象漂移（比降档更根本，需用户裁定）**：v1.22「错位归位」下，玩家的主读取对象**已不是空槽**——开局**满盘**（`swaps` 两两交换构造）、空槽数 = 托盘持珠数 `h ≤ 2×MISPLACED_PAIRS_MAX = 16`，主读取路径变为「**filled 格的底色 vs 珠色**」。而底色在 filled 格**完全不可见**（珠 50px = 格 50px、同 radius r=11 ⇒ 零露出；`grep misplaced src/view/` **零命中** = 代码侧亦无任何错位标记）。⇒ A2b 的判据（「在**未填**状态辨识这个槽要放哪个色」）**已不覆盖 v1.22 核心循环的可读性需求**；本 r2 不自行改判据（属用户裁定域），只登记：**A2b 降档 ≠ 核心循环可读性已解决**，后者是独立的 **P0 缺口**（三个候选方案与图元代价见 §5 待审批项 4）。<br>**⑤ 可恢复条款（用户签字裁定的一部分，非自由裁量）**：T-130 用户裁定原文承诺「E4 绘制**保留为关闭的开关/死路径，重开零成本**」。落点具体化：**开关名** `SOCKET_GHOST_SYMBOL`（布尔，归 `src/config/tuning.ts` 呈现层、**不进 `systems-index §3`**，承 `PLATE_OUTSET`/`GLOW_BAND_ALPHAS`/`SOCKET_*` 判例）；**落点** = `bead-render.ts::drawEmptySocket` 现有 `if (colorIdx !== undefined)` 分支**外再套一层开关判定**（`GHOST_SYMBOL_SCALE`、`emitSymbol` 调用、`EMPTY_GHOST_ALPHA` 引用**全部保留不删**）；**重开成本** = 开关置 `true`（`EMPTY_GHOST_ALPHA = 0.32` 的值仍在 `palette.ts:209`、§3.8 只标作废未删值）⇒ **零几何改动、零新增 hex、零新增图元**（回到旧口径：网格空槽 2→3 层）。⚠️ **口径注**：旧「网格空槽 2 层」把 E4 当 1 图元，而 `symbols.ts` 实测符号命令数随色而异（○★●■▽▲◆ = 1、◐✚ = 2、**♥ = 3**）⇒ 旧值实为 **2～4 命令**（本表图元口径 = 规格层数，详见 A5 ⑧ 口径注）。 | ⚠️ **部分降级（v1.5，用户签字 2026-09-16；`[v1.5-r2]` 量化后收紧）**：原「Basic+Standard 落地」中 **Basic 层（E4 符号映射）降级失效**；❌ **v1.5 原句「Standard 层（色相可辨）维持」不成立**⇒ 实际可达 = **Basic 的弱化形态**（明度 6 档 + 形态 1 bit，4 色无独立明度位，核算见上 ②③）。**代码态下本行仍为 ✅**（E4 未删）、**规格态/落码后为 ⚠️**，两态不得混写（见 ①）。恢复路径见 ⑤。`[v1.3·F5]` 数值争议随 E4 移除失效 |
| A3 | **灰度可辨** | 转黑白后 10 色 + 6 状态 100% 可区分（符号 + 明度 + 纹理保证）。**`[v1.5]` 降档登记**：**范围收窄为 `filled` 态**——转黑白后 **10 色 filled 珠**仍 100% 可区分（符号 + 明度不变，`bead-grid §8-10` 同步范围注记）；**`empty` 空槽在黑白下同明度档不可分**（E4 符号移除 + 纯色底无纹理辅助，降级范围与用户签字见 A2b）；`locked`（斜纹）/`hint`（描边）/`wrong`（红描边）/`selected`（圆点+上浮）各态非颜色通道不受影响。**可恢复路径同 A2b**（E4 开关重开）。<br><br>**`[v1.5-r2]` 量化核算（WXG-T-131；本条为本次降档的实质核心，口径同 A2b ②：Rec.601 luma、阈值 10.2/255）**：<br>**① 原「100% 可区分」的真实承载者 = 符号通道，不是明度**。实测：**base 目标色**（未混色）的 10 色 luma×255 降序中已有 **2 组相邻差 < 4% 阈值**（玫红 120.9 / 湖蓝 118.4 Δ**2.6**❌、湖蓝 / 赭棕 113.6 Δ**4.7**❌）⇒ 单链聚类 **8 档**、落榜 2 色。⇒ **即 v1.5 之前，明度通道单独就不足以分开 10 色**；「100%」靠的是 **10 个互异矢量符号**（`symbols.ts::BEAD_SYMBOLS`）。E4 移除**精确地**抽掉了 empty 态唯一的 10 位离散通道。<br>**② E4 移除后 empty 态灰度可辨性实测**：**S2 坑底**（主读面，占格 77.4%）= **6 档 / 4 色落榜**；**S1 暗缘框**（−0.30）= **6 档 / 4 色落榜**（相邻差最小 **1.5**，比 S2 更紧）；**S4 下受光**（+0.20）= **6 档 / 4 色落榜**。⇒ **结论（照实写、不软化）**：**灰度下无法辨识「哪个色的空槽」**——丁香紫/玫红/湖蓝/赭棕 四色与草绿共同构成一条**相邻差全部 < 4% 的链**（草绿 122.9 → 丁香紫 113.6 → 玫红 103.9 → 湖蓝 101.8 → 赭棕 97.9），链内**任两相邻色不可分**；链两端（草绿 vs 赭棕 Δ25.0）仍可分 ⇒ 不是「5 色全塌」而是「**5 色塌成一条不可局部区分的链**」（该链恰好包含本仓饱和度最高、色相最“有用”的四个色）。<br>**③ 凹凸形态能做什么、不能做什么**：**能**——区分「空 vs 珠」：同色下 S2 与 L1 的 ΔL = `0.14×L_base`（活力橙 **23.7/255**）；且形态差异不止明度：坑有 3px 暗缘框 + 上暗下亮的内缘线（S3 2.5px / S4 2px），珠有上亮下暗的倒角（L2/L3）+ rim（L3b）+ 三层软高光（L4a–L4c）+ 两道投影（L0a/L0b）⇒ 灰度下**纹理/结构仍可辨**，A3 原判据中的「纹理保证」在**空 vs 珠这一维仍然成立**。**不能**——区分「哪个色的空槽」：10 色的坑几何**完全同构**（`SOCKET_INSET_RATIO`、`max(2,S×0.05)`、`max(2,S×0.04)`、mix 系数全为常量，唯一变量 = base）⇒ 形态通道携带的色序信息 = **0 bit**。<br>**④ 新判据文字（替代原「转黑白后 10 色 + 6 状态 100% 可区分」）**：「转黑白后，**`filled` 态 10 色 100% 可区分**（L5 符号 + 明度，`symbols.ts` 10 符号互异）；**`empty` 态：10 色中 6 色有独立明度档，4 色（丁香紫/玫红/湖蓝/赭棕）落入一条相邻差 <4% 的不可分链 ⇒ 空槽目标色在灰度下不保证可辨**；**状态维度：6 态中 5 态可区分**。」⇒ 原口径「10 色 × 6 状态 = 100%」中，「10 色」这一维在 empty 态降到 **6/10**、「6 状态」这一维降到 **5/6**（见下条）。<br>⚠️ **⑤ 第 6 态的缺陷（v1.4 既有、非 v1.5 回归，本 r2 自查补登）**：**`hint` 环（`accent_blue #3D7BF5`，L=118.4）与 `wrong` 环（`danger #E8434A`，L=117.1）的灰度 Δ = 1.2/255 = 0.49%、WCAG **1.00:1** ⇒ 不可辨**。动态下尚可由「呼吸 α 0.5↔1.0@600ms」vs「抖动 ±3px×2 + 单次脉冲」区分，但 **D1（`reduceMotion`）下两者同退为静态描边 ⇒ 该维通道归零**（“这个蓝环是引导还是报错”不可判）。两环规格 v1.5 未改 ⇒ **属 v1.4（及更早）既有缺陷**；**零 hex 修复候选**：(a) 线宽分档（hint 2px / wrong **3px**）、(b) hint 改 **dash 6/4**（承 `tray_slot_dashed` 既有判例）——两者均不新增色值、不动 §3，**待裁定**（§5 待审批项 5）。<br>**⑥ 授权与代价（诚实口径）**：用户已明确裁定「暂时不需要支持色盲玩家」⇒ **本降档是被授权的，不需为它辩护**。代价照实登记：**灰度/单色显示设备、以及色觉障碍玩家，无法从空槽读出目标色**；在 v1.22 核心循环下其后果是**无法判断手中珠该落哪一格**（除色相外无任何通道承载「这一格要什么色」）⇒ 该类玩家的**核心循环不可玩**，不只是「体验降级」。另：**色觉正常玩家也读不到 filled 格的底色**（见 A2b ④）⇒ 那一条**不在本降档授权范围内**，属独立 P0 缺口。 | ⚠️ **部分降级（v1.5，用户签字 2026-09-16；`[v1.5-r2]` 量化后收紧）**：`filled` 态维持 ✅（10 符号互异）；`empty` 态 **10 色 → 6 档**（4 色不可分，量化见 ②）；❌ **原「6 状态 100%」口径不再成立**（v1.5 已登）且本 r2 进一步查明**第 6 态缺陷在 v1.4 已存在**（`hint` 环 vs `wrong` 环灰度 Δ1.2/255、WCAG 1.00:1，D1 下不可辨，见 ⑤）⇒ 现为 **5/6**。恢复路径同 A2b ⑤ |
| A4 | **色觉障碍友好 · 道具图标** | 3 个道具**以形状为唯一识别**（魔法棒/扫帚/磁铁）+ 文字标签并列，不靠颜色 | ✅ **落地**（WXG-T-062 补齐）：三图标按 `assets-spec §1.4` 程序化绘制（形状互异、不靠颜色）**且**卡下方 28px 标签并列（「区域消除 / 槽位清空 / 随机消除」）。此前标 ✅ 属**假绿**（实现只有空白卡）；WXG-T-060 补形状、WXG-T-062 补标签并裁定 §1.4 与 §3.1 的几何冲突（卡高 150 → 116，带位不动）。`[v1.3·F7④]` 标签墨色由 `textDim`（≈3.7:1）改 `text_secondary`（4.75:1）达标 |
| A5 | **丙案新增层次不破坏三重编码**（v1.3 回查新增；`[v1.4]` 追加动效核算） | 暖光 band / 容器板 / 背景层次均位于珠子**下层**且低饱和（band `glow_warm` 累计 α≤0.15、背景 ΔL≤4%）；珠色/符号/明度三通道参数**零改动**；软高光叠层在符号**之下**绘制，10 色符号墨对合成面对比经 `bead-render.test` 复算仍 ≥3:1（B2 地板）。<br>`[v1.4]` **动画期逐条核算**（几何参数见 `art-bible §7.3` / `assets-spec §1.6.1–§1.6.7`）：① **G1 落座**峰值 scale 1.06 ⇒ 珠宽 53px ⇒ 半宽 **26.5**，与静止邻珠（pitch 52、半宽 25）合计 `26.5 + 25 = **51.5 < 52**` ⇒ 间隙 **0.5px、零重叠**（`[r2]` 旧写法「27 + 25 = 52 ⇒ 恰好相切」系把半宽 26.5 上取整为 27 的保守式，结论同为零重叠，但与 ④ 的**精确算法不一致** ⇒ 统一为精确值）；符号 `stroke = max(2, 53×3/64) = 2.48px` ≥ 2px 地板。② **G2 溶解**三通道（scale / α / dy）中只有 α 衰减，**珠色 HEX 与符号矢量零改动**（不换色 ⇒ 不制造「靠变色识别消除」的色通道依赖）；最坏 scale 0.60 ⇒ 托盘珠 44×0.60 = 26.4px ⇒ `stroke = max(2, 1.24) = **2.0px**`（钳制生效）；合规依据 = `assets-spec §1.1` 最小特征约束已声明「**50% 缩放下仍可辨**」，而 v1.4 全部动画 scale ∈ [0.60, 1.08]、最坏 **0.60 > 0.50** ⇒ **落在既有约束覆盖内，无需新增约束**。③ **G3 扫光**为纯白包装层（单层 α≤0.14、三层累计 ≤0.27），**不承载任何编码**、不改变珠色/符号。④ **G4 庆祝波浪**峰值 scale 1.08 ⇒ 珠宽 54px，同列相邻行同相位 ⇒ 上下各叠压 **1px**，仅及最外倒角带（L2/L3 位于内缘 3.1–3.9px），**符号区（中心 20px）零遮挡**；`stroke = max(2, 54×3/64) = 2.53px`。⑤ **G5**（**案 B 定案**，裁定 2；案 A 已废）scale 1.015 ⇒ 珠宽 50.75px < pitch 52 ⇒ **零重叠**（案 B 为**全场同相缩放** ⇒ 相对间隙同比放大，几何结论与选案无关；a11y 口径不变）。⑥ **G6 彩带**为纯装饰 polygon，**零编码承载**；前景枚在 y∈[447,787]（按钮行 + 金色缎带）**跳过绘制** ⇒ 不遮 UI 文字与热区（C1）。⑦ **G7 轻压**谷值 scale 0.96 ⇒ **只有被点那一颗**珠宽 48px，与**静止**邻珠（50px）的间隙 = `52 − 48/2 − 50/2 = 52 − 24 − 25 =` **3px**（静息间隙 = `52−25−25 = 2px` ⇒ **变大 1px**）⇒ 零重叠；❌ `[r2]` 旧值 **4px 作废**——那是「两颗都缩」的算式（`52−48`），与本卡「单颗形变」不符；`stroke = max(2, 48×3/64) = 2.25px`；D1 退化环用 `slot_border #D8D5E6` 1px ⇒ **不占** `accent_blue` 环状提示配额（§3.5 F6），也不与 `wrong` 红环 / `hint` 蓝环混淆。<br>`[v1.5-r2]` **追加两条（WXG-T-131）**：<br>⑧ **G1 的 empty 侧联动（S1 露缘环）几何核算**（详卡 `assets-spec §1.6.1`）：v1.5 后落座格在动画期需**条件绘制 S1 暗缘框**（= §1.2 四层中的第 1 层，**非新元素、非新 hex、非新系数**）。**可见窗**：压下段 `scale = 1.06 − 0.10·easeOut(t/40)`，`scale = 1.00 ⇔ easeOut(u) = 0.6 ⇔ 1−(1−u)² = 0.6 ⇔ u = 1−√0.4 = 0.3675 ⇔ t = 0.3675×40 =` **14.70ms**；回升段 `scale = 0.96+0.04·easeOut((t−40)/80) < 1.00` 对全部 `t < 120` ⇒ 露缘窗 = `t ∈ [14.70, 120)ms`（占 **87.8%**）。**露缘宽** `w(t) = 25×(1−scale(t))`：t=14.70 ⇒ 0、**t=40 ⇒ 1.00px（峰）**、t=120 ⇒ 0。**重叠复核**：S1 = 格全尺寸 50px、**不随 scale 变化**（它是坑、不是珠）⇒ 与邻格 S1 间隙 = `BEAD_PITCH 52 − 50 =` **2px**、**恒不重叠**（比 ① 的珠峰 53px ⇒ 0.5px 更宽裕）。**最小特征（诚实登记，不套用兵底）**：w 峰 **1.00px < 2px 地板** ⇒ ⚠️ **本环不适用 §1.1 `stroke(ratio) = max(2,·)` 兜底**——它是「被珠体遮后**露出的面**」而非「笔画」，钳到 2px 会画出比坑缘更宽的环、几何失真 ⇒ **1px 露缘在低分屏/50% 缩放下不可辨**；其作用为**质感咬合读数（AO）**、**不承载任何编码信息** ⇒ **A5 三通道不因它增减**（色/符号/明度全部来自珠体十层，S1 只是地色）。**图元口径注（防误读）**：本表 ①–⑨ 与 `assets-spec §1.8` 的「图元」一律 = **规格层数**、**不是 builder 命令数**——`symbols.ts` 实测 ♥ = 3 命令、◐/✚ = 2、其余 7 符 = 1 ⇒ filled 珠的真实命令数为 **12～14**（规格记「十层」）、旧「网格空槽 2 层」的真实命令数为 **2～4** ⇒ **命令级最坏值高于层级值**，层级口径是**偏乐观**的口径（历代一致，本 r2 只显式声明、不改口径以免与 §1.8 全表断裂）。<br>⑨ **取回入槽 = 跳格位移，①–⑦ 的前提不覆盖（登记，待补卡）**：`ux-spec §5` v1.4 新增三行「错位珠选中 100」「**取回入槽 200+120**」「解环器归位 200+120/颗」在 `assets-spec §1.6` **无对应参数卡**（§1.6.1–§1.6.7 只覆盖 G1–G7）。其中「取回入槽」视觉列写明「错位珠**从格位飞至目标槽**（直线插值 + 轻微弧度）」⇒ 这是**跳格位移**，而 ①–⑦ 全部建立在「**原地缩放、零位移**」前提上 ⇒ 飞珠**必然与途经格重叠**（① 的「间隙 0.5px」结论**不得引用于飞珠**）。**遮挡时长核算**（不靠未定值）：设飞行距离 D px、时长 200ms ⇒ 速度 `D/200` px/ms，遮住一个 50px 格的时长 = `50×200/D = 10000/D` ms。两端：D = 675px（网格顶行 y≈1095 → 托盘首行槽心 y≈420，均取自 §3.1/§3.4 带值）⇒ **14.8ms ≈ 1 帧**（途经 ≈11 行网格，每行仅遮 1 帧）；D = 85px（网格底行 y≈505 → 托盘 y≈420）⇒ **117.6ms ≈ 7 帧**，但此距离下途经的是**托盘空槽**（无符号、无编码承载）⇒ 两端均**不构成编码通道丢失**（A6 同理：取回事实由 `tray:stored` 事件 + 托盘珠数变化承载）。⚠️ **但本条不给最终结论**：「**轻微弧度**」的**弧高未定值** ⇒ 弧高一旦使飞珠偏离格心连线 >1px，上述「途经托盘空槽」的论据需重算 ⇒ 需 ux 域先冻结弧高（或明确「弧高 = 0、纯直线」），见 §4 第 7② 条。 | ✅ **回查通过（v1.3）**；`[v1.4]` ✅ **纸面几何核算通过**（⚠️ 真机未验，见 §2 v1.4 复核 ③）；`[v1.4-r2]` ①/⑤/⑦ 已按缺陷单修正（结论均未变）；`[v1.5-r2]` ⑧ **通过**（S1 露缘环恒不重叠、不承载编码）、⑨ **暂不给结论**（待 ux 域冻结弧高） |
| A6 | **动画不承载独占信息**（`[v1.4]` 新增，**WXG-T-128**） | D1 敢对 G3/G4/G6 下「**整条关停**」的合法性前提 = 关停后信息仍有**非动画通道**可达。逐条：**G1 落座** ⇒ 关停后仍有格子状态变更本身（`empty`→`filled`，色 + 符号 + 凸起形态三通道）；**G2 溶解** ⇒ 仍有槽位变 `free`（槽数 / 剩余计数）；**G3 扫光** ⇒ 本身零信息量，道具生效的事实由 G2 的 α 淡出 + 槽位变化承载；**G4 庆祝波浪** ⇒ 过关事实由结算面板 + 星级 + 音效承载；**G6 彩带** ⇒ 纯装饰、零信息量；**G7 轻压** ⇒ D1 退为 1px `slot_border` 静态描边环，「这一格点不动」的事实仍在（由动效退为静态）；`[r2·裁定 3]` 且 G7 现有**音频通道** `sfx_denied`（第 20 剪辑，极轻闷「哒」）⇒ D1 关停视觉形变后，信息仍由**音频 + 静态描边环**两条非动画通道承载，A6 结论**更强**（音频不属动效通道，不受 `reduceMotion` 影响）。⇒ 六条动画**无一为信息的唯一通道**，A1–A3 三重编码不因动画关停而减档。**钉住的纪律**：后续任何新动效若要承载状态信息，**必须同时给出非动画通道**，否则不得进 D1 关停清单（只能进保留清单）。 | ✅ **落地（v1.4 规格层）**——各条退化口径已写入 D1 关停清单；实装复验随 G1–G7 落码 |
| B1 | **对比度 · 文字** | 正文对背景 ≥ 4.5:1。`text_primary #2A2E43` on 白 ≈ **13.4:1**、on 冷底 ≈ **11.2:1** ✅；`text_secondary #6E7288` on 白 ≈ **4.75:1** → 仅用于**白底 ≥28px**；冷底小字一律 `text_primary`。**v1.3 修订（F7④ 假绿根治）**：废弃 `textDim #8B8578`（on 白 ≈3.7:1）与「dim 仅 ≥32px」旧口径——28px 道具标签/次数改 `text_secondary` 达标，22px HUD 小字升 28px 改 `text_primary`；规格自定规则与实装重新一致 | ✅ **落地（v1.3 回查修正）** |
| B2 | **对比度 · 符号/图形** | 符号对珠面 ≥ 3:1（亮珠用 `mix(base,#000,55%)`，暗珠用 `#FFF@90%`，见 `assets-spec.md` §1.1 L5） | ✅ **落地** |
| B3 | **倒计时不仅靠颜色** | 最后 10s：变红**同时**数字脉冲 + 图标脉冲（双重），色盲下靠“脉冲”可辨 | ✅ **落地（WXG-T-087 GAP-10 三通道）**——`view-model.ts` drawHud（L418–424）：数字切 `palette.danger` + 时钟图标 `textDim→danger` 切色 + `dangerAlpha` 1000ms 三角波 α 脉冲；不靠色相即可靠脉冲辨识。D1 减弱动效下脉冲退静态（α=1），色/图标双通道切换不受影响（仍不靠颜色）。此前「仅色变、脉冲未实现移交 T-086」注由 WXG-T-093 回写 |
| C1 | **触控目标尺寸** | 交互元素 ≥ 44pt → **≥ 88 × 88 设计 px**。设置齿轮热区 88×88、道具卡 176×150、`btn_expand` 132×48 → **⚠️ 48 < 88，高度不足，须扩热区至 132×88（视觉不变、热区扩大）** | ✅ **落地（含 1 处修正，已实装）**——修正项于 WXG-T-097/BD-15 落码：热区 132×88（`tuning.ts::expandButtonLayout()` 与渲染同源） |
| C2 | **元素间距** | 相邻可点元素中心距 ≥ 96 设计 px；道具卡间距 = `POWERUP_CARD_GAP` **60px**（卡宽 176×3 + 间距 60×2 = 648 ≤ 750 ✓；文档原记 32px 系陈旧值，与 `tuning.ts` 实现不符，BD-26，WXG-T-091 回写） | ✅ **落地** |
| D1 | **动效可减弱** | 设置提供「减弱动效」开关（暂停面板行3，`settings.reduceMotion`），**关停清单**：错误抖动（位移归零 + 红描边改静态）、告急脉冲（α 恒 1 静态红字）、hint 呼吸（退为静态蓝描边、**保留**）、**满槽告警呼吸**（退为静态 danger 描边、**保留**，WXG-T-097/BD-10）、结算/通关星弹跳缩放（退为静态高亮）；`[v1.4]` **新增六条**（参数见 `art-bible §7.3` / `assets-spec §1.6`）——**G1 落座回弹**（关 scale + L0b 投影联动，**保留** L0a 接触阴影 α 单峰 0.12→0.18→0.12、振幅 ≤0.06；与「hint 呼吸退静态描边」同族＝**去形变、留接触**）、**G2 消除溶解**（关 scale + dy 下沉，**保留** α 线性淡出 1→0）、**G3 全屏扫光**（**整条关停**）、**G4 过关庆祝波浪**（**整条关停**）、**G6 结算彩带**（**整条关停**）、**G7 不可填格轻压**（关 scale，**退为被点格 1px `slot_border #D8D5E6` 静态描边环 α=1**，0 往复）；**G5 连击 Lv2** **案 B 定案**（`[r2·裁定 2]`：给 `RenderModelBuilder` 加全局变换通道，跨 `packages/framework` 域、另立工程单；**案 A 已废且不作降级预案**）⇒ **D1 口径不变：整条关停**（选案不改 a11y 结论）；**保留清单**：珠子颜色/符号变化、面板淡入淡出、hint 静态描边、满槽告警静态描边、`[v1.4]` G1 接触阴影 α 单峰、G2 α 淡出、G7 静态描边环 | ✅ **落地（WXG-T-088，满槽通道随 WXG-T-097/BD-10 接入）**——开关接通且逐通道生效。`[v1.4·G9]` **口径变更（N/A → 关停项）**：此前标「清单中『落座回弹 / 消除溶解缩放』本作无对应动画通道可关（落子/消除为瞬时、无 tween），属 **N/A** 非假绿」；v1.4 规格已为 G1（`vfx_fill_pop` 120ms scale 1.06→0.96→1.00 + L0a/L0b 联动）与 G2（`vfx_clear_dissolve` 200ms scale→0.60 + α→0 + dy）定义了动画通道，**该 N/A 的前提自动失效** ⇒ 现改为上述**关停项**（G3/G4/G6 整条关停、G1/G2/G7 退化保留）。⚠️ **落码时须同步接通 `reduceMotion` 分支**，否则本行将由「N/A 诚实」转为「✅ 假绿」——已列入 `assets-spec §6` 验收项。见 §2 v1.4 复核 ① |
| D2 | **闪烁安全（光敏）** | 任意闪烁 < 3Hz；`wrong` 描边闪 ≤2 次/秒；**禁止**全屏白闪。`[v1.4]` **新动效逐条核算**（口径承 BD-29：闪烁 = 同一区域不透明度**往复**变化；单次淡入→淡出计 1 峰 **0 往复**）：G1 120ms scale + L0a α **单峰**（0 往复）、G2 200ms α **单调递减**（0 往复）、G3 400ms **单次通过**（0 往复）、G4 800ms scale **单峰**（0 往复）、G7 120ms scale **单谷**（0 往复）⇒ 五条**零往复、零周期量**；G5 为 scale 脉冲（1.00→1.015→1.00，每次连击一次、**非周期**；`[r2·裁定 2]` **案 B 定案**后走框架全局变换，**脉冲形态与频率不变**）⇒ 折算频率 ≪ 3Hz。**唯一往复量 = G6 彩带旋转**：1.25 转 / 800ms ⇒ **1.25Hz < 3Hz** ✓，且单枚可见面积 6×14 = 84px² ≈ 占屏 **0.008%**。**「全屏白闪」专项核算（G3）**：广层 210×1214 = 25.5 万 px² ≈ 玩法区 **28%** < 30%，三层累计 α ≤ **0.27** < 结算 scrim 的 0.5 一档，且单次通过 0 往复 ⇒ **不构成全屏白闪**。**§3.8 屏震令核算**：G4 / G5 **零位移**（纯 scale + dy ≤3px 单峰）⇒ 不触「真屏震不使用」；G7 重启门 250ms（同格 ≤4 次/秒）但**零 α 往复、零色变** ⇒ 不构成闪烁，且 G7 **不属「错误反馈」**（`verdict = ignored` 走表现层：零事件、零扣次、零状态写）⇒ **不占** §3.8「错误反馈 ≤2 次/秒」预算；`[r2·裁定 3]` **G7 新增音频 `sfx_denied` 与视觉同门**——音频侧 `minInterval` **0.25s** = 视觉重启门 250ms 的换算（**不新造数值**）⇒ **不新增任何视觉往复量**，D2 结论一字不变（音频不属光敏通道）。 | ✅ **落地**；`[v1.4]` ✅ **纸面核算通过**（真机未验） |
| E1 | **文字可读性** | 最小 28 设计 px（≈14pt），正文 32px，倒计时 44px；系统字体随系统缩放。**v1.3 回查新增（F7⑤）**：实装 `hudSmall=22px` 曾低于本 floor（与 B1 同源假绿），v1.3 升 28px；`largeText` 映射同步 28→35 | ✅ **落地（v1.3 修正登记）** |
| E2 | **文本缩放开关** | 「大字号」开关（暂停面板行3，`settings.largeText`）：仅放大**正文/说明类**字号（`sub` 28→35、`hudSmall` 28→35，v1.3 随 F7⑤ 同步）；倒计时数字/标题/按钮字号不变 | ✅ **落地（基础版，WXG-T-088；v1.3 映射同步）**——弹窗/按钮排版自适应延后至 v1.1（§5.2 用户拍板基础版） |
| F1 | **无时间压力 / 可暂停** | ⚠️ 本作**有关卡倒计时**（用户已拍板），无法消除时间压力 → 提供**暂停即冻结倒计时** + 告急提示充分（B3） | ✅ **落地（以暂停冻结补偿）** |
| F2 | **输入精度要求** | 点按填色（无拖拽精度要求）；空位命中区域 = 珠位外扩 8px，降低误触 | ✅ **落地** |
| G1 | **字幕 / 语音替代** | demo 无语音内容 | N/A |
| G2 | **屏幕阅读器** | 微信小游戏平台不支持 | ⏸ 延后（平台限制） |
| H1 | **符号显示开关** | 允许熟练玩家关闭珠面符号（纯色模式）——三重编码默认开启，此项为**可选减负** | ⏸ 延后（待用户拍板，见 §5） |

> **落地状态复核（2026-09-14，WXG-T-052）—— 三处 ✅ 曾与实现不符，现已补齐并留下偏差登记。**
>
> **A1 / A3 / B2 复核**：此前 `view-model.ts` 自述「the 10 vector symbols … deliberately deferred」，
> `view/symbols.ts` 与 `view/bead-render.ts` **并不存在** —— 珠子实际只画了**颜色 + 明度**两重通道，
> 而符号是三重编码中**唯一的非颜色通道**。即：A1「三重编码」实为两重、A3「灰度下 100% 可辨」在只有
> 明度 5 档时不成立、B2「符号对珠面对比度」当时**无对象**。
> 现两文件已落地（六层参数卡 L0–L5 + 10 符号矢量 path），并由 `tests/symbols.test.ts`、
> `tests/bead-render.test.ts` 断言：10 色 ↔ 10 符号一一映射、每颗满珠发射 L5 符号、
> 线宽 ≥ 2px、**10 色符号墨对珠面对比度全部 ≥ 3:1**。
>
> **偏差 1 · B2 与 `assets-spec.md` §1.1 L5 的阈值冲突（已处置）**：按 L5 的「亮度 > 0.6 → 深色墨」，
> 草绿 `#3FBF6B` 取 `#FFF@90%` 仅约 **2.4:1**，低于 B2 的 3:1 地板。处置：**以 B2 为硬地板**，
> 该色翻转为深色墨（约 3.7:1）。10 色中**仅此一色**需要翻转，测试中已显式钉住这条例外。
>
> **偏差 2 · `locked` 主体混色不可实现（✅ 已正式追认，WXG-T-080）**：`assets-spec.md` §1.2 原规定 locked 主体为
> `mix(base, #B9B4CC, 0.60)`，但 `x`/`.` 两格在 `entities/grid.ts` 中 `colorIdx = 0`，**没有 base 可混**。
> 现按 `palette.locked` 平涂（无高光、无符号；非颜色通道由 45° 斜纹承担）。
> **处置**：`assets-spec.md` §1.2 `locked` 行已修订为正式条文（`palette.locked` 平涂 + X 斜纹），偏差消除。
>
> **偏差 3 · locked 斜纹密度低于规格（登记待对齐）**：§1.2 要求「45° 斜纹，间距 10」，现实现为两条
> 对角 X 线。可辨性不受影响（非颜色通道仍在），但密度低于规格。
>
> **D1/E2 开关落地（2026-09-14，WXG-T-088）—— 第三次假绿根治**。此前 D1 标 ✅ 但**根本没有开关**
> （settings 无字段、面板无按钮、view 不消费）；E2 标 🔶 同理。现：
> ① `save-schema` 落 `reduceMotion`/`largeText`（存档 v1→v2，旧档逐字段降级不炸）；② 暂停面板行3
> 常驻两枚副按钮（`toggle-reduce-motion`/`toggle-large-text`，与去冲刺同排，面板维持 560×480）；
> ③ view 经 snapshot 消费——`reduceMotion` 让告急脉冲/hint 呼吸/错误抖动/星弹跳全部退为静态，
> `largeText` 放大正文/说明类字号。**诚实边界**：D1 清单里的「落座回弹 / 消除溶解缩放」本作并无
> 对应动画（beads 落子与消除为瞬时态，无回弹/溶解 tween），故无可关项，已在上表标 N/A 而非假称已抑制。
>
> **v1.3 丙案回查（2026-09-16）—— F7 a11y 假绿根治 + 新色板/新层次复核**：
> ① **假绿根源**：B1 旧口径「dim 仅 ≥32px」被实装违反（28px 道具标签用 `textDim` ≈3.7:1；22px HUD 小字用 dim 且低于 E1 的 28px floor）。处置：**改色不升号**——标签/次数 → `text_secondary`（4.75:1）；HUD 小字 → 28px `text_primary`（11.2:1）；B1/E1 口径与实装重新对齐，假绿消除。不升 32px 是因 `POWERUP_BAND` 高 152 冻结（116+4+32=152 零余量）。
> ② **新色板对比复核**：`text_primary` on 冷底 11.2:1 / on 白板 13.4:1；`text_secondary` on 白板 4.75:1（on 冷底 3.99:1 → 故冷底禁用，规则入 B1）；`ad_badge`/主按钮深藏青配白 ▶/白字 13.4:1。全部 ≥4.5:1。
> ③ **三重编码复核**：见 A5；珠色十色 HEX 与符号映射零改动（§3.2 真源不动）。
> ④ **新增动效**：丙案 band/容器板/背景层次均为**静态**叠层，无新增闪烁/动效通道，D1/D2 不受影响。
>
> **v1.4 动态质感章回查（2026-09-16，**WXG-T-128**）—— G9 连带 + 六条新动效复核**：
> ① **G9：D1「落座回弹 / 消除溶解缩放」由 N/A 改为关停项（并显式推翻上文 WXG-T-088 段的「诚实边界」）**。上文写「D1 清单里的『落座回弹 / 消除溶解缩放』本作并无对应动画（beads 落子与消除为瞬时态，无回弹/溶解 tween），故无可关项，已在上表标 N/A 而非假称已抑制」——该口径在 **v1.3 时点诚实**（当时确无通道），但 v1.4 规格已为 G1（`vfx_fill_pop`）与 G2（`vfx_clear_dissolve`）定义了通道，**该 N/A 的前提自动失效**。若不回写，则 G1/G2 落码后 D1 行会呈现「✅ 落地 + 清单里有项 + 实际无关停分支」的**新假绿**（与 B1/E1、A4、D1/E2 三次假绿同型：**规格自定的豁免缺失效条件**）。处置：D1 关停清单补六条（G1/G2 退化保留、G3/G4/G6 整条关停、G7 退静态描边环），并在落地状态列写明「落码时须同步接通 `reduceMotion` 分支」，同时列入 `assets-spec §6` 验收项。**上文旧「诚实边界」段保留不删**（留作历史时点记录），以本条为准。
> ② **D2 复算**：六条动画**零周期量、零 α 往复**（G1 单峰 / G2 单调递减 / G3 单次通过 / G4 单峰 / G7 单谷）；唯一往复量 = **G6 彩带旋转 1.25Hz < 3Hz**（单枚 84px² ≈ 占屏 0.008%）。G3「全屏白闪」专项：广层覆盖玩法区 28% < 30%、三层累计 α ≤0.27 < scrim 0.5、单次通过 ⇒ **不构成白闪**。G4/G5 **零位移** ⇒ 不触 `systems-index §3.8`「真屏震不使用」；G7 重启门 250ms（同格 ≤4 次/秒）但零 α 往复、零色变，且 **G7 不属「错误反馈」**（`verdict = ignored` 走表现层：零事件、零扣次、零状态写）⇒ **不占** §3.8「错误反馈 ≤2 次/秒」预算。
> ③ **A5 复算**：见 A5 行 ①–⑦。**诚实口径：全部为纸面几何核算**（基于 pitch 52 / BEAD 50 / 托盘珠 44 / `SYMBOL_REF_SIZE 64` / `stroke(ratio) = max(2, size×ratio)` 等**代码真值**推导），**真机截图未验**（Cocos 构建阻塞，ADR-0009 P2）。其中 G4 峰值 54px 造成同列相邻行上下各叠压 1px 是**唯一的重叠项**，经核算仅及最外倒角带、符号区（中心 20px）零遮挡；若真机观感判定叠压过强，回退阀 = `WAVE_SCALE_PEAK` 1.08→1.05（**属呈现层常量、非冻结值**，可由主理人直改）。
> ④ **A6 新增**：六条动画**无一为信息的唯一通道**（逐条论证见 A6 行）。这是 D1 敢对 G3/G4/G6 下「整条关停」的**合法性前提**——若无 A6，「关停」就等于「信息丢失」，D1 会从可访问性项退化为功能缺陷。A6 同时钉住一条纪律：**后续任何新动效若要承载状态信息，必须同时给出非动画通道**，否则不得进 D1 关停清单（只能进保留清单）。
> ⑤ **G7 裁定变更的跨域连带（`[v1.4-r2]` 已由「本文未改，仅登记」升为 **已闭合**）**：用户已拍板 G7「加极轻非惩罚反馈」，此前与 `ux-spec §4` 现行矩阵行「点锁定/已填格 → 静默忽略（**零反馈**）」冲突。连带 **4 处规格**——`design/ux/ux-spec.md §4`（矩阵行）、`design/gdd/**/input-control.md §2.4`、`bead-grid.md §6`、`bead-grid.md §8-4`（判据「零事件、零反馈」→「**零事件**」）——**已由主理人于 WXG-T-128 串行落笔完毕**（美术侧零改动、仅引用其锚点）⇒ **本条闭合，不再中转**。`[r2·裁定 3]` **追加事实**：用户同时拍板 G7 **加音效** `sfx_denied`（**新增第 20 个音频剪辑**，非零音效、非复用 `sfx_place` 降 volume）⇒ 原「零音效」口径**作废**；`ux-spec §5` 已增行「**不可填格轻压**」（音效列已写「极轻闷「哒」」）⇒ **「§5 未列的音效一律不发明」纪律已满足**。落码硬约束（A05-24 活测试 ⇒ 五项同批提交）登记于 `assets-spec §1.6.7`，解除条件见 `audio-events §5` Q-A05-5。
> ⑥ **G5 **案 B 定案**（`[v1.4-r2·裁定 2]`，原「两案并列」结构作废）**：用户 2026-09-16 拍板采**案 B**——给 `packages/framework` 的 `RenderModelBuilder` 加**全局变换通道**（`setTransform({ scale, anchorX, anchorY })` 一类），**跨 framework 域、另立工程单**；**案 A（不改框架、全场 `filled` 珠面同相 scale 脉冲）作废、且不作降级预案保留**（若案 B 工程单被驳，Lv2 维持现状不呈现）。**a11y 口径一字不变**：D1 **整条关停**、D2 单往复非周期、A5 零重叠（两案口径本已一致 ⇒ 选案不改本文件结论，见 A5 ⑤ / D1 / D2）。⚠️ **`art-bible §7.1` Lv2 行「整屏 scale 1.015」措辞不动**——上一轮本条曾写「**若采案 A，须同步改 §7.1 Lv2 措辞**（『整屏』→『全场珠面齐脉冲』）」，该前提随案 A 作废 ⇒ **改写为：案 A 已废 ⇒ §7.1 Lv2 措辞不动**（案 B 下规格与实现一致，「写整屏做珠面」的**假绿种子自动消解**）。**案 B 三条未验风险（诚实登记，不得写成已解决）**：① 跨 `packages/framework` 域改动需**全矩阵回归**（breakout + beads 两游戏视图）；② **`FIXED_WIDTH` 下整屏放大 1.015 可能露黑边——未验证**，属**落码前置验证项**；③ 视图侧 `_commands` 为 `private readonly`（WXG-T-074 已登记）是本案要解的**根因**。另新增一条**真机观察项**（a11y 相关）：整屏缩放可能使 **HUD 文字与 1px 描边产生亚像素抖动**（影响 E1/B1 的真机观感，纸面无法判定，待真机）。
> ⑦ **`[v1.4-r2]` 自查：同型残留清理（本轮新增复核条）**：① **A5 ① 的取整写法**——「27 + 25 = 52 ⇒ 恰好相切」把半宽 26.5 上取整为 27，与 A5 ④（G4：54px ⇒ `27+27 = 54 > 52` ⇒ 上下各叠压 1px）的**精确算法不一致** ⇒ 已统一为 `26.5 + 25 = 51.5 < 52` ⇒ 间隙 **0.5px**、零重叠（**结论未变**，仅口径严谨化；同型三处已同批改：本文 A5 ① / `art-bible §7.3.1` / `assets-spec §1.6.1`）。② **A5 ⑦ 的 4px** 与 G7「单颗形变」前提矛盾（那是「两颗都缩」的算式）⇒ 改 **3px**（同批改 `art-bible §7.3.7` / `assets-spec §1.6.7`）。③ **全文 `WXG-T-127` 4 处 → `WXG-T-128`**（台账并发改号，127 已改派程基岩工程单）⇒ 追溯链修复。④ **复核「把线性 α 当 easeIn 反解」的同型错误**：本文**无**时间反解算式（阈值时间只出现在 `art-bible §7.4` / `assets-spec §1.6.2·§1.6.9`，三处 126ms 已全改 **80ms**），本文**无残留**。

---

## 3. 落地范围小结

**本 demo 落地（17 项）**：A1 A2 A2b A3 A4 **A5** **A6** B1 B2 B3 C2 D1 D2 E1 E2（基础版）F1 F2

> `[v1.4]` A6 为 **WXG-T-128** 新增（动画不承载独占信息），**规格层落地**；实装复验随 G1–G7 落码。

> `[v1.4]` D1 由「✅ 落地 + 部分 N/A」改为「✅ 落地 + 六条新关停项**待落码接通**」——**落码前不得复标 ✅ 全绿**，见 §2 v1.4 复核 ①。本轮为**零代码变更**，上述六条均为规格承诺而非实装事实。

> D1 / E2 开关随 WXG-T-088 真实接通（此前 D1 标 ✅、E2 标 🔶 均无开关实现，属假绿；见 §2 复核）。

> A2b 为 WXG-T-080 新增（空槽目标色识别），随 T-085 落码实现。
>
> **`[v1.5]` A2b/A3 降档登记**（WXG-T-130，用户签字 2026-09-16）：两项由 ✅ 改 **⚠️ 部分降级**——E4 幽灵符号移除 ⇒ 黑白模式下同明度档空槽不可分；**可恢复路径 = E4 绘制保留为关闭开关/死路径，重开零成本**（详见 §2 A2b/A3 行）。落地清单中的 A2b/A3 字母保留（特性仍被跟踪），但不计为全绿。

> A4 的转真路径（留档，防再次假绿）：WXG-T-060 补三图标形状识别 → WXG-T-062 补「卡下方 28px 文字标签并列」并裁定 `assets-spec §1.4`（卡 176×150）与 `systems-index §3.1`（`POWERUP_BAND` 高 152）的几何冲突（改卡高 150→116，带位与底部留白不动）。

**落地含修正（1 项，已实装）**：C1 —— `btn_expand` 视觉 48px 高但**热区扩至 88px 高**（132×88），视觉不变。WXG-T-097/BD-15 落码：为腾出不与槽热区重叠的 88 高净空，`systems-index` 升 **v1.20**（`TRAY_BAND` 上沿 420→450、托盘面板改贴带上沿）；玩法数值与 `PUZZLE_BAND` / 网格布局**零改动**。

**部分落地**：**无**——B3 脉冲已随 WXG-T-087（GAP-10 三通道）落地，状态行于 WXG-T-093 回写；复验以 T-092 G4 探针重跑为准。

**延后（2 项）**：G2（屏幕阅读器，平台限制）、H1（符号显示开关，待用户拍板）

---

## 4. 与 UX 规格的接口（`[待 ux-spec 对齐]`）

UX 规格产出后须满足/引用：

1. 设置页含**「减弱动效」**开关（D1，关停/保留清单见 `assets-spec.md` §1.2/§1.6 的动效项）。
2. 可点元素 ≥ 88×88、间距 ≥ 96px（C1、C2）。
3. 倒计时告急须"变色 + 脉冲"双通道（B3）。
4. 填色命中区域外扩 8px（F2）。
5. 暂停必须**冻结倒计时**（F1）。
6. `[v1.4 / v1.4-r2]` **G7 不可填格反馈（`[r2]` 已由「中转登记」升为 **已落地**）**：`ux-spec §4` 矩阵行已由主理人改为「**极轻非惩罚反馈**」；`ux-spec §5` 已**增行**「**不可填格轻压** ｜ **120**（复用「珠子落座」行值）｜ 被点格 scale 1.00→0.96→1.00，零位移 ｜ **极轻闷「哒」**」⇒ **该毫秒已「有源」、原「未决项 3」清除**（裁定 4）。`[r2·裁定 3]` **音效口径变更**：原本文写「**零音效**」⇒ **作废**，现为 `sfx_denied`（**新增第 20 个音频剪辑**，须原子落码，约束见 `assets-spec §1.6.7`）。视觉规格 = `art-bible §7.3.7` / `assets-spec §1.6.7`；可访问性口径 = 本文 D1（退静态描边环）/ D2（零往复；音频与视觉**同门** 0.25s）/ A5 ⑦（间隙 **3px**、不占 `accent_blue` 配额）/ A6（信息仍有非动画通道，且**新增音频通道**）。另 `[r2·裁定 4]` **G6「结算彩带」行**亦已由 ux 域增行（**800**，复用「过关庆祝」行值；**音效列仍为「无」**，裁定 3 维持）⇒ 本文 D1/D2/A5 ⑥ 对 G6 的口径**不变**。

---

## 5. 待用户审批项（2026-09-11 已全部拍板）

1. **H1 符号显示开关**：❌ **不提供**（用户拍板）——三重编码默认开启保证色盲安全，demo 不设纯色模式；本项关闭，不再追踪。
2. **E2 大字号**：✅ **基础版即可**（用户拍板）——正文 32→40px 开关落地，弹窗/按钮排版重排延后至 v1.1；UX 规格只须覆盖基础版。
3. ~~倒计时告急阈值（最后 10s）~~ → **已冻结**：`TIMER_URGENT_T` = 10s（`systems-index.md` §3.5 v1.0）。

---

*可访问性是基础要求而非可选装饰。任何新增视觉/交互元素须回查本矩阵。*
