# 《拼豆》资产规格表（Asset Spec）

> 项目：微信小游戏矩阵 · 第二款《拼豆填色消除》
> 引擎：Cocos Creator 3.8 LTS ｜ 平台：微信小游戏
> 版本：**v1.5-r7**（demo 阶段，程序化绘制占位；本批增量登记为 **v1.5-r7**）｜ 作者：林绘澄
> **v1.5-r6（v1.24 托盘 24/48 槽几何重验 + B′ × 动态质感章交叉重算「对齐批」，2026-09-17，林绘澄）**：**修订对象 = v1.5-r5（WXG-T-142 **B′ 垫色显缝**，该批只改 §1.1 L11 卡 / §1.2 `filled` 行 / §1.8 基线 ④，**§1.6 动态质感章七卡一字未动**）+ v1.5-r4（其案 (a′) 前提被 v1.24 推翻）+ 对齐 `systems-index` v1.24（WXG-T-141 托盘容量 6→**24**）**。来源 = `memory/2026-09-17.md` 遗留项 ①「4 行态带高 / `btn_expand` 热区重验（art/ux 新单）」（= §3.4 `TRAY_BASE_SLOTS` 行原文自带 TODO）+ 主理人自认欠账（T-142 改了 §1.1 未改 §1.6）。<br>**⚠️ 让号自查（承 r3→r4、r4→r5 判例）**：`grep -rn 'v1\.5-r6'` 全仓（`*.md`/`*.ts`）**零命中** ⇒ **号未被占，本批照单使用 v1.5-r6，无需让号**。⚠️ **同时自查出 v1.5-r5 落盘时未写头注**（正文有 `[v1.5-r5]` 标记 L57/L91/L900，头注块缺行）⇒ **本批下行补记，不追溯改写 r5 正文**。<br>**⚠️ 幂等自查 · 点名纠正派单 §零 的四处过时/错引（承「按冻结真源、派单事实有误即纠正并留痕」判例）**：① **HEAD 实为 `2c433f3`**（WXG-T-139 E5）而非 `d9f99aa`；② **派单所列「工作树有未提交改动」的 art/设计/代码/memory 十一项，已全部提交进 `d9f99aa`/`2c433f3`** ⇒ 当前工作树的 `M` 仅 `production/TASKS*.md` + **`src/config/tuning.ts` / `src/view/{bead-render,palette,view-model}.ts` / `tests/{bead-render,feedback-vfx,view-model}.test.ts` + 未跟踪 `tests/zz-dbg.test.ts`** ⇒ **这些是 WXG-T-143（程基岩「E6 渲染改造：四层凹陷卡 + L11 垫 + 端点表 + 托盘 24 槽适配」🔄 进行中）的在途产出**，本批**一律只读、不回退、不改写**；**`art/*.md` 三件此刻是干净的** ⇒ 本批编辑无并发写冲突。③ **台账实为「已分配至 WXG-T-143、下一可用号 WXG-T-144」**（派单称 142/143）；④ **`BEAD_DRAW_INSET` 在 art 三件并非零命中，而是 5 处命中**（`accessibility.md:172/:175/:176` + `assets-spec.md:57/:60/:91`）——派单的「`v1.24`/`WXG-T-141`/`48 槽` 零命中」与「§1.6 全章对 `BEAD_DRAW_INSET`/垫/L11 零命中」两条**经我复验成立 ✓**。<br>**⚠️ 代码真值新事实（派单未列，且直接决定本批多条结论）**：`BEAD_DRAW_INSET = 2` **已在 `tuning.ts:35` 存在**、`drawFilledBead` 已接 `FilledBeadOptions.padColorIdx`（`bead-render.ts:94`）、`view-model.ts:731` 网格 filled **传 `padColorIdx` 且不传 `size`** ⇒ **网格 filled 珠 draw 基准 = `BEAD_CELL 50 − 2×2 = 46` 由既有实现唯一确定**（§三-1 的「46 还是 50」之争在代码侧**已无二义**）；托盘侧 `view-model.ts:797` 传 `size: TRAY_BEAD_SIZE(=44)`、**不传 pad** ⇒ 托盘珠 44、**无垫**。<br>**① 任务 1 · 4 行态（48 槽）物理不可行 ⇒ 已复验并给出四候选**：`trayLayout(rows)` 实算 `panelH = rows×54 + 18` ⇒ **rows=2 ⇒ 126 / 面板 y∈[324,450] / 溢出 0 / 与热区重叠 0 ✓**；**rows=4 ⇒ 234 > 带高 220 ⇒ 面板底 216 越 `TRAY_BAND.yMin 230` 达 14px，且与 `btn_expand` 热区 [230,318] 重叠 88px = 热区全高**（派单算式**逐位复核一致 ✓**）。⇒ 本批裁定 **V1 = 常态只画 2 实线行 + 单条「扩展预告胶囊」，扩展态采「按钮收起 + 瞬时切换」**（**零 §3 变更、零 `ux-spec §5` 增行**）；V2/V3 为备选、V4（须动 §3 冻结常量）**不在本文档下判论断**，已列「需主理人走 §3 变更单 + 用户拍板」。全量算式与比较表见 **§1.3.2**。<br>**② 任务 1 连带 · v1.5-r4 案 (a′) 正式作废（留档划线、不静默删除）**：其前提「6 实槽 + 右半 6 虚线预告槽 = 留白 324px」在 v1.24 下**不存在**（24 槽 = 满 12 列 × 2 行，**零留白**）⇒ §1.3.1 三案表整表划线留档、`tray_slot_dashed` 的「由边缘态升常驻态」「判据改 `idx >= TRAY_BASE_SLOTS`」两处注记**回滚到 v1.24 语义**（虚线 = **扩展预告行**，判据回到 `row > 0` 同义）；`btn_expand` 行的「净空 11px→65px 自动改善」亦**双作废**（rows=2 时实测仍是 11px，且新增「row1 热区 [329,391] 与按钮热区重叠 43px」的**负值**）。⚠️ **P0 落码陷阱（须在 T-143 前送达）**：`view-model.ts:782 const dashed = row > 0` 在 v1.24 的 `rows = ceil(24/12) = 2` 下**对 row1 的 12 个实槽恒真** ⇒ **基础态第二行实槽会被画成虚线**（与 r4 头注所报「恒假」为**同一段代码的反向失效**）。<br>**③ 任务 2 · B′ 与 §1.6 七卡的交叉重算（七卡中四卡受累）**：`[v1.5-r2]` 的「S1 露缘环」体系在 B′ 下**被「垫缝」体系整体替换** —— 静息露缘由 **0px 变 2.00px（常驻）**、峰由 `1.00px` 变 **0.62px**、谷 **2.92px**；`w(t) = 25 − 23×scale` 取代 `25(1−scale)`。⇒ **`drawSocketRim = scale < 1.00` 的可见性剔除判据、穿越 14.70ms、露缘窗 87.7% 三件套全部作废**；**G1 图元由 +1 回落到 +0**（垫属 §1.2 静态 11 层，动画期不再新增图元）⇒ **§1.6.9 / §1.6.8 / §1.8 三处同步、解环器期净 +5 → +3**。A5 重叠：峰 46×1.06 = **48.76 < 50 ⇒ 珠永不出格**，与静息邻珠（46）间隙 **4.62px** ⇒ **v1.5-r2 唯一未闭合项（L0a 越界 0.56px 侵入邻格）随 B′ 自动关闭**（越出本格上界由 2.56px 降至 0.355px，完全落在 2px 格缝内）。G4 峰 46×1.08 = **49.68 < pitch 52 ⇒ 零叠压**（旧「上下各叠压 1px」作废）。G7 谷 46×0.96 = **44.16** ⇒ A5 ⑦ 间隙 3px → **6.92px**、`stroke = 2.07 ≥ 2` ✓。<br>**④ 层序死结论（落码不留二义）**：**L11 = 珠层之下、全格平面（50、`radius 11`、ink `edge`），不参与 `scale`、不参与 `lift`、恒锁格缘**；`S1–S4` 与 `L11` **互斥**（`filled` 格不画坑）。⚠️ 但**代码现值与此相反**：`bead-render.ts:118–122` 的 pad rect 用的是 `outer = options.size ?? BEAD_CELL` 与 `y = cy + lift` ⇒ **`size`/`lift` 会同时带动垫** ⇒ **G1 严禁以 `size` 承载 scale**（须新增 `scale?: number`，默认 **1**，仅作用于珠体）。<br>**⑤ 峰的语义裁定**：v1.4「珠压进坑」的**凹侧对象（S1–S4）在 B′ 的 filled 格里不存在** ⇒ 叙事改读为三拍「鼓包盖垫（t=0, 缝 0.62）→ 压进露垫最多（t=40, 2.92）→ 坐定（2.00）」，**读感成立但摆动量 ±0.92/1.38px 属亚地板级**（诚实登记，不软化）；`ux-spec §5` 的 `1.06→1.0 / 120ms` **本批不改、不提变更请求**，改判据只在 art 侧给候选（谷 `0.96→0.92` 或 `INSET 2→3`），标 `[待 playtest]`。<br>**⑥ v1.5-r4 自查 6 处新发现的存续复核（承派单 §三-5，不扩大范围）**：(1) **B2 无测试支撑 + 4 色打穿 3:1 ⇒ 仍成立**，但 B′ 使符号区**不受影响**（垫在珠外，`r ≤ 23` 区域零改动）⇒ 结论不变、A5 ⑩ 补登 B′ 侧证；(2) `slotDashed` 死 token + 色/线宽双漂移 ⇒ **仍成立**，但「升常驻态」理由作废、改由 24 虚线槽（扩展预告）承接；(3) 面板圆角 **18 vs 24** 漂移 ⇒ **仍成立**；(4) `art-bible §7.1` Lv1 四处不符 ⇒ **不属 §1.6 卡、本批不动**，留 c1 与主理人串行清单；(5) 「净空 11px 派生值变 65px」⇒ **被 v1.24 推翻**（rows 回到 2 ⇒ 仍是 11px），跨域回写建议**撤销并改为新措辞**；(6) E4 开关只恢复网格侧 ⇒ **仍成立**，且 B′ 使其**代价下降**（托盘侧本就有 2px 垫缝替代读数？——**否**：托盘不传 `padColorIdx` ⇒ 托盘侧仍零读数，详见 §1.6.1a 末「新发现：咬合不对称方向反转」）。<br>**零代码改动、`palette.ts` 零新增 hex（新 ink 全部从 §1.9.2 端点表推导）、零新增 `systems-index §3` 冻结常量（建议常量一律归 `tuning.ts` 呈现层并给默认值）、零外部资产、零动他域正文**；**`ux-spec §5` 变更请求 = 零**（V1 扩展态采瞬时切换 ⇒ 不需「面板变宽/滚动」行）。<br>**诚实边界**：`[待真机]` = 垫缝视认性（原 2px，`[v1.5-r7]` 已改 6px 并待复验）、亚地板摆动可辨性、66px 胶囊；`[待 playtest]` = G1 幅度重定两候选；`[待扩展实装复核]` = 扩展态整体裁定（`beads-game.ts:1441–1447` MVP 只 `_showTapHint`、**不调 `expandTray`** ⇒ 48 槽路径当前不可达，本批属**规格承诺**）。
> **v1.5-r7（B′ 缝宽乙档 + 珠/垫圆角同心勘误，2026-09-18，代码侧代落盘，待林绘澄正主复验）**：让号自查 `grep -rn 'v1\.5-r7'` 全仓零命中 ✓。① **`BEAD_DRAW_INSET` 2 → 6（乙档，真机 playtest 裁定）**：旧 2 设计px 在真机 scale 0.5 下 ≈1 CSS px 不可见 ⇒ 错位垫缝读不出（BD-52）；新值 ⇒ 网格 filled 珠视觉 **38**（50 − 2×6）、四周露垫 **6px** 匀缝（缝宽比 24%，竞品 ≈31%）。⛔ **本文档内基于「珠 46 / 缝 2px」的全部派生重算值（§1.1/§1.2/§1.6 的 48.76 / 49.68 / 44.16 / 静息 2.00px / `w(t) = 25 − 23·scale` 等）数值作废**，原文留档供追溯；新基准代算参考：峰 38×1.06 ≈ 40.28、谷 38×0.96 = 36.48、静息缝 6.00px（`w(t) = 25 − 19·scale`）；**正式重算归正主**。② **珠/垫圆角同心**（真机反馈「珠子与底色形状不匹配」）：等距内缩要求珠圆角 = 垫圆角 − INSET = `round(50×0.22) − 6 = 5`（旧式 `round(46×0.22) = 10` 不同心 ⇒ 缝宽转角处不均）；L11 垫 `radius 11` 不变。实现 = `bead-render.ts drawFilledBead`；零图元增减、零新 hex、零 §3 变更。
> **v1.5-r5（`[v1.5-r6]` 本行为补记头注：WXG-T-142 落盘时只改正文、未写头注，故由本批补齐，措辞为事后追述、非当期原笔）**：**谜面可读性载体切换 B′「目标色垫 · 垫色显缝」**（用户 2026-09-17 拍板，切换 `[v1.5-r3]` 案 (b)「珠上目标色描边环」）。正文三处：§1.1 **L11 目标色垫**卡、§1.2 `filled` 行「静态 = **11 层**」、§1.8 基线 **④ = 1828**（+1 层/格，与 r3 描边环同层数 ⇒ **基线数值不变**）。要点：垫 = 画于珠层之下的**单层平面 roundRect 全格**，ink `mix(target, #000, 0.30)`（与空坑 **S1 同源** ⇒ **零新 hex**）；**珠 draw rect 内缩 `BEAD_DRAW_INSET = 2`** ⇒ 视觉珠 **46**、四周露垫 **2px 缝**；`BEAD 50` 仍为格几何／热区真值（内缩仅 draw rect，绕开 §3.3 冻结）。⚠️ **该批的影响面只算到静态层，未追至 §1.6 七卡 ⇒ 即本批 v1.5-r6 任务 2 的由来**。<br>⚠️ **自查批间欠账（诚实、点名自己）**：`v1.5-r4` 头注⑤ 声称「**新增 §1.6.2a `vfx_solver_restore`**」，但该节**在 r4 当批从未落盘**（本批前 `grep -n "^#### 1.6.2a"` **零命中**）⇒ **本批补落为 §1.6.2a**，并把旧「解环器期真值 净 +5」按其派生项重算为 **净 +3**（详该节与 §1.6.9 末）。
> **v1.5-r4（v1.23 对齐 + 三项新活批，WXG-T-131，2026-09-17，林绘澄）**：**修订对象 = v1.5-r2（本单主体）+ v1.5-r3（WXG-T-140 的 **L11 目标色环**，本批已并入全部基线）+ 对齐 `systems-index` **v1.23**（WXG-T-138「大胆重制」，提案正本 `design/proposals/v1.23-bold-remake.md`）**。⚠️ **版本号让号（诚实登记，承 v1.5-r2 的让号判例）**：主理人本轮派单纪律 ⑩ 指令「三件统一走 **v1.5-r3**」，但开工自查发现 **`v1.5-r3` 号已被 WXG-T-140（主理人本人 / WorkBuddy 会话，2026-09-17，台账状态 ✅ 完成）占用并落盘**——本文件 §1.1 **L11 卡** / §1.2 `filled` 行「静态 = 11 层」/ §1.8 基线 **④ 1828**，与 `accessibility.md §5` 第 **4** 条关项（worktree 未提交态）。沿用 r3 会造成「同一版本号承载两批不相干改动」的追溯断裂 ⇒ **本批改走 `v1.5-r4`**（与 v1.5-r2 同理：当轮自查出号已被占即让号并显式声明，不静默占用）。<br>**① P0-2 · 三基线按 v1.23 的 6 槽重算（双值并列，写法承 §1.9.5「托盘空槽 规格 2 / 代码真值 1」判例）**——`systems-index §3.4` L113/L114 已冻结 `TRAY_BASE_SLOTS` **12→6**、`TRAY_EXPAND_SLOTS` **12→6**（Q9；几何常量 `TRAY_COLS` 12 / `TRAY_SLOT` 48 **不动**）⇒ **扩展后共 12 槽、不是 24**；而 `src/config/tuning.ts:101/:103` **仍为 12/12** ⇒ **规格／代码漂移中**（v1.23 工程落码「另排」）。⇒ §1.6.9 与 §1.8 两表全部升级为**双口径并列**：**规格口径（6+6 ⇒ 最坏 12 槽）= ①1690 / ②2029 / ③1624**（未扩展 **1606**、v1.4 材质 **1597**（规格 2 层）／**1585**（代码真值 1 层））；**代码口径（12+12 ⇒ 24 槽）= ①1810 / ②2173 / ③1672**（= v1.5-r2 原值，**标注「代码现状，v1.23 工程落码后作废」**）。**并入 T-140 的 L11** ⇒ 新增**基线 ④′ = 1780**（规格口径，= ③1624 + 156 格 ×1 层）／**④ = 1828**（代码口径，T-140 原值）。⚠️ **`h` 不变量在 L11 下破例**（网格 filled **11** 层、托盘 filled **10** 层（**托盘不带环**）⇒ 一珠网格→托盘 = 网格 `−11+4 = −7` / 托盘 `+10−4 = +6` ⇒ **净 −1/颗**）⇒ 规格口径 `h=0/6/12` = **1780/1774/1768**（`h` 上限由 24 降为 **12**、未扩展 **6**）；无 L11 的 ③ 口径 `h` 不变量**仍恒成立**（`h=0/4/8/12` 四帧**恒 1624**，相消逻辑与槽数无关，已复验）。⚠️ **纠正主理人派单的粗算**：派单给「基线 ② 2173 → **2149**」，实测应为 **2029**——2149 只把托盘**空槽增量** 48→24，却保留了 ① 里「托盘最坏 filled `24×10 = 240`」项；而 **12 槽的托盘物理上不可能持 24 颗珠** ⇒ 该项须同改 `12×10 = 120`（差恰 = **120** = 12 槽 × 10 层）。派单的「③ 1672 → **1624**」**与本批一致 ✓**。<br>**② 新活 1 · 托盘面板随 6 槽收窄的视觉裁定（v1.23 §2-1 明写「归美术规格（WXG-T-131 一并考虑）」）**——(a) 满宽留白／(b) 收窄／(c) 居中 三案比较后**裁定建议 = 案 (a′)**：满宽面板 + **6 实槽居左** + **右半 6 个虚线预告槽**（复用既有 `tray_slot_dashed` 语言填 324px 留白）。理由：`trayLayout()` **一字不改**（`rows` 在 v1.23 下恒为 1）、**零新动画 ⇒ `ux-spec §5` 无需增行**、`btn_expand` 与最上排**槽热区**净空由 **11px**（rows=2 实测，§3.4 现值）自动升到 **65px**、且把「可扩展余量」变成**可读的预告**（6 个虚位 = 看广告能拿到的 6 槽）⇒ **直接消解 v1.3 的 F2「构图空旷」而不需重演容器板/暖光 band 对策**。案 (b) 需「面板动态变宽 **324px**」新动画（§5 缺行）+ 微拱白瓷内阴影宽与右缘位置随扩展态**跳变** + 按钮与面板视觉脱节 ⇒ **驳回**；案 (c) 需改 `trayLayout` 的 `slotCenterX`（与命中测试共用的几何单一真源）且「扩展方向」不可读 ⇒ **驳回**。**连带自查（归工程单）**：`view-model.ts:758` `const dashed = row > 0` 在 v1.23 下 **恒假**（12 槽 ⇒ `rows ≡ 1`）⇒ 可扩展位的虚线语言**整体失效**，建议判据改 `idx >= TRAY_BASE_SLOTS`。<br>**③ 新活 2 · 珠材质主题换肤占位（v1.23 Q7+Q13；§2-3「demo 阶段只落解锁判定 + 一套换肤占位」）**——新增 **§1.9.7**。结构可行性取证结论：`BeadsPalette`/`DEFAULT_PALETTE`（UI token 层）**已可注入 ✓**，但 `BEAD_PALETTE`（`Object.freeze` 模块级）+ `beadColor()` + **七个材质系数**全为模块级、且 `drawFilledBead(builder, cx, cy, colorIdx, options)` **签名无 `palette` 形参**（对比 `drawEmptySocket(builder, cx, cy, palette, size, colorIdx?)` **有**）⇒ **珠体十层对主题化零参数通道 ⇒ 需工程最小重构**（最小改法建议见 §1.9.7，**本单不改代码**）。**换肤面裁定**：`base` **锁死**（换即新增 hex 违纪律 ④，且它是 A1 色相通道 + §1.9.2 十色间距 + `symbolInk()` 判据输入的**唯一承载面** ⇒ base 锁死则墨选择恒不变）；**软高光 α 只可下调、不得上调**（§1.9.7 反解：B2 的 `k` 上限 **0.2042～0.4126 < 1** ⇒ **现值已越过 3:1 地板**）；倒角三系数（`BEAD_BEVEL_DARK_MIX`/`LIGHT_MIX`/`RIM_MIX`）**可自由换**（几何证明：三条倒角带 `[0.78,5.47]px` 与符号区 `[15,35]px` **零交集**）；投影 α 可换但**不得降为 0**（§1.9.4 五通道之第 3 条）⇒ **demo 的「换肤占位」只能是「材质主题」（光泽度/倒角对比/rim 强度），不能是「配色主题」**（后者需新增 hex ⇒ 登记为待裁定，本轮不交付）。<br>**④ 新活 3 · 普通模式「归位连击」粒子渐强（v1.23 Q4，进 MVP）**——新增 **§1.6.11 `vfx_gather_streak`（G11）**，真源 = `ux-spec §5`「归位连击（普通模式）」行（**200ms，复用 Lv1 行值**、升调阶梯 ≤4 档封顶、零 HUD 数字不计分、与 sprint 连击两套系统）。参数卡为 sprint Lv1 星光粒子的**时间反演（向心）**：枚数阶梯 **2/3/4/5**、`R(p) = 60−34p`、`r(p) = 3+5p`、`α = 1−p`、`θ_i = 2π·i/n + π/4 + 0.35p` ⇒ **零新常量、零新缓动、零新 hex**（ink = `BEAD_HIGHLIGHT_HEX #FFFFFF`）。角度式的 `n=4` 逐枚与 `comboParticleOffsets()` 原式**恒等 ⇒ sprint Lv1 零回归**（原式对 `n=2/3/5` 退化，`n=5` 时第 5 枚与第 1 枚**完全重叠**）。A5：`min(R−r) = 18.00px @ p=1 > 10px`（符号区半宽）⇒ **全程不进符号区**；D2：`α` 单调递减 ⇒ **0 往复** + 非周期事件驱动 + `filled` 终态不可回退 ⇒ 同格重复 **0**；⚠️ 解环器逐颗 80ms ⇒ 并发 `ceil(200/80) = 3` 个 burst，保守折算 **8.3 峰/秒 > 3Hz** ⇒ **消解 = 采 global 门**（同一时刻只播一个 burst，承 `combo-vfx.ts` 头注「多档重叠只播最高档」判例）⇒ 同区域**恒 1 峰**。图元：global 门 **+5 `circle`**／per-cell 门 **+15**（原语 = `circle` ⇒ **零 polygon**），已并入 §1.6.9 / §1.8 两表。**区隔方案 = 模式互斥**（`score-combo §2.4`「普通模式完全不走本节」⇒ **永不同屏**，结构性区隔最强）+ **方向反转**（普通 = 向心收敛「归位」／sprint = 离心飘散「爆发」）+ **枚数阶梯**（sprint 恒 3–5 ／普通 2→5 递增）三重保险。<br>**⑤ G2 卡重立 + 阀② 正式作废 + 新静态回退阀**——§1.6.2 原「消除溶解」卡**整卡保留为历史留档**（锚 = `ux-spec §5`「消除（道具）」行，该行已由主理人加 ⛔ 作废注并**保留行位**、溶解"沙" clip **保留不删**）；**新增 §1.6.2a `vfx_solver_restore` — 解环器归位（G2′）**：错位珠高亮闪 1 次 → 逐颗复用 G1 落座（`ux-spec §5`「解环器归位」行 **200 + 120/颗、逐颗 80ms 间隔**）；批量上限 `SOLVER_PLUS_COUNT 3` / `SOLVER_RANDOM_COUNT 1` ⇒ **最坏同帧 3 颗**（**与 v1.5-r2 的「并发上限 +3」对上 ✓**）；payload `affectedSlots → **affectedCells**`。⚠️ **本批自查新风险**：`beads-game.ts:810-816` 的 swap 路径**一次发 2 个 `bead:placed`** ⇒ solverPlus 3 颗最坏发 **6** 事件 ⇒ 若「高亮闪」按**事件**计则净 **+8**（本卡按**颗**计 = **+5**，事件口径登记为风险）。**阀② 正式作废**（v1.5-r2 已算出收益由 −144 缩水到约 **−18**）⇒ §1.6.9 / `art-bible §7.4` 同批标注「**不再是有效降配手段；真机承压优先动静态回退阀**」，并把 v1.5-r2 的洞察「真正的性能风险不在动画期增量，而在静态基线本身」**落成可执行的四级静态回退阀 S1–S4**（§1.9.5 末）。<br>**⑥ 自查新发现（主理人未列出，共 6 处）**——(1) **B2「对合成面 ≥3:1」的声称无测试支撑且实测打穿**：`grep -n contrast tests/bead-render.test.ts` **零命中**，唯一断言在 `tests/symbols.test.ts:82` 且测的是 `symbolInk(base).contrast` = **对 base**；实算（`temp/v15r3-theme.mjs`）符号区被软高光叠加的上 **45%** 高度内，**4 色（玫红/丁香紫/湖蓝/赭棕）对合成面 = 2.48～2.83:1 < 3**（顶缘 1px 三层全叠处 **6 色**打穿、最低 **1.83**）⇒ 已入 `accessibility.md` A5 ⑩ + §6 验收项改写。(2) `palette.slotDashed`（`palette.ts:40/:69` `#C9C5DA`）**定义但零消费者**，`drawDashedRect` 实参为 `palette.slotBorder`（`#D8D5E6`）、`lineWidth = 1` ⇒ §1.3「描边 `slot_dashed` 2px」**双漂移**。(3) 面板圆角代码 **18** vs §1.3 规格 **24**（§1.5 已局部承认，本批正式登记）。(4) `art-bible §7.1` Lv1 行**四处**与代码不符（四角星 polygon vs `circle`／对角 8px vs 直径 16→6px／飘散 12px vs 半径 26→60px／随机方向 vs 确定性四象限）。(5) `systems-index §3.4` `btn_expand` 行「与扩展态最上排槽热区实测净空 **11px**」是 **rows=2** 的实测值，v1.23 下 `rows ≡ 1` ⇒ 自动变 **65px**（跨域回写建议）。(6) **E4 开关只恢复网格侧**：`view-model.ts:765` 托盘侧调 `drawEmptySocket` **不传 `colorIdx`** ⇒ 开关开启时**托盘侧仍无 E4**；而托盘空槽是**中性槽**（`base = palette.slot`、无目标色语义）⇒ **A2b 判据本就不覆盖托盘侧**，「分侧核算」应为**三侧**（网格侧 / 托盘侧 / 开关态），已入 `accessibility.md` A2b。<br>**零代码、`palette.ts` 零新增 hex、零新增 §3 冻结常量（新参数一律归 `tuning.ts` 呈现层）、零外部资产**；**`ux-spec §5` 变更请求 = 零**（新活 1 采案 (a′) ⇒ 无需「面板动态变宽」行；新活 3 断连回落建议**瞬时归零、无退场动画** ⇒ 无需增行，备选复用既有「连击断连 150」行值 ⇒ **两案均不需主理人增行**）。
> **v1.5-r2（三方交叉收口追加批，WXG-T-131，2026-09-16，林绘澄）**：**修订对象 = 下行 v1.5，与 v1.4 / v1.4-r2 无涉**。⚠️ **任务 D · 顺序误读消解**：本文件头注原顺序为 v1.5 / v1.4 / v1.4-r2 ⇒ `v1.4-r2` 排在 `v1.5` **之后**，读者会误以为「r2 修订的是 v1.5」。**本文件采「保留原位 + 行首显式注」处置**——理由：头注是**新→旧**的散文块、不是编年表，且 `v1.4-r2` 本就**紧随其修订对象 `v1.4`** 之后（现序 v1.5-r2 → v1.5 → v1.4 → v1.4-r2 → v1.3，两两相邻即读作「上行被下行修订」）⇒ 已在 `v1.4-r2` 行首加「**修订对象 = 上行 v1.4，与 v1.5 / v1.5-r2 无涉**」显式注。**`art-bible §9` 同批采「物理移行」处置**——理由：该表是**旧→新**的严格编年表、`版本` 列即排序键，错位行会破坏表自身的序 ⇒ 把 `v1.4-r2` 行移到 `v1.4` 与 `v1.5` **之间**，并新增 `v1.5-r2` 行于表末。**两文件处置不同、理由各异，误读均已消解**。四件事：<br>**① 任务 B · 三基线重算与边界标注**——§1.6.9 末与 §1.8 末表把 T-128 的四个净值（道具期规格口径 **−141** / 本轮落码口径 **+3** / 庆祝期 **−580** / 启阀③ **−600**）在 **1810 = v1.4 基线**（E1 混色底 + empty 2 层）与 **2173 = v1.5 基线**（纯色凹陷四层 + 托盘微拱白瓷内阴影 +3）上**并列开列**；并补第三基线 **1672 = v1.5 材质 + v1.22 构型的可达真值**（**h 不变量**：一颗珠从网格移到托盘 = 网格 −6 / 托盘 +6 ⇒ 相消，与手持珠数 h 无关）。**三套口径不得混用**（纪律承 §1.6.8 双口径注写法）。同时更正两处：**2173 是松上界、非可达单帧值**（它把「全格 filled」的 1560 与「全格 empty 的 v1.5 增量 +312」叠加，而两者同帧互斥）；**§1.9.5「托盘空槽 旧 2」与代码不符**（`view-model.ts:765` 不传 `colorIdx` ⇒ 无 E4 ⇒ **代码真值 1 层**）⇒ 已加「规格 2 / 代码真值 1」双值注。<br>**② G2「240→96」语义失效处置（主理人复核发现，本批自行验证后确认成立）**——原式意为「溶解珠由十层 LOD 降到 **4 层降档珠**（L1+L2+L3+L5）」。v1.5 已把**托盘空槽也改成凹陷四层**（S = `TRAY_SLOT` 48、中性 `base` = `palette.slot`、S4 亮线 `#FFFFFF`）⇒ 溶解的**终态**（τ ≥ 200、槽 `free`）由「**2 层**（E1 混色底 + E4）」变「**4 层凹坑**（S1–S4）」⇒ **终态 48 → 96（+48）**，而 **LOD 后半仍是 96**：`24×4 = 96` 两处**数值巧合相同、构成完全不同**（一为降档珠、一为凹坑）⇒ §1.6.2 / §1.6.8 / §1.6.9 三处**分行开列并各自标注构成**（防落码者把「96」当同一件事、或按「降档珠」去画终态）。回退阀②的判据文字同批重述。<br>**③ 任务 C · §1.6.1 补 empty 侧联动（凹凸咬合）**——原 G1 卡建立在 v1.3 基座（E1 混色底 + empty 2 层）上、**零 `empty` 提及**；v1.5 后 empty = 凹陷坑、filled = 凸起十层 ⇒ 新增「**S1 露缘环**」逐帧口径：**可见性剔除**（`scale(t) < 1.00` 才画 S1；穿越时刻 `t = 14.70ms`、占窗口 **87.7%**），露缘宽 `w(t) = 25×(1−scale(t))`（t=14.70 ⇒ 0、**t=40 ⇒ 1.00px 峰**、t=120 ⇒ 0），**S2/S3/S4 全窗恒被珠体覆盖 ⇒ 不画**（证明：动画期珠 ≥ `50×0.96 = 48px` > 坑 `50×0.94 = 44px`、S3/S4 贴坑内缘带 ⇒ 三层零露出）。**L0a 接触阴影落点改判**（平面底 → S1 暗缘框 `mix(base,#000,0.30)`）的量化后果与处置见该节：**改判承载者、不改值**——`ux-spec §5`「珠子落座」行的 `α 0.12→0.18→0.12`、`宽比 0.88→0.92→0.88` **一字未改**（⇒ **本批零 `ux-spec §5` 变更请求**）。<br>**④ 图元重算（本批最易错处，已自算）**——§1.6.1 原「图元 **+0**」**在 v1.5 材质下作废**：落座动画期为 **+1/颗**（S1 条件绘制），并发上限 = 解环器链式 `ceil(120/80) =` **2** + 玩家手动取回落座 **1** = **+3**；而 `empty→filled` 的 **+6**（4 层坑 → 10 层珠）属**放置瞬间的状态迁移**、**不属动画期增量**，**两者不得相加**。⇒ 同批同步 §1.6.8 汇总表 / §1.6.9 / §1.8 末表 / `art-bible §7.3.1`·§7.4·§7.5 五处。<br>另：**§1.2 / §1.9.5 / §1.9.6 / §6 的 a11y 引用改指真实锚点**——`accessibility.md` **v1.5**（= 降档**事实登记**，文策渊代落盘于提交 `90a2059`）+ **v1.5-r2**（= **量化核算**与 A6/D1/D2/§3 清单的连带复核，WXG-T-131，林绘澄）；原句「已由 T-130 登记」的**承载方错认**已更正（T-130 第二批只改 `systems-index.md` 与 changelog）。<br>**连带登记（本批不重写卡，只钉事实）**：**v1.22 道具反转**（`systems-index §3.6`：`POWERUP_TYPES` → `['solver','solverPlus','solverRandom']`、`['region','clearAll','random']` ⛔作废、**托盘零读写**、作用对象 = 网格**错位珠**、`REGION_CLEAR_SLOTS 6`/`RANDOM_CLEAR_COUNT 5` ⛔作废、新增 `SOLVER_PLUS_COUNT 3`/`SOLVER_RANDOM_COUNT 1`）⇒ **§1.6.2 G2 卡的「作用对象钉死 = 托盘槽位珠」及其唯一依据（「`powerups.ts` 绝不写网格」）被正面推翻**、批量上限 **24 → 3** ⇒ **T-128 道具期双口径（−141/+3）的前提失效**（三基线重算见 §1.6.9；**解环器期真值 = 净 +5 / 绝对 1677**，基线 C）；`powerup:used` payload `affectedSlots` → **`affectedCells`**（G3 不读 payload ⇒ 卡不需改）；`ux-spec §5` 现同时留旧行「消除（道具）200」与新行「解环器归位 200+120/颗」⇒ **双源风险已列入跨域清单**（`accessibility.md §4` 第 7① 条，交主理人串行处置）。**G2 卡重立归后续美术单、不在本批**。<br>**零代码、`palette.ts` 零新增 hex、零新增冻结常量、零外部资产**（本批新增的只有派生判据与既有层的条件绘制，无新常量；`SOCKET_GHOST_SYMBOL` 开关归工程单，见 `accessibility.md §4` 第 8 条）。
> v1.5：随 art-bible v1.5（**「纯色底 + 光影材质」**，**WXG-T-131**；玩法重塑「错位归位」WXG-T-130 带来的视觉窗口）**重写 §1.2 `empty` 卡为目标色纯色凹陷坑四层（S1–S4）**、§1.3 托盘面板增「微拱白瓷」内阴影、**新增 §1.9 质感语言卡**（三物体材质分工 / 10 色渐变端点对照表与推导规则 / `SOCKET_*`·`TRAY_PLATE_*` 常量组 /「must not read as a bead」五通道复核 / 图元与性能核算含**端点预计算缓存建议**）；同步更新 §1.8 统计与图元表、§6 验收项。**E1 混色底 / E4 幽灵符号作废**（引用态：§3.8 冻结值作废落点 = T-130 第二批 systems-index 冻结变更 + 工程单，本单不改 §3 / `src/**`）。**零外部资产、`palette.ts` 零新增设计 hex**（端点为 mix 推导值，模块级预计算）、**零冻结常量变更**。可访问性 A2b/A3 降级的**事实登记** = `accessibility.md` **v1.5**（文策渊代落盘，提交 `90a2059`）、**量化核算 + A6/D1/D2/§3 清单连带复核** = `accessibility.md` **v1.5-r2**（WXG-T-131，林绘澄）。❌ 本行原句「可访问性 A2b/A3 降级已由 T-130 登记，本单只交叉引用（`accessibility.md` 未改）」的**承载方错认**已由 `[v1.5-r2]` 更正——经查 T-130 第二批（changelog v1.22）**只改了 `systems-index.md` 与 changelog**，`accessibility.md` 的 v1.5 笔迹系文策渊在提交 `90a2059` 中代为登记，且为**定性描述、零量化核算**⇒ 量化与连带复核的责任落回 T-131。
> v1.4：随 art-bible v1.4（**「动态质感章」**，**WXG-T-128**；台账已改号，`WXG-T-127` 现为程基岩工程单）把 **§1.6 VFX 由 4 行简表升级为 7 张可直接落码的参数卡**（§1.6.1–§1.6.7，照 §1.1/§1.7 写法：常量 / 逐帧公式 / 层序 / clamp 与回退阀），新增 §1.6.8 汇总表、§1.6.9 LOD 与图元核算、§1.6.10 落码骨架判例；同步更新 §1.8 末统计行与 §6 验收项。**毫秒值零改动**（真源 = `ux-spec §5`）、**色值零新增**（沿用 art-bible §3 色板）、**冻结常量零新增**（新增参数全部归 `src/config/tuning.ts` 呈现层，承 v1.3 `PLATE_OUTSET`/`GLOW_BAND_ALPHAS` 判例）。
> **v1.4-r2（三方复核后修订，同批未提交）**：**〔任务 D 显式注：本行的修订对象 = **上行 v1.4**，与 v1.5 / v1.5-r2 **无涉**。本行物理位置未动（头注为新→旧散文块，本行紧随 `v1.4` 即为「上行被下行修订」的读法），只加此注以消解「r2 修订 v1.5」的顺序误读；`art-bible §9` 因是编年表而采物理移行，处置理由见 v1.5-r2 注〕** ① 修 4 处缺陷——**§1.6.2/§1.6.9 G2 LOD 阈值 126ms → 80ms**（本卡定 `α = 1−q` 线性 ⇒ α≥0.6 ⇔ τ ≤ 200×0.40 = 80ms；旧 126ms 系误把线性 α 当 easeIn 反解）、**§1.6.9「砍掉的四层」→「六层全砍」**、**§1.6.7 A5 间隙 4px → 3px**（只被点一颗缩到 48 ⇒ `52−24−25 = 3`）、**全文改号 127 → 128**；② 回写用户 7 项裁定（§1.6.4 面板延迟 800ms **定案**、G5 **案 B 定案**、§1.6.7 **G7 加 `sfx_denied`** + 原子落码约束、§1.6.6/§1.6.7/§1.6.8 毫秒真源改引 `ux-spec §5`「结算彩带」/「不可填格轻压」两行、§1.6.9/§1.6.10 **LOD 本轮只预埋 G4 六层档**、§1.6.3 **G3 限定玩法区 y∈[0,1214]**、§1.6.1 **1.06 = 规格起点值**）。**零代码、`palette.ts` 零新增 hex、本表零新增冻结常量**（唯一跨域例外：裁定 3 连带 `systems-index §3.12` `AUDIO_CLIP_TOTAL` 19→20 冻结变更单，属音频/GDD 域、随工程原子批）。
> v1.3（承前）：随 art-bible v1.3（丙案「双色温对撞」）增补 §1.1 珠子十层卡、§1.7 容器板+暖光 band 卡、§1.8 背景层次卡，修订 §1.3–§1.5 HUD/道具参数（评审 F1–F8）。UI token 真源 = `art-bible.md` §3.1 v1.3 表。
> 配套：`art-bible.md`（视觉身份）、`accessibility.md`（可访问性）
> **纪律**：`[待 systems-index §3 对齐]` 项不自行发明；本表其余数值可直接落码。

---

## 0. 前置约定（程基岩落码直接引用）

| 项 | 值 |
|---|---|
| 设计分辨率 | **750 × 1334（竖屏）**（已裁定） |
| 适配策略 | **`FIXED_WIDTH`** |
| 坐标原点 | **画布左下角，y 轴向上** |
| 渲染方式 | Cocos `Graphics` + 纯色 `Sprite`；**不加载任何外部图片** |
| 颜色空间 | sRGB；`mix(A,B,t)` = 线性插值两 HEX，t 为 B 的占比 |
| 珠子单元尺寸 | `BEAD` = **50**（`systems-index.md` §3.3 冻结值，pitch 52 = 50+2）；下文参数卡以 **BEAD = 64** 为示例值演算展示比例，落码按公式等比换算 |

---

## 1. 资产清单（可见元素规格表）

### 1.1 珠子绘制参数卡（**核心，逐层落码**；v1.3 十层质感升级 F4）

> 单颗珠 = 以下 10 层按序绘制。所有层均为圆角矩形/线/矢量，无贴图无模糊无真渐变；软高光/接触阴影一律靠叠层 + 递减 α 模拟（渲染原语面合规）。

```
常量：BEAD = 珠子边长（冻结值 50，来源 systems-index §3.3；下文比例以 BEAD=1 归一）
      r    = round(BEAD × 0.22)
      base = 珠子基色（art-bible §3.2 十色之一）
      stroke(ratio) = max(2, BEAD × ratio)   // §1.1 最小特征约束：笔画 ≥2px

L0a 接触阴影 roundRect(x + 0.06, y − 0.02, 0.88, 0.10, r×0.5)
           fill #1E2033, α 0.12          // 贴底窄条：珠"坐"在面上（F4 新增）
L0b 投影    roundRect(x, y − 3/64, 1, 1, r)
           fill #1E2033, α 0.15
L1 主体    roundRect(x, y, 1, 1, r)              fill base
L2 暗倒角  下+右内缘（内缩 2/64）   lineWidth stroke(5/64), mix(base,#000,0.26)
L3 亮倒角  上+左内缘（内缩 1.5/64） lineWidth stroke(4/64), mix(base,#FFF,0.20)
L3b rim光  上内缘单线（内缩 1/64）  lineWidth stroke(2/64), mix(base,#FFF,0.38)
L4a 软高光·广 roundRect(x+0.06, y+0.52, 0.82, 0.38, 0.19)  fill #FFF, α 0.08
L4b 软高光·中 roundRect(x+0.10, y+0.60, 0.72, 0.26, 0.13)  fill #FFF, α 0.16
L4c 软高光·核 roundRect(x+0.16, y+0.68, 0.56, 0.14, 0.07)  fill #FFF, α 0.30
L5 符号    居中，尺寸 BEAD×0.40，线宽 ≥ 2px
           color = (base 亮度 > 0.6) ? mix(base, #000, 0.55) : #FFFFFF @ 0.90
           （B2 地板 3:1 优先：草绿一色翻转深色墨，见 accessibility 偏差 1）

// ↓ [v1.5-r5] WXG-T-142：filled 格「目标色垫」——谜面可读性载体 **B′ 垫色显缝**（用户 2026-09-17 拍板，切换 [v1.5-r3] (b) 描边环；语义不变、层数不变）
L11 目标色垫 roundRect(x, y, BEAD, BEAD, r)   // **画于珠十层之下**（渲染序：L11 → L1…L10）；垫 = 平面，零投影零高光（不读作珠，与 §1.9.4 无冲突）
           fill mix(pattern_colorIdx, #000, 0.30)   // 与 empty 坑 S1 同源端点，§1.9.2 已存在 ⇒ 零新 hex
           **珠视觉内缩**：draw rect = BEAD − 2×BEAD_DRAW_INSET（**`[v1.5-r7]` BEAD_DRAW_INSET = 6（乙档，真机 playtest 拍板；原值 2 ⇒ 视觉珠 46/缝 2px，真机 scale 0.5 下不可见）**，art 常量归 `tuning.ts`，零 §3 变更）⇒ 视觉珠 **38**、四周露垫 **6px** 匀缝（竞品缝宽比 ≈31%，我方 24%）；**珠圆角 = 垫圆角 − INSET = 5（同心，承 §1.1 层序死结论）**
           base_source = 该格 **pattern 目标色**（beadColor(pattern(row,col))），**不是珠身基色**
           作用范围 = **仅棋盘 filled 格**；托盘珠 / locked / empty 三态**不画**（托盘无目标色语义）
           归属 = **「格」而非「珠」**：不随 selected 上移 4px、不随落座 scale 缩放，恒锁格缘
```

> **F4 说明**：v1.2 的「L4 硬边单高光条 α0.38」作废，改为 L4a→L4c 三层**外扩递减、中心递增**叠层（累计中心 α≈0.48、边缘 α≈0.08）模拟柔光；倒角由 3/2px@64 加宽至 5/4px@64 并新增 L3b rim 光与 L0a 接触阴影，消解真机「塑料贴片感」。L4a–L4c 均在 L5 符号**之下**绘制，符号对比不受影响（accessibility A5 复算）。

**符号矢量定义**（程序化 path，不依赖字体；占珠面 40%）：

| 珠色 | 符号 | path 定义 |
|---|---|---|
| 奶白 `#FDF6E9` | ○ 圆环 | 圆 r=13，stroke 3，不填充 |
| 柠黄 `#FFD23F` | ★ 五角星 | 5 角星路径，外接圆 r=13，填充 |
| 活力橙 `#F59B23` | ● 实心圆 | 圆 r=11，填充 |
| 草绿 `#3FBF6B` | ■ 实心方 | 方 20×20 圆角 4，填充 |
| 玫红 `#E84C3D` | ♥ 心形 | 双圆(r=6, 圆心±6x) + 下三角，填充 |
| 丁香紫 `#8E6FD9` | ◐ 半圆 | 半圆弧（左半）填充 + 直径线 |
| 湖蓝 `#3D7BF5` | ▽ 下三角 | 等边下三角，边 24，填充 |
| 赭棕 `#A5652C` | ▲ 上三角 | 等边上三角，边 24，填充 |
| 深棕 `#6B3E1E` | ◆ 菱形 | 菱形对角线 26/16，填充 |
| 炭黑 `#33333D` | ✚ 十字 | 两矩形 22×6 正交，填充 |

**最小特征约束**：符号线宽 ≥ 2px、最窄填充 ≥ 6px、软高光核高 ≥ 0.14×BEAD、倒角/rim 经 `stroke()` 钳至 ≥2px → 50% 缩放下仍可辨；**禁止**给符号加 <2px 细节。

> **性能回退阀（登记）**：珠子层数 6→10，最坏 13×12=156 珠 ≈ +624 图元/帧；若真机掉帧，由主理人裁定对 `filled` 珠合并 L4a+L4b（降回 8 层），本表不自行降配。

### 1.2 珠子状态参数

| 状态 | 绘制差异（其余同 §1.1） |
|---|---|
| `filled` 已填 | **静态 = 11 层**：§1.1 十层 + **L11 目标色垫**（`[v1.5-r5]` WXG-T-142，用户 2026-09-17 拍板 **B′ 垫色显缝**，切换 `[v1.5-r3]` 描边环载体、语义不变）。语义：**珠四周露 2px 垫色（= 该格目标色暗一档）、珠面 = 珠自身色 ⇒ 垫≠珠即错位**，恢复 v1.22「颜色对照」谜面（此前珠 50px = 格 50px、坑底色零露出 ⇒ 谜面仅靠图案语境色块冲突可读）。 (c)「只标错位珠」泄底、**未采纳**；(a) 缩珠径曾被驳回（`BEAD` 50 是 §3.3 冻结值）——B′ 采**渲染内缩**绕开该冻结（`BEAD` 50 仍为格几何/热区真值，内缩仅 draw rect，诚实记录）。已接受代价：全盘 156 格的垫色缝（与空坑同源色，噪声低于描边环；垫是平面 ⇒ 无「must not read as a bead」冲突）。<br>⚠️ **`[v1.5-r2]` 动画期例外（已由 `[v1.5-r6]` 按 B′ 重写；旧文本保留于下行划线注，不静默删除）**：**动画期该格恒为 11 层、图元增量 +0**——B′（`BEAD_DRAW_INSET = 2`）后，**静息露缘不再是 0 而是常驻的 2px 垫缝** ⇒ 「`scale(t) < 1.00` 才画 S1 露缘环」的**可见性剔除判据失去对象**（`filled` 格本就与 S1–S4 互斥，坑不画）⇒ 动画期不再产生「+1/颗」。逐帧露缘（现称**垫缝**）宽公式与图元重算见 **§1.6.1a**；汇总见 §1.6.8 / §1.6.9 / §1.8 末表。<br>~~⚠️ `[v1.5-r2]` 旧文：落座 120ms 内该格虽为 `filled`，但 **S1 暗缘框条件绘制**（「露缘环」，判据 `scale(t) < 1.00`）⇒ 该格瞬时为 **11 层**、图元 **+1/颗**（**不是** v1.4 的 +0）。~~ **作废理由（`[v1.5-r6]`，WXG-T-142 连带）**：该判据的唯一物理前提是「静息时珠 50px **恰好铺满** 50px 格 ⇒ 露缘 0px ⇒ 不画则无读数」；B′ 后珠视觉 **46px**，静息即有 **2.00px** 垫面露出（且它不是“坑缘”而是“目标色垫”，语义上属**静态谜面载体**、不得随动画开关消失）⇒ 剔除判据若仍执行，会在 120ms 内把**常驻谜面层**画掉再画回 ⇒ 构成 `D2` 语义下的同区 α 往复风险。**新结论：垫层不参与动画开关（恒画）。** |
| `empty` 空位（**目标色纯色凹陷坑**，v1.5 重写 · WXG-T-131） | 四层绘制（几何比例以坑边 S 计；棋盘 S = `BEAD_CELL` 50，托盘 S = `TRAY_SLOT` 48；**`base`** = 棋盘 = 目标色 `beadColor(colorIdx)` **纯色，不再 `mixWith`**——E1 作废，用户裁定「底色花」；托盘中性槽 `base` = `palette.slot`、S4 亮线改 `#FFFFFF`）：<br>**S1 暗缘框** rect 全尺寸 radius r，fill `mix(base, #000, 0.30)`（兼作旧 E2 边界的材质化，ink 复用端点表 Lpit 同源系数）；<br>**S2 坑底** rect 内缩 `SOCKET_INSET_RATIO 0.06`（50 格 → 坑 44px），fill `mix(base, #000, 0.14)`（同色相暗一档，**仍读目标色**）；<br>**S3 上内缘内阴影线** line，线宽 `max(2, S×0.05)`，ink `mix(base, #000, 0.30)`（凹坑最强线索：背光内缘）；<br>**S4 下内缘受光亮线** line，线宽 `max(2, S×0.04)`，ink `mix(base, #FFF, 0.20)`（= `BEAD_BEVEL_LIGHT_MIX` 同值，注明同源）。<br>**禁画**：L0a/L0b 投影、L2–L4c 倒角高光、L3b rim、L5/E4 符号（**E4 幽灵符号移除** ⇒ 连带可访问性 **A2b 部分降级 / A3 降档至 `empty` 态**。**`[v1.5-r2]` 锚点更正**：原句「可访问性降级已由 WXG-T-130 登记」系**承载方错认**——T-130 第二批（changelog v1.22）**只改了 `systems-index.md` 与 changelog**，`accessibility.md` 零 T-130 笔迹 ⇒ 登记责任落回 T-131。**真实锚点**：降档**事实登记** = `accessibility.md` **v1.5**（文策渊代落盘，提交 `90a2059`，定性描述）；**量化核算 + 恢复条款 + A6/D1/D2/§3 清单连带复核** = `accessibility.md` **v1.5-r2**（WXG-T-131，林绘澄；含「**落码即假绿**」窗口与开关名 `SOCKET_GHOST_SYMBOL`）。同批已改锚点的还有 §1.9.5 / §1.9.6 / §6 与 `art-bible` §3.3 / §3.4）。<br>**设计意图**：「an unfilled cell must not read as a bead」——E4 移除后区分主轴 = **凹凸对比**（与珠十层卡明暗方向互换 + 内缩 + 零投影零高光），五形态通道复核成立，见 §1.9.4；坑底只暗一档 ⇒ 「同色入格」目标色信息不丢。<br>**与 `hint` 叠加**：hint 态 = 本四层 + **外描边** `accent_blue #3D7BF5` 2px + 600ms 呼吸（α 0.5↔1.0），外描边覆盖 S1、坑底保留可见。 |
| `locked` 锁定 | 主体 `palette.locked`（`#B9B4CC`）**平涂**（locked 格 colorIdx=0 无语义基色，无法执行原规格 `mix(base,#B9B4CC,0.60)`，正式追认此偏差）+ 45° X 斜纹（`palette.background` @ α0.9，线宽 2，两条对角线）；**无高光无符号**。非颜色通道由 X 斜纹承担。 |
| `hint` 提示 | 目标色凹陷坑 `empty`（v1.5 四层 S1–S4）+ 外描边 `accent_blue #3D7BF5` 2px + 600ms 呼吸（α 0.5↔1.0）；叠加优先级：外描边 > S1 暗缘框 > 坑底（毫秒与循环频率以 `ux-spec §5` 为权威（600ms α 0.5↔1.0）；原 `[待 ux-spec 对齐]` 占位经 WXG-T-091 删除） |
| `wrong` 错误 | 当前珠 + 描边 `danger #E8434A` 2px 闪 2 次（≤2 次/秒）+ 位移 ±3px 抖动 200ms |
| `selected` 选中 | 完整 6 层 + 整体上移 4px + L0 投影 α 0.15→0.25 + 珠下 6px 处 Ø8 圆点 `accent_blue` |

> `[v1.3·F5]`（历史留档）**冻结常量引用声明（已闭合，WXG-T-125）**：`EMPTY_TINT_MIX` **0.35→0.42** / `EMPTY_GHOST_ALPHA` **0.20→0.32** 已走 §6 冻结变更流程落地（systems-index v1.21，用户 2026-09-16 拍板甲），本表 E1/E4 已同步回写；此前版本（0.35/0.20）系真机偏淡致「同色入格」首道解锁削弱的历史口径。
> **`[v1.5]` 作废引用态（WXG-T-131）**：`EMPTY_TINT_MIX` / `EMPTY_GHOST_ALPHA` 因 **E1 混色底 / E4 幽灵符号作废**而**待删**——§3.8 冻结值作废归 **T-130 第二批 systems-index 冻结变更**（本单不改 §3），`palette.ts` 两常量删除与 `drawEmptySocket` 改造（删 `GHOST_SYMBOL_SCALE` 路径、改四层卡）归**工程单**。本表只改视觉方案。

### 1.3 槽位托盘

| 名称 | 规格（设计 px） | 参数 |
|---|---|---|
| `tray_panel` | 690 × 190 `[待 systems-index §3 对齐]` | 白 `#FFFFFF`，圆角 24，描边 `panel_border` 1px，投影 = 圆角矩形 (0,−3) `#1E2033` α0.10。**v1.5 微拱白瓷质感（WXG-T-131）**：板内侧**底缘两段内阴影**（高 4px α0.03 + 高 2px α0.05，ink `#1E2033`）+ **右缘单段**（3px α0.02，光源左上故弱一档）；白板无明端余量 ⇒ 材质走**暗端渐变**（§1.9.1/§1.9.3）。**WXG-T-097 注**：本行与 §3.4 仍未对齐（实装按槽簇派生：宽 12×54−6+2×12 = 666、高 rows×54−6+24；垂直锚定 v1.20 由「带内居中」改「**贴带上沿**」），尺寸修正属美术侧待办，本次只同步锚定口径，不改本行原值。<br>**`[v1.5-r4]` v1.23 · 6 槽下的面板视觉裁定（新活 1；v1.23 §2-1 原文「几何常量 `TRAY_COLS`/`TRAY_SLOT`/`btn_expand` 热区**不动**，只改容量语义；**托盘面板视觉是否随 6 槽收窄归美术规格（WXG-T-131 一并考虑）**」）**：<br>**裁定 = 案 (a′)「面板保持满宽 + 6 实槽居左 + 右半 6 列画虚线预告槽」**（三案比较与否决理由见下）。⇒ **本行规格值零改动**：`trayLayout(rows=1)` 派生 `rowWidth = 12×54−6 = **642**`、`left = (750−642)/2 = **54**`、`panelW = 642+2×12 = **666**`、`panelH = 1×54−6+2×12 = **72**` ⇒ 面板 `x∈[42,708]`、`y∈[378,450]`（贴 `TRAY_BAND.yMax 450`）——**与 v1.23 前（未扩展 12 槽）逐字相同**，因为 `rowWidth` 只依赖 `TRAY_COLS`（**几何常量不动**）、不依赖容量。<br>**微拱白瓷内阴影的横向尺度（v1.23 · 案 (a′)）**：底缘两段宽 = `panelW − 2×TRAY_PANEL_PAD = 666−24 = **642**`（= `rowWidth`，与槽簇同宽）；右缘单段 x = `panelX + panelW − PAD − 3 = 708−12−3 = **693..696**`。**三案对照**：(a′) 642 / 693（**与现行同值、零改动**）；(b) 收窄 ⇒ 318 / 531，**扩展后跳回 642 / 693** ⇒ 内阴影宽与右缘位置成为**逐帧量**（新动画、§5 缺行）；(c) 居中 ⇒ 642 / 693 但**内阴影与槽簇不再同宽对齐**（槽簇 x∈[216,534]）⇒ 「光从板缘来」读作「光从空处来」。<br>**圆角登记（本批正式化）**：代码实装 `radius: 18`（`view-model.ts:748`），本行规格 24 ⇒ **漂移**；§1.5 已局部承认「圆角随面板实装值 18」，本批**登记为待裁定**（建议以 18 为准回写本行，理由：18 与 `panelH 72` 的比例 = 1/4，24 会使上下直边段只剩 `666−48 = 618`、且与 §1.4 卡圆角 20 的层级关系倒挂）。<br>**`[v1.5-r6]` v1.24 回写（24/48 槽）**：上句的案 (a′) 前提（6 实槽 + 右半 6 虚线）**已被 v1.24 推翻** ⇒ **正式作废**（留档见 §1.3.1）。**但本行的几何结论仍成立、且成立得更彻底**：`rowWidth` 只依赖 `TRAY_COLS = 12`（v1.24 明写不动）⇒ 基础态 **24 槽 = 满 2 行 × 12 列 = 零留白** ⇒ 面板**天然无需变宽**。实算（`trayLayout(rows)`、`pitch = TRAY_SLOT + TRAY_GAP = 48 + 6 = 54`、`TRAY_PANEL_PAD = 12`、贴上沿 `TRAY_BAND.yMax = 450`）：<br>· **rows = 2（v1.24 基础态）⇒ `rowWidth = 12×54 − 6 = 642`、`panelW = 642 + 24 = 666`、`panelH = 2×54 − 6 + 24 = 126` ⇒ 面板 `x∈[42,708]`、`y∈[324,450]`、**溢出 0**（带高 220 ≥ 126）✓<br>· 微拱白瓷内阴影尺度**不变**：底缘两段宽 = `panelW − 2×PAD = 642`、右缘段 `x∈[693,696]`（均只依赖 `TRAY_COLS`/`PAD`）⇒ **与 v1.23 前逐字相同**。<br>· **圆角漂移值重算**：`panelH` 由 72 变 **126** ⇒ `radius 18 / 126 = 0.143`（非 1/4）、`24 / 126 = 0.190`；直边段 `666 − 48 = 618` 不变 ⇒ **仍建议以 18 回写本行**（理由不变，但原「1/4」比例论述作废）。<br>· **规格值 `690 × 190` 仍为 `[待 §3 对齐]` 漂移项**（实算 666×126），本批不修正该字面值（属旧欠账，修正需同步 §3.4 与 `art-bible §3`）。<br>· ⚠️ **扩展态（rows = 4）本行不能成立**：`panelH = 4×54 − 6 + 24 = 234 > 220` ⇒ 面板 `y∈[216,450]`、越带下沿 **14px**，并与 `btn_expand` 热区重叠 88px（全高）⇒ **处置见 §1.3.2（本批裁定 V1）与 §1.3.1 作废批注**。 |
| `tray_slot` | 同 BEAD（示例 64，内缩 4） | `empty` 态绘制（§1.2 **v1.5 凹陷四层**，中性 `base` = `palette.slot`、S4 亮线 `#FFFFFF`） |
| `tray_slot_dashed` | 同上 | 描边 `slot_dashed #C9C5DA` 2px，dash 6/4，无填充 → 可扩展位<br>⚠️ **`[v1.5-r4]` 双漂移登记（本批自查，归工程单）**：代码实装为 `drawDashedRect(builder, …, palette.slotBorder)`（`view-model.ts:763`）⇒ **色 token 漂移**（规格 `slotDashed #C9C5DA` vs 实装 `slotBorder #D8D5E6`），且 `lineWidth = 1`（`view-model.ts:869-892`）⇒ **线宽漂移**（规格 2px）。另：`palette.slotDashed`（`palette.ts:40` 声明 / `:69` 赋值）**定义但零消费者**（`grep -rn slotDashed src/ tests/` 仅 2 命中 = 声明与赋值）⇒ **死 token**。处置：本行规格值**不动**（#C9C5DA / 2px 是 `art-bible §3.1` UI token 真源），**漂移归工程单修正**；若工程侧判定 1px 为真机最优 ⇒ 回写本行为 1px 并退役 `slotDashed`（二选一，不得两存）。<br>**`[v1.5-r4]` v1.23 下本行语义升格（新活 1 案 (a′)）**：12 槽 ⇒ `rows ≡ 1` ⇒ 虚线槽由「**第二行**（扩展行）」改读为「**同一行右半 6 列**（扩展余量预告）」⇒ 本行由**边缘态**升为**常驻态**（未扩展时恒有 6 个虚线槽在屏）。⚠️ 连带：`view-model.ts:758` `const dashed = row > 0` 在 v1.23 下**恒假** ⇒ 虚线语言**整体失效**，建议判据改 **`idx >= TRAY_BASE_SLOTS`**（归工程单；本行规格不依赖判据实现，只依赖「可扩展位 = 虚线」这一语义）。<br>**`[v1.5-r6]` v1.24 语义回滚 + 本行降为备用方案（承派单 §二「注记同步回滚」）**：~~由边缘态升为常驻态~~ ⇒ **作废**：v1.24 基础态 = 24 槽 = **满 2 行×12** ⇒ **不存在“同一行右半列”的虚线位**；~~判据改 `idx >= TRAY_BASE_SLOTS`~~ ⇒ **作废**，判据**回到 `row >= 已实装行数`**（与现有 `row > 0` 同义，仅当 `rows > 2` 时才需真判据）。**v1.24 下的虚线语义 = 「扩展预告行」**（预告的是**整行**，不是半行）。⚠️ **P0 落码陷阱（本批新取证、须在 T-143 前送达工程侧）**：`view-model.ts:782` 的 `const dashed = row > 0` 在 v1.24 基础态 `rows = ceil(24 / 12) = 2` 下，**会对 row1 的 12 个实槽恒真** ⇒ **基础态第二行实槽被误画为虚线**（与 `[v1.5-r4]` 所报“恒假”为**同一段代码的反向失效**；旧报的行号 758 现行号 **782**）。⇒ 上线的判据应为 **`row >= TRAY_BASE_SLOTS / TRAY_COLS`（= 2）**，而非 `row > 0`、也非 `idx >= TRAY_BASE_SLOTS`。<br>**本批裁定（详 §1.3.2 V1）**：逐槽虚线（24 个）**不默认绘制**，改用**单条「扩展预告胶囊」**（图元 2–4 vs 逐槽 24 层 / **480 条 `line`**）⇒ **本行降为备用方案**（若真机判定胶囊语义不够直白、或需逐槽对齐广告收益预期时回升）；规格值（`slot_dashed #C9C5DA` 2px、dash 6/4）**不动**，双漂移仍归工程单。 |
| `btn_expand` | 132 × 48，圆角 24 | 底 `#2A2E43`，白色 ▶ 三角 12px + 「扩展」28px 白字（参考图同款）。**落位（WXG-T-097/BD-15）**：`TRAY_BAND` 带下沿居底，视觉 y∈[250,298] / 热区 y∈[230,318]（源 `systems-index §3.4` v1.20 + `accessibility C1`）；右上角常驻 `ad_badge`（参数继承 §1.4 同 token：28×28 圆角 8、白 ▶ 边 10、内缩 (8,8)）。本节未定的两项已标为派生：▶ 与文字间隔 12px、「扩展」宽 56px（CJK 等宽近似，RenderModel 无文本度量），见 `tuning.ts::EXPAND_BTN_*`。<br>**`[v1.5-r4]` v1.23 下的净空自动改善（本批复算）**：`systems-index §3.4` 本行说明列写「与扩展态最上排槽热区实测净空 **11px**」——该值是 **rows=2**（扩展后 24 槽）的实测：`slotCenterY(1) = 450−12−24−54 = 360` ⇒ 槽热区（`TRAY_HIT_SIZE 62`）`y∈[329,391]` ⇒ `329 − 318 = **11px**` ✓。**v1.23 后 12 槽 ⇒ `rows ≡ 1`** ⇒ 唯一排 `slotCenterY(0) = 414` ⇒ 热区 `y∈[383,445]` ⇒ 净空 = `383 − 318 = **65px**`（**提升 54px**）。⇒ **本行规格与热区零改动**（v1.23 明写「`btn_expand` 热区不动」），仅「净空 11px」这一**派生说明值**在工程落码后变 65px ⇒ 已列入跨域回写建议（`accessibility.md §4` 第 9 条）。另：面板底边口径的净空（非 §3.4 所用口径）= rows1 `378−318 = 60px` / rows2 `324−318 = 6px` ⇒ **两个「净空」口径不同、不得互证**（本批自查旧稿曾误用 6px，已纠正）。<br>**`[v1.5-r6]` v1.24 回写：上述「65px 自动改善」作废，且净空变负**：v1.24 使 `rows` 回到 **2** ⇒ 唯一排假设（`rows ≡ 1`）不成立 ⇒ **实测净空回到 11px**（`slotCenterY(1) = 450 − 12 − 24 − 54 = 360` ⇒ 热区 `[329,391]` ⇒ `329 − 318 = 11`）⇒ **§3.4 现值反而正确**，但措辞错：该 11px 是 **基础态最下排（row1）**的热区净空，**不是“扩展态最上排”**（§3.4 行原文措辞需主理人串行修正，已入清单）。⚠️ **真正的风险在扩展态（rows = 4）**，本批逐行实算（槽心 `414 / 360 / 306 / 252`，槽视觉 ±24、槽热区 `TRAY_HIT_SIZE 62` ⇒ ±31）：<br>· `row2` 槽心 **306** ⇒ 视觉 `[282,330]`、热区 `[275,337]` ⇒ **与按钮热区 `[230,318]` 重叠 43px（占热区高 88 的 49%）、与按钮视觉 `[250,298]` 重叠 16px**；<br>· `row3` 槽心 **252** ⇒ 视觉 `[228,276]`、热区 `[221,283]` ⇒ **与按钮热区重叠 53px（60%）、与视觉重叠 26px**；面板整体 `[216,450]` 与热区 `[230,318]` **重叠 88px = 热区全高**。<br>· 列向：按钮 `x∈[309,441]`（宽 132）⇒ **完全吞掉 col5(366)/col6(420) 两列槽心所在列**（该两列 row2/row3 共 4 个槽的热区被按钮覆盖），col4(312)/col7(474) 各侵入 16px 热区（视觉 9px）。<br>· ⇒ **本行与槽热区/面板的重叠量不是“微改善”，而是扩展态下物理不可行**；**零 §3 变更的出路只有“扩展态收起按钮”**（本行与视觉/热区同时从 `[230,318]` 消失 ⇒ 重叠归零）⇒ 即 §1.3.2 裁定 **V1**。**本行规格值与热区仍零改动**（v1.24 明写“`btn_expand` 热区不动”），改的是**可见性条件**（新增呈现层参数：建议 `tuning.ts` `EXPAND_BTN_VISIBLE_IN_EXPANDED = false`，默认 **false** ⇒ 扩展后收起；另建 `TRAY_EXPAND_HINT_CAPSULE = {h: 22, yMin: 300, yMax: 322}`）。 |

#### 1.3.1 `[v1.5-r4]` 新活 1 · 托盘面板随 6 槽收窄的三案比较与裁定（v1.23 §2-1 派给 WXG-T-131）—— `[v1.5-r6]` ⛔ **整节作废留档（前提被 v1.24 推翻；不静默删除）**

> ⛔ **作废声明（`[v1.5-r6]`，2026-09-17，林绘澄）**：本节的**公共前提**「`TRAY_BASE_SLOTS = 6` ⇒ 12 列只填 6 槽 ⇒ **右半 324px 留白**」已由 `systems-index §3.4` L113 **v1.24（WXG-T-141，用户 2026-09-17 拍板）** 推翻（基础容量 **24 = 满 2 行×12 ⇒ 零留白**）⇒ 案 (a′) 的采纳裁定、案 (b)/(c) 的驳回理由、“净空 65px”派生值、以及“6 个虚位 = 看广告能拿到的 6 槽”的语义均**失去承载对象** ⇒ **整节作废**。按本仓「作废留档划线」纪律（承 `[v1.5-r5]` 对 (b) 描边环、`[v1.5-r4]` 对 (b) 收窄案的处置写法）：**原文逐字保留、以划线与批注标记**，不删除、不改写。
> **仍然有效的三条副产品**（作废裁定 ≠ 作废事实，本批 §1.3.2 直接引用）：① `rowWidth` **只依赖 `TRAY_COLS`、不依赖容量**的几何独立性证明 ⇒ v1.24 下直接推出「面板无需变宽」；② `drawDashedRect` 的**命令级代价口径**（每槽 20 条 `line`，本节首次给出）⇒ 本批否定“逐槽 24 虚线”的量化依据；③ 「渲染与命中**共用 `trayLayout()` 单一真源** ⇒ 动 `slotCenterX` 风险最高」的**架构约束** ⇒ 本批 V3（滚窗）否决理由。
> **历史原文（以下不再作为现行规格）：**

> **核心设计矛盾**：`TRAY_COLS = 12` **不动**（`systems-index §3.4` L115），但基础容量只有 **6** 槽 ⇒ **6 个槽摆在 12 列的网格上，右半 6 列是什么？** 下表为三案的几何/图元/连带成本全量比较（几何均由 `trayLayout()` 同式推出，可逐条复算）。

| 案 | 面板几何 | 槽簇 / 留白 | `btn_expand` 关系 | 微拱白瓷内阴影 | 图元增量（规格层口径） | 新动画？ | 裁定 |
|---|---|---|---|---|---|---|---|
| **(a) 满宽 + 右侧纯留白** | `x∈[42,708]` 宽 **666**、`y∈[378,450]` 高 **72**（`rows≡1`） | 槽簇 `x∈[54,372]` 宽 **318**；**右侧留白 324px** | 热区零改动；净空 **65px** | 底缘两段宽 **642**、右缘 x **693..696**（零改动） | **+0** | 无 | ⚠️ **部分可取**：零成本，但 324px 白板**读作「空旷」而非「余量」**⇒ **重演 v1.3 的 F2** |
| **(a′) 满宽 + 6 实槽居左 + 右半 6 个虚线预告槽** | 同 (a)（**`trayLayout` 一字不改**） | 实槽 `x∈[54,372]`；虚线槽 `x∈[378,696]`（col 6..11）⇒ **留白 0** | 同 (a)：热区零改动、净空 **65px** | 同 (a)：642 / 693（**零改动**） | **+6 层**（虚线槽按规格 1 层/槽）⇒ ③u **1606**；builder 命令口径 = 6×**20 line** = **120**（`drawDashedRect` 每槽 5 段/边×2 边×2 方向） | 无 | ~~✅ **采纳**~~ ⇒ ⛔ **`[v1.5-r6]` 作废** |
| **(b) 面板收窄到 6 槽宽** | `rowWidth = 6×54−6 = 318`、`panelW = **342**`、`left = 216` ⇒ `x∈[204,546]`；**扩展后 12 槽 ⇒ `panelW` 回到 666、`x` 跳回 [42,708]** | 槽簇满板；留白 0 | 热区不动（`x = (750−132)/2 = 309`，宽 132）⇒ 按钮恒居中、面板变宽时**按钮不动 ⇒ 视觉脱节** | 底缘 **318**、右缘 x **531..534**；**扩展后跳到 642 / 693** ⇒ 两者成为**逐帧量** | 静态 **+0**，但需**面板变宽动画**（宽度变化 **324px**） | ⛔ **需新动画**（`ux-spec §5` **缺行**） | ❌ **驳回** |
| **(c) 6 槽居中 + 两侧对称留白** | 满宽 666（同 (a)） | 槽簇 `x∈[216,534]`；**两侧各 162px 留白** | 同 (a) | 底缘 642 / 右缘 693，但**与槽簇不再同宽对齐** ⇒ 「光从板缘来」读作「光从空处来」 | (c1) 纯留白 **+0**；(c2) 两侧各 3 虚线槽 **+6** | 无 | ❌ **驳回** |

~~**裁定建议 = 案 (a′)**~~ ⇒ ⛔ **`[v1.5-r6]` 作废**。以下五条理由**原文保留供追溯**（第 4 条“净空 11px → 65px”**已被 v1.24 推翻**（`rows` 回到 2 ⇒ 仍是 11px）；第 5 条“+6 层 / ③u **1606**”的 **6 槽口径已失效**（v1.24 下逐槽预告 = 24 层 / **480 line**）；**第 1/2/3 条的几何独立性、零新动画、单一真源论证仍然有效**并被 §1.3.2 直接复用）：

1. **零几何改动、零新动画**：`rowWidth` 只依赖 `TRAY_COLS`（v1.23 明写**不动**）⇒ `trayLayout()` **一字不改**，面板 `666×72` / `x∈[42,708]` / `y∈[378,450]` 与 v1.23 前**逐字相同** ⇒ **`ux-spec §5` 无需增行**（案 (b) 需「面板动态变宽」行，主理人需串行落笔）。且**渲染与命中共用 `trayLayout()` 单一真源**（`tuning.ts:600-604` 注释明写「各写一份就会出现「画出来的槽」与「点击落点」不一致的静默漂移」）⇒ 案 (c) 要改 `slotCenterX` 就是**动这个单一真源**，风险最高。
2. **直接消解 F2「构图空旷」，且不重演 v1.3 对策**：v1.3 为 F2 做过容器板 + 暖光 band（§1.7），但那是**拼图区**的对策；托盘区若再现 324px 白板，用同一对策（给托盘加 band）会**与拼图区争焦点**（违铁律 1「一屏一焦点」）。案 (a′) 用**已存在、已入库、零新 token** 的 `tray_slot_dashed` 语言填这 324px ⇒ **零新元素**即解。
3. **把「可扩展余量」变成可读预告（正向语义）**：6 个虚位 = 「看广告能拿到的 6 槽」⇒ `btn_expand` 的**价值可视化**（现在按钮只有文字「扩展」，无空间锚点）。且虚线语言本就是为此而设（§1.3 `tray_slot_dashed` 行原注「→ 可扩展位」）⇒ **不是新发明，是把既有语义从第二行搬到右半行**。
4. **`btn_expand` 净空自动改善 11px → 65px**（详 §1.3 本行注）⇒ C1 触控间距余量变宽，**案 (b) 反而使按钮与面板脱节**（按钮恒居中、面板宽度变）。
5. **图元代价最小且已入账**：**+6 层**（规格口径）⇒ 未扩展基线 ③u = **1606**（§1.6.9 / §1.8 已列）；builder 命令口径 = **120 line**（`drawDashedRect` 每槽 20 命令）⇒ ⚠️ **命令级远高于层级**（承 A5 ⑧ 口径注：本表图元 = 规格层数，命令级是**偏乐观**口径的另一面）⇒ 真机承压时**虚线槽是静态回退阀 S2 的首选目标**（详 §1.9.5 末，−120 命令 / −6 层，零玩法语义损失）。

**案 (a) 与 (a′) 的唯一差别 = 那 6 个虚线槽**；若真机判定 120 条 `line` 命令不可承受，**退到案 (a)** 的代价是 F2 重现 ⇒ 因此**退让顺序必须是「先降虚线段密度（dash 6/4 → 8/6，命令数 −25%）、再降为 1px、最后才整槽取消」**，不得直接跳到 (a)。

**连带工程项（归工程单，本单不改代码）**：① `view-model.ts:758` `const dashed = row > 0` ⇒ v1.23 下**恒假**，改 `idx >= TRAY_BASE_SLOTS`；② `palette.slotDashed` 死 token 与 `drawDashedRect` 的色/线宽双漂移（详 §1.3 该行注）；③ 案 (a′) 要求 `snap.traySlots.length` **恒为 `TRAY_COLS` 的整数倍或至少覆盖 12 位**（否则右半 6 列无槽可画）⇒ 需核实 `tray.ts` 扩容后 `traySlots` 是否**预分配 12 位**（`state = 'free'`）还是**只推 6 位**；若只推 6 位，则虚线预告槽需由 view 侧**按 `TRAY_COLS − length` 派生绘制**（零玩法状态，合 L5）——**该核实项属 `src/**` 域，本单只登记不落笔**。

#### 1.3.2 `[v1.5-r6]` 托盘 4 行态（2 实 + 2 虚）带高 / `btn_expand` 热区重验（§3.4 L113 交付项；v1.24 24/48 槽）

> **本节是什么**：`systems-index §3.4` L113 原文自带欠账「**4 行态（2 实+2 虚）带高/`btn_expand` 热区需重验，归 art/ux 后续单**」，L118 又明写「**本行与 `btn_expand` 几何行的 y 值全部仅适用 1 行态，4 行态重验前不作为 QA 判据**」⇒ **本批即该解锁项的 art 侧交付**。

**前提（全部引用冻结真源 + 代码实测，不发明值）**

| 量 | 值 | 源 |
|---|---|---|
| `TRAY_BASE_SLOTS` / `TRAY_EXPAND_SLOTS` | **24 / 24**（扩展后共 **48 槽 = 4 行**） | §3.4 L113/L114（v1.24 冻结） |
| `TRAY_COLS` / `TRAY_SLOT` | **12 / 48**（不动） | §3.4 L115/L116 |
| `TRAY_GAP` ⇒ `pitch` | 6 ⇒ **54** | `tuning.ts:147` |
| `TRAY_PANEL_PAD` | **12**（**非 §3 冻结**，属 `tuning.ts` 呈现层） | `tuning.ts:149` |
| `TRAY_BAND` | y∈**[230,450]** ⇒ 带高 **220**，面板**贴上沿** | §3.4 L80（v1.20 上沿 420→450） |
| `btn_expand` | 热区 y∈**[230,318]**（132×**88**）/ 视觉 y∈**[250,298]**，x∈[309,441] | §3.4 L119 + `accessibility C1` |
| `TRAY_HIT_SIZE` | **62**（槽热区边长） | `tuning.ts:309` |
| `POWERUP_BAND` | y∈[48,200] | §3.4 L81 |
| ⚠️ **代码漂移** | `tuning.ts:136/:141` 仍为 **12 / 12**（行注已自记「v1.24 变更 12→24，工程侧尚未适配 ⇒ 当前仍生效 12」）⇒ **规格/代码漂移中**（承 §1.9.5「规格 2 / 代码真值 1」判例）⇒ 本表结论以**规格口径（24/24）为准**，代码口径另列。<br>🔍 **`[v1.5-r6]` 本批新取证（不改变任何结论，但把风险边界说清）**：行数在代码侧**不读 `TRAY_BASE_SLOTS`**，而是派生：`view-model.ts:762` ⇒ **`rows = ceil(traySlots.length / TRAY_COLS) = ceil(len / 12)`**（`traySlots.length ∈ {12, 24}`，`crash-snapshot.ts:210-211` 已断言）⇒ 代码口径 **基础态 rows=1（panelH 72）／扩展态 rows=2（panelH 126）⇒ 全部可行 ✓**。<br>⇒ **两个结论**：① 本节的不可行**不发生在现码**，只在 **T-143 把常量切到 24/24 的那一帧**发生（`len ∈ {24, 48} ⇒ rows ∈ {2, 4}`）；② **代码扩展态（24 槽 / rows=2）与规格基础态（24 槽 / rows=2）是同一个几何构型** ⇒ §1.6.9 图元基线里两个口径的 **N=24 同值不是巧合而是同构型**（`1828`），差异仅在**容量语义**（前者 = 已扩展、后者 = 根态）与**是否存在逐槽虚线预告**。 |

**算式（均取 `trayLayout()` 同式，可逐条复算）**

```
rowWidth(row)  = TRAY_COLS × 54 − 6            = 12×54 − 6  = 642
panelW         = rowWidth + 2×12                                = 666   ⇒ x ∈ [42, 708]
panelH(rows)   = rows × 54 − 6 + 2×12         = rows×54 + 18
panelBottom    = TRAY_BAND.yMax − panelH      = 450 − panelH          （贴上沿）
slotCenterY(r) = 450 − 12 − 24 − 54r          = 414 − 54r
slotCenterX(c) = 54 + 24 + 54c                = 78 + 54c
可行上界（硬不等式）：panelH ≤ TRAY_BAND.yMax − 热区上界 = 450 − 318 = 132
                  ⇒ rows ≤ (132 − 18) / 54 = 2.11  ⇒ rows(max) = 2
```

**逐行实测（rows = 2 基础态 / rows = 4 = §3.4 L113 的“2 实 + 2 虚”期望态）**

| 行 | 槽心 y | 槽视觉 y（±24） | 槽热区 y（±31） | vs 热区 [230,318] 重叠 | vs 视觉 [250,298] 重叠 | 在场条件 |
|---|---|---|---|---|---|---|
| row0 | **414** | [390,438] | [383,445] | **0** | 0 | 基础态 ✓ |
| row1 | **360** | [336,384] | [329,391] | **0**（净空 11px） | 0 | 基础态 ✓（v1.24 新增） |
| row2 | **306** | [282,330] | [275,337] | **43px（= 热区高 88 的 49%）** | **16px** | “2 实+2 虚”的**第一条虚线行** |
| row3 | **252** | [228,276] | [221,283] | **53px（60%）**；**面板 y∈[216,450] 与本区重叠 88px = 热区全高** | **26px** | 第二条虚线行 |

⇒ **三条硬结论（数学层面，不依赖审美判断）**：
> 1. **`rows = 4` 在现冻结几何下物理不可行**：`panelH = 234 > 220` ⇒ 越带下沿 **14px**；且与 `btn_expand` 热区重叠 **88px = 热区全高**（与派单粗算逐位一致 ✓）。
> 2. **锚定方向不救援**：改「贴带下沿」⇒ 面板 `y∈[230,464]`、越带上沿 14px 进入 450..480 的间隙（距 `PUZZLE_BAND` 底 480 尚余 16px），但按钮热区 `[230,318]` 仍被面板**完全内部包含** ⇒ 重叠不变。⇒ **两种锚定下 rows=4 均无解**。
> 3. **带内剩余空间不可能容下“4 行槽 + 按钮”**：按钮上方可用高 = `450 − 318 = 132`，而 4 行槽簇高 = `4×54 − 6 = 210` ⇒ **132 < 210**，差 **78px**。⇒ “把按钮往上面摆”也不是自由度问题而是**无空间**（上方 450..480 仅 30px < 48 视觉 < 88 热区；下方 200..230 仅 30px，且再下就是道具带）。

**候选比较（逐格给出派单要求的五项：`panelH` / 面板 y / 与热区重叠 / 与视觉重叠 / 需 §3 变更？ / 需 `ux-spec §5` 增行？ / 图元 ±）**

| 案 | 做法 | `panelH` | 面板 y 区间 | vs 热区 [230,318] | vs 视觉 [250,298] | 需 §3 变更？ | 需 §5 增行？ | 图元 ± | 裁定 |
|---|---|---|---|---|---|---|---|---|---|
| **V1** | **基础态只画 2 实线行（不常驻虚线行）；扩展态 `btn_expand` 收起**（面板越带 14px 作为已知情形接受）+ 预告改由按钮文案承担 | 基础 **126** / 扩展 **234** | 基础 [324,450] / 扩展 **[216,450]** | 基础 **0** / 扩展 **0（因按钮收起）** | 同左 **0** | **否（数值零变更）**；仅需 §3.4 **L113 说明列措辞回写**（“2 实+2 虚”→“2 实 + 非逐槽预告”）+ L118「4 行重验」欠账关项 | **否**（扩展态明令**瞬时切换、零动效**；若做按钮淡出/面板生长则**需增行** ⇒ 本裁定禁止） | 基础 **±0**（−24 层逐槽虚线 ⇒ 改为 **0～+2**）/ 扩展 **−3**（按钮 rect+polygon+text 退场） | ✅ **本批建议** |
| **V2** | `TRAY_PANEL_PAD` **12 → 5** 压面板高度 | 扩展 **220**（= 恰等带高） | [230,450] | **仍 88px（吞热区未解）** | 仍全包含 | **否**（`PAD` 非 §3 冻结） | 否 | ±0 | ❌ **驳回**：只修越带 14px、**不修吞按钮（致命项仍在）**；且槽外缘距板缘仅 5px（< 珠半宽 24）⇒ 珠贴板缘、**微拱白瓷内阴影读感塌**（底缘两段宽 642→632、右缘 693→686） |
| **V3** | 扩展行改**滚窗 / 分页**（视口恒 2 行） | **126**（固定） | [324,450] | **0** | **0** | 否（几何不动） | **是**（需「托盘翻页/滚动」动效行 + `ux §3` 输入矩阵新行） | ±0，但需分页指示器 **+1～2** | ❌ **驳回**：① 破坏「**画出的槽 = 点击落点**」单一真源（§1.3.1 副产品 ③）；② 与 v1.24 本体语义相左（L113：「24 槽 = **从容批量取回**，托盘回归纯缓冲/工作台」+「v1.23 小托盘滚窗手感论述**作废**」）⇒ **采 V3 = 把已被用户作废的滚窗手感请回来**；③ 需 §5 增行 |
| **V4** | **动 §3 冻结常量**（三子案 4a 加高 `TRAY_BAND` / 4b `btn_expand` 移出托盘带 / 4c `TRAY_COLS` 12→24 或 `TRAY_SLOT` 缩小） | — | — | 可归零 | 可归零 | ⛔ **是（多阶）** | 待定 | 待定 | ⛔ **不在本文档下判**（见下） |

**V4 的子案算式（仅供主理人变更单使用，本批不作裁定）**
> · **4a 加高 `TRAY_BAND`**：要「4 行面板 + 按钮」上下堆叠互不重叠 ⇒ 带高 ≥ `234 + 48 + 6 = 288`（**不遮视觉**）或 ≥ `234 + 88 + 6 = 328`（**不撞热区**）⇒ 当前 220，缺口 **68 ～ 108**px ⇒ `yMin ≤ 450 − 328 = 122 < POWERUP_BAND.yMax 200` ⇒ **必顶道具带**，需同时改两条冻结常量。⚠️ §3.4 L118 已有同族自证（旧口径）：「带加高不能解决重叠：…需带高 ≥ 292 ⇒ 上沿撞 12 行关最底行格热区（481）✗」⇒ **该条虽是在“面板居中”旧假设下算的（已作废），但其“撞上方带”的结论方向仍适用**，本批的新值是 **288 / 328**。
> · **4b `btn_expand` 移出托盘带**：上方空隙 450..480 = **30px**、下方 200..230 = **30px**，均 < 视觉 48 且 ≪ 热区 **88** ⇒ **无落位**；若再外推则必撞 `PUZZLE_BAND` / `POWERUP_BAND` 或触底部安全区。
> · **4c 加列减槽宽**：`TRAY_COLS` 12→24 ⇒ `rowWidth = 24×54 − 6 = 1290 > 750` ✗；若为守住 750 而 `TRAY_SLOT ≤ 750/24 − 6 = 25.5` ⇒ 槽视觉 25.5 ≪ `TOUCH_MIN 88` 且邻槽热区（若扩到 88）**互相吞** ⇒ **违 `accessibility C1`** ✗。
> · **4d 减扩展容量（`TRAY_EXPAND_SLOTS` 24→12，扩展 = 3 行）**：`panelH = 3×54+18 = 180 ≤ 220` ⇒ **不越带 ✓**，但 3 行槽簇 `[270,450]` 仍与热区 `[230,318]` 重叠 **48px** ⇒ **仍不可行**；且属**容量数值变更（玩法域）** ⇒ 不归 art。
> ⇒ **四条子案均需主理人走 §3 变更单 + 用户拍板；本档不下论断**（派单 §二 铁律）。

**V1 规格细则（本批交付的可直接落码口径）**

| 项 | 值 | 佐证 / 图元 |
|---|---|---|
| 基础态槽行 | **只画 row0/row1（2 实线行）** | 面板 `x∈[42,708]`、`y∈[324,450]`、`panelH 126`；与按钮热区净空 **11px** |
| 扩展预告 | **不画逐槽虚线**；由 `btn_expand` 的**文案**承担（建议「扩展 +24 槽」） | 图元 **±0**（仅改 `text` 内容）。⚠️ 文案属 `ux` / UI 文案域 ⇒ **已列入回传④串行清单，本批不自写** |
| 预告备选（若真机判定文案不足） | 单条 **扩展预告胶囊**：`x∈[255,495]`（宽 240、居中）、`y∈[300,322]`（高 **22**）、ink `slot_dashed`、**不可点**（热区零改动） | 可用带宽验：面板顶 324 与按钮顶 298 之间只有 **26px** ⇒ 22 高胶囊居中得 **2px/侧**（`[待真机]` 是否拥挤）。图元：不带角标 **+2**（1 `rect` + 1 `text`）；带 `ad_badge`（§1.4 同 token）**+4**。⇒ 基础态 ③ **1674/1676** / ④ **1830/1832** |
| 逐槽虚线（已降为备用） | 24 个（row2/row3 全画） | ⛔ **不默认**：越带 14px + 吞热区 88px + 图元 **+24 层 / 480 条 `line`**（每槽 20 命令，承 §1.3.1 副产品 ②）⇒ 基础态 ③ **1696** / ④ **1852**；若回升必须先走 V4 变更单 |
| 扩展态 | 4 行（48 槽）+ **按钮收起** + **瞬时切换（零动效）** | 面板 `y∈[216,450]`；越带 **14px**；`POWERUP_BAND` 余 **16px**（216−200）；底部安全区（设计空间高 1334，y=216 即屏底上 216px）**不触** ✓ |
| 新增呈现层参数（**零 §3 变更**） | `tuning.ts`：`EXPAND_BTN_VISIBLE_IN_EXPANDED = false`、`TRAY_EXPAND_HINT_CAPSULE = { x: 255, w: 240, yMin: 300, yMax: 322 }` | 两值均为**非冻结**，与 `EXPAND_BTN_*` / `TRAY_PANEL_PAD` 同层 |

**本批结论（一句话）**：**基础态（24 槽）完全可行 ✓ 零变更**；**扩展态（48 槽）在冻结几何下无解（不论如何锚定）** ⇒ 唯一零 §3 变更出路 = **V1（扩展态收起按钮 + 瞬时切换）**；而 §3.4 L113 所期望的「**常驻 2 实 + 2 虚**」与「`btn_expand` 热区不动」**两事直接互斥** ⇒ 这是一个**需用户拍板的真两难**（已列入回传⑤，我给推荐、不拍板）。

### 1.4 道具图标（64 × 64，**靠形状区分**）

| # | 名称 | 对应道具 | 图标形状（程序化 path） | 主色 |
|---|---|---|---|---|
| 1 | `item_area_clear` | 区域消除 | 斜置魔法棒（矩形 6×40 旋转 −45°）+ 顶端五角星 r=12 | 棒 `#8E6FD9` / 星 `#FFD23F` |
| 2 | `item_tray_clear` | 槽位清空 | 扫帚：柄（矩形 5×34 旋转 −30° `#A5652C`）+ 刷毛扇形（`#FFD23F`，3 条分缝线） | `#FFD23F` |
| 3 | `item_random_clear` | 随机消除 | 马蹄磁铁：U 形（外弧 r=18/内弧 r=8）+ 两极白/浅蓝端帽 | `#E84C3D` / 端帽 `#D8D5E6` |
| 4 | `item_reserved_04` | `[待 GDD 对齐]` | 预留位，不实装 | — |

- 图标底：白卡 `#FFFFFF` 圆角 20（卡 **176 × 116**），描边 `panel_border` 1px，投影 α0.10。
  - ⚠️ **卡高 150 → 116（WXG-T-062 主理人裁定，方案 A「改卡高、不动带位」）**：原「176×150 + **卡下方**标签 28px」需 182 > `systems-index §3.1` 的 `POWERUP_BAND` 高 **152**，两条冻结规格**无法同时成立**。取 116 + 间隔 4 + 标签 28 = **148 ≤ 152** ⇒ `§3.1` 与底部留白 48 **一字不动**，A4 的「文字标签并列」得以落地。工程侧同源常量见 `tuning.ts` 的 `POWERUP_CARD_*` / `POWERUP_LABEL_*`。
- 卡下方标签：28px **`text_secondary`（`#6E7288`，on 白 ≈4.75:1）**（「区域消除 / 槽位清空 / 随机消除」），与卡间隔 **4px**；卡内「×n」次数同 28px `text_secondary`。 `[v1.3·F7④]` **禁用 `textDim`**（on 白 ≈3.7:1 < 4.5:1，accessibility B1 假绿根源）；不升 32px 是因 `POWERUP_BAND` 高 152 冻结（116+4+32=152 零余量），**改色为唯一合规解**。
- **视频角标 `ad_badge`**：28 × 28 圆角 8，底 **`#2A2E43` 深藏青**，白色 ▶（边 10px），贴卡**右上角**内缩 (8,8)。 `[v1.3·F6]` 代码亮黄 `#FFCB3D` 作废，以本行深藏青为准（palette.adBadge 同步）。
- 触控热区：整卡 ≥ 88 × 88（实际 **176×116** 达标）。

### 1.5 HUD 元素

| 名称 | 规格 | 参数 |
|---|---|---|
| `hud_timer_capsule` | 220 × 64，圆角 32，中心 (375, HUD_BAND 中线) | **白胶囊板落地（F7①）**，层序：① 投影 roundRect 偏移 (0,−2) `#1E2033` α0.10 → ② fill `panel_surface` → ③ stroke `panel_border` 1px；内：时钟图标中心 (375−78, midY) Ø36（环 r16 stroke 3 + 分针 (0,+9) + 时针 (+7,0) stroke 3，`accent_blue`）+ 数字中心 (375+12, midY) 44px `text_primary`；告急时环/针/数字同切 `danger` + α 脉冲 |
| `hud_timer_danger` | 同上 | 数字/图标切 `#E8434A` + 1000ms α 脉冲**循环**（周期/时长以 `ux-spec §5`「倒计时告急」行为权威，与 `ux-spec §3.1` HUD 告急条款同源；α 幅度两文均未定义 → 本表不自行发明。原 `[待 ux-spec 对齐]` 占位经 WXG-T-098 删除） |
| `tray_panel_danger` | 沿 `tray_panel` 边缘 2px 描边（圆角随面板实装值 18） | `danger #E8434A` + **500ms α 呼吸循环**（周期以 `ux-spec §5`「满槽告警」行为权威 = 500ms/循环，≈2Hz 在 §3.8 ≤3Hz 红线内；α 幅度 ux-spec 未定 ⇒ **沿用同族既有实现值 0.6↔1.0**（`view-model.ts::dangerAlpha()` 已用的那组，本行不新造第三档；同上行口径，若 ux-spec 日后冻结幅度则以 ux-spec 为准）。WXG-T-097/BD-10 落地；`reduceMotion` 下退为**静态描边 α=1**，见 `accessibility` D1） |
| `btn_settings` | 图标 Ø48，中心 (60, midY)，热区 88×88 | **8 齿齿轮落地（F7②）**，层序：① hub 圆 r13 fill `accent_purple` → ② 8 齿 line（角度 k×45°，k=0..7，r13→r21，lineWidth 6，`accent_purple`）→ ③ 中心孔 圆 r5 fill `panel_surface`；外径 2×(21+3)=48 ✓；**v1.2「circle+中心点」无齿实装作废** |
| HUD 图标笔画纪律 | — | 图标主笔画 ∈ {3,6}：Ø36 级（时钟环/针）=3、Ø48 级（齿轮齿）=6；面板描边 1 / 状态环 2 不属图标笔画；**禁止 1–2px 图标主笔画**（F7③ 粗细不统一根治） |
| `hud_mode_label` | 28px（原 22px 作废） | 冷底小字用 `text_primary`（on `bg_base` ≈11.2:1）；`largeText` 下 35px（F7⑤ / accessibility E1 最小 28 floor） |
| `panel_dialog` | 560 × 480，圆角 24 | 白 + `panel_border` + 投影（弹窗/结算） |
| `btn_primary` | 240 × 88，圆角 20 | 底 **`accent_primary #2A2E43`**（v1.3 F6：`accent_blue` 让位给环状交互提示），白字 32px |
| `text_body` | 字号 32（**最小 28**） | `text_secondary` |

### 1.6 VFX 参数卡（**v1.4 全量升级**：4 行简表 → 7 张可直接落码卡）

> **升级理由**：v1.3 的 §1.6 只有四行一句话描述（如「scale 1.06→1.0，120ms ease-out-back」），既无逐帧分段、无联动层、无 clamp、无回退阀，也无缓动定义（渲染层**无缓动库**）⇒ 工程无法照此落码，结果就是 G1–G4 **四条全欠账**。v1.4 按 §1.1/§1.7 的写法范式（常量块 + 逐层/逐帧公式 + 层序 + clamp）重写。
>
> **三条硬纪律**：① **毫秒真源 = `ux-spec §5`**，本表只抄不改（G6/G7 的真源行已由 ux 域**增行落地**：`ux-spec §5`「**结算彩带**」800 / 「**不可填格轻压**」120（WXG-T-128），两行值均**复用既有行值**、不新造时长 ⇒ **已有源**）；② **色值零新增**，全部引 `art-bible §3` / `palette.ts` 既有 token；③ **新增常量全部归 `src/config/tuning.ts` 呈现层**，**不进** `systems-index §3`（承 v1.3 `PLATE_OUTSET`/`GLOW_BAND_ALPHAS` 判例注：「与 `WRONG_*` 判例同族：反馈/呈现层参数」）。⚠️ ③ 的**唯一跨域例外**：裁定 3 的 `sfx_denied` 连带 `§3.12` `AUDIO_CLIP_TOTAL` **19→20**（冻结值变更单，属音频/GDD 域、随工程原子批，见 §1.6.7）。
>
> **缓动定义（全 §1.6 共用；渲染层无缓动库 ⇒ 必须显式给式）**：
> ```
> clamp01(x)   = x < 0 ? 0 : (x > 1 ? 1 : x)
> easeOut(x)   = 1 − (1 − clamp01(x))²          // 二次，起快末慢 =「到达」
> easeIn(x)    = clamp01(x)²                     // 二次，起慢末快 =「被抽走」
> easeInOut(x) = x < 0.5 ? 2x² : 1 − (−2x + 2)²/2
> ```
> 所有逐帧函数必须是**纯函数 + 零分配**（承 `wrongFlashAlpha(p)` / `comboParticleOffsets(progress)` 判例），禁 `new`/`map`/`filter`/展开造集合。落码骨架见 §1.6.10。
>
> **排版约定**：以下参数卡为等宽代码块（便于直接对照落码），块内 Markdown **不渲染** ⇒ 块内的 `**…**` 一律读作「强调/硬约束」标记，不是字面内容。

#### 1.6.1 `vfx_fill_pop` — 珠子落座回弹 `[v1.4·G1]` **P0**

```
常量（tuning.ts；不进 systems-index §3）：
  FILL_POP_MS              = 120    // ux-spec §5「珠子落座」，冻结，不改
  FILL_POP_PRESS_MS        = 40     // 压下段时长（逐帧参数，v1.4 新增）
  FILL_POP_SCALE_START     = 1.06   // v1.3 既有起点值（**规格起点，非过冲量**；裁定 7）
  FILL_POP_SCALE_TROUGH    = 0.96   // 压下谷（v1.4 新增**中间插值**；§5 只冻结总时长与起止值 ⇒ 不违 §5）
  FILL_POP_CONTACT_A_PEAK  = 0.18   // L0a α 峰（静息 0.12 = BEAD_CONTACT_SHADOW_ALPHA）
  FILL_POP_CONTACT_W_PEAK  = 0.92   // L0a 宽比峰（静息 0.88 = BEAD_CARD.contactW）
  FILL_POP_SHADOW_A_TROUGH = 0.12   // L0b α 谷（静息 0.15 = BEAD_SHADOW_ALPHA）
  FILL_POP_SHADOW_DY_MIN   = 2     // L0b 偏移分子谷（静息 3，/64 归一）
  FILL_POP_RESTART_GATE_MS = 120    // = 动画自身长；防连点堆叠（同 _wrongFx 重启门判例）

触发：`bead:placed` 事件（**代码事实：当前 view 侧零消费者 = G1 P0**）
作用对象：刚填入的**网格 filled 珠，单颗**（格几何 `BEAD_CELL 50`）
  ⚠️ **`[v1.5-r6]` scale 作用基准钉死 = 46（不是 50）**：B′（WXG-T-142）后珠体 draw = `outer − 2×BEAD_DRAW_INSET = 50 − 4 = 46`，
     且 `view-model.ts:731` 网格侧**不传 `size`**（`outer ≡ BEAD_CELL`）⇒ **46 由既有实现唯一确定、非本批新裁**（派单 §三-1① 的两条路已裁定）：
       · **基准 46（采）**：峰宽 46×1.06 = **48.76 < 50 ⇒ 珠永不出格**（动画期仍在自家格内）；
       · 基准 50（驳）：峰宽 **53.00** ⇒ 越格 **1.50px/侧** 盖邻格，且动画结束后珠需**跳回 46** ⇒ 静息不连续
         ⇒ **`[v1.5-r2]` 全卡隐含以 50 为基准 = 本批要修的错**（诚实登记：非用户新需求、而是 T-142 未交叉回算的遗留）。
     ⇒ **落码禁令**：`scale` 只可乘在**珠体**（`size`）上、**不得**乘在 `outer` 上（`outer` 同时驱动 **L11 垫**，详下「层序死结论」）。

读法声明（裁定 7）：**1.06 = 规格起点值、不是过冲量**——分段口径为
  起点 1.06 → 谷 0.96（t=40ms）→ 终 1.00（t=120ms），全程**单调、不过冲**。
  v1.3 旧措辞「ease-out-back，过冲 ≤1.05」**作废**（与起点 1.06 自相矛盾：过冲应 >1.06）。
  **零冻结变更**：`ux-spec §5`「珠子落座」行的 `scale 1.06→1.0` 与 `120ms` **一字未改**；
  谷值 0.96 是 v1.4 新增的**中间插值**，不违 §5（§5 只冻结总时长与起止值；
  同族判例：`WRONG_FADE_IN_MS 60` / `WRONG_HOLD_MS 80` 均为 §5 未列的逐帧分段）。

逐帧（t = elapsedMs ∈ [0,120]）：
  t < 40 : u = easeOut(t / 40)
           scale     = 1.06 − 0.10·u        // 1.06 → 0.96（携动量撞进槽、被压扁）
           contactA  = 0.12 + 0.06·u        // 0.12 → 0.18
           contactW  = 0.88 + 0.04·u        // 0.88 → 0.92
           shadowA   = 0.15 − 0.03·u        // 0.15 → 0.12
           shadowDyN = (3 − 1·u) / 64       // 3/64 → 2/64
  t ≥ 40 : v = easeOut((t − 40) / 80)
           scale     = 0.96 + 0.04·v        // 0.96 → 1.00，**单调、不二次过冲**
           contactA  = 0.18 − 0.06·v
           contactW  = 0.92 − 0.04·v
           shadowA   = 0.12 + 0.03·v
           shadowDyN = (2 + 1·v) / 64
  t = 120: 全部回静息值 ⇒ **零残留**（不得留任何偏移量）

层序与几何（= §1.1 十层卡的**时间函数化**，锚点 = 珠心 cx,cy；s = scale）：
  通用：§1.1 中所有归一偏移量（0.06 / −0.02 / 0.88 / 0.10 / 2⁄64 / 5⁄64 / 0.52 / …）
        与边长一律乘 s；线宽走 stroke() = max(2, BEAD·s·ratio)（§1.1 最小特征约束不破）
  L0a 接触阴影：§1.1 式中 宽比 0.88 → **contactW**、α 0.12 → **contactA**
  L0b 柔和投影：§1.1 式中 偏移 3/64 → **shadowDyN**、α 0.15 → **shadowA**
  L1–L5：仅随 s 缩放，α / 色 / mix 比例 **全部不变**
  ⇒ 这是「重量 + 接触」两要素的载体：若只改 scale 不改 L0a/L0b，读感退化为「贴图缩放」

D1 退化（reduceMotion）：关 **scale** 与 **L0b** 联动（形变/位移 = 运动通道）；
  **保留** L0a α 单峰（0.12→0.18→0.12，振幅 ≤0.06，属 α 通道）
D2：单次、**0 往复**、非周期 ⇒ 不构成闪烁
clamp：scale ∈ [0.96, 1.06]（**硬钳**，防浮点漂移越界致重叠）、contactA ∈ [0.12,0.18]、shadowA ∈ [0.12,0.15]
A5 重叠核算（**`[v1.5-r6]` 按 B′ 基准 46 重算；旧「珠 = 50」算式作废留档**）：
  ~~峰 50×1.06 = **53px** ⇒ 半宽 26.5，与静止邻珠（半宽 25）合计 26.5 + 25 = **51.5 < 52** ⇒ 间隙 **0.5px、零重叠**~~
  ~~（r2 旧写法「27+25 = 52 ⇒ 相切」系半宽上取整的保守式，与 G4 卡精确算法不一致 ⇒ r2 已统一）~~
  **新（基准 46）**：峰 = 46×1.06 = **48.76** ⇒ 半宽 **24.38**；静息邻珠（同为 46）半宽 **23** ⇒ 24.38 + 23 = **47.38 < BEAD_PITCH 52**
    ⇒ **间隙 4.62px、零重叠** ✓（**静息间隙本身已是 52 − 46 = 6px**，较 r2 时代的 2px 宽 4 倍 ⇒ **重叠余量大幅改善**）
    ⇒ 附带结论：**峰 48.76 < 格 50 ⇒ 动画期珠永不越出本格**（r2 全卡按 50 推导时隐含的「越格 1.5px」可能性被 B′ **一并消除**）
  符号最小特征走 §1.1 兜底 stroke(ratio) = max(2, size×ratio)（○ 环 SYMBOL_REF_SIZE 64 ⇒ ratio = 3/64）：
    峰 max(2, **48.76**×3/64) = max(2, 2.286) = **2.29 ≥ 2** ✓；谷 max(2, **44.16**×3/64) = max(2, 2.070) = **2.07 ≥ 2** ✓
    （⚠️ 旧值 2.48 作废；且**注意兜底式已在工作**：44.16×3/64 = 2.07 与地板 2 仅差 0.07 ⇒ 若谷再深即触底）
图元：（**`[v1.5-r6]` 双材质口径归一为 +0；旧「v1.5 材质 +1」作废**）
  ~~**+0（v1.4 材质）**／**+1（v1.5 材质，S1 露缘条件绘制）**~~
  · v1.4 基座（E1 混色底 + empty 2 层）：原地缩放、十层照画 ⇒ **+0**（历史口径，仍成立）
  · **B′（v1.5-r5）基座**：落座格静态 = **11 层（十层 + L11 垫）**，**垫恒画、不参与 `scale`**（层序死结论见下）
    ⇒ 动画期层数 ≡ 静态层数 = 11 ⇒ **增量 +0/颗**；**并发上限作废**（旧 +3 不再适用，§1.6.9 / §1.6.8 / §1.8 / §1.6.2a 同批）
  · ⚠️ **`empty→filled` 的 +7（4 层坑 → 11 层珠+垫；`[v1.5-r2]` 旧值 +6 因 L11 而升 **+1**）属「放置瞬间的状态迁移」、不属动画期增量**，
    两者**不得相加**；逐帧口径与三案对比见本节末「empty 侧联动」（`[v1.5-r2]`，已被 `[v1.5-r6]` 按 B′ 重写）

`[v1.5-r6]` **层序死结论（落码不留二义；承派单 §三-4）**：
  **L11 垫 = 珠层之下、全格平面（50×50、radius 11、ink `edge = mix(base_target,#000,0.30)`）**
    **不参与 `scale`** ・ **不参与 `lift`** ・ **恒锁格缘** ・ **不受动画开关控制（恒画）**
  逐帧层序（与 §1.1 十层卡同一自下而上序）：**L11 垫 → L0a → L0b → L1 … L5**
  ⛔ **`S1–S4` 与 `L11` 互斥**：`filled` 格不画坑（§1.2）⇒ 所谓「露缘」在 `filled` 格里**只有垫**、没有坑缘。
  ⚠️ **代码现值与本结论相反（P0 陷阱 #2，须在 T-143 前送达工程侧）**：`bead-render.ts:118–122` 的 pad rect 用的是
    `outer = options.size ?? BEAD_CELL` 与 `y = cy + (options.lift ?? 0)` ⇒ **`size` / `lift` 会同时带动垫**。
    ⇒ 禁令：**G1 严禁以 `size` 承载 `scale`**（否则垫跟着缩 ⇒ 「垫缝」读数与珠体同步 ⇒ **目标色谜面在 120ms 内被自己抹除**）；
      需新增 `scale?: number`（默认 **1**）仅作用珠体：`size = (outer − 2×inset) × scale`。
    ⇒ 同理 `lift`（取回入槽跳格、网格落座没有）若用于网格侧会抬垫 ⇒ **现 `view-model.ts:731` 不传 `lift` ⇒ 零实害**，登记为约束。
  ⇒ 两条可选语义的对比（为何必选「不参与」）：
    · **不参与（采）**：珠放大 = **垫缝收窄**（2.00 → 0.62 → 2.92 → 2.00）⇒ 垫始终是**静态谜面载体**，读数连续；
    · 参与（驳）：垫随珠同缩 ⇒ **缝宽恒定 2px 不变** ⇒ 动画期零接触读数，且目标色垫面积随 `scale` 呼吸 ⇒ **同格目标色浓度周期变化** ⇒ 与 A1「色相通道不随状态漂移」相左。

`[v1.5-r6]` **峰的语义裁定（承派单 §三-1⑤；`ux-spec §5` 一字不改）**：
  v1.4 原设计「珠压进坑」的**凹侧对象（S1–S4）在 B′ 的 `filled` 格里不存在** ⇒ 该叙事在字面意义上已失效；
  **新叙事（三拍）**：鼓包盖垫（t=0，缝 0.62）→ 压进露垫最多（t=40，缝 2.92）→ 坐定（t=120，缝 2.00）
  ⇒ **「落座」读感仍成立**（叙事由“珠压进坑”改为“珠压实到目标色底面上”），但摆动量仅 ±0.92 / 1.38px ⇒ **属亚地板级**（< 2px 最小特征阈），**是否可辨 `[待 playtest]`、不软化。
  **幅度重定候选（不改 §5 的 `1.06→1.0 / 120ms`；均为 art-owned 中间插值）**：
    · 候选 α：`FILL_POP_SCALE_TROUGH 0.96 → 0.92` ⇒ 峰缝 `25 − 23×0.92 =` **3.84px（达地板）**、谷帧间隙 `52 − 42.32 =` **9.68px** ✓、`stroke = max(2, 42.32×3/64 = 1.98) =` **2.00**（**恰触地板**，无余量）
    · 候选 β：`BEAD_DRAW_INSET 2 → 3` ⇒ 静息缝 **3.00px**、珠视觉 **44**（全盘噪声↑、与用户「底色花」抱怨同方向）⇒ **不推荐**
    · 候选 γ：**不改、维持 0.96**（本批默认）⇒ 零回归风险，代价 = 接触读数可能不可辨（仅剩 α / 宽比两通道，而它们在目标色垫上的 ΔL 余量见 §1.6.1a (2)）
  ⇒ **本批不定案**（需真机/playtest 依据），已列为 `accessibility.md §5` 未决项 7 的替代口径 + 回传⑤待拍板项。
```

#### 1.6.1a `[v1.5-r2]` G1 的 **empty 侧联动（凹凸咬合）** — 主理人 WXG-T-131 任务 C（**`[v1.5-r6]` 已按 B′ 重写：标题保留、机制换为「垫缝联动」**）

> ⚠️ **`[v1.5-r6]` 重写声明（承派单 §三-1②）**：本节的标题与全部门票都建立在「`filled` 格静息时**露缘 0px** ⇒ 需要一个条件绘制才拚出咬合读数」上。WXG-T-142 的 **B′**（珠 46 / 垫 50）使**静息即露 2px**（且露的不是坑缘、而是**目标色垫**）⇒ **「可见性剔除」整套机制（`drawSocketRim` 判据 / 穿越 14.70ms / 露缘窗 87.7% / 峰 1.00px / +1 图元）作废**。下面各案**原文保留划档**，每处紧随 **B′ 新口径**；本节的**任务 C 目标（给落座动画一个物理叙事）本身仍成立**，只是承载者由 S1 换成 L11。

> **为何原卡零 `empty` 提及**：§1.6.1 是在 **v1.3 基座**（E1 混色底 + empty 2 层 = 一块平面色斑）上设计的——那时格底是**平面**，珠子只是「缩放在平板上」，没有可联动的对象。v1.5 后 `empty` = **凹陷坑**（S1–S4）、`filled` = **凸起十层** ⇒ 物理叙事升级为「**珠子压进坑里、坑缘被挤出一线**」，正命中用户「要有质感」的原始要求。**本节零新增元素**（S1 就是 §1.2 四层卡的第 1 层）、**零新增 hex**（墨 = §1.9.2 端点表 L− 同源系数 −0.30）、**零新增常量**（判据全由既有 `scale(t)` 曲线派生）、**零 `ux-spec §5` 变更**。 ⇒ **`[v1.5-r6]` 更新**：“零新增元素”仍成立（L11 就是 §1.1 第十一层）、**“零新增判据”更成立（B′ 下无需任何判据）**；但“坑缘被挤出一线”的措辞已换为“**珠边缘在目标色垫上收放**”（详见下）。

**(1) 垫缝的逐帧口径（`[v1.5-r6]` B′ 重写；取代旧「S1 露缘环 = 可见性剔除」）**

```
判据（每帧、每颗落座珠）：无。 L11 垫恒画（= §1.2 静态 11 层之一），**不再有条件绘制开关**。
  ~~drawSocketRim = scale(t) < 1.00~~  ⇒ ⛔ **作废**（`filled` 格与 S1–S4 互斥 ⇒ 无对象可剔）
  层序（与 §1.6.1 末「层序死结论」同值）：**L11 垫（全格平面、不随 scale）→ L0a → L0b → L1 … L5**
  垫几何（= §1.1 L11 卡，一字不改）：rect 全格 BEAD_CELL 50、radius r = round(50×0.22) = 11、
           fill `edge = mix(base_target, #000, 0.30)`（`palette.ts:240`，`SOCKET_EDGE_DARK_MIX 0.30`，与空坑 S1 同源）
           ⇒ **不随 scale 变化**（它是底面、不是珠）
垫缝宽（单侧、新公式） w(t) = (50 − 46·scale(t)) / 2 = 25 − 23·scale(t)
  与旧式的关系：r2 旧式 = 25·(1 − scale) = 25 − 25·scale ⇒ **本式仅把系数 25 → 23**（即珠半宽由 25 变 23）
           ⇒ **静息 scale=1 不再归零**（旧 0 ⇒ 新 2.00），这是两套体系的唯一结构性差异。 【`BEAD_DRAW_INSET` 的代数位置：25 → (25 − INSET)】
```

| 时刻 t | 段 | `scale(t)` | 珠宽（46·s） | **垫缝 w(t)** | L11 垫 | S2/S3/S4 |
|---|---|---|---|---|---|---|
| 0 | 压下 | 1.0600 | **48.76** | **0.62px（峰盖垫）** | **画**（恒画） | **不画**（与 L11 互斥） |
| ~~14.70~~ | — | — | — | ~~旧「穿越时刻」作废：无判据即无穿越~~ | — | — |
| 40 | 谷 | 0.9600 | **44.16** | **2.92px（峰）** | 画 | 不画 |
| 60 | 回弹 | 0.9775 | 44.97 | 2.74px | 画 | 不画 |
| 80 | 回弹 | 0.9900 | 45.54 | 2.62px | 画 | 不画 |
| 100 | 回弹 | 0.9975 | 45.89 | 2.55px | 画 | 不画 |
| 120 | 终 | 1.0000 | **46.00** | **2.00px（静息）** | 画 | 不画 |

- ~~**穿越时刻推导**（14.70ms / 露缘窗 87.7%）~~ ⇒ ⛔ **`[v1.5-r6]` 整条作废**（无开关 ⇒ 无穿越点）。旧推导保留供追溯：`scale = 1.00 ⇔ easeOut(u) = 0.6 ⇔ u = 1−√0.4 = 0.36755 ⇔ t = 14.70ms`。**新口径无需任何时间解**，缝宽是 `scale(t)` 的线性函数。
- ~~**S2/S3/S4 全窗不画的证明**（珠 ≥ 48px > 坑 44px ⇒ 三层零露出）~~ ⇒ ⛔ **`[v1.5-r6]` 遮罩窗口整条消失**（派单 §三-1④ 预感正确）：新证明不再比较尺寸，而是比**状态互斥**：
    ① `drawFilledBead` 与 `drawEmptySocket` 在**同一格上二选一**（`view-model.ts` 网格侧按 `cell.state` 分支、托盘侧 `state === 'free' ? drawEmptySocket : drawFilledBead`）⇒ **S1–S4 与 L11 永不同框**；
    ② 尺寸式旧结论在 B′ 下**反转为不可用**：珠 46 vs S2 坑 44 ⇒ 若同框，珠 46 仍可盖住 44 的坑底，但 **S3/S4 两条内缘线（`y∈[3,5.5]` 与 `[44,47]`）会露出**（46 半宽 23 < 24 ⇒ 边缘 1px 带不再覆盖）⇒ **旧证明的唯一支撑（“珠必硬大于坑”）已失效**，而它本身已因 ① 而不再需要 ⇒ 旧式仅在「未来出现坑/珠同帧的重叠态（如过渡动画）」时需重算，**已登记为核查点**；
    ③ ⇒ 动画期只需处理 **L11 一层，且它恒画** ⇒ **退场处理归零**。
- **垫缝的可辨性（与旧露缘环的关键差别）**：峰 **2.92px / 静息 2.00px ⇒ 均 ≥ §1.1 的 2px 最小特征地板** ✓（旧值 1.00px **低于地板** ⇒ 本条从「不可辨」升为「可辨」）；动画期内**最低点 0.62px 仍低于地板**，但：
    ① 垫是**静态谜面载体**（目标色读数），不是瞬时读数 ⇒ 0.62px 只持续数十毫秒；
    ② ⚠️ **仍不适用 `stroke(ratio) = max(2, ·)` 兜底**（承 A5 ⑧ 旧论证，理由不变）：它是“被珠体遮后露出的**面**”而非笔画，钳到 2px 会画出比垫带本身更宽的环（2px > 0.62px ⇒ 溢出到珠面上、几何失真）。
    ⇒ **新结论：静息缝达地板（不依赖动画即可读）；动画期摆动量 ±0.92/1.38px 属亚地板，不承载编码（A5 三通道不因它增减）**。
- **对比仍够、瓶颈从分辨率转为“峰时可见宽度”**：垫墨 `edge = mix(base,#000,0.30)` 对珠面 `base` 的 ΔL = **与旧 S1 完全同值**（同墨同系数 ⇒ **ΔL = 15.3（炭黑）～74.1（奶白）/ 255**、WCAG **1.23:1～2.10:1**，10 色全表见 `temp/out3.txt` §J / `temp/out5.txt`）⇒ **10 色全部 ≥ 本仓 4% 不可辨阈（10.2/255）**；且缝宽 2.00px ≥ 地板 ⇒ **旧「不可辨风险来自亚像素」的结论作废**，改为「来自珠径与目标色同系时的色相差不足」（`[待 playtest]`）。⚠️ **诚实登记**：垫与珠**同色相**（垫 = 珠 base 暗一档），而旧 S1 = **格目标色**暗一档 ⇒ **若珠色 = 该格目标色（正确放置），缝对比 = 本条 ΔL 表；若珠色 ≠ 目标色（错位），缝对比可能更低或更高** ⇒ 谜面就是靠这个变化读“错位”（⇒ 本条不再是“质感”而是**编码通道**，入 `accessibility` A5 ⑧ 改判）。
- ⛔ **`[v1.5-r6]` 以下两条为 `[v1.5-r2]` 旧文**（已被上方两条 B′ 口径**取代并作废**，逐字保留仅供追溯、**不得引用为现行规格**）：**露缘的可辨性（诚实登记、不软化）**：峰 **1.00px < §1.1 的 2px 最小特征地板**。⚠️ **本环不适用 `stroke(ratio) = max(2, ·)` 兜底**——它是「被珠体遮后**露出的面**」而非「笔画」，钳到 2px 会画出**比坑缘更宽**的环（2px > 1px ⇒ 环会溢出到珠体上、几何失真）⇒ **1px 露缘在低分屏 / 50% 缩放下不可辨，照实登记**。其作用 = **质感咬合读数（AO / 环境光遮蔽）**，**不承载任何编码信息** ⇒ `accessibility` A5 三通道（色/符号/明度）**不因它增减**（详 `accessibility.md` A5 ⑧）。【**作废原因**：峰不再是 1.00px而是 2.92px；且承载者由“坑缘质感”变为“目标色编码通道”，**不得再记作「不承载编码信息」**】
  **对比是够的、瓶颈在分辨率**：S1 墨 `mix(base,#000,0.30)` 对珠面 `base` 的 ΔL 实测（Rec.601，`temp/out5.txt`）= **15.3（炭黑）～74.1（奶白）/255**、WCAG **1.23:1～2.10:1** ⇒ **10 色全部 ≥ 本仓 4% 不可辨阈（10.2/255）** ⇒ 只要那 1px 被渲染出来，色调分离够用；**不可辨的风险来自亚像素、不来自对比**。【**作废原因**：墨与系数未变 ⇒ **ΔL 数值可直接援用**；但“那 1px”已不存在（静息 2.00px）⇒ “风险来自亚像素”的陈旧判断作废】
- **网格 vs 托盘的不对称（`[v1.5-r2]` 旧文 ⇒ ⛔ **`[v1.5-r6]` 本条结论被证伪且方向反转**；原文保留供追溯）**：旧文：网格 `BEAD_CELL 50` = 珠 50 + 同 `radius 11` ⇒ 静息露缘 **0px**；托盘 `TRAY_SLOT 48` > 珠 `48−4 = 44` ⇒ **静息即露 S1 环 `(48−44)/2 = 2.00px`（达地板）** + S2 环 **0.56px** ⇒ 旧结论「**托盘侧的咬合读数不依赖动画、D1 下仍在；网格侧只在落座 120ms 内出现**」。<br>⇒ **`[v1.5-r6]` 本批新发现（主理人未列出、属 §三-5 的“与 §1.6 卡数值耦合”自查项）：旧论证的事实前提不成立**——`drawTray` 对 `state === 'free'` 调 `drawEmptySocket`、否则调 `drawFilledBead`（二选一）⇒ **托盘珠从不在 S1 同框** ⇒ “托盘静息即露 S1 环 2.00px”**为假**（那 2px 露的是**面板白（plate）**、不是暗色 S1 ⇒ **无 AO 咬合读数**）。⇒ **B′ 后不对称方向完全反转**：网格侧 **2.00px 常驻目标色垫圈（有实体、且为编码通道）** vs 托盘侧 **0px**（珠 44 / 槽 48，露白）。⇒ 连带：`accessibility` **A5 ⑧ / D1 代价 2 的“托盘侧更强”论证需同步改判**（已入 b1 与回传④）。
  **回退阀**（旧文保留）：若真机判定「珠像贴在板上、不像嵌在底面上」= `FILL_POP_SCALE_TROUGH` **0.96 → 0.92** ⇒ 旧式露缘峰 `25×0.08 = 2.00px`；**B′ 下该阀的新效果与完整三候选见 §1.6.1 末「峰的语义裁定」（候选 α）**，数值不再沿用旧式。**art-owned 中间插值、零 `ux-spec §5` 变更**（§5 只冻结总时长与起止值，同上卡裁定 7）；A5 副作用重算（基准 46）：谷帧间隙 `52 − 46×0.92 =` **9.68px**（旧文“3px 变 4px”作废）、`stroke = max(2, 42.32×3/64 = 1.98) =` **2.00 ≥ 2** ✓（**恰触地板、无余量**）。⇒ **默认不改、仅真机触发**（`accessibility.md §5` 第 **7** 条已因 B′ 改判，详 b1）。

**(2) L0a 接触阴影的落点改判（`[v1.5-r6]` 按 B′ 重算：旧「段 0 / 段 1」二分为**单段恒落垫上**）**

L0a 几何 = `roundRect(x+0.06, y−0.02, 0.88, 0.10, r×0.5)`；**B′ 下以珠体 `46·s` 代入**（`bead-render.ts` 各层均由 `size` 派生）⇒ 格内坐标（格 `[0,50]`、珠底 = `25 − 23s`）：
  可见条 = `y' ∈ [25 − 23.92s, 25 − 23s]`、高 = **`0.92s` px**（旧值 `1×s`）
    · 静息 `s=1` ⇒ **[1.08, 2.00]** ⇒ **全在格内 ⇒ 地色 = L11 垫**（旧值：在容器板白上）
    · 谷 `s=0.96` ⇒ **[2.04, 2.92]** ⇒ 垫；峰 `s=1.06` ⇒ **[−0.355, 0.62]** ⇒ **仅 **0.355px** 越入 2px 格缝（容器板白）**
⇒ **旧三段表作废留档于下、现行口径 = 单段恒落垫**：

| 段 | 珠 vs 格 | L0a 可见条落在 | 地色 | 峰 ΔL(α 0.18) | 摆动（峰−静息） |
|---|---|---|---|---|---|
| ~~段 0 `t < 14.70ms`~~ | ~~珠 ≥ 格~~ | ~~**2px 格缝**~~ | ~~容器板 `panel_surface #FFFFFF`~~ | ~~**0.156**（= v1.4 原值）~~ | ~~**0.051**~~ |
| ~~段 1 `t ≥ 14.70ms`（占 **87.7%**）~~ | ~~珠 < 格~~ | ~~**S1 暗缘框**~~ | ~~`mix(base,#000,0.30)`~~ | ~~**0.097（奶白）～0.003（炭黑）**~~ | ~~**0.031～0.002**~~ |
| ~~D1 静息（关停 `scale`）~~ | ~~珠 ≡ 格（50 = 50）~~ | ~~**2px 格缝**（永不露缘）~~ | ~~容器板 `#FFFFFF`~~ | ~~**0.156 不变**~~ | ~~**0.051 不变**~~ |
| **`[v1.5-r6]` 现行·全窗（含 D1 静息）** | 珠 46·s < 格 50 | **L11 垫**（仅 t=0 邻域 0.355px 越入格缝） | `edge = mix(base_target,#000,0.30)`（**与旧 S1 同墨同系数 ⇒ 下表 ΔL 值直接沿用**） | **0.097（奶白）～0.003（炭黑）** | **0.031～0.002** |

⚠️ **`[v1.5-r6]` 本批新发现（与 D1 直接相关、主理人未列出）**：旧表第三行「**D1 静息 ⇒ 落回容器板白 ⇒ 峰 ΔL 0.156 / 摆动 0.051 全部维持**」在 B′ 下**不成立**——D1 关停 `scale` 后 `s ≡ 1` ⇒ 珠宽 46 ⇒ 可见条落 `[1.08, 2.00]`，**仍在格内 = 落在垫上** ⇒ **D1 路径的 L0a α 单峰同样降为静默层**（旧值 0.156 → 新值 0.097～0.003）⇒ `accessibility` **D1 的「保留 L0a α 单峰」条目需加注**（保留 ≠ 可辨；已在 b1 落笔、并列入回传④。零改值、零改条款、仅加事实注）。旧结论之所以成立是因为「静息珠 = 格 ⇒ 条越出格外」；B′ 把珠缩到 46 ⇒ 反而把它拉回格内垫上。
⇒ **连带后果（诚实、不软化）：「静默层」的覆盖比例由 87.7% 升至≈ 100%**（旧值至少还有 12.3% 的窗口落在白板上、D1 下全程落在白板上）⇒ **L0a 作为接触通道的价值进一步下降 ⇒ 更强化甲案（承载者改判为几何缝宽）的必要性**。

**段 1 的量化后果**（Rec.601 luma，ink = `BEAD_SHADOW_HEX #1E2033`、`L_ink = 0.132`；`ΔL = α × (L_地 − L_ink)`；与 `palette.ts::luminance` 同函数，10 色全表见 `temp/out3.txt` §J）：

| 地色 | `L_地` | 余量 `L_地 − L_ink` | 峰 ΔL(α0.18) | 降幅 | 摆动 | 恢复旧峰 0.156 所需 α |
|---|---|---|---|---|---|---|
| 容器板 `#FFFFFF`（段 0 / D1） | 1.000 | 0.868 | **0.156** | — | **0.051** | 0.18（原值） |
| 奶白 S1 `#B1ACA3` | 0.676 | 0.545 | 0.097 | −38% | 0.031 | 0.286 |
| 柠黄 S1 `#B3932C` | 0.568 | 0.436 | 0.080 | −49% | 0.027 | 0.356 |
| 活力橙 S1 `#AC6D19` | 0.464 | 0.332 | 0.060 | −61% | 0.021 | 0.468 |
| 湖蓝 S1 `#2B56AC` | 0.325 | 0.194 | 0.035 | −77% | 0.012 | 0.803 |
| 深棕 S1 `#4B2B15` | 0.196 | **0.065** | 0.012 | −92% | 0.005 | **不可达**（需 α 2.42 > 1） |
| 炭黑 S1 `#24242B` | 0.144 | **0.013** | 0.003 | −98% | 0.002 | **不可达**（需 α 13.0 > 1） |

⇒ **10 色的摆动全部 < 本仓 4% 不可辨阈（0.040 归一 = 10.2/255）⇒ L0a 的 α 单峰在 v1.5 网格落座期成为「静默层」**（画了、但读不出来）；且深棕/炭黑两色**结构性不可达**（余量 < 0.156 ⇒ 任何 α ≤ 1 都回不到旧峰）。

**处置 = 改判承载者、不改值（三案对比，本批零 `ux-spec §5` 变更请求）**：

| 案 | 内容 | 判定 |
|---|---|---|
| **甲** | 接触读数改由 **S1 露缘宽 `w(t)`** 承载：峰 **1.00px @ t=40ms**，与 L0a 的 α 峰（0.18 @ t=40ms）**同刻、同曲线族**（两者都由 `scale(t)` 派生）⇒ 「压得越实、接触面越宽」的原语义**完整保留**，只是承载者由 α 换成**几何宽度**。L0a 的 α / 宽比**一字不改**，角色**降为辅助层**（浅色上仍有余量、深色上不可读）。 | ✅ **采纳** ⇒ **`[v1.5-r6]` 结论不变、且更强**：承载者换成 **垫缝 `w(t) = 25 − 23·scale`**，摆动量由旧 `0→1.00→0`（全低于地板）升为 **0.62→2.92→2.00**（峰与静息均 ≥ 2px 地板）⇒ **接触读数从「不可辨」变「可辨」**；L0a α / 宽比仍**一字不改**。 |
| 乙 | 提 L0a 峰 α `0.18 → 0.39`（活力橙可复旧峰，源 `temp/out2.txt` §D） | ❌ **驳回**：① 需改 `ux-spec §5`「珠子落座」行的 **α 幅度**（跨域、且 §5 是幅度真源）；② 对**奶白 S1**（`L_地 0.676`）α0.39 ⇒ ΔL **0.211** > 旧峰 0.156 ⇒ 在浅格上变成**明显黑边**；③ 深棕/炭黑**仍不可达**（需 α 2.42 / 13.0）⇒ 花了跨域代价只修 8/10 色 |
| 丙 | 改亮墨（把 L0a 的 `#1E2033` 换成更亮的色） | ❌ **驳回**：需**新增设计 hex**（违纪律 3：hex 只住 `palette.ts`，且 §1.1 十层卡的 L0a/L0b 同源墨会被拆成两套）；且「接触阴影用亮墨」与光照模型（左上光源 ⇒ 阴影为暗）**自相矛盾** |

> **与 `ux-spec §5` 的关系（显式声明，防两处不同源）**：§5「珠子落座」行的 `120ms`、`scale 1.06→1.0`、L0a `α 0.12→0.18→0.12`、L0a `宽比 0.88→0.92→0.88`、L0b `α 0.15→0.12→0.15`、L0b `dy 3/64→2/64→3/64` **全部一字未改**。本节新增的只有 ① S1 的**条件绘制判据**（派生自既有 scale 曲线）与 ② L0a 的**角色说明**（承载者改判、值不变）⇒ **不需主理人串行落笔 §5**。（职责划分：§5 = **毫秒与幅度真源**；本节 = **逐帧公式真源**；两者不重叠，口径承 §1.6 三条硬纪律 ①。）

**(3) S1–S4 在 120ms 内的退场口径（三案对比）**

| 案 | 退场方式 | 逐帧 | 图元（vs `filled` 静息 10 层） | D1 连带 | 判定 |
|---|---|---|---|---|---|
| **甲** | ~~**可见性剔除**：S1 仅在 `scale < 1.00` 时画；S2/S3/S4 恒被珠体覆盖 ⇒ **全窗不画**~~ ⇒ **`[v1.5-r6]` 新口径：无剔除、无判据——L11 垫恒画（静态 11 层之一）**；S1–S4 与 L11 **互斥**（`filled` 不画坑）⇒ 坑的退场问题不存在。 | 见 (1) 表 | ~~段 0 +0；段 1 +1 ⇒ 最坏 +1/颗~~ ⇒ **全窗恒 +0**（11 层 = 静息 11 层） | **零新增关停项 ✓ 且比旧案更成立**：旧案靠「`scale ≡ 1` ⇒ 判据恒假」自动不画；新案**无判据可关** ⇒ D1 下垫照画（垫是静态谜面、不是动效）⇒ **D1 零新增项、零分支** | ✅ **采纳（B′ 版）** |
| 乙 | 四层全帧淡出（S1–S4 各乘 `α = 1−p`） | 全窗 4 + 10 = 14 层 | **+4/颗**（并发最坏 **+12**） | ⚠️ 新增一个 α 单峰 ⇒ **D1 必须增列一个关停项**（虽仍 0 往复、不违 D2） | ❌ **驳回**（三重不利：图元 ×4、D1 增项、淡出与「被珠体挤掉」的物理叙事相反） |
| 丙 | 四层立即消失（= v1.4 旧行为：落座帧起只画十层） | 全窗 10 层 | **+0/颗** | 无 | ❌ **驳回**——**这正是本任务要消除的缺口**：凹坑在落座瞬间「啪」地消失、珠子出现在平板上 ⇒ 物理叙事断裂，v1.5 的质感增益**归零**（敷衍不得） |

> ⛔ **`[v1.5-r6]` 下段为 `[v1.5-r2]` 旧文（甲案 D1 自洽性），已被 B′ 重写；逐字保留供追溯、不得引用为现行规格**。旧文中**三处事实已失效**，逐地点名：① ~~`drawSocketRim = (1.00 < 1.00) = false` ⇒ **S1 永不绘制**~~ ⇒ **无此判据**（L11 垫恒画、不参与任何开关）；② ~~网格侧静息露缘 **0px**、L0a 落回 **2px 格缝 = 容器板白** ⇒ **峰 ΔL 0.156 / 摆动 0.051 全部维持**~~ ⇒ **不成立**（D1 下 `s ≡ 1` ⇒ 珠宽 46 ⇒ L0a 落 `[1.08, 2.00]` **仍在格内 = 落垫**，ΔL 降为 **0.097～0.003**，见上 (2) 新发现）；③ ~~**托盘侧例外**（静息即露 2.00px）~~ ⇒ **已被证伪**（托盘珠从不在 S1 同框，且托盘侧不传 `padColorIdx` ⇒ **无垫**；那 2px 露的是面板白，见 (1) 末条）。
> **`[v1.5-r6]` 现行口径 · 甲案（B′ 版）的 D1 自洽性**：D1（`reduceMotion`）关停 **scale** 与 **L0b** ⇒ `s ≡ 1` ⇒ 珠 **46**、**L11 垫照画**（垫是**静态谜面载体**、不是动效 ⇒ **无可关项、无分支**）⇒ **`accessibility` D1 零新增关停项 ✓（结论比旧案更成立：旧案靠判据恒假“顺带不画”，新案根本没有判据）**。
> **成立的部分（值与条款一字未改）**：D1 下仍**保留 L0a α 单峰**（0.12→0.18→0.12）——这是 §5 冻结表述，不改。
> **代价（诚实登记、不软化，且比旧文更重）**：D1 下该 α 单峰的落点是**目标色垫**而非白板 ⇒ 10 色摆动全部 < 4% 阈 ⇒ **L0a 通道在 D1 与非 D1 下同为静默层** ⇒ 「**去形变、留接触**」的“留接触”在**网格侧名存实亡**（保留 ≠ 可辨）。⇒ 已列入 `accessibility.md` **D1 加注**（零改值、零改条款、仅加事实注）与 **§5 未决项**。
> **仍然成立、不随 B′ 改变的两条**：① v1.5 的凹凸咬合叙事在 D1 下**不再有“网格侧归零”之说**（垫圈 2.00px 常驻，静态可读）；② **不做补偿设计**——补偿需引入静态形变，与 D1 语义相左且会改 **A5 ⑦** 的间隙算式（该理由与 B′ 无关）。详见 `accessibility.md` D1 ①–⑦。

**(4) 图元重算（直接回答主理人「还是不是 +0」）**

**答（`[v1.5-r6]` B′ 版）：`[v1.5-r2]` 的「不是 +0」已翻回「是 +0」。** ⚠️ 注意这与「静态层数 11」不矛盾：**+1 来自静态卡（L11 垫，属基线）、动画期增量为 0**。

| 项 | 值（现行） | 口径归属 | 算式 |
|---|---|---|---|
| **静态基线差**（`filled` 有垫 vs 无垫） | 10 层 → **11 层** ⇒ **+1/颗** | **静态**（WXG-T-142 B′ 已落码，归 r5 账，本批不重记） | §1.2 `filled` = §1.1 十层 + L11；已入 §1.8 末表与 §1.6.9 |
| **状态迁移**（`empty → filled`，放置瞬间的**一帧内**） | ~~**+6/格**~~ ⇒ **`[v1.5-r6]` +7/格** | **状态迁移**（已含在 §1.8 静态基线的构型差里） | `filled` **11** 层 − `empty` 4 层（§1.2 S1–S4）= **7**（旧值 6 的差 = L11） |
| **动画期增量**（120ms 内，vs `filled` 静息 11 层） | 段 0 ~~+0~~／段 1 ~~+1~~ ⇒ **全窗恒 +0** | **动画期** | 层序 = **L11 + 十层 = 11**，与静息**同层数**；L11 恒画 ⇒ 无“多画一层” |
| **并发上限** | ~~**+3**~~ ⇒ **`[v1.5-r6]` +0（并发口径作废）** | 动画期 | 旧式 `ceil(120/80) = 2` + 取回 1 = 3 颗同帧**仍然成立**，但每颗动画期增量已是 **+0** ⇒ 乘任何并发数仍为 **0**；并发数只在 §1.6.9 LOD 里作为**层数/帧预算**关注量保留 |
| **各口径关系** | **不得相加** | — | 状态迁移发生在 t=0 那一帧、随即进入静态基线口径；动画期增量是**相对该基线**的额外量 ⇒ 落座期该格的绝对层数 = **11**（不是 10+7+0 = 17） |

⇒ **`[v1.5-r2]` 结论「v1.5 材质 +1（最坏并发 +3）」作废**（留档于此，理由：该 +1 的前提是「S1 条件绘制」判据，B′ 后判据不存在）。现行 = **v1.4 材质 +0／v1.5(B′) 材质 +0**；两代材质在**动画期**口径上**归一**，差异全部体现在**静态**口径（+1 层）。**同批需同步（本批执行）**：§1.6.8 汇总表 G1 行、§1.6.9「不降层」理由列 + 三基线表（24/48 双口径）、§1.8 末表「单珠动画期」行、`art-bible §7.3.1` 图元增量条、`art-bible §7.4/§7.5` G1 行。

> ⚠️ **「不降层」的结论不变、理由已第三次改（B′ 版）**：§1.6.9 原理由「增量本来就是 +0 ⇒ 降层零收益」（v1.4）→ r2 改「+1 来源是 S1 不是珠体层」→ **`[v1.5-r6]` 现行理由**：动画期增量回到 **+0**，但**静态层数 10→11 是 L11 垫带来的**，而垫是**谜面载体**（不可降）⇒ 若真机掉帧需要砍，**可砍对象只有 L2/L3b 等高光层**，砍它们会伤「凸面质感」⇒ **驳回降层**；LOD 分级仍按 §1.6.9（珠数阈值 + 关闭 G3/G4 等**并发大卡**优先，不关闭 G1）。

> ⚠️ **`[v1.5-r2]` 自查新发现（主理人未列出；v1.4 既有、亚像素级、本轮不修）**⇒ **`[v1.5-r6]` 本项已闭合，原文划线留档**：~~**段 0（`scale > 1.00`）L0a 可见条会越出 2px 格缝**……越界 **0.56px** 落在**下方邻格**的顶缘~~【**闭合算式（基准 46）**：`scale = 1.06` ⇒ 珠底 = `25 − 23×1.06 = −0.38`、L0a 高 = `0.92×1.06 = 0.9752`、下偏移 ⇒ 可见条 `y' ∈ [−0.355, 0.62]` ⇒ **越入格缝的量只有 0.355px**（旧值 0.56px），而 **2px 格缝区间为 `[−2, 0]`** ⇒ **0.355 < 2 ⇒ 完全落在自家格缝内、不触碰下方邻格顶缘** ⇒ **旧「越界 0.56px 落邻格」的结论作废**，**无需 y 钳制、无需新常量**】。**影响量级**：~~`42.24px × 0.56px`、α ≤ 0.18 的 `#1E2033` ⇒ ΔL ≤ 0.156 的一条亚像素暗线~~ ⇒ 新量级 = `42.24 × 0.355`、地色 = **容器板白（仅峰帧的 0.355px）** ⇒ ΔL ≤ 0.156 的**更短更窄**的暗线（≈0.18pt）；**是否可见不再取决于 `drawGrid` 行序**（它已在邻格之外）⇒ 旧「待工程确认行序」**一并关闭**。**登记**：这是 `[v1.5-r2]` 本节唯一未闭合项，**B′ 使其自动消解**（不是被修复，而是被**改小到落回缝内**）——与「未决项 6/7 因 B′ 消解」同族，均已入 `accessibility.md §5`。

#### 1.6.2 `vfx_clear_dissolve` — 消除溶解 `[v1.4·G2]` P1

```
常量：
  DISSOLVE_MS         = 200    // ux-spec §5「消除（道具）」，冻结，不改
  DISSOLVE_SCALE_END  = 0.60   // v1.3 既有终值
  DISSOLVE_STAGGER_MS = 12     // 逐枚错峰（24 枚最坏尾延 276ms；不改主时长）
  DISSOLVE_SINK_PX    = 6      // 下沉吸向槽底（非飘散）
  DISSOLVE_LOD_ALPHA  = 0.60   // LOD 阈值：α 低于此降层（**规格保留、本轮不预埋**，裁定 5）
  DISSOLVE_LOD_LAYERS = 4      // 降层档：L1 + L2 + L3 + **L5**（同上，回退阀②启用时才落码）

作用对象（**钉死，防误读**）：**托盘槽位珠**（TRAY_SLOT 48 ⇒ TRAY_BEAD_SIZE = 48−4 = **44**），**不是网格珠**。
  依据：systems/powerups.ts 三条「绝不」之一 = **绝不写网格**（filled 是终态）。
  ⇒ 若照「消除网格珠」落码即破坏玩法状态机。
  批量上限：region ≤6 / random ≤5 / clearAll ≤24（TRAY_BASE_SLOTS 12 + TRAY_EXPAND_SLOTS 12）

⛔ **[v1.5-r2] 以上四行「作用对象钉死」已被 v1.22 正面推翻（连带登记，本批不重写卡）**：
  `systems-index §3.6`（v1.22，WXG-T-130 第二批）已冻结**道具反转**——
    · `POWERUP_TYPES` = `['solver','solverPlus','solverRandom']`（解环器族）；
      `['region','clearAll','random']` ⛔**作废**
    · `REGION_CLEAR_SLOTS 6` / `RANDOM_CLEAR_COUNT 5` ⛔**作废**；
      新增 `SOLVER_PLUS_COUNT 3` / `SOLVER_RANDOM_COUNT 1`
    · **托盘零读写**；道具的**作用对象 = 网格错位珠**（语义 = 「自动归位」）
  ⇒ 本卡「绝不写网格」的**唯一依据已反转**（现在是「**绝不写托盘**」）；
     批量上限 **24 → 3**（`SOLVER_PLUS_COUNT`），且错位珠总数 ≤ `2×MISPLACED_PAIRS_MAX 8` = **16 < 24**
     ⇒ **即使按旧语义，24 亦不可达**（旧上限本身就是松值）。
  ⇒ **本卡自 v1.22 起为「失效规格」**：逐帧包络（scale/α/dy/LOD）仍可复用，但**作用对象、触发语义、
     批量上限、`powerup:used` payload（`affectedSlots` → `affectedCells`）四项全部需重立**。
     **G2 卡重立归后续美术单、不在本批**（本批只做事实钉定，不发明新玩法视觉）。
  ⇒ 连带后果：**T-128 的道具期双口径（−141 / +3）前提失效**（两值都以「24 枚托盘珠同帧溶解」为最坏场景）
     ⇒ 三基线重算与**解环器期真值（净 +5 / 绝对 1677，基线 C）**见 §1.6.9 末表。
触发：`powerup:used` 且 outcome.slots 非空（A05-07：零槽 ⇒ 零扣次零发声**零动画**）
音画同步：与 `AUDIO_CLIP_DISSOLVE` **同帧启动**（该音效已实装，v1.3 音画不同步 = G2）

逐帧（第 idx 枚，τ = clamp(t − 12·idx, 0, 200)，q = τ/200）：
  scale = 1.00 − 0.40·easeIn(q)   // 1.00 → 0.60；ease-in =「被吸走」（与 G1 ease-out 语义相反）
  α     = 1 − q                   // **线性**；与 scale 不同缓动 ⇒ 前半段「形还在、色已褪」= 沙化
  dy    = −6·q²                   // 下沉吸向槽底（y 向下为负）
  τ ≤ 0   ⇒ 该枚按静息十层绘制（未开始）
  τ ≥ 200 ⇒ **G2 不再绘制该珠**（槽已 free）
     ⚠️ **[v1.5-r2] 语义重述（防误读为「该格零图元」）**：「不绘制」只指 **G2 这条动画停止输出珠体**，
        **该槽仍由 §1.2 `empty` 卡接管** ⇒ 终态图元**不为 0**：
          · v1.4 材质（E1 混色底 + E4）：终态 = 24 × **2** 层 = **48**
          · v1.5 材质（凹陷四层 S1–S4）：终态 = 24 × **4** 层 = **96**（**+48**）
        「槽 free ⇒ 画 empty」这条在 v1.4 下同样成立，只是**层数由 2 变 4** ⇒ 终态核算必须分材质列（见下）。

LOD（层数时间函数）：
  ⚠️ **本轮不预埋（裁定 5）**：以下 α 阈值分支**保留为规格**（= 回退阀②被启用时的判据），
     本轮落码**先十层全开**（不传 lodLayers）⇒ 溶解期图元恒为 24×10 = 240（净 0）。
     **本轮唯一会传 lodLayers 的动画是 G4**（见 §1.6.10）。
  α ≥ 0.60（**τ ≤ 80ms**）：**十层全画**（§1.1）——「看得见质感在溶解」的关键窗口
  α < 0.60（**τ > 80ms**）：**4 层** = L1 主体 + L2 暗倒角 + L3 亮倒角 + **L5 符号**
    · 砍掉（**六层全砍**）：L0a / L0b / L3b / L4a / L4b / L4c（十层 − 保留四层 = 六层；
      α<0.6 后软高光实际 α ≤0.048，低于可辨阈）
    · 保 L2/L3 = 保 3D read；**保 L5 = 保符号通道（accessibility A5 硬要求）**
    · 切换用 **α 判定**（不用 τ）⇒ 与视觉衰减严格同步
    · **阈值时间复算（r2 修正，防再算错）**：本卡定 `α = 1 − q`（**线性**）⇒
      α ≥ 0.60 ⇔ q ≤ 0.40 ⇔ `τ ≤ 200 × 0.40 =` **80ms**。
      ❌ 旧值 126ms 作废：那是把 α 当 **easeIn** 反解的结果（`1 − p² = 0.6 ⇒ p = 0.632 ⇒ 126.5ms`），
         与本卡的线性 α **矛盾**；若按 126 落码会多画 46ms 十层 = 白烧 CPU。
         （scale 用 easeIn、α 用 linear 是**刻意不同缓动**，两者阈值不得互推。）

α 施加方式（关键）：整体 α **乘到每一层的 fill α** 上（L1 base → withAlpha(base, α)）；
  **L5 符号 α 同比衰减** ⇒ 三通道同步消失，不出现「只剩符号」或「只剩颜色」偏读
色：溶解期**不换色**（禁止褪向 text_secondary/灰——灰读作「无效/禁用」，与「被吸收」相反）
D1 退化：关 **scale** 与 **dy**；**保留** α 线性淡出（信息：槽位变 free 完全可达）
D2：α **单调递减**、0 往复 ⇒ 不构成闪烁
clamp：scale ∈ [0.60,1.00]、α ∈ [0,1]、dy ∈ [−6,0]
A5：符号 α 同比衰减 ⇒ 三通道同步消失。最小特征核算：托盘珠 size 44 × scale 0.60 = **26.4px**，
  ○ 环 max(2, 26.4×3/64) = max(2, 1.24) = **2.0px**（钳制生效 = §1.1 兜底正在起作用）。
  **合规依据**：§1.1 最小特征约束已声明「**50% 缩放下仍可辨**」，而本卡最坏 scale = **0.60 > 0.50**
  ⇒ 落在既有约束覆盖范围内，**无需新增约束**（也无需为本卡单独抬高 minStroke）
图元（**[v1.5-r2] 三行分列 · 构成必须标注 · 不得混用**）：
  ⚠️ **主理人复核发现的语义失效已确认成立**：旧写法「240→96（净 −144）」把**两个不同时段、不同构成**的
     「96」混为一谈。v1.5 后 `24×4 = 96` 在**两处**出现、**数值巧合相同、构成完全不同**：
       (甲) **LOD 后半窗**（τ ∈ (80,200)）的 96 = 24 × 4 层【**降档珠** L1+L2+L3+L5】
       (乙) **终态**（τ ≥ 200、槽 free）的 96 = 24 × 4 层【**凹陷空槽** S1–S4】
     ⇒ **误读风险（照实写明）**：落码者若把 (乙) 读成 (甲)，会在 `free` 槽上**画一颗降档珠**
        （L1+L2+L3+**L5 符号**）⇒ 直接违反 §1.2「禁画 L5/E4 符号」与 §1.9.4「must not read as a bead」
        五通道复核 ⇒ **空/珠混淆、v1.5 质感语言当场崩塌**。这是**规格级严重误读**，故三行分列。
  · **① LOD 后半窗**（规格口径 / 回退阀②启用时）：绝对 24×10 = 240（前半 τ≤80）→ **24×4 = 96**（后半 τ>80）
      构成 = 【降档珠 L1+L2+L3+L5】；vs 静态十层 240 ⇒ **净 −144**
      · v1.4 材质基线 1810 ⇒ 道具期 **−141**（含扫光 +3）／绝对 **1669**
      · v1.5 材质基线 2173 ⇒ 同净值 **−141**／绝对 **2032**（三基线表见 §1.6.9 末）
  · **② 终态**（τ ≥ 200，槽 free；**与阀② 是否启用无关，恒成立**）：
      · **v1.4 材质**：24 × 2 层【E1 混色底 + E4 幽灵符号】= **48**
      · **v1.5 材质**：24 × 4 层【S1–S4 凹陷坑】= **96**（**+48 vs v1.4**）
      ⇒ **v1.5 使「溶解后的静态代价」翻倍**（48→96），但这**已计入静态基线 2173**（托盘 24 槽 ×+2 = +48）
        ⇒ **不构成动画期新增量**，只是基线本身抬高。**这是 2173 与 1810 两套口径不得混用的直接原因之一。**
  · **③ 本轮落码口径**（裁定 5：不预埋、十层全开）：溶解期绝对 **240**（全窗口）⇒ 动画期净 **0**
      ⇒ 道具期净 **+3**（仅 G3 扫光）／绝对 v1.4 基线 **1813** · v1.5 基线 **2176**；见 §1.6.9 / §1.8 末表
  · ⛔ **v1.22 后本行全部数值的批量前提（24 枚）已失效** ⇒ 解环器期真值（≤3 枚）= ~~净 **+5** / 绝对 **1677**（基线 C）~~ ⇒ **`[v1.5-r6]` 现行：净 +3 / 绝对 ④ 1831（基础 24 槽）· ④ 1927（扩展 48 槽）· ③ 1675**，推导见 **§1.6.2a**（本行旧值 +5 已作废：其「S1 露缘 +2」项在 B′ 下不存在）、完整基线表见 §1.6.9 末
```

#### 1.6.2a `[v1.5-r6]` **补落** · `vfx_solver_restore` — 解环器归位（**G2′**） P1

> ⚠️ **补落声明（诚实、点名我自己）**：`v1.5-r4` 头注⑤ 写「**新增 §1.6.2a `vfx_solver_restore` — 解环器归位（G2′）**」，但**该节当时从未落盘**（自查：本批前 `grep -n "^#### 1.6.2a"` **零命中**，全文件对 `1.6.2a` 的命中仅 r4 头注那一句）⇒ **这是 v1.5-r4 的虚报，责任在我**。凡引用过该句的下游（含我上轮回传）都是**对不存在规格的引用**。本批按派单 §三-3 补落，并同批修正三处旧值（本节、§1.6.9 末推导、§1.9.6 未决 3）与 `accessibility §5` 第 4 条的「错位标记无卡」表述。

**真源与不发明原则**：毫秒与节奏全取自 `ux-spec §5`「**解环器归位**」行（现 **L189**：「错位珠高亮闪 1 次 → 复用「珠子落座」落座收尾（就位格逐格触发）；链式无递归（一次点击至多 `SOLVER_PLUS_COUNT` 颗，逐颗 80ms 间隔）」/ 时长 **200 + 120/颗**）⇒ **本卡零 `ux-spec §5` 变更请求、零新增 §3 冻结常量、零新增 hex**。

```
常量（全归 src/config/tuning.ts 呈现层；不进 systems-index §3）：
  SOLVER_HINT_MS         = 200   // ux-spec §5 前段「高亮闪」，冻结，不改
  SOLVER_PER_BEAD_MS     = 120   // = FILL_POP_MS（直接复用 §1.6.1 常量，不复制值）
  SOLVER_STAGGER_MS      = 80    // ux-spec §5「逐颗 80ms 间隔」，冻结
  SOLVER_MAX_BEADS       = SOLVER_PLUS_COUNT = 3   // 引用 §3.6 冻结值，不新增
  SOLVER_RING_W          = 2     // = §1.5「状态环 2」既有档位（与 hint/wrong 同档）
  SOLVER_RING_INK        = palette.slotBorder #D8D5E6   // 墨选裁定见下表（候选 I）
  SOLVER_HINT_A_PEAK     = 1.00  // α 单峰峰值（起于 0）

作用对象（钉死）：**网格 `filled` 且「珠色 ≠ 该格 pattern 目标色」的错位珠**，≤ 3 颗。
  ⛔ 不是托盘珠（v1.22 `§3.6` 道具反转：**托盘零读写**）⇒ §1.6.2 的「作用对象 = 托盘槽位珠」已作废。
  ⚠️ 落码前置（P0、归工程单）：`grep -rn misplaced src/view/` **零命中** ⇒ view 侧当前**无错位标记、无 affectedCells 消费路径**；
     payload 由旧 `affectedSlots` → **`affectedCells`**。本卡**不新增玩法判定**（谁是错位由 core 判）。
相序（两相、串行）：
  相 A 高亮预警 t ∈ [0,200)：每颗错位珠一圈**状态环**（**直接复用 `drawStateRing`** ⇒ 1 个 rect-stroke 命令/颗）
        α = sin(π · t/200) ⇒ 单峰、**0 往复、非周期事件驱动**（同 §1.6.6 单次峰判例 ⇒ **不违 D2**）
        几何贴格缘（size = `BEAD_CELL` 50、线宽 2）⇒ **环的内侧一半压在 B′ 垫带上**（详下表「为何 B′ 使候选 I 成立」）
  相 B 逐颗归位 t ∈ [200, 200 + 120 + 80·(n−1)]：第 i 颗的 scale / contactA / contactW / shadowA / shadowDyN
        **逐字复用 §1.6.1 的逐帧表**（基准 46、层序 L11→L0a→L0b→L1…L5、**垫不参与 scale**），本卡不重列公式。
        ⇒ **B′ 的关键联动**：§1.6.1a (4) 已定「动画期图元增量 = **+0/颗**」⇒ 相 B 净 **+0**（旧「S1 露缘 +2（并发 2 颗）」作废）。
音画同步：与「道具生效」魔法“叮”**同帧启动**（`ux-spec §5` 该行音效列；与 §1.6.3 扫光同事件不同包）。
D1 退化：相 A **保留**（纯 α 通道、非形变非位移 ⇒ 承 D1 既有「去形变、留 α」口径）；相 B = G1 的 D1 退化
        （关 scale 与 L0b、留 L0a α 单峰）⇒ **本卡零新增 D1 关停项**。
A5：相 A 不改珠色/符号/明度；“错位”可辨性由 **B′ 垫色缝**承担（≠ 相 A）⇒ 相 A 关停不违 **A6**。
A5 重叠：环 size 50 **不随 scale 变化** ⇒ 与邻格环间隙 = `BEAD_PITCH 52 − 50 =` **2px、恒不重叠**
        （援旧 A5 ⑧ 的 S1 几何式：同值、语义由「坑缘」改「状态环」）。
图元：**净 +3（最坏，全来自相 A）** = 3 颗 × 1 rect-stroke；相 B **+0** ⇒ **本卡合计 +3**。
  ⚠️ **[落码修正 WXG-T-150]** 该值的前提是本卡立项时「错位珠**无**恒亮标记」；T-148 已上线
  **可选取白环（错位珠恒亮）** ⇒ 点名格为**白环 ↔ 相 A 环 1:1 互换** ⇒ **实为净 +0**（已钉测试，详本节末注）。
  ⇒ 旧值 **净 +5**（= 高亮闪 +3 ⊕ S1 露缘 +2）**作废**：`[v1.5-r6]` B′ 使「露缘条件绘制」不存在 ⇒ 相 B 归零。
  ⇒ 绝对值（**双口径 × 托盘 24/48**）：**④ 1828 + 3 = 1831**（基础 24 槽，含 L11）／**④ 1924 + 3 = 1927**（扩展 48 槽，**前提 = §1.3.2 的裁定被采行**）
     ／③ 口径（无 L11 的 as-if）1672 + 3 = **1675**（扩展 48 ⇒ 1768 + 3 = **1771**）。详 §1.6.9 末表。
  ⚠️ 若「高亮闪」按 **事件**计而非按**颗**计 ⇒ 净 **+6**（`beads-game.ts:810-816` swap 一次发 2 个 `bead:placed` ⇒ 3 颗最坏 6 事件）
     ⇒ **登记为落码风险，规格采「按颗」**（与 **待补的** G11 卡同类风险同族，见下注）。
  ⚠️ 若归位同帧触发普通模式「归位连击」（§1.6.11 G11）⇒ 再叠 global 门 **+5 `circle`** ⇒ **同帧最坏 +8**。
     ⛔ **引用诚实注（本批自查第二处虚报）**：`v1.5-r4` 头注④ 亦声称「**新增 §1.6.11 `vfx_gather_streak`（G11）**」，
     而**该节正文同样不存在**（`grep -n "^#### 1.6.11"` 零命中；参数仅存于 r4 头注散文）⇒ **上述 +5 / +8 不是现行规格值、
     是“待补卡后复算”的估计值**。本批不补写 §1.6.11（与 v1.24 / B′ 无耦合、超出派单范围）⇒ **已登记为欠账（回传②/④）**。
```

**墨选裁定（本批新立，四候选；⚠️ 与 `accessibility §5` 未决项 5/6 联动）**

| 候选 | 墨 / 形 | 图元 | 与既有环的冲突 | 判定 |
|---|---|---|---|---|
| **I** | `slot_border #D8D5E6` **实环 2px** | **+1/颗 ⇒ +3** | 与 G7 的 D1 退化环**同墨**，但 G7 落点恒为 `occupied/locked` 格 ⇒ **永不同框**；线宽 **2 vs 1** 分档区隔 | ✅ **采**（B′ 后新增支撑，详下） |
| II | `accent_blue` 实环 | +1/颗 | 与 `hint` 蓝环（600ms 呼吸）、`selected` 蓝点**争同一色相**；且 solver 与 hint **可同屏**；D1 下 hint 退为静态蓝环 ⇒ 与单峰闪难区分 | ❌ **驳回**（色相预算已被占） |
| III | `accent_blue` **虚线**环（dash 6/4，承 `tray_slot_dashed` 判例） | **+20 `line`/颗 ⇒ 最坏 +60** | 形态区隔最强，但无 dash 命令 ⇒ 需合成 20 条线 ⇒ **图元代价为实环 20 倍** | ❌ **驳回**（≤200ms 瞬时层不值 +60）；⚠️ 若未决项 5 采「hint 改 dash」⇒ 本卡**不得**改用本候选（形态撞车） |
| IV | 外扩至 pitch 52 格缝的实环 | +1/颗 | 地色 = 容器板白（ΔL 39.2 ✓），但**几何余量仅 0.5px/侧**（承未决项 6 候选 (a) 的同一风险）`[待真机]` | ⏸ **备用**（真机判 I 仍不可辨时才升） |

**为何 B′ 使候选 I 从「有风险」变「成立」**（本批新推论，与派单 §三-2 的 A5 ⑦ / 未决项 6 重算同源）：旧墨算式是「环 vs **珠面**」——柠黄珠 `#FFD23F`（L×255 = 206.7）对 `slot_border`（215.8）⇒ Δ **9.1/255 = 3.6% < 4%** ⇒ 10 色中唯一失败者。B′ 后珠体收为 **46** ⇒ **格缘 2px 带被目标色垫占据**，而 2px 环的中心线在格缘 ⇒ **内侧一半落在垫上** ⇒ 地色变 `edge = mix(target,#000,0.30)`：最不利者**奶白** `#B1ACA3`（L 172.4）⇒ Δ **43.4/255 = 17.0%** ✓；柠黄 `#B3932C`（144.9）⇒ Δ **70.9/255 = 27.8%** ✓ ⇒ **10 色全过 4% 阈、最低余量 4.25 倍**。
⇒ **未决项 6 在「目标格为 `filled`」这一半场景被 B′ 自动消解**（`locked` 侧无垫 ⇒ 仍 ΔL 31.6 ✓；而 **G7 的退化环落在 `occupied` 格 = 也是 `filled` 格 ⇒ 同一机制、同样被消解**，详 §1.6.7——本句初稿曾误写为「不被消解」，已按 §1.6.7 的落点材质取证改判）。

> ⚠️ **本卡不拍板项（入回传⑤）**：候选 I 的成立前提是「相 A 环只落在 `filled` 格」。若未来 solver 目标范围扩到非 `filled` 格（属玩法变更），本裁定需重算。

**[WXG-T-150 落码回写注（工程侧代落盘，2026-09-17）]** —— 本卡已按下列口径落码（代码 = `tuning.ts` G2′ 常量块 + `game/beads-game.ts::SolverFxQueue` + `view/scene-vfx.ts::solverHintAlpha/solverBeadProgress` + `view/view-model.ts` 相 A/相 B 接线；判据 = `tests/solver-vfx.test.ts` 15 例）。**三处与规格原文不同，均为用户当场裁定而非实现偏差**：
>
> 1. **相 A 的 200ms 从「观感时长」升为「玩法提交时刻」**（用户裁定「甲：归位延后到 200ms 后」）。
>    原卡只写了「先高亮闪、后逐颗复用 §1.6.1」，未钉住**归位发生在哪一帧**；而现码 `usePowerup` 是
>    同帧立即 `_solveMisplaced` ⇒ 不延后就根本不存在相 A（点名时棋盘已空）。采「甲」后：
>    `powerup:used` 载荷 = **点名格**（原卡口径、非「实际归位格」）、`bead:placed` / 过关判定 / 托盘
>    可取状态全部推到各自到点帧；**PAUSED 冻结不作废**（道具已扣次）、离开 `playing`（过关/失败/换关）
>    ⇒ 队列作废；预警窗口内玩家已自行取走的格 ⇒ 相 B 静默跳过。**`ux-spec §5` 零变更**（毫秒未改），
>    但本卡**不再是纯表现层卡** ⇒ 改动 `SOLVER_HINT_MS` 等同改玩法时序（已在 `tuning.ts` 头注同批钉住）。
> 2. **恒亮环在场时点名期退让**（用户裁定「甲：clamp 白环 + 点名期互斥」）。T-148 的 `selectableRing`
>    初版为**外扩环**（实测环带半径 24.15–28.29）与本卡的相 A 环（24–26）**争同一带**，且外扩环本身
>    有四处代价（遮 L11 垫缝≈43%、邻环涂覆重叠≈3.7px、越格缘 2.29px、静态层数 11→12 ⇒ §1.8 基线
>    ④ 最坏 +16）。落码采：**白环收进珠体内缘**（环带 18.86–23 ⇒ 零遮缝/零越格/邻环零糊连）+ **被点名的格
>    在相 A 期内不画白环**（由相 A 环独占该带 ⇒ 单帧语义单一）。原卡 A5 重叠行（邻格间隙 2px 恒不重叠）**仍成立**；
>    代价如实记 = 白环改画在珠体之上（L5 符号之后），靠 α 0.92 / 4.14px 描边读「可选」，**不新增任何图元层**。
> 3. **图元净值由 +3 修正为 +0**（理由见上行②与「图元」行的落码注）⇒ **§1.8 基线 ④ 1828 / 1924 与
>    §1.6.9 末表零变动**（本单不推高任何静态口径）。
>
> 另有两处**命名/形态漂移**（不影响数值，登记待后续美术单收口）：① 常量名 规格 `SOLVER_MAX_BEADS` →
> 代码 `SOLVER_MAX_CELLS`（引用 §3.6 `SOLVER_PLUS_COUNT` 不变）；② 总时长不写在卡里而是取
> **单一真源函数** `tuning.solverSequenceMs(count) = SOLVER_HINT_MS + SOLVER_STAGGER_MS×(n−1) + SOLVER_PER_BEAD_MS`
> （game 定窗口 / view 还原绝对毫秒共用，防双真源漂移，承 §1.6.4 `lodLayers` 6 vs 7 判例）。
>
> ⚠️ **逐颗 80ms 与 G1 120ms 重启门的冲突消解**（`assets-spec §1.6.1` 侧的连带事实）：解环器相 B
> **不走** G1 的单槽 `_armPlaceFx`（否则每隔一颗被 120ms 门掐掉而无声），而是走**独立逐颗队列**
> ⇒ 交换场景一步两格**共享同一 `step`**（同时落座）。这与 §1.6.1「作用对象单颗」不矛盾：
> 手动连点仍受门约束，本单只开了道具路径的旁通。

#### 1.6.3 `vfx_powerup_sweep` — 道具生效扫光（**新增**） `[v1.4·G3]` P1

```
常量：
  SWEEP_MS        = 400                  // ux-spec §5「道具生效」，冻结，不改
  SWEEP_TAN       = 0.364                // tan(20°)；斜向剪切（**非水平带**）
  SWEEP_X_FROM    = −260                 // 起点（完全在屏外左侧；≥ 广层宽 210 + 余量）
  SWEEP_X_TO      = 1010                 // 终点（完全在屏外右侧；750 + 210 + 50）
  SWEEP_Y_MIN     = 0
  SWEEP_Y_MAX     = 1214                 // HUD_BAND 下沿（yMin）⇒ **不侵入顶部 HUD 带与胶囊避让区**（用户裁定）
  SWEEP_WIDTHS    = [36, 84, 210]        // 核心 / 中 / 广（由内向外）
  SWEEP_ALPHAS    = [0.14, 0.10, 0.06]   // **单调递减**，累计 ≤0.27
  SWEEP_FILL      = BEAD_HIGHLIGHT_HEX #FFFFFF   // 既有 token，**零新色**

触发：`powerup:used` 且 outcome.used（与 §1.6.2 **同帧启动**：G3 =「因」的全场包装、G2 =「果」的逐枚表现）

形态裁定（为何斜向）：Lv3 burst 已占用「水平上扫光带」（view-model.ts::drawComboVfx，
  96px 高 / 全宽 / α0.22）⇒ 若 G3 也做水平带，两个语义不同的事件（连击能量 / 道具魔法）
  会长得一样。Lv3 =「能量自下而上涌」；G3 =「一道魔法斜切而过」。

几何（3 × polygon，四点平行四边形）：
  p  = t / 400
  x0 = lerp(−260, 1010, easeInOut(p))
  第 i 层（w = SWEEP_WIDTHS[i]）四角，自 y=Y_MIN 到 y=Y_MAX 向右上倾斜：
    (x0 − w/2 + SWEEP_TAN·Y_MIN,  Y_MIN)
    (x0 + w/2 + SWEEP_TAN·Y_MIN,  Y_MIN)
    (x0 + w/2 + SWEEP_TAN·Y_MAX,  Y_MAX)
    (x0 − w/2 + SWEEP_TAN·Y_MAX,  Y_MAX)
  ⇒ 顶部相对底部右移 0.364 × 1214 ≈ **442px**，形成 20° 斜切

层序（背→前）：广(α0.06) → 中(α0.10) → 核心(α0.14)，**后画的更亮**（守约束 3：每处 ≤4 层、α 单调）
绘制位置：在 drawGrid/drawTray **之后**、drawComboVfx/drawHud **之前**
  ⇒ 扫光叠在珠面上、但被 HUD/面板压住（**不侵入 HUD**，否则盖住倒计时 = 违纪）

「全屏」口径（**用户裁定，2026-09-16**）：取**玩法全屏** y∈[0,1214]，**不侵入 HUD_BAND 与胶囊避让区**
  （字面全屏含 HUD 的读法**作废**，不再列为未决项）。
  真源：`tuning.ts` `HUD_BAND = { yMin: 1214, yMax: 1334 }`（= `systems-index §3.1`「**顶部 HUD 带**：
  左设置齿轮、中倒计时胶囊、右胶囊避让」；原点左下、y 轴向上 ⇒ yMax 端即屏顶）。
  裁定理由：白扫过 HUD 会让倒计时胶囊**瞬时发白**，触 accessibility B1（文字对比 ≥4.5:1）/ E1；
  亦合 art-bible §3.5「暖光只服务焦点区、不得扩散到 HUD」同精神。
  `ux-spec §5`「道具生效」行写「全屏轻扫光」未界定 y ⇒ **以本裁定为准**（不改 §5 原文）。

红线核算（「无全屏白闪」）：
  广层面积 = 210 × 1214 = 254,940 px² ≈ 玩法区（750×1214 = 910,500）的 **28.0%** < 30%
  累计 α = 0.06+0.10+0.14 = **0.27** < PANEL_SCRIM_ALPHA 0.5 一整档
  单次通过、0 往复 ⇒ **非全屏白闪** ✓
D1 退化：**整条关停**（纯包装层、零信息量；信息仍由 §1.6.2 的 α 淡出承载 ⇒ accessibility A6）
状态无关性（**用户裁定 WXG-T-128 待裁③ = 甲，2026-09-19**）：臂起后**恒播满 `SWEEP_MS`**，
  **不因状态切换中断** —— 含离 `playing` 进 `level-clear`（同在途的还有 G6 彩带、落座回弹、
  轻压等表现层动效，见 `beads-game.ts::_update` 末尾步进组）。理由三条：
    ① **同判例**：G6 彩带（§1.6.6）本就是「`level-clear` 臂起、播满 800ms」，扫光与其同类；
    ② **同精神**：裁定 1「庆祝先行放完再落遮罩」（§1.6.4）—— 结算面板延迟 `CLEAR_PANEL_DELAY_MS`
       （= 800ms）才开，而扫光 400ms **必然先行播完** ⇒ 无遮挡、无时序打架（层序上扫光本就在面板之下）；
    ③ **零实现改动**：`_stepSweepFx` 与 `_stepConfettiFx` 同属表现层步进组、**无状态门** ⇒
       本裁定 = 以实现为准回写。**「给扫光加 `playing` 门」属错误修法**（与 ①② 分叉）。
  判据 = `tests/scene-vfx.test.ts`「裁定『恒播满』…solver 归位致过关」例（K-036 变异自检：
  加 `playing` 门 ⇒ 该例红）。
D2：单次通过、0 往复、非周期 ⇒ 不构成闪烁
clamp：p ∈ [0,1]；t ≥ 400 ⇒ **不绘制**
图元：**+3**（固定，与珠数/槽数无关）
```

#### 1.6.4 `vfx_complete_wave` — 过关庆祝波浪 `[v1.4·G4]` P1

```
常量：
  WAVE_MS            = 800   // ux-spec §5「过关庆祝」，冻结，不改
  WAVE_COL_DELAY_MS  = 20    // 同上行「20ms/列」，冻结，不改
  WAVE_SCALE_PEAK    = 1.08  // v1.3 既有「scale 1→1.08→1」
  WAVE_RISE_RATIO    = 0.35  // 鼓起段占单列窗口比例（逐帧参数）
  WAVE_LIFT_PX       = 3     // 微抬幅度（= WRONG_SHAKE_PX 同量级，单峰非震动）
  WAVE_WINDOW_MIN_MS = 240   // clamp 下限：GRID_MAX_COLS 13 ⇒ W=560，留 240 兜底防异常
  WAVE_LOD_LAYERS    = 6     // L0b + L1 + L2 + L3 + L4c + L5（**本轮预埋**；裁定 5 下唯一会传 lodLayers 的卡）

触发：`level:cleared`（图案填满）
作用对象：**网格全部 filled 珠**，最坏 GRID_MAX_COLS 13 × GRID_MAX_ROWS 12 = **156 颗**
  ⚠️ **`[v1.5-r6]` B′ 注**：这 156 颗在现码均为 **11 层**（十层 + L11 垫）⇒ 本卡的静态对照值由 **1560 变 1716**（基线 ④）；
     且垫**不参与 `scale`**（§1.6.1 层序死结论）**也不参与 LOD 降层**（见下 LOD 行）⇒ 本卡只动珠体与 `dy`。

派生（§5 两个冻结值的**唯一自洽推导**，不新造时长）：
  W = max(WAVE_WINDOW_MIN_MS, WAVE_MS − WAVE_COL_DELAY_MS × (cols − 1))
      cols=13 ⇒ W = 800 − 240 = **560ms**；cols=7 ⇒ 680ms；cols=1 ⇒ 800ms

逐帧（列 j ∈ [0,cols)，τ = clamp(t − 20·j, 0, W)，p = τ/W）：
  p < 0.35 : scale = 1 + 0.08·easeOut(p / 0.35)             // 1.00 → 1.08 鼓起
  p ≥ 0.35 : scale = 1.08 − 0.08·easeIn((p − 0.35) / 0.65)  // 1.08 → 1.00 落下
  dy = +3·sin(π·p)                                          // 微抬后归位，**单峰 0 往复**
  τ ≤ 0 ⇒ 该列按**静息十层**绘制（未轮到）；t ≥ 800 ⇒ 全列回静息
    ⚠️ **`[v1.5-r6]` B′ 注**：现行“静息”= **11 层**（十层 + L11 垫，垫恒画）⇒ 本句里的“十层”一律读作“十层珠体 + 垫”；
       下方“恢复十层的瞬间恰逢遮罩落下”同理（= 恢复满层）。

LOD（**本轮预埋**，裁定 5）：~~弹跳中的列（0 < p < 1）降 **6 层** = L0b + L1 + L2 + L3 + L4c + L5~~
  ⇒ **`[v1.5-r6]` B′ 重算：降 **7 层** = L0b + L1 + L2 + L3 + L4c + L5 ⊕ **L11 垫（不参与降层、全程恒画）****
    ⇒ **`WAVE_LOD_LAYERS` 6 → 7**（**值变更、非新增常量**；仍归 `tuning.ts` 呈现层）。
    ⛔ **禁令：L11 不得进入 `lodLayers` 的可砍集** —— 三条理由：
      ① 垫 = **静态谜面载体**（目标色读数）⇒ 波浪期（最长 **800ms**）砍垫 = 全盘错位可读性周期消失再回来；
      ② 波浪按列传播 ⇒ 砍垫会形成一条**“目标色底面逐列抽走”的横扫亮带**（α 从 1→0→1）⇒ 与 **D2**（同区 α 往复）擦边；
      ③ 收益为负：多画 156 个 rect（+156）换到的是一层本就不该动的东西。
  · 砍掉（四层）：L0a / L3b / L4a / L4b（**保 L4c 核高光** = 保「糖果感」最亮的一层）
  · 未轮到的列保持十层 ⇒ 波浪前沿有清晰的「质感落差」，**强化传播感**（四要素之能量传递）
  · 恢复十层的瞬间恰逢结算面板遮罩落下 ⇒ **跳变被遮**（**裁定 1 已定案**：面板延迟 800ms 开
    ⇒ 此句为**定案陈述、非前提句**，跳变被遮已是成立的事实）
  · ⚠️ **本卡是本轮唯一预埋 LOD 分支的动画**：G2 的 α 阈值降层与 G6 的 44→24 枚均为
    「规格保留、本轮不预埋」（回退阀②③，见 §1.6.9）

几何：同 §1.6.1 的「§1.1 时间函数化」，但 s ∈ [1.00,1.08]、cy 加 dy；
  **L0a/L0b 不做 α 联动**（G4 是集体波浪，逐颗阴影联动会让 CPU 与视觉都变噪；只保形变）

层序定案（裁定 1，用户 2026-09-16 拍板 = 原三案之 (a)）：
  结算面板**延迟 800ms 开**——庆祝先行放完再落遮罩。
  代码事实（仍成立，需工程侧改）：LEVEL_CLEAR.onEnter **现行同帧**调 _clearPanel.open()，
  drawClearPanel **首行即全屏遮罩 α0.5** 且在 drawGrid **之后** ⇒ 不改时序则庆祝被完全遮死。
  真源：`ux-spec §5`「过关庆祝」行视觉列（含代价：过关到可点按钮多等 800ms）+ `bead-grid §4` 表同批注。
  ⇒ 本卡为**定案陈述**（原「三案待裁 / 本卡按 (a) 撰写」措辞已废）。

D1 退化：**整条关停**（scale + dy 全归零）；合法性前提 = 结算面板本身即信息通道（A6）
D2：每颗单峰 0 往复；156 颗是**空间相位差**非时间闪烁（BD-29 口径）
clamp：scale ∈ [1.00,1.08]、dy ∈ [0,3]、p ∈ [0,1]
A5 重叠核算（**`[v1.5-r6]` 按 B′ 基准 46 重算；旧「珠 = 50」算式作废留档**）：
  ~~峰 50×1.08 = **54px** > pitch 52 ⇒ 同列相邻行同相位时**上下各叠压 1px**；叠压仅及最外倒角带（L2/L3 在 50px 珠上宽 3.1–3.9px），**符号区（中心 20px）零遮挡**~~
  **新（基准 46）**：峰 = 46×1.08 = **49.68 < pitch 52** ⇒ **零叠压 ✓**
    · 同列（同相位、同 `dy` ⇒ **相对间距不变**）两珠均到峰：间隙 = `52 − 24.84 − 24.84 =` **2.32px**（最不利值）
    · 跨列（邻列滞后 20ms ⇒ 近峰 vs 静息）：间隙 = `52 − 24.84 − 23 =` **4.16px**
    · 附带：峰 49.68 **< 格 50** ⇒ **动画期珠永不越出本格**（旧「越格 2px/侧」随 B′ 一并消失；与 §1.6.1 同结论）
  ⇒ **旧「上下各叠压 1px」的整个论证链（包括“仅及倒角带、符号区零遮挡”的豁免理由）作废**：
    现在**零叠压** ⇒ 符号区保护不再需要论证（但仍成立）；`[v1.5-r2]` 登记的「庆祝期叠压」**不是回归、是被改善**。
  符号最小特征 max(2, **49.68**×3/64) = max(2, 2.329) = **2.33 ≥ 2** ✓（⚠️ 旧值 2.53 作废；§1.1 兜底未触发、scale 1.08 远高于 0.50 阈）
图元（**`[v1.5-r6]` 双口径**）：~~绝对 156×6 = **936**（vs 静态十层 1560 ⇒ **净 −624**）~~
  · **③ 口径（无 L11 的 as-if）**：156×6 = **936** vs 1560 ⇒ 净 **−624**（历史值，仍成立）
  · **④ 口径（现行，含 L11）**：156×**7** = **1092** vs **1716** ⇒ 净 **−624**
  ⇒ **净值恒等（两个口径同时 +156）** ⇒ 这是一个可记忆的巧合，**引用 −624 时必须声明它不带绝对值信息**；绝对值详 §1.6.9 末表。
```

#### 1.6.5 `vfx_wrong_shake` — 放错拒绍（**既有实装，本轮不改，仅登记为骨架判例**）

```
已实装常量（tuning.ts，真值）：
  WRONG_FX_MS             = 200   // ux-spec §5「放错拒绍」
  WRONG_SHAKE_PX          = 3
  WRONG_FADE_IN_MS        = 60
  WRONG_HOLD_MS           = 80
  WRONG_FADE_OUT_MS       = 60    // 60+80+60 = 200 ✓
  WRONG_FX_RESTART_GATE_MS= 500

逐帧（view-model.ts::wrongFlashAlpha(p)，**纯函数零分配 = v1.4 各卡的写法模板**）：
  const ms = p * WRONG_FX_MS;
  ms <  60 : α = easeOut(ms / 60)                 // 淡入
  ms < 140 : α = 1                                // 峰值保持
  ms < 200 : α = 1 − easeIn((ms − 140) / 60)      // 淡出
  位移：dx = 3·sin(p·π·4)                         // 200ms 内 2 个完整周期 =「抖动 ×2」
  D1：dx 归零 + α 恒 1（静态红描边，**0 往复**）

登记（本轮不动）：实装的 α 包络是**单峰**（1 峰 0 往复，BD-29 裁定），比 art-bible §7 主表
  「danger 描边闪 2 次」**更保守** ⇒ 合规，v1.4 不翻案；位移仍为 2 周期（§5「抖动 ×2」原值）。
本卡在 v1.4 的作用是**骨架参照**：G1/G2/G3/G4/G6/G7 全部沿用同一模式（表现层计时 +
  重启门 + 快照标量 + view 侧纯函数包络），详见 §1.6.10。
图元：+1（drawStateRing 描边环）；珠层数不变
```

#### 1.6.6 `vfx_confetti` — 结算彩带礼花（**新增·真源已入 `ux-spec §5`**） `[v1.4·G6]` P2

```
常量：
  CONFETTI_MS            = 800    // 真源 = ux-spec §5「**结算彩带**」行（WXG-T-128 已增行；
                                  //   该行值**复用「过关庆祝」**，不新造时长 ⇒ 已有源）
  CONFETTI_COUNT         = 44
  CONFETTI_MAIN_COUNT    = 36     // 主体层（面板遮罩**之后**）
  CONFETTI_FG_COUNT      = 8      // 前景层（面板**之前**）= 44 − 36
  CONFETTI_W             = 6
  CONFETTI_H             = 14
  CONFETTI_FALL_FACTOR   = 1.15   // 落程系数（>1 ⇒ 保末帧已出屏）
  CONFETTI_SWAY_PX       = 18
  CONFETTI_SWAY_CYCLES   = 2
  CONFETTI_SPIN_TURNS    = 1.25   // 旋转圈数 ⇒ **1.25Hz**（D2 核算基准）
  CONFETTI_FADE_START    = 0.7    // 末段淡出起点（p 比）
  CONFETTI_SPAWN_STEP_Y  = 60
  CONFETTI_NOFLY_YMIN    = 447    // 前景禁飞带下沿（clearPanelLayout 按钮行 yMin）
  CONFETTI_NOFLY_YMAX    = 787    // 前景禁飞带上沿（titleY 745 + ribbonH 84/2）
  CONFETTI_GOLDEN_RATIO  = 0.618
  CONFETTI_GOLDEN_ANGLE  = 137.5

色（**5 色子集，显式排除暖橙**，守 art-bible §3.5）：
  CONFETTI_COLORS = [ panel_surface #FFFFFF, STAR_GOLD #FFD23F,
                      珠色4 草绿 #3FBF6B, 珠色5 玫红 #E84C3D, 珠色6 丁香紫 #8E6FD9 ]
  · **排除珠色3 暖橙 #F59B23**：§3.5 冻结「暖橙**仅**珠子本体」，彩带非珠子 ⇒ 撒暖橙即违纪
  · 排除珠色1 奶白 #FDF6E9（与 panel_surface 近重）、珠色8/9/10 暗档（落白面板读作**污渍**）
  · **排除珠色2 柠黄 #FFD23F**：与 STAR_GOLD **同值** ⇒ 列入会造成两档同色、浪费一个色位
  ⇒ 共 **5 个不同 HEX**，全部为 §3/palette 既有 token，**零新色值**
  · STAR_GOLD 引用合法性：palette.ts 既有判例「珠色2 柠黄同值但语义不同源——结算场景
    珠面不在场，无 §3.5 冲突」⇒ 结算彩带同此判例

触发：`level:cleared` 后的**结算面板入场期**（与 §1.6.4 **接力**，不同帧）

零 RNG 分布（守铁律 L4：禁 Math.random()）——索引 lattice，承 comboParticleOffsets 判例：
  frac(v) = v − floor(v)
  x      = 750 × frac(idx × 0.618)        // 黄金比均布（不聚不空）
  phase  = idx × 137.5                    // 黄金角错相（度）
  color  = CONFETTI_COLORS[idx mod 5]
  spawnY = 1334 − (idx mod 5) × 60        // 五档错高，避免齐平下落
  layer  = idx < 36 ? MAIN : FOREGROUND
  ⇒ **确定性可测**（同输入同画面）、**不占 services.rng**

逐帧（p = t/800，每枚 idx ∈ [0,44)）：
  y  = spawnY − 1334 × 1.15 × easeIn(p)         // 飘落；ease-in = 加速下落（重力直觉）
  x′ = x + 18·sin(2π·(2p + phase/360))          // 横向摆动 2 周期
  θ  = 360 × 1.25 × p + phase                   // 旋转 1.25 转 ⇒ 1.25Hz
  α  = p < 0.7 ? 1 : 1 − (p − 0.7)/0.3          // 末段淡出，避免 800ms 硬切

几何（无旋转变换通道 ⇒ 逐帧算四角，polygon 扁平点列）：
  hw = 3, hh = 7；c = cos(θ·π/180), s = sin(θ·π/180)
  P0 = (x′ − hw·c − hh·s, y − hw·s + hh·c)
  P1 = (x′ + hw·c − hh·s, y + hw·s + hh·c)
  P2 = (x′ + hw·c + hh·s, y + hw·s − hh·c)
  P3 = (x′ − hw·c + hh·s, y − hw·s − hh·c)
  builder.polygon([P0x,P0y, P1x,P1y, P2x,P2y, P3x,P3y], { fill: withAlpha(color, α) })
  **预分配 scratch**：44 × 8 = 352 float（一次分配、逐帧写入，禁循环内 new）

层序（与结算面板的 sandwich）：
  MAIN 36 枚：drawClearPanel **之前** ⇒ 被 scrim α0.5 压住（读作「远处彩带」）
  FG    8 枚：drawClearPanel **之后** ⇒ 读作「近处彩带」

禁飞带（可读性硬约束）：FG 枚当 y ∈ [447, 787] 时**跳过绘制**（不是淡出，是不画）
  依据 clearPanelLayout() 派生：按钮行 y∈[447,535]（rowY 447 + PANEL_BUTTON_H 88）、
  金色缎带 y∈[703,787]（titleY 745 ± ribbonH 84/2）
  ⇒ 保按钮可辨识、保热区无视觉噪声（accessibility C1）；MAIN 枚**不受**禁飞带限制

D1 退化：**整条关停**（纯装饰、零信息量；过关事实由面板 + 星级 + 音效承载）
D2：飘落/淡出单次 0 往复；**唯一往复量 = 旋转致单枚可见面积 1 次往复 ⇒ 1.25Hz < 3Hz** ✓；
  单枚面积 6×14 = 84px² ≈ 占屏 **0.008%** ⇒ 远低于闪烁敏感阈值
A5：**不承载任何编码**（装饰层）；禁飞带 + MAIN 在面板后 ⇒ 不遮挡珠色/符号/UI 文字
clamp：p ∈ [0,1]、α ∈ [0,1]；t ≥ 800 ⇒ 不绘制
图元：**+44 polygon**（固定）；回退阀 44 → 24 枚（§1.6.9）
```

**[WXG-T-153 落码回写注（工程侧代落盘，2026-09-17）]** —— 本卡已按下列口径落码（代码 = `tuning.ts` G6 常量块 16 项 + `view/palette.ts::CONFETTI_COLORS` + `game/beads-game.ts::_confettiElapsedMs/_stepConfettiFx`（快照只导出单调标量 `confettiProgress`，L5）+ `view/scene-vfx.ts` 六纯函数 + `view/view-model.ts::drawConfetti` 两层 sandwich；判据 = `tests/confetti.test.ts` 14 例）。**两处与规格原文不同，均诚实登记、未改规格本体，待 art 复验**：
>
> 1. **「预分配 scratch 352 float」在现契约下不成立**：`RenderModelBuilder.polygon()` **按引用**保存 points
>    （`render-model.ts`），跨帧共用一块点列缓冲会让上一帧命令跟着变形 ⇒ 采 G3 扫光同判例：
>    **逐枚新建 8-float**，仅在 800ms 窗口内分派（窗口外 `drawConfetti` 早退，零常态开销）。
>    逐帧复用的只有标量态 `CONFETTI_SCRATCH`（x/y/θ/α，命令只抄标量 ⇒ 热路径零分配精神仍守）。
> 2. **公式与文案方向矛盾**：本卡正文写「飘落」，而 `y = spawnY − 1334×1.15×easeIn(p)` 数学上单调递减
>    = 屏底起飞**上行出屏**。按硬纪律「只抄不改」**以公式为准**字面移植（测试亦按公式断言单调递减）；
>    若 art 本意确为下落，需改公式为 `y = spawnY − Δ×easeIn(p)` 的符号反向并重钉 spawn 布局 ⇒ **属规格变更**，归下一轮 v1.5 修订，不在落码批内偷改。
>    **[差异 2 裁定闭合（用户 2026-09-17「认可」，WXG-T-156 burst v2.1 重拍实证）]**：Cocos 产物连拍
>    G6 8/8 相位帧目视 = 顶部炸出→**飘落底部**（p=0.21 成带在顶、p=0.85 落至屏底），与卡文案一致。
>    复核坐标系：设计坐标 **y 向上**（`cellCenter = gridTop − pitch×row` 同族口径）⇒ 公式单调递减
>    本就是屏面下落，旧注「递减 = 上行出屏」系误按 y 向下读。**公式不改、规格本体不改、无 v1.5 修订项**；
>    本差异定性为登记时误读，非实现/规格冲突。
>
> 其余按卡执行：arm 时机 = 裁定 1 延迟门到点 `open()` 同帧（D1 直开路径天然不臂 = 整条关停）；
> 5 色零新 hex（`panel_surface` + `STAR_GOLD` + 珠色 4/5/6 引用既有 token，排除暖橙已入判据）；
> 禁飞带 = 跳过绘制非淡出（被跳枚 α 仍 1，测试验此）；触发全程零玩法事件、零新 clip。

#### 1.6.7 `vfx_denied_press` — 不可填格轻压（**用户已拍板·真源已入 `ux-spec §5`**） `[v1.4·G7]` P2

```
常量：
  DENIED_PRESS_MS              = 120   // 真源 = ux-spec §5「**不可填格轻压**」行（WXG-T-128 已增行；
                                       //   该行值**复用「珠子落座」**，不新造时长 ⇒ 已有源）
  DENIED_PRESS_TROUGH_MS       = 40    // 与 FILL_POP_PRESS_MS 同族
  DENIED_PRESS_SCALE_TROUGH    = 0.96  // 与 FILL_POP_SCALE_TROUGH 同值（同族「单珠形变」语义）
  DENIED_PRESS_RESTART_GATE_MS = 250   // 同格计；**独立于** WRONG_FX_RESTART_GATE_MS 500
  DENIED_RING_LINEWIDTH        = 1     // D1 退化环
  DENIED_RING_COLOR            = slot_border #D8D5E6

触发：placement.ts verdict = `ignored` 且 reason ∈ { `occupied`, `locked` }
  **不触发**：`out-of-bounds` / `no-color`（无格锚点，画不出「被点的格子」）
  裁定来源：用户拍板「加极轻非惩罚反馈」（原 ux-spec §4「静默忽略（零反馈）」为已裁定条款）
  ⇒ 连带 **4 处**规格须回写（见 art-bible §7.3.7）；**美术侧不改 ux/GDD**

逐帧（t ∈ [0,120]）：
  t < 40 : scale = 1.00 − 0.04·easeOut(t / 40)      // 1.00 → 0.96
  t ≥ 40 : scale = 0.96 + 0.04·easeOut((t−40) / 80) // 0.96 → 1.00
  **零位移、零 α 变化、零描边、零色变**
  （与 G1 的区别：G1 起点 1.06 且有 L0a/L0b 联动；G7 起点 1.00 且**无任何联动**
   ——因为没有东西被放进去，只有「按不动」）
  ⚠️ **`[v1.5-r6]` scale 作用基准钉死 = 46（与 §1.6.1 同裁定）**：谷 = 46×0.96 = **44.16**；
     与 G1 同：**不得以 `size` 承载 scale**（`bead-render.ts:118` 的 `outer` 会带动 L11 垫）。
  ⚠️ **`[v1.5-r6]` 落点格的材质事实（本批新取证，直接改变 D1 环的对比结论）**：
     `occupied` ⇒ 该格是 `filled`（**网格侧带 L11 垫**，`view-model.ts:731`）；`locked` ⇒ **无垫**（locked 不走 `padColorIdx`）
     ⇒ 旧算式「环 vs **珠面**」在 `occupied` 侧已被 B′ 换成「环 vs **目标色垫带**」（详下）。

音效裁定（裁定 3，用户 2026-09-16 拍板：**推翻原「零音效」**）：
  **G7 现有音效** = `sfx_denied`（**极轻闷「哒」**），且是**新增第 20 个音频剪辑**
  ——**不是**零音效、**也不是**复用 `sfx_place` 降 volume。
  非惩罚语义靠**音色（闷、短、低频）+ 极低音量**承担，不靠「无声」；
  权威描述已入 `ux-spec §5`「不可填格轻压」行音效列 ⇒「§5 未列的音效一律不发明」纪律已满足。
  （对照：**G6 仍无音效**——纯装饰、不属关键反馈，判例 BD-16；art-bible §7.5 注）

❗ **原子落码约束（硬事实，工程单必须同批提交；拆开即测试红）**：
  A05-24 是**活测试**（`games/beads/tests/audio-dispatch.test.ts` 断言
  `SPEC_CLIPS.length === AUDIO_CLIP_TOTAL` 且 voice 表 ≡ clip 集）⇒ 以下 **5 项必须同批**：
    ① `audio-events §1` 表行（触发源写「`verdict = ignored` 且 `reason ∈ {occupied, locked}`」）
    ② `systems-index §3.12` `AUDIO_CLIP_TOTAL` **19 → 20**（**冻结值变更单** + changelog 新行）
    ③ `tuning.ts` `AUDIO_CLIP_DENIED = 'sfx_denied'`
    ④ `audio-voices.ts` 音色配方（**非冻结**，§3.12 已划界「音色合成参数不入 §3」；
       程序化合成 ⇒ **0 KB、不占包体、无需生成音频文件**）
    ⑤ 测试 `SPEC_CLIPS` 加 id
  限流口径：音频侧 `minInterval` 取 **0.25s** = 视觉重启门 250ms 的换算（**不新造数值**）。
  解除条件与全部登记处 = `audio-events §5`「**Q-A05-5 预约项**」（音频域主理人已立）。
  ⇒ **美术侧只登记约束、不定义音色**（音色属音频域）；本卡不新增音频常量。

不占 §3.8「错误反馈 ≤2 次/秒」预算：① G7 不是错误反馈（点已填格是**无效**操作非**错误**操作）；
  ② 走表现层（零事件、零扣次、零状态写，同 _tapHint 判例）⇒ bead-grid §8-4 计数器断言仍 =0

D1 退化：关 scale ⇒ 退为被点格 **1px slot_border 静态描边环 α=1**（直接复用 drawStateRing，
  120ms 后由 game 侧清除），**0 往复**，与 D1 既有「抖动→静态描边」同族。
  选 slot_border（而非 danger/hintBlue）是刻意的：**不占用** ~~§3.5 accent_blue 环状提示配额~~，
  也不会与 wrong 红环 / hint 蓝环混淆
  ⚠️ **`[v1.5-r6]` 引用纠错（自查新发现，主理人未列）**：~~§3.5 accent_blue 环状提示配额~~ 是 **phantom citation**：
     `systems-index §3.5` 实文 = 「倒计时与失败」（`LEVEL_TIME_DEFAULT` / `TIMER_URGENT_T` / 失败条件 / 失败重开），
     **无任何环状提示配额条款**（`grep -n "accent_blue\|3D7BF5\|HINT" systems-index.md` **零命中**）。
     ⇒ 措辞改为：**art 侧自设的色相预算纪律**（`accent_blue` 已被 `hint` 环与 `selected` 占位）；**同句在 `art-bible §7.3.5` 尾并存，需同批改**（已入回传④/c1）。

**`[v1.5-r6]` D1 退化环的落位重定（承派单 §三-2「环与 2px 垫缝争同一条像素带」）**：
  几何：`drawStateRing(size = BEAD_CELL 50, lineWidth = 1)` ⇒ 笔中线在格缘 ⇒ 覆盖 **格内 0.5px + 格缝 0.5px**；
       而 B′ 的垫带 = 格缘向内 `[46,50]`（**2px**）⇒ **环的内半确实与垫带重合 0.5px**（派单事实成立）。
  三案（均**零新 hex**；线宽不变 1px）：
    **甲（采，零改动）**：维持 size 50 ⇒ 环被两种地色分占（内半 = 目标色垫、外半 = 容器板白）
      ⇒ 两侧对比均过阈（垫侧最不利奶白 ΔL **43.4/255 = 17.0%** ✓；白侧 **39.2 = 15.4%** ✓）
      ⇒ **不需“让位”**：环画在垫**之上**（状态环序最高，§1.2），1px 不透明环盖 0.5px 垫带 ⇒ 无拼色歧义。
    乙（备档）：外扩 size **51** ⇒ 环全落 2px 格缝内 `[50,52]`（地色统一为容器板白）；余量 **1.0px/侧**（优于旧候选 (a) 的 0.5px）`[待真机]`。
    丙（备档）：内移 size **49** ⇒ 环全落垫带内（`[48.5,49.5] ⊂ [46,50]`）⇒ **地色最统一**，代价 = 需新增 `DENIED_RING_SIZE` 呈现层常量（默认 49）与「不再标记格缘」的语义变化。
  ⇒ **未决项 6（柠黄珠上退化环不可辨）因 B′ 自动消解**（本批新结论）：旧值 ΔL **9.1/255 = 3.6% < 4%** 的算式对象是**珠面 `#FFD23F`（206.7）**；
     B′ 后珠收为 46 ⇒ **格缘不再被珠面占据** ⇒ 地色改为目标色垫 ⇒ 最不利者仍为**奶白垫 `#B1ACA3`（172.4）⇒ Δ 43.4 = 17.0%**（10 色全部 ≥ 17.0%）。
     `locked` 侧无垫但地色为 `#B9B4CC` ⇒ 旧值 ΔL **31.6 ✓ 不变**（该侧**未被 B′ 改变**，也未恶化）⇒ **未决项 6 可从「待裁定」降为「已消解、保留真机抽检」**（入 b1）。
     ⚠️ **不软化**：若工程侧将退化环改为**外扩**（候选乙）⇒ 地色重新变回容器板白，消解依旧成立；若改为**环压在珠面上**（内收至 <46）⇒ 旧问题回归，需重算。
D2：**零 α 往复**（本条完全不动 α）；BD-29 口径「闪烁 = 同一区域不透明度往复变化」⇒
  scale 形变不计入闪烁；保守仍给 250ms 门 ⇒ 同格 ≤4 次/秒
clamp：scale ∈ [0.96, 1.00]
A5（**r2 修正：间隙 4px → 3px** ⇒ **`[v1.5-r6]` B′ 重算：3px → 6.92px；旧算式作废留档**）：
  ~~谷 50×0.96 = 48px，符号最小特征 max(2, 48×3/64) = max(2, 2.25) = **2.25 ≥ 2** ✓；
  只有被点那一颗缩到 48，与静止邻珠（50px）的间隙 = 52 − 48/2 − 50/2 = 52 − 24 − 25 = **3px**
  （静息间隙 = 52 − 25 − 25 = 2px ⇒ 间隙**变大 1px**）⇒ **零重叠**，方向安全~~
  **新（基准 46）**：谷 = 46×0.96 = **44.16** ⇒ 半宽 **22.08**；静止邻珠（46）半宽 **23**
    ⇒ 间隙 = `52 − 22.08 − 23 =` **6.92px**（静息间隙本身已是 `52 − 23 − 23 =` **6px** ⇒ 谷时**仅变大 0.92px**）⇒ **零重叠 ✓✓**
    （旧 r2 登记「间隙 3px」与 r1 旧值 4px 均作废；重叠余量由 3px 升为 6.92px = **2.3 倍**）
    （⚠️ 另纠正一处旧笔误：r2 曾写「静息间隙 = 2px」——那是**珠 = 50** 时代的值；B′ 后静息间隙 = **6px**）
  符号最小特征 max(2, **44.16**×3/64) = max(2, 2.070) = **2.07 ≥ 2** ✓（⚠️ 旧值 2.25 作废；**与地板仅差 0.07 ⇒ 若谷再深即触底**，与 §1.6.1 谷帧同值）
  颜色/符号/明度三通道全程不变
图元：**+0**（原地缩放；落点格静态层数不因动画改变——`occupied` 格恒 11 层、`locked` 格恒平涂+斜纹）；D1 退化态 **+1**（描边环）
  ⇒ ⚠️ 旧写法「vs 静息十层」在本卡**从未成立**（G7 不画露缘环 ⇒ 与 B′ 前的 v1.5 同为 +0），本行只是把基线口径补正为 11 层。
```

**G7 vs `wrong` 形态隔离矩阵**（两者可同时在场，**不得共用状态槽**）：

| 维度 | `vfx_wrong_shake`（§1.6.5，惩罚） | `vfx_denied_press`（§1.6.7，确认） |
|---|---|---|
| 色 | `danger` 红描边单峰 | **无**（本色不动） |
| 位移 | ±3px × 2 周期 | **零** |
| 形变 | 无 | scale 谷 **0.96** 单峰 |
| 音效 | `AUDIO_CLIP_WRONG`（低「咚」） | **`sfx_denied`**（极轻闷「哒」，裁定 3 新增第 20 剪辑）——靠音色/音量拉开，**不靠无声** |
| 重启门 | 500ms | **250ms**（独立计时器） |
| 触发 | verdict = `wrong`（颜色不匹配） | verdict = `ignored` + reason ∈ {occupied, locked} |
| 玩法语义 | 你做错了 | 这里放不了 |

#### 1.6.8 VFX 汇总表（毫秒真源 / 常量组 / 图元 / 退化） `[v1.4]`

| 卡 | 名称 | 毫秒真源（`ux-spec §5`） | 常量组 | 图元（绝对/增量） | D1 退化 | 状态 |
|---|---|---|---|---|---|---|
| §1.6.1 | `vfx_fill_pop` | 落座 **120** ✓不改 | 9 | ~~**`[v1.5-r2]` +0（v1.4 材质）／+1（v1.5 材质，S1 露缘条件绘制）**；并发上限 **+3**~~ ⇒ **`[v1.5-r6]` 两代材质归一 = 全窗恒 +0/颗（并发口径作废）**；⚠️ `empty→filled` 的 **+7**（旧 +6）属**状态迁移**、**不得与本列相加**（详 §1.6.1a (4)） | 关 scale/L0b，留 L0a α；**零新增关停项（B′ 下无判据可关，垫恒画）**；⚠️ **新事实：D1 下 L0a 仍落垫 ⇒ “保留 ≠ 可辨”（见 §1.6.1a (2)）** | **待落码（P0）**；⛔ **禁令：不得以 `size` 承载 `scale`**（`bead-render.ts:118` 的 `outer` 会带动垫） |
| §1.6.2a | `vfx_solver_restore`（**G2′**） | **「解环器归位」行 200 + 120/颗、逐颗 80ms** ✓不改 | 7 | **`[v1.5-r6]` 本批补落：净 +3**（相 A 3 颗 × 1 rect-stroke；相 B = G1 ⇒ **+0**）；~~旧值净 +5~~（其“S1 露缘 +2”项在 B′ 下不存在）；绝对 **④1831 / ④48 槽 1927 / ③1675** | 相 A **保留**（纯 α 单峰）；相 B = G1 退化 ⇒ **零新增关停项** | **`[v1.5-r6]` 补卡·待落码（P1）**；前置 P0：view 侧无错位标记 / `affectedCells` |
| §1.6.2 | `vfx_clear_dissolve` | 消除 **200** ✓不改（⚠️ v1.22 后 `ux-spec §5` 另有新行「解环器归位 200+120/颗」⇒ **双源风险**已入跨域清单） | 6 | **本轮 +0**（十层全开，裁定 5 不预埋 LOD）；规格口径（阀②启用后）**240→96（净 −144）**——**`[v1.5-r2]` 构成必标**：此 96 = 24×4 层【**降档珠** L1+L2+L3+L5】，与**终态**的 96 = 24×4 层【**凹陷空槽** S1–S4】**数值巧合、构成不同**（终态 v1.4 = 48 / v1.5 = 96，三行分列详 §1.6.2 末）。⛔ **v1.22 道具反转 ⇒ 批量前提 24→3、作用对象钉死已失效**（卡重立归后续美术单） | 关 scale/dy，留 α | **待落码（P1）· 卡自 v1.22 起为失效规格** |
| §1.6.3 | `vfx_powerup_sweep` | 道具生效 **400** ✓不改 | 9 | +3 | 整条关停 | **待落码（P1）** |
| §1.6.4 | `vfx_complete_wave` | 庆祝 **800** + **20**/列 ✓不改 | 7（**`WAVE_LOD_LAYERS` 值 6→7，非新增常量**） | ~~1560→936（净 −624）~~ ⇒ **`[v1.5-r6]` 双口径：③ 936 vs 1560／④ 1092 vs 1716，两者净均 −624**（**本轮预埋 ⇒ 即成立**）。⛔ **L11 不参与降层**（垫恒画 ⇒ 6 层变 7 层） | 整条关停 | **待落码（P1）** |
| §1.6.5 | `vfx_wrong_shake` | 放错 **200** ✓不改 | 6（已有） | +1 | 静态红描边 | ✅ **已实装** |
| §1.6.6 | `vfx_confetti` | **「结算彩带」行 800** ✓（WXG-T-128 增行；值复用「过关庆祝」，**不新造时长**） | 17 | +44 | 整条关停 | **已定案·待落码** |
| §1.6.7 | `vfx_denied_press` | **「不可填格轻压」行 120** ✓（WXG-T-128 增行；值复用「珠子落座」，**不新造时长**） | 6 | +0（D1 +1）；**`[v1.5-r6]` 基线口径补正：落点 `occupied` 格恒 11 层** | 1px `slot_border` 静态环（**环落 size 50 不变**；⚠️ **未决项 6 因 B′ 自动消解**，详 §1.6.7） | **已定案·待落码**（⚠️ 音效须**原子批**，见 §1.6.7） |
| — | Lv2 伪震屏（G5） | §7.1 既有 **150** ✓不改 | 0（复用 `COMBO_SHAKE_SCALE_MAX`；**案 B 不新增美术常量**，需框架变换接口） | +0 | 整条关停（同 §1.6.4 口径） | **案 B 定案·另立工程单**（跨 `packages/framework`；art-bible §7.3.5） |

> **常量总计**：v1.4 新增 **6 组 ≈54 个**呈现层常量，**全部归 `src/config/tuning.ts`**（承 v1.3 判例：「反馈/呈现层参数，不进 `systems-index §3`」）⇒ **本轮零新增冻结常量**（清单见回传②）。
> **色值总计**：`CONFETTI_COLORS`（5 项）= **既有 token 的数组组合**，`SWEEP` 用 `BEAD_HIGHLIGHT_HEX`、`DENIED_RING` 用 `palette.slotBorder` ⇒ **`palette.ts` 零新增 hex 字面量**。
> **`[v1.4-r2]` 毫秒真源列口径（裁定 4）**：G6/G7 两行原标「**§5 无此行**，复用 800/120（未决项 3）」⇒ 已由 ux 域**增行落地**，现改为**真源行名**「`ux-spec §5`「结算彩带」行」/「「不可填格轻压」行」，**未决项编号清除**。
> **`[v1.4-r2]` 图元列双口径（裁定 5 连带）**：本表 G2 行同时给出**本轮落码口径**（+0，十层全开）与**规格口径**（−144，阀②启用后）⇒ 道具期总增量由 **−141** 变为 **+3**；两套口径均有效，详见 §1.6.9 末与 `art-bible §7.4`。
> **`[v1.5-r2]` 本列升级为「双材质口径 × 三基线」**：上表图元列的**增量/净值与基线无关**，但**绝对值必须指明基线**（1810 / 2173 / 1672）⇒ **三套基线 + 两套材质口径 = 六组数，不得任取两组相加**。完整并列表见 **§1.6.9 末**（本表只留增量/净值，不再重列绝对值，防两处不同源）。**G1 行已由 +0 改为 +0/+1 双材质口径**（任务 C 自算结果，详 §1.6.1a (4)）。

#### 1.6.9 LOD 与图元核算 + 动画期四级回退阀 `[v1.4]`

**对主理人「动画中的珠子降层绘制」建议（约束 5）的分级回应**：

| 卡 | 降层？ | 层档 | 理由（可驳） |
|---|---|---|---|
| §1.6.1 G1 | **不降** | 十一层（十层珠体 + L11 垫） | **驳回**（理由已**第三次**改）。~~单颗原地缩放，图元增量本来就是 **+0** ⇒ 降层零收益~~（v1.4）→ ~~**+1 的来源是 S1（凹坑缘）、不是珠体层 ⇒ 降珠体层数对 +1 零影响~~（`[v1.5-r2]`；**字面前提已随 B′ 消失**）。**现行理由（`[v1.5-r6]`）**：动画期增量回到 **+0** ⇒ 降层在**帧预算上零收益**；而可砍集内只剩**垫（谜面载体，不可砍）**与**珠体高光层 L2/L3b/L4x**（砍 = 「珠子瞬间变平」的**质感塌陷**，与 v1.4「要质感」的原始拍板直接相左）⇒ **驳回**（逐帧口径见 §1.6.1a） |
| §1.6.2a G2′ | 相 A **无层可降**；相 B 同 G1 **不降** | 相 A +1 rect-stroke/颗；相 B 十一层 | **`[v1.5-r6]` 本批新增行**。相 A 已是**最小实现**（1 命令/颗，`drawStateRing`）⇒ 降无可降；要降只能**砍颗数**（= 改玩法可见集，属 core 域 ⇒ **驳**）。相 B = G1 同结论。真机承压时的合法降量出口：**并发颗数门**（同帧只允许 1 颗在归位，代价 = 归位总时长拉长，需主理人定） |
| §1.6.7 G7 | **不降** | 十一层（落点 `occupied` = `filled`）／locked 平涂+斜纹 | 同上；且同屏最坏只有 **1 颗**在动。**`[v1.5-r2]` 补正仍成立**：G7 的 +0 不随材质变化——落点格永远是 `filled`/`locked`、**永无 S1 凹坑** ⇒ 无露缘环可画。⚠️ **`[v1.5-r6]` 两句旧结论同时改**：① 「G1 与 G7 在 v1.5 下图元口径分岔」——**分岔已消失**（G1 回到 +0 ⇒ 两者永久同为 +0）；② 基线口径由十层补正为**十一层**（`occupied` 格带垫 ⇒ 与 §1.6.2a 的「同墨不同框」论证同根） |
| §1.6.2 G2 | **规格保留、本轮不预埋**（裁定 5；= 回退阀②） | 本轮：**十层全开**；规格（阀②启用时）：α≥0.6（**τ ≤ 80ms**）十层；α<0.6（**τ > 80ms**）**4 层** | **本轮不预埋理由（裁定 5）**：先十层全开落码，降层分支只留规格 ⇒ 溶解期图元恒 24×10 = 240（净 0），道具期总增量 **+3**（见 §1.6.9 末双口径）。**规格理由保留不删（阀②启用时的判据）**：主理人原建议为「溶解期只画 L1 主体 + L5 符号」（2 层），本卡改为 α 阈值——α≥0.6（**τ ≤ 80ms**）正是「**看得见质感在溶解**」的关键窗口；且 2 层会丢 3D read ⇒ **保留 L2/L3**（暗/亮倒角）；**六层全砍**（L0a/L0b/L3b/L4a/L4b/L4c）在 α<0.6 时实际 α ≤0.048，**不可辨**。<br>⚠️ **阈值时间复算（r2 修缺陷 1）**：本卡定 `α = 1 − q`（**线性**）⇒ α ≥ 0.60 ⇔ q ≤ 0.40 ⇔ **τ ≤ 200×0.40 = 80ms**。❌ 旧值 **126ms 作废**——那是把 α 当 **easeIn** 反解的结果（`1−p² = 0.6 ⇒ p = 0.632 ⇒ 126.5ms`），与本卡的线性 α 自相矛盾；**若按 126 落码会多画 46ms 十层 = 白烧 CPU**。（`scale` 用 easeIn、`α` 用 linear 是**刻意不同缓动**，两者阈值**不得互推**。） |
| §1.6.4 G4 | **采纳·本轮预埋**（裁定 5 唯一预埋项） | ~~**6 层**（传 `lodLayers = 6`）~~ ⇒ **`[v1.5-r6]` 7 层**（旧六层 + **L11 垫不降**；`WAVE_LOD_LAYERS` 值 6→7） | G4 瓶颈是 **CPU 逐图元重算几何**（156 颗 × 每帧 10 层坐标）而非图元数；~~6 层 = 936 vs 1560，**−40%**~~ ⇒ **`[v1.5-r6]` 双口径：③ 936 vs 1560（−40%）／④ 1092 vs 1716（−36%）**。保 L4c 核高光（糖果感最亮一层）。⛔ **垫不得进入可砍集**（三条理由见 §1.6.4 LOD 行）。**恢复满层的瞬间恰逢遮罩落下 ⇒ 跳变被遮**——`[v1.4-r2]` 此句已由「前提：未决项 1 采 (a)」升为**定案事实（裁定 1）**：结算面板**延迟 800ms 开**，庆祝先行放完再落遮罩（真源 `ux-spec §5`「过关庆祝」行视觉列 + `bead-grid §4` 表注） |
| §1.6.3 G3 | 不适用 | 3 polygon | 与珠数无关 |
| §1.6.6 G6 | 不降层；**枚数本轮亦不预埋**（裁定 5，= 阀③只留规格） | 44 polygon | 独立装饰层；降量走**枚数**而非层数（阀③） |

**动画期四级回退阀**（原则：**先砍动画期细节，最后才砍静态质感**；需真机帧率依据，**由主理人裁定启用**）：

| 级 | 动作 | 效果 | 代价 |
|---|---|---|---|
| ① | ~~G4：6 层 → **4 层**（L1+L2+L3+L5）~~ ⇒ **`[v1.5-r6]` G4：7 层 → 5 层**（L11 垫 + L1+L2+L3+L5；旧六层中的 L0b 可砍、**垫不可砍**） | ~~936 → **624**（再 −33%）~~ ⇒ ③ 936→**624**（不变，因两侧同时 +156）／④ **1092 → 780**（再 −29%） | 庆祝期丢 L0b 投影与 L4c 高光，糖果感略降 |
| ② | **G2：预埋 α 阈值 LOD**（本轮**不预埋**，裁定 5）——α≥0.6（**τ ≤ 80ms**）十层 / α<0.6（**τ > 80ms**）**4 层【降档珠 L1+L2+L3+L5】** | 溶解期 **LOD 后半窗** 240→96（净 −144）⇒ 道具期总量由 **+3** 回到 **−141**（绝对值分基线：1810⇒**1669** / 2173⇒**2032** / 1672⇒**1531**）。**`[v1.5-r2]` 判据重述（防语义混淆）**：本阀只改 **τ ∈ (80,200) 的珠体层数**，**不改终态**；终态（τ≥200、槽 `free`）永远由 §1.2 `empty` 卡接管 = **v1.4 材质 24×2 = 48 / v1.5 材质 24×4 = 96**，**与阀② 是否启用无关**（两处的 `24×4 = 96` 数值巧合、构成不同：一为降档珠、一为凹坑 ⇒ 详 §1.6.2 末三行分列）。⛔ **v1.22 后本阀的批量前提（24 枚）已失效**（≤`SOLVER_PLUS_COUNT 3`）⇒ 启用收益同比例缩小（−144 → 约 **−18**），**不再是有效的降配手段** | 需新增逐珠 α 判定分支；**阈值论证（α≥0.6 ⇔ τ ≤ 80ms）保留为规格理由**，即本阀启用时的判据 |
| ②-b | 若 ② 仍不够：G2 取消 α 阈值，**全窗口 4 层**（= 主理人原建议） | 代码路径更简 | 丢「看得见质感在溶解」的关键窗口 |
| ③ | G6：`44 → 24` 枚（**本轮不预埋**，裁定 5） | +44 → **+24** ⇒ 庆祝期净 −580 → **−600** | 彩带密度降，节日感略减（仍覆盖全宽） |
| ④ | **最后**才动静态层（§1.1 既有阀：合并 L4a+L4b 降 8 层）——⚠️ **`[v1.5-r6]` 本行重写并移回表末**（上一次编辑曾把新行误插在 ① 之后 ⇒ 表内一度出现两个 ④、行序 `①④②②-b③④`；此处为**自我纠错、非回退他人内容**） | ~~1560 → 1248~~ ⇒ **`[v1.5-r6]` 双口径：基线 ③（无垫）1560 → 1248；基线 ④（含垫 11 层/颗）1716 → 1404**（合并后珠体 9 层 + 垫 = 10 层/格，**绝对值必带基线标签**，见下禁令 ①③） | **伤及 v1.3 已入库的静态质感** ⇒ 非到不得已不用；⚠️ **本阀也不得顺带砍 L11**（与 §1.9.5 末静态回退阀的“可砍集”同口径：**砍序 = S3/S4 两线 → 彩带枚数 → 珠体高光层；垫永远在后**） |

##### `[v1.5-r2]` 图元**三基线并列核算表** → **`[v1.5-r6]` 扩为「四基线 × 双托盘规模 × 规格/代码双口径」**（任务 B 主体；取代原「双口径」两行散文）

> **为何从「双口径」升为「三基线 × 双材质」**：原 `[v1.4-r2]` 双口径（规格 / 本轮落码）只区分了**动画期的 LOD 预埋与否**，但**共用同一个静态基线 1810**。v1.5 把 `empty` 由 2 层改 4 层、v1.22 又把**托盘由「最坏 24 颗 filled 珠」改判为「恒 24 个空槽」**（托盘零读写）⇒ **静态基线本身出现了三个互不相容的取值**。主理人复核发现「同一文件内 1810 与 2173 并存且无交叉引用」即源于此。

> **`[v1.5-r6]` 本批再扩两轴（派单 §二 连带）**：① **B′（WXG-T-142）** 给网格 filled 珠加了 **L11 垫** ⇒ 新增基线 **④**；② **v1.24** 把托盘由「24 实槽」改判为「**基础 24 / 扩展 48**」⇒ 每个绝对值必须同时带 **基线标签 + N**。旧表（①②③ 与 N=24）**不删、不重写**，仅作 `empty`/托盘材质史与上界保留。

**`[v1.5-r6]` 参数化构成式（把 N 代入即可复算，避免逐表背数）**

- **N ≡ 托盘实槽数**：规格口径 v1.24 **基础态 N=24**（2 行×12）／**扩展态 N=48**（4 行×12）；**代码口径 N=12**（`tuning.ts` 现仍 `TRAY_BASE_SLOTS / TRAY_EXPAND_SLOTS = 12 / 12`，**漂移中**）⇒ 承 `[v1.5-r2]` 的「规格/代码漂移」判例，本批**同样双口径并列、不改代码**。
- **层数事实**：网格 `filled` = **11 层**（10 珠体 + L11 垫）／网格 `empty` = **4 层**（S1–S4）／托盘 `filled` = **10 层**（**无垫**：`view-model.ts` 仅网格侧传 `padColorIdx`）／托盘 `free` = **4 层**／**虚线预告槽 = 1 层（层级口径）＝ 20 条 `line`（命令口径）**。
  · **①** `= 背景 3 + 板/band 7 + 网格 156×10 + 10N =` **`1570 + 10N`**
  · **②** `= ① + 156×2 + 2N + 3 =` **`1885 + 12N`**（⚠️ 松上界，见下表 ② 行警示）
  · **③** `= 156×10 + 4N + (10 + 3 + 3) =` **`1576 + 4N`**
  · **④** `= ③ + 156×(+1 垫) =` **`1732 + 4N`**
- ⚠️ **`h` 不变量在 48 槽下的取值（派单点名重给）**：
  · **③ 恒等仍成立**：一颗珠 网格→托盘 = 网格 `−10+4 =` **−6** / 托盘 `+10−4 =` **+6** ⇒ **相消**，与 `h` 无关；**N 变为 48 不改变斜率**（斜率只由「两侧层差」决定）。
  · ⛔ **但 ④ 打破了恒等**：网格 filled 升 11 层 ⇒ 移出 = 网格 `−11+4 =` **−7** / 托盘 `+10−4 =` **+6** ⇒ **净 −1/颗** ⇒ **`④(h) = ④ − h`**：N=24 `1828 → 1804`（h∈[0,24]）／N=48 `1924 → 1876`（h∈[0,48]）／代码 N=12 `1780 → 1768`。
  · ⛔ **旧「h ≤ 12」的取值域作废**（那是 v1.23 6 槽 / 代码 12 槽时代的界）⇒ 现行域 = `h ∈ [0, min(156, N)]`。
  · **诚实登记（不软化）**：v1.22 起**托盘零读写 ⇒ `h ≡ 0`** ⇒ ③ 的恒等与 ④ 的斜率在现行玩法下**都不可达**；登记目的是**防止将来误用 ③ 的恒等去推 ④**（两套基线的守恒性质不同，这是 B′ 引入的**新失真面**）。
  · **交叉校验（同一事实的两种数法必相等）**：一次「放珠」= 网格 `empty→filled` **+7** ⊕ 托盘 `filled→empty` **−6** ⇒ 净 **+1** ⇔ `④(h) − ④(h+1) = 1` ✓（与 §1.6.1a (4) 的「状态迁移 +7」同源、不重复计数）

**三条基线的构成与适用版本边界（逐步可复算）**

| # | 基线 | 构成算式 | **适用版本边界** | 性质 |
|---|---|---|---|---|
| **①** | **1810** | `背景 3 + 容器板/band 7 + 网格最坏 156×10 = 1560 + 托盘最坏 24×10 = 240` | **= v1.4 基线**（`empty` = **E1 混色底 + E4 幽灵符号 2 层**；托盘可持珠 = v1.21 及以前的供料玩法） | v1.4 记账原文。**主理人已逐条复算验真 ⇒ 数字保留不删**。⚠️ `[v1.5-r2]` **自查新发现（不改冻结值，只登记）**：本基线**未计托盘面板自身 3 层**（投影/板体/描边，§1.3）⇒ 若补计 = **1813**，与下表「道具期·本轮落码 1813（= 1810 + 扫光 3）」**数值巧合、构成完全不同**（一为面板层、一为扫光 polygon）⇒ 引用 1813 时**必须写明是哪一个** |
| **②** | **2173** | `1810 + 网格空槽 156×(+2) = +312 + 托盘空槽 24×(+2) = +48 + 托盘微拱白瓷内阴影 +3` | **= v1.5 基线**（`empty` = **纯色凹陷四层 S1–S4**；托盘面板 +3 段内阴影） | ⚠️ **`[v1.5-r2]` 更正：2173 是「松上界」、不是可达单帧值**——它把「网格**全 filled** 的 1560」与「网格**全 empty** 的 v1.5 增量 +312」**叠加**，而这两者**同帧互斥**（一格非 filled 即 empty）⇒ 用作**压力测试上界**可以，用作**性能预算真值**会高估。可达真值走基线 ③ |
| **③** | **1672**（N=24） | `网格全 filled 156×10 = 1560 + 托盘全 empty 24×4 = 96 + 背景/板 10 + 托盘面板 3 + 微拱白瓷内阴影 3` | **= v1.5 材质 + v1.22 构型的可达真值**（v1.22：开局满盘、**托盘零读写 ⇒ 托盘恒为空槽**、托盘已扩展至 24 实槽；⚠️ **`[v1.5-r6]` 补注：v1.24 起基础态即 24、扩展态 48 ⇒ 本行的 1672 = 旧「扩展后」值，现读作「基础态」**） | **`h` 不变量（已验证）**：一颗珠从网格移到托盘 = 网格 `−10+4 =` **−6** / 托盘 `+10−4 =` **+6** ⇒ **相消**，与手持珠数 `h` 无关（实测 `h=0 → 1560+96`、`h=8 → 1512+144`、`h=16 → 1464+192`，**三者总和恒 1672**）。<br>~~**未扩展托盘**（12 实槽 ×4 = 48 + 12 虚线槽 ×1 = 12）⇒ **1636**~~ ⇒ ⛔ **`[v1.5-r6]` 本行作废（v1.24）**：12/12 仅作为**代码侧漂移真值**存在，不再叫「未扩展」；现行同族值（含虚线预告槽）见下「双托盘规模并列表」。<br>**v1.4 材质同构型对照** ⇒ **1621**（托盘空槽按**规格** 2 层）／ **1597**（按**代码真值** 1 层：`view-model.ts:765` 不传 `colorIdx` ⇒ 无 E4） |
| **④** | **1828**（N=24 规格）／**1924**（N=48 规格）／**1780**（N=12 代码） | `③ + 网格 156×(+1 L11 垫) = ③ + 156` ⇒ `1732 + 4N` | **= v1.5 材质 + v1.22 构型 + `B′`（WXG-T-142）的唯一现行真值** ⇒ **本批起凡引用绝对值默认指此基线** | **`[v1.5-r6]` 本批新增基线**（派单 §二 要求）。⚠️ **垫只在网格侧** ⇒ +156 而**不是** +156+24/48；⚠️ **④ 不再满足 `h` 不变量**（斜率 **−1/颗**，推导见上）。⛔ **不得把 ④ 与 ③ 混用**：两者只差「垫」，而垫**不参与任何动画**（§1.6.1 层序死结论）⇒ 动画期**净值在两套基线上完全相同**，只有**绝对值**差 156。 |

**净值 × 基线 并列开列（净值 = 动画期增减量，**与基线无关**；绝对值 = 基线 + 净值）**

> ⚠️ **`[v1.5-r6]` 本表的三列绝对值均为「`N=24` 且无 L11 垫」（基线 ①②③）的 `[v1.5-r2]` 口径 ⇒ 保留供追溯，**不再作为现行引用**；现行基线 ④ 的双托盘规模见下张表。本表内只修正**已被 B′ 改判的两行**（单珠落座期 / 解环器期），其余行不动。

| 场景 / 口径 | **净值** | ① v1.4 基线 **1810** | ② v1.5 基线 **2173**（松上界） | ③ v1.5 材质 + v1.22 **1672**（可达真值） |
|---|---|---|---|---|
| 道具期 · **规格口径**（阀②③全启用） | **−141** | **1669** | **2032** | **1531** |
| 道具期 · **本轮落码口径**（G2 十层全开不预埋、G6 44 枚、**G4 已预埋 6 层**；净 +3 = 仅 G3 三层白光） | **+3** | **1813** | **2176** | **1675** |
| 庆祝期（G4 六层 −624 + G6 +44，**本轮即成立**） | **−580** | **1230** | **1593** | **1092** |
| 庆祝期 · **若再启阀③**（G6 44→24） | **−600** | **1210** | **1573** | **1072** |
| **单珠落座期**（G1） | ~~**+1 ～ +3**（`[v1.5-r2]` 任务 C 自算：+1/颗、并发上限 +3）~~ ⇒ ⛔ **`[v1.5-r6]` 净值 = +0**（B′ 后「S1 露缘」条件消失，详 §1.6.1a (4)；**「并发上限 +3」口径一并作废**） | 1810 | 2173 | **1672** |
| **G7 轻压**（v1.5 下**仍 +0**，落点格无 S1 凹坑，见上表理由列） | **+0** | 1810 | 2173 | 1672 |
| **G5 案 B**（框架全局变换，图元数不变） | **+0** | 1810 | 2173 | 1672 |
| ⛔ **解环器期真值**（v1.22 玩法；**仅基线 ③ 适用**，见下推导） | ~~**+5**~~ ⇒ **`[v1.5-r6]` +3**（旧值的「S1 露缘 +2」项在 B′ 下不存在 ⇒ 详 §1.6.2a） | — | — | **1675**（旧 1677） |

**`[v1.5-r6]` 现行基线 ④（v1.5 材质 + B′）× 双托盘规模并列（派单 §二 要求；绝对值 = ④ + 净值）**

> 净值与基线、与 `N` 无关（除非本行注明）；代入式 `④(N) = 1732 + 4N` 可逐格复算。四个口径列齐（规格 base / 规格 expanded / 代码 base / 代码 expanded）⇒ **不再让读者自己推**。

| 场景 | 净值 | **④ N=24·rows=2**（规格基础态 **1828**） | **④ N=48·rows=4**（规格扩展态 **1924**） | **④ N=12·rows=1**（代码基础 **1780**） | **④ N=24·rows=2**（代码扩展 **1828**） |
|---|---|---|---|---|---|
| 静息（现行真值） | 0 | **1828** | **1924** | **1780** | **1828** |
| 单珠落座期 G1／G7 轻压／G5 案 B | **+0** | 1828 | 1924 | 1780 | 1828 |
| 解环器期 **G2′**（相 A +3 ⊕ 相 B +0，详 §1.6.2a） | **+3** | **1831** | **1927** | **1783** | **1831** |
| 道具期（G3 扫光 3 polygon，本轮落码口径） | **+3** | **1831** | **1927** | **1783** | **1831** |
| 若 G2′ 与 G3 同帧（解环器使用瞬间） | **+6** | 1834 | 1930 | 1786 | 1834 |
| 庆祝期（G4 七层 −624 + G6 +44，**本轮即成立**） | **−580** | **1248** | **1344** | **1200** | **1248** |
| 庆祝期·若再启阀③（G6 44→24 枚） | **−600** | **1228** | **1324** | **1180** | **1228** |
| 手持珠数 `h`（h 颗由网格移入托盘；v1.22 起 **`h ≡ 0`**） | **−1/颗**（④ 专属；③ 为 0） | `1828 − h`，h∈[0,24] | `1924 − h`，h∈[0,48] | `1780 − h`，h∈[0,12] | `1828 − h`，h∈[0,24] |
| 基础态 + **逐槽虚线预告 24 个**（§1.3.2 **备用**、不默认） | **+24（层级口径）**／**+480（命令口径：24 × 20 条 `line`）** | **1852** | —（扩展态 48 槽全为**实槽** ⇒ 无预告位） | —（rows=1，无预告行） | —（rows=2 = 全实槽，**现码无虚线槽**） |
| 基础态 + **V1 预告胶囊**（§1.3.2 建议案：不带/带 `ad_badge` 角标） | **+2 / +4** | **1830 / 1832** | —（扩展态按钮收起 ⇒ 无需预告） | — | — |

> ⚠️ **两条必须随表同读的诚实注**：
> ① **解环器期 +3 与道具期（G3）+3 是「数值巧合、构成不同」**（前者 = 3 × 1 `rect-stroke` 高亮环；后者 = 3 × 1 `polygon` 扫光）⇒ 引用 “1831” 必须写明是哪一个；与 §1.6.9 末基线 ① 的「1813 二义」为同一类陷阱。
> ② **阀②（G2 α 阈值 LOD，净 −144）不列入本表** —— 其批量前提（24 枚同帧溶解）已随 v1.22 失效（≤ `SOLVER_PLUS_COUNT 3` ⇒ 约 −18），**不再是有效降配手段**（原文字保留于上表阀②行，不删）。

> **⛔ 三套口径不得混用（纪律，承 `[v1.4-r2]` 双口径注写法并升级）**：
> ① **不得跨基线相加**——「1810 的 −141」与「2173 的 +3」相加得 1951 **无任何物理意义**；
> ② **不得跨材质口径相加**——「规格口径 −141」与「本轮落码口径 +3」是**同一场景的两种落码决策**、互斥；
> ③ **不得把净值当绝对值**——净值与基线无关，绝对值必须带基线标签；
> ④ **不得把基线 ② 当性能预算**——2173 是松上界（构成互斥项叠加），真值走 ③；
> ⑤ ~~**不得把「状态迁移 +6」与「动画期 +1」相加**~~ ⇒ **`[v1.5-r6]` 现行：不得把「状态迁移 **+7**」与「动画期 **+0**」相加**（详 §1.6.1a (4)；两套旧值均随 B′ 作废）；
> ⑥ ⛔ **`[v1.5-r6]` 新增：不得省略 `N` 标签**——`1828` 在规格口径是**基础态**（rows=2）、在代码口径是**扩展态**（rows=2，同构型）；`1924` 只在规格扩展态存在，代码侧永远达不到 ⇒ 引用绝对值必须写「**基线 + N + rows**」三件；
> ⑦ ⛔ **`[v1.5-r6]` 新增：不得把 §1.6.11（G11）的 +5 / +8 当现行规格值引用** —— 该节正文**不存在**（`[v1.5-r4]` 头注虚报，与本批 §1.6.2a 同类），相关数字一律标**估计值**（详 §1.6.2a 末诚实注）。

> **⛔ 解环器期真值推导（v1.22；基线 ③ = 1672）**：
> · 批量上限 = `SOLVER_PLUS_COUNT 3`（**不是**旧 24）⇒ 同帧最多 **3 颗**在动；
> · ~~`ux-spec §5`「解环器归位」= **200ms + 120ms/颗、逐颗 80ms 间隔** ⇒ G1 并发 = `ceil(120 / 80) =` **2** 颗处于落座窗内 ⇒ **S1 露缘 +2**~~ ⇒ ⛔ **`[v1.5-r6]` 本条的图元推论作废**（「S1 露缘」项在 B′ 下不存在，详 §1.6.1a (4)）。⚠️ **但并发量本身仍保留登记（与 §1.6.1a (4) 同式）：**`ceil(120 / 80) =` **2** 颗处于落座窗内作为**“几颗同时在动”的 LOD 关注量仍成立**，只是它**不再带图元代价**（归位动画期 +0/颗）⇒ 仅用于风险描述，不得计入净值。
> · 错位珠高亮闪 ≤ 3 颗 × 1 层 = **+3**。⚠️ **诚实标注：此「高亮闪」尚无美术卡**（v1.22 新增玩法，`grep -rn misplaced games/beads/src/view/` **零命中** ⇒ 代码侧亦无错位标记）⇒ 本项按**最小实现口径（1 层/颗）估算**；**若后续立卡增加层数，本行必须重算**。该缺口已作为 **P0 待审批项**登记于 `accessibility.md §5` 第 **4** 条（filled 格底色不可见 ⇒「找错位珠」谜面不可读，含三候选与图元代价）。
> · ⇒ ~~**解环器期净（最坏）= +3 + 2 = +5**；绝对 = `1672 + 5 =` **1677**~~ ⇒ **`[v1.5-r6]` 现行：净 = 相 A（+3）⊕ 相 B（+0）= +3**；绝对 = ③ `1672 + 3 =` **1675**（旧 1677）、**④ 现行基线 = `1828 + 3 =` 1831**（N=24）／**1927**（N=48）／**1783**（代码 N=12）。⇒ **旧值恰好多算 2**（= 已作废的露缘项），与 §1.6.2a 一致。
> · **与 T-128 旧值的落差**：旧「道具期 −141」的最坏场景（24 枚同帧溶解 + 阀② 降层）在 v1.22 下**根本不存在** ⇒ **−141 已不可达**；同理旧「+3」也只在 G3 扫光单独成立时有效。**T-128 道具期双口径的前提整体失效**，本行为其替代值。

> **结论修订（`[v1.5-r2]` 三度修订，前两版均保留可追溯）**：
> · `[v1.4]` 原结论：「六条动画在最坏情况下反而降低总图元数」。
> · `[v1.4-r2]` 修订：该结论**只在规格口径成立**；本轮落码口径下道具期**微增 +3**、庆祝期仍 **−580**。
> · `[v1.5-r2]` 再修订：**上述两版结论都建立在「基线 1810 + 批量 24」上，v1.22 后双双失效**。当前有效结论为：**（a）庆祝期 −580 仍成立**（G4/G6 与道具语义无关，三基线下均为负）；**（b）道具期在 v1.22 下不再是「降图元」场景，而是净 +5**（解环器期，基线 ③）；**（c）单珠落座期由 +0 变 +1/颗**（v1.5 材质的必然代价，并发上限 +3，量级可忽略）；**（d）真正的性能风险仍不在图元数，而在 CPU 重算路径**（逐珠 scale × 逐层坐标；G5 走案 B 全局变换 ⇒ **零逐珠重算**）**与真机未验**（ADR-0009 P2）。**（e）静态基线本身的抬升（v1.4→v1.5 的 +363 松上界 / 同构型 +51～+75 典型值）远大于任何动画期增量** ⇒ 若真机承压，**优先动 §1.9.5 末的静态回退阀（空槽 S3/S4 两线先砍，+2→0/槽）**，而不是动画期阀①②③。
>
> **`[v1.5-r6]` 第四度修订（承上保留全部旧版；本批只改与 B′ / v1.24 直接相关的四项）**：
> · **（b′）** 旧「（b）道具期在 v1.22 下是净 **+5**」**修正为 +3**；旧「（c）单珠落座期由 +0 变 +1/颗」**整体作废 ⇒ 动画期回到 +0**。⚠️ v1.4 时代的结论在 B′ 下**重新成立、但理由完全不同**（旧为「单颗原地缩放」，新为「露缘项消失 ⇒ 全窗恒 +0」）⇒ **不得据旧理由推新场景**。
> · **（f）真图元风险从动画期转回静态与命令口径**：动画期增量收敛在 **−624 ～ +6**；而静态侧由 ③ 的 1672 抬到 ④ 的 **1828（N=24）/ 1924（N=48）** ⇒ 若真机承压，**第一序仍动静态（§1.9.5 末阀）与预告行（§1.3.2 逐槽虚线已降为备用）**。⛔ **本批新登记一类风险：命令口径的雷不在 `rect`/`circle`、在 `drawDashedRect` 合成（1 层 = 20 条 `line`）** ⇒ 若回升逐槽虚线，**层级口径的 +24 实为命令口径的 +480**（§1.3.1 副产品 ② 同根）`[待真机]`。
> · **（g）垫（L11）不参与任何降层阀** ⇒ 基线 ④ 的 **+156 是唯一不可回收的增量**（§1.6.4 三条理由 + 阀①④ 禁令）⇒（e）（f）的全部砍序里 **垫永远在末尾**。
> · **（h）“槽越多图元越多”在含预告行的对比下不成立**：扩展态 **1924**（N=48，全实槽、无预告）反而**比“基础态 + 逐槽虚线”的 1852 贵 72**，但两者都低于「基础态 + 逐槽虚线 + 手持珠」等复合上界 ⇒ **引用前必带 `N` + rows + 是否含预告行三件**（禁令 ⑥）。

#### 1.6.10 落码骨架判例（`_wrongFx` **1:1 可复用**） `[v1.4]`

> 本节不是实现方案（本轮零代码），而是把**现成骨架**钉下来，让工程不必发明新模式。依据：`games/beads/src/game/beads-game.ts::_wrongFx / _armWrongFx / _stepWrongFx` + `src/view/view-model.ts::wrongFlashAlpha`。

```
game 侧（表现层，零玩法状态，L5）：
  1) 私有计时槽  private _fillPopFx: { row: number; col: number; elapsedMs: number } | null = null;
  2) 重启门      private _fillPopArmedAtMs = Number.NEGATIVE_INFINITY;
                 if (this._pulseClock − this._fillPopArmedAtMs < FILL_POP_RESTART_GATE_MS) return;
                 this._fillPopFx = { row, col, elapsedMs: 0 }; this._fillPopArmedAtMs = this._pulseClock;
  3) 推进        _stepFillPopFx(dt) { fx.elapsedMs += max(0,dt)*1000;
                            if (fx.elapsedMs >= FILL_POP_MS) this._fillPopFx = null; }
  4) **PAUSED 不冻结**：与 _stepComboVfx/_stepWrongFx/_stepTapHint 同列在 update() 的表现层
     推进段（既有判例：表现层动画不受相位门控）
  5) 快照字段（只读派生）：fillPopRow / fillPopCol / fillPopProgress ∈ [0,1]

view 侧（纯函数包络，零分配）：
  6) export function fillPopScale(progress: number): number {
       const ms = progress * FILL_POP_MS; …… }      // 同 wrongFlashAlpha(p) 的写法
  7) drawGrid 中：const isPop = i === snap.fillPopRow && j === snap.fillPopCol
                              && snap.fillPopProgress > 0;
  8) drawFilledBead(builder, bx, cy, cell.colorIdx,
       isPop ? { scale: fillPopScale(snap.fillPopProgress) } : undefined)

  9) **drawFilledBead 需扩 FilledBeadOptions**（现仅 size / lift / shadowAlpha）：
       + scale?: number         // 全局缩放（G1 / G4 / G7 **三条共用**；
                                //   `[r2·裁定 2]` G5 走**案 B 框架全局变换**，不占本通道）
       + masterAlpha?: number   // 整体 α 乘子（G2）
       + contactAlpha?: number  // L0a α 覆写（G1）
       + contactWidth?: number  // L0a 宽比覆写（G1）
       + shadowDyN?: number     // L0b 偏移覆写（G1；建议与既有 shadowAlpha 合为一组）
       + lodLayers?: 4 | 6 | 10 // LOD 档（**通道保留**；`[r2·裁定 5]` **本轮只有 G4 会传**
                                //   `lodLayers = 6`；G2 的 α 阈值降层**规格保留、本轮不传**）
     ⇒ **一个 options 通道服务四条动画**（G1 / G2 / G4 / G7；G5 另走框架），
       避免每加一条动画就改一次签名

 10) **G5（案 B，裁定 2）不走本 options 通道**：需给 `RenderModelBuilder` 增**全局变换语义位**
     （`setTransform({ scale, anchorX, anchorY })` 一类）。**代码事实**：`render-model.ts` 现仅
     `begin(background?)` / `build() → { background?, commands }`，**无任何变换字段**，且
     `_commands` 为 `private readonly`（WXG-T-074 已登记）⇒ 视图侧无法后置改写。
     ⇒ **跨 `packages/framework` 域、另立工程单**（不在本美术单范围）；
       **三条未验风险见 `art-bible §7.3.5`**，其中「`FIXED_WIDTH` 下整屏放大 1.015
       **可能露黑边**」是**落码前置验证项**（未验证，不得当已解决）。

批量动画的槽位设计（**热路径零分配**）：
  · G2：_dissolveFx: { elapsedMs } + **预分配定长** Int8Array(24) + count（不用 push）
  · G4：**无需存珠列表**——列号由 drawGrid 的 j 直接算 ⇒ 快照只暴露
        waveProgress + waveCols 两个**标量**，零数组分配
  · G3 / G6：_sweepFx / _confettiFx 均为**单标量** elapsedMs（44 枚全由 idx 派生）
  ⇒ **G3/G4/G6 的快照字段全是标量**，这是「零 RNG + 索引 lattice」设计的直接红利

常量归位（**全部 → src/config/tuning.ts**，不进 systems-index §3）：见 §1.6.1–§1.6.7 各卡首行。
色值归位（**全部 → src/view/palette.ts**，hex 只住 palette）：见 §1.6.8 注 ⇒ **零新增 hex**。
```

### 1.7 拼图容器板 + 暖光 band 参数卡（丙案核心，F2/F3） `[v1.3]`

> 渲染原语无渐变/模糊 ⇒ 暖晕用**同心圆角 rect 叠层 + 由外向内递增 α** 模拟伪径向光；容器板复用面板同族 token（板感语言统一）。

```
输入：gridW = cols×52−2, gridH = rows×52−2（§3.3 派生）；gridCenter = PUZZLE_BAND 内居中点
派生：plateW = gridW + 16, plateH = gridH + 16        // 容器板外扩 8px/边
      plateRect 圆角 20，中心 = gridCenter
      availV = (640 − plateH) / 2                     // PUZZLE_BAND 高 640（§3.1 冻结）
      availH = (750 − plateW) / 2 − 6
      bandOut = max(0, min(18, availV, availH))       // clamp：不越带、不越屏
      e1 = bandOut, e2 = bandOut×2/3, e3 = bandOut×1/3

层序（背→前）：
B1 ring外  roundRect(plate 外扩 e1) 圆角 20+e1×0.6  fill glow_warm #FFF3E2 α 0.04
B2 ring中  roundRect(plate 外扩 e2) 圆角 20+e2×0.6  fill glow_warm          α 0.05
B3 ring内  roundRect(plate 外扩 e3) 圆角 20+e3×0.6  fill glow_warm          α 0.06
B4 板投影  roundRect(plate 偏移 (0,−3)) 圆角 20       fill #1E2033           α 0.10
B5 板体    roundRect(plate) 圆角 20                   fill panel_surface + stroke panel_border 1px
B6 完成贴纸 完成区外扩 6px 白描边 + α0.10 投影（v1.2 贴纸感，叠于板体上）
B7 珠/槽阵  §1.1 / §1.2 逐格
```

> - 叠层 α 由外向内 0.04→0.05→0.06 单调递增（贴板缘累计 ≈0.15）= "自带光" 但**极淡**，不抢珠子焦点（铁律 1）。
> - `bandOut=0` 时（12 行满图案，plateH=638 近满带）B1–B3 宽 0 不绘制，**不越 `PUZZLE_BAND`**；demo 典型图案（如 L1 心形 7×6）avail 充足 → bandOut=18 全量。
> - B4–B5 **全程态常驻**（含全空开局/中途态/完成态）→ 根治 F3「中途态拼图无承载板」；同时以板+晕填充 F2 的构图空旷（`BEAD_CELL` 冻结不可放大，只靠容器+背景层次充实）。

### 1.8 背景层次参数卡（F8，解锁 v1.2「纯色无层次」自限） `[v1.3]`

```
层序（背→前，均在容器板/珠/托盘/HUD 之下）：
G1 背景底   RenderModel.background = bg_base #ECEAF3
G2 冷沉层   roundRect(0, 0, 750, 1334, 0)         fill bg_depth #E3E0EE α 0.04
G3 中心提亮 roundRect(屏心, 645×830, r48)         fill bg_lift  #F4F2FA α 0.35
G4 提亮·核  roundRect(屏心, 470×620, r40)         fill bg_lift  #F4F2FA α 0.30
```

> 约束（守铁律 1）：G2–G4 **仅冷色**、饱和 ≤8%、与 `bg_base` 明度差 ≤4%（WCAG 对比 ≈1.03–1.06:1，刻意极低）；**禁止暖色入背景**（暖只属于拼图 band 与珠子）；无网格无纹理无贴图。

**统计**：可见元素约 **24 项（+容器板/暖光 band/背景层次 3 组叠层）+ 10 珠色 × 6 状态矩阵 + 7 张 VFX 参数卡（§1.6.1–§1.6.7）+ §1.9 质感语言卡（v1.5：三物体材质分工 / 10 色端点表 / 凹陷四层卡）**，**外部美术文件 0 个，全部程序化绘制**。

**单帧图元核算** `[v1.4]` · **`[v1.5-r2]` 重构为「基线构成表」**（任务 B）：

> ⚠️ **单一真源声明**：主理人复核发现「同一文件内 **1810** 与 **2173** 并存且无交叉引用」⇒ 本批把职责切开：**本表只算「基线怎么构成」**，**「净值 × 基线」的并列绝对值一律以 §1.6.9 末表为唯一真源**（本表不重列，防两处不同源）。**三套基线 + 两套材质口径 = 六组数，不得任取两组相加**（纪律五条见 §1.6.9 末）。

| # | 基线 / 项 | 构成算式 | 值 | **适用版本边界与性质** |
|---|---|---|---|---|
| **①** | **静态基线（v1.4）** | 背景 +3、容器板/band +7、网格最坏 156×10 = 1560、托盘最坏 24×10 = 240 | **≈ 1810** | **= v1.4 基线**：`empty` = **E1 混色底 + E4 幽灵符号（2 层）**；托盘**可持珠**（≤ v1.21 定时供料玩法）。主理人已逐条复算验真 ⇒ **数字保留不删**。⚠️ `[v1.5-r2]` **未计托盘面板自身 3 层**（投影/板体/描边）⇒ 补计 = **1813**，与「道具期·本轮落码 1813（= 1810 + 扫光 3）」**数值巧合、构成不同** |
| **②** | **静态基线 · v1.5**（空槽凹陷四层后） | ① + 网格空槽 2→4/个（最坏全空 156 槽 **+312**）+ 托盘空槽 2→4/个（24 槽 **+48**）+ 托盘微拱白瓷内阴影 **+3** | **≈ 2173**（= 1810 **+363**） | **= v1.5 基线**（**纯色凹陷四层 S1–S4** + 托盘微拱白瓷内阴影 +3）。⚠️ **`[v1.5-r2]` 更正：本值是「松上界」、不是可达单帧值**——它把「网格**全 filled** 的 1560」与「网格**全 empty** 的 +312」叠加，而两者**同帧互斥**（一格非 filled 即 empty）⇒ 只作**压力测试上界**，**不得当性能预算真值**。典型开局增量（满盘错位、空槽 = 错位珠数 k）≈ **+51～+75**，远低最坏值（核算见 §1.9.5） |
| **③** | **静态基线 · v1.5 材质 + v1.22 构型**（**`[v1.5-r2]` 新增**） | 网格全 filled 156×10 = **1560** + 托盘全 empty 24×4 = **96** + 背景/板 **10** + 托盘面板 **3** + 微拱白瓷内阴影 **3** | **= 1672** | **= 可达真值**。边界：v1.22「错位归位」（开局满盘、**托盘零读写 ⇒ 托盘恒为空槽**、托盘已扩展至 24 实槽）。**`h` 不变量已验证**：一珠网格→托盘 = 网格 `−10+4 = −6` / 托盘 `+10−4 = +6` ⇒ 相消（`h=0 → 1560+96`、`h=8 → 1512+144`、`h=16 → 1464+192`，**恒 1672**）。~~**未扩展托盘**（12 实槽×4 + 12 虚线槽×1）⇒ **1636**~~ ⇒ ⛔ **`[v1.5-r6]` v1.24 作废**（12/12 仅作代码漂移真值，不再叫「未扩展」）；**`[v1.5-r6]` 双规模现行值**：`N=48`（规格扩展态）⇒ **1768**；代码口径 `N=12` ⇒ **1624**（= 旧注的 1636 位置，但**不含虚线槽**——现码 `rows ∈ {1,2}` 恒无预告行）。**v1.4 材质同构型** ⇒ **1621**（托盘空槽按规格 2 层）/ **1597**（按代码真值 1 层） |
| **④** | **静态基线 · v1.5-r5 材质 + v1.22 构型 + L11 目标色垫**（**`[v1.5-r5]`，WXG-T-142**；载体 B′ 切换自 `[v1.5-r3]` 描边环，+1 层/格不变） | ③ **1672**（N=24）+ 网格 L11 +1/格（全 filled 156 格 **+156**） | **= 1828**（N=24）／**1924**（N=48）／**1780**（代码 N=12） | **= 可达真值**（**非松上界**）——v1.22 构型下开局即**满盘 filled**，每颗 filled 珠必画 L11 ⇒ +156 **同帧可达**（不同于②里与「全空」互斥的局面）。**托盘不带垫**（无目标色语义）⇒ 托盘侧 96 不变。**`h` 不变量在此破例**：一珠网格→托盘 = 网格 `−11+4 = −7` / 托盘 `+10−4 = +6` ⇒ 每颗手持珠净值 **−1**（`h=8 ⇒ 1820`、`h=16 ⇒ 1812`；③ 的「恒 1672」不再成立，是两基线唯一的真实差异）。⚠️ **`[v1.5-r6]` 回退优先级与本批禁令的冲突消解（不得两处各自说法）**：上行旧注「真机承压时**先砍 L11**」与 §1.6.4 / 阀①④ 的新禁令（**垫不得进入可砍集、砍序永远在尾**）**直接矛盾** ⇒ 现行口径 = **分两层说清**：① **动画期四级回退阀（§1.6.9）永不调垫**（理由：波浪期 800ms 内抽走底面 = 一道横扫亮带 ⇒ 擦 **D2**；且“全盘错位可读性周期消失再回来”）；② 作为**全局静态降配项**砍垫（−156）技术上仍是最直接的减层出口，**但本批不把它排进砍序**——理由：垫 = 用户 2026-09-17 拍板以 **+156 层换「找错位珠」谜面可读**的标的 ⇒ 砍它 = **撤销一项已拍板的代价**，**需再走用户拍板、不由 art 单方排定**（已入 §6 验收项与回传⑤）。**诚实登记另一面**：若真机瓶颈被证明在「filled 格 11 层的 CPU 重算」而非图元数，则砍垫的收益确实大于砍 S3/S4 ⇒ 届时以实测帧率为据开裁定请求，**本批只登记、不预支**。**代价/收益**：+156 层换回「找错位珠」谜面可读，用户 2026-09-17 拍板接受 |
| — | **动画期各场景的构成** | **G1 落座**：`[v1.5-r2]` v1.4 材质原地缩放十层照画 **+0**／v1.5 材质 **S1 露缘条件绘制 +1/颗**（并发上限 **+3**；详 §1.6.1a）。**G7 轻压**：**+0**（v1.5 下仍 +0，落点格无凹坑）。**G2 溶解**：本轮十层全开 **+0**／规格口径 LOD 后半窗 240→**96【降档珠】**（净 −144）；**终态**另计（v1.4 = 24×2 = **48** / v1.5 = 24×4 = **96【凹坑】**）。**G3 扫光**：**+3**。**G4 庆祝**：网格 1560→**936**（6 层，净 −624）。**G6 彩带**：**+44**（阀③ ⇒ +24）。**G5 案 B**：**+0**（框架全局变换、零逐图元重算） | — | **绝对值一律查 §1.6.9 末「净值 × 基线」并列表**（唯一真源；含三基线下的 1669/2032/1531、1813/2176/1675、1230/1593/1092、1210/1573/1072，以及 v1.22 **解环器期净 +5 / 绝对 1677**）。⚠️ **`empty→filled` 的 +6（4 层坑→10 层珠）属状态迁移、不属动画期增量，两者不得相加** |

> **结论（诚实口径，`[v1.5-r2]` 三度修订；前两版保留可追溯）**：`[v1.4]` 原结论「六条动画在最坏情况下反而降低总图元数」⇒ `[v1.4-r2]` 修订为「只在**规格口径**成立，本轮落码口径下道具期微增 +3、庆祝期仍 −580」⇒ `[v1.5-r2]` 再修订：**两版都建立在「基线 1810 + 批量 24」上，v1.22 后双双失效**。现行结论：**(a)** 庆祝期 **−580** 三基线下均成立；**(b)** 道具期在 v1.22 下**不再是降图元场景**，解环器期净 **+5**（仅基线 ③ 适用）；**(c)** 单珠落座期由 **+0** 变 **+1/颗**（v1.5 材质必然代价，并发上限 +3，量级可忽略）；**(d)** **真正的性能风险不在图元数，而在 CPU 几何重算路径**（**G4** 需对全场 ≤156 颗逐帧重算 scale 与 6 层坐标；**G5 走案 B 框架全局变换 ⇒ 零逐珠重算**，案 A 已废；G6 需 44×4 点旋转计算）；**(e)** **静态基线本身的抬升（v1.4→v1.5 的 +363 松上界 / 同构型 +51～+75 典型值）远大于任何动画期增量** ⇒ 真机承压时**优先动 §1.9.5 末静态回退阀**（空槽 S3/S4 两线先砍，+2→0/槽）。静态回退阀见 §1.1 注；**动画期四级回退阀见 §1.6.9**（阀②③ 本轮**不预埋**，只留规格）。**真机帧率未验**（Cocos 构建阻塞，ADR-0009 P2），以上均为纸面推演。 `[v1.3/v1.4/v1.4-r2/v1.5-r2]`

### 1.9 材质感质感语言卡（v1.5，WXG-T-131）

> **背景**：玩法重塑「错位归位」（WXG-T-130）+ 用户裁定「**暂时不需要支持色盲玩家，我需要的是珠子和托盘、棋盘都有质感**」⇒ E4 幽灵符号移除（**`[v1.5-r2]` 锚点更正**：原句「降级登记归 T-130、本卡只交叉引用」系**承载方错认**——T-130 第二批只改 `systems-index.md` 与 changelog，`accessibility.md` 零 T-130 笔迹 ⇒ 登记责任落回 T-131；**真实锚点** = `accessibility.md` **v1.5**（事实登记，文策渊代落盘于 `90a2059`）+ **v1.5-r2**（量化核算与恢复条款））、E1 混色底作废改**目标色纯色底**（用户此前嫌「底色花」）；质感 = **同色相明暗渐变 + 高光 + 投影 + 内阴影**，底色**不做异色混入**。
>
> **能力边界（先读后写，权威来源 C）**：`RenderModelBuilder` 命令集仅 `rect / circle / line / polygon`（`packages/framework/src/core/render/render-model.ts`），**无渐变原语、无模糊、无全局变换** ⇒ 「渐变」一律落为**同色相明暗端点平涂 + 分段/叠层 α 伪渐变**（承 §1.7/§1.8 判例）。本卡所有参数均在该边界内可直接落码。

#### 1.9.1 三物体材质分工

| 物体 | 材质语言 | 落码载体 | 本轮改动 |
|---|---|---|---|
| **棋盘格底座（空槽）** | **凹陷坑**：目标色纯色底（同色相暗一档）+ 暗缘框 + 上内阴影/下受光（明暗反转）+ 内缩 6% | §1.2 重写为四层卡 S1–S4 | **重写**（E1/E4 作废） |
| **托盘底座** | **微拱白瓷**：白板 + 底/右缘内阴影两段（白板无明端余量，材质走暗端） | §1.3 `tray_panel` 增 3 段内阴影 | 增量（+3 图元） |
| **托盘珠 / 棋盘珠** | **凸起糖果体**：十层卡（接触阴影/投影/倒角/rim/三层软高光/符号） | §1.1 现行，@50 / @44px | **零改动**（v1.3 十层卡已达材质感达标线；可选增项见 §1.9.6） |

> 材质主轴一句话：**凸 = 受光体（上亮下暗 + 有影子），凹 = 承光坑（上暗下亮 + 无影子）**——同一几何骨架、明暗方向互换，这是 E4 移除后空/珠区分的唯一主轴（§1.9.4）。

#### 1.9.2 10 色渐变端点对照表（同色相明暗轴）

**推导规则（先规则后查表；日后加色按此自算，不新造系数）**：

```
L+   明端 = mix(base, #FFF, 0.38)   // 复用 BEAD_RIM_MIX 判例系数
          钳制：luminance(base) > 0.85 ⇒ L+ = base（奶白，白端无余量）
L−   暗端 = mix(base, #000, 0.26)   // 复用 BEAD_BEVEL_DARK_MIX
Lpit 坑底 = mix(base, #000, 0.14)   // 新系数（凹坑背光一档），归 tuning.ts
```

- **色相可辨性论证**：向纯黑/白线性混色**只动明度轴、色相角不变** ⇒ 每色的 L+/L−/Lpit 与 L0 保持同一色相，10 色的色相间距**不塌**；渐变端仅出现在 ≤5px 线/带（S3/S4/L3b 级别），**色相识别由 L0 主体承担**。真正的退化风险是「低饱和暗色（炭黑/深棕）的明端偏灰」——明端不承担识别，无碍。
- **落码纪律**：端点 hex **不写死在 view**（control-manifest §3：hex 只住 `palette.ts`）——`palette.ts` **模块级预计算** `BEAD_GRADIENT_LPLUS / BEAD_GRADIENT_LMINUS / BEAD_GRADIENT_LPIT` 三个 readonly 数组（10 项 each；加载时一次 `mix()` 构建，或直接抄下表为字面量，后者零运行时计算，两法均可）。

| # | 色名 | L0 base | L+ 明端 | L− 暗端 | Lpit 坑底 |
|---|---|---|---|---|---|
| 1 | 奶白 | `#FDF6E9` | `#FDF6E9`（钳制） | `#BBB6AC` | `#DAD4C8` |
| 2 | 柠黄 | `#FFD23F` | `#FFE388` | `#BD9B2F` | `#DBB536` |
| 3 | 活力橙 | `#F59B23` | `#F9C177` | `#B5731A` | `#D3851E` |
| 4 | 草绿 | `#3FBF6B` | `#88D7A3` | `#2F8D4F` | `#36A45C` |
| 5 | 玫红 | `#E84C3D` | `#F19087` | `#AC382D` | `#C84134` |
| 6 | 丁香紫 | `#8E6FD9` | `#B9A6E7` | `#6952A1` | `#7A5FBB` |
| 7 | 湖蓝 | `#3D7BF5` | `#87ADF9` | `#2D5BB5` | `#346AD3` |
| 8 | 赭棕 | `#A5652C` | `#C7A07C` | `#7A4B21` | `#8E5726` |
| 9 | 深棕 | `#6B3E1E` | `#A38774` | `#4F2E16` | `#5C351A` |
| 10 | 炭黑 | `#33333D` | `#818187` | `#26262D` | `#2C2C34` |

#### 1.9.3 新增常量组（全部归 `src/config/tuning.ts`，**不进 systems-index §3**，承 v1.3 `PLATE_OUTSET`/`GLOW_BAND_ALPHAS` 判例）

```
SOCKET_INSET_RATIO      = 0.06   // 坑内缩比（50 格 → 坑 44px；48 托盘槽 → 45px）
SOCKET_PIT_DARK_MIX     = -0.14  // 坑底明度（= Lpit 系数）
SOCKET_RIM_DARK_MIX     = -0.30  // S1 暗缘框 + S3 上内阴影共用墨
SOCKET_INNER_LIGHT_MIX  = +0.20  // S4 下内缘受光亮线（= BEAD_BEVEL_LIGHT_MIX 同值，注明同源）
SOCKET_SHADOW_LINE_W    = 0.05   // S3 线宽比（max(2, S×ratio)，§1.1 minStroke 同兜底）
SOCKET_LIGHT_LINE_W     = 0.04   // S4 线宽比
TRAY_PLATE_SHADOW_A1    = 0.03   // 托盘面板底缘内阴影第一段 α（高 4px）
TRAY_PLATE_SHADOW_A2    = 0.05   // 第二段 α（高 2px，更近边缘更深）
TRAY_PLATE_SHADOW_A_R   = 0.02   // 右缘单段 α（3px；光源左上故弱一档）
```

> 墨色一律 `BEAD_SHADOW_HEX #1E2033`（托盘面板）与 §1.9.2 端点表（空槽），**`palette.ts` 零新增设计 hex**。

#### 1.9.4 「must not read as a bead」复核（E4 移除后）

五条形态通道，全部与珠十层卡**正交**：

| # | 通道 | 凹陷空槽 | 凸起珠 |
|---|---|---|---|
| 1 | 几何 | 内缩 6%（44px@50）+ 暗缘框 | 满格 50px + L0 投影超出 |
| 2 | 明暗方向 | **上暗下亮**（S3/S4，反转倒角） | **上亮下暗**（L3/L2） |
| 3 | 投影 | 无（L0a/L0b 缺席） | 接触阴影 + 柔和投影 |
| 4 | 高光 | 无（L4a–c / L3b 缺席） | 三层软高光 + rim |
| 5 | 符号 | 无（E4 移除） | L5 满符号 |

**复核结论：成立，且比原方案更强**。原方案区分主要靠「E1 粉彩混色 + α0.32 幽灵符号」两条弱通道；新方案换为五条纯形态/光影强通道，符号移除后区分度**不降反升**（原通道 1–4 与珠本就正交，符号只是第五条）。坑底只比目标色暗一档（−0.14）⇒ 「同色入格」目标色信息不丢。新玩法「同色错位珠压同色坑」场景：珠的投影（L0a/L0b）+ 满尺寸 + 高光三通道足以压过坑缘，**不产生空/珠混淆**。

> ⚠️ **`[v1.5-r2]` 本复核只回答「空 vs 珠」、不回答「filled 格的目标色可读」**：五通道成立的前提是**两物体并置**（空坑 vs 凸珠）。但 v1.22「错位归位」的谜面要求玩家从**已满盘的 `filled` 格**里找出错位珠，而珠 50px = 格 50px、同 `radius 11` ⇒ **格底色零露出**（`grep -rn misplaced games/beads/src/view/` 零命中 ⇒ 代码侧亦无错位标记）。**本节不能用来证明该谜面可读**，反而正是它使谜面**仅靠「相邻色块冲突」可读**。已作为 **P0 待审批项**登记于 `accessibility.md §5` 第 **4** 条（含三候选与图元代价：基线 ③ 1672 → 1828（(b) 目标色环）/ ≤1688（(c) 只标错位珠）），**属玩法裁定域、美术不自决**。

#### 1.9.5 图元与性能核算

| 项 | 旧 | 新 | 增量 |
|---|---|---|---|
| 网格空槽/个 | 2（rect + E4 幽灵符号）——✅ **与代码一致**（`view-model.ts:699` 对网格空槽传 `cell.colorIdx` ⇒ E4 有画） | 4（S1 暗缘框 + S2 坑底 + S3/S4 两线） | **+2** |
| 托盘空槽/个 | **规格 2 ／ 代码真值 1**——⚠️ **`[v1.5-r2]` 更正（本行旧值「2」与代码不符）**：`view-model.ts:765` 调 `drawEmptySocket` **不传 `colorIdx`** ⇒ E4 分支不执行 ⇒ 托盘空槽**实际只画 1 层 rect**。旧表把网格口径（2）无条件推到托盘 ⇒ 高估 24 层 | 4（S1–S4，中性 `base` = `palette.slot`） | **规格 +2 ／ 代码真值 +3** |
| 托盘面板 | 3（投影/板体/描边） | +3 段内阴影 | **+3** |
| **落座动画期 S1 露缘**（`[v1.5-r2]` 新增行，任务 C） | —（v1.4：empty 为平面底，无联动对象） | 1（S1 条件绘制，判据 `scale(t) < 1.00`） | **+1/颗**（并发上限 **+3**；详 §1.6.1a） |
| **最坏静态**（全空 156 网格槽 + 24 托盘槽 + 面板） | 1810 | **≈2173** | **+363** |
| **可达静态真值**（v1.5 材质 + v1.22 构型：网格全 filled + 托盘全 empty） | 1621（规格）／1597（代码真值） | **= 1672** | **+51（规格）／+75（代码真值）** |

- **⚠️ `[v1.5-r2]` 上表末两行必须分开读（任务 B 核心）**：**2173 是松上界**（把「网格全 filled 的 1560」与「网格全 empty 的 +312」叠加，两者**同帧互斥**）、**1672 是可达真值**（`h` 不变量已验证）。**两值不可互推、不可相加**；三条基线的构成算式与适用边界见 **§1.8 末表**，净值×基线的并列绝对值见 **§1.6.9 末表（唯一真源）**。
- ⚠️ **`[v1.5-r2]` 两处 `+51 / +75` 是「不同推导的数值巧合」，不得互证**：末行「可达静态真值增量 **+51（规格）/ +75（代码真值）**」= 同构型下 v1.4→v1.5 的材质差（+51 = 托盘 24×(+2) +48 与面板 +3；+75 = 托盘 24×(+3) +72 与面板 +3）；而下条自查项里的「旧注 +51～+75」= 以 1810/2173 为基线的**开局空槽数 k 扫描区间**（+51 = k=0，+75 = k=12）。**两组端点相同、含义无关** ⇒ 引用时必须写明取自哪一行。
- **⚠️ `[v1.5-r2]` 自查新发现（主理人未列出）：旧注「典型增量 ≈ +51～+75」的推导前提已随 v1.22 失效**。旧式 = 托盘 24×(+2) = +48 与面板 +3 合计 **+51**，再加网格空槽 `2k`（k = 错位珠数，取 k ≤ 12）得 **+75**。但 **v1.22 = 开局满盘**（`swaps` 两两交换，网格零空槽）⇒ **开局 k = 0**，网格侧增量为 **0** ⇒ 典型值**收敛到 +51**（仅托盘 + 面板）；游戏进行中网格空槽数 = **已被取回、尚未归位的格数**（玩法域量，**本批不裁定其上限**，每格 +2）。**旧区间不作废、但只适用于 v1.21 及以前的供料玩法**。
- **缓存/预烘焙建议（工程实现约束项，回传已列）**：本管线**无渐变对象可缓存**，真实性能风险是 `mix()` / `withAlpha()` 的**每帧字符串构造**（静态层随 RenderModel 每帧重建）。对策 = §1.9.2 末端点表**模块级预计算**（10 色 × 3 端点 + 托盘中性槽 1 组 = 31 串，一次性构建），`drawEmptySocket` / 托盘槽只读表；`BEAD_SHADOW_HEX` 等常量已存在 ⇒ 新增运行时字符串分配**为零**。热路径零分配纪律（`AGENTS.md` L 系列）不破。
- 真机帧率未验（Cocos 构建阻塞，ADR-0009 P2），口径同 §1.8 末注；若真机承压，回退阀 = 空槽 S3/S4 两线先砍（+2→0/槽，凹陷读感仍由 S1/S2 内缩与明暗差保底）。

#### 1.9.6 未决（待主理人/工程排期裁，本单不拍板）

1. **珠体 L1 两段伪渐变增项**（规格保留、本轮不落码）：L1 主体上 60% 段 fill `mix(base,#FFF,0.12)` + 下 40% 段 base —— 3D 感再 +1 档，代价 +1 图元/珠（最坏 +156）。当前十层卡已达标，**默认不做**。
2. **L5 珠面符号去留**：属 a11y 降级 A2b/A3 范围。**`[v1.5-r2]` 锚点更正 + 结论修订**：原句「（T-130 已登记）…随 T-130 工程单统一处理」的**承载方错认**已更正（T-130 第二批只改 `systems-index.md` 与 changelog）⇒ **真实锚点 = `accessibility.md` v1.5（事实登记）+ v1.5-r2（量化核算）**。**结论：L5 珠面符号必须保留、不再是「未决」**——`accessibility.md` **v1.5-r2 A6 · G4** 条已核算：E4 移除后 **L5 成为全场唯一的符号承载层**，且 **A6（动画不承载独占信息）的 G1 条论证已改写为「符号的**出现**即事件」**（filled 态 L5 出现承载 `empty→filled` 转变）⇒ 移除 L5 会**同时打穿 A3（filled 侧灰度 100% 可辨）与 A6 两条已结绿的承诺**。本项由「未决」**转结**（仅 E4 的开关去留仍待工程单，开关名 `SOCKET_GHOST_SYMBOL`，详 `accessibility.md` A2b ⑤ / §4 第 8 条）。
3. **`[v1.5-r2]` 新增未决：「错位珠高亮闪」无美术卡**（v1.22 新增玩法）——§1.6.9 末解环器期真值（净 +5）中的「+3」按**最小实现口径（≤3 颗 × 1 层）估算**，代码侧 `grep -rn misplaced games/beads/src/view/` **零命中** ⇒ **无规格、无代码**。立卡归**后续美术单**（与 G2 卡重立同批）；**若层数 >1，§1.6.9 末表解环器期行必须重算**。与之耦合的 P0（filled 格底色不可见 ⇒ 谜面不可读）已入 `accessibility.md §5` 第 **4** 条待用户裁定。

---

## 2. 未来真实资产替换提示词（AI 生成备用）

| 目标资产 | 提示词 |
|---|---|
| `bead_set` | `candy-like 3d perler bead set, rounded square beads, soft top gloss highlight, beveled edges, 10 candy colors, matte plastic, top-left light, flat vector rendering, sprite sheet, transparent background` |
| `tray` | `white rounded tray panel for bead slots, soft shadow, minimal casual game UI, flat vector, 9-slice friendly` |
| `item_icons` | `3 casual game item icons 64x64: star magic wand, broom, horseshoe magnet, glossy candy style, flat vector, transparent background` |
| `bg_playfield` | `very light lavender-gray plain background, no texture, soft and clean, casual mobile game, flat color` |

---

## 3. 图集策略

| 阶段 | 策略 |
|---|---|
| **demo（当前）** | **不建图集**，全 `Graphics`，主包美术位图 = 0 KB |
| 后期（v1.1+） | 1024×1024 × 2 张：`atlas_gameplay_01`（珠子 10 色 × 状态）/ `atlas_ui_01`（托盘/图标/HUD）；padding 2px；珠子**关闭 trim**（保证尺寸一致） |

---

## 4. 命名规范

- 全小写 + 下划线；模式 `{域}_{实体}_{变体}_{状态}.{ext}`。
- **域**：`bg` / `bead` / `tray` / `item` / `hud` / `vfx` / `ui` / `audio`
- 示例：`bead_orange_filled.png`、`bead_lime_locked.png`（`bead_` + 色名英文）、`tray_panel.png`、`item_area_clear.png`、`hud_timer_normal.png`。
- 色名英文对照：`white/lime/orange/green/red/purple/blue/brown/darkbrown/black`。
- 禁止版本号、`final`、空格、中文、大写。

---

## 5. 包体预算表（微信红线 vs 内部目标，**分清两条线**）

| 线 | 项 | 值 |
|---|---|---|
| **微信红线（不可越）** | 主包 | ≤ **4096 KB** |
| | 主包 + 分包合计 | ≤ **30720 KB** |
| **内部目标（自我约束）** | 主包合计 | ≤ **2000 KB**（systems-index §3.9） |
| | Cocos 引擎运行时（裁剪后·实测 `cocos-js`） | **1500 KB** |
| | 业务代码 + 构建资源（实测 `assets` 384 + `src` 68） | **452 KB** |
| | 美术位图（demo，全程序化） | **0 KB** |
| | 音频（demo，程序化合成，产物内零文件；采样走分包） | **0 KB**（原「预留 ≤400 KB」不可兑现，audio-spec §4.1/§7.3；WXG-T-091 重算） |
| | ⇒ 实测合计（web-mobile release、`du -sk`） | **1968 KB**（audio-spec §4.2，2026-09-14） |
| | 安全余量 = 内部目标 − 实测合计 | **32 KB**（原「≥ 2000 KB」系分项和超目标的自相矛盾值，已废弃） |
| **未来资产（放分包）** | 图集 ×2（1024² RGBA PNG） | ≤ 1600 KB |
| | 单游戏美术总预算 | ≤ 10 MB |

---

## 6. 交付与验收

- [ ] 全部元素程序化绘制，无外部图片引用。
- [ ] 珠子按 §1.1 六层参数卡绘制，全局受光方向一致（左上）。
- [ ] **10 色珠子在黑白模式下仅凭符号+明度 100% 可辨**。⚠️ **`[v1.5-r2]` 本项必须分侧考核（旧写法未分侧 ⇒ 已变部分假绿）**：**`filled` 侧 100% 成立**（L5 符号 10 色 ↔ 10 符号一一映射 + 明度，量化见 `accessibility.md` **v1.5-r2 A3** 行）；**`empty` 侧仅 6/10**（E4 移除后符号通道消失，10 色同色相凹陷坑在灰度下只剩 **6 个可分离明度档**，丁香紫/玫红/湖蓝/赭棕 **4 色无独立明度位**、**4 组相邻对 ΔL < 4%**）⇒ **A3 已降档**，本项对 `empty` 侧**暂停考核**（用户授权，代价与恢复路径详 `accessibility.md` A3 ⑥ / A2b ⑤）。
- [ ] **空槽目标色可辨（v1.5 改判，WXG-T-131）**：坑底（Lpit，同色相 −0.14 暗一档）肉眼可区分不同目标色（3 色关卡 ≥ 3 类，5 色关卡 ≥ 5 类）；~~E4 幽灵符号 deuteranopia 可辨~~（**作废**：E4 移除。**`[v1.5-r2]` 锚点更正**：原句「可访问性 A2b/A3 降级已由 WXG-T-130 登记」系**承载方错认** ⇒ **真实锚点** = `accessibility.md` **v1.5**（事实登记，提交 `90a2059`）+ **v1.5-r2**（量化核算：实际可达档位 = **Basic 的弱化形态**，**不是** Standard）；恢复路径 = E4 绘制保留为**关闭的开关/死路径**，开关名 **`SOCKET_GHOST_SYMBOL`**，落点 `drawEmptySocket` 的 `if (colorIdx !== undefined)` 外再套一层，重开成本 = **置 true**）。⚠️ **「落码即假绿」窗口**：E4 代码**当下仍在**（`bead-render.ts:200/232-240`）⇒ A2b 的代码态 ✅ **不是假绿**；但 **E4 移除一旦落码，A2b 的 Basic 档立即消失** ⇒ **关闭 E4 的提交必须同批把 `accessibility.md` A2b/A3 状态列改为降档**（纪律详 `accessibility.md §4` 第 **8** 条）。
- [ ] **空/珠凹凸区分（v1.5 新增）**：同色空坑与同色就位珠并置时零混淆——五形态通道（§1.9.4：内缩 + 明暗反转 + 零投影 + 零高光 + 无符号）逐条可断言（`bead-render.test` 命令检视：`drawEmptySocket` **不发射** L0/L2–L4/L5 命令、S3 在上内缘/S4 在下内缘且墨色一暗一亮）。
- [ ] **端点预计算（v1.5 新增）**：10 色 × 3 端点 + 托盘中性槽墨 = **模块级预计算**（`palette.ts`），`buildRenderModel` 热路径**零新增字符串分配**；端点 hex 与 §1.9.2 表逐项一致（或由 `mix()` 派生一致）。
- [ ] `locked`/`empty`/`hint`/`wrong`/`selected` 状态不依赖颜色可辨。
- [ ] 道具图标 3 实装 + 1 预留，靠形状区分；视频角标右上角。
- [ ] 触控热区 ≥ 88×88；文字最小 28px。
- [ ] 主包 ≤ 2000 KB（内部目标），红线 4096 KB 不越。
- [ ] 命名符合 §4。
- [ ] UI token 与 `art-bible.md` §3.1 v1.3 表逐字段一致（`palette.ts` 为 `view/` 唯一 hex 持有处）。 `[v1.3·F1]`
- [ ] 拼图容器板全程态可见 + 暖光 band 不越 `PUZZLE_BAND`；12 行满图案时 band 自动退化不溢出。 `[v1.3·F3/丙案]`
- [ ] 珠子十层卡落码后，10 色符号墨对「含软高光合成面」对比仍 ≥3:1（`bead-render.test` 钉住）。 `[v1.3·F4/B2]`
- [ ] 全屏除珠子与拼图暖晕外无第二处高饱和暖色块；adBadge/主按钮为深藏青 `#2A2E43`。 `[v1.3·F6]`
- [ ] 28px 标签/小字对比 ≥4.5:1（白底 `text_secondary` / 冷底 `text_primary`）；无 22px 文字；齿轮 8 齿、时钟胶囊白板落地。 `[v1.3·F7]`
- [ ] **G1 落座**：`bead:placed` 有 **view 侧消费者**（当前为零，P0）；单颗 120ms 内 scale 1.06→0.96→1.00 且 L0a/L0b 联动；峰值帧珠径 53px ⇒ 半宽 26.5 + 静止邻珠半宽 25 = **51.5 < pitch 52** ⇒ 间隙 **0.5px、零重叠**（单测钉住；`[r2]` 旧述「相切」已精确化）。 `[v1.4·G1]`
- [ ] **G2 溶解**：作用对象为**托盘珠**，网格 `filled` 珠**零改动**（`powerups` S6「绝不写网格」不破）；与 `AUDIO_CLIP_DISSOLVE` **同帧启动**（音画同步）；**`[v1.4-r2·裁定 5]` 本轮十层全开、不传 `lodLayers`**（α<0.6 降 4 层为**规格保留、本轮不预埋** = 阀②）；若后续启用阀②，验收改为：α≥0.6（**τ ≤ 80ms**）十层 / α<0.6（**τ > 80ms**）降 4 层且 **L5 符号仍在**（❗ 阈值用 **80ms**，旧 126ms 已废）。 `[v1.4·G2]`
- [ ] **G3 扫光**：3 层 α 单调递减（0.14/0.10/0.06）、累计 ≤0.27；广层覆盖 ≤玩法区 30%；y 上限 **1214**（= `HUD_BAND.yMin`）**用户裁定**限定玩法区 `y∈[0,1214]`，**不侵入顶部 HUD 带**（白扫过 HUD 会使倒计时瞬时发白触 B1/E1）；与 Lv3 burst 的水平带**形态可区分**（斜向 20°）。 `[v1.4·G3 / v1.4-r2·裁定 6]`
- [ ] **G4 庆祝**：每列窗口 = `800−20×(cols−1)`（13 列 ⇒ 560ms，clamp 下限见 §1.6.4）；峰值帧同列相邻行叠压 ≤1px、**符号区零遮挡**；**`[v1.4-r2·裁定 1]` 层序为定案**：结算面板**延迟 800ms 开**（庆祝先行放完再落遮罩，真源 `ux-spec §5`「过关庆祝」行），6→10 层的跳变**必须落在遮罩之后**；`lodLayers = 6` 是**本轮唯一预埋的 LOD 档**（裁定 5）。 `[v1.4·G4]`
- [ ] **G6 彩带**：44 枚分布**零 `Math.random()`**（索引 lattice，同输入同画面）；**5 色子集不含 `#F59B23`**（grep 断言）、且不含与 `STAR_GOLD` 同值的珠色2；前景 8 枚在 `y∈[447,787]` **跳过绘制**（按钮/缎带无遮挡）。 `[v1.4·G6]`
- [ ] **G7 轻压**：仅 `ignored` 且 reason ∈ {`occupied`,`locked`} 触发；**零事件、零扣次、零状态写**（`bead-grid §8-4` 事件计数器断言仍 =0）；与 `wrong` 形态零交集（无红、无位移）；重启门 250ms **独立于** `WRONG_FX_RESTART_GATE_MS`；**`[v1.4-r2·缺陷 3]` 峰值帧间隙 = 3px**（只被点一颗缩到 48 ⇒ `52−24−25 = 3`，静息 2px ⇒ **变大 1px、零重叠**；旧值 4px 已废）。 `[v1.4·G7]`
- [ ] **G7 音效原子批（`[v1.4-r2·裁定 3]` 硬约束）**：`sfx_denied` 为**新增第 20 个音频剪辑**（非零音效、非复用 `sfx_place` 降 volume）；A05-24 是**活测试**（`tests/audio-dispatch.test.ts` 断言 `SPEC_CLIPS.length === AUDIO_CLIP_TOTAL` 且 voice 表 ≡ clip 集）⇒ **五项必须同批提交**（`audio-events §1` 表行 / `systems-index §3.12` `AUDIO_CLIP_TOTAL` **19→20** 冻结变更单 / `tuning.ts` `AUDIO_CLIP_DENIED` / `audio-voices.ts` 音色配方 / 测试 `SPEC_CLIPS`），**拆开即测试红**；音频 `minInterval` = **0.25s**（= 视觉重启门 250ms 换算，**不新造数值**）；解除条件登记处 = `audio-events §5` Q-A05-5。**G6 仍无音效**（纯装饰、判例 BD-16）。
- [ ] **D1 全条退化**：`reduceMotion = true` 时六条动画按 art-bible §7.3 逐条退化（G1/G2 保留 α 通道、G3/G4/G6 整条关停、G7 退 1px `slot_border` 静态描边环），且退化态**零 α 往复**（`accessibility.md` §2 D1/D2 钉住）。 `[v1.4·G9]`
- [ ] **热路径零分配**：六条动画的逐帧函数为**纯函数**，`buildRenderModel` 内零 `new`/`map`/`filter`/展开造集合；G6 用**预分配 scratch**（44 枚 × 4 点 × 2 坐标 = 352 float）。 `[v1.4]`
- [ ] **G5 案 B 不属本美术单验收，但有三条须登记**（`[v1.4-r2·裁定 2]`，另立工程单、跨 `packages/framework`）：① **前置验证项**——`FIXED_WIDTH` 下整屏放大 1.015 **可能露黑边，未验证**（不得写成已解决）；② **回归项**——框架域改动须跑**全矩阵回归**（breakout + beads 两游戏视图）；③ **根因**——视图侧 `_commands` 私有（WXG-T-074）。**a11y 口径不变**（D1 整条关停）。

- [ ] **`[v1.5-r2]` G1 落座的 empty 侧联动（凹凸咬合，任务 C）**：落座 120ms 内 **S1 暗缘框按可见性剔除条件绘制**（判据 `drawSocketRim = scale(t) < 1.00`，**不是淡出**）；层序 = `S1（若 drawSocketRim）→ L0a → L0b → L1…L5`；**S2/S3/S4 全窗恒不画**（单测钉住：动画期珠最窄 `50×0.96 = 48px` 占格内 `[1,49]` ⊃ 坑 `[3,47]`）；穿越时刻 = **14.70ms**（`u = 1−√0.4`）、露缘窗占 **87.7%**、峰宽 **1.00px**（t=40）。⚠️ **D1 下 `scale ≡ 1.00` ⇒ 判据恒假 ⇒ S1 自动不画、零新增关停项**（`accessibility.md` D1 行钉住）。**`ux-spec §5`「珠子落座」行的 `α 0.12→0.18→0.12`、`宽比 0.88→0.92→0.88` 一字未改**（L0a 落点改判只换承载者、不改值，详 §1.6.1a (2)）。 `[v1.5-r2·任务 C]`
- [ ] **`[v1.5-r2]` 图元口径（任务 C 自算结果）**：G1 落座动画期 = **+1/颗**（v1.5 材质）而非 v1.4 的 +0；并发上限 = **+3**（解环器链式 `ceil(120/80) = 2` + 玩家手动 1）；**`empty→filled` 的 +6（4 层坑 → 10 层珠）属状态迁移、不得与 +1 相加**（落座期该格绝对层数 = **11**、**不是 17**）。G7 在 v1.5 下**仍 +0**（落点格恒为 `filled`/`locked`、无凹坑）。 `[v1.5-r2·任务 C]`
- [ ] **`[v1.5-r2]` 三基线口径纪律（任务 B）**：任何图元数引用**必须带基线标签**（**1810** = v1.4 / **2173** = v1.5 松上界 / **1672** = v1.5 材质 + v1.22 构型可达真值）；**三套基线 + 两套材质口径 = 六组数，不得任取两组相加**（五条禁令详 §1.6.9 末）；**2173 不得当性能预算真值**（构成含同帧互斥项）。 `[v1.5-r2·任务 B]`
- [ ] **`[v1.5-r2]` G2 终态不得画降档珠（任务 B 语义失效处置）**：溶解终态（τ ≥ 200、槽 `free`）由 §1.2 `empty` 卡接管 = **v1.5 材质下 4 层【凹陷坑 S1–S4】**，**不是** 4 层【降档珠 L1+L2+L3+L5】（两处 `24×4 = 96` 数值巧合、构成不同，详 §1.6.2 末三行分列）。单测断言：`free` 槽**不发射** L1/L2/L3/L5 命令、**发射** S1–S4 四条。 `[v1.5-r2·任务 B]`
- [ ] **`[v1.5-r2]` 版本序与锚点无断链（任务 A/D）**：本文件与 `art-bible.md` 的变更记录序已消解「r2 修订对象」误读（本文件头注 = 保留原位 + 行首显式注；`art-bible §9` = 物理移行）；全文对 `accessibility.md` 的引用**一律指 v1.5（事实登记）/ v1.5-r2（量化核算）**，零残留「已由 T-130 登记」类断链句（grep 断言：`grep -n "T-130 登记" games/beads/art/*.md` 命中项必须均为**更正说明**、不得为现行声称）。 `[v1.5-r2·任务 A/D]`

---

*本文件为资产生产与替换的权威规格。§3 网格/托盘/告急常量按 `systems-index.md` **v1.0（2026-09-11）** 冻结值回写；§3 现版为 **v1.22（2026-09-16）**（唯一真源 `systems-index-changelog.md`）——⚠️ **`[v1.5-r2]` 旧注 v1.18 已过期**：v1.19–v1.22 中 §3 网格/托盘/告急三组常量**仍零改动**，但 **v1.22（WXG-T-130 第二批）有两组与本表直接相关的冻结变更**：① **§3.6 道具反转**（`POWERUP_TYPES` → `['solver','solverPlus','solverRandom']`、`REGION_CLEAR_SLOTS 6`/`RANDOM_CLEAR_COUNT 5` ⛔作废、新增 `SOLVER_PLUS_COUNT 3`/`SOLVER_RANDOM_COUNT 1`、**托盘零读写**、作用对象 = 网格错位珠）⇒ **§1.6.2 G2 卡的「作用对象钉死」与批量上限 24 已失效**（钉事实于该卡 ⛔ 块；**卡重立归后续美术单**）；② **§3.8 `EMPTY_TINT_MIX`/`EMPTY_GHOST_ALPHA` 作废待删**（随 v1.5 E1/E4 移除；删除归工程单，本表只登记引用态）。§1.2 `empty` 行于 WXG-T-080 修订（增目标色底 E1–E4）、**WXG-T-131 重写为凹陷四层（S1–S4）**、`locked` 行追认实现偏差（2026-09-14）。**UX 规格（WXG-T-081）早已产出**，动效毫秒与循环频率一律以 `ux-spec §5` 为权威：`hint`（:73，经 WXG-T-091）与 `hud_timer_danger`（:106，经 WXG-T-098）两处 `[待 ux-spec 对齐]` 占位均已回写删除，不再有「待 UX 规格产出」之说；本表现存续待对齐项仅 :81 `tray_panel`（§3 未直接冻面板几何）与 :93 道具 4 预留（待 GDD），均非 UX 域。⚠️ **跨域待处置（非本表责任）**：`ux-spec §5` 现同时保留旧行「消除（道具）200」与新行「解环器归位 200+120/颗」⇒ **双源风险**，已入 `accessibility.md §4` 第 **7①** 条交主理人串行处置（本批**未改** `ux-spec.md`）。**v1.5（WXG-T-131，2026-09-16）**：§1.2 `empty` 重写为目标色纯色凹陷坑四层（E1/E4 作废）、§1.3 托盘微拱白瓷、新增 §1.9 质感语言卡（10 色端点表 + 推导规则 + 缓存建议）；零外部资产、零冻结常量变更、零代码。**v1.5-r2（WXG-T-131，2026-09-16，林绘澄）**：任务 B（§1.6.9 末 + §1.8 末表升级为**三基线并列核算**：1810 = v1.4 / 2173 = v1.5 松上界 / 1672 = v1.5 材质 + v1.22 构型可达真值；四个净值×三基线开列 + 五条不得混用禁令；G2「240→96」语义失效分行开列并标注构成；回退阀② 判据重述）、任务 C（新增 **§1.6.1a** G1 的 empty 侧「凹凸咬合」逐帧口径 + §1.2 `filled` 行动画期例外；图元 **+0 → +1/颗**、并发 **+3**、与状态迁移 **+6** 严格分离）、任务 D（头注采「保留原位 + 行首显式注」消解顺序误读；`art-bible §9` 采物理移行）、六处 a11y 锚点更正（§1.2 / §1.9 / §1.9.4 / §1.9.5 / §1.9.6 / §6 改指 `accessibility.md` v1.5 + v1.5-r2）。**零代码、`palette.ts` 零新增 hex、零新增冻结常量、零外部资产、零 `ux-spec §5` 变更请求**。*
