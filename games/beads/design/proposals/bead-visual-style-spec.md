# 拼豆渲染视觉规格 · 提案 v0.1（决议 v0.2）

> **状态 = 已确认・待落码**（用户 2026-09-23：Q1–Q7 全按推荐、Q8 = (c)）。决议表见 §9，17 套材质风格候选与评级见附录 A。
> 本文件仍是**提案**（正源归属见 §10 第 3 步：确认后并入 `art/assets-spec.md`）。
> 决议表 = §9；材质风格候选（17 套）与可程序化评级 = 附录 A。
> 任务号 **WXG-T-203 续作** · 参考图 = `~/.qoder-cn/vibe_images/perler-bead-style-sheet_1790140409.png`
> （设计参照，**不进包**：本仓走零外部位图资产 + 主包 ≤4MB 红线）。

---

## 0. 要解决什么

四条目标，各自给**可验收判据**（否则"有质感"无法收口）：

| 目标 | 判据（真机 / harness 可查） |
|---|---|
| **质感** | 单颗珠在 1× 缩放下能看出：穹顶偏心高光、孔的内壁明暗、侧壁厚度。不允许出现"纯色圆角方块贴纸"观感 |
| **空间感** | ① 珠与底板有可辨的高度关系（接触阴影 + 侧壁）；② `lift` 抬升时高度语言连续（见 §5），不允许"平移 6px 其他什么都不变" |
| **辨识度** | ① 一眼认定是拼豆 = **中心孔存在且在 fit 档最小珠径下仍可见**；② 相邻异色可分 = 生成侧 ΔE 约束（§8）+ 孔底透出目标色（§4/§7） |
| **与实物神似** | 对照参考图逐条核（§2 表）。已知主动放弃项 = 表面微颗粒（P5），须在验收时知情 |

---

## 1. 分层模型（自上而下）

```
B0  连续目标色底图   ← 每可填格一格，pitch 满铺、方角、恒画（与格内有无豆无关）
B1  空格凹陷层        ← 仅 empty：pit 内缩块 + 上暗缘 + 下受光（画在 B0 之上）
L0b 投影              ← 仅 filled
L0a 接触阴影          ← 仅 filled
L1  珠主体            ← 圆角方轮廓
L2' 侧壁（新）        ← 主体下缘一条暗带，高度随 lift 增长
L2  暗倒角 ×2
L3  亮倒角 ×2 / L3b rim 光
L1c 中心孔（新）      ← 孔底 + 内壁上暗弧 + 内壁下亮弧
L4' 偏心椭圆高光（并档）
```

**沿用不变量（死结论，改动即回归）**：

- ⛔ **B0/B1 不参与 `lift`、不参与 `scale`** —— 恒锁格心恒画（§1.6.1 P0 陷阱 #2 同源；旧 bug 是"抬珠把垫一起抬走 ⇒ 读数被自身抹除"）。本模型把底图独立成层，从结构上隔离该风险。
- ⛔ `scale` 只作用珠体族（L0a…L4'），**不作用 B0/B1**。
- 渲染序固定 B0 → B1 → 珠 → 孔 → 高光。

---

## 2. 参考图 → 本仓可实现性逐条核对

| 实物特征 | 现状 | 处置 | 编号 |
|---|---|---|---|
中央通孔（最强识别特征） | ❌ 无 | 新增 `L1c` | **P1** |
熔合面微穹顶，高光为左上偏心椭圆 | △ 三条水平亮带 L4a–c | 并为一条偏心椭圆 | **P2** |
侧壁厚度（币状） | △ 仅同色压暗倒角，读作斜切边 | 新增 `L2'` 暗侧壁，随 lift 变长 | **P3** |
不是正圆、颗间微不规则、缝宽不匀 | ❌ 完全规则 | 圆角提档 + 确定性微抖动 | **P4** |
哑光塑料微颗粒 / 灰尘 | ❌ 无 | **主动放弃**（见 P6） | **P5** |
缝隙里是底板色 + 更深 AO | △ 有接触阴影 | 由 B0 承担缝色 | **P6** |
空格 = 底板上挖的洞，与有豆处同一块板 | ❌ 空格涂亮 `base`、有豆露 `edge` ⇒ **两张图** | B0 统一 + B1 只做凹陷 | **P7** |

### P5 主动放弃项（须知情）

程序化颗粒只有两条路，都不划算：① 噪声位图 → 违反零外部资产 + 占包体；② 逐颗撒小图元 → 图元爆量。
**替代** = 靠倒角/高光塑形给"哑光塑料"的读感。**代价**：放大到珠径 >60px 的特写会显得比实物干净。

---

## 3. 参数表（每项：建议值 / 派生 / 状态 / 依据 / 回退）

| # | 参数 | 建议值 | 依据 | 状态 | 回退档 |
|---|---|---|---|---|---|
K1 | 珠轮廓圆角 | `size × 0.30`（38px 珠 → 11.4） | 现值 `0.22` 折算后 = **5**，近乎方角 ⇒ 用户判"变成正方形"的直因。实物熔合后是圆角方，**不是正圆** | 🔴 待确认 | `0.5` 正圆（更卡通，但参考图不支持） |
K2 | 孔直径 | `size × 0.36` | 实物 Midi ⌀5 / 孔 ⌀2.2 = 0.44；0.44 在小屏吃掉色面太多 ⇒ 0.36 起步 | 🔴 待确认 | 0.28（更含蓄）/ 0.44（更真实） |
K3 | 孔底颜色 | **= 该格目标色的 pit 档**（通孔物理透下去） | 一石三鸟：神似 + 深度 + 把"该放什么色"直接送到珠子身上（§7） | 🔴 待确认 | 一律暗灰 `mix(#000, 0.55)`（不透色，纯装饰） |
K4 | 孔内壁自阴影 | 上缘弧 α **0.30**、下缘亮弧宽 `max(1, size×0.05)` | 复用既有 mix 系数族 ⇒ **零新 hex** | 🟡 建议照做 | — |
K5 | 侧壁高 | `size × 0.10`，`lift` 时 × `(1 + lift/size)` | P3 | 🟡 建议照做 | 0.07 |
K6 | 偏心椭圆高光 | 中心偏左上、宽 `size×0.55`、高 `size×0.28`；L4a–c 三条并此一条（α 取原中档） | P2；**净 −2 图元/格** | 🟡 建议照做 | 保留三条不做椭圆 |
K7 | 微抖动 | `scale ∈ [0.97,1.03]`，**种子 = 格坐标纯哈希**（禁 `Math.random`；禁走 `rng` 的 draw 序列，否则换关/重开跳变） | P4 | 🔴 待确认 | 关闭（保持完全规则） |
K8 | 底图色档 | **`edge = mix(base, −0.30)`**（= 今天 filled 侧已用档）⇒ 零新 hex；空格只在其上画 `pit = mix(base, −0.44)` 内缩块 | P7 / §4 | 🔴 待确认 | 底图用更浅档（需新 hex ⇒ 违纪律④，须单独裁） |
K9 | `--minadj`（生成侧） | **20**（`ADJ_MAX_DRIFT_DE = 12`，已落码） | §8 八关实测：命中 L5(21)/L6(15)/L8(21) 三张真冲突盘，五张健康盘一格不动 | 🟢 已落码，可调 | 0 = 关 |

---

## 4. 「有豆 / 无豆 = 同一张图」的论证与代价

**现状**（实测数值）：`empty` 最外层 = 亮 `base` + 内缩 6% 的 `pit`(−0.44) + 圆角 r=11 + 格间 `BEAD_GAP=2`；
`filled` 可见底 = `edge`(−0.30) + 方角 pitch 52（上批所改）。⇒ **色档与几何双重不一致**。

**目标**：B0 对**每一格**都画 `edge` 方角 pitch 满铺；`empty` 与 `filled` 的差别只出现在 B0 **之上**画什么。

**"空 vs 珠"仍可辨的依据**（沿用 `accessibility.md` A3 ③ 的既有量化）：同色下坑底 S2 与珠体 L1 的 ΔL = `0.14 × L_base`（活力橙 23.7/255），且坑的暗缘在上、珠的暗倒角在下 —— **明暗方向相反** ⇒ 凹陷/凸起可分。

**代价与风险（不掩饰）**：

1. 现方案里"空格外圈是亮 `base`"这一条**顺带**承担了"哪些格可填"的读数。改成统一 `edge` 后，该读数只剩 B1 的凹陷线 ⇒ **可填轮廓的辨识度可能下降**。若真机判定不足，回退 = 给 `empty` 的 pit 再加一档深度（**不新增 hex**，只调 mix 系数）。
2. `filled` 侧不再单独画垫 ⇒ **−1 图元/格**；`empty` 侧 base 被 B0 承担、坑层保留 ⇒ ≈0。净 **−156**（156 格盘）。
3. 色盲相关：见 §7。

---

## 5. 抬起 / 放下的空间语言（解决"突兀"）

**现状**：`lift` = 选中 +6 / 波浪 `WAVE_LIFT_PX` +3，**纯瞬时平移**，阴影、侧壁、缩放全部不变 ⇒ 不透明物体凭空挪 6px。

**目标**（四者联动，缺一即仍突兀）：

| 通道 | 规则 | 既有可复用 |
|---|---|---|
| 位移 | `lift` 走 **ease-out**，时长取 `ux-spec §5` 既有档 | ✅ 不新增毫秒值 ⇒ §5 不加行 |
| 投影 | `lift↑` ⇒ L0b 偏移↑ + α↓ | 复用 G1 包络的 `shadowDy` / `shadowAlpha` 通道 |
| 接触阴影 | `lift↑` ⇒ L0a **收窄 + 变淡** | 复用 `contactWidth` / `contactAlpha` |
| 放大 | `lift↑` ⇒ scale `1.0 → 1.04`，仍受 `FILL_POP_SCALE_START=1.06` 钳 | 复用既有 clamp，不新增阈值 |
| 侧壁 | K5：高度 ∝ 抬升 | 新层 |

⛔ 硬约束照旧：B0/B1 不动；`D1(reduceMotion)` 下**位移可保留但曲线退化为线性、放大归零**（与既有 D1 口径一致，须单测钉住）。

---

## 6. 图元预算与 LOD（必须一起裁，不能事后补）

- 孔 = **+2~3 条/格**，侧壁 **+1**，椭圆高光并档 **−2**，垫并入底图 **−1** ⇒ 净 **约 +1~2/格**；156 格 ≈ **+156~312**。
- 按仓内纪律（`important_decision_experience`：图元/包体增量类规格值**必须落码后差分复算**）：本表**不给基线数字**，落码后跑 `check:size` 与 §1.8 基线表复算并回填。
- ⚠️ 量级上这会**重新逼出 ADR-0017 的降档通道需求**。且上批实现的 LOD 红线原文是「**L11 垫与 L5 符号任何档不砍**」，现 L5 已随符号删除作废 ⇒ 红线段须重写为：
  **B0 底图 + B1 空格凹陷 + L1c 中心孔 = 任何档不砍**（孔是识别特征，砍了立刻退回"贴纸"）；质感族（倒角/rim/高光/接触阴影）= 降档可砍集。

---

## 7. 可访问性连锁（改规格必须一起改的账）

- 上批已把 **A1 从「色相+符号+明度」三重降为二重**（符号层整层删除），并把 B2「符号对珠面 ≥3:1」判为 ⛔ 作废。`check:a11y` 锚点门已对齐（A1 锚改指 `padColorIdx`、B2 锚 `-`）。
- **K3 若采纳「孔底透目标色」**：等于给每颗珠自带一个"我该变成什么色"的对照物，且孔是**几何特征**，在 fit 档珠径 ~19px 时仍在 —— 而上批实测证明 **6px 垫缝在那个尺度上恰好消失**（那才是"分不清"的真凶）。这条对本仓当前唯一可行的非色相通道有实质价值。
- **但 K3 不是色盲等价替代品**：孔只回答"这一格要什么色"，**不回答"这两颗珠哪个是哪个色"**。后者在符号删除后只剩色相 + 明度，而 `accessibility.md` A3 ① 早已量化：**明度单独不足以分开 10 色**（玫红/湖蓝、湖蓝/赭棕相邻差 <4%）。⇒ 用户 2026-09-16「暂不支持色盲」的裁定继续成立，本规格不偷偷宣称已修复。

---

## 8. 色板与相邻格 ΔE（生成侧，与视觉规格耦合）

- 真机取证 + 一次纠错：上一轮我报的 `红↔粉 ΔE76 = 29.8` 来自**错误的色彩转换**（sRGB→XYZ 多做了一次逆伽马）。**正确值 = 41.9**；同批色盲模拟四列**全部作废**，不得引用。
- 八关实测（正确管线）：L1 最小相邻 ΔE 41.9（相邻 88 对红/粉）⇒ **L1 的问题不在色相，在"目标色只有 6px 缝能表达"**（本规格 §4 治它）。真实低分盘是 L5 14.1 / L6 13.9 / L8 18.0。
- 已落码：`beads-gen.mjs::separateAdjacent()` + `adjacentConflicts()`，`--minadj` 默认 **20**，跑在 `balance()` **之后**（链路最后一步），改不到就保持原色（色彩保真优先），`report.adjacency` 上报 `changed`/`conflicts`。
- 建议 K9 与 B0 底图**一起看真机效果**再定阈值：底图统一后缝变连续、可读性上升，20 可能偏松或偏紧，**不要现在猜**。

---

## 9. 确认结果（2026-09-23，用户：「其他走推荐」）

| # | 问题 | 决议 |
|---|---|---|
**Q1** | 珠轮廓圆角 | ✅ **圆角方 `size × 0.30`**（K1；正圆方案作废） |
**Q2** | 孔径 | ✅ **`size × 0.36`** 起步，真机嫌小再评估 0.44（K2） |
**Q3** | 孔底颜色 | ✅ **透出该格目标色**（K3；非色相通道收益全依此项） |
**Q4** | 放弃表面微颗粒 | ✅ 接受（P5；验收时知情“特写比实物干净”） |
**Q5** | 底图统一 `edge` | ✅ 接受；代价 1（可填轮廓辨识度）的回退档已备（调 pit mix 系数，不新增 hex） |
**Q6** | 颗间微抖动 | ✅ **开**（K7；真机判“脏”则关） |
**Q7** | LOD 红线重写 | ✅ 确认：**B0 底图 + B1 空格凹陷 + L1c 中心孔 = 任何档不砍**；需同步改 `ADR-0017` 红线段 |
**Q8** | 10 套图案的口径 | ✅ **(c) 参考图** —— 首批 10 套已交付；用户随后要求「去掉不可落地的 3 类 + 再增 10 种」⇒ 现为 **17 套候选**，见 **附录 A**。**不新增资产、不进包** |

本文件状态从「待确认」转为 **「已确认・待落码」**；落码与文档对齐的动作序列见 §10。

---

## 附录 A · 材质风格参考图（17 套候选 + 可程序化评级）

> 全部沿用**同一版式**（A 单豆三视 / B 3×3 成块 / C 缺三颗的凹坑 / D 八色横排），
> 只变材质与光照语言 —— 不可比的参考图没法拿来做决定。图均在设计侧仓外
> `~/.qoder-cn/vibe_images/`，**不进仓也不进包**（零外部位图资产 + 主包 ≤4MB）。
>
> **修订记录**：v0.2 = 首批 10 套（Q8=(c)）。v0.3（2026-09-23 用户「把不可落地/否掉的 3 类去掉，
> 再增加 10 种」）：① 05 原木 / 07 像素复古 / 08 毛毡 **移出候选**（理由见 A.2 留档）；
> ② 新增 11–20；③ 15 磨砂玻璃**自否**（与本规格 §4 不变量直接冲突，见下表）。

评级口径：“在程序化 canvas 十层卡 + 零位图约束下能不能做”——**只改形状/反照率/α 与少量
额外图元 = 可做；需要噪声或位图 = 不可做**。
⚠ 另记既有裁定：`assets-spec §1.9.7` 定“换肤只能是**材质主题**（光泽度/倒角对比/rim 强度），
**不能是配色主题**”⇒ 只改这三类系数的能走既有通道，改图元集的需另走 §6 预算复算。

### A.1 候选表（17 套）

| # | 风格 | 评级 | 要动什么 / 与哪条不变量有关 |
|---|---|---|---|
01 | 哑光注塑 | 🟢 **基准** | 就是 K1–K8 本身，零额外 |
02 | 亮面烤漆 | 🟢 **高**（换肤通道） | 只改高光形状/α 与倒角对比，**不新增层** |
03 | 半透糖果 | 🟡 中 | “中心暗 + 边缘亮”径向近似 = **+2 层/格**；光溢到板上还要额外图元 |
04 | 蓄光夜明 | 🟡 中 | 亮边与孔圈自发光可；**halo 需同心多层**且 canvas 无 bloom；需暗背景 ⇒ 主题级联动 |
06 | 珐琅/金属包边 | 🟢 **高** | 金属描边 = L3 亮倒角提档 + 复用 L1c 孔圈，全程序化 |
09 | 陶瓷/窑变釉 | 🟡 中 | 窑变可走 K7 同一套哈希；开片线 = **+1~2 层**；孔底素坯可直接做 |
10 | 霓虹/赛博 | 🟡 中 | 发光描边可；**反射地面与光斑不可** ⇒ 只能取上半 |
11 | 珠光贝母 | 🟢 **高** | L4′ 改双色 + 加一条虹彩带（**+1 图元/格**）；无噪声依赖 |
12 | 彩窗玻璃 | 🟢 **高** | 铅条 = 给 B0 加深色 stroke（**零新增图元**，改现有 rect 参数）；不需真半透 |
13 | 双色注塑对角拼 | 🟢 **高** | 一条对角 `polygon`（**+1 图元**）；原语已有 |
14 | 铬金属 | 🟡 中 | 硬地平线反射带 = +1 图元；但**每个颜色的 mix 需重算**，美术工作量大 |
**15** | 磨砂玻璃 | 🔴 **自否** | 半透会**模糊 B0 底图边缘** ⇒ 直接破坏 §4「有豆/无豆 = 一张图」与 K3「孔底透出目标色」两条本规格核心不变量 ⇒ **不采纳**，不归入候选 |
**16** | **明度阶梯单色** | 🟢 **高 + 可访问性最高** | 强制 8 级明度等距 ⇒ **直接反治** `accessibility.md` A3 ①「明度单独不足以分开 10 色」（玫红/湖蓝、湖蓝/赭棕相邻差 <4%）。零新增图元 |
**17** | 版画排线 | 🟡 中（**性能红线**） | 每珠 4–8 条线 = **+4~8 图元/格** ⇒ 与 ADR-0017 性能方向**正面冲突**；只在降档集里保留才可考虑 |
**18** | **线稿描边** | 🟢 **高 + 非色相通道** | 每珠 +1 描边；轮廓即信息 ⇒ 是**符号删除后代价最低的非色相通道候选**（见 A.3） |
**19** | 黑白胶片 | 🟢 **高（当验收工具用）** | 它不是商品风格，而是把 A3「灰度可辨」判据**可玩化实装**的自检主题 |
20 | 感温双色 | 🟡 中 | 需“状态→配色”通道（端点表可承载）；但“触发条”属玩法/UX 决策，不在美术单方面定 |

### A.2 移出候选的三类（不静默删，留理由）

| 原编号 | 风格 | 否决理由 |
|---|---|---|
05 | 原木 / 激光切割 | 木纹 = 噪声或位图，**两条都违零位图纪律**；只能退化成几条弧线，易显脏 |
08 | 毛毡 / 织物 | 纤维破边 = 噪声/大量小图元；软阴影可近似但 fuzzy 轮廓做不了 |
07 | 像素复古 | 不是材质而是**渲染范式变更**（3 阶硬边 + 无 AA），与本规格 §0「质感」目标相左 ⇒ 归独立议题，不入材质风格集 |

### A.3 本批新增的真正的东西：非色相通道候选

上批删除 L5 符号后，三重编码降为二重（色相 + 明度），而 A3 ① 已量化证明**明度单独不够**。
本批里 **16 / 18 / 19 三套正好是三条不同代价的非色相通道补回路线**，建议当真候选而非“风格”讨论：

| 路线 | 代价 | 覆盖什么 |
|---|---|---|
**18 线稿描边** | +1 图元/珠 | 形状；不区分“哪颗是哪个色”，但把“珠/洞/格”结构提硬 |
**16 明度阶梯** | 零图元，只重排色板 | 直接修 A3 ① 的明度不足；**代价 = 色彩自由度**（十色必须拉开亮度 ⇒ 照片转拼豆的还原度会降） |
**19 黑白主题** | 零图元 | 不修问题，而是**把问题变成可验收项**（一键自检：灰度下还认得出吗） |

⇒ 如果只想要“更好看”，选 11/12/13；如果真想补回符号删除的欠账，**16 + 18 组合才是答案**。

### A.4 建议的落地顺序

先做 **01（基准）** ⇒ 首批两个**材质主题**用 **02 / 06**（只改系数，不碰图元预算，守得住 §1.9.7）
⇒ 要非色相通道则上 **18（+1 图元）** 并评估 **16** 的色彩代价 ⇒ 03/09/10/11/12/13/14 均需先过
§6 图元预算差分复算 ⇒ **17 需先解 LOD 才可考虑；05 / 08 / 15 已否，不重提**。

> **落码记录（2026-09-23）**：合 §1.9.7 材质主题（只改系数、零图元、不动 base），将 top1「06 珐琅·金属包边」落成盘面默认：`BEAD_BEVEL_LIGHT_MIX` 0.20→0.28、`BEAD_RIM_MIX` 0.38→0.50、`BEAD_CARD.rimWidth` 2/64→3/64（越过 50px 珠 `minStroke=2` 钳才真变粗）⇒ §11.2 基线 1587 不变。可切换皮肤需给 `drawFilledBead` 增 theme 形参 + 系数抽 preset（另立单）。

### A.5 参考图与规格的两处故意不一致

1. 图里的珠子仍偏圆，而本规格 K1 定为**圆角方**（实物熔合后确实是圆角方；参考图未能完全摆脱“圆豆”刻板印象）⇒ **以 K1 与真机为准，不以图为准**。
2. 图 D 排的八色是自由配色；本仓真实色板 = 品牌注册表（`games/beads/art/<slug>.json`），**不得参考图新增 hex**。

---

**历史版本注**：本节原为「待确认清单 Q1–Q8 + 推荐值」；用户 2026-09-23 逐条采纳推荐并选 Q8=(c)，
故改写为决议表。原推荐值与理由全部保留在 §3 参数表（K1–K9）与各节正文，未丢失。

---

## 10. 落码与文档对齐的动作序列（已确认・待执行）

1. 本文件按确认结果定稿 → 参数进 `tuning.ts`（表现层，**不进 §3 冻结常量**）+ `bead-render.ts` 落 B0/B1/L1c/L2'/L4'/P4
2. 复算并回填 §1.8 图元基线 + `check:size` 差分（纪律：不预估）
3. 文档对齐：`assets-spec §1.1`（新增 L1c / B0 行、L4 并档）、`§1.2 filled/empty`、`ADR-0017` 红线段、`accessibility.md`（孔作为新载体 + 其边界）、`ux-spec §5`（抬起联动注记，**不新增毫秒值**）
   → 建议 spawn **林绘澄** 做这一步的正式化（她的文档有让号与留档纪律）
4. 单测：`bead-render.test` 层序与红线、`view-model` 底图恒在、`denied-press`/`scene-vfx` 的 footprint 判据随 pitch 更新
5. 材质风格候选（Q8 = (c)）**已交付 17 套**（附录 A），属于设计参照，无需落码。若后续要挑其中几套做成
   可切换的**材质主题**，需先解 `assets-spec §1.9.7` 的参数通道（那是一条需要工程重构的前置，
   且已裁定「不能是配色主题」）⇒ 本步不属于本规格的落码范围。

**Q1–Q8 已全部回示（2026-09-23）⇒ 本节生效，按 §10 顺序执行。17 套材质风格参考图（Q8=(c) 及其后续扩容）已交付。**

---

## 11. 实施记录（v0.3，2026-09-23）

### 11.1 已落码（§10 第 1 步）

| 项 | 实现 | 位置 |
|---|---|---|
K1 圆角 | `BEAD_CARD.radius` 0.22 → **0.30**；**删除**「珠圆角 = 垫圆角 − inset」特例（它就是把 38px 珠算成 r=5 近乎方角的元凶），网格珠与托盘珠统一 `round(size × radius)` | `bead-render.ts` |
K8 B0 | 新增导出 `drawTargetTile()`：pitch 满铺、方角、`edge` 档；view-model 对 **empty 与 filled 两个分支都调** ⇒ 同图元同色档 | `bead-render.ts` / `view-model.ts` |
B1 | `drawEmptySocket` 新增尾参 `tilePainted`；网格空格传 true ⇒ **不再刷亮 `base`**（托盘空槽不受影响） | `bead-render.ts` |
K2–K4 L1c | 两枚 `circle`：孔底（`targetColorIdx` 存在时 = 目标色 `pit`）+ 内壁自阴影偏左上⇒留右下亮弧 | `bead-render.ts` |
K5 L2′ | 主体与倒角之间插一条暗带；高度 `× (1 + lift/size)` ⇒ 抬起时侧壁变长（§5 四通道之一） | `bead-render.ts` |
K6 L4′ | L4a–c 三条并为一枚（取 `softHighlight[1]` 几何与 α），LOD 可砍 | `bead-render.ts` |
命名 | `FilledBeadOptions.padColorIdx` → **`targetColorIdx`**（它现在只服务孔底透色，旧名会说谎） | 含镜像与测试 |

### 11.2 图元基线：差分复算结果（§10 第 2 步）

同一夹具（13×12，**78 填 / 78 空 / 156 可填**），`git stash` 切回 HEAD 量旧值后对比：

```
旧  total 1509 = rect 877 · circle   3 · line 610 · text 9 · polygon 10
新  total 1587 = rect 799 · circle 159 · line 610 · text 9 · polygon 10
净 +78 = 恰好「+1 图元 / 已填格」（+5.2% / 帧）
  已填格 12 → 13（B0 +1、侧壁 +1、孔 +2、高光并档 −2、珠内垫 −1）
  空格   净  0（B0 +1、自带亮 base −1）
  circle 156 = 78 颗 × 2（孔底 + 内壁阴影）+ 托盘 3 ⇒ 孔确实只长在网格珠上
```

⇒ 落在 §6 预估的 **+1~2/格下沿**；`WAVE_LOD_LAYERS` 降档集由砍 4 条变为砍 **3 条**
（L0a / L3b / L4′），孔与底图作为红线保留（已写进判据）。

### 11.3 判据改写（不为了变绿而改）

原 13 条旧层模型判据已按新模型**重写为同强度或更强**，未简单删除：

| 旧判据 | 新判据 |
|---|---|
`bead-render` 卡层序 11 条 | 12 条逐 kind 枚举（含侧壁与两枚孔）；同时间接钉住“不复活符号/不复活珠内垫” |
`bead-render` L4a/b/c 三条几何 | L4′ 单枚几何+α；**新增** L1c 孔半径/偏移方向断言（阴影在 +y、−x） |
`bead-render` / `scene-vfx` 「L11 垫红线」 | 改为「**中心孔红线**」：降档后两枚 circle 逐字段不变 |
`fill-pop` 「垫 = 第 0 条、scale 不带动」 | 改为「B0 是独立函数 ⇒ 珠体输出**根本不含 pitch 宽图元**」，结构上不可被包络带动 |
`scene-vfx` 「垫恒锁格缘」 | 「珠体函数不输出底图」+ **新增**「孔必须跟 `lift` 一起抬」（否则孔会从珠上滑开） |
`view-model` A1/A3「每格都有符号」 | 换为更强的同位判据：**B0 覆盖全部可填格** 且 **放豆前后逐字段不变**（= “一张图”的可机检形式） |
`view-model` §4「符号数 = 156」 | 换为「底图砖数 = 可填格数」+ 保留每帧命令下限 |

证据：`pnpm run verify` **18/18**；beads 540 例全通过；`harness:smoke` 对 beads 通过（无运行时异常，1463 draw commands/帧）。

### 11.4 仍未做（下次开口时先读这里）

1. **K7 微抖动未做** ⇒ 颗间完全规则（待真机定 amplitude）
2. ~~**§5 抬起四通道只做了侧壁**~~ ⇒ **已做，见 §11.5**；仅剩 **ease-out 斜坡**未做（需新时长，见该节末）
3. **§10 第 3 步文档对齐未做**：`assets-spec §1.1 / §1.2 / §1.8` 与 `accessibility.md` 仍描述旧模型
   （十层含符号、珠四周露 2px 垫缝、基线 1828），**已与代码不符**；建议由 林绘澄 正式化
4. `ADR-0017` 红线段未改写（§6 要求的「底图 + 空格凹陷 + 中心孔不砍」还没写进 ADR）

### 11.5 §5 抬起三通道（v1.5-r9，2026-09-23）

单一高度参量 `liftT = clamp(lift) / liftRef`（`liftRef = 6` = 选中/锚组抬起量；波浪 `WAVE_LIFT_PX = 3` ⇒ liftT 0.5），四通道同向：

| 通道 | 规则 | 满抬起值（静息 → 抬起） |
|---|---|---|
珠体放大 | `size × (1 + 0.04·liftT)` | 38 → 39.52px（仍 ≪ 格宽 50，A5 零重叠前提不变） |
投影偏移 | `shadowDy × (1 + 1.5·liftT)` | 1.78 → 4.63px |
投影浓度 | `shadowAlpha × (1 − 0.4·liftT)` | 0.15 → 0.12（仅未覆写时） |
接触面宽度 | `contactW × (1 − 0.35·liftT)` | 33.4 → 21.7px |
侧壁长度 | `wallH × (1 + 0.9·liftT)` | 3.8 → 7.2px |

**两条被现成判据拦下、改代码而非改判据的坑（重要，别踩第三次）**：

1. **α 越界**：初版把 L0a 接触阴影的 **α 也随 lift 淡掉**，而 `§1.2 lift and shadow α` 判据明写「L0a 固定 α 不受影响」——那是 art 已定的语义。⇒ 改为**只收窄、不动 α**；"离地 ⇒ 接触面变小"由宽度传达已足够。
2. **覆写与 lift 打架**：`selected` 传 `SELECTED_SHADOW_ALPHA`（"抬起 + 阴影更深 = 重量/强调"），而 lift 的物理结论是"抬高 ⇒ 影子变淡"；两者同现时上一版得到 0.183 而非 0.25 ⇒ **会静默推翻 §1.2 的选中语义**。现规则 = **显式覆写优先，lift 只调制未被覆写的默认值**（G1 落座包络与选中态逐字不变；G4 波浪期吃 lift 衰减）。

另：位移类判据（`§1.2 bodyY`、`scene-vfx lift`）**从「比底边」改为「比珠心」** —— 珠体现在随 lift 放大，底边比心多下移 half-growth，拿底边量会得到 178.33 而非 179。**那不是位移错，是尺子错了**；珠心才是与缩放无关的正确基准。

图元数：本批**只改数值、不加图元** ⇒ §11.2 基线不变（仍 1587，+1/已填格）。

**仍缺一步：ease-out 斜坡。** 本批只建立「通道 ↔ 高度」的静态映射；选中态的 `lift` 仍是 0 直接跳到位（无时域缓动），所以选中瞬间仍会偏硬。要补需要：① game 侧给锚组一个抬起进度（类 `placeProgress`，目前无此状态）；② `ux-spec §5` 新增一行时长 —— 建议**复用 `FILL_POP_MS = 120` 不新增毫秒值**，属 ux 口径变更，需拍板后再动。

⚠ D1（`reduceMotion`）本批无需特批：没有自发动画与 α 往复（不触 D2）；斜坡落地时再按 §5 注记处理（曲线退化为线性、放大归零）。
