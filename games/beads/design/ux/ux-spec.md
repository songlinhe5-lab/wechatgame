# UX 规格（UX Spec）· beads

- 项目：`games/beads`（拼豆填色消除）· 版本 v1.2 · 任务号 WXG-T-015（失败续时 U5：WXG-T-057；教学引导/首供/泄压阀 U6–U8：WXG-T-081；§5 闪烁规格与列归属 / §6.2 首供等价口径回写：WXG-T-098，BD-29/BD-30）
- 方法论：`my-skills/wxgame-ux-spec/SKILL.md`（线框 / 状态×输入矩阵 / 动效毫秒表 / 首屏留存）
- 数值纪律：坐标与热区引用 `gdd/systems-index.md` §3（v1.11 冻结）；动效毫秒**接替** art-bible §7 的 `[待 ux-spec 对齐]` 占位——本表即权威，冲突时回写 art-bible（经主理人）。
- 模式说明：普通模式（8 关）+ 冲刺模式（`gdd/score-combo.md`，常量 C1–C8 已冻结——**2026-09-12 用户拍板（WXG-T-020）**，本篇冲刺 UI随之定稿；冻结常量见 systems-index §3.10）。失败续时仅普通模式（§3.11）。

---

## 1. 设计原则（沿用 skill 四条）

1. **首屏即玩**：启动 → 进关 ≤2 次交互，无登录无授权（core-loop 判例：无主菜单）。
2. **拇指可达**：托盘/道具/扩展全在屏幕下 1/3（`TRAY_BAND`+`POWERUP_BAND`），信息区（HUD）在顶部。
3. **反馈即语言 · 0 文字教学**（**GAP-03 已裁定，WXG-T-081，取 (a) 项**）：落座 / 拒绝 / 连击 / 告急 / **新手引导**一律走动效+音效通道，玩法界面**不出现任何教学文字气泡**（原 §6 的「气泡 1『点托盘选珠』/ 气泡 2『同色入格』」**作废**，与本条不可两读）。
   - **引导载体**：复用 art 层**既有**规格——① `empty` 格的「目标色底 E1 + 幽灵符号 E4」（`art/assets-spec.md` §1.2），本身就是「同色入格」的**无文字表达**（且是色盲冗余通道）；② `hint` 态（`art/art-bible.md` §3.4 + `assets-spec.md` §1.2：`accent_blue` 2px 外描边 + 600ms α 0.5↔1.0 呼吸）把注意力收敛到单一格；③ 托盘槽脉冲。**不新造气泡容器**（`assets-spec` 的 21 项元素清单里没有「白底黑字气泡」，(b) 项反而要新增美术规格与包体）。
   - **依赖登记（诚实口径，不得当已成立用）**：视觉通道依赖 **GAP-04**——上述 ①②③ 在 `src/`、`src/view/` **全部零实现**（`EMPTY_TINT_MIX` / `EMPTY_GHOST_ALPHA` / `hint` / 脉冲均 grep 零命中）；`art/accessibility.md` A2 把 `hint` 蓝描边呼吸标「✅ 落地」属**假绿**，须回写。音效通道依赖 **GAP-05**（音频实质为零，本轮不落地）。**裁定：视觉单通道必须能独立完成教学**（对齐 skill §5「静音玩家不失去信息」与 `accessibility` D1「减弱动效时 `hint` 保留静态描边」），GAP-05 落地后为**增强项**、不作为引导成立前提。**GAP-04 未落地前，§6 的引导路径不成立**（T-086 同波次落码）。
   - **边界澄清**：本条只约束「教学文字」。按钮标签（继续/重玩本关/下一关/重试本关）、面板标题、道具卡 28px 文字标签（`accessibility` A4 硬要求）**不属教学文字**，不受本条约束。
4. **随时可走**：任意玩法状态 1 次点齿轮暂停；进度自动存档（S8），下次续进。

## 2. 界面流程（Screen Flow）

```
点击小程序图标
   │  BOOT（加载 ≤1.5s；读档失败/越界 → 安全降级第 1 关，绝不中断）
   ├─ runs==0 ──────────► 玩法·普通 L1（纯动效引导，0 文字，见 §1 原则3 / §6）
   └─ runs>0 ───────────► 玩法·普通 unlockedLevel 最远关
                              │
              ┌───────────────┼────────────────┐
              ▼               ▼                ▼
        暂停面板          结算·过关          结算·失败
      （继续/重玩/      （星级+去冲刺）     （+60秒续时主钮 /
        设置开关）            │               重试次钮；冲刺无续时）
              │               ▼                │
              │        有下一关 → 玩法·n+1      ├─ 续时成功 → 玩法·本关（同局）
              │        全通关 → 通关画面        └─ 重试 → 玩法·本关（整关重置）
              ▼
          玩法·恢复
（冲刺模式为玩法态的模式标志，不新增独立界面；入口/结算见 §3.4/§3.5；冲刺归零无续时）
```

> 已移除的界面（有意为之，非遗漏）：主菜单、独立设置页、返回菜单。退出交微信胶囊/系统返回。设置入口仅齿轮一条通道（S9 §2.1）。

## 3. 关键界面线框（750×1334，坐标 y 向上，来源 §3.1 布局带）

### 3.1 玩法·普通模式

```
┌──────────────────────────────────┐ y=1334
│ ⚙88×88        ⏰04:55     ••• ⊙ │  HUD_BAND y∈[1214,1334]
│   ↑齿轮(避让)   ↑胶囊220×64  ↑CAPSULE_AVOID
│                                  │
│        ┌─────────────┐           │
│        │  图案矩阵    │           │  PUZZLE_BAND y∈[480,1120]
│        │  (带内居中)  │           │  珠位外扩 8px 命中区（§3.8）
│        └─────────────┘           │
│ ╭──────────────────────────╮    │
│ │ ●●●●●●●●●●●● 12 实线槽    │    │  TRAY_BAND y∈[230,420]
│ │ ┄┄┄┄┄┄┄┄┄┄┄┄ +[▶扩展]     │    │  虚线行=扩展（未解锁置灰角标）
│ ╰──────────────────────────╯    │
│  [🪄区域] [🧹清空] [🧲随机]       │  POWERUP_BAND y∈[48,200]
│   ▶角标     ▶角标    ▶角标        │  3 卡 176×150 热区≥88
└──────────────────────────────────┘ y=0
```
- HUD 信息分区：齿轮=左（暂停+设置唯一入口）、倒计时=中、**右侧永远留给胶囊**。
- 倒计时告急（≤`TIMER_URGENT_T`）：数字+图标切 danger + 1000ms α 脉冲。
- 满槽告警：托盘面板边缘 2px danger 描边呼吸（500ms 周期，≤3Hz 红线内）。

### 3.2 玩法·冲刺模式（HUD 差异，提案）

```
│ ⚙88×88   ×3 ▸连击4   01:32  ••• ⊙ │  ← 左：倍率角标(×≥2 才显示)
│        ┌─────────────┐            │     中：分数 6 位数字
│        │  小图案(梯级) │            │  梯级进度点 ●●●○○ 置于拼图上方
```
- 倍率角标出现条件：streak ≥ `COMBO_STREAK_TIERS[0]`（score-combo 附录 C3）；切档瞬间角标弹跳 1 次。
- 连击窗口剩余量：倍率角标下沿 4px 细进度条收缩（>窗口 50% 不显示——只警示后程）。

### 3.3 暂停面板（S9）

```
╔═══════════ 560×480 panel_dialog ═══════════╗  遮罩 rgba(42,46,67,0.5)
║            ── 暂停 ──                      ║
║       ┌────────────────────────┐          ║
║       │     继续 (240×88 主钮)   │          ║
║       └────────────────────────┘          ║
║   重玩本关    🎵音乐 [开]  🔊音效 [开]       ║  行高 ≥88 热区
║  减弱动效[关]  大字号[关]   ▶ 去冲刺         ║  行3·三格均分·次要样式
╚════════════════════════════════════════════╝
```

- **面板几何冻结**：本体 560×480 不变（`panel_dialog`）；行3 由三格均分（每格 ≈160≥TOUCH_MIN 88）承载「减弱动效 / 大字号 / 去冲刺」，冲刺模式下「去冲刺」退场、两个可访问性开关常驻（WXG-T-088）。
- **D1 减弱动效 / E2 大字号**：行为归 `pause-settings §2.2` 与 `accessibility §2 D1/E2`；字号开关仅放大正文/说明类（×1.25），面板按钮不因它重排（自适应延后 v1.1）。

### 3.4 结算·过关（普通）

```
╔══════════════════════════╗
║      🎉 闯关成功          ║  金色缎带横幅（参考分析 §3.2 语言）
║       ★ ★ ★              ║  星级弹跳入场（1→2→3 逐颗 150ms）
║  剩余 02:18 ｜ 道具 1/3    ║  次要信息 text_secondary
║  ┌────────┐ ┌─────────┐  ║
║  │ 下一关  │ │ ▶去冲刺* │  ║  *冲刺入口（U1 已冻结：三处入口）
║  └────────┘ └─────────┘  ║
╚══════════════════════════╝
```

### 3.5 结算·冲刺与失败

```
冲刺结算:                          失败（普通 · 未续时）:
╔══════════════════════╗         ╔══════════════════════════╗
║  单局 12,340  NEW BEST║         ║    ⏰ 时间到               ║
║  最高梯位  第 7 梯     ║         ║  （温和提示，非红闪）        ║
║  ▸×5 最高连击 14      ║         ║  ┌────────────────────┐  ║
║ ┌────────┐┌─────────┐ ║         ║  │ ▶ +60秒 继续本关    │  ║  主钮 480×88
║ │ 再来一局││ 返回关卡 │ ║         ║  │  看完视频后从当前进度续打│  ║  奖励写清；热区≥88
║ └────────┘└─────────┘ ║         ║  └────────────────────┘  ║
╚══════════════════════╝         ║        重试本关            ║  次钮 480×88 描边
                                  ║  （倒计时回满，图案清空）    ║  不看广告也可点
                                  ╚══════════════════════════╝
```

- 续时主钮文案引用 `REVIVE_BONUS_SEC`（§3.11），不得只写「看视频」。
- 已达 `REVIVE_MAX_PER_LEVEL`：不渲染主钮，退回单钮「重试本关」（可加一句「本关已续时」）。
- 冲刺归零：**不出现**续时主钮，只走左列冲刺结算。
- 框架未接广告 API：主钮可画，点击 = 轻提示「即将开放」，面板不关、钟不加；**零 `wx` 调用**。
- Won't：自动拉视频、藏起重试、倒计时强迫选择。

### 3.6 通关画面（FINISH，8 关全清）
全屏庆祝 + 星级总览（8 关星数和）+「去冲刺」+「重玩第 1 关」。

## 4. 状态流转 × 输入响应矩阵（QA 直接转用例；与 input-control §2.3 门禁互为镜像）

| 当前状态 | 输入 | 响应 | 下一状态 |
|---|---|---|---|
| BOOT | （无交互） | 读档分流；校验失败→降级 L1 | 玩法·普通 |
| 玩法·普通 | 点托盘珠 | 选中/换选（tray:selected） | 玩法·普通 |
| 玩法·普通 | 点匹配空格 | 落座（bead:placed） | 玩法·普通 |
| 玩法·普通 | 点不匹配空格 | 拒绝抖动（bead:rejected） | 玩法·普通 |
| 玩法·普通 | 点锁定/已填格 | 静默忽略（零反馈） | 玩法·普通 |
| 玩法·普通 | 点道具卡（次数>0） | 清槽生效（powerup:used） | 玩法·普通 |
| 玩法·普通 | 点道具卡（超限） | ad_badge 占位轻提示，零事件 | 玩法·普通 |
| 玩法·普通 | 点扩展 | 角标占位（MVP 无解锁路径，S6 §2.6） | 玩法·普通 |
| 玩法·普通 | 点齿轮 | 面板入 + 冻结计时/供料 | PAUSED |
| 玩法·普通 | 图案填满 | 庆祝 → 结算·过关 | LEVEL_CLEAR |
| 玩法·普通 | 倒计时归零 | 温和失败提示 → 结算·失败 | GAME_OVER |
| PAUSED | 继续 / 重玩本关 / 开关 | 恢复续算 / 五项重置+重开 / 即写 S8 | 玩法 / PAUSED |
| LEVEL_CLEAR | 下一关 / 去冲刺*(U1) | 玩法·n+1 / 玩法·冲刺 | 玩法 |
| GAME_OVER（普通，未达续时上限） | 点「+N 秒继续本关」 | 玩家主动 show 激励（未接 SDK→轻提示，零加时） | GAME_OVER（等回调） |
| GAME_OVER（视频中） | 看完 `onRewarded` | `remaining += REVIVE_BONUS_SEC`；同局续打，不重置 | 玩法·本关 |
| GAME_OVER（视频中） | 中途关 / 失败 / 未看完 | 零加时；可再点或重试 | GAME_OVER |
| GAME_OVER（已续时） | （无续时钮） | — | GAME_OVER |
| GAME_OVER | 重试本关 | 整关重置（含续时记账清零）→ 玩法·本关 | 玩法 |
| GAME_OVER | 点面板外 / 点齿轮 | 遮罩拦截 / 非 PLAYING 忽略 | GAME_OVER |
| 玩法·冲刺 | 倒计时归零 | 冲刺结算（无续时） | 冲刺结算 |
| FINISH | 去冲刺* / 重玩第 1 关 | 玩法·冲刺 / 玩法·L1 | 玩法 |
| 玩法·冲刺 | 同上玩法行（**归零除外**） | 同上 + 连击/梯级反馈（score-combo §2.5） | 玩法·冲刺 |
| 任意面板态 | 点面板外 | 被遮罩拦截，零响应 | 不变 |

> 无 MENU 行（已移除，防复活）。
>
> **超时兜底与「玩家不会卡死」的条件式承诺（WXG-T-081 改写；原文为「供料与倒计时保证玩法永续推进，玩家不会卡死」的绝对承诺，现已认定为假承诺）**
>
> 1. 本矩阵**不含**超时自动兜底行。其成立前提是：**托盘里的每颗珠都能被落子**。
> 2. 当前实现**不满足**该前提：S4 `tray-spawner.md` §2.4 只有「落子成功 / 道具清除 / 整关重置」三出口；而 ① 杂色与图案色 **disjoint**（`levels-spec.md` §2 硬约束 + `src/config/levels.ts` 校验强制）⇒ 杂色珠**永不能**落子成功；② `powerups.md` §2.6 自认道具第 2 次效果与扩展位均**无解锁路径**（`ad_badge` 占位、零事件零槽变化）⇒ 实战只剩「整关重置」。
> 3. 故 **GAP-06 泄压阀落地前**：满槽 + 死珠 = **软锁死**，唯一出路是等倒计时归零 → GAME_OVER → 整关重置。**这是判负，不是兜底**，不得当作「玩家不会卡死」的证据。失败页续时（§3.11）**也不能**解此局：`REVIVE_BONUS_SEC` 只加时间、不清托盘，harness 上 `MockRewardedAdProvider('complete')` 让续时必然成功——续时后仍是同一个死局（验证时勿误读为已修复）。
> 4. **泄压阀落地后**（推荐方案 A′：供料侧维持 `held ≤ demand` 不变量，见 §8 U8）：托盘每颗珠恒可落子 ⇒ 满槽仅为暂时性空间压力、玩家总能腾槽 ⇒ 本矩阵**无需**新增兜底行，「玩家不会卡死」才成为真命题。
> 5. 若用户改选 **B（丢弃出口）**，本矩阵**须增行**：「玩法·普通 | 丢弃手势（S2 路由 `[待定]`，现 §2.1「单指点击、无拖拽/长按」需破例） | 珠离槽、无惩罚（`tray:discarded`） | 玩法·普通」——**B 未拍板前不得预写该行**。

## 5. 交互反馈与动效时长表（权威毫秒值，回写 art-bible §7 占位）

> 本次（WXG-T-081）新增三行「教学引导」**同时接替** `art/assets-spec.md` §1.2 `hint` 行末尾的 `[待 ux-spec 对齐]` 占位（叠加优先级「外描边 > E2 描边 > E1 色底」仍归美术域，本表只接管**毫秒与循环频率**）——需美术侧回写删占位，经主理人中转。

统一缓动：进入 ease-out、退出 ease-in。红线：反馈 ≤400ms（庆祝/结算除外）；关键反馈视觉+音效双通道；**无 >3Hz 闪烁**；无真屏震（§3.8）。**闪烁口径（WXG-T-098，BD-29）**：闪烁 = 同一区域内不透明度的**往复**变化，单次「淡入→淡出」计 1 峰、**0 次往复**；频率 = 每秒往复次数（全屏白闪另按 §3.8 单独计）。该红线为**硬红线，不为一次描边脉冲开豁免**（主理人定向，对齐 WCAG 2.3.1 从严口径）⇒ 要改的是本表**视觉列的规格**，不是红线。

| 事件 | 视觉 | 音效 | 时长 (ms) |
|---|---|---|---|
| 珠子落座 | `vfx_fill_pop` scale 1.06→1.0 | 落座"嗒"（软） | 120 |
| 托盘选中 | 上移 4px + 投影加深 + 圆点 | 轻"啵" | 100 |
| 放错拒绝 | ±3px 抖动 ×2（属**位移**，不在闪烁通道内）+ danger 描边**单次脉冲**：α 0→1 淡入 60ms（ease-out）→ 峰值保持 80ms → 淡出 60ms（ease-in）＝200ms；**一次 fx 窗口内 α 极值点 ≤1（不往复）**。连续拒绝时**视觉脉冲重启门 500ms** ⇒ 有效闪烁 **≤2 次/秒**（= `systems-index §3.8`「错误反馈 抖动+描边闪 ≤2 次/秒」冻结值，对 ≤3Hz 红线留 33% 余量）；`reduceMotion` 开启 ⇒ 退为**静态红描边**（200ms 保持后直接消失，0 往复）+ 抖动位移归零（WXG-T-088 R1甲 / accessibility D1 保留清单；实测口径 QA P22） | 低"咚"；此列「≤2 次/秒」= **音效重触发频次上限**（§3 `AUDIO_REJECT_MIN_INTERVAL` 0.5 s 的换算），非声音振荡频率 | 200 |
| 消除（道具） | `vfx_clear_dissolve` scale→0.6 淡出 | 溶解"沙" | 200 |
| 道具生效 | 全屏轻扫光 | 魔法"叮" | 400 |
| 连击 ×2（Lv1） | 珠面星光粒子 3–5 枚 | 上行双音 | 200 |
| 连击 ×3（Lv2） | 全屏 scale 1.00→1.015→1.00 伪震屏 | 三连上行音 | 150 |
| 连击 ×5（Lv3） | 边缘径向光 + 珠面波浪 | 和弦爆发 | 350 |
| 连击断连 | 倍率角标灰缩消失 | 下行单音 | 150 |
| 倒计时告急 | danger + 1000ms α 脉冲循环 | 心跳节拍（同周期） | 1000/循环 |
| 满槽告警 | 托盘描边呼吸 500ms 循环 | 轻提示音 1 次 | 500/循环 |
| **教学引导·首珠脉冲**（WXG-T-081） | 首供落槽后该槽外描边 `accent_blue` 2px + α 0.5↔1.0 呼吸（1.67Hz，在 §3.8 ≤3Hz 红线内） | `[GAP-05 未落地前静默]` 落地后：软引导音（**增强项，非必需通道**） | 600/循环 |
| **教学引导·目标格 `hint`**（WXG-T-081） | 首珠颜色对应的**单一**目标 `empty` 格走 art 既有 `hint` 态（蓝描边 2px + α 呼吸）；减弱动效开关下退为**静态描边**（`accessibility` D1 保留清单） | 同上（非必需） | 600/循环 |
| **引导终止** | 首次 `bead:placed` 即清（**事件驱动，无计时器上限**）；`runs>0` 或本关已完成 1 次落座 → 永不再现 | — | 一次性 |
| stage 切换（冲刺） | 旧图波浪淡出→新图波浪淡入 | 换场"唰" | 250+250 |
| 过关庆祝 | `vfx_complete_wave` 逐列弹跳 20ms/列 | 胜利琶音 | 800 |
| 结算星入场 | 逐颗 scale 0→1.2→1 弹跳 | 每星"叮"上行 | 150×3 |
| 面板入 / 出 | scale 0.9→1.0 / 淡出 | 抽屉音 | 200 / 150 |
| 续时成功回 PLAYING | 面板出 + 胶囊数字跳到 N | 轻「叮」 | 150 出 + ≤400 反馈红线 |
| 续时未看完 | 面板不动；主钮轻抖 1 次 | 低「咚」（≤2 次/秒） | 200 |

> **列归属更正（WXG-T-098，BD-29）**：本表两处「≤2 次/秒」（放错拒绝 / 续时未看完）均指**事件重触发频次上限**（每秒最多触发 2 次该反馈），**不是**每秒闪烁次数。放错拒绝行的该限定**正本在 `systems-index §3.8` 的视觉条目**（错误反馈 = 抖动+描边闪 ≤2 次/秒），旧表将其只挂在音效列属**列错位**——自本次起视觉列同值写明，音效列仅保留 §3 音频换算那一层。
>
> **实现落差登记（后续项，不假装已一致）**：现实现仍按旧表——QA P4 实测 wrong 态 danger α **4 档起伏 / 200ms**（=2 次闪 ⇒ **10Hz**），既违本表新规格亦违 §3.8 冻结的 ≤2 次/秒。**本条回写后由工程侧跟进**（改码单由主理人另开；本单不改 `src/`）。跟进落地前 QA 对该行按**已知偏差**沿 BD-29 记录、不重复开新缺陷；`reduceMotion` 已实现退静态（P22），与本表新措辞一致，无需改动。

## 6. 首屏留存策略（微信前 10 秒）· WXG-T-081 可执行化重写

### 6.1 首 10 秒时间轴（0 文字；引导均为动效，毫秒值见 §5）

| t | 事件 | 依赖 |
|---|---|---|
| 0s | 点击图标 | — |
| ≤1.5s | BOOT 完成（程序化绘制零加载资产） | — |
| 1.5s（= PLAYING 第 1 帧） | **已进 L1、首颗珠已在托盘**（首供语义见 §6.2）+ 该槽 600ms 脉冲引导 | GAP-02（本单裁定）、GAP-04（脉冲 VFX） |
| 1.5s 起 | **单一目标格 `hint` 高亮**：高亮首珠颜色对应的匹配 `empty` 格中**行主序最前的 1 格**（单一指向 = 引导设计原则，非 §3 数值常量；避免多格同亮造成认知过载） | GAP-04（`hint` 态实现）、**与 T-080（林绘澄）在「格子高亮/目标格强调」上有交互，经主理人中转** |
| ~5s | 首颗珠落座 → 首个爽点（`vfx_fill_pop` 120ms + 音效）；**引导即时终止**（事件驱动，无计时器） | GAP-04；音效为 GAP-05（未落地前静默） |
| ~10s | 玩家已理解「选珠→填色」（观测口径，非硬定时器） | — |

- 无强制引导遮罩、无奖励弹窗打断；失败无惩罚（D7）；引导于 `runs>0` 或本关已完成 1 次落座后**永不重现**（存档 `runs` 字段归 S8）。
- **引导无超时兜底计时器**：未落座则引导持续呼吸（≤1.67Hz，不触 §3.8 闪烁红线），不升格为文字、不弹遮罩——**原「气泡 2 仅在 20s 内零落座时出现」的计时器分支作废**。

### 6.2 首供语义裁定（GAP-02，不可两读）

**裁定：采「`spawner.reset()` 后置 `_acc = interval`」，不采「`_setupLevel` 内同步 feed」。**

| 项 | 内容 |
|---|---|
| 语义 | 装配时先 `reset()`、再赋 `interval`、**最后**置 `_acc = interval`（赋值顺序**不可颠倒**：`interval` 的 setter 会清零累加器，见 `src/systems/spawner.ts` 注释）⇒ 进入 PLAYING 后**第 1 帧的供料段**完成首次供料 |
| 为何不在装配期同步 feed | ① 装配期供料会让 `tray:spawned` 落在 PLAYING **之外**，与 `core-loop` §2.1 状态语义及 S4 §8.8「非 PLAYING 零 `tray:spawned`」的观测口径相冲；② 需新增 `prime()` API 或暴露私有 `_feedOnce`，破坏封装；③ 走既有 `tick` 路径则抽色规则、随机空闲槽、事件广播全部自动一致，且帧内序仍属**供料段**（`core-loop` §2.2.2 真源零改动） |
| 一致适用面 | 冲刺 `_loadStage`（C8 stage 切换托盘清空）**同样立即首供**，避免每 stage 开局 2–3s 空窗；存档恢复路径（`_restoreFromSnapshot`）**不适用**（快照里托盘已有珠，再首供会凭空多一颗） |
| **首珠必须可落子色** | 首供抽色仅从 `demand = needed − held > 0` 的集合中取，**杂色与已满足色不参与首供**。理由：若首珠是死珠，则 1.5s「珠已在托盘」与 ~5s「首个爽点」**两条承诺同时失效**，且纯动效引导会指向一个无法完成的目标格 → 教学反成挫败。**本条独立于 GAP-06 泄压阀方案**（A/B/C/D 任选均成立）；若 A′ 落地，本条为其推论，仍保留作为首屏硬判据 |
| 判据影响（登记，交 QA） | S4 §8.1「60s 内 `tray:spawned` 恰 15 次」与 S1 §8.2「60 秒误差 ≤±1 颗」在首供落地后语义变为 **1 首供 + 15 周期 = 16 颗**；旧容差 ±1 会**恰好吞掉**本次语义变更（16 ∈ [14,16]）⇒ 属假绿风险，须显式改为 16。S4 §8.3（3:1 抽色统计）受「首供强制可落子色」影响恰 1 个样本（100 次中），可忽略但需注明 |
| **等价口径（WXG-T-098，BD-30）** | 本条裁定的是**可观测语义**：`reset()`（新关/stage 切换）后，PLAYING **第 1 个 tick 的供料段**即完成首次供料（≤1 帧）。实现落点两型均满足该语义：**(i)** `_acc = interval` 后置（顺序：先 `reset()` → 赋 `interval` → **最后**置 `_acc`，不可颠倒，因 setter 清零累加器）；**(ii)** 首供标志位 `_firstFeed`——`reset()` 置 true、首个 `tick()` 走 `_feedOnce` 后清零 ⇒ **现实现即 (ii)**（`src/systems/spawner.ts`；本单只读核实、未改码）。**后续轮次以 (ii) 为准据名**，不得因「代码中未见 `_acc = interval` 赋值」按字面误报。**两型唯一的可观测差异**（语义等价，但影响 §8-1 帧量化口径）：(i) 型周期供料相对名义时刻滞后 1 帧、(ii) 型滞后 2 帧（首供分支不占用 `_acc` 累计），故 `tray-spawner §8-1` 容差上界取 **2 帧** 以覆盖两型（BD-27） |
| 文档归位 | 本条规则的 S4 侧正文应补记在 `tray-spawner.md` §2.2（本单 Output Path 限定只改 §2.4，故 §2.2 补记经主理人转后续单落笔）；**权威条文暂以本节为准** |

### 6.3 启动路径表（skill §6 要求项，本次补齐）

| 玩家类型 | 落地页 | 引导 |
|---|---|---|
| 首次（`runs==0`） | 玩法·普通 L1（BOOT 直进，无主菜单） | §6.1 纯动效引导（首珠脉冲 + 单一 `hint` 格），首次落座即终止 |
| 有进度（`runs>0`） | 玩法·普通 `unlockedLevel` 最远关 | **零引导**（永不重现）；仅保留常态反馈（§5） |
| 存档非法/越界/版本不识 | 安全降级至 L1（不报错、不中断启动） | 同首次（降级后 `runs` 以存档实际值为准，不伪造） |

## 7. 可访问性（对齐 `art/accessibility.md` 与 §3.8）

| 项 | 落点 |
|---|---|
| 触控 | UI 控件 ≥88px（`TOUCH_MIN`）；棋盘/托盘珠命中区=名义外扩 8px、重叠按格心最近（§3.8 v1.2 消歧） |
| 色盲 | 三重编码（颜色+符号+明度，art-bible §3.3）；连击档位另有 数字"×2/×3/×5" 通道（不靠颜色） |
| 告急/告警 | 图标+颜色+脉冲三通道，不单靠颜色（§3.8） |
| 对比度 | 文本 `text_primary #2A2E43` on `#FFFFFF` ≥12:1；符号对比 ≥3:1（art-bible §3.2 注） |
| 动效 | 屏震默认不使用；伪震屏幅度 ≤1.5% scale；闪烁 ≤3Hz 全局红线 |
| 单手 | 全部核心交互在下 1/3；倒计时只在顶部信息区 |

## 8. 决策记录与裁决项

**已裁定**（沿袭）：无主菜单两路径（core-loop 判例）；齿轮单通道入口；道具 MVP 占位口径（S6 §2.6）；面板 200/150ms（art-bible §7 原占位值转正）。

**已冻结**（U1–U4 全部按推荐值，2026-09-12 用户拍板，WXG-T-020）：

| # | 项 | 推荐 |
|---|---|---|
| U1 | 冲刺入口位置：结算过关页+通关画面+暂停面板三处都放 vs 仅通关画面 | ✅ 三处（转化最高）；按钮样式=副按钮不抢"下一关" |
| U2 | 冲刺结算是否展示"好友排行"入口（开放数据域，meta 提案 §3） | ✅ 展示但 MVP 置灰"即将开放" |
| U3 | Lv2 伪震屏 vs 真屏震（真屏震需改 §3.8 冻结令） | ✅ 伪震屏（1.5% scale），§3.8 冻结令维持不改 |
| U4 | 首局保底：L1 前 3 次落错是否隐藏拒绝反馈（纯鼓励开局） | ✅ 不隐藏（拒绝反馈本身已零惩罚） |
| U5 | 失败面板续时 vs 重试权重（WXG-T-057） | ✅ 续时视觉主钮（奖励写清 +N 秒）；重试满热区次钮，不强制观看。冲刺无续时 |

**WXG-T-081 新增裁决（GAP-02/03/06）**：

| # | 项 | 裁定 / 待拍板 |
|---|---|---|
| **U6** | GAP-03 教学引导矛盾：(a) 保留「0 文字教学」+ §6 改纯动效引导 vs (b) 保留气泡 + §1 原则降为「v1.1 目标」 | ✅ **(a)**（策划已裁，无需再拍）。理由：① 支柱 1「一眼读懂的治愈」——气泡把「读题」塞进首 10 秒，与解压气质相违背；② 微信首屏注意力预算极小，气泡需「读→理解→执行」三步、动效只需「看到亮点→点它」一步；③ (a) 的美术素材**已定稿**（`hint` 态）且可访问性三重保障已备（A2 非颜色通道 + D1 减弱动效保留静态描边），(b) 反而要新造气泡容器（美术规格 + 包体增量）；④ GAP-08（文字镜像）虽仅预览缺陷，但说明文字通道在 harness 验证链上本就弱一档，动效引导可直接断言（`hint` 格集合 / 脉冲态）。**依赖：GAP-04 未落地前引导不成立（T-086 同波次）；GAP-05 为增强项、非成立前提** |
| **U7** | GAP-02 首供语义：装配期同步 feed vs `_acc = interval` 后置 | ✅ **`_acc = interval` 后置**（PLAYING 第 1 帧供料段）+ **首珠强制可落子色**（杂色/已满足色不参与首供）。细则见 §6.2。**裁定名（`_acc = interval`）与实现名（`_firstFeed` 标志位）不等名，后续按字面核码会误报 ⇒ 等价口径见 §6.2「等价口径」行（WXG-T-098，BD-30）** |
| **U8** | GAP-06 泄压阀（A 供料侧不抽死珠 / B 丢弃出口 / C 满槽自动清杂色 / D `DECOY_COLORS_MAX`=0） | ✅ **已拍板 A′ + D**（用户拍板 2026-09-14；WXG-T-086 落码：A′ 不变量 `held ≤ demand`（`spawner.ts`）+ D `DECOY_COLORS_MAX` 2→0（§3 **v1.17** 已冻结，关卡 JSON L1–L6 杂色已清空）。§4 尾注条件式随 A′ 落地收敛为无条件承诺；备选 B 丢弃出口**不做**，其矩阵行继续不预写 |

> **待确认项已归零**（U6–U8 全部裁定；U8 拍板值已由主对话串行落 `systems-index` §3 v1.17 + §6 变更记录，随 WXG-T-086）。后续新增裁定项仍按同口径：待确认不超个位数、§3 由主对话串行落笔避竞写（WXG-T-093 回写，2026-09-15）。
