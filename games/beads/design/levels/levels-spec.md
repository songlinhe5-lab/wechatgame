# 关卡数据规范（Levels Spec）· beads

- 项目：`games/beads`（拼豆错位归位）· 版本 v1.2 · 任务号 WXG-T-130（v1.2 = `misplaced` 错位字段 + 交换构造法 + §5 时长重推；v1.1 见 §10）
- 数据文件：`levels-01-08.json`（本目录；**本版不改 JSON 数值**——`swaps` 字段与逐关值由工程后继按 §2.1 写入）
- 数据形态：**ADR-0004**——`BEAD_CHARSET` 行字符串数组 + 元数据字段（v1.2 扩展 `swaps`）
- 消费方：S3（图案矩阵 + 错位装配）、S5（时限）、S1（BOOT 校验装配，architecture-beads §6）；~~S4（供料间隔/杂色）~~ **⛔ v1.2 作废**（供料关停，`systems-index §3.4`）
- 数值纪律：范围常量全部来自 `gdd/systems-index.md` §3（现版 **v1.22**），本篇不新增冻结数值。

---

## 1. 文件结构总览

```
LevelsFile（levels-01-08.json）
├─ version: 1            数据结构版本（v1.2 规格新增 swaps 字段 ⇒ 工程落盘时升 version: 2，旧文件无 swaps = 校验失败）
├─ gameId: "beads"
├─ description           元信息
└─ levels[]              8 个 LevelDef（ADR-0004 形态 + v1.2 扩展）
     ├─ id / name        序号（1..8 连续）/ 显示名
     ├─ cols / rows      网格尺寸（rows = pattern 行数，冗余自检字段）
     ├─ time             本关倒计时秒（合法区间 [180,420]，来源 §3.5）
     ├─ swaps            **v1.2 新增**：错位交换对数组 [[r1,c1,r2,c2], ...]，对数 ∈ [`MISPLACED_PAIRS_MIN`(1), `MISPLACED_PAIRS_MAX`(8)]，来源 §3.13
     ├─ spawnInterval    **⛔ v1.2 作废**（供料关停；字段保留向后兼容，校验器不再检查，工程 version:2 清理时删除）
     ├─ decoys           **⛔ 惰性字段维持**（≤ `DECOY_COLORS_MAX` = 0 ⇒ 一律为空数组；供料关停后连惰性消费方也不存在）
     └─ pattern[]        行字符串数组，字符集 BEAD_CHARSET = `.x1-9A`（= 每格**底色**/目标色）
```

**字符集语义**（来源 §3.2，2026-09-12 澄清）：`.`=空位（无图案位：不可填、不计完成、渲染为空白）、`x`=锁定格（不可填、不计完成，渲染为斜纹收边）——`.` 与 `x` 运行时语义同为 `locked`，差异仅在表现层；`1-9`+`A`=色板索引。**v1.2 语义升级**：pattern 字符 = 格**底色**（目标色）；初始珠 = pattern 全满后按 `swaps` 交换的结果（见 §2.1）。

**色板对照**（索引 → `art/art-bible.md` §3.2 十色，demo 只用 1–8）：
1=奶白　2=柠黄　3=活力橙　4=草绿　5=玫红　6=丁香紫　7=湖蓝　8=赭棕

## 2. 单关字段与硬约束（BOOT 校验，architecture-beads §6）

| 约束 | 值/规则 | 来源 |
|---|---|---|
| 尺寸 | `GRID_MIN_COLS`(6) ≤ cols ≤ `GRID_MAX_COLS`(**50**)；`GRID_MIN_ROWS`(5) ≤ 行数 ≤ `GRID_MAX_ROWS`(**50**) —— v1.34 由 13/12 放开至 29/29，**v1.37（2026-09-21）29→50**（用户拍板「最大可玩 ≤50」；上限≠档位，MVP 仍 14×14/18×18；异形盘 void 格不占宽高，导入前包围盒裁剪）；⚠️ >13 列的盘单屏放不下 ⇒ 依赖缩放（ADR-0015 丁-3） |
| 字符集 | pattern 每字符 ∈ `BEAD_CHARSET`；每行长度 === cols | §3.2 |
| 色数 | pattern 去重色数 ∈ [3, `BEAD_COLOR_MAX`(10)]（§3.2 v1.36） | §3.2 |
| **色板引用（v1.40 新增）** | 可选字段，**成对出现**：`palette`（品牌 slug，如 `'artkal-s'`，须在 `PALETTES` 注册表）+ `paletteCodes`（紧凑序色号数组，≤ `BEAD_COLOR_MAX`，逐项可解析）。游戏侧 `beadInksFor` 按 slug+色号从品牌生成物查 hex 渲染（hex 不随关卡携带）。缺省 ⇒ 回落顶层 `palette`（demo 十色）。违反任一条 → BOOT 拒收 | changelog v1.40 / §3.2 |
| 锁定格 | `x` 不计完成；关卡须 ≥1 可填格（全部关卡满足，x 仅装饰） | core-loop §6 / §3.2 |
| 时限 | time ∈ [180, 420] | §3.5 |
| ~~供料~~ | ~~spawnInterval ∈ [2.0, 6.0]~~ **⛔ v1.2 作废**（供料关停，§3.4；字段惰性保留） | §3.4 |
| 杂色 | `decoys` 必须为空数组（惰性维持，非空即 BOOT 拒收） | §3.2（v1.17 U8=D） |
| **错位（v1.2 新增）** | `swaps` 每对 `[r1,c1,r2,c2]`：① 四坐标均在可填格范围内（非 `.`/`x`）；② 两格**珠色不同**（同色交换无效，含交换后同色）；③ 对数 ∈ [`MISPLACED_PAIRS_MIN`(1), `MISPLACED_PAIRS_MAX`(8)]；④ 交换对**按数组序执行**（允许链式：同格出现在多对中即构造 3+ 珠轮转环）。违反任一条 → BOOT 拒收，错误含关卡 id + 对序号 + 行列 | §3.13 |
| **全错位初盘（v1.3 新增）** | 可选字段 `misplaced`（rowstring，字符集同 pattern）。**存在则替代 `swaps`** 作为初盘真源（引擎直读装配、零 RNG ⇒ 输出确定）。BOOT 校验：① 行数=rows、每行宽=cols；② 每字符 ∈ `BEAD_CHARSET`；③ **可填轮廓逐格匹配** pattern（pattern 可填 ⟺ misplaced 可填）；④ **每色珠数守恒**（可填格上各色计数与 pattern 相等 ⇒ 同色同数、仅位置错开 ⇒ 可解）；⑤ 错位格数 ≥ 1（非全就位）。**不强制 100% 全错**（允许部分就位，作难度调节）。缺 `misplaced` ⇒ 走 `swaps` 路径（旧语义零扰动） | §3.13 |
| 图案 | **全部原创**（房屋/爱心/动物/器物剪影），禁用任何第三方 IP 图案 | 参考分析 §8 |

校验器形态沿用 breakout `validateLevel()` 判例（错误信息含关卡 id + 行列 + 违规字符）。

### 2.1 交换构造法（v1.2 新增；可解性的构造保证）

**错位布置 = pattern 全满 → 按 `swaps` 顺序对可填格珠两两交换。**

- **可解性由构造保证（关键性质）**：交换是**可逆置换**——玩家对任意错位珠执行「取回（进托盘）→ 归位到其目标格」的周转，等价于逐步还原该置换；交换构造不存在「结构性不可还原」的布置（对比：任意涂改式错位可能造成色数不守恒型死局）。**可解性校验因此免于运行时搜索**，仅需 §2 表 ③ 的静态规则。
- **错位珠计数恒等式**（校验器可断言）：`misplaced 总数 === 2 × swaps.length`——每次合法交换（两格珠色不同）恰产生 2 颗错位珠；链式交换逐对累加，恒等式在任意 `swaps` 下成立。
- **底色不动**：pattern 永远是底色真源；珠的初始归属 = 交换结果。运行时 `misplaced(cell) = filled 且 colorIdx ≠ pattern(row,col)`（§3.13）。
- **authoring 建议**（关卡作者侧，非校验项）：优先构造「同排近距交换」（视觉冲突易读，符合支柱 1）；L1 教学关用 1 对同排相邻交换。

### 2.2 全错位初盘（v1.3 新增；成片错豆）

`swaps` 封顶 8 对 ⇒ 最多 16 颗错豆，**无法表达「成片错豆」**。MVP 大盘（14×14 / 18×18）改用 `misplaced` 初盘：关卡直接携带**整盘初始珠色**（rowstring），引擎 `assembleFromMisplaced` 逐格解码写入，不经任何随机 ⇒ **输出逐帧确定**。

- **成片错豆来源**：`misplaced` 由生成器按色循环移位产出（同色连续游程整体移到下一色 ⇒ 整块区域统一错色），故错豆呈大片连通而非散点。
- **可解性**：靠 §2「每色珠数守恒」保证——初盘与目标盘同色同数，玩家经取回/归位（大盘依赖直填 T-162）即可还原，无需运行时搜索。
- **与 swaps 互斥**：`misplaced` 存在即忽略 `swaps`（数据侧 `swaps` 置空数组占位）；`MISPLACED_PAIRS_MAX` 仅继续管辖 swaps 型关卡。
- **作者侧**：全错位大盘的通关时长/难度另经 playtest 调（`time` 不再套 §3.5 的 k 定价）。

## 3. 前 8 关参数总表（**v1.4 照片转拼豆资源重制**，2026-09-21 用户拍板）

> **重制方案**：8 关盘面 **10×10 → 29×29** 递增，关卡图案由 beads-studio 照片转拼豆管线从原图生成
> （像素级 k-means 限色 + 异形盘裁空边）。**豆子数量与原图宽高 px 直接相关**：原图每颗珠占的像素越多，
> 限色/测格越准 ⇒ 原图宽 ≈ 盘面格数 × **40px（推荐）/ × 25px（最低）**；**前端不缩图直传**
> （v1.5 2026-09-21 用户拍板：原始 px 是测格/豆数计算输入，解像上限 4096²）。难度旋钮不变：**①尺寸 ②色数 ③错位对数 k**（区间 [1,8] 冻结 §3.13）；
> time 列 = §3.5 v1.38 三维度公式 v0.1 实算（假设 A=0.5、swaps 模式 M=2k，clamp [120,420]）。

| 关 | 名称 | 尺寸 | 可填格≈ | 色数 | 用色索引 | time | spawnInterval | swaps 建议 | 源图推荐 px | 本关新变量 |
|---|---|---|---|---|---|---|---|---|---|---|
| L1 | 暖心 | 10×10 | 95 | 3 | 1,2,3 | 120 | ~~6.0~~ ⛔ | 1 | 400×400 | 基础取回/归位（教学关：同排相邻 1 对；大色块简单图） |
| L2 | 小屋 | 12×12 | 135 | 4 | 1,2,3,4 | 120 | ~~5.5~~ ⛔ | 2 | 480×480 | 4 色辨识 + 异色穿插 |
| L3 | 摆尾小鱼 | 14×14 | 165 | 4 | 1,2,3,4 | 145 | ~~5.0~~ ⛔ | 3 | 560×560 | 异形轮廓（**原图必须纯色/白底**，背景去格靠 flood fill） |
| L4 | 结果的树 | 17×17 | 260 | 5 | 1,2,3,4,5 | 216 | ~~4.5~~ ⛔ | 4 | 680×680 | 5 色 + 跨排交换 |
| L5 | 猫脸 | 20×20 | 370 | 6 | 1,2,3,4,5,6 | 300 | ~~4.0~~ ⛔ | 5 | 800×800 | 6 色 + 轮廓复杂化 |
| L6 | 顺风帆船 | 23×23 | 500 | 7 | 1,2,3,4,5,6,7 | 394 | ~~3.5~~ ⛔ | 6 | 920×920 | **锁定格 x** + 错位叠加 |
| L7 | 升空热气球 | 26×26 | 640 | 8 | 1,2,3,4,5,6,7,8 | 420（顶格） | ~~3.0~~ ⛔ | 7 | 1040×1040 | 8 色满配 + 链式环 |
| L8 | 星际火箭 | 29×29 | 780 | 10 | 1,2,3,4,5,6,7,8,9,A | 420（顶格） | ~~2.5~~ ⛔ | 8 | 1160×1160 | 综合考验（10 色上限，索引 A = v1.36 启用；29×29 触控待复评 ADR-0018） |

> **源图素材口径**：原图宽高比须匹配盘面（方盘配方图，否则变形）；每颗珠在图中 ≥25px（低于此测格不准、
> 压缩伪影占比过大）；推荐 ≥×40px/格。任意尺寸高清图直传（不缩图，4096² 上限内），
> 低关选大色块简单图、高关才用照片级细节图。生成参数基线：像素级限色、主导色风格、smooth=0、不铺满整盘。
> L6 锁定格布局：波浪行两端各 1 枚 `x`（视觉浪花收边），是 `x` 机制的教学位。
> **错位策略（v1.2 沿用 + v1.4 注）**：swaps 模式错位珠总数 = 2k（L1=2 … L8=16）均 ≪ 可填格数；
> **L7/L8 大盘若需真实难度**，改走全错位初盘（`misplaced` 字段，允许部分就位调难度）——swap 路径
> k≤8 冻结上限在 ≥20×20 盘面上只有 16 颗错位，难度天花板已触顶（time 两关均 420 顶格即此故）。

### 3.1 素材制作规范（照片转拼豆管线，2026-09-21 用户拍板；素材暂在仓外，登记表留空待回填）

四步流程，逐关可复现：

**① 原图选材**：宽高比匹配盘面（方盘配方图，否则变形）；宽 ≈ 盘面格数 × **40px（推荐）/ × 25px（最低，**
低于此每珠像素不足、测格不准、压缩伪影占比过大）；任意尺寸直传不缩图（4096² 上限内，
原始 px 是测格真源）。
选图内容随难度走：低关选大色块简单图，高关才用照片级细节图；**L3 异形轮廓原图必须纯色/白底**
（背景去格靠 flood fill）。
**组合图（Plate）口径（2026-09-22 用户拍板，详见 `proposals/level-content-pipeline.md` §0.3）**：
2048 源图 @32px/颗 = 64×64 母版 → 自动切 2×2 = 4 宫 32×32（一张完整组合图 4 关）；
1024 源图 @32px/颗 = 32×32 单图（最大单图档）。`GRID_MAX=50` 不变——50 是单图/组合图分界线，
>50 自动切块入 plate 路径。**宫错位密度上限 ≤60% 且必须按「整区域就位」构造**
（E1 两轮 bot 实测定案，§3.4 v1.42 丙档：32×32 reg60 + tray24 = 90–96 taps ≤ 116 达标；
全错位宫 176–178 taps 结构性不可入关，逐格随机散点部分错位是反模式——见
`proposals/tray-capacity-large-board-playability.md` §6.1/§6.2）。

**② 生图**：以原图为底 AI 生图时保持构图与主体轮廓不变，**压平渐变、扩纯色块、减并相近色**
（目标色数见 §3 表色数列），背景干净；生图输出尺寸不低于原图（避免二次插值损失）。

**③ beads-studio 生成**：上传生图 → 自动测格（应测得 §3 表盘面规格；偏差大则检查原图）→
参数基线：**像素级限色 · 主导色·卡通干净 · smooth=0 · 不铺满整盘（异形自动裁空边）**；
色数按 §3 表逐关设定；错位模式 L1–L6 swaps（k 见表）、L7/L8 全错位初盘（见 §3 错位策略注）；
验收看：盘面轮廓符合主题、色差可接受、可填格 ≈ 表列值、色数不多不少。

**④ 落位与回填**：生成记录（`apps/beads-studio/data/<盘面>/<记录ID>/`）→ 经 `importLatest`
（`level-import.ts`）写入 `levels-01-08.json`（含 time = §5 公式实算）；§4 明细表由脚本
按新 JSON 机械重算回填（沿用「以脚本实算为准」判例）。

**产物登记表（素材在仓外，逐关完成后回填此表）**：

| 关 | 盘面 | 原图来源/文件名 | 生图记录 | studio 记录 ID | 实际可填 | 实际色数 | 色差 | 回填状态 |
|---|---|---|---|---|---|---|---|---|
| L1 | 10×10 | `assets/l1-heart-250.jpg`（实拍拼豆爱心 900×921 → 250×250 jpeg q85） | 直传原图（AI 未重绘） | — | — | — | — | 原图就绪 |
| L2 | 12×12 | — | — | — | — | — | — | ⬜ 待制作 |
| L3 | 14×14 | — | — | — | — | — | — | ⬜ 待制作 |
| L4 | 17×17 | — | — | — | — | — | — | ⬜ 待制作 |
| L5 | 20×20 | — | — | — | — | — | — | ⬜ 待制作 |
| L6 | 23×23 | — | — | — | — | — | — | ⬜ 待制作 |
| L7 | 26×26 | — | — | — | — | — | — | ⬜ 待制作 |
| L8 | 29×29 | — | — | — | — | — | — | ⬜ 待制作 |

## 4. 砖块构成明细（逐关权威核对表，防手工数错）—— ⛔ **v1.4 整体作废**

> 下表对应**旧 6×5–13×12 关卡数据**；v1.4 重制后盘面全变（10×10→29×29），逐行构成待新 8 关
> 生成落 JSON 后由脚本机械重算回填（沿用「以脚本实算为准」判例）。原文保留如下仅作历史参照：

| 关 | 可填格 | 其中 `x` | 逐行可填数（r0→rN） | 算式 |
|---|---|---|---|---|
| L1 | **22** | 0 | 4,6,6,4,2 | 22×1 |
| L2 | **30** | 0 | 2,4,6,6,6,6 | 30×1 |
| L3 | **31** | 0 | 5,6,8,6,4,2 | 31×1 |
| L4 | **38** | 0 | 2,6,8,10,8,2,2 | 38×1 |
| L5 | **56** | 0 | 4,6,9,9,9,7,7,5 | 56×1 |
| L6 | **59** | 2 | 2,2,4,6,8,10,9,12,6 | 59×1 + 2×`x`（不计） |
| L7 | **79** | 0 | 7,10,11,11,11,9,3,4,5,8 | 79×1 |
| L8 | **65** | 0 | 1,3,5,6,7,7,7,9,7,5,3,5 | 65×1 |

> 本表数值由脚本对 `levels-01-08.json` 机械实算得出（2026-09-12），与手工目测冲突时**以脚本实算为准**（沿用 breakout 构成明细表判例）。

## 5. 难度与时长预估（**v1.4 实测口径**，2026-09-21；公式 v0.2 = 真引擎实测点击数 × `SEC_PER_TAP`）

> **重推前提（WXG-T-130 案 A）**：核心循环 = 「错位归位」，玩家只处理错位珠；定时供料不存在。

### 5.0 难度估算公式（正本；实现 = `level-import.ts measuredLevelTime`，**全仓唯一**）

```text
T = clamp( taps × SEC_PER_TAP , 120 , 420 )     （秒）
D = round( 100 × T / 420 )                       难度分 0–100（占满档时长比例）

taps         = 真引擎实测的**成功点击数**（`tools/scripts/beads-bot.ts`）
SEC_PER_TAP  = 3.6s（`tuning.ts`；来源见下「取值锚」）
```

`taps` 的取法（**不复刻任何判定逻辑**，同 g3 取证铁律）：装盘走生产 BOOT
（`pattern` + `misplaced` → `applyMisplacedToGrid`），每一步只经 `BeadsGame` 四个公开命令
（`tapGridCell` / `selectBoardBead` / `selectTraySlot` / `retrieveSelectedGroup`），
包一层计数（**只计返回 true 的成功命令**）⇒ bot 从不失败点击，所以 `taps` 是人类点击数的
**下界**。单盘耗时 0.1–0.4s，比拟合任何静态代理公式都便宜且准。

- **取值锚（`SEC_PER_TAP = 3.6`，用户 2026-09-21 拍板冻结）**：= L1–L8 已入库关的
  `time / 实测 taps` **中位数**。这 8 关的 time 是 playtest 校准过的真值（非公式产物），
  实测反推区间 3.01–5.71、中位 3.60 ⇒ 本值语义 = 「与已校准的 8 关同节奏」。
- **为何不按盘面结构量算**：v0.1 三维度公式（f_N/f_C/g_A）已作废，实测证伪见 §5.0.1 v0.2 行。
  真实批量由「托盘占用动态 + 同色空格连通域几何」支配，而全错位初盘**空格数为 0**、
  连通域随玩法演化 ⇒ 静态盘面根本看不见这两个量。
- **入关接线**：`time` 的唯一正源 = 实测。生成期 `beads-gen.mjs` spawn bot（`--patch` 回填
  `report.difficulty` + `levelDraft.time`）；入关期 Studio `ingestLevel` 再测一次并取实测值，
  **不采信草案自报 `time`**（同 `cycleProfile` 纪律）。
  `draftToLevel`（游戏内一键导入，客户端无引擎）走静态兜底 `estimateLevelTime` = `ceil(M/2)` taps。
- **Studio report 字段**：`difficulty {score, timeEst, misplaced, taps, bMed, split, cleared, secPerTap, blockers, formula}`
  —— 前四项是前端旧契约（名字不变、值源换成实测），其余为 v0.2 新增取证字段。
- **压力性质（v1.2 结论沿用）**：倒计时对熟练玩家几乎不构成失败风险，难度主承载是解谜本身；
  但 `time` 同时是**倒计时预算**，高估会白给 3★（`STAR3_RATIO=0.32`）、并让失败页与续时激励位永不开张。

#### 5.0.0 硬拦闸（§3.13 v1.41 · 用户 2026-09-21 拍板「生成期拒产 + 入关期拒收」）

**只用真值，不用静态代理量**：

| 判据 | 生成期（`beads-gen`） | 入关期（`ingestLevel`） |
|---|---|---|
| `taps × SEC_PER_TAP > LEVEL_TIME_MAX` | 打印完整报告后 `exit 4`（拒产） | 422 拒收 |
| bot 未通关（可解性未证实） | 同上 | 422 拒收 |
| bot 不可用（容器无引擎） | 写 `difficulty.error`，不给假数 | 500 拒收（**不回落静态估算**） |

> **为何不设 M / B_med 静态闸**（曾拟定 `M ≤ 280 ∧ B_med ≥ 8`，已入库 10 关实测**证伪并删除**）：
> 甜心 B_med=6 而 44 taps 轻松通关、猫咪脸 B_med 同为 6 而 134 taps 超预算 ⇒ 静态量**无分辨力**，
> 用作闸纯误伤 3 关；而 bot 单盘 0.3s，也不需要静态量做早期剪枝。

### 5.0.1 校准协议（「不断优化」的机制）

公式版本化演进，**不静默改数**：

1. **回流数据**：playtest 记录每关实际清关时长 `t_act` 与实际点击数（现无遥测，先手工记录；接入遥测后自动化）。
2. **触发阈值**：单关 `|t_act − T| / T > 25%` 或全关均值偏差 > 15% ⇒ 回调 `SEC_PER_TAP`。
3. **回调面**：v0.2 只有**一个自由参数** `SEC_PER_TAP`（v0.1 的四个系数已随三因子作废）。
   动它需用户拍板，新值与依据数据登记下表。
4. **记账**：每次调整在下方版本表登记新值、依据数据、生效范围；旧公式不删除。

| 公式版本 | 日期 | 系数 | 依据 |
|---|---|---|---|
| v0.1 | 2026-09-21 | f_N=1+0.25·log10(N/200)，f_C=0.75+0.083·C，g_A=1.45−0.9·A，基准 22.5s/珠 | 首版：量纲推导 + v1.23 k×45 兼容锚；无 playtest 数据，纯先验。**⛔ 已作废，见下行** |
| **v0.2** | 2026-09-21 | `T = clamp(taps × 3.6, 120, 420)`；`SEC_PER_TAP=3.6` = L1–L8 `time/taps` 中位 | **v0.1 被 32 盘真实语料证伪**（`design/forensics/diff-v02/grid.log.txt`）：① 32/32 盘触顶 420s ⇒ D100 饱和、难度分失去分辨力；② 隐含 s/tap 跨 0.46–70s（150 倍）⇒ 同一公式既造出倒计时内不可能通关的盘（19/32），又造出白给 3★ 的盘；③ 根因是**自变量选错**而非系数偏：v0.1 按「颗」定价，而 T-162/T-180/T-168 之后机制按「次」批量（`collectMisplacedGroup` 8 向连通不限步、`retrieveSelectedGroup` 一次 min(组,连续空槽)、`planGroupFill` BFS 连通空格不限步、`FILL_POP_RESTART_GATE_MS` 门内不重启动画）⇒ 单颗珠边际时间≈0；④ 6 个静态候选模型全部被实测否掉（最好的 P5 `2·ceil(M/6)` 中位误差 38%、最大 87%；曾拟的 `2·ceil(M/min(12,B_mean))` 中位比值仅 0.38，严重低估）⇒ 放弃拟合，改为**直接测真值** |

- 3★ 线（`STAR3_RATIO=0.32`）不随本变更动：时限改由实测给出后，3★ 可得性随 T 同步伸缩，待 playtest 复核。
- 失败重开零惩罚（D7 不变）；重试 = 恢复初始错位布置（`timer-gameover §2.4` v1.3）。冲刺模式另见 `gdd/score-combo.md`。
- **历史债（v0.2 实测暴露，2026-09-21）**：L7 猫咪脸实测 134 taps ⇒ 482s > `LEVEL_TIME_MAX`，
  按 v0.2 闸属「不可入关」，但其现存 time=403 是 playtest 校准值 ⇒ 用户拍板**保留不动**（L1–L8
  一律不回写），记为待重做的历史债（减 M 或拆分盘面）。同批 L9 `studio-4-4c47`（29×29、M=841、
  实测 332 taps ⇒ 1195s，而 time 竟是 120s）已**从关卡表移除**；原 L10 `studio-5-8b07` 顺位为 L9，
  time 由触顶 420s 回写实测 392s。

### 5.1 尾盘满槽（GAP-06）交叉校验 —— ⛔ v1.2 整体作废（WXG-T-130 案 A）

> **⛔ 作废声明**：本节全部前提（定时供料、供料冗余、满槽跳供、死珠棘轮）随供料关停消失——托盘珠唯一来源是玩家取回、必有可周转路径（`core-loop §6`、`tray-spawner §2.4` GAP-06 闭合声明）。原文保留于 git 历史，此处不复制。3★ 与满槽的交叉约束由 §5 重推口径取代。

<!-- 原供料侧定性分析（WXG-T-081）随 v1.2 作废删除；作废原因见上。 -->

**供给冗余（派生自 §3 表与 §3.4/§3.5 常量）**

| 关 | 可填格 | 供料上限 = time ÷ spawnInterval | 对需求的冗余 | 本节手速假设（2–3s/珠，取中 2.5s）vs 供料节律 |
|---|---|---|---|---|
| L7 | 79 | 380 ÷ 3.0 ≈ **126 颗** | ≈ **+60%** | 2.5s/珠 **快于** 3.0s/颗 ⇒ 名义上不因总量堆积 |
| L8 | 65 | 280 ÷ 2.5 = **112 颗** | ≈ **+72%（八关最高）** | 2.5s/珠 **恰等于** 2.5s/颗供料节律（**零余量**）；手速落在假设下沿（3s/珠）或更慢时，**供料快于手速** ⇒ 托盘常驻高位 |

**定性结论**

1. L7/L8 的风险**不在总量而在颜色构成的棘轮效应**：零杂色不等于零死珠——供料按「网格仍需数」加权、不扣「托盘已持有同色数」（见 `gdd/tray-spawner.md` §2.4 缺口登记第 2 条），故某色需求归零后仍持有的珠不可落子且**不可逆累积**，有效容量单调下降。
2. 尾盘（剩余需求 < 剩余供料量）窗口越长，棘轮越可能触顶 12 槽；L8 时限最短（280s）+ 冗余最高（+72%）+ 供料最快（2.5s）⇒ **尾盘窗口占比八关最大**。故：**L7/L8 尾盘软锁死属「高可能」而非「小概率」，L8 > L7**。
3. **3★ 交叉（派生自本节既有硬上限口径 + `STAR3_RATIO`=0.32）**：L8 供料受限通关下限 = 65 × 2.5 = 162.5s，3★ 线 = 280 × 0.68 = 190s ⇒ 余量 27.5s；而满槽跳过供料「不排队、不补发」（S4 §2.2 第 4 步）⇒ 每次跳过永久吃掉 2.5s，**L8 的 3★ 只容得下 ≤ 11 次满槽跳过**（(190 ÷ 2.5) − 65）。L7 同法：下限 237s、3★ 线 258s ⇒ 余量 21s ≈ **≤ 7 次跳过**。即：L7/L8 的 3★ 实质上是「**零死珠 + 几乎零满槽**」的熟练挑战。

**建议（按优先序，均不新增数值）**

1. **首选：先落泄压阀 A′**（供料侧维持 `held ≤ demand` 不变量，见 `ux/ux-spec.md` §8 U8）。落地后 L7/L8 **无需调 `spawnInterval`、无需扩容**：每颗珠恒可落子 ⇒ 满槽不再等于死锁，上述 3★ 余量从「死锁风险」降为「纯节奏挑战」。**修可玩性优于调参**，调参只会把死锁概率往下压、不会归零。
2. 若用户选 B / C / D（三者均**不解决「过度供给产死珠」**：D 只去杂色、C 只清杂色、B 靠玩家手动补救），则 L8 需二选一：① 把 `spawnInterval` 由 2.5s **上调至 3.0s**（= L7 值，仍在 §3.4 合法区间 [2.0, 6.0] 内，且等于本节手速假设上沿）；或 ② 给 `TRAY_EXPAND_SLOTS` 一条**免广告解锁路径**（该常量已在 §3.4 冻结但 `powerups` §2.6 自认无解锁路径 = 惰性常量）。**推荐 ①**：改 1 个关卡字段 vs 造一条新解锁通道（新事件 + 新 UI + 新常量，且与 A1「激励视频解锁」用户裁定相冲）。
3. **不推荐**把扩容当首选：参考作确实随棋盘规模扩容（L1=12 / L2=24，参考分析 §2.2/§5），但 beads 的扩容绑定激励视频（A1 用户拍板），本轮无 AppID / 广告 SDK ⇒ 扩容是「纸面容量」，救不了实战死锁。
4. **playtest 触发线**：本节既有口径与预估法不改；**观测项**交 QA 的 playtest 计划定义（建议观测：尾盘是否出现「满槽且不可恢复」、以及 L7/L8 实测满槽跳过次数是否超过上述 3★ 余量），**阈值不在本策划文档内发明**。

## 6. 布局可视化（图例：`·`=空位　`▓`=锁定 x　`1`–`8`=色板索引对应色）

### L1 暖心（6×5）
```
·55·55
555555
515555      ← 1=奶白高光
·5556·      ← 6=丁香紫底影
··66··
```
### L2 小屋（8×6）
```
···88···
··8888··
·888888·
·333333·      ← 3=活力橙墙　8=赭棕屋顶
·321123·      ← 2=柠黄窗　1=奶白门
·333333·
```
### L3 摆尾小鱼（9×6）
```
··7777··5      ← 5=玫红气泡
·777777··
27777771·      ← 2=柠黄尾尖　1=奶白眼
·777777··
··7777···
···77····
```
### L4 结果的树（10×7）
```
····24····      ← 2=柠黄顶芽
··414444··      ← 1=奶白高光
·44444444·
4444444444
·44454444·      ← 5=玫红果实
····88····      ← 8=赭棕树干
····88····
```
### L5 猫脸（11×8）
```
·33·····33·      ← 耳廓
·353···353·      ← 5=玫红耳内
·338383833·      ← 8=赭棕条纹
·337357333·      ← 7=湖蓝眼　5=玫红鼻
·333111333·      ← 1=奶白口鼻
··3336333··      ← 6=丁香紫蝴蝶结
··3333333··
··3·333·3··      ← 爪
```
### L6 顺风帆船（12×9）
```
··········22      ← 2=柠黄落日
······84····      ← 4=草绿旗　8=赭棕桅
·····1881···      ← 1=奶白帆
····188881··
···18588881·      ← 5=玫红帆纹
·8888888888·      ← 船体
·788888887··      ← 7=湖蓝吃水线
777777777777      ← 海面
x·66·777·7x·      ← x=锁定浪花（新变量）
```
### L7 升空热气球（13×10）
```
·7··15551··7·      ← 7=湖蓝天　1=奶白球顶高光
··153333351·2      ← 2=柠黄日
·15333333351·
·15333333351·
·15333633351·      ← 6=丁香紫球底纹
··153333351··
·····151·····
·7··8···8··7·      ← 8=赭棕吊索
····88888····      ← 吊篮
4·111···111·4      ← 4=草绿树　1=奶白云
```
### L8 星际火箭（13×12）
```
······5······      ← 5=玫红鼻锥
·····555·····
····55555····
6···11111····      ← 1=奶白箭体　6=丁香紫星
···1177711···      ← 7=湖蓝舷窗
···1177711···
···1111111···
··551111155··      ← 尾翼
·55··111··55·
····23332····      ← 3=活力橙火焰芯
·····222·····      ← 2=柠黄外焰
8···444····8·      ← 8=炭边星　4=草绿行星
```

## 7. 关卡设计约束（新增关卡必须遵守）

1. 字符集限定 `BEAD_CHARSET=".x1-9A"`，其余字符（含空格/小写/0）均为错误——校验器报错到 id+行+列。
2. 每关只引入一个主新变量（支柱 3；对照 §3 表"新变量"列）。
3. 色数 ∈ [3, 10] 且 ≤ `BEAD_COLOR_MAX`；索引 9/A（深棕/炭黑）**v1.36 起启用**（用户 2026-09-21 拍板「允许不超过 10 色」，原 v1.1 扩展预留·ADR-0004 复评条款触发；demo 旧关仍 3–8 色不变）。
4. decoys 恒为空数组（惰性）。
5. 图案原创；异形轮廓靠 `.` 空位塑形，`x` 仅作视觉收边/教学，≤8 枚/关。
6. time ∈ [180,420] 越界即 BOOT 拒绝；~~spawnInterval ∈ [2.0,6.0]~~ **⛔ v1.2 作废**。
7. **swaps 遵守 §2 错位约束 + §2.1 恒等式**（对数 ∈ [1,8]、坐标可填、异色对）；可解性由交换构造保证（§2.1），无需运行时搜索。
8. 可填格 ≥1 且预估清关时长 ≤ time×0.85（防结构性不可通关；v1.2 预估法见 §5——错位玩法下该约束在八关建议值下自动满足）。

## 8. 校验分工

与 breakout 判例一致（breakout `levels-spec.md` §5.1）：**结构校验**（字符集/行宽/行数/色数/时限/**swaps 静态规则**/杂色空数组）归 BOOT 期 `validateLevel()` 单一权威；**跨关/玩法校验**（§7 约束 2、难度单调性、k 曲线合理性）归 QA 脚本；运行时合法性归 S1/S3。错误信息格式：`L{id} row{i} col{j}: ...`（swaps 违规另含对序号）。

## 9. 冲刺模式（sprint）图案来源

sprint 梯级图案从本文件 8 关图案池**循环取用**（`gdd/score-combo.md` §2.3），不另建图案库；梯级参数由公式生成（score-combo 附录 C6，真源 `gdd/systems-index.md` §3.10——色数/格数为该阶上限预算，实际棋盘取池图案本身，见 score-combo §9 C6 设计说明；**v1.2：stage 切换后由 `swaps` 交换构造生成新错位布置**，供料间隔公式段作废），不改本 JSON。

## 10. 变更记录

| 版本 | 日期 | 变更 | 任务 |
|---|---|---|---|
| v1.1 | 2026-09-14/15 | 建档后回写：§5.1 尾盘满槽交叉校验（WXG-T-081）、§1/§2 `decoys` 上限追正 §3 v1.17（WXG-T-098） | WXG-T-081/098 |
| v1.2 | 2026-09-16 | **错位归位规格化（WXG-T-130 第二批，用户 2026-09-16 裁定案 A）**：① §1/§2 **新增 `swaps` 字段**（交换对数组）与错位约束（坐标可填/异色对/对数 ∈ [`MISPLACED_PAIRS_MIN`, `MISPLACED_PAIRS_MAX`]）；② **新增 §2.1 交换构造法**——可解性由可逆置换构造保证、错位珠计数恒等式 `misplaced === 2 × swaps.length`、底色语义升级；③ §3 表加 swaps 建议列 k=[1,2,2,3,4,5,6,8] `[待 playtest]`（区间冻结、逐关值调优走 JSON）、难度旋钮③由供料间隔改错位对数、spawnInterval 列 ⛔ 作废；④ **§5 时长重推**（清关 ≈ 2k×2.5s + 寻找 ≤100s 量级；时间压力降级为软压力，难度主承载转移至解谜——案 A 固有推论，诚实记录）；⑤ §5.1 尾盘满槽校验整体 ⛔ 作废（GAP-06 闭合连带）；⑥ §7 约束第 6/7/8 条更新。**本版不改 `levels-01-08.json` 数值**（swaps 字段写入归工程后继，version 升 2） | WXG-T-130，用户 2026-09-16 四项裁定 |
| v1.5 | 2026-09-21 | **§3 v1.4 素材制作规范固化（新增 §3.1）**：四步流程（原图选材 → 生图 → beads-studio 生成 → importLatest 落位回填）+ 产物登记表（素材暂在仓外，逐关完成后回填）；盘面规格维持 10×10 → 29×29 阶梯不变 | WXG-T-179 |
| v1.6 | 2026-09-22 | §3.1① 补**组合图（Plate）做图口径**（用户拍板）：2048@32px/颗 = 64×64 母版 → 2×2×32×32 组合图 4 关；1024@32px = 32×32 最大单图档；`GRID_MAX=50` 不变（= 单图/组合图分界线，>50 自动切块）。正本 = `proposals/level-content-pipeline.md` §0.3。**零 JSON/常量改动。** | WXG-T-185 |
| v1.7 | 2026-09-22 | §3.1① 组合图口径补**宫错位密度上限**（WXG-T-203 E1 第二轮定案，随 §3.4 v1.42 丙档同批）：宫（32×32）错位密度 ≤60% 且必须「整区域就位」构造；全错位宫结构性不可入关（176–178 taps 两轮复现）。**零 JSON/常量改动。** | WXG-T-203 |
