# GDD · S3 拼图网格与填色（Bead Grid）· beads

- 项目：`games/beads` · 版本 **v2.9**（v2.9 = **§3.2 批2 落码：`max`（最大化错位）档转正** ⇒ §8-1 的 `max` 句由「属批2 另单、未生效」改为**生效口径 + QA 可断言形式**（详 §9）；v2.8 = **§3.2 翻值生效连带同步**（`BEAD_COLOR_MAX` 10→35 / `BEAD_CHARSET` → `.x1-9A-Z`，§2.1 口径转正、§8-10 采推荐甲收窄、§8-1 适用域注转正），详见 §9；v2.7 = 同主题**提案注**（已被 v2.8 转正覆盖，提案本体保留）；
  ⚠️ 头注曾长期停在 v2.6（v2.7 行入表时未回头注）⇒ v2.8 行一并修正该既存漂移，非内容变更。以下历史括注自 v2.6 起沿用，任务号 **WXG-T-204**：v2.6 = **取回落槽回退「自动归类」**（落位 = `insertGrouped` 逐颗归类，同色成块 + 紧凑无洞；点击空槽仅作触发、不再决定落位），用户 2026-09-23 裁定，**推翻 v2.3 / WXG-T-168 点槽定落位、恢复 v2.1 裁定①**，配套 `tray-spawner` v2.4 / `input-control` v2.10；v2.5 = **组珠消费序改「沿组连通的剥皮序」，直填与取回落槽共用同一份序**，用户 2026-09-22 裁定，推翻 v2.4 的几何切比雪夫口径；v2.4 = **board 锚直填消费序改「选豆点固定 + 同序配对」**，用户 2026-09-20 裁定；v2.3 = **取回落槽改「玩家点槽定落位 + 整组连续相邻」+ 容量不足改「部分收纳·距锚就近优先」**，用户 2026-09-18 真机复测后**推翻 v2.1 裁定①（自动归类）**；v2.2 = **盘侧组选改完整连通块（8 向不限步）+ board 锚直填放开距离**，用户 2026-09-18 真机裁定，覆盖 T-157 ①②③，任务号 WXG-T-162；v2.1 = **托盘侧组归位（同色全组批量填充，8 向不限步、部分填充）+ 取回落槽自动归类**，用户 2026-09-18 四项裁定；v2.0 = 「错位归位」状态机改写；v1.0 见 §9）
- 数值纪律：只引用 `systems-index §3` 常量名，不写死数值（来源标注见各节）。
- 依赖锚点：挂在 `gdd/core-loop.md` §2.2 关内微循环第 1–4 步（错位判定、取回/归位裁决与完成判定）。

---

## 1. 目标

持有图案矩阵（唯一进度真源），裁决每次**取回（retrieve）**与**归位（placed）**的合法性，维护格子状态机（v2.0：`filled(错位) ⇄ empty` 双向），并在错位珠全部归位时广播通关信号。

## 2. 机制

### 2.1 数据结构（关卡 JSON → 运行时矩阵；v2.0 加错位装配）

- 关卡以**行字符串数组**描述图案，字符集 = `BEAD_CHARSET`（来源 §3.2）：`.`=空位（**无图案位**：不可填、不计完成、渲染为空白）、`x`=锁定格（不可填、不计完成，渲染为斜纹收边，见 levels-spec §6 图例）——`.` 与 `x` 运行时语义同为 `locked`，差异仅在表现层；`1-9`+`A-Z`=色板索引 **1–35**（**§3.2 v1.55 生效**，旧口径 `1-9`+`A`=1–10；索引对应本关色板顺序，`A` 即第 10 色）。〔**v1.55 生效登记（正本 §2.1 提案 v1.54）**：色索引 = **1–35**（= 单字符 rowstring 的编码天花板）；两条消歧硬规定：**只收大写** 与 **`X`=索引 33（`Y`=34、`Z`=35）而 `x`=锁定符** ⇒ 本节的解码**不得大小写不敏感**（正本原文写「`X`=34」系**偏一**，已按表位序与 `charCodeAt-55` 双路实测订正）。原「本节与 `config/levels.ts` / `game/misplaced-assembler.ts` 同名判定属**同源三写**」的风险**已由落码消除**：全仓独立解码表实测 **7 份**（正本记 4 份），现收敛为两份同源件（游戏侧 `config/bead-charset.ts`、工具侧 `tools/scripts/lib/bead-charset.mjs`），`entities/grid.ts` 与 `misplaced-assembler.ts` 均改调同一表，由判据 **X1** 逐字符钉死（`tests/bead-charset.test.ts`）〕
- BOOT 期静态校验（字符集、行宽 === 声明 cols、行数 ≤ `GRID_MAX_ROWS`、≥1 可填格、**`swaps` 合法性**——坐标可填、每对珠色不同、对数 ∈ [`MISPLACED_PAIRS_MIN`, `MISPLACED_PAIRS_MAX`]，§3.13）归关卡校验器，形态沿用 breakout `validateLevel()` 判例；**S3 运行时不再重复字符集校验，但保留防御性断言**。
- **初始装配（v2.0）**：BOOT 按 `swaps` 顺序对可填格珠**两两交换**得初始布置——棋盘开局**全满**；pattern 字符 = 格**底色**（目标色）。装配后托盘为空（供料关停，§3.4 作废标注）。
- 运行时矩阵每格状态 ∈ `empty | filled | locked`，`filled` 格附带 colorIdx。
- **错位判定（v2.0）**：`misplaced(cell) = (state === filled) 且 (cell.colorIdx ≠ pattern(row,col))`。**已就位珠** = 非 misplaced 的 `filled`（珠色 = 底色，不可选）。

### 2.2 格子状态机（v2.0 改写：`filled` 不再是终态）

```
empty ──(归位: 空位 且 颜色匹配，珠来自托盘)──► filled(就位)【终态】
filled(错位) ──(取回: 托盘有空槽)──► empty        ← v2.0 新增转移
empty ──(归位: 空位 但 颜色不匹配)──► empty（播 wrong 态，状态不变）
locked：恒定，不参与任何转移与完成计数
```

- **`filled(就位)` 为终态**：珠色 = 底色，不可选中、无转移（v2.0 原文「`filled` 为终态：道具不可消除、无回退」**被本版推翻**——`filled(错位)` 可经取回回到 `empty`，也可被解环器归位/交换）。
- **`filled(错位)` 是可转移态**：唯一回退通道 = 取回（`tray:stored`，托盘有空槽为前提，§3.13）；唯一正向通道 = 解环器（S6 → S3 写入，§3.6）或该珠被取回后由托盘重新归位。道具/取回以外的任何指令不得改变 `filled(就位)`。
- **组语义（v2.2 · WXG-T-162 用户真机裁定④⑤，覆盖 T-157 ①）**：选中锚 ⇒ 组 = 锚起 **8 向（3×3 含对角）连通、同色错位珠的完整连通块，不限步数**（「找相邻直到找不到，所有找到的珠子一起抬起」）；就位珠/锁定格/空格/异色错位珠均**阻断传播**（隔珠同色 ⇒ 不同组）。**board 锚直填**（`input-control §2.1` 5b，T-157 立、v2.2 裁定③放开距离、v2.4 消费序改写、v2.5 消费序换度量）：点底色 = 组色的 `empty` 格，**任意距离**（旧切比雪夫 ≤2 门已删）⇒ **组批量归位（v2.4，用户 2026-09-20 裁定）**：目标 = 被点格 + 8 向 BFS 连通同色空格（由近及远，上限 = 组内可用错位珠数）；组员消费序 = **剥皮序（v2.5 · 用户 2026-09-22 裁定，见下「消费序」条）**；两者**同序配对**——消费序首颗填最近目标（~~v2.2「取组内离目标最近者」~~ ⛔ 作废）retrieve+fill 同帧两写直接归位（搬移语义，源格空出）；组保持、锚珠被填 ⇒ 仅展示用**头珠**转移到剩余序首、**选豆点不改**、组空锚清；归位达成零错位 ⇒ cleared-priority 照常。
- **消费序（v2.5 · WXG-T-186 用户裁定，推翻 v2.4 的「距选豆点切比雪夫升序」）**：「距离近者优先」不变，但「最近」从**几何距**改为**沿组连通的图上距离**——组是 8 向连通块，几何近 ≠ 连通近，旧口径两条后果：① 组拐弯/有缺口时跳过缺口先吃对岸珠（隔空取物）；② 同圈层行主序裁决 ⇒ 最左上的珠最先被吃（与玩家「一片揭起、最远的留到最后」相反）。四级优先级（高→低，写死不可调）：

  | 级 | 规则 | 性质 |
  |---|---|---|
  | 1 | 候选必须与「已揭起区」8 向邻接 | **硬** ⇒ 全程不隔空取物 |
  | 2 | 优先取「删掉后剩余仍一整片」者（= 非割点） | 软 ⇒ 尽量不吃散；全池皆桥或组已碎则停判 |
  | 3 | 同层内「仍有未吃下层邻居」的引路珠押后 | 软 ⇒ 末端珠先吃、引路珠最后吃 |
  | 4 | 图上距离（BFS 层号）↑ ⇒ 行主序 ↑ | 确定性平局 |

  级 1 与级 2 不可兼得（往外揭迟早要吃桥接珠），让步方向固定为**级 2 让步**。**消费序只在拾取那一下算一次**（一次点击只取前缀，不设颗数上限），**直填与取回落槽共用同一份序** ⇒ 两路口径一致由构造保证。**选豆点仍恒定**（v2.4 裁定沿用，作为 BFS 根）。**§3 数值零改动**；实现 = `systems/consume-order.ts::planConsumeOrder`（O(n²)，12×12 整片 144 颗实测 0.7ms）/ `beads-game._boardSelected.order`。
- `hint` / `wrong` / `selected` 为**表现层态**（绘制参数见 `art/assets-spec.md` §1.2），不改逻辑态；S3 只广播事件，由视觉层驱动。v2.0 起 `selected` 态可属于错位珠（`board:selected`）或托盘珠（`tray:selected`），两者互斥（选择锚，`input-control §2.1`）。
- 图案字符 `.` 与 `x` 在矩阵初始化时**均映射为 `locked`**（§3.2 语义澄清，2026-09-12，不变）。

### 2.3 裁决（核心规则；v2.0 = 取回 + 归位两条路径）

**路径 A · 取回（retrieve）**——输入：`(row, col)`（组收进逐颗执行），来自 S2，锚 = `board`（`board:selected` 的错位珠组）：
1. 目标格非 `filled(错位)`（即 `locked` / `empty` / 已就位珠）→ 请求不成立（S2 层拦截；已就位珠/锁定格点击走极轻非惩罚反馈，裁定 4）。
2. 托盘无空槽 → **满槽禁取珠**（§3.13）：拒绝，极轻非惩罚反馈、**零事件、零状态写**；**整组收进口径（v2.6 / WXG-T-204：可收数按总空槽数）**：可收数 = `min(组大小, tray.freeCount)`（紧凑无洞 ⇒ free 恒尾段连续）—— **有多少空槽就收多少颗**，所收 = 组珠**消费序的前 `count` 颗**（v2.5：与 §2.2 直填共用同一份剥皮序，拾取时一次算好；~~v2.3/v2.4「距选豆点最近者」几何序~~ ⛔ 作废）；余珠留格且**头珠改指剩余序首**（可续点，锚变更不发事件）。`freeCount = 0`（满槽）⇒ 拒绝。
3. 通过 → **同帧原子写入**：格 `filled(错位)` → `empty`，槽 `free` → `holding(colorIdx)`（**v2.6（WXG-T-204）：落槽 = 自动归类插入 `insertGrouped`** —— 同色块尾 / 最左可用空格、紧凑无洞，`tray-spawner §2.1/§2.4`；点击空槽仅作 4b 触发；~~v2.3（WXG-T-168）点槽定落位~~ ⛔ 作废），广播 `tray:stored {slot, colorIdx, fromRow, fromCol}`。取回**不触发完成判定**（`filled` 只减不增）。

**路径 B · 归位（placed；v2.1 托盘侧改组归位）**——输入：`(row, col)` + 当下选中组色 `colorIdx`，来自 S2（锚 = `tray`）或 S6（解环器，单颗、无 `slot`）：
1. 目标格非 `empty` → 忽略（不广播、无反馈噪声，core-loop §6）。
2. 目标格 `empty` 但 `pattern(row,col) ≠ colorIdx` → 广播 `bead:rejected {row, col, colorIdx}`，组**整组留在托盘**（无惩罚，A4 已确认；仅托盘路径可达——解环器按构造必匹配）。
3. **匹配（`pattern === colorIdx`）⇒ 组批量填充（用户裁定③④）**：从被点格起 **8 向 BFS、不限步数**，沿「`empty` 且底色 = 组色」的连通空格蔓延（含被点格），至多填 `min(组珠数, 连通格数)` 格：被点格必最先填，其余按 BFS 距层就近逐格；每格一份 `bead:placed {row, col, colorIdx, slot}`（逐颗携各自来源槽，同一调用栈内串行发完）。**部分填充口径（裁定③）**：珠不够 ⇒ 点到珠为止（未触达的连通格保持 `empty`）；珠有余 ⇒ 剩余珠**保持 `selected`**（锚不变，可续点）；组清空 ⇒ 锚回 `none`。**不得把批量填充判成「一次归位多发事件」缺陷**（判据见 §8-11）。
4. 每颗 `bead:placed` 后执行完成判定（同帧逐格计数，末颗落定即判）：可填格（`empty + filled` 总数）中 `filled` 占比 100%（= **错位珠全部归位**）→ 广播 `level:cleared` 前置信号给 S1（cleared-priority 同帧成立）。

### 2.4 定位派生（来源 §3.3 公式，本文只引用）

- `colCenterX(j) = gridLeft + BEAD_CELL/2 + 52 * j`
- `rowCenterY(i) = gridTop − BEAD_CELL/2 − 52 * i`（第 0 行在最上，y 向上）
- 网格在 `PUZZLE_BAND` 内垂直+水平居中；`gridLeft ≥ 30`、`gridTop ≤ 1120`、`gridBottom ≥ 480`。

## 3. 输入

| 来源 | 内容 | 说明 |
|---|---|---|
| S2 | 取回请求 `(row, col, targetSlot)` / 归位请求 `(row, col, colorIdx, slot?)` | 每次最多 1 个在途请求（见 §6 并发） |
| S6 | `powerup:used {type, affectedCells}`（解环器） | S3 执行网格写入（v2.0：S3 是解环器写入的执行方），逐格发 `bead:placed`（无 `slot`） |
| S1 | 关卡装配指令 | BOOT → PLAYING 时装入矩阵、按 `swaps` 执行错位装配（全 `filled/locked`，托盘侧为空） |
| 关卡数据 | 矩阵 + cols/rows + swaps | BOOT 期已过静态校验（含 §3.13 错位校验） |

## 4. 输出与反馈

| 事件/表现 | 条件 | 对齐 |
|---|---|---|
| `tray:stored` | 取回裁决通过（v2.0 新增） | 错位珠离格飞入托盘槽（`ux-spec §5`「取回入槽」行）；S3 格写入与 S4 槽写入同帧原子（`core-loop §2.2.2`） |
| `bead:placed` | 归位裁决通过 | 落座动画 `vfx_fill_pop`（assets-spec §1.6）+ 音效 |
| `bead:rejected` | 颜色不匹配（仅托盘归位路径） | `vfx_wrong_shake` ±3px ×2 + `danger` 描边闪 ≤2 次/秒（§3.8） |
| 完成前置信号 → `level:cleared` | 可填格全满（= 错位珠全部归位） | 庆祝 `vfx_complete_wave`（按列延迟 20ms）。**层序裁定（WXG-T-128，2026-09-16 用户拍板）**：庆祝 800ms **先行放完**、结算面板**延迟 800ms 开**——否则同帧开面板会使全屏 scrim α0.5 把庆祝完全遮死（真源：`ux-spec §5`「过关庆祝」行） |
| **（无事件）表现层轻压**（WXG-T-128 新增；v2.0 适用对象扩展） | 对 `locked` 格或**已就位珠**（`filled` 且珠色=底色，v2.0 语义）点击被忽略（`verdict = ignored`）；**错位珠不适用本行**（错位珠可选中，走 `board:selected`） | `vfx_denied_press` scale 1.00→0.96→1.00（120ms，`art-bible §7.3.7` / `assets-spec §1.6.7`）+ `sfx_denied` 极轻闷「哒」（已随 WXG-T-152 原子批落码，`audio-events §5` Q-A05-5 已闭环）。**零事件、零扣次、零状态写**；`reduceMotion` 开 ⇒ 1px `slot_border` 静态描边环 |
| 格子绘制 | `filled(就位)/filled(错位)/empty/locked` | assets-spec §1.2 参数卡。**⚠️ 错位/就位的形态区分（2026-09-17 更正，WXG-T-140）**：本行原文「珠色≠底色即可辨」**属循环论证**——珠 50px = 格 50px、坑底色被珠体零露出，玩家在 filled 态**看不到底色**，该句不可用于证明「找错位珠」谜面可读（`assets-spec` L945 自认同款缺陷）。**更正后的真实现**：渲染层加 **L11 目标色垫（B′ 垫色显缝，`assets-spec §1.1`）**——每颗棋盘 filled 格在珠层下画全格垫（ink = 该格 `pattern` 目标色暗一档 `mix(target,#000,0.30)`），珠视觉内缩 2px/边露垫成缝；**垫色 = 珠色 ⇒ 就位，垫色 ≠ 珠色 ⇒ 错位**（用户 2026-09-17 拍板 `accessibility.md §5.4`，先 (b) 描边环后切 B′，WXG-T-140→142）。托盘珠 / `locked` / `empty` 三态不带垫。逻辑态（`misplaced` 判定 `colorIdx ≠ pattern(row,col)`）**不变**，本条只改可读性载体 |

## 5. 数值

全部引用 `systems-index §3`：§3.2（`BEAD_CHARSET`、`BEAD_COLOR_MAX`、`BEAD_PALETTE` 索引映射）、§3.3（`BEAD_CELL`、pitch 52、`GRID_MAX_COLS/ROWS`、`GRID_MIN_COLS/ROWS`、定位公式）、§3.8（错误反馈频率红线）。

## 6. 边界条件

**极端值**
- 矩阵无可填格（全 `x`/预填）：关卡硬约束（core-loop §6），静态校验拒绝，运行时断言兜底 → BOOT 拒入。
- `colorIdx` 越界（> `BEAD_COLOR_MAX` 或 0）：防御性拒绝，按 `bead:rejected`（归位路径）处理并记警告日志；取回路径的越界格不可能存在（`swaps` 只在合法色板索引内交换，BOOT 已校验）。
- 最后一颗错位珠归位与失败同帧：S1 按 cleared 优先裁决（`core-loop §6`，机制见 §2.2.2：cleared 属输入段、failed 属计时段），S3 照常广播 `bead:placed` —— 且该回执**先于** `level:cleared`（玩法事件段内序，WXG-T-061 补齐）。
- 托盘满 + 多颗错位珠：取回被拒（§3.13 满槽禁取珠）但不死锁——已持有托盘珠总可归位或由解环器腾挪；不存在「珠既回不了格也进不了盘」的状态（错位珠原格被占仅在交换构造中瞬时出现，装配期不落盘）。

**并发 / 竞态**
- 同一帧多次取回/归位请求：**不可能**（每帧最多 1 条输入指令，`input-control §2.4`）；测试/harness 程序化注入须显式排序并按 `core-loop §2.2.2` 解释。
- 归位请求与 `level:cleared` 广播同帧：两者同属**输入段**、每帧最多 1 条指令 ⇒ 本帧内**不可能**发生「cleared 后再归位」；跨帧时 cleared 之后的请求一律忽略（状态机已离开 PLAYING）。WXG-T-061 换基准，结论不变。
- 取回请求与 S4 槽态竞态：S3 裁决前向 S4 复核目标槽仍为 `free`（满槽禁取珠在 S4 侧兜底），否则走拒绝分支（零事件）。
- 解环器写入与托盘归位争用同一格：同属输入段 ⇒ 不可能同帧；跨帧后到者按最新格态重解释（格已 `filled` 则走忽略/交换规则，`powerups §2.3`）。

**非法输入**
- `(row, col)` 越界：忽略 + 警告日志。
- 对 `locked` 格或**已就位珠**点击：**零事件忽略**（core-loop §2.2/§6，裁定 4）+ 极轻非惩罚反馈（WXG-T-128 形态，见 §4 表末行与 §8-4）。**v2.0 语义收窄**：原「对 `filled` 格」→ 现「对 `filled(就位)`」——错位珠（`filled(错位)`）**可选**，不属本条。
- 托盘满时取回请求：拒绝，零事件零状态写 + 极轻反馈（§2.3 路径 A 第 2 步）。
- BOOT 未装配即收到取回/归位请求：忽略（状态机保护）。

## 7. 依赖

- **上游**：S1（装配/状态机/错位装配）、S2（取回/归位请求）、S4（空槽复核）、S6（解环器请求，经 `powerup:used`）、关卡数据（BOOT 静态校验后输入，含 `swaps`）。
- **下游**：S1（cleared 前置信号）、S4（`tray:stored` 的槽位写入对端，同帧原子）、S7（placed/cleared 计数）、视觉/音频层（事件消费）。
- **明确边界（v2.0）**：~~S4 依赖 S3 的"仍需颜色集合"~~ 已随供料关停作废（§3.4）；S3 对托盘的唯一写入 = 取回路径的槽位变更（经 S4 执行、同帧原子）。

## 8. 验收标准（可测试硬判据，QA 直接造用例）

1. **初始装配（v2.0 改写）**：装载合法关卡后，`filled` 格数 === 可填格总数、`locked` 格数 === cols×rows − 可填格总数、托盘全 `free`；`misplaced` 计数 === 2 × `swaps.length` 且每颗错位珠满足 `colorIdx ≠ pattern(row,col)`、每颗就位珠满足 `colorIdx === pattern(row,col)`。**❗适用域注（文档事实澄清，**不依赖 §3.2 翻值**，随 v2.8 转正）**：恒等式 `misplaced === 2 × swaps.length` **只对 `swaps` 型关卡成立**；`misplaced` 初盘型的真判据 = 「可填轮廓逐格匹配 + 每色珠数守恒 + 错位 ≥1」（`misplaced-assembler.ts::validateMisplacedGrid`，其头注已自行修订该恒等式为 `misplaced = swaps.length + c`）。✅ **`max`（最大化错位）档已生效（§3.2 批2，v2.9）**：该档下 **`M` 是可变输出**（`M = min(N, 2(N−m))`；`m > ⌊N/2⌋` 时**不再恒等于可填格数**）⇒ **QA 不得把「全错位」写成硬断言**，只能断言四条：① 可填轮廓逐格匹配；② **每色珠数守恒（`max` 档不放宽，= §3.13 校验④）**；③ 错位格数 `M ≥ 1`；④ `max` 档下 `M === min(N, 2(N−m))` 且**固定点数 === `max(0, 2m−N)`**（极值必达，「尽力而为」不是模糊语义而是可硬断言的确定值；已由判据 **X2** 穷举 6561 例钉死）。唯一退化 `M_max = 0 ⇔ N<2 ∨ m=N` 在合法关面（colors ≥ 3）**不可达**（判据 **X3**，实测 `min M = 4`）⇒ **不存在「合法却 0 错位」的盘**。`full` 档的 all-or-nothing 语义**保留不动**（`F = 0`），两档并存由调用方选；本条其余部分（`swaps` 型恒等式）不变。
2. **取回（v2.0 新增；v2.6 / WXG-T-204 落槽回退自动归类，推翻 v2.3 点槽定落位、恢复 v2.1）**：`board` 锚点空槽取回 → 目标格变 `empty`、**归类落位槽**变 `holding(colorIdx)`（**落位 = `insertGrouped`：同色块尾 / 最左可用空格，同色成块 + 紧凑无洞**，§2.1/§2.4；点击空槽仅作 4b 触发、不参与定位；~~v2.3 点槽定落位~~ ⛔ 作废）；`tray:stored` 恰广播 1 次/颗（**部分收纳时 = 实收颗数**，非组大小），payload 含 slot/fromRow/fromCol/colorIdx；逐颗取回后 `misplaced` 计数 −1、`filled` 总数 −1。
3. **归位（v2.1 改写为组归位口径；单珠组时退化为旧单珠行为）**：匹配归位 → 被点格变 `filled(就位)` 且 colorIdx 与图案一致；**托盘组为单颗时 `bead:placed` 恰 1 次**（payload 含 slot）；解环器路径不带 slot 逐格发；不匹配归位 → 被点格保持 `empty`；`bead:rejected` 恰广播 1 次；对应托盘组**一颗未被移除**（整组留盘）。
4. 对 `locked` 格与**已就位珠**点击：**零事件**（用事件监听计数器断言 =0）。**判据改写（WXG-T-128，2026-09-16 用户裁定；WXG-T-130 v2.0 语义收窄为「已就位珠」）**：原文为「零事件、**零反馈**」，后半已推翻——现行为「零事件 + 极轻非惩罚反馈」（被点格 120ms 内出现 scale < 1.00 的绘制命令；`reduceMotion` 开 ⇒ 1px `slot_border` 静态描边环）。**QA 不得再按旧文把轻压判成缺陷**；不可放宽的红线仍是「事件计数 =0」（不进状态机、不扣道具次数）。与 `input-control §8-5` 同批改写（两文互为镜像，不得只改一侧）。**错位珠不适用本条**（可选中）。
5. 将最后一颗错位珠归位 → S1 收到 cleared 前置信号 ≤ 1 帧内；`filled` 数 === 可填格总数、`misplaced` 数 === 0。
6. **`filled(错位) ⇄ empty` 双向性（v2.0 改写，原「不可回退」判据作废）**：对 `filled(错位)` 格执行取回 → 变 `empty` 且可再被归位（构造「取回后重新归位到原格」用例，终态回到 `filled(就位)`）；**`filled(就位)` 仍不可转移**——非取回指令（解环器/任何事件）命中就位珠 → 状态不变。
7. 同帧双归位同格（程序化注入）：仅第 1 条生效，第 2 条走忽略分支，`bead:placed` 总数 =1。
8. 全部 13×12 = 156 格极端关卡：格中心坐标逐格符合 §2.4 公式（抽样四角 + 中心共 5 格断言，误差 ≤0.5px）。
9. colorIdx = 0 或 99 的归位请求：按 `bead:rejected` 处理且记警告，不崩溃。
10. 黑白模式下 `filled` 格（就位与错位）仅凭符号+明度可与 **demo 十色**区分（对照 assets-spec §6 验收第 2 条联合用例）。**v2.0 范围注记**：本条只覆盖 `filled` 态——空槽 `empty` 的灰度可辨性已随 E4 移除**降档**（降级登记正本 = `art/accessibility.md` A2b/A3，用户签字 2026-09-16），**不得把空槽灰度判据算进本条**。〔**§3.2 v1.55 生效连带（采正本 §5-D 推荐甲）**：翻值后本条**适用域收窄为「仅覆盖 demo ≤10 色关」**；索引 11–35（`B`–`Z`，必携品牌引用）的关**不适用本条**——理由 = `accessibility.md` A3① 实测「明度单独**不足以分开 10 色**」，**35 色更无实测依据 ⇒ 不得把本判据直接改成「与全部 35 色区分」（不发明数值）**。>10 色关的灰度可辨性 = **待 art/a11y 复评**（用户 2026-09-16「暂不支持色盲」搁置裁定继续成立 ⇒ 本轮不新开判据、也不删历史登记）；❗**a11y 不新增拦点**：>10 色削弱色相可辨性一事**依赖已搁置的决策、非本次新造**〕
11. **新增（v2.1，用户裁定③④）组批量填充**：锚 = `tray`（组色 C、珠数 n）点底色 = C 的 `empty` 格 → 从被点格起 8 向连通（`empty` 且底色 = C、不限步数）区域内按 BFS 就近顺序填 `min(n, 连通格数)` 格；`bead:placed` 计数增量 = 实填格数且逐一携来源槽；被点格恒先填；珠数 < 连通格数 ⇒ 未触达格保持 `empty`（部分填充非缺陷）；珠数 > 连通格数 ⇒ 只填连通区、剩余珠保持 `selected`；区外同色空格**不填**（非连通 ⇒ 需再次点击）；不匹配点击（底色 ≠ C）⇒ 仍单格 `bead:rejected`、零填充。
12. **新增（v2.5 · WXG-T-186）组珠消费序（剥皮序）**：对 8 向连通错位珠组，`planConsumeOrder` 产出的序需同时满足 —— ① **全程不隔空**：第 i 颗（i ≥ 1）必与前 i−1 颗之一 8 向邻接；② **不吃散（形状无桥时可达）**：存在非割点候选时不得选割点，且对 3×3 实心块这类无桥形状，任意前缀消耗后剩余仍一整片；③ **同层引路珠押后**：同层候选中「仍有未吃下层邻居」者不得先于该层末端珠被消费（3×3 块中心珠恒为序尾）；④ **两路同序**：同一组在同一局内，直填消耗的前 k 颗 === 取回落槽消耗的前 k 颗；⑤ **确定性**：同输入两次调用逐颗相同（无随机、无集合迭代序依赖）。选豆点为 BFS 根且恒为序首。判据 = `tests/consume-order.test.ts`（7 例）。

## 9. 变更记录

| 版本 | 日期 | 变更 | 任务 |
|---|---|---|---|
| v2.9 | 2026-09-24 | **§3.2 批2 落码生效 ⇒ `max`（最大化错位）档在 §8-1 由「未生效」改转正**。唯一正文改点 = **§8-1 末句**：旧写「该档属批2 另单、至今未生效；**若**生效，QA 不得…」，现为**已生效的 QA 断言口径**（四条：轮廓匹配 / 每色守恒 / `M ≥ 1` / `max` 档 `M === min(N,2(N−m))` 且固定点 === `max(0,2m−N)`），并把退化情形与存在性写成可断言结论（`M_max=0 ⇔ N<2 ∨ m=N`，合法关面 colors≥3 不可达）。**为何本批只改 §8-1**：本档**不改变 §2.1–§7 的任何机制描述**（初盘仍由 `misplaced` 直读装配、零 RNG；裁决/状态机/依赖全不动），改变的只是**生成侧能产出什么样的初盘**（属 §8 判据与 `levels-spec §2.3`）。**判据取值来源（不是发明）**：极值式与三例 = 正本 §3.3（文策渊推导），**现已由代码穷举实测复核**（X2 6561 例全绿、X3 5796 例全绿、三例与手算逐字吻合）⇒ 本条从「文档内推导」升为「实测断言」。**§8-10 维持 v2.8 推荐甲不改数**（本批与灰度可辨性无关，>10 色关仍待 art/a11y 复评）。**未改（诚实记录）**：① `systems-index §3.13` 「错位档位」行仍标 `[提案 v1.54]` 且写「现行为（未改）」⇒ 已与代码漂移（改 §3 需翻值授权，本批任务单未含）→ 已列未决；② **`max` 盘的 `F` 颗就位珠观感未取证**，而 §8-1 旧文曾把「允许部分就位」当**难度调节手段** ⇒ 本档的 `F` 是**守恒副产物而非设计旋钮**，二者同字不同义，待 playtest；③ 本文无新自动化用例（游端 `misplaced-assembler` 校验未改，新判据全在生成侧 `tools/scripts/*.test.mjs`）。**零实现变更（本篇）、零 §3 冻结值变更、零关卡 JSON 改动**。登记人 = 会话执行（程基岩 · engineering-lead） | 用户 2026-09-24 批2 任务单；正本 = `design/proposals/sec3-color-max-35-and-mis-max.md` §5 C 清单；关卡侧 = `levels-spec.md` **v1.12** |
| v2.8 | 2026-09-24 | **§3.2 翻值已生效（批1 = A 天花板 + B 护栏）⇒ 本文两处行内注由「提案」改「生效」；§8-10 采推荐甲收窄适用域**。对应 §3 变更 = systems-index changelog **v1.55**（提案行 v1.54 保留）。**改点**：① **§2.1** —— 现值直写 `1-9`+`A-Z`=索引 **1–35**，去「未生效」注；❗**事实订正**：正本 §2.1 与 v2.7 行均写「大写 `X`=索引 **34**」⇒ 工程落码实测为 **33**（`A`=10、`X` 为第 24 字母、`Y`=34、`Z`=35），本文已按实测订正（正本提案文件未改，交主理人）；原「同源三写（全仓 4 份）」的风险**已由落码消除**：实测 **7 份**独立解码表，现收敛为两份同源件（`config/bead-charset.ts` / `tools/scripts/lib/bead-charset.mjs`），`entities/grid.ts` 与 `misplaced-assembler.ts` 均改调同一表，判据 **X1** 逐字符钉死。② **§8-1 适用域注转正**（恒等式只对 `swaps` 型成立 = **文档事实澄清、不依赖 §3.2 翻值**），但其中 `max` 档相关句**仍标未生效（批2 另单）**。③ **§8-10 采甲**：口径由「与全部 10 色区分」改为「**仅覆盖 demo ≤10 色关**」，11–35 色关**不适用本条、待 art/a11y 复评** ⇒ **未**把数值自 10 改成 35（无实测，不发明判据）。**附带一处陈年修正**：§2.1 旧写「索引对应 `BEAD_PALETTE` 顺序」—— 该全局真源已随 §3 changelog **v1.40** 删除（改品牌引用制），本文滞后，现改「**本关色板顺序**」。**头注版本 v2.6→v2.8**（修既存漂移：v2.7 行入表时未回头注，非内容变更）。**本篇零实现变更、零关卡 JSON 改动**。 | 用户 2026-09-24 批1 任务单；正本 = `design/proposals/sec3-color-max-35-and-mis-max.md` §5 A/B 清单 |
| v2.7 | 2026-09-24 | **同步注（提案·未生效）：§2.1 字符集口径与 §8-10 色数口径跟随 §3.2 提案 v1.54（`BEAD_COLOR_MAX` 10→35 / `BEAD_CHARSET` → `.x1-9A-Z`）；§8-1 补适用域注**。三处均为**行内注、不改现行值/现行判据**：① §2.1 —— 色索引将扩到 **1–35**，两条消歧硬规定（只收大写；`X`=索引 34 vs `x`=锁定符）与「全仓 4 份同源解码必须一次收干」的风险提醒；② §8-1 —— 恒等式 `misplaced === 2 × swaps.length` **只对 `swaps` 型成立**（`misplaced` 初盘型的真判据 = 轮廓匹配 + 每色守恒 + 错位≥1），且新档 `max`（最大化错位）使 **`M` 可变** ⇒ QA 不得把「全错位」写硬断言；③ §8-10 —— 「与全部 10 色区分」口径随翻值失效，**不得直接改成 35**（`accessibility.md` A3① 实测「明度不足以分 10 色」），列甲/乙/丙三选项待裁（推荐甲：本条降为仅覆盖 demo ≤10 色关）。背景 = 用户 2026-09-24 两条裁定（beads-studio 设 4 色产 11 色 + `illegal char "B"` 报障链）。**本节零实现描述变更、零 §3 冻结值变更**；正本 = `design/proposals/sec3-color-max-35-and-mis-max.md`，§3 侧登记 = `systems-index-changelog.md` **v1.54（提案·未生效）**，关卡侧 = `levels-spec.md` **v1.10** | 用户 2026-09-24 两条裁定，任务号占位 `WXG-T-___` |
| v2.6 | 2026-09-23 | **取回落槽回退「自动归类」（WXG-T-204，用户裁定，推翻 v2.3 / WXG-T-168 点槽定落位、恢复 v2.1 裁定①）**：§2.3 路径 A 第 3 步落槽 = `insertGrouped` 逐颗归类（同色成块 + 紧凑无洞）；§2.2 整组收进可收数 = `min(组, tray.freeCount)`（消点槽起点口径）；§8-2 取回判据同步。**消费序（§2.2 剥皮序）与直填（§2.2 路径 B）零改动**。实现 = `judgeRetrieve` 去 `landSlot`、`tray.insertRun/freeRunFrom` 删除；同源 `tray-spawner` v2.4 / `input-control` v2.10；判据 = `retrieve-landing.test.ts`（改写 5 例） | WXG-T-204，用户 2026-09-23 裁定 |
| v1.0 | 2026-09-12 | 建档八节定稿；后续 v1.0 内回写：WXG-T-061 到达序换基准（§6）、WXG-T-128 轻压改写（§4/§6/§8-4，零事件 + 极轻非惩罚反馈） | WXG-T-009 |
| v2.0 | 2026-09-16 | **「错位归位」状态机改写（WXG-T-130 第二批，用户 2026-09-16 裁定）**：① **`filled` 终态被推翻**——状态机改为 `filled(就位)【终态】` + `filled(错位) ⇄ empty` 双向（唯一回退通道 = 取回，前提托盘有空槽）；② §2.3 改双路径裁决（取回 retrieve / 归位 placed），取回 = S3/S4 同帧原子写入、`tray:stored`、不触发完成判定；③ §2.1 加 `swaps` 错位装配与 `misplaced` 判定（§3.13）；④ **与 `powerups.md`「零网格写入」判据同批镜像改写**——道具反转为解环器后 S3 接受 S6 写入请求（`powerup:used {affectedCells}`），S3 仍是网格唯一真源；⑤ §6/§8 改写：判据 1（初始装配）、2（取回，新增）、6（双向性 + 就位珠仍不可转移）、4（适用对象收窄为已就位珠）、10（空槽灰度判据范围注记，E4 降档见 `art/accessibility.md`）。裁定依据：供料关停（案 A）/ 已就位珠与锁定格静默忽略（极轻反馈，维持 G7 形态） | WXG-T-130，用户 2026-09-16 四项裁定 |


| WXG-T-157 | 2026-09-17 | **组选收窄 + board 锚直填（用户 2026-09-17 三项裁定，规格代落盘待正主复验）**：① §2.2「错位珠可取回」的**组语义收窄**——选中锚 ⇒ 组 = 8 向连通、锚起两步内（切比雪夫 ≤2）**且同色**的错位珠；异色错位珠不抬。② 新增 **board 锚直填**：锚组非空 ⇒ 点对应色空格（切比雪夫 ≤2）直接归位（搬移语义：源格空出），组保持逐颗续填、锚珠被填 ⇒ 锚静默转移；超距 ⇒ 轻提示拒绝。③ 取回（托盘路径）与直填（盘内路径）并存；「格间必经托盘」对**同色错位珠就近归位**开例外口（非 swap：直填目的地 = 对应色空格，非任意格）。实现 = `grid.collectMisplacedGroup` / `beads-game._tryDirectFillFromBoard`；判据 = `misplaced-group` / `selection-anchor`（迁移）+ `misplaced-direct-fill`（新增 6 例） | WXG-T-157，用户三项裁定 |
| v2.3 | 2026-09-18 | **取回落槽改「点槽定落位」+ 部分收纳（WXG-T-168，用户 2026-09-18 真机复测两项裁定，推翻 v2.1 裁定①）**：① §2.3 路径 A 第 2 步 —— 旧「`free` 槽数 ≥ 组大小、不足 ⇒ 整组拒」改**部分收纳**（可收数 = 起点起连续空槽数；所收 = 距锚珠最近者；余珠留格、锚改指剩余首颗可续点）；② 第 3 步落槽改**玩家点槽定落位**（被点空槽起连续相邻排布，~~自动归类~~ ⛔ 作废）；③ §8-2 判据随迁（落位 = 被点槽段、`tray:stored` 次数 = 实收颗数）。**§3 数值零改动**；实现 = `Tray.insertRun/freeRunFrom` + `judgeRetrieve(landSlot)` + `retrieveSelectedGroup(targetSlot)` + `_nearestFirst`；判据 = `retrieve-landing.test.ts`（新增 5 例）+ `misplaced-group`/`selection-anchor`（2 例迁移）；规格同源回写见 `tray-spawner` v2.3 | WXG-T-168，用户 2026-09-18 两项裁定（点槽定落位/部分收纳·就近优先） |
| v2.4 | 2026-09-20 | **board 锚直填消费序改「选豆点固定 + 同序配对」（用户 2026-09-20 裁定，任务号 WXG-T-183）**：① §2.2 组语义 —— `_boardSelected` 新增 `anchorRow/anchorCol`（拾取那一下的坐标，填珠/收纳全程不改）；旧 `row/col` 降级为展示用「当前头珠」，可随转移漂移、不参与排序。② §2.2 直填 —— 目标序 = 被点格 + BFS 由近及远，组员消费序 = 距选豆点切比雪夫升序（平局行主序），两者同序配对（~~v2.2「逐目标就近取珠」~~ ⛔ 作废）；锚珠被填 ⇒ 仅头珠转移、选豆点不改。③ §2.3 路径 A —— 部分收纳就近基准同步改选豆点。**托盘侧不适用**（托盘同色珠可互换、不在棋盘格坐标上，「离选豆点距离」只对 board 锚有意义）。**§3 数值零改动**；实现 = `beads-game._boardSelected`/`selectBoardBead`/`retrieveSelectedGroup`/`_tryDirectFillFromBoard`（src 与 cocos 镜像同步）；判据 = `misplaced-direct-fill.test.ts`（新增选豆点基准判例 1 例） | 用户 2026-09-20 裁定（选豆点固定/消费序切比雪夫/同序配对） |
| v2.5 | 2026-09-22 | **组珠消费序改「沿组连通的剥皮序」（WXG-T-186，用户 2026-09-22 裁定，推翻 v2.4 的几何口径）**：① §2.2 「最近」从距选豆点**切比雪夫**改为**图上距离**（组 = 8 向连通块，几何近 ≠ 连通近 ⇒ 旧序会跳缺口隔空取物、同圈层行主序先吃最左上珠），并新增「消费序」条定四档优先级：**不隔空（硬）> 不吃割点（软）> 同层押后有下层邻居的引路珠 > 层号↑⇒行主序↑**；级 1/2 不可兼得（实测互斥），让步固定为级 2。② 消费序**只在拾取时算一次**（不设颗数上限，一次点击只取前缀），**直填（§2.2）与取回落槽（§2.3 路径 A）共用同一份序** ⇒ 两路口径一致由构造保证（旧两处各写一份几何排序：`_nearestFirst` 欧氏² vs 内联切比雪夫）。③ 选豆点 `anchorRow/Col` 仍恒定（降为 BFS 根）。④ §8 新增判据 **12**（五不变式：不隔空 / 不吃散 / 同层引路珠押后 / 两路同序 / 确定性）。**§3 数值零改动**；实现 = `systems/consume-order.ts::planConsumeOrder`（每步一次迭代 Tarjan，O(n²)、零逐步分配；12×12 整片 144 颗 0.7ms / 324 颗 3.3ms）+ `beads-game._boardSelected.cells` → `.order`（删 `_nearestFirst`，src 与 cocos 镜像同步）；判据 = `consume-order.test.ts`（新增 7 例：U 形不跳缺口 / 3×3 任意 k 剩余连通 / 同层引路珠押后 / 单双珠 / 确定性 / 直填与取回同序集成 2 例）+ `misplaced-direct-fill.test.ts`（旧基准例注词迁移） | 用户 2026-09-22 裁定（图上距离/不限量/同层引路珠押后/两路共用一份序） |
| v2.1 | 2026-09-18 | **托盘侧组归位 + 落槽自动归类（WXG-T-158，用户 2026-09-18 四项裁定）**：① §2.3 路径 B 改写——锚 = `tray` 时归位从单珠改**同色全组批量填充**：被点格起 8 向 BFS 不限步沿「`empty` 且底色 = 组色」连通空格蔓延，至多填 `min(组珠数, 连通格数)` 格（裁定④）；**部分填充口径**（裁定③）：珠不够点到珠为止、珠有余剩余保持选中；每格一份 `bead:placed`（携来源槽）；② 路径 A 落槽改 **S4 自动归类插入**（裁定①，点击槽仅作触发）；③ §8-2/3 适配 + 新增 §8-11（批量填充硬判据）。**§3 数值零改动**；实现 = `placement.planGroupFill` / `beads-game._placeSelected`；判据 = `tray-batch-fill`（新增）。盘侧直填（T-157 ≤2 收窄）与本路径（不限步）口径**故意不同**：前者是错位珠搬移、后者是供珠填坑 | WXG-T-158，用户四项裁定 |
| v2.2 | 2026-09-18 | **盘侧组选改完整连通块 + 直填放开任意距离（WXG-T-162，用户 2026-09-18 真机裁定③④⑤，覆盖 T-157 ①②③）**：① §2.2 新增「组语义」条——组选从「锚起切比雪夫 ≤2」改为 8 向（3×3 含对角）连通同色错位珠**完整连通块、不限步数**；就位/锁定/空格/异色阻断传播。② board 锚直填删 ≤2 距离门与超距轻提示分支（任意距离）；其余口径（取最近者/组保持/锚静默转移/cleared-priority）沿用 T-157 裁定 B。③ 托盘侧组归位（v2.1）不受影响；旧注「盘侧直填与本路径口径故意不同」中距离一项随本裁定收敛（两者均不限距/不限步，仍异于搬移 vs 供珠填坑）。**§3 数值零改动**；实现 = `grid.collectMisplacedGroup` / `beads-game._tryDirectFillFromBoard`；判据 = `misplaced-group`（连通判据 5 例新增）/ `misplaced-direct-fill`（放开回归）。规格代落盘待设计域正主复验 | WXG-T-162，用户 2026-09-18 真机反馈裁定 |
