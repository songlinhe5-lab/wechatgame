# GDD · S10 元游戏与图鉴（Meta UI & Album）· beads

- 项目：`games/beads` · 版本 **v0.3（草案 · 已含六项裁定）** · 任务号 **WXG-T-167**（上位来源：`ADR-0015` §3.5 **C-4** 设计轨并行单；提案正本 `design/proposals/v2-album-dense-boards.md`）
- **状态：草案 · 待主理人与用户确认。** 已由用户 2026-09-19 拍板、本文**不重开**的前提：**Q1 = 甲**（子版面完成 = 原样复用「错位归位」核心循环，**不新增第二个玩法内核**）· **Q2 = 逐格推进 / 点格放大**（≡ 本文后文的「Q2=甲」，**一屏只见一格**，整图可作缩略进度呈现；注意：提案 §3 表里的原义「甲 = 多格同屏可见」**未被采纳**，该选项对应提案表的乙+丙合成 ⇒ 引用一律看正文不看字母，详见 §2.3-2）。另已由用户同日拍板：**Q4 = 甲**（主菜单页族内独立页；启动红线零改动）· **Q5 = 甲**（完成度纯显示 ⇒ 不发奖、不设解锁门槛、不因奖励扩档）· **Q7 = 甲**（逐格扣 ⇒ **§3.14 现文与数值零改动**）· **Q7-b = 甲**（从子版面退回整图页 = **弃本局棋盘**，与 v1.29 同构）。其余结论凡依赖 Q3/Q6/Q8–Q11 者逐条标 `[待确认]`；本文列「候选读法」只为消歧，**不构成推荐，更不等于已裁**。
- 补欠账定位：systems-index §1 S10 行自 v1.28 标「`gdd/meta-ui.md` **待建 · 归批1**」（批 0 只落 shell/路由/设置与签到 overlay/体力钱包服务）⇒ 本文 = 该正本首版；**S10 行改记由主理人落，本文不自改索引**。
- 数值纪律：**`systems-index §3` 是唯一数值真源**，本文**只引用常量名、不新增常量、不给任何未冻结的具体数字**（清单见 §5）。
- 依赖锚点：`concept.md` 三支柱 + D1/D8/D10/D11/D12/D13（Q1=甲 ⇒ **D1 与支柱 3 不改判**，提案 §5「新系统 S11」**不启用**）· `core-loop.md` §2.1/§2.2（S1 六状态与相位）· `bead-grid.md` §2.1/§2.3 · `save-progress.md` §2.2–§2.4 · `pause-settings.md`（「回主菜单」现行语义 = systems-index v1.29）· `score-combo.md` §2.1（双模式 / §3.10）· `ux-spec.md` §2/§3/§4（只读）· `ADR-0015` §3.4（相机生效面与复位）。
- **与 `ux-spec` 的分工（本单硬边界）**：三层 Screen Flow **落在本文 §4.1**，**不改 `ux-spec.md` 一个字**（该正本实测 19,587 tok、其豁免 note 要求「下次触达先评 §3 线框外移」⇒ 本单不加重）；线框几何、状态×输入矩阵行、动效毫秒表仍归 `ux-spec`，待 Q7/Q7-b 裁定后由 UX 轨决定回写（已于 v0.3 裁完 ⇒ 可由 UX 轨排期回写）。

---

## 1. 目标（为什么存在）

把关卡组织成「一张画 = 若干部面」，让玩家以**完成整图**为单位获得成品感与收藏驱动；连同主菜单页族入口规则写成可验收正本。回应用户 2026-09-18 批评（盘面珠子太少、缺连续珠、要图鉴），**不碰玩法内核**（Q1=甲）。

## 2. 机制

### 2.1 三层对象与命名（借词指代，标识符归代码）

```
Album（图鉴）    = 页族中的一页；整图的列表与收集视图，不持有玩法状态
 └─ Plate（整图）= 一幅完整拼豆画作；N 个 Cell-Level 的组织单位，完成态 = 其全部子版面完成
     └─ Cell-Level（子版面）= 一个盘面关卡 = **S1 状态机里的一局 PLAYING**（Q1=甲）
```

- **Cell-Level = 现有 LevelDef 的一个成员**，与 demo 8 关同构：同份行字符串图案（§3.2）、同套 `swaps` 构造与 BOOT 校验（§3.13）、同份计时（§3.5）、同套星级/结算/解锁（§3.7 / S7）、同套取回-归位裁决（`bead-grid §2.3`）。**本系统对这些零改写、零读写。**
- **不新增完成判定**：整图「完成」= 其 Cell-Level 的 `level:cleared` 已满，是**派生量**，不得成第二真源（S8 §2.3「可派生的值不入档」+ L5）。
- **命名纪律**：Album/Plate/Cell-Level 是**设计语义词**；代码标识符、JSON 字段名、存档键名归代码（D9，命名空间 `wxgame.beads.*`）。

### 2.2 整图 / 子版面数据结构（提案形态，**不含数值**）

```
Plate : { plateId, cellRefs[]（有序，指向现有 LevelDef）,
          宫格形态 = [待确认 · Q1/Q2/Q3], 图案来源(独立/切块/混合) = [待确认 · Q11] }
Album : { plateRefs[] }
完成态: completed(plate) = cellRefs 全部 completed（派生，不落档）
显示量: 整图进度 = completedCount / totalCells（**Q5 = 甲** ⇒ 纯显示；不参与结算 / 解锁）
```

- 进度归属（**Q5 = 甲** ⇒ 只读派生，**不因发奖/解锁扩档**）：现码两条档线——**S8 主档**（`unlockedLevel`/`stars[]`/settings）与 **meta sidecar**（体力/币/签到/购心记账，**无收集字段**）。收集进度语义属「进度」（S8 域）不属「钱包」（§3.14 域）。**本文只提案归属，键名与 schema 归代码（D9）。**
- **关卡数据链前置**：内容正本仍是 `design/levels/*.json` → 生成 `levels-data.ts`（ADR-0004 行字符串形态），校验器在 `config/levels.ts`（报错格式 `levels-spec §8`）。新增「图→格→关卡」映射 ⇒ 校验器扩面，**属 §10 CH-5；本文不动数据、不动代码。**

### 2.3 Q1=甲 / Q2=甲 的两条硬后果（钉死，防后文漂）

1. **Q1=甲** ⇒ `concept` D1「忠实复刻不加戏」与支柱 3 维持原判据；S1/S3 判据面**不翻倍**；提案 §5「新系统 S11（若 Q1=新玩法）」及「S1 不再唯一内核」的拓扑重排**全部不启用**。本系统是**内容组织 + 页族**问题，不是玩法问题。
2. **Q2=甲** ⇒ 第二层（整图页）**只呈现一格 + 缩略进度**，一屏之内**不存在 N 个独立可缩放版面** ⇒ `ADR-0015` **丁-3 单相机**（一份相机配一张网格）前提成立 ⇒ §5-3「图鉴立项即复评丁-2 分组变换通道」的触发条件**不成立**，**不预付通用性**。
   - 语义消歧（K-053 ①）：本文「Q2=甲」 = 用户选项正文「**逐格推进 / 点格放大**」（提案表乙+丙合成）；提案原义「甲 = 多格同屏可见」未被采纳。

### 2.4 页面流与门禁（机制层，图形见 §4.1）

- **启动路径零改动**：图鉴不是启动落地页。首屏即玩红线维持（`ux-spec §1` 原则 1 / §6.3 三路径；`concept` v1.4 主菜单 = 非启动落地页）。
- 进图鉴的唯一通道 = 主菜单（菜单经暂停次钮进入，≤2 次交互）。**Q4 = 甲（已裁）**：主菜单页族内独立页；「取代关卡选择」与「双入口」两向**不采纳** ⇒ `ux-spec §2/§6.3` 启动路径与留存策略**零改动**。
- 页族内输入**全部属区外**（面板语义）：按下帧即响应、零玩法事件、面板外零响应（`ux-spec §4` 表尾行 + `input-control §2.3` 镜像）。
- 相机生效面：第一/第二层属 meta 页族 ⇒ **不进相机**（`ADR-0015 §3.4`，用户已裁「页族含图鉴不缩放」）；第三层 = 玩法屏 ⇒ 棋盘区缩放/平移适用，复位仍只有 `_setupLevel`/`_loadStage` 两点，复位档 = **`fit` 初始**（非恒等，WXG-T-172 甲裁）。
- 局→页回跳（**Q7-b = 甲 已裁**）：第三层→第二层退回 = **弃本局棋盘**（关卡/格完成态不丢，本局未归位珠不保留），与 v1.29「回主菜单 = 弃本局棋盘」**同构、不新增语义**；回到第二层后再进本格 = **一次新局**，按 Q7 口径扣 1 心。结算面板出口从「下一关」改为「回整图页 + 该格落位」（形态）；**按钮组具体排布与推进顺序仍归 `ux-spec` 轨**。~~「保留在途可续」与「禁止退回」两向未被采纳~~。

### 2.5 体力扣减口径（**Q7 = 甲 已裁**；本文不改 §3.14 现文与数值）

§3.14 现文 = `STAMINA_START_COST` 1，语义「**普通局**开局消耗；第二模式入口当前不存在，其余模式消耗不冻结」。**裁定 = 读法一（逐格扣）**：Cell-Level 就是一局 ⇒ 进一格 = 一次新局 = 扣 1 心 ⇒ **§3.14 现文与数值零改动、不走反转批**。下表读法二/三为当时候选读法，**未被采纳**（留档以免后人重答）。**诚实登记的已知后果（非数值承诺）**：单图预算 = 格数 × 1 心，而 `STAMINA_MAX=8` / 8 min-心 ⇒ **玩到半张图就会碰 0 心门**，得靠等待或回满广告；该挫败面列为 **playtest 首要观察项**（若坐实，回调途径 = §3.14 语义变更单，而不是本文自行改口径）。

| 读法 | 语义 | 连带面（诚实清单） |
|---|---|---|
| ✅ **一 · 逐格扣（已裁）** | Cell-Level 就是一局 ⇒ 每次进格扣 1 | §3.14 **现文零改动**（新局集合不变）；单图体力预算 = 格数 × 现值，**半图即碰 0 心门列为 playtest 首要观察项** |
| 二 · 整图扣（未采纳） | 进 Plate 扣 1，格间跳转不扣 | **数值不动、行为语义变** ⇒ K-053 反转批（同 v1.29「新局集合扩大」判例）：「普通局」定义须重写；S9「开始游戏=全新开当前关扣 1 心」、GAME_OVER 重试扣心、0 心闸门三处判据的**挂载位置**从格上移到图上；一次进图即预付多局成本，挫败集中 |
| 三 · 逐格扣 + 整图发奖（未采纳） | 一 + 完成整图给奖励 | 发奖出口须对齐 §3.14 三条获取源（签到币/周榜结算/通关首通）：**整图完成算不算第 4 类源**须一并答；额度 `[待确认]`，本文不给 |

**Q7-b（本单新登记，非提案 Q 号）= 甲 已裁**：从 Cell-Level **退回整图页** = **弃本局棋盘**，与 v1.29「回主菜单 = 弃本局棋盘」**同口径**（关卡解锁与各格完成态不受影响；重进本格按 Q7 再扣 1 心）⇒ §2.4 回跳通道与 `pause-settings §8-11` 系判据的适用范围**不再悬空**（退回与回主菜单归同一类语义，不新增第三种）。

### 2.6 与 demo 8 关、与 v1.23 收藏册的关系（均 `[待确认]`）

- **Q8**：甲 并存（8 关日常 + 图鉴长线）／乙 替代（8 关重组为首张图）／丙 分层（图鉴为**独立模式**，如冲刺的模式标志）。连带面：§3.7 `DEMO_LEVEL_COUNT=8`、`core-loop §2.1` FINISH「8 关全清」、S8 `stars[]` 长度与 `unlockedLevel` 上界（现**硬绑** `DEMO_LEVEL_COUNT`）、`ux-spec §6.3` 落地关语义。**若裁丙**：S1 六状态可复用（sprint 已证「模式标志零改动」先例，`score-combo §2.1`），但 §3.14「第二模式入口当前不存在」即刻过期 ⇒ 须文本变更（§10 CH-4b），FINISH / 启动分流需分模式口径。
- **Q9**：`concept §7` Should 行有「收藏册（8 格相册 + 集齐彩蛋占位，v1.23 列入）」，`§4` `meta:overlay` 值域含 `collection`。甲 图鉴 = 收藏册升级 ⇒ 须回写 `concept §7` 该行，**防两套收集语义并存**；乙 并存（收藏册记单图、图鉴记整图）⇒ 须先定去重规则。本文**不预设**。

## 3. 输入（来自玩家 / 其他系统）

| 来源 | 内容 | 说明 |
|---|---|---|
| 玩家 | 菜单/页族内的点击（进图鉴、选整图、进格、返回、页族内滚动与切换） | 全部属**区外**按下帧语义；本系统**不接受**棋盘区/托盘区任何输入 |
| S1 / S3 | `level:cleared {levelId, remaining, ratio, stars}`（§4） | 本系统唯一的「落位」依据；Cell-Level 完成 = 该 levelId 已 cleared（口径待 Q5） |
| S8 | 进度读档（`unlockedLevel` / `stars[]`）+（若 Q5 需扩档）整图完成集合 | 只读快照进入页族呈现；写回走 S8 §2.5 时机 |
| S9 | 「回主菜单」意图与暂停面板门禁 | 现行语义真源 = v1.29 / `pause-settings §2.2` |
| S7 | 星级与结算分（§3.7 / §3.10 C7 `SETTLE_SCORE`） | 本系统**不改**任何结算公式，只消费结果做收集呈现 |

## 4. 输出与反馈

### 4.1 三层 Screen Flow（ASCII，本单落本文而非 `ux-spec`，理由见文首分工）

```
        点击小程序图标 ── BOOT ──► 玩法·普通 L1（runs==0）/ unlockedLevel 最远关（runs>0）
        （启动直进玩法 = 首屏即玩红线，本系统零扰动；ux-spec §1 原则1 / §6.3）
                                     │ 齿轮 → 暂停面板次钮「回主菜单」（弃本局棋盘，v1.29）
                                     ▼
     ┌──────────────── 主菜单（shell 屏 · 非启动落地页）────────────────┐
     │ 设置 / 签到（批0 已落码）  图鉴（本系统拟建页 · Q4 入口形态待裁）│
     │ 主钮「开始游戏」恒 = 全新开当前关 + 扣 1 心（§3.14）             │
     └──────────────────────────────┬───────────────────────────────────┘
                                    ▼
     【第一层】图鉴 Album ── 整图列表（缩略图 + 每张整图完成度呈现）
                                    │ 点选一张整图；**不受解锁门槛约束**（Q5 = 甲）
                                    ▼
     【第二层】整图 Plate ── 一屏只见「当前格」（Q2=甲）+ 整图缩略进度呈现
                                    │ 「进入本格」= 一次新局（体力扣减 = Q7 甲·逐格扣 1 心）
                                    │ 相机旁路（meta 页族不缩放，ADR-0015 §3.4）
                                    ▼
     【第三层】子版面 Cell-Level = S1 的一局（玩法屏）
        BOOT 校验（BEAD_CHARSET + swaps ∈ §3.13 区间）→ PLAYING 取回/归位 → 
        ├ 错位全归位 → LEVEL_CLEAR → S7 星级结算 → 该格落位 → 回【第二层】
        ├ 倒计时归零 → GAME_OVER → 续时(§3.11)/重试(扣心 §3.14) → 留第三层
        └ 退出（次钮）→ 回【第二层】/回主菜单     ← 棋盘去留 = Q7-b 甲（两者同为弃本局）
                                    │ 整图全部格落位
                                    ▼
                    整图完成 → 收入图鉴（Q5 = 甲 纯显示；**不发奖**；灰态/点亮态归 art 轨）
```

- **一屏只见一格**是第二层的硬形态（Q2=甲）：缩略整图仅作**进度呈现**，不得做成「九宫格各自可放大」——那会反翻 §2.3-2 的单相机前提。
- 整图灰态、完成格点亮态 = **美术规格**（归 art 轨另单），本文不定义色值/材质。
- 反馈：**0 文字教学**红线维持（`ux-spec §1` 原则 3 / GAP-03 裁定）；页族内按钮标签与页标题不属教学文字。

### 4.2 事件与档面输出

| 输出 | 说明 |
|---|---|
| `meta:overlay {id, open}`（§4 v1.28） | 图鉴/整图页入出栈**复用本事件**，**不新造事件名**；`id` 值域需补 `album` ⇒ 属 §4 **文本**变更（非 §3 数值），见 §10 CH-6 |
| 无玩法事件 | 第一/第二层内 `board:selected`/`tray:stored`/`bead:placed`/`bead:rejected` 计数恒 = 0（meta 域事件不带 `levelId`） |
| 顶栏心/币胶囊（`stamina:changed`/`wallet:changed`） | 页族内可见；本系统不产生扣减，只呈现既有值 |
| 触觉振动 | 页族点击是否走 `VIBRATE_DEFAULT`（§3.8）→ `[待确认]`（现状：设置行仅 `platform.isMiniGame` 显示，不伪造能力） |

## 5. 数值（全部引用，零抄写）

`systems-index §3` 的：§3.1 布局带与 `CAPSULE_AVOID`（页族避让同屏）· §3.2 `BEAD_CHARSET`/`BEAD_PALETTE`/`BEAD_COLOR_MAX` · §3.3 `GRID_MAX_COLS`/`GRID_MAX_ROWS`/`GRID_MIN_*`/pitch（**Cell-Level 仍受现值管辖，本文不解除**）· §3.4 托盘四常量 · §3.5 计时与失败唯一条件 · §3.7 `STAR*_RATIO`/`DEMO_LEVEL_COUNT`/线性解锁/主题解锁（D12）· §3.8 `TOUCH_MIN`、闪烁 ≤3Hz、字号 floor · §3.9 包体（CH-1 连带）· §3.11 续时（第三层出口）· §3.13 `MISPLACED_PAIRS_MIN/MAX`/`cycleProfile`（Q10）· §3.14 体力钱包四常量 + 三条获取源（Q7）。§3.10 冲刺与本系统无交集（除非 Q8 裁丙并另裁）。

**本文刻意不给出的数**（全 `[待确认]`，括号内为挂靠 Q 号）：宫格数与盘面尺寸（Q1/Q2/Q3）、单格色数与密度规则（Q6）、k 区间与逐格值（Q10）、整图奖励额度（Q5-乙/Q7-三）、缩放档位与位移阈值（`ux-spec §8` U9/U10 同源）、图数与每图格数（Q8/Q11）。**理由（K-051）**：属增量型数值，纸面推算会被并行单推翻，须待缩放与生成器落码后**差分复算** + playtest 再由 §3 变更单提交。

## 6. 边界条件

- **对 S2/S3/S4/S5/S6 零读写**：盘面/托盘/计时状态不由本系统持有（L5）；「进格即开新局」全走 S1 既有 BOOT→PLAYING，**不得**为图鉴开绕过 BOOT 校验的装载路径（`core-loop §2.1` 不允许带病进 PLAYING）。
- **缩略图 ≠ 可玩面**：第二层缩略必须纯只读呈现；点击缩略格「跳关」的合法性 `[待确认]`（牵 §3.7 线性解锁；**Q5 = 甲 已排除「图鉴另立门槛」一解**，故若允许跳关只能复用 S8 线性解锁真源）。
- **数据不一致**：Plate 声明格集合与实际可装载 LevelDef 不符（缺格/越界/字符集外/`swaps` 越界）→ BOOT 期**拒收该图并降级**（沿 `levels-spec §8` 格式与 S8 §2.4「安全降级优先于报错」），不允许整图半可玩。
- **档面异常**：收集字段缺失/越界/来自高版本 → 按 S8 §2.4 逐字段钳正、不弃整档、不中断启动；sidecar 与主档**字段级隔离**（现码 `meta-save-schema` 降级即钳 `STAMINA_MAX` 界的同型处理）。
- **换图 / 时间跳变**：跨图跳转不得把上一局事件带进新图（每帧最多 1 条输入指令、离开 PLAYING 后请求一律忽略，`core-loop §2.2.2/§6`）；体力离线恢复依赖 `wallClock`（§3.14），本系统不自建恢复 tick。
- **非法点击**：页族遮罩外的玩法热区一律不响应；页族内无玩法热区，故不存在「误取珠/误归位」面（**Q4 = 甲** 已裁 ⇒ 双入口不采纳，本结论成立；若日后复批双入口，需同批复评 `ux-spec §4` 矩阵）。
- **范围纪律（防认知过载 / 防范围膨胀）**：本文**不**预告 FTUE、分享、排行、名片、每日图案等页族内容；它们与图鉴同属 S10 页族，各自 `[待另单]`。

## 7. 依赖

- **上游**：S1（Cell-Level 即一局，六状态机零改动）· S3/S4/S5/S6（由 S1 编排，不直连）· S7（完成事件与星级结果）· S8（进度读写与降级）· S9（暂停/回主菜单语义，v1.29 真源）· S10 体力钱包服务（批0 已落码部分）。
- **下游**：S8（**Q5 = 甲 且 Q7 = 甲 ⇒ 不扩档、不走 §3.14 变更单**）· 关卡数据与校验器（Q1/Q2/Q3/Q11 ⇒ CH-5）· `ux-spec`（**Q4/Q5/Q7/Q7-b 已裁 ⇒ 三层流可回写**，但触达时须先评 §3 线框外移）· art 轨（整图灰态/点亮态、缩放后描边随缩与否 = `ADR-0015` C-5 仍未答）· QA 五件套（本文 §8 转正后重导出）。
- **依赖拓扑不变**：S10 在 systems-index §2 中位于 S7/S8/S9 之后；**Q1=甲 ⇒ 不出现 S11、不重排拓扑**。

## 8. 验收标准（草案 · **只引用形态与既有 §8 判据，零新数值**）

> 转正前置：Q7 / Q7-b 已于 v0.3 裁定 ⇒ 7、9 两条转为可验不变式；尚余 `[待裁]` 的只有依赖 **Q3/Q6/Q8–Q11** 的内容（本轮仍**只给形态**，不得据以导 QA 硬判据）。

1. **内核复用性**：任一 Cell-Level 装载后，`core-loop §8-1…10`、`bead-grid §8-1…11`、`tray-spawner §8`（现行未作废条目）**逐条同样成立**；本系统**不新增**完成判定或事件发射点（可测反证 = 事件名集合与 §4 逐字相等）。
2. **派生量不双真源**：整图完成度与格点亮态**不得**独立于 `level:cleared` 历史存在；构造「手改派生显示值不影响 `stars`/`cleared` 真源」用例（S8 §2.3 + L5）。
3. **页族零玩法事件**：在第一/第二层注入任意点击序列 ⇒ `board:selected`/`tray:stored`/`bead:placed`/`bead:rejected`/`powerup:used` 计数恒 = 0；`meta:overlay` 是唯一跨层可观测量。
4. **相机旁路**：第一/第二层内 drag/捏合**不产生任何** `offsetX/offsetY/zoom` 变化（形态镜像 `ADR-0015 §3.4` + `ux-spec §4` 表尾注）；第三层内复位仍只在 `_setupLevel`/`_loadStage`，复位档 = `fit` 初始（不得写成「回菜单当帧复位」，`ADR-0015 §3.4` 禁该断言）。
5. **启动红线**：`ux-spec §6.3` 三路径（首次/有进度/存档非法）与 `core-loop §8-1` 在本系统落地后**逐字不变**；任何测试不得因图鉴存在而把启动落地判为页族页。
6. **校验器拒收面**：Plate 映射缺格 / 越界字符 / `swaps` 对数 ∉ [`MISPLACED_PAIRS_MIN`, `MISPLACED_PAIRS_MAX`] / 行列 > `GRID_MAX_*` → BOOT 拒收且错误含图 id 与行列（沿 `levels-spec §8` 格式），**不崩、不中断**（联合 `core-loop §8-9`/`§8-10`、S8 §2.4）。
7. **体力形态不变式（**Q7 = 甲** 已裁）**：一格 = 一次新局 ⇒ 同一局内扣心次数恒 ≤1；每次「新局」成立的事件必伴随且仅伴随一次扣心（禁同局重复扣、禁新局不扣）；**§3.14 现文逐字不变**（可验反例：若实现把「整图进入」当唯一扣心点 ⇒ 本条红）。
8. **完成度语义一致性（**Q5 = 甲** 已裁）**：整图完成**只做呈现**——不得触发发奖、不得另立解锁门槛；结算与解锁判据须与 §3.7 / S8 线性解锁现文**逐字相同**（若日后复批乙/丙 ⇒ 前者需与 §3.14 三条获取源逐条对账，后者门槛必须与 S8 **同源单点**，禁止页族侧另立门槛）。
9. **退回即弃本局（**Q7-b = 甲** 已裁）**：第三层→第二层退回后，再进同一格必为**全新一轮棋盘**（未归位珠不保留、错位重新 BOOT 装配）；**不得**实现「回上层续进同一局」，也**不得**因退回而重复扣心（一次退回 = 一次旧局结束 + 下次进格一次新局，各对应上第 7 条的一次扣心）。形态与 `pause-settings §8-11`（回主菜单）逐字同构 ⇒ 两者共用同一测试形状，不得另建第二套。

## 9. 变更记录

| 版本 | 日期 | 变更 | 任务 |
|---|---|---|---|
| v0.1 | 2026-09-19 | **建档（草案）**：S10 页族 + 图鉴三层对象（Album/Plate/Cell-Level）八节首版；Q1=甲 ⇒ 内核复用、不启用 S11；Q2=甲 ⇒ 单相机前提维持（`ADR-0015 §5-3` 不触发）；Q3–Q11 + Q7-b 逐条 `[待确认]`；§10 出变更单**草案文本**（未提交）。**§3 冻结值零变更、未新增常量、未改任何既有文档/代码/测试。** | WXG-T-167（上位 `ADR-0015` C-4） |
| v0.2 | 2026-09-19 | **用户同日补裁两条 + 字母消歧钉死**：**Q4 = 甲**（图鉴 = 主菜单页族内独立页；「取代选关」与「双入口」不采纳 ⇒ `ux-spec §2/§6.3` 启动路径与留存策略零改动、§6「页族内无玩法热区」结论成立）· **Q5 = 甲**（整图完成度**纯显示**：不发奖、不另立解锁门槛、不因奖励扩档 ⇒ §7 下游 S8 由「若裁乙/丙则扩档」改判为**不扩**；§8-8 从 `[待裁]` 转为可验不变式）。另：文件头明写本文「Q2=甲」指**逐格推进 / 点格放大**（提案表原义「多格同屏」未被采纳），引用看正文不看字母。**仍零 §3 变更、零新常量**；未决仅余 Q3/Q6/Q7/Q7-b/Q8/Q9/Q10/Q11。 | WXG-T-167 |
| v0.3 | 2026-09-19 | **用户补裁最后两项 + §10 外移分片**：**Q7 = 甲**（**逐格扣**：一格 = 一次新局 = 扣 1 心 ⇒ **§3.14 现文与数值零改动、不走反转批**；§2.5 转「已裁」并将读法二/三标为未采纳留档；**已知后果写进 §2.5：单图预算 = 格数、`STAMINA_MAX=8`/8 min-心 ⇒ 半图即碰 0 心门**，列为 playtest 首要观察项，回调途径 = 变更单而非本文自行改口径）· **Q7-b = 甲**（子版面退回整图页 = **弃本局棋盘**，与 v1.29 同口径 ⇒ 不新增第三种语义；新增 **§8-9** 不变式，与 `pause-settings §8-11` 共用测试形状）；§8-7 由 `[待裁]` 转正（含反例）。**另：§10 CH-1…CH-7 整表外移至 `gdd/meta-ui-changelog.md`**（v0.2 仅剩 8 tok 余量 ⇒ 分片而非申请豁免，先例 = WXG-T-062 的 `systems-index-changelog.md`），正文 §10 改为一行指针。**仍零 §3 变更、零新常量**；未决仅余 Q3/Q6/Q8/Q9/Q10/Q11。 | WXG-T-167 |

## 10. §3 变更单草案（**已外移分片**）

> 本节的 CH-1…CH-7 表已整体迁至 **`gdd/meta-ui-changelog.md`**（v0.3，动机与纪律见该文件头）。**结论未变**：本轮 `§3` 与 `§6` 一字未动、无新常量、无数值；图鉴相关数值须待 §3 变更单被拍板后方可实现。
