# GDD · systems-index §6 变更记录（拆分件）· beads

- 项目：`games/beads` · 归属：`systems-index.md` 的 **§6 变更记录**
- **拆分原因**（WXG-T-062）：本节累积到使 `systems-index.md` 命中 `ctx:check` **B 项单文件 8000 估算 tokens 硬门**（实测 **8147**）。按门禁提示「要么拆分为多文件，要么登记豁免」选**拆分**——不削弱硬门。
- **指针**：`systems-index.md` §6 现只留指引，**全部变更记录以本文件为唯一真源**；引用「systems-index §6」等价于本文件。

## 6. 变更记录

| 版本 | 日期 | 变更 | 依据 |
|---|---|---|---|
| v1.0 | 2026-09-11 | 初版：9 系统清单 + 拓扑 + §3 冻结令（含 8 项 `[待确认]` 提案值） | WXG-T-007，参考图 + concept 附录 A |
| v1.1 | 2026-09-11 | A1–A8 全部拍板，`[待确认]` 标记解除，§3 数值正式冻结。裁定：A1 扩展不跨关保留 / A5 每道具 1 次免费 / A6 三道具仅作用托盘（用户拍板）；A2 供料随机槽+满槽跳过 / A3 加权 3:1 / A4 放错无惩罚 / A7 扩展不扣星 / A8 音乐浮钮列 Should（主理人依支柱 1/3 裁定） | 用户 + 主理人确认 |
| v1.2 | 2026-09-11 | §3.8 `TOUCH_MIN` 语义澄清：88px 限 UI 控件；棋盘/托盘珠命中区=外扩 8px（66²/62²），不扩至 88（防跳格误触，重叠按格心最近命中）。数值不变，仅消歧 | S2 input-control §2.2 定稿 + 主理人裁决 |
| v1.3 | 2026-09-12 | S6 GDD 交付（WXG-T-014，文策渊）：§1 去掉 S6「待写」标记；§4 `tray:expanded` 广播方消歧为 S4 唯一。**§3 数值零改动**。待决 Q1–Q5（空作用扣次 / clearAll 是否立常量 / 连点冷却 / 广告未接入口径 / region 单步 vs 两步）未冻结，不得据以实现 | powerups.md 回传 + 主理人核对 |
| v1.4 | 2026-09-12 | **Q1–Q5 全部裁决完毕**（标记已回写 powerups.md）：Q1 空作用拒用不扣 / Q2 clearAll 由 §3.4 容量派生不立常量 / Q3 无独立冷却沿用 S2 去抖 / Q4 广告未接=MVP 角标占位零事件零扣次 / Q5 单步自动锚点，两步瞄准列 Should。**改值提案登记（未生效）**：STAR3_RATIO 0.50→0.40——暂缓理由：关卡数据未产出、3★ 廉价度待 playtest、2★ 带压缩须与 STAR2_RATIO 一并由 S7 GDD 整体设计。另：concept.md D6 错引 §3.6→§3.7 已修正。仍挂起：扩展位解锁路径、激励视频拉起逻辑（均待用户拍板） | quality-lead（Qoder L3 拒写）+ 主理人裁决 |
| v1.5 | 2026-09-12 | **STAR3_RATIO 0.50→0.40 生效**（§3.7）：剩余时间 / 关卡总时长 ≥ 40% → 3★。`STAR2_RATIO` 0.20 不动。依据 = 用户 2026-09-12 明示裁定，豁免 v1.4 登记的三条暂缓理由（逐条照录：①关卡数据未产出；②3★ 廉价度待 playtest；③2★ 带压缩须与 STAR2_RATIO 一并由 S7 GDD 整体设计）——用户知情豁免，3★ 廉价度与 2★ 带影响转 playtest 验证。登记人 = 主理人（执笔 文策渊） | WXG-T-014，用户 2026-09-12 明示裁定 |
| v1.6 | 2026-09-12 | **§1 目录维护**：S2–S5「待写」标记清除（文件早已交付，标记过期）；S7 文件名 `score-stars.md` → `score-combo.md`（A+ 路线扩容：冲刺模式连击计分并入 S7，用户 2026-09-12 拍板）+ 职责描述更新；S8/S9 标记清除（WXG-T-015 交付）。**§3 数值零改动**。WXG-T-015 六件套新常量 C1–C8 / v1.1 存档字段 / U1–U4 / MF1–MF7 均为 `[待确认]` 提案，冻结需用户拍板后主理人回写 | WXG-T-015 交付 + 主理人机械回写 |
| v1.7 | 2026-09-12 | **WXG-T-015 三十项决策点用户 2026-09-12 拍板冻结，S7 冲刺模式常量组生效**（新增 §3.10：C1–C8 + U3 伪震屏，§3 既有常量零改动）；C8 条款同步 core-loop §2.3 补记；S8 v1.1 字段 / S9 P1 / UX U1–U4 转冻结；元游戏 M4 大厅 = Won't 定案、M5 生命体力 = 永不采纳定案，meta-framework 升 v1.0，整体 v1.1 排期，MVP 只留礼盒进度条 + 置灰排行入口两钩子。登记人 = 主理人（执笔 文策渊） | WXG-T-020，用户 2026-09-12 拍板 |
| v1.8 | 2026-09-12 | **§4 机械登记 4 个冲刺事件**（`combo:up` / `combo:break` / `sprint:stage` / `sprint:ended`）：依据 = score-combo §2.5 冻结设计 + §8 判据 + core-loop §2.3 已引用 `sprint:stage`（不登记即真源自相矛盾）。payload 为登记时定稿，实现如需增删字段须走 §6 变更。§3/既有事件零改动 | 主理人机械登记（WXG-T-020 验收收尾） |
| v1.9 | 2026-09-12 | **语义澄清（无数值变更）**：① §3.10 梯级公式「色数/格数」明确为该阶**上限预算**（用于选池与校验），实际棋盘取池图案本身，供料间隔公式仍为精确注入值——工程实现暴露"公式生成 vs 池图案冻结"的字面冲突；② §3.10 C8 裁决补第三条「stage 切换**托盘清空**（新图新供料）」，消解 score-combo §6 → C8 的悬空引用；③ §3.2 `.` 措辞由「空位（待填）」改为「无图案位（不可填/不计完成/渲染为空白）」，`.` 与 `x` 运行时语义同为 `locked`、差异仅在表现层（`x` 斜纹收边渲染，levels-spec §6 图例）。**§3 全部数值零改动** | WXG-T-027 工程实现暴露 + 主理人裁定（执笔 文策渊） |
| v1.10 | 2026-09-14 | **组 C 星级×关卡整组生效**（WXG-T-054）：§3.7 `STAR3_RATIO` 0.40→**0.32**、`STAR2_RATIO` 0.20→**0.12**；`LEVEL_TIME_DEFAULT` **仍 300**（D-05：竞品 180/209s 不可对齐供料循环）。关卡覆盖 L5 `time` 280→360、L6 260→360、L7 240→380、L8 240→280（`spawnInterval` 全不动）。**D-01 交叉约束已用 `levels-01-08.json` 实参复算**（等间隔硬上限 + 3:1 期望）：现行 0.40 下 L5–L8 3★ 均不可达、L5/L6 期望不可通关 → v1.5「关卡数据未产出」豁免前提**已不成立**，本版关闭该豁免。§3.10 C7 `SETTLE_SCORE` 公式结构不改；四点采样改为 0.32/0.319/0.12/0.119。§3.7 增 T-B 预留口径（`starRemaining = remaining − reviveBonusSec`；续命局 2★ 封顶）标 **[T-B / 待确认]**，不冻结续时常量。登记人 = 主理人（执笔 文策渊） | WXG-T-054，用户 2026-09-14 明示拍板组 C |
| v1.11 | 2026-09-14 | **T-B 失败续时冻结**（WXG-T-057，spec_only）：新增 §3.11 `REVIVE_BONUS_SEC=60`、`REVIVE_MAX_PER_LEVEL=1`；布局 A（失败页唯一 live 激励位，局内 4 角标不拉起）；续时=同局续打不走整关重置；冲刺不续时；Won't=体力/强制插屏/未看完发奖。§3.7 星级口径解标冻结（`starRemaining` + `revived` 2★ 封顶）。§3.5 失败重开追加续时记账清零。§3.6 `AD_PLACEMENTS=4` **值不动**，说明改为布局 A 休眠。`STAR3_RATIO` / `STAR2_RATIO` / 关卡 `time` **零改动**。实现另排，禁止改 `src/`、禁止伪造 wx 广告 API。登记人 = 主理人（执笔 文策渊） | WXG-T-057，用户 2026-09-14 明示拍板 T-B |
| v1.12 | 2026-09-14 | **Mock 续时落地**（WXG-T-058）：§3 数值零改动。框架 `RewardedAdProvider` + Mock/Noop 注入；失败页主钮续时 / 次钮重试；`onRewarded` 后 +60s 同局续打。weapp 仍 Noop，**禁止伪造 wx 广告 API**。登记人 = 程基岩 | WXG-T-058 |
| v1.13 | 2026-09-14 | **S6 道具系统落码**（WXG-T-060）：本文 **§3 数值零改动**（`POWERUP_TYPES` / `REGION_CLEAR_SLOTS` / `RANDOM_CLEAR_COUNT` / `POWERUP_FREE_USES` / `AD_PLACEMENTS` 全部按 §3.6 原文镜像进 `tuning.ts`，并新增常量镜像测试）。落码内容：只读镜像 + 三道具 + 三计数；**点名归 S6、清槽归 S4**；`AD_PLACEMENTS=4` 仍为角标占位（布局 A 不变）。**新增实现约定**（非数值）：`powerups.md §6` 补 `region` 偶数窗口居中偏向。**连带改真**：`accessibility.md` A4 由假绿 ✅ 改为 ⚠️ 部分落地（三图标落码、文字标签因 §1.4 与 §3.1 规格冲突未落，已登记 backlog）。登记人 = 主理人 | WXG-T-060 |
| v1.14 | 2026-09-14 | **跨系统裁决基准统一**（WXG-T-061）：本文 §3 数值零改动、§4 事件表零改动。把「同帧」裁决的唯一基准定为 **`core-loop §2.2.2` 帧内执行序**（输入（段内序：状态指令 → 玩法事件）→ 连击窗 → 供料 → 计时），并补齐**玩法事件段内序**（落子回执 → 通关判定）；`tray-spawner` / `powerups` / `score-combo` / `bead-grid` / `input-control` 的 **14 处**「到达序 / 先到为准」条款换基准（outcome 不变），`pause-settings` 与 `timer-gameover` 的复述改为归口引用。**理由**：单线程固定步长下「事件到达序」不可观测（一帧内没有先后事件，只有代码执行序）⇒ 原表述不可测；WXG-T-031 已就 S5/S9 两处如此处理，本轮推广到全域并立「不得各自复述」纪律。登记人 = 主理人 | WXG-T-061 |
| v1.15 | 2026-09-14 | **三项裁定落地**（WXG-T-062，用户裁定「按推荐」）：① **道具卡几何冲突**（`assets-spec §1.4`「卡 176×150 + 卡下方标签 28」vs 本文 §3.1 带高 152）按「**改卡高、不动带位**」裁定 ⇒ 卡 **176×116**、标签 28、间隔 4（整块 148 ≤ 152），**本表 §3.1 零改动**、底部留白 48 不动；`accessibility.md` A4 转真（形状 + 文字标签）。② `powerups.md §8-4` 判据由「偏差 ≤ ±20%」改**卡方检验**（df=11、α=0.01、临界 24.725）——原容差统计上偏紧（12 槽族极大偏差期望 ≈20–24%，约一半概率误报）。③ `powerups.md §2.2` 补 `region` **偶数窗口明文**（含锚点、左 2 右 3），§6 约定改为指引。**本文 §3 数值全表零改动、§4 事件表零改动**。登记人 = 主理人 | WXG-T-062 |
| v1.16 | 2026-09-14 | **波次1 拟增常量串行落盘**（WXG-T-080/083 回传，主理人代落避 T-080/081/083 竞写）：① §3.8 新增 `EMPTY_TINT_MIX=0.35` / `EMPTY_GHOST_ALPHA=0.20`（GAP-01 空槽目标色 E1 色底 / E4 幽灵符号，可访问性 A2b，**落码归 T-085**）；② 新增 §3.12 音频节 `AUDIO_SFX_MIN_INTERVAL=0.05s` / `AUDIO_REJECT_MIN_INTERVAL=0.5s`（修正现状 reject 违约）+ 选型=程序化合成 0 KB + 三总线（`AUDIO_BUS_GAIN_*`=[TODO] 不冻结）。**§3 既有数值零改动**。**U8 泄压阀（`DECOY_COLORS_MAX` 2→0 + A′ 供料不变量 held≤demand）与 R1 甲（D1/E2 settings）因耦合 `levels.json`/`spawner.ts`/`save-schema.ts`，随 T-085 落码同步登记（v1.17）**。登记人 = 主理人 | WXG-T-080/083，用户 2026-09-14 拍板 U8=A′+D / R1=甲 |
| v1.17 | 2026-09-14 | **U8 泄压阀落码（GAP-02 首供 + GAP-06 尾部软锁）**（波次2 T-086，用户拍板 U8=A′+D）：① §3.2 `DECOY_COLORS_MAX` **2→0**（D）——关卡真源 `design/levels/levels-01-08.json` L1–L6 `decoys` 清空、`levels-data.ts` 重生、`tuning.ts` 常量同改；② **A′ 供料不变量 `held ≤ demand`**（`spawner.ts::_drawColor` 仅当 `tray.heldCount(c) < tray.neededCount(c)` 才候选色 c，无可用色则本 tick 不供）——保证托盘不会持有超需求杂色→尾部不软锁；③ **GAP-02 首供**：`Spawner.reset()` 置首供标记，入 PLAYING 后首个 `tick` 立即喂一颗（≤ 1 帧，兑现 ux-spec §6 “1.5s 珠已在托盘”承诺，取代原 6.02s 空托盘；间隔节奏不变）。`DECOY_WEIGHT` 因无杂色可抽失效但保留。**除 §3.2 DECOY_COLORS_MAX 外其余冻结常量零改动**。登记人 = 主理人（串行落） | WXG-T-086，用户 2026-09-14 拍板 U8=A′+D |
| v1.18 | 2026-09-15 | **§3.12 音频拟增常量串行落盘**（WXG-T-096 前置，依 `audio-spec §7.2` 回传清单 + `audio-events §3.2` 分档表）：新增 `AUDIO_CLIP_TOTAL=19` / `AUDIO_MAX_PER_FRAME=6`（框架默认显式冻结）/ `AUDIO_URGENT_BEAT_PERIOD=1.0s`（= `TIMER_TICK`，不新造）/ `AUDIO_URGENT_MIN_INTERVAL=0.9s`（派生：周期−0.1s 余量）/ `AUDIO_TRAYFULL_MIN_INTERVAL=1.0s`（防御档）/ `AUDIO_HEARABLE_FLASH_HZ≤3Hz`（名沿用 §7.2 原文）；另划界一行——**音色合成参数（基频/包络/波形）不入 §3**，属工程实现选择，住 `src/config/audio-voices.ts`，听感由 A05-26 `[P]` 验收。`AUDIO_SFX_MIN_INTERVAL` / `AUDIO_REJECT_MIN_INTERVAL` / 选型 / 三总线（`AUDIO_BUS_GAIN_*` 仍 `[TODO]` 不冻伪 dB）**均不动**。登记人 = 主理人（串行落） | WXG-T-096（波次4 T-094 立项，用户拍板「甲」） |
| v1.19 | 2026-09-15 | **B7 裁定包回写的 §3 侧登记（WXG-T-098，波次4；执笔 文策渊 / 林绘澄）**：**§3 全部数值零改动**（无新常量、无改值）。① §3 前言「使用约定」新增第 4 条 —— **「帧」的时基口径** = 框架 `GameLoop.fixedDt` 默认 `1/60 s`，并显式声明**不列入 §3 真源**（属框架实现事实，一处改值不致两处漂移），同时定「帧量化只允许出现在时刻轴、不允许出现在次数轴」。此条源于 **BD-27**（`tray-spawner §8-1` 已补「窗口 `t∈[0,60]` 闭区间 + 第 i 次实际时刻滞后 ≤2 帧」）；成员呈请的两个新常量 `SIM_FIXED_STEP` / `SPAWN_WINDOW_SLACK` **主理人不采**，理由即上述划界。② 连带登记三条**非 §3** 回写：`tray-spawner §8-3` **作废（v1.17 D 方案，BD-28）** ⇒ QA P26 改判 ⛔ 不可验（不记 PASS）；`ux-spec §5` 放错拒绝改**单次脉冲 + 500ms 重启门**且「≤2 次/秒」正本移回**视觉列**（**BD-29**；>3Hz 红线**不开豁免** ⇒ 现实现 10Hz 属**已知偏差**，改码另单）；`ux-spec §6.2` 首供**等价口径**（`_firstFeed` 为准据名，**BD-30**）。③ **文首版本行漂移修正**：`systems-index.md:3` 停在 v1.12（实际已 v1.18）⇒ 改为与 changelog 末行同步维护。登记人 = 主理人（串行落） | WXG-T-098（BD-25 关单前置） |
| v1.20 | 2026-09-15 | **`btn_expand` 落位（WXG-T-097 / BD-15，用户拍板「丙：改带几何」）**：两根候选杠杆实测后只有一根可行——`TRAY_COLS` 12→8 虽保住「带内居中」但会把**基础容量 12→8**（动失败阈值与手感，属玩法数值），用户明示「不要影响核心区域玩法」⇒ 不采。采**纯几何**方案：① §3.1 `TRAY_BAND` **yMax 420→450**（+30，只吃托盘带与拼图带之间 60px 空白，`PUZZLE_BAND` 480..1120 与 `gridLayoutFor()` **零改动**）；② §3.4「托盘槽定位」垂直基准由**带内居中**改**贴带上沿**（面板顶边 = `TRAY_BAND.yMax`）——居中态下每侧净空 (带高−116)/2 要 ≥ 88 需带高 292，会与 12 行关最底行格热区（481）冲突，故必须换锚定；③ §3.4 新增一行冻结 `btn_expand` 几何：视觉 **132×48**（源 `assets-spec §1.3`）/ 热区 **132×88**（源 `accessibility C1`）/ 带下沿居底 y∈[230,318]，实测与扩展态最上排槽热区净空 11px、与 `POWERUP_BAND` 净空 30px。**玩法数值零改动**：`TRAY_BASE_SLOTS`/`TRAY_EXPAND_SLOTS`/`TRAY_COLS`/`TRAY_SLOT`/`TRAY_GAP`/`SPAWN_*` 全部不动，关卡数据不动。落码附带消除一处既有隐患：托盘槽几何原先在 `beads-game._traySlotX/Y` 与 `view-model.drawTray` **各写一份**（WXG-T-062「画出的框 ≠ 点击落点」判例），本次统一收进 `tuning.ts::trayLayout()` 单一真源。登记人 = 主理人（串行落） | WXG-T-097（BD-15），用户 2026-09-15 拍板丙-A |
| v1.21 | 2026-09-16 | **§3.8 幽灵符号可见性冻结变更（WXG-T-125 / F5，独立于风格单 WXG-T-124）**：`EMPTY_TINT_MIX` **0.35→0.42**、`EMPTY_GHOST_ALPHA` **0.20→0.32**。起因：art 评审 F5——两常量真机上空槽目标色/幽灵符号几乎不可读，削弱「同色入格」第一道解锁（TC-PER-01 可感知判据）。用户拍板甲（按拟定值）。连带回写：`palette.ts` 两常量同步 + cocos 镜像；`assets-spec §1.2` E1/E4 数值与 `[v1.3·F5]` 声明闭合；测试断言全部引用常量零改动（beads 全量 251 绿 + typecheck 0）；A2b 指令流探针复验（TC-PER-01 [Probe]：全关卡空槽 E1 填充色去重数 ≥ 该关色数 + E4 ink α=0.32，2/2 过）。Cocos 产物截图肉眼比对（R1+R2 道次）仍受构建阻塞，诚实记录待真机补验。登记人 = 主理人（串行落） | WXG-T-125，用户 2026-09-16 拍板甲 |
| v1.22 | 2026-09-16 | **核心循环反转「错位归位」+ 四项用户裁定落盘（WXG-T-130 第二批，文策渊执笔）**。用户 2026-09-16 四项裁定：①**定时供料 = 关停（案 A）**——错位珠唯一供给、托盘纯缓冲；②**幽灵符号（E4）= 移除**（可访问性降级裁定·用户签字，登记正本 `art/accessibility.md` A2b/A3；E1 同批改纯色直填）；③**道具 = 反转为解环器**；④**已就位珠/锁定格 = 静默忽略（极轻反馈不弹错，维持 G7 形态）**。§3 冻结变更逐条：**(a)** §3.2 `NEEDED_WEIGHT`/`DECOY_WEIGHT` **作废标注**（供料关停，抽色无触发源）；**(b)** §3.4 `SPAWN_INTERVAL_DEFAULT` + 供料规则行 **作废标注**（替代事实：托盘初始空、唯一入槽通道 = 取回 `tray:stored`）；**(c)** §3.5 失败重开第 2 项「图案清空」→「**恢复初始错位布置**」；**(d)** §3.6 道具反转——`POWERUP_TYPES` → `['solver','solverPlus','solverRandom']`（原三类型作废）、新冻结 `SOLVER_PLUS_COUNT=3` / `SOLVER_RANDOM_COUNT=1`、`REGION_CLEAR_SLOTS`/`RANDOM_CLEAR_COUNT` 作废、道具目标「仅托盘珠」→「棋盘错位珠（托盘零读写）」；**(e)** §3.8 `EMPTY_TINT_MIX` **作废（E1 纯色直填，质感语言归 WXG-T-131）**、`EMPTY_GHOST_ALPHA` **作废（E4 移除，绘制保留为关闭开关/死路径，重开零成本）**；**(f)** §3.10 C6 梯级公式**供料间隔段作废**（stage 换图改 `swaps` 交换构造）；**(g)** §3.12 `AUDIO_TRAYFULL_MIN_INTERVAL` **作废**（`tray:full` 随供料关停）；**(h)** **新增 §3.13 错位参数**：`MISPLACED_PAIRS_MIN=1` / `MISPLACED_PAIRS_MAX=8`（区间冻结）、逐关曲线 k(n)=[1,2,2,3,4,5,6,8] 为建议值 `[待 playtest]`（判据形态沿 `SPAWN_INTERVAL_DEFAULT` 判例）、交换构造法（`swaps` 两两交换，可解性由可逆置换构造保证）、错位判定 `misplaced = filled 且 珠色≠底色`、取回前提 = 托盘有空槽（满槽禁取珠）。**§4 事件表**：`tray:spawned` / `tray:full` 作废标注；**新增 `board:selected {row,col,colorIdx}`** 与 **`tray:stored {slot,colorIdx,fromRow,fromCol}`**；`bead:placed` payload `slot` 改**可选**（解环器路径不带）；`powerup:used` payload `affectedSlots` → **`affectedCells`**（消费方 S4 → S3）。§1/§2 系统清单与依赖约束随核心循环同步改写（S6 依赖改 S3、S4 供料依赖作废）。连带 GDD/UX/美术修订见各文 §9/§ 变更记录（core-loop v2.0 / bead-grid v2.0 / input-control v2.0 / tray-spawner v2.0 / timer-gameover v1.3 / score-combo v1.1 / powerups v1.3 / levels-spec v1.2 / ux-spec v1.4 / accessibility v1.5）。登记人 = 主理人（授权落盘，执笔 文策渊） | WXG-T-130，用户 2026-09-16 四项裁定（任务单 `production/TASKS-DETAIL.md ## WXG-T-130`） |
| v1.23 | 2026-09-17 | **「大胆重制」裁定落盘（WXG-T-138，提案正本 `proposals/v1.23-bold-remake.md`；grill-me 两轮 16 问，用户全部按推荐拍板 2026-09-16）**。核心诊断：v1.22 后供料关停 + 托盘 12 槽 ≫ k≤8 + 恒定 300s ⇒ 支柱 2「槽位压力」事实死亡，失败页/续时位/星级区分度连坐悬空。**诚实修正存档（提案 §5）**：托盘 2 槽即可滚窗解任意置换 ⇒ 容量是**手感旋钮**非难度闸门，难度全押 k × 环长分布。冻结变更逐条：**(a)** §3.4 `TRAY_BASE_SLOTS` **12→6**、`TRAY_EXPAND_SLOTS` **12→6**（对 concept 附录 A1 的二次变更；几何常量不动，面板视觉归 WXG-T-131）；**(b)** §3.5 合法区间 [180,420] → **[120,420]** + 新冻结 `LEVEL_TIME_OVERRIDE 公式 = clamp(k × 45 s, 120, 420)`（时间按 k 定价；levels JSON 逐关值走数据侧复核；星级比例先不动待 playtest）；**(c)** §3.7 新增**主题解锁语义**行（累计★解锁基础主题 / 高级主题走视频——对「星级只记录不做门槛」的正式变更，demo 只落解锁判定 + 一套换肤占位，阈值 `[待 playtest]`）；**(d)** §3.10 新增 `SPRINT_K_CURVE(stage) = min(2+⌊stage/2⌋, MISPLACED_PAIRS_MAX)`（`[待 playtest]`；供料关停后冲刺唯一难度旋钮 = 读图速度）；**(e)** §3.13 新增**环长分布 `cycleProfile`** 行（levels JSON 建议值不冻结；同色共享错位留生成器二期 mode）；**(f)** §3.8 `EMPTY_GHOST_ALPHA` 由 v1.22 作废态**转正为设置开关**（默认关、E4 绘制由设置项控制、值 0.32 恢复有效）。**非 §3 连带**：input-control v2.1（`swap` 格间直换由 Should 改**裁定永不实现**）；concept v1.2（支柱 2 改写「托盘工作台的手感与环谜题」、MDA/§6 去供料化、§7 范围分层随 Q15 更新——MVP 追加**归位连击**（纯感官 streak，零 HUD 数字）与**幽灵符号开关**，动画/收藏册进 Should，主题/生成器进 Could，D11/D12 入决策记录、附录 A 加「历史轨迹」声明）；§1 S4 职责行补容量变更注。**后继**（不随本单）：score-combo 增归位连击机制节、levels-spec/levels-01-08.json 按 `LEVEL_TIME_OVERRIDE` 公式复核逐关 time、art 侧主题材质与托盘 6 槽面板视觉（WXG-T-131 一并）、save-schema 增幽灵开关字段。登记人 = 主理人（WorkBuddy 会话串行落） | WXG-T-138，用户 2026-09-16 grill 会话两轮拍板（提案 `production/TASKS-DETAIL.md ## WXG-T-138`） |
| v1.24 | 2026-09-17 | **参考竞品后四项用户裁定落盘（WXG-T-141，主理人执笔）**。素材：第三方拼豆游戏高清录屏分析（`design/references/ref-video-2026-09-17-ui-ux-analysis.md` §10）。①**§3.4 `TRAY_BASE_SLOTS` 6→24**（固定 **2 行 × 12**）与 **`TRAY_EXPAND_SLOTS` 6→24**（**+2 行 × 12**，扩展后 48）——用户 2026-09-17 拍板，**推翻 v1.23 Q9 的 6 槽方案**（对 concept 附录 A1 的三次变更）；诚实记录：容量是手感旋钮非难度闸门，v1.23「小托盘滚窗」论述作废，24 槽 = 从容批量取回，难度仍全押 k × 环长（§3.13）；**4 行态带高/`btn_expand` 热区需整体重验**（旧 y 值仅适用 1 行态，重验前不作 QA 判据）。②**HUD 不设「更多游戏」入口**（用户裁定「暂时不要」）。③**棋盘缩放只做双指捏合手势**（若引入，Should 未冻结；无滑轨/按钮控件）——落 `input-control` v2.2 多点触控条款 + `ux-spec` v1.6。④**失败页语义确认**：倒计时归零 → 看广告续时（§3.11）或退出关卡，与现稿一致零变更。连带修订：`tray-spawner` v2.1（2 实线行 + 2 虚线行）、`concept` v1.3（支柱 2/MDA/MVP/D13/附录）、`ux-spec` v1.6（头注补记 v1.5 漏升版）。**零 §3.5/§3.7/§3.10/§3.13 变更** | WXG-T-141，用户 2026-09-17 四项裁定（任务单 `production/TASKS-DETAIL.md ## WXG-T-141`） |
| v1.25 | 2026-09-17 | **§3.11 `REVIVE_BONUS_SEC` 60→180**（WXG-T-149）：用户 2026-09-17 拍板，对齐参考视频失败挽留实测 +180s 量级（`ref-video-2026-09-17` §10.5）；原 60s 为 v1.11（WXG-T-057）初版值，timer-gameover 曾自标「值得复核」至此闭环。连带：`tuning.ts` 落码（src 与 cocos 镜像同步）；`ref-video` §10.7 勘误（关卡时长方向存疑，见该文件 v1.1 勘误注）。§3.7 星级口径零变更（`starRemaining` 扣减按常量自动放大，`revived` 2★ 封顶不变）。`proposals/ads-revive.md` 为 2026-09-14 历史推导记录，不改 | WXG-T-149，用户 2026-09-17 拍板 |
| v1.26 | 2026-09-17 | **§3.12 `AUDIO_CLIP_TOTAL` 19→20（新增 `sfx_denied` 不可填格轻压）**（WXG-T-152 原子批）：执行 WXG-T-128 用户裁定 3（2026-09-16 拍板「新增第 20 个音频剪辑」，非零音效非复用），规格真源 `art/assets-spec.md §1.6.7` + `ux-spec §5`「不可填格轻压」行。**六项同批（A05-24 活测试禁拆，拆开当场红）**：① `audio-events.md §1` 增 `sfx_denied` 行（触发源 = 裁决旁路 `verdict=ignored` 且 `reason∈{occupied,locked}`，零总线事件）② 本表 19→20 + 总线行 20 clip ③ `tuning.ts` `AUDIO_CLIP_DENIED` + `AUDIO_CLIP_TOTAL=20` ④ `audio-voices.ts` 音色配方（非冻结，程序化合成 0 KB）⑤ `audio-dispatch.test.ts` `SPEC_CLIPS` +cap 120 ⑥ `audio-events §3.2` 限流 0.25 s（= 视觉同格重启门 `DENIED_PRESS_RESTART_GATE_MS` 250ms 换算，不新造）。同批落码 G7 轻压动效本体（game 侧多格并存计时器 + view 侧 scale 包络 + D1 静态 1px 环，零事件红线）与判据 `tests/denied-press.test.ts`（18 例）；Q-A05-5 闭环。头注版本行同步弥合 v1.25 漂移（v1.19 「与 changelog 末行同步维护」纪律）。登记人 = 主理人（串行落） | WXG-T-128 裁定 3 / WXG-T-152（任务单 `production/TASKS-DETAIL.md ## WXG-T-152`） |
| v1.27 | 2026-09-18 | **托盘同色组选 + 自动归类 + 批量填充（WXG-T-158，用户 2026-09-18 四项裁定；§3 数值零改动，仅 §4 payload/语义登记）**。裁定：**①自动归类**——取回落槽 = 系统插入同色堆尾部（`tray-spawner §2.1` 新增归类不变式），玩家点击空槽仅作触发；**②同色全组互斥选中**——点任一 holding 珠 ⇒ 该色全部 `selected`，换点他色整组换选，再点同组任一颗整组静默取消（零事件），同帧至多一色组被选（选择锚从「至多 1 槽」放宽为「至多 1 色组」）；**③部分填充**——组批量填充珠不够 ⇒ 点到珠为止，珠有余 ⇒ 剩余珠保持 `selected`；**④8 向连通不限步**——盘侧组选 T-157 的 ≤2 收窄不适用于托盘填充（BFS 沿 `empty` 且底色=组色的连通空格）。**§4 事件表变更逐条**：(a) `tray:selected` payload `{slot, colorIdx}` → **`{slot, colorIdx, count}`**（`slot` = 被点槽、`count` = 组珠数，与 `board:selected.count` 对称）；(b) `bead:placed` **语义登记（payload 形状不变）**：托盘组归位一次输入可产出**多份**逐格事件（同一调用栈串行发完，每颗携各自来源 `slot`），消费方（S7 完成判定/音频/视觉）须按逐格事件口径处理，**不得判成「一次归位多发事件」缺陷**。连带 GDD 修订：`tray-spawner` v2.2 / `bead-grid` v2.1 / `input-control` v2.3（各文 §9 同批）；QA 判据同步 `production/qa/beads/test-cases.md` TC-TRAY-06 / TC-INP-06 改写。登记人 = 主理人（串行落） | WXG-T-158，用户 2026-09-18 四项裁定（任务单 `production/TASKS-DETAIL.md ## WXG-T-158`） |
| v1.28 | 2026-09-18 | **主菜单+元游戏页族反转冻结（WXG-T-164，用户 2026-09-18 八项拍板：B 壳双对象 / 分批 0-1-2 / 本地榜+好友置灰 / 装体力+广告+钱包 / 体力宽容包 B / 钱包 A 包 / 装触觉振动 / 首启仍直进玩法）**。反转登记：ux-spec §2「主菜单有意移除」与 meta-framework **M4 大厅 Won't、M5 生命体力永不采纳**（2026-09-12 定案）均被推翻；「三货币不做」禁令以**消费闭环成立为条件**解除。§3 冻结变更：**(a)** 新 §3.14——`STAMINA_MAX=8` / `STAMINA_REGEN_MIN=8min/心`（离线恢复 f(Δt wallClock)、登录钳上限）/ `STAMINA_START_COST=1` / 回满=激励视频 `onRewarded` 第二 live 位 / 签到第 3/5 天各 1 心（溢出即弃）/ 钱包获取仅签到币·周榜结算·通关首通、消耗=买心（阶梯价不冻结 `[待 playtest]`）+头像框 / Won't=只发不耗·币购道具；**(b)** §3.8 新冻结 `VIBRATE_DEFAULT=ON`（设置可关、仅 `platform.isMiniGame` 显示该行）+ 屏震行语义钉死（禁屏幕震动反馈；§3.10 C3 scale 伪震屏属视觉动效不受管；触觉另列不混引）；**(c)** §3.11 激励主位 1→2（失败续时+体力回满）、Won't 体力移出。§1 新 **S10 元游戏（主菜单+页族）**（`gdd/meta-ui.md` 待建·批0；架构=BeadsShell 双对象、玩法冻结面零扰动）+ S9 行补回主菜单次钮（保留进度不惩罚）+震动行。§4 新增 meta 域事件 5 行（`meta:overlay` / `signin:claimed` / `stamina:changed` / `wallet:changed` / `settings:vibrate`，局外不带 levelId）。连带同批回写：ux-spec / concept §7 / meta-framework / pause-settings（见各文版本行）。§3 既有常量零改动 | WXG-T-164，用户 2026-09-18 八项拍板（任务单 `production/TASKS-DETAIL.md ## WXG-T-164`） |
| v1.29 | 2026-09-18 | **§3.14/S9「回主菜单」语义反转（WXG-T-165，真机首验用户裁定）**。用户真机反馈：「暂停面板次钮『回主菜单』→ 进主菜单 **不保留进度**」⇒ 推翻 v1.28「保留进度不惩罚/在途续进不重复扣心」。新口径：回主菜单**弃本局棋盘**（关卡解锁进度与当前关指针保留），菜单「开始游戏」恒 = `goToLevel` 复位当前关 + 扣 1 心（**新局**集合扩大，`STAMINA_START_COST=1` 数值不变，**无 §3 数值漂移**）。连带：删 `beads-game.resumeFromPause()`（唯一调用者 shell 菜单分支已移除）；同单另落「错位珠恒亮白环（T-148 反馈①）移除」——纯视觉、不触 §3。文档同批：`pause-settings.md` v1.4（§2.2 行 + §8-11 + §9）/ S9 行 / 本 changelog。代码与 455 例 beads 测试同批绿。登记人 = 主理人（Qoder 提交会话） | WXG-T-165，用户真机首验反馈 |
| v1.30 | 2026-09-18 | **§3.4 托盘容量 24→12 + 扩展 24→12（WXG-T-168，用户 2026-09-18 真机体检第二项裁定）**。用户裁定原文：「版面没有那么多槽，可以先开放一行 12 个槽，扩容再开放第二行」⇒ `TRAY_BASE_SLOTS` **24→12（1 行 × 12）**、`TRAY_EXPAND_SLOTS` **24→12（+1 行 × 12，扩展后共 24 / 2 行）**。**(a) 推翻 v1.24（WXG-T-141，用户 2026-09-17 拍板 6→24）的取值** —— v1.24「24 槽 = 从容批量取回」论述作废。**负面后果（诚实登记，不得隐去）**：空间压力回升，且与同单落码的**部分收纳**（空槽不足只收距锚最近 N 颗，余珠留格）叠加 ⇒ **部分收纳触发频率显著上升**；属用户明示取舍（扩容即解压阀）。若 playtest 显示手感受损，本值是最优先回调旋钮。(b) **同单修视觉误判（非 §3）**：`view-model.drawTray` 的 `dashed = row > 0` 把**基础第 2 行**也画成虚线 ⇒ 玩家把已开放行误读为「未解锁/虚位」（正是上一轮「以为容量只有 12」的视觉来源）；改 `row >= ceil(TRAY_BASE_SLOTS / TRAY_COLS)` ⇒ 虚线语言**只属于扩展行**。(c) 布局：面板高随行数派生（`trayLayout(rows)`，1 行 ⇒ 72），仍**贴带顶锚定**（v1.20 规则、零特判）；带高 234 未改（仍容 4 行态）。连带回写：`tuning.ts`（常量 + 注释）、`tray-spawner` v2.4（§1 容量表述 / §8-5 扩展容量）、QA `TC-TRAY-05`（扩展 12+12）；迁移测试 `tuning.test.ts`（行对齐断言）+ `misplaced-group`（满槽构造 22→10 颗）。beads **460/460 绿**、`verify` 17/17。登记人 = 主理人（CodeBuddy 提交会话） | WXG-T-168，用户 2026-09-18 真机体检裁定 |
| v1.31 | 2026-09-19 | **S10 行指针订正 + v1.29 反转的下游镜像登记（WXG-T-164 收口批；非 §3 变更、无 §3 数值漂移）**。① S10 行内 `gdd/meta-ui.md` 待建归属由「批0」改记「批1」（核实：文件确实不存在；批0 已落 shell/路由/设置与签到 overlay/体力钱包服务，GDD 正本与 ui-kit 图元层未随批，已在 `production/TASKS.md` backlog 无号挂账）。② 补记 **v1.29 菜单语义反转的漏回写点**（判例 K-053：反转批只改了 systems-index/S9/pause-settings，未跟 ux-spec 与 concept）：`ux-spec.md` **v1.11**（§2 流程图 / §3.3 面板注 / §6.3 启动路径补注 / §8 已裁定行四处旧「保留进度不惩罚·在途续进不扣心」改注为弃本局棋盘）+ `concept.md` **v1.5**（D10 行「有进度→续进在途局」作废）+ `proposals/meta-framework.md` **v1.2**（§7 分批口径订正）。代码零改动（反转实现已随 T-165 `3f79f6d` 入库）。登记人 = 主理人（Qoder 收口会话） | WXG-T-164 收口批（补 WXG-T-165 反转遗留） |
| v1.32 | 2026-09-19 | **S7 行登记「冲刺入口三处隐藏」（WXG-T-177，用户 2026-09-19 裁定「去冲刺按钮先隐藏，咱不需要这个功能」）**。变更性质 = **入口收敛，非功能删除**：结算过关页 / 通关画面 / 暂停面板行 4 三处入口**全部隐藏**，各面板仅剩单按钮并**居中**；**§3.10 冲刺常量（C1–C8）与 `sprint.ts` / `sprint-settle.ts` 实现一律保留不动**，入口收敛后冲刺模式**暂时不可达**（仅测试 / harness 注入可达）。U1 条按判例 K-053 **删划线留档**、不静默删。下游回写：`ux-spec` **v1.12**（U1 反转 + §3.3 暂停面板图行 4 + §3.4 结算面板图 + §4 矩阵 LEVEL_CLEAR/FINISH 两行）/ `pause-settings`（§2.2 去冲刺行）/ `input-control`（§2.1 路由表 + §8-8b/8c 判据原文）。**§3 数值零改动、所有毫秒与几何值一字未改**。恢复条件：按 `ux-spec §8` U1 原口径复建三处入口即可，无需回滚本行 | WXG-T-177，用户 2026-09-19 裁定（原 U1 冻结于 2026-09-12 / WXG-T-020） |
| v1.33 | 2026-09-19 | **S10 行指针改记：`gdd/meta-ui.md` 草案 v0.2 已落盘（WXG-T-167 设计轨交付物 ii；非 §3 变更、无 §3 数值漂移、零新常量）**。新建 `games/beads/design/gdd/meta-ui.md`（185 行 / 7,992 估算 tokens，B 门内达标未申请豁免），补 WXG-T-164 批 0 与 WXG-T-176 前「归批1」欠账的 GDD 半边（**ui-kit 独立图元层仍待建，批1 其余部分不变**）。用户 2026-09-19 四项裁定写入该文头注与 §2.3/§2.4/§7/§8：**Q1 = 甲**（子版面 = 复用「错位归位」内核 ⇒ 不启用 S11、`concept` D1/支柱 3 不改判）；**Q2 = 逐格推进 / 点格放大**（一屏只见一格 ⇒ `ADR-0015` 丁-3 单相机前提成立、§5-3 丁-2 复评不触发；**提案表原义「甲 = 多格同屏可见」未被采纳**，引用看正文不看字母）；**Q4 = 甲**（主菜单页族内独立页 ⇒ `ux-spec §2/§6.3` 启动路径零改动）；**Q5 = 甲**（完成度纯显示 ⇒ 不发奖、不另立解锁门槛、不因奖励扩档）。该文 §10 另出 **CH-1…CH-7 `§3` 变更单草案文本**（数值列全空、未提交）。未决仅余 Q3/Q6/Q7/Q7-b/Q8–Q11。登记人 = 主理人（Qoder）。 | WXG-T-167（上位 `ADR-0015` §3.5 C-4） |
| v1.34 | 2026-09-20 | **§3.3 盘面上限放开：`GRID_MAX_COLS/ROWS` 13/12 → 29/29**（WXG-T-180；用户 2026-09-20 盘面调研「标准盘 = 29×29 Midi」+「MVP 先做几关 29×29」，落档 `design/proposals/board-size-29-mvp.md`）。**这是 §3 数值变更**，连带三条已如实登记：① **大盘不再完整可见** —— 29×52 = 1508px ≫ 设计宽 750px，依赖缩放（ADR-0015 丁-3 已落码，无需新渲染通道）；② **静态图元 ≈8410**（原 ≈1560，5.4×）⇒ **zoom 自适应 LOD 待立项**（工程单），未落地前大盘性能**未经真机验证**；③ `WAVE_WINDOW_MIN_MS` 在 29 列下 W = max(240, 800−20×28) = **240（触及下限）** ⇒ 过关波浪由「逐列递降」退化为「全体同窗口」，观感待复核。**难度曲线**（`levels-spec §3` 的 k / time）随格数 5.4× 需重推 —— 亦未做。**测试迁移 2 例**（`tests/tuning.test.ts`）：①「最大盘居中且留边 ≥30」⇒ 改对「屏内容得下的最大盘」成立，并**新增**「29×29 显式溢出设计宽」的诚实断言；② 梯级饱和用例 index 50 → 100（原 530 已不足以触顶 841）。beads **500/500 绿**、`verify` 17/17 |
| v1.35 | 2026-09-21 | **§3.7 `DEMO_LEVEL_COUNT` 8→9（WXG-T-179 续作「一键入关」转正）**：用户拍板「beads-studio 直接导出到工程目录，打包上传时一并更新进去」⇒ studio 生成的关卡走 `ingestLevel` 写回真源 `levels-01-08.json`（追加为第 9 关，命名 `studio-<id后6位>`，`cycleProfile='short'` 交换法构造、time 按 §3.5 公式 clamp(k×45,120,420)），随包发布。连带：写死「8」的测试判据改引用常量（`levels.test` 本就引常量零改、`misplaced-assembler` `toHaveLength(8)`→`DEMO_LEVEL_COUNT` 及标题去数字化、`level-import` `toBe(9)`→`DEMO_LEVEL_COUNT+1`）；`tuning.ts` 同改并由 `framework:sync` 同步 cocos 镜像。文件名 `levels-01-08.json` 暂不改（改名会破 sync 契约「每游戏恰 1 JSON」路径引用，随下次管线演进一并处理）。登记人 = 主理人（用户拍板「直接导出到工程目录」） | WXG-T-179 续作，用户 2026-09-21 拍板 |
| v1.38 | 2026-09-21 | **§3.5 关卡时长定价 k×45 → 三维度估算（公式 v0.1）**：用户拍板「游戏时间需根据盘面豆子数量、同色豆子相邻度、豆子颜色数量三个维度估算难度，并评估可完成时间，总结公式并不断优化」。公式：`T_est = clamp(M×22.5×f_N×f_C×g_A, 120, 420)`，f_N=1+0.25·log10(N/200)（规模，次线性）、f_C=0.75+0.083·C（辨识）、g_A=1.45−0.9·A（聚集，A 低=碎花难）；D=round(100·T_est/420)。落码：`level-import.ts` 新增 `boardStats`+`estimateLevelTime`（`timeForSwaps` 退役），`draftToLevel` time 兜底改用；Studio `server.mjs` ingest 同式 JS 副本 + `misplacedCountOf`；`beads-gen` report 下发 `difficulty`；前端 stats 显示难度/预估。兼容锚：N=200/C=3/A=0.5 ⇒ 与 k×45 逐值相等，`LEVEL_TIME_PER_PAIR=45` 保留为锚不废。校准协议（不断优化机制）= `levels-spec §5.0.1`：playtest 实际时长回流，单关偏差 >25%/均值 >15% 触发系数回调，回调序 g_A→f_C→f_N→基准，版本表记账。**负面后果如实登记**：① 公式 v0.1 纯先验无 playtest 数据，系数可能整体偏；② 三端实现（level-import/server/beads-gen）同式副本，漂移风险以测试锚点夹住；③ demo 旧关 time 为 JSON 自报值不受影响，仅导入新关走公式；④ 3★ 可得性随 T_est 伸缩未复评。登记人 = 主理人（用户拍板原文见引） | WXG-T-179 续作，用户 2026-09-21 拍板 |
| v1.37 | 2026-09-21 | **§3.3 `GRID_MAX_COLS/ROWS` 29→50 + beads-gen 异形盘包围盒裁剪**：用户两条拍板——「GRID_MAX 限制到一个最大可玩，比如不能超过 50，否则体验变差」；「背景没有颜色部分不算在错位豆子、玩法不展示，减少宽高豆子数量，类似异形盘」。**现状核实**：异形盘机制本已成立——charset `.` = void 格（`grid.ts` 构造为 locked+void、不可填、`fillableCells` 仅收 colorIdx>0 不参与错位交换、渲染 `if (cell.void) continue` 不画）；本轮新增 = `beads-gen` 在背景去格/形状掩码后做**包围盒裁剪**（重赋全局 cols/rows，早于聚集/平衡/错位构造，下游全走裁后坐标；noframe 整盘可填时空转）。连带：`tuning.ts` 同改并由 `framework:sync` 同步 cocos 镜像；`tuning.test` 溢出断言引常量自动跟随；beads-studio `importBlockers` 门限文案 6–29/5–29 → 6–50/5–50。**负面后果如实登记**：① ADR-0018 裁定 29×29 触控不可玩（fit=0.393、命中 26px），50 上限远超可玩口径——本值仅是导入硬顶，玩法实际用盘仍按 ADR-0018 档位，勿以「能导入」推「可发布」；② 50×50 = 2500 格静态图元 ≈ 1.2 万（LOD 待立项）性能未真机验证；③ 裁剪改变 levelDraft 坐标系（相对源图平移），旧存档产物不回填。登记人 = 主理人（用户拍板原文见引） | WXG-T-179 续作，用户 2026-09-21 拍板 |
| v1.36 | 2026-09-21 | **§3.2 `BEAD_COLOR_MAX` 8→10（v1.1 色数扩展预留启用）**：用户拍板「允许不超过 10 色」（beads-studio 实物图纸还原用例 10 色产物被门文案误伤）。连带：`tuning.ts` 同改并由 `framework:sync` 同步 cocos 镜像；`levels.test` 两条「越上限拒收」判据反向（charset 最大索引 'A'=10 恰等于新上限，「字符越上限」分支不可构造，改正向断言 9/A 与 10 色合法）；`levels-spec §2/§7` 色数 [3,10] 与索引 9/A 启用注；`art-bible` 对齐注升级 v1.36；beads-studio `importBlockers` 门限与 `beads-gen` 合规打印同改。**负面后果如实登记**：① 难度爬梯（score-combo C6）饱和色数 8→10，高梯级难度略升未复推；② 可访问性分析（accessibility A2b v1.5-r2）基于 8 色口径，明度 6 档结论不随本变更自动复评；③ demo 旧关仍 3–8 色未变，无存档兼容风险。登记人 = 主理人（用户拍板「现在可以允许不超过 10 色」） | WXG-T-179 续作，用户 2026-09-21 拍板 |
| v1.39 | 2026-09-21 | **色板真源统一：`BEAD_PALETTE` 十色 hex 真源由 `palette.ts` 硬编码迁移至 `games/beads/art/game-10.json`，游戏 / beads-studio / beads-gen 三方同源（**§3 数值零改动**——10 色 hex 值逐字节不变，仅真源位置迁移）**。用户拍板「游戏侧和 studio 侧色板逻辑保持一致可复用，支持的色板都要在游戏侧能读取到」。落码：① 真源 = `art/<slug>.json`（16 个，含 game-10 与 artkal-s/c/m/a/r、hama/perler/mard/nabbi 等）；② `tools/scripts/sync-palettes.mjs` 生成 `src/config/palettes-data.ts`（生成物 16 色板 ≈190KB 源码级，**进主包**，4MB 红线内余量充足但如实登记）+ `--check` 门禁 `palettes:check` 入 verify STEPS（判例 K-031：副本漂移静默报绿）；③ `view/palette.ts`：`BEAD_PALETTE` 改 re-export（禁再复制 hex 副本）+ 新增注册表 `PALETTES` / `getBeadPalette(slug)`（品牌色板游戏侧可读，codes↔hex 同序）；④ `beads-gen.mjs` `GAME_PALETTE` 改读 JSON（缺失 exit 3）；⑤ beads-studio `loadGamePalette()` 改读 JSON；⑥ Cocos 镜像随 `framework:sync` 同步。连带（同日用户拍板「去掉游戏 10 色真源拦截」）：studio `importBlockers` 删色板行，非 10 色板入关时 `remapToGamePalette` 按 **CIE Lab 距离**就近落到 game-10（ingest 与 /level 两出口同口径，判据实时重算；sRGB 欧氏会把粉/灰误配丁香紫已弃用）。单测 `tests/palettes.test.ts` 3 例；`verify` **18/18 绿**（含 palettes:check）；端到端复验 artkal-s 爱心记录 /level 映射输出不变。**负面后果如实登记**：① palettes-data.ts 进主包 ≈190KB（可访问性/明度等分析数据一并带上），如后续触包体预算可改按需子集；② 游戏运行时仍只认 game-10 十色渲染，品牌色板仅供读（映射精度天花板，真粉红需扩 BEAD_PALETTE 另立任务）；③ artkal-palette 老副本 JSON 被门禁排除，待归档清理。登记人 = 主理人 | WXG-T-179 续作，用户 2026-09-21 拍板 |
| v1.40 | 2026-09-21 | **色板体系重构：删全局 10 色渲染真源，改「关卡品牌引用制」（v1.39 的 game-10.json 方案废弃）**。用户拍板：「game-10.json 游戏里面不要保留，和 studio 保持一致」「palettes-data 色值映射需压缩减包体 + 加密存储」「编译时将 games/beads/art 打包成生成物放 assets 下读取」「不要每关一份色板数据，按品牌生成」。落码：① `game-10.json` **删除**；demo 十色降级为 `levels-01-08.json` 顶层 `palette` 数据（值逐字节不变）；② 关卡只存**品牌引用**：`palette`（slug）+ `paletteCodes`（紧凑序色号 ≤10，charset 上限不变），hex 不随关卡携带——只住品牌生成物；③ `view/palette.ts` 重构：`BEAD_PALETTE/beadColor/beadEndpoints` 全局常量删除，新增 `BeadInks` + `beadInksFor(level)`（WeakMap 按关卡对象缓存，热路径零分配）+ `beadColorOf(inks,idx)/endpointOf(inks,idx)`（越界炭黑兑底）；渲染链 view-model/bead-render 线程化传参；④ levels.ts BOOT 校验品牌引用（slug 在注册表、色号可解析、成对出现）；⑤ `sync-palettes.mjs` 重写：去 game-10 依赖（15 品牌色板 2104 色），生成物**紧缩编码 + XOR 混淆自解码**（≈190KB → **36KB**，⚠️ 诚实边界：混淆非加密——解码器随包发布，防明文提取不防逆向；数据本身是公开色号）；⑥ `beads-gen`：`GAME_PALETTE` 改读 levels JSON 顶层 palette；品牌模式输出 `paletteCodes`（紧凑序色号，hex 去重同步 codes）；⑦ beads-studio：`remapToGamePalette/hex2lab/labDist/loadGamePalette` 映射链**整体删除**（品牌色直接入关，旧记录缺色号拒收提示重新生成）；ingestLevel 写回 slug+codes；前端删「游戏 10 色」下拉项；⑧ Cocos 镜像随 `framework:sync`。单测：palettes.test 重写（注册表/beadInksFor 三分支）+ 12 个测试文件适配，verify **18/18 绿**；端到端：studio 生成 → importable=True → /level 草案带 `paletteCodes: ['S66','SP8','ST5']` 品牌真值。**负面后果如实登记**：① §3.2 十色降级为关卡数据——色盲三重编码仅对 demo 色板成立，品牌色板符号跟随 clamp 兜底；② 旧 studio 记录（无 paletteCodes）不可入关，需重新生成；③ XOR 密钥在包内，仅提高提取门槛。登记人 = 主理人 | WXG-T-180，用户 2026-09-21 四次拍板 |
| v1.41 | 2026-09-21 | **难度/时长公式 v0.1 → v0.2：按「颗」定价作废，改为真引擎实测点击数定价**（§3.5 `LEVEL_TIME_OVERRIDE` + §3.13 新增「规模闸（实测硬拦）」；新增常量 `SEC_PER_TAP=3.6`，`LEVEL_TIME_PER_PAIR` 降为死值）。用户起点：「我理解这里触发点击10次左右就能过关，难度失真导致时间估算错误，这个如何修正」（17×15 全错位盘报 D100 / 420s）。**取证**（`design/forensics/diff-v02/grid.log.txt`，32 盘真实语料 + 真引擎点击埋点）：① v0.1 **32/32 盘触顶 420s** ⇒ D100 饱和、难度分零分辨力；② 隐含 s/tap 跨 **0.46–70s（150 倍）** ⇒ 同一公式既造出倒计时内不可能通关的盘（**19/32**），又造出白给 3★ 的盘（`STAR3_RATIO=0.32` + §3.11 唯一 live 激励位永不开张 = v1.23 修掉的病症复发）；③ **根因是自变量选错而非系数偏**：v0.1 按颗定价，而 T-162/T-180/T-168 之后机制按「次」批量（`collectMisplacedGroup` 8 向连通不限步、`retrieveSelectedGroup` 一次 min(组,连续空槽)、`planGroupFill` BFS 连通空格不限步、`FILL_POP_RESTART_GATE_MS=120` 门内不重启动画）⇒ 单颗珠边际时间≈0，g_A 只折 25% 而实际该折 ~92%；④ **6 个静态候选模型全部被实测否掉**（最好的 P5 `2·ceil(M/6)` 中位误差 38%/最大 87%；曾拟的 `2·ceil(M/min(12,B_mean))` 中位比值仅 0.38）⇒ 放弃拟合代理公式，改为**直接测真值**（bot 单盘 0.1–0.4s）。**落码**：① 正本 `level-import.ts measuredLevelTime(taps)` = `clamp(taps × SEC_PER_TAP, 120, 420)`；`estimateLevelTime` 降为**静态兜底**（`ceil(M/2)` taps，仅供游戏内一键导入的无引擎路径，注释标注 ±50% 误差且刻意偏高）；② 新工具 `tools/scripts/beads-bot.ts`（装盘走生产 BOOT、每步只经 `BeadsGame` 四个公开命令、包一层只计成功命令的计数器 ⇒ taps 是人类点击数的**下界**；`--patch` 回填 `report.difficulty`/`levelDraft.time`；有 blocker ⇒ `exit 4`）；③ 生成期 `beads-gen.mjs` **删公式副本**改 spawn bot，拒产时先打印完整报告再 `exit 4`；容器无引擎 ⇒ 写 `difficulty.error`，**不静默回落失真公式**；④ 入关期 `server.mjs` **删 `estimateLevelTime` + `misplacedCountOf` 两副本（−40 行）**改调 bot，`time` **只取实测值、不采信草案自报**（同 `cycleProfile` 纪律），bot 不可用 ⇒ 500 拒收；⑤ studio 前端难度行改报「真引擎实测 N 次点击｜公式 v0.2」并显示 blockers。**规模闸只用真值**：曾拟 `M ≤ 280 ∧ B_med ≥ 8` 静态闸，被已入库 10 关实测**证伪并删除**——甜心 B_med=6 而 44 taps 轻松通关、猫咪脸 B_med 同为 6 而 134 taps 超预算 ⇒ 静态量无分辨力，用作闸纯误伤 3 关。**`SEC_PER_TAP=3.6` 取值锚**（用户拍板冻结）= L1–L8 已入库关 `time/实测taps` 的**中位数**（这 8 关 time 是 playtest 校准真值、非公式产物；反推区间 3.01–5.71）。**关卡表变更（用户拍板「只回写 L9/L10，L1–L8 不动」）**：L9 `studio-4-4c47`（29×29、M=841、实测 332 taps ⇒ **1195s**，而 time 竟是 **120s** = 玩家绝无可能通关的坏盘）**从关卡表移除**；原 L10 `studio-5-8b07` 顺位为 L9（选关按数组下标存星级 ⇒ id 重编为连续），time 由触顶 420s 回写实测 **392s**（D93）。**负面后果如实登记**：① **L7 猫咪脸成为历史债**——实测 134 taps ⇒ 482s > `LEVEL_TIME_MAX`，按 v0.2 硬拦闸属「不可入关」，但现存 time=403 是 playtest 校准值 ⇒ 用户拍板保留不动，待重做（减 M 或拆盘）；② `SEC_PER_TAP` 锚在**bot taps（人类点击下界）**上，人类误点/犹豫未计入 ⇒ 实际可能偏紧，须按 §5.0.1 协议 playtest 回调；③ bot taps 依赖 `runBot` 的贪心策略（A 归位 → C 直填 → B 取回），**策略不同 taps 不同**——已实测 `allowDirectFill:false` 时 10/10 盘全数死锁（全错位初盘空格数为 0，纯批量取回造成托盘色与洞色不匹配）⇒ 直填是必需路径，taps 口径与该策略绑定，改 bot 策略须重锚 `SEC_PER_TAP`；④ 生成期硬拦让「大盘全错位」直接不可产出（如 29×29 全错位 ⇒ 332 taps），MVP 若仍需 29×29 关，须降错位密度（`--mis swaps`）或拆分盘面；⑤ 容器内 beads-gen 产出的 `report.difficulty` 只有 `error` 字段 ⇒ studio 列表显示「难度 ——（未实测）」，须本地仓生成才有难度读数。登记人 = 主理人（用户 2026-09-21 两次拍板：修正路径=生成期真引擎实测、规模闸=硬拦；`SEC_PER_TAP`=3.6、超标关=只回写 L9/L10） | WXG-T-180 续作，用户 2026-09-21 拍板 |
| v1.42 | 2026-09-22 | **§3.4 托盘丙档扩容：`TRAY_BASE_SLOTS` 12→24（2 行×12），`TRAY_EXPAND_SLOTS`=12 不变（扩展后 36 槽/3 行）**（WXG-T-203，用户 2026-09-22 拍板 D1=丙・D2=接受 v2.1 小托盘深度降格・D3=E1 矩阵先行）。**依据 = E1 两轮 bot 实测矩阵**（`proposals/tray-capacity-large-board-playability.md` §6.1/§6.2，`temp/tray-matrix{,-r2}.mjs`）：第一轮发现「连通度 ≫ 错位密度 ≫ 托盘容量」（speckle 同盘差 4.5–12×、tray 对碎花零增益）与「随机散点部分错位」反模式（逐格随机恢复破守恒且打碎大组，mis60 taps > mis100）；第二轮（整区域就位 + bs=8 + heart void）证实 reg60 部分错位下 **tray12=148–160 全超 116 taps 线（420s÷SEC_PER_TAP 3.6）、tray24=90–96 达标**（余量 22%）——托盘增益窗口 = 大色块 + 整区域就位部分错位（真实组合图宫形态）。**连带裁定**：① 32×32 全错位两轮复现 176–178 taps ⇒ **结构性不可入关**（v1.41 规模闸维持，扩容不解锁全错位大盘）；② 组合图档位（`level-content-pipeline.md` §0.3：2048@32px → 64×64 母版 → 2×2×32×32）解锁条件 = **每宫整区域就位 ≤60% 错位**，落入 levels-spec §3.1 口径；③ `GRID_MAX=50` 不改（= 单图/Plate 分界线，>50 自动切块）。**推翻关系登记**：推翻 v1.30（WXG-T-168「先开一行」）的取值但保留其版面方法论——v1.30 针对的是 4 行态（24/24 面板高 216），丙档 2 行基础态几何 v1.24 已验（panelH 126 零溢出）、3 行扩展态（panelH 180）与 `btn_expand` 热区重叠 48px **待 E2 真机重验，未验前不作 QA 判据**。**负面后果如实登记**：① v2.1「小托盘滚窗 = 操作深度所在」论述降格（用户明示取舍，`input-control §2.1` 同批复注）；② `core-loop §2.2` 空间压力句同批复注；③ 18×18 大色块全错 tray12=126 亦超线（已入库关全为部分错位未触雷，登记备查）。**落码**：`tuning.ts` 两常量 + 注释；`tuning.test.ts` 镜像断言 1 行→2 行；`trayLayout(rows)` 派生零接线；虚线判据 `row >= ceil(base/cols)` 在丙档下正确划界。登记人 = 主理人（执笔） | WXG-T-203，用户 2026-09-22 三项拍板 |
| v1.43 | 2026-09-22 | **§3.7 `DEMO_LEVEL_COUNT` 9→10（studio 入关转正第 10 关 `studio-9-19d5`，10×10 部分错位，随包发布）**。工作树在途产物（manifest.json contentVersion 1→2 + L0010 entry、`levels-data.ts` 追加数据体、`singles/L0010.json`）测试期望未同步致 4 处 FAIL，本批收口：`tuning.ts` 同改并由 `framework:sync` 同步 cocos 镜像；`tests/__fixtures__/levels-body.snapshot` 重新生成（快照落后数据体 115 行）；`manifest.json` `description`「前 8 关」旧文案一并修正；`levels` / `misplaced-assembler` / `level-import` 三测试全引常量**零改**（v1.35 拆硬编码的先例红利）；`levels:check`（BOOT 校验）已在 verify 中 PASS，数据本身合格。登记人 = 主理人（用户 2026-09-22 拍板「开始执行」收口） | L0010 入关收口（studio 产物，无独立任务号），用户 2026-09-22 拍板 |
| v1.44 | 2026-09-22 | **§3.7 `DEMO_LEVEL_COUNT` 10→8（pre-release 关卡表全量重置；同时 uid 序号位宽 4→5）**。用户拍板：「L0001-L0010 全部去掉，序号要 5 位数」「重新导入 1～8 关」。落码：① `singles/` 重建为 `L00001..L00008.json`——**内容由原 1–8 关逐字节保留**（playtest 校准资产不重做，数字 `id` 1–8 与 `order` 均未变），`manifest.contentVersion` 2→3；② **两张 studio 试验关移出**：L0009（`studio-5-8b07`，29×29 实测 1195s 触顶盘，v1.41 遗留历史债）与 L0010（`studio-9-19d5`，10×10，**本会话 v1.43 刚转正**）；③ `level-store.mjs::assignUid` `padStart(4→5)`，`level-store.test.mjs` 断言转 5 位并把「不回绕」边界改测 `L99999→L100000`；④ 规格正本 `level-content-pipeline.md` §1.2 升 v0.7。**推翻关系如实登记**：本单**推翻同日 v1.43**（9→10 转正）——v1.43 的收口动作（快照/manifest description/镜像）全部作废重做；连带 §1.2 三条不变式被破（uid 永不复用 / order 不物理重排 / 删除标 `retired`），已在 §1.2 写明为 **pre-release 一次性例外**（本仓未上架、无真实玩家存档），不得作先例扩展到上架后。**负面后果**：① 本地 demo 存档的第 9/10 关星级记录失效（关 1–8 下标与数字 id 未变 ⇒ 其余兼容）；② L0009/L0010 若日后想复用须重新走 studio 入关（含 bot 实测 time），旧 uid 号段视为作废；③ `assignUid` 位宽变更对**已存在窄号只宽容读取**（正则 `\d+` 未动），混用 4/5 位号的目录仍能正确续号。登记人 = 主理人（执笔） | WXG-T-203，用户 2026-09-22 拍板 |
| v1.45 | 2026-09-22 | **§3.3 `GRID_MAX_COLS/ROWS` 50→32 —— 语义由「导入硬顶」升为「单图/组合图（Plate）分界线 = 切块阈值」**。用户拍板：「单图上限 32 个珠子宽度，另外如果超过 32 珠子宽高，studio 需要拆成组合图」。**与同日 v1.42 的连带裁定构成推翻关系**：v1.42 曾写「`GRID_MAX=50` 不改（= 单图/Plate 分界线）」——本单把该分界线本身改到 32，50 作为分界线的定位作废（v1.37「最大可玩硬顶」定位一并废止）。**取值依据**：1024 源图 @32px/颗 = 32×32 恰为最大单图档；2048 @32px = 64×64 母版 → `k=ceil(64/32)=2` → 2×2 均分得 4 宫 32×32、**零残余**（旧 50 阀下 40×40 母版会被当合法单图直接投放，而 ADR-0018 已裁 29×29 触控不可玩 ⇒ 32 阀把它切成 4 宫 20×20）。**落码**：① `tuning.ts` 两常量 + 注释全面改写，`framework:sync` 同步 cocos 镜像；② `level-slice.mjs` `sliceBoard` 默认 `gridMax: 50 → 32`（注释「≤50 自然不切」同步改）；③ `beads-studio/server.mjs`：`isPlate = cols>32 \|\| rows>32`、`sliceBoard({gridMax: 32})`、`importBlockers` >32 判定与门限文案 6–32/5–32；④ 前端 `index.html` 提示语「>32 ⇒ 一键入关自动走组合图 Plate 切块」（推断上限 128 不变——那是解像域可推断范围，非单图上限）。BOOT 校验（`levels.ts`）纯引常量、零改自动跟随。**负面后果如实登记**：① 旧 33–50 维度的单图（含异形盘包围盒裁剪后 33×36 类，v1.37 当初放开的动机场景）从「合法单图」重分类为「须切块组合图」——studio 侧自动处理不拒收，但**手工写的 >32 关卡 JSON 现会被 BOOT 拒收**；② 难度梯级（`stageParamsFor`）饱和上限 2500 → 1024（`tuning.test` 断言引常量自动跟随，index 250 仍触顶）；③ 32×32 = 1024 格静态图元 ≈1.1 万，LOD 仍待立项、性能未真机验证；④ E1 结论不变：32×32 **全错位** 176–178 taps 超预算 ⇒ 满尺寸单图仍须 ≤60% 整区域就位；⑤ 扫描中发现两处**既存过期数值引用**（非本批引入，仅登记）：`audio-spec.md:230` 写 `GRID_MAX_COLS=13`、`save-progress.md:122` 写 `GRID_MAX_COLS×GRID_MAX_ROWS = 156`，均为 v1.34 前旧值，待各自域收口时订正。登记人 = 主理人（执笔） | WXG-T-203，用户 2026-09-22 拍板 |
| v1.46 | 2026-09-22 | **§3.7 `DEMO_LEVEL_COUNT` 8→1（pre-release 关卡表重置第二批：demo 8 关移出，关表改由 beads-studio 逐张入关重建）**。用户拍板：「我是想重置前 8 关 demo 关」+ 选项「重做成 8 关（逐张入）」。落码：① `manifest.entries` 清空、`contentVersion` 4→5，`description` 改写重置口径；② 原 `singles/L00001..L00008.json`（v1.44 逐字节保留的 playtest 校准内容）**移出工作树**（HEAD `81b08ed` 可回滚）；③ 入第一张 studio 盘 `16x16-1790078262102-3851`（trim 后 14×13 全错位，bot 实测 58 taps ⇒ `time` 209s、D50、cleared=Y、B_med=2）→ 新 `singles/L00001.json`、`order` 1、`contentVersion` 5→6；④ 本常量**随表走 = 当前关数**（先例 = v1.43 随入关 9→10），每入一张 +1，入满 8 张即回 v1.44 基线；⑤ `levels-body.snapshot` 重 bless、`framework:sync` 同步 cocos 镜像；⑥ `levels.test` / `misplaced-assembler.test` / `level-import.test` 三处全引常量零改（v1.35 先例红利），唯 `core-loop.test.ts §8-1` 的 `levelIndex toBe(2)` 隐含「出货关表 ≥ 3 关」⇒ 改喂**受控 3 关夹具**（判据不降，只去掉对关表条数的耦合；先例 = board-input-timing 的 `noAssemble + levels` 写法）。**同批带出的既存缺陷修复**（非本单引入，诊断中撞见）：`beads-gen.mjs` spawn `beads-bot.ts` 未写 `stdio` ⇒ 默认 `pipe` 吞掉 bot 实测行（M/B_med/taps/split），studio 只拿到「实测 N 次点击」一句孤话；`server.mjs` 拒产时 `rmSync(outDir)` 销毁 `pattern.json`/`raw.json` 判据 —— 两处已改（stderr 透传 + 证据留存并回传路径）。**负面后果如实登记**：① 本地 demo 存档按**下标**存星级 ⇒ 关 1 位置换成新关、关 2–8 记录失效（v1.44 那次保留了 1–8 下标语义，本单不再保留）；② uid 号段复用：新 studio 关占用 `L00001`（v1.44 已裁定重置后旧号段作废，本单沿用该例外，**不得作上架后先例**）；③ demo 8 关的 playtest 校准值（含 v1.41 登记的 L7 猫咪脸历史债）随移出而脱离关表，复用须重走 studio 入关含 bot 实测；④ 过渡期关数 1 低于 5–10 区间下限（该区间针对成品表，入满即闭合）；⑤ 号段并发风险：v1.45 由并行会话同日登记，本 v1.46 号若与他单相撞由主理人改号。登记人 = 会话执行 | WXG-T-203 续作（挂靠），用户 2026-09-22 拍板 |
| v1.47 | 2026-09-22 | **§3.5 `LEVEL_TIME_MAX` 420 → 2500（开发期验证档）—— 同时撤销 v1.41 的 L7 猫咪脸「不可入关」历史债挂账**。用户拍板：「我要求这张图能够通过错位生成，拦截需要调整下」→ 选「丙｜放宽倒计时预算」，定数「改成 2500，我先验证玩法，时间上线需要再调整」。**改档依据（逐动作探针，`temp/taps-probe.ts`）**：规模闸 `taps × SEC_PER_TAP ≤ LEVEL_TIME_MAX` 等价于「**点击硬顶 = 顶值 ÷ 3.6**」，旧顶 420 ⇒ 116 击；而全错位盘因**初始零洞**（每颗珠都要先挖后填，两遍均受 24 槽顶与连通片大小限制）实测 `taps ≈ 0.65 × 错位珠数`：31×31 大色块树图（M=828、同色相邻率 0.843、块数 18、最大块 301 —— **正是最理想的成片形态**）bot 实测 540 击 ⇒ 1944s 被拒；分解后 270 次是纯「选取」点击（`selectBoardBead` 236 + `selectTraySlot` 34）、执行击均值仅 3.07 颗。旧顶下全错位盘上限 ≈ 178 颗珠（~14×14 全填）⇒ **闸在系统性误杀大色块全错位盘**（与闸的本意相反）。**落码**：`tuning.ts` 单常量（`measuredLevelTime` 的 clamp 上界、BOOT `time` 合法区间上沿、生成期/入关期两处硬拦共用 ⇒ 零接线，`framework:sync` 同步 cocos 镜像）；`levels.ts` 校验注释改引用常量；正本 `levels-spec §5.0` 公式块改引常量并新增版本表 **v0.2.1** 行（公式与 `SEC_PER_TAP=3.6` 均不变，仅顶值）、§2 字段表/§2.2 判据表同步；§3.5 两行与 §3.13 规模闸行改写。**历史债撤销**：L7 猫咪脸 134 taps ⇒ 482s 在旧顶下「不可入关」（v1.41 登记、用户当时拍板保留不动）；新顶下 482 ≤ 2500 不再触闸，且该关已随 v1.46 关表重置移出 ⇒ 挂账撤销。**负面后果如实登记**：① **难度分 `D = round(100 × T / LEVEL_TIME_MAX)` 分母随之漂移**，同一盘 D 值缩水 ≈6×（旧 420s 顶格盘 D100 → D17）⇒ **D 不可跨档比较**，历史取证/报告里的 D 仅作当时档位记录（若需稳定难度刻度，得另裁「D 分母 = 固定基准时长」而非全局顶，属 §3.5 变更，本单不做）；② 单局最长 2500s = **41:40**，与微信小游戏短局预期相左，失败页与 §3.11 唯一 live 续时激励位在长预算下开张率下降（v1.23 当初放开预算的动因反向），大盘 3★（需剩 ≥32% ⇒ 540 击盘要在 1700s 内打完 = 3.1s/击）可得性待 playtest；③ 本值是**开发期验证档、非产品值**，上线前须按 `levels-spec §5.0.1` 协议（偏差 >25%/15%）回调，回调时同表登记新行不覆写 v0.2.1；④ 未做分档预算（小盘 420 / 大盘放宽）——需先定大盘 3★ 与激励位口径，已挂账；⑤ `formatTime` 分钟位 `padStart(2,'0')` 在 ≤99 分钟（5940s）内不破版，但 HUD 未做 >99 分钟防护，若顶值再往上抬需同批改显示。登记人 = 会话执行（用户直接定数） | WXG-T-203 续作（挂靠），用户 2026-09-22 拍板 |
| v1.48 | 2026-09-22 | **§3.7 `DEMO_LEVEL_COUNT` 1→8（v1.46 启动的关表重建已入满 8 张）**。用户拍板：「demo 8 个关卡数据已经添加完成」（在 beads-studio 逐张生成 + 一键入关）。**关表清单**（`contentVersion` 10→15，`L00001..L00008` 数字 id 1–8 连续、`order` 1–8、均 `misplaced` 型全/部分错位初盘、`swaps` 一律为空）：L00001 14×13 209s、L00002 14×13 320s、L00003 14×12 418s、L00004 28×29 1588s、L00005 26×26 1692s、L00006 24×31 1260s、L00007 20×29 1750s、L00008 16×29 857s（`time` **全来自真引擎实测**，§3.13 规模闸 v0.2 口径，无一自报）。**落码**：`tuning.ts` 单常量（三处关卡数断言全引常量 ⇒ 零改自动跟随，v1.35 先例红利）+ `levels:sync`/`framework:sync` 重跑产物与镜像 + 快照重 bless。**与同日 v1.47 的硬依赖（必须一起看）**：8 张里有 **5 张 time 超过旧顶 420s**（857–1750s）⇒ 它们能入关完全靠的是 v1.47 把 `LEVEL_TIME_MAX` 抬到 2500；**若上线前按 §5.0.1 把顶值回调到接近 420，这 5 关会反被自家 BOOT/规模闸拒收**，需同步重做（降错位密度或拆 Plate）—— 本批不隐这一手。**负面后果如实登记**：① **旧 demo 1–8 关的 playtest 校准资产已整体脱离关表**（v1.44 曾声明「内容逐字节保留」，本批不再成立）⇒ 难度曲线、星级分布、§3.13 逐关 k 曲线建议值均作废待重测；② 存档按**下标**存星级 ⇒ 旧 1–8 关记录对应到全新盘面（v1.46 已登记，本批入满后依旧成立）；③ **触控档位风险**：ADR-0018 已裁 29×29 单盘不可玩（珠 20px、命中 26px < `TOUCH_MIN`），而本批 5 张含 28×29 / 26×26 / 24×31 / 20×29 / 16×29 —— 属**开发期验证档玩法**，真机触控体验未验，不得视为已推翻 ADR-0018；④ `DEMO_LEVEL_COUNT` 仍靠手维（再入/再移出即红），已挂账待改断言为读 manifest 实数。登记人 = 会话执行 | WXG-T-203 续作（挂靠），用户 2026-09-22 拍板 |
| v1.49 | 2026-09-23 | **关表 L00004–L00008 换盘（内容级，`DEMO_LEVEL_COUNT` 不变 = 8）**。用户拍板：「32×32 关太难了，需要将前 8 关难度降到 20×20 以内」（在 beads-studio 重生成后逐张一键入关）。**新关表清单**（`contentVersion` 15→20）：L00001 14×13 209s / L00002 14×13 320s / L00003 14×12 418s（**未动**）、L00004 14×14 331s（原 28×29 1588s）、L00005 14×13 850s（原 26×26 1692s）、L00006 12×16 583s（原 24×31 1260s）、L00007 10×14 554s（原 20×29 1750s）、L00008 8×15 302s（原 16×29 857s）；`time` 仍全来自真引擎实测（§3.13 规模闸 v0.2 口径）。**无 §3 冻结常量变更**（`DEMO_LEVEL_COUNT`/`GRID_MAX`/`LEVEL_TIME_MAX` 均不动）。**连锁后果**：① **v1.48 登记的「5 张大盘超旧顶 420s ⇒ 依赖 v1.47 抬顶」风险自然解除**（现最大 time 850s）；② **ADR-0018 触控档位风险同步消除**：v1.48 那 5 张 20–29 维盘（珠 20px、命中 < `TOUCH_MIN`）已全部移出，现关表最大 14×14 ⇒ 本批不再需对 ADR-0018 的例外声明；③ v1.48 正文的关表清单行（L00004–L00008 = 28×29/26×26/24×31/20×29/16×29）**自本行起过期**（按不静默改史惯例保留原文 + 本行覆盖声明）；④ **本换盘发生于 v1.48 的 playtest 校准之前**，所以今日 §5.0.1 的 4 条回流读数（L5–L8）对应的就是**新盘**，无旧盘数据混入。**未做（挂账）**：关表 `order` 仍非单调（L3 M=79 排在 L4 M=122 前、L8 M=70 收尾），难度曲线需按 `M` 重排；`DEMO_LEVEL_COUNT` 仍手维。登记人 = 会话执行 | WXG-T-203 续作（挂靠），用户 2026-09-23 拍板 |
| v1.50 | 2026-09-23 | **三档时钟与选档即定星（§3.5 + §3.7 双章变更）—— 同时重锚时长自变量与单价**。用户 2026-09-23 逐条拍板：① 星级与时间解耦 → 采「每关三个时钟、选档即定星」（方案甲，**且每关某档开放需同时满足 (a) 本关前一档已过 ∧ (b) 上一关同档已过**）；② `LEVEL_TIME_MIN` 120 → **30**；③ 三档系数 `STAR_TIER_K = [1.00, 0.85, 0.70]`；④ **取消 `revived` 封顶 2★**；⑤ **首关豁免 (b)**（补裁，见下“推翻关系”）；⑥ `SEC_PER_TAP` **改名 `SEC_PER_STEP` 并重锚 3.6 → 1.69**，自变量由「成功点击」换为「BAC 批量动作数」。**改锚依据（八关同一玩家 playtest，`levels-spec §5.0.1`）**：先重扫六种分支尝试序，**八点上 C 先手（旧 n=4 结论）已崩**（`CAB` 最大误差 **162%**、Spearman ρ 0.37，L2 预测 55s vs 实测 143s）；`BAC`（先取回挖洞→落子→直填）同时拿下两个独立判据 —— 最大误差 **±28%**（现况 `ACB` ±39%）与难度排序 **ρ=0.95**（现况 0.83）。现况单价 3.6 的旧锚（L1–L8 旧关 `time/taps` 中位）已随 v1.46 关表重置而**悬空**，本单把它换成有读数的锚。**落码**：`tuning.ts`（`LEVEL_TIME_MIN=30`、`SEC_PER_STEP=1.69`、新 `STAR_TIER_K`/`STAR_TIER_COUNT`/`tierSecondsFor()`/`nextTierFor()`，**删** `STAR3_RATIO`/`STAR2_RATIO`（不是弃用是删，让残留引用在类型检查上红）、`computeClearStars` 签名改 `(remaining, total, tier)` 并去封顶）；`beads-game.ts`（`_tierThisLevel` 在 `_setupLevel` 由存档推导，**先于写 `_starsByLevel`**；时钟走 `tierSecondsFor`）；`level-import.ts`（`measuredLevelTime(steps)`；静态兜底 `ceil(M/2)` taps → `round(M×0.8)` steps）；`beads-bot.ts` + `g3-lib.ts`（新 `branchOrder`/`reuseAnchor` 旋钮，**默认 `'ACB'` 逐字保持现行为，回归已验**；定价路径显式传 `BAC`）；`sync-levels-data.mjs`（`assertPricing` 随字段改名：`steps`/`stepsLB`/`secPerStep`，不变式 `stepsLB ≤ steps`）；关卡 `time` 八关全改写为实测 `t_act`（209/320/418/331/850/583/554/302 → **84/143/118/98/288/205/111/84**），`pricing.source` 改 `'playtest'`。**推翻关系（不静默改史）**：本单**推翻同日前三项自身结论** —— （1）`SEC_PER_TAP=3.6` 的锚解释（v1.41「与已校准 8 关同节奏」）作废；（2）`CAB` 作为最稳顺序的 n=4 结论作废（已入 `§5.0.2` 反面教材）；（3）上一单刚落的 `pricing.actions` 语义被 `steps` 取代（旧值降为 `taps` 取证字段）。**首关豁免为何是补裁而非原案**：用户原条 (b) 按字面会使 L1 的 2★ 永不可开，而 (b) 具传递性 ⇒ **整表 2★/3★ 链在源头锁死**，本会话已按字面实现前拦下。**负面后果如实登记**：① **八关难度曲线是乱序的**（真难度按 `steps` 排 = L1 39 < L4 46 < L8 64 < L7 70 < L2 80 ≈ L3 80 < L6 102 < **L5 199**），而表序 1..8 ⇒ L4→L5 出现 98s→288s 断崖，**order 重排未做（用户未答）**；② **`1★ = 1.00 × t_act` 意味着锚点玩家在约一半的关上会失败**，而 `t_act` 是 **1 人 8 关** 样本，新手从 L1 就撞墙（多人复测/前几关放宽均未定）；③ `SEC_PER_STEP` 残差仍 ±29% ⇒ 新关头一次入表的时间**仍是粗锚**，靠 playtest 逐关覆盖；④ `LEVEL_TIME_MAX=2500` 在新单价下已**不再被任何盘命中**（clamp 与规模闸两处空转），而难度分 `D` 的分母就是它 ⇒ 八关 D 全落 2–12 已无分辨力（只裁了 D 需固定基准的方向，未落码）；⑤ 取消封顶后**看广告即可拿到所选档的满星**，1★ 档因此变成「广告→保送」的闭环（与甲裁一致，但是开张率与星值贬值需同时监控）；⑥ 存档结构零改动（因 (a) 保证不跳档 ⇒ `starsByLevel` 与「档已过」等价），但**旧存档的星数在新制下语义已变**（旧 3★ = 打得快，新 3★ = 打赢过 3★ 档），未做迁移（pre-release 可接受）；⑦ 选档无 UI（合法档唯一 ⇒ 自动推导），但选关屏**不显示“下一次会进哪档”**，玩家可能察觉不到递进（待 UX 补）。登记人 = 会话执行（用户逐条拍板） | WXG-T-203 续作（挂靠），用户 2026-09-23 拍板 |
| v1.51 | 2026-09-23 | **音频选型改判（§3.12）+ 主包内部目标抬高（§3.9）—— BGM 从运行时合成改为采样文件**。用户 2026-09-23 两条裁定：①「**主包内部目标 2000 → 4096 KB**（微信上限 4 MB）」；②「范围只 BGM，走 C 方案（文件进主包）」。**动因（真机试听三轮反馈）**：合成 BGM「太小 / 难听 / midi 味」，第三轮读码定位到根因不是谱子而是引擎能力 —— `platform/audio-synth.ts` 的包络是 `env = 1 − (rel−atk)/decay`，即 **attack→线性衰到 0 的拨奏引擎，无 sustain**；且 `wave`/`filter` 是 **voice 级不是 note 级**（一首曲子只能一种音色）、无混响、无 per-note detune ⇒ 持续音/弦乐垫/真实乐器感**在引擎能力之外**，改谱已无解（r11 长音 pad → 18 个"正在死掉的蜂鸣"是本轮踩过的坑，已登记 `bgm-main-composition.md §12`）。**为何原裁决被推翻**：ADR-0013 §2 方案丁与 `audio-spec §4.1` 否掉采样的**唯一硬否因**是「BGM ≥240 KB 破内部目标 2000 KB（实测余量仅 32 KB）」，并预留「回退前须由主理人解除内部目标冲突」—— 本单正是执行那条解除。**落点**：`§3.9` 内部目标 4096（红线合流、缓冲消失，已⚠标注）；`§3.12` 音频选型改「SFX 运行时合成 + BGM 采样文件进主包」（**SFX 19 条保留合成不动**：≤800 ms 短瞬态、零延迟、零包体、判据已绿）；`audio-events.md` **A05-25 判据反转**（原「音频文件数 = 0」→ 「SFX 侧仍零文件；BGM 文件 ≤3、总大小 ≤1536 KB；格式限 mp3/m4a/wav，iOS 不支持 ogg」）。
| v1.52 | 2026-09-23 | **二次改判：SFX 也改素材文件**（推翻 v1.51「SFX 保留运行时合成、改判范围仅 BGM」）。用户 2026-09-23「确认推翻 v1.51 的 SFX 保留合成」⇒ §3.12 音频选型再改、`audio-spec §4.1` 与 A05-25 同步二改。**产出路径已实测可用**：新增 skill `my-skills/wxgame-audio-gen`（BGM 走阿里云百炼 `fun-music-v1`：同步返回 `output.audio.url`，需 `DASHSCOPE_API_KEY` + 业务空间 ID，属计费服务；音效走本地 `jsfxr`：零网络零付费、md5 可复现）+ 两脚本 `gen-music.mjs` / `gen-sfx.mjs`；已产出 `games/beads/assets/audio/audioAssets.js`（19 条 = base64 **60.5 KB** 进主包 JS；逐条 ms/KB/是否越 §5 上限由脚本实测打印；`sfx_clear` 经 `fitDuration` 由 861 ms 压回 798 ≤ 800）。**长短分流已落地（同日晚）**：实测 40 s BGM 若走 `decodeAudioData` 会有 **≈7 MB PCM 常驻 JS** ⇒ 新增 `AudioVoice.assetFile` → 平台原生播放器通路（`_startFileLoop`，原生不可用回退合成；`suspend`/`stopAll` 两条循环容器都覆盖）。BGM 侧**尚未启用**（无文件）。<br>**接线已落地（同日晚，方案 A）**：`AudioVoice.asset` + 后端 unlock 期 `decodeAudioData`（Promise/回调双签名兼容）+ `withAssets()` 挂载；解码失败或 runtime 不支持 ⇒ **回退 notes 合成，不静音**；资产因 tsconfig/镜像只收 TS 而落在 `src/config/audioAssets.ts`（非 .js）。新增 6 条框架判据 + 3 条挂载闭合判据，`verify` 18/18。剩余真边界：weapp 真机 `decodeAudioData` 能力未取证（`[R]`）；② `gen-music.mjs` **未实测**（本会话无 key）：`format` 挂载位置、返回 url 取值路径、url 有效期三项须以首次真实响应校准；③ jsfxr 定位含 8-bit/chiptune，与 `audio-spec §1` 负面清单①相邻 —— 配方已强制只用三角波/噪声 + 低通，但**治愈度仍须真听**，且 7 条 clip 时长远低于 §5 上限（jsfxr 包络量纲所致），可能偏"硬点击"。clip 数量不变（仍 20）⇒ 本轮不触发六方原子批。登记人 = 会话执行 | WXG-T-203 续作，用户 2026-09-23 裁定 |
⚠ **本单登记时自查出的两处事实纠正**（原先写快了，实读码为准）：① `check:size` **不会自动跟随 §3.9** —— `check-bundle-size.mjs:71` 硬编码 `mainTargetKb = 2000`，且自检 **A7（L377）断言它 == 2000** ⇒ 改 §3.9 必须同批改脚本 + A7（脚本未改，故当前**文档与守卫不一致，属已知待办**）；② 脚本 L14 明写「**禁止把 4096 当目标**」⇒ 本裁定是**知情豁免**而非无冲突落地。**负面后果如实登记**：① **内部目标 = 平台红线 ⇒ 缓冲消失**，此后任何主包增量直接顶 4096 KB，`check:size` 失去"提前收紧"的意义；② **与 `ux-spec §6`「≤1.5 s BOOT，程序化绘制零加载资产」正面冲突** —— 主包 +1.44 MB 意味着冷启动下载变大（登记为 audio-spec §7.3 冲突，处置归主理人；注：**分包路线可免掉这条代价**，用户仍选 C）；③ **本仓首次引入外部二进制资产**，「demo 阶段零外部美术/音频文件」支柱正式开口子，资产落位与 `framework:sync` 镜像规则需新增；④ `InnerAudioContext` **无缝循环未证实**（`audio-spec §2.3` 池上限本就是 `[TODO]` R-3 真机项）⇒ 接缝质量不得在文档里承诺；⑤ 素材生成路径受限：本仓 `indie-game-ost-pack` 依赖 `beatra.music.generate` 云端付费服务，**本会话未配置该 MCP** ⇒ 实际可行路线是**本地离线渲染**（`ffmpeg 7.1.1` + `lame` 已在机）——好处是离线渲染不受运行时引擎限制（可做 ADSR sustain / 多泛音 / 简易混响 / 样本级无缝），代价是自建渲染脚本。**未做（不冒充交付）**：文件后端实现、分包装配、`§3.9` 阈值联动 `check:size` 的实装验证、任何音频文件入库。登记人 = 会话执行（用户 2026-09-23 两条裁定） | WXG-T-203 续作（挂靠），用户 2026-09-23 裁定 |
| v1.54 | 2026-09-24 | **§3.2 `BEAD_COLOR_MAX` 10→35 提案登记（未生效）+ §3.13 新增「最大化错位」档提案（未生效）**。⚠️ **先例形 = v1.4（提案登记）→ v1.5（生效）**；**§3.2 现值仍为 10、§3.13 现契约未改**，本行仅登记。**改判（不静默改史）**：用户 2026-09-24 裁定「放开 10 色上限（抬到品牌色板，或干脆不限制）」⇒ **推翻用户自己 v1.36（2026-09-21「允许不超过 10 色」）**；取值 35 = 当前 rowstring（每格一字符）形态的**编码天花板** `1-9`+`A-Z` = **该形态下的「不限制」**；三处对齐实测（`beads-gen.mjs:156` 35 发符 / studio `#colors max="35"` / 本常量）；**语义收窄** = 单关**实际用色数**上限（候选色板规模不受约束，v1.40 品牌引用制下 hex 不随关走）；**>35 需换编码格式 = 另案**。**命中 ADR-0004 §5 复评条件①**（原文：「`BEAD_COLOR_MAX` 需要超过 10……→ 复评字符编码扩展方案」）⇒ 需 **adr-arch** 落正式复评（并顺带订正 §4.2 现文「锁死在 16 语义内」与事实不符：现 charset 12 字符、提案后 37）。**连带三项如实登记**：① `BEAD_CHARSET` `.x1-9A` → `.x1-9A-Z`（只收大写；`X`=索引 34 vs `x`=锁定符 ⇒ 校验器不得大小写不敏感）与**全仓 4 份独立解码表**（`levels.ts` / `entities/grid.ts:70` / `misplaced-assembler.ts:72,280` / `level-derange.mjs:15`，后者自认「刻意不共用」）必须一次收干；② **包体量级（实测斜率非拍脑袋）**：`levels-data.ts` 内 `paletteCodes` 3 条=69 B、8 条=146 B ⇒ **≈15.4 B/条**（minify 后≈7–9 B），每关现 3–8 条 ⇒ 10→35 最坏 +27 条 = **≤0.42 KB/关（源码）/ 8 关满配 ≤3.3 KB**，相对 §3.9 内部目标 4096 KB **<0.08%，非拦点**；真量级项是 `palettes-data.ts` 实测 **39,728 B**（与 `BEAD_COLOR_MAX` 无关）；③ **a11y 不新增拦点**——用户 2026-09-16「暂不支持色盲」搁置裁定继续成立（E4 符号层已整层删除），`bead-visual-style-spec` A3① 实测「明度单独不足以分 10 色」⇒ 35 色只会更弱但**不改变结论性质**；属**依赖已搁置决策、非本次新造**。**§3.13 侧**：新档 `max` = 在「可填轮廓逐格匹配 + **每色珠数守恒（硬约束不可破）**」下**最大化错位格数**，`M_max = min(N, 2(N−m))`、强制就位 `max(0, 2m−N)`；可行时等价 `full`，不可行时**尽力而为而非拒产**；**唯一退化 `M_max=0 ⇔ N<2 ∨ m=N` 且合法关面不可达**（`colors≥3 ⇒ M≥4`）；Plate 域 = **逐宫**（P2b）；**不放宽**宫密度 ≤60% / 规模闸 / BOOT「错位≥1」。**负面后果如实登记**：ⓐ **本单新发现的静默缺口**——`view/palette.ts:96,107` 无品牌引用回落 demo 十色、索引越界静默兑炭黑（`BEAD_FALLBACK_HEX`），现 10 上限顺带挡住该组合，抬到 35 后**必须**新增 BOOT「索引 >10 必携 `palette`+`paletteCodes`」否则「不限制」=「不限制到全黑」；ⓑ §3.10 C6 `min(3+⌊n/2⌋, BEAD_COLOR_MAX)`（`tuning.ts:491`）高梯级不再封顶（v1.36 同类风险放大 3.5 倍）是否另冻 `SPRINT_COLORS_MAX` 待裁（推荐挂账，冲刺入口 v1.32 已隐）；ⓒ 生成端 `--minadj 20` 在 35 色候选下可满足性**未取证**（交 QA 轨，本单不发明阈值）；ⓓ 极值式为**文档内推导 + 三例手算**，**未经代码实测** ⇒ 需工程断言复核。**未生效前置** = 用户对 Q1–Q4 裁定（`balance()` 在 max 档跑不跑 / Plate 乙档去留 / C6 预算 / 翻转执行形态）+ 主理人复核 + 工程 A/B/C 批落码。**本单零代码、零关卡 JSON、零 ADR 本体改动。**正本 = `design/proposals/sec3-color-max-35-and-mis-max.md`（含粘贴级 §3.2/§3.13 新行全文与逐处施工清单）。§3.2 两行与 §3.13 新行已先加 `[提案 v1.54 待复核]` 行内注（值未翻）。登记人 = 会话执行（执笔 文策渊），**生效与否待主理人复核** | 用户 2026-09-24 两条裁定（beads-studio 设 4 色产 11 色 + `illegal char "B"` 报障链），本单无编号占位 `WXG-T-___`（⚠️ `proposals/sec3-5mm32-render-base-change.md` 已声拟 v1.53 且尚未落表 ⇒ 按 v1.46 让号判例本行号交主理人改定） |
| v1.55 | 2026-09-24 | **§3.2 两行翻值生效（批1 = A 天花板 + B 护栏）**：`BEAD_COLOR_MAX` **10→35**、`BEAD_CHARSET` **`.x1-9A` → `.x1-9A-Z`**。判例 = v1.4（提案）→v1.5（生效），**上行 v1.54 提案行保留不动**。执行形态采正本 §7 **丙**（拆两批）⇒ **本行只覆盖 A+B**；§3.13 `max`（最大化错位）档与 studio 加档 = **批2 另单、至今未落**，故 `levels-spec §2.3` / `bead-grid §8-1` 内 max 相关注**仍保持提案措辞**，不得据本行读成已生效。<br>**A 批落码（逐处）**：新增 `src/config/bead-charset.ts`（游戏侧唯一表 + `colorIndexOfChar`/`charOfColor`/`isBeadCharsetChar`，三态：`.` `x`→`null`、表内→1–35、表外→`undefined`）与 `tools/scripts/lib/bead-charset.mjs`（工具侧同源件）；`tuning.ts` 两常量与注释；`levels.ts` 三个解码函数改走同源表（**删 `==='A'` 特例**，A–Z 支置于 1–9 之后、大小写精确）；`entities/grid.ts` 内联解码与 `misplaced-assembler.ts::baseColorOfChar/isCharValid` 改调同源；**`level-derange.mjs::CHAR` 12 枚→35 枚**（正本 §5-A8 标「最危险一条」，实测漏改即 Plate >12 色静默丢色，已补 13 色往返断言）；正本未列而工程实读码补改的两处 = `beads-mvp-patterns.mjs::CHARSET`（旧作解码白名单，漏改会把 `B`–`Z` 直接抛错）与 `tests/helpers.ts::defaultSwaps`（旧对 `B`–`Z` 静默当不可填）；`beads-gen.mjs` 35 发符与 `server.mjs`/`board-render.js` 文案注释同批；`levels-data.ts` **走 `sync-levels-data.mjs` 模板重跑**（改的是 `levels-data.header.txt`，未手改产物）。`placement.ts`/`spawner.ts` 已引常量 ⇒ **零改**（spawner 系 §3.4 v1.22 死路径，未据以判活）。<br>**B 批护栏（与翻值同笔）**：**B1** = `levels.ts::validateBeadsLevel` 新增「`pattern` 最大色索引 > **demo 色板长度** ⇒ 必携 `palette`+`paletteCodes`」，阈值取 `LEVELS_DATA.palette.length` **不硬编 10**（否则本单自己就把冻结值抄进第二处）；入关期**天然继承**（`level-import.ts::draftToLevel` 本就走 `validateBeadsLevel` ⇒ **该文件零行改动**，无重复实现也无漂移风险），studio `server.mjs::importBlockers` 则需镜像（VPS 容器无 `games/` ⇒ 读 `design/levels/palette.json` 失败时回退 10），三端口径一致以避「dev 关表绿、产物红」。**B2** = `view/palette.ts` 三条原**全静默**兜底（无引用回落 demo 十色 / 未知色号 / 索引越界）各加一次性 `console.warn` + `resetPaletteFallbackWarnings()`（测试专用复位）；⚠️ 越界告警门**只在 `colorIdx > 色板长度` 时报**——首版按「未命中即报」实现，实测 `colorIdx=0`（void/locked 的常规入参）在正常渲染路径上刷屏，已收严并保持返回值行为与 v1.54 逐字一致（仍返 `BEAD_FALLBACK_HEX`）。<br>**判据**：**X1** 由「交叉断言」升级为**结构性消除**——游戏侧/工具侧各一份表，跨语言以 `readFileSync` 抽表字面量逐字符比对，并加「全仓不得再出现第二份表字面量」扫描（`tests/bead-charset.test.ts`，10 例）；**X4 判据不成立**（诚实登记）：`BEAD_COLOR_MAX=35` 恰等编码容量 ⇒ 提案设想的「以 36 色验证越上限拒收」在本编码形态下**不可构造**，已改写为「1–35 全部可解 + 第 36 索引 `charOfColor` throw + ASCII 穷举恰 35 枚可解」，并留**自我复位哨兵**（§3.2 若降回 <35 该判据立刻变硬）；**X5** = B1 红→绿两例 + 边界例（`maxIdx === demoLen` 仍可不携引用 ⇒ 现 8 关零扰动）+ 入关期同校例。既有判据翻转四处：`levels.test`（`B` 由非法变索引 11、`charOfColor(11)` 由 throw 变合法）、`misplaced-assembler.test`/`core-loop.test`（非法字符样本 `B`/`Z` → 小写 `b`/`z`，判据面移到「charset 之外的大小写敏感面」）。计数：beads vitest **568→579 全绿**、`node --test tools/scripts/*.test.mjs` **37/37**、`tsc --noEmit` 干净。<br>**两处事实订正（不静默改史）**：① 正本 §2.1/§3.6 与 `levels-spec`/`bead-grid` 行内注皆写「`X`=索引 **34**」⇒ 实测为 **33**（`A`=10，`X` 为第 24 字母 ⇒ 10+23；与 `charCodeAt-55` 双路同值），`Y`=34、`Z`=35；已在落码注释与本仓三份文档侧订正，**正本提案文件未改**（是否回交往主理人定）。② 正本记「全仓 4 份独立解码表」⇒ 实读码 **7 份**，现收敛为 2 份并由 X1 钉死。<br>**负面后果（诚实）**：① **本行实际包体增量≈零**——`check:size` 读构建产物而本批未重构建，且**现 8 关无一携带 >10 色** ⇒ 正本 §2.3 的 `paletteCodes` ≈15.4 B/条（满配 ≤3.3 KB/8 关、<0.08% 内部目标）是**未来 >10 色关入表时**的斜率、非本行实测；本轮源码面实测（**去注释后的代码字节**）：新增 `bead-charset.ts` **+936 B**、`palette.ts` **+2,126 B**（B2）、`grid.ts` +117 B、`levels.ts` **−112 B**、`misplaced-assembler.ts` **−57 B**（删内联表反而变小）⇒ **净≈+3.0 KB 未压缩源码**（minify 后更小）；`levels-data.ts` **去注释后 Δ=0**（只改注释）⇒ 本包未重构建，`check:size` 读数（beads 2775.1 KB）**与本变更无关、属存量**。② **§3.10 C6 高梯级不再封顶**（`min(3+⌊n/2⌋, 35)`）⇒ **未**另冻 `SPRINT_COLORS_MAX`（不发明 §3 没有的值，正本 Q3 待裁）；缓解事实 = 冲刺入口 v1.32 已隐 ⇒ 现网不可观测，挂账不销。③ `levels.ts` 既有「索引 > `BEAD_COLOR_MAX`」校验现**不可达**（35 = 容量），保留作自我复位、不删。④ studio 前端 `board-render.js::beadColor` 越界**仍静默**落炭黑（本批只同步其同源注释、未动逻辑），与游端 B2 同族 ⇒ 交批2。⑤ 游戏内导入 `draftToLevel` **不携带** `palette`/`paletteCodes`（`LevelDraft` 无 slug 字段，`/api/results/:id/level` 也只回草案）⇒ >10 色在游端导入路径**恒被 B1 拒**，可接受集合与抬值前相同（**无回归**），但**该缺口自本行起变承重**（此前由 10 色上限顺带遮住）。⑥ `tools/scripts/*.test.mjs` **未挂进 `verify`** ⇒ X1 的跨语言锁与 12→35 往返断言目前靠手动 `node --test`，是否入门禁待主理人裁。⑦ 命中 **ADR-0004 §5 复评条件①**，本行只登记翻值事实，**正式复评记录未做**（需 adr-arch 落笔，含其 §4.2 现文「锁死在 16 语义内」订正为 37 字符容量）。⑧ `levels-spec` 历史图例（§3/§6 只到 8 色）**不追溯重写**。 | 用户 2026-09-24 批1 任务单（A+B 原子护栏，「落文件 + 跑 verify，不 commit」）；正本 = `design/proposals/sec3-color-max-35-and-mis-max.md` §5 A/B 清单 + §2.1/§3.6 粘贴级新行全文；登记人 = 会话执行（程基岩 · engineering-lead）。⚠️ **号位说明（待主理人确认）**：任务单指定生效行用 v1.54，但 v1.54 已被上行的**提案登记行**占用（按 v1.4→v1.5 判例，生效行必须另起一号），且挂起件 `proposals/sec3-5mm32-render-base-change.md` 声拟的 **v1.53 至今未落表** ⇒ 本行取 **v1.55**。本行号已被 `systems-index.md §3.2`、`levels-spec.md`、`bead-grid.md` 三处内联引用互锁，若主理人改判号位需四处置换 |
| v1.56 | 2026-09-24 | **§3.13 错位档位新增 `max`（最大化错位）档翻值生效（批2 收口，与 §3.2 v1.55 同族）**。判例 = v1.54（提案登记）→ v1.55（§3.2 批1 生效）→ **本行（§3.13 批2 生效）**，**上行 v1.54 / v1.55 保留不动**。生效前提 = **代码 `352515f`（`max` 已落码）**；语义正本 = **`levels-spec §2.3` v1.12**（`bead-grid.md v2.9 §8-1` 亦已转生效口径）。**本行仅文档追平已落码**——把 systems-index §3.13「错位档位」行从文策渊 `❗[提案 v1.54 待复核]` + 「未经代码实测」的提案态翻为生效，收口一条 doc≠code 漂移；**§3.13 其它既有值零改动**（`MISPLACED_PAIRS_MIN/MAX` 1/8、逐关曲线建议值、规模闸、交换构造法、环长分布、全错位初盘行等一字未动）。**`max` 语义（与 levels-spec v1.12 逐字对齐、两处口径一致）**：`full\|max\|swaps\|none` 四档；`max` = 在「可填轮廓逐格匹配 + 每色珠数守恒（硬约束不可破）」两硬约束下把可错位的可填格数推到 `M_max = min(N, 2(N−m))`、强制就位 `F = max(0, 2m−N)`；`m ≤ ⌊N/2⌋` 时等价 `full`、否则尽力而为而非失败；**`full` 的 all-or-nothing 语义保留不动**、两档并存由调用方选；Plate 域 = 逐宫（P2b，不得跨宫搬珠）；唯一退化 `M_max=0 ⇔ N<2 ∨ m=N` 在合法关面不可达（`colors≥3 ⇒ M_max≥4`）。极值式已由 **X2 穷举实测**、退化不可达已由 **X3 穷举**钉死（不再是文档内手算，v1.54 提案注「未经代码实测」随本行解除）。**不放宽任何既有闸**（宫密度 ≤60% / 规模闸 / BOOT「错位≥1」）、**BOOT 校验面 ①–⑤ 一条未改**。**边界（不发明、不顺手改）**：本行不删 Plate 乙档、不改 MISPLACED 区间、不发明 `--minadj`/a11y 新数值；正本 §7 **Q1**（`balance()` 在 max 档跑不跑）/**Q2**（Plate 乙档去留）/**Q3**（`SPRINT_COLORS_MAX`）仍挂账未裁，本行不代裁；`levels.ts` BOOT 侧「`max` 档 `M>0` 即通过」的入关校验属 `352515f` 代码面，本行仅登记文档追平。**号位说明（让号）**：本行为 §3.13 批2 生效行，取 **v1.56 = 现表末行 v1.55 之后的下一空号**；**v1.54 = 文策渊提案登记行**（保留不动）、**v1.55 = §3.2 批1 生效行**（保留不动）；挂起件 `proposals/sec3-5mm32-render-base-change.md` 曾声拟 **v1.53 且至今未落表** ⇒ 按 v1.46 让号判例**本行不占 v1.53**（该号留待该文 eventual 落表），故顺延取空号 v1.56。登记人 = 程基岩（engineering-lead） | 主理人下派收口单（doc≠code 漂移：§3.13 错位档位行未翻）；代码正本 `352515f`；语义正本 `levels-spec §2.3` v1.12；提案正本 `proposals/sec3-color-max-35-and-mis-max.md` §3/§3.6（文策渊 prepared 的 §3.13 新行全文）；仅改设计文档、未动代码、未 commit |
