# GDD · S7 计分连击（Score & Combo）· beads

- 项目：`games/beads` · 版本 v1.1 · 任务号 WXG-T-130（v1.1 = 取回不计分不断连 + 「错位归位」语义适配；v1.0 见 §9）
- 数值纪律：冻结数值只引用 `systems-index §3`（星级 §3.7、托盘 §3.4、倒计时 §3.5、可访问性 §3.8、**冲刺 §3.10**）；冲刺常量 C1–C8 已于 2026-09-12 用户拍板冻结并回写 §3.10（唯一真源），本篇 §9 附录为推导记录。
- 依赖锚点：`core-loop.md` §2.2 微循环（placed/rejected 事件）、`bead-grid.md` §2.3（裁决）、`tray-spawner.md` §2.2（供料）、`timer-gameover.md` §2.1（计时真源）。
- 文件名说明：本文件即 systems-index §1 所列 S7（原规划名 `score-stars.md`，随路线 A+ 扩展更名 `score-combo.md`；§1 已于 systems-index v1.6 完成回写，本节已对齐，无悬置项）。

---

## 1. 目标

给两类模式各配一条"进度可感"的计分线：普通关以**星级**承载成就型驱动（零局内 HUD 负荷，支柱 1）；冲刺模式以**连击倍率 + 单局分**承载节奏感与长时沉浸（用户六目标），并产出可排行、可存档的最佳分。

## 2. 机制

### 2.1 双模式总览

| | 普通模式（现有 8 关） | 冲刺模式（本篇新增，2026-09-12 用户拍板冻结） |
|---|---|---|
| 目标 | 填满图案 → 星级 | 无尽爬梯：连续完成小图案冲分 |
| 局内 HUD 计分 | **无**（决策 D6 维持） | 有：分数 + 当前倍率 + 连击数 |
| 计时 | `LEVEL_TIME`（§3.5，180–420s） | `SPRINT_TIME_DEFAULT` 单局时长（附录 C1） |
| 失败 | 倒计时归零（唯一，D7） | 倒计时归零（唯一；托盘满仍只告警不判负，与 D7 一致） |
| 结算 | 星级（§3.7）+ 结算分（附录 C7） | 单局分 + 最佳梯位 + 是否破纪录 |

- 冲刺模式 = S1 `PLAYING` 内的**模式标志**（`mode: 'normal' | 'sprint'`），六状态机**零改动**（core-loop §2.1 不动，经 core-loop 兼容性核对：状态集、cleared/failed 裁决、PAUSED 冻结语义全部复用）。

### 2.2 普通模式：星级（冻结）与结算分（提案）

- 星级 = f(剩余时间占比)，来源 §3.7：`ratio ≥ STAR3_RATIO(0.32) → 3★`；`≥ STAR2_RATIO(0.12) → 2★`；过关即 ≥1★；扩展行不扣星。`ratio` 分子现行 = `remaining`；T-B 落地后改用 `starRemaining`（§3.7 `[T-B / 待确认]`）。`level:cleared` payload 的 `stars` 由 S7 计算后随事件分发（§4 既有事件，零新增）。
- 结算分（用于未来排行/段位，**局内不显示**）：`settleScore = stars×1000 + round(ratio×1000) − powerupsUsed×50 + (未用扩展 ? 200 : 0)`——全部系数列附录 C7（已冻结，2026-09-12 用户拍板）。

### 2.3 冲刺模式：无尽爬梯

- 单局结构：1 局 = `SPRINT_TIME_DEFAULT` 秒倒计时（附录 C1）＋ 串行小图案阶梯。每梯级（stage）装载一个小图案（复用 S3/S4/S3 全套机制，参数按梯级公式生成，附录 C6——公式为**预算口径**：色数/格数是该 stage 的上限预算（用于选池与校验），实际棋盘取池图案本身，见 §9 C6 设计说明）；**stage 填满 → 立即换下一 stage**（广播 `sprint:stage`，见 §2.5），不结算不弹窗，节奏不断；**换 stage 时托盘清空**（新图新供料，裁定依据见 §9 C8 补记）、连击不断（C8 例外条款）。
- 难度爬梯（附录 C6 公式，已冻结 2026-09-12）：梯级 n 的 色数 = min(3+⌊n/2⌋, `BEAD_COLOR_MAX`)；格数 = min(6×5+…) 上限 13×12（§3.3）；供料间隔 = max(6.0−0.5n, 2.0)（区间端点对齐 §3.4）。图案库从 levels-spec 8 关图案池循环取用（原创图案，规避 IP 风险——参考分析 §8）。
- stage 完成奖励：加时 `STAGE_BONUS_TIME` 秒（附录 C5）+ 固定分（附录 C5），倒计时与爬梯双向形成"越玩越紧"的心流坡。

### 2.4 冲刺模式：连击系统

- **连击窗口**：两次正确落子间隔 ≤ `COMBO_WINDOW_S` 秒（附录 C2）保持连击；超窗 = "犹豫断连"（用户目标明示）。
- **倍率阶梯**：连续正确落子数（streak）达 `COMBO_STREAK_TIERS` 阈值（附录 C3，推荐 2/4/7）→ 倍率 ×2/×3/×5，上限 `COMBO_MULT_MAX=×5`。
- **断连条件**（三选一即断，streak 清零、倍率回 ×1）：
  1. 放错（`bead:rejected`）——立刻断连；
  2. 窗口超时（正确落子间隔 > `COMBO_WINDOW_S`）；
  3. stage 切换**不断连**（跨 stage 连击延续——连续挑战的核心爽点，附录 C8 例外条款，已冻结 2026-09-12）。
- **取回不计分不断连（v1.1 新增，WXG-T-130 用户裁定）**：`tray:stored`（取回）**不产生分数**（非落子）、**不重置连击窗口**、**不推进也不打断 streak**——连击唯一计数进 = `bead:placed`、唯一断出 = 上两条。理由：取回是解谜周转步而非「正确落子」，若取回续窗则玩家可借无限取回白嫖窗口，破坏「犹豫断连」的节奏张力；若取回断连则惩罚了必要操作，违背 A4 零惩罚精神。同理，**取回不计入 stage 完成判定**（完成只看错位珠全部归位）。
- 计分：每次正确落子 = `SCORE_PER_BEAD × 当前倍率`（附录 C4）。普通模式**完全不走本节**（连击系统仅存在于 sprint 模式）。
- 断连不扣分、不扣时（惩罚 = 倍率归零的机会成本本身；与普通模式 A4"放错无惩罚"精神一致，冲刺模式因有倍率增益故惩罚已有）。

### 2.5 特效分级与事件

- 特效三档，随倍率档触发：**Lv1（×2）粒子星光**（复用 `vfx_fill_pop` 强化，assets-spec §1.6）→ **Lv2（×3）伪震屏**（整屏 scale 脉冲 1.00→1.015→1.00，**无位移抖动**——§3.8 冻结"屏震不使用"，伪震屏规避红线，若用户坚持真震屏须走 §3.8 变更）→ **Lv3（×5）全屏爆发**（边缘径向光 + 珠面波浪，复用 `vfx_complete_wave` 语言）。
- 全部动效毫秒值归 UX 规格（`ux/ux-spec.md` §5），本篇不自写；闪烁红线 ≤3Hz（§3.8）对 Lv3 同样生效。
- **新增事件（已登记 `systems-index.md §4`，v1.8 机械登记 2026-09-12）**：`combo:up {streak, multiplier, tier}`、`combo:break {reason: 'wrong'|'timeout'}`、`sprint:stage {stageIndex, nextParams}`、`sprint:ended {score, bestStage, settleScore}`——命名沿用 §4 `xxx:yyy` 风格。
- **payload 对齐说明**：以上 payload 与 `systems-index.md §4` 登记定稿一致（2026-09-12 主理人裁定：事件 payload 以 §4 为唯一真源）。`tier` = 倍率档位序号（[2,4,7] 阈值依次 1/2/3）；`stageIndex` = 新 stage 索引；`sprint:ended` 不含 `reason`（sprint 结束唯一原因 = 倒计时归零，§3.5）。

## 3. 输入

| 来源 | 内容 |
|---|---|
| S3 | `bead:placed` / `bead:rejected`（连击唯一的计数进/断出） |
| S3/S4 | `tray:stored`（取回；v1.1：S7 只消费做统计，**不计分不断连不断窗**） |
| S1 | 模式标志与状态门禁（PLAYING）；`level:cleared`（普通模式星级/结算分） |
| S5 | `timer:tick`（sprint 单局倒计时，复用 S5 真源，S7 不自建计时器） |
| S6 | `powerup:used`（结算分扣减项；**解环器不动连击、不触碰托盘**——连击只看 `bead:placed`） |
| S8 | 冲刺最佳分/梯位读取（局内 NEW BEST 判定） |

## 4. 输出与反馈

| 输出 | 条件 | 对齐 |
|---|---|---|
| `level:cleared {levelId, remaining, ratio, stars}` | 普通模式过关（S1 发，S7 算好 stars 注入） | §4 既有 |
| §2.5 四枚冲刺事件 | sprint 模式对应时机 | 已登记 §4（v1.8 机械登记，2026-09-12） |
| 倍率 HUD | sprint 且 streak ≥ 2 才显示（首倍率前零噪声） | ux-spec §3 HUD 注记 |
| 特效三档 | ×2/×3/×5 档位切换瞬间 | §2.5 分级 + assets-spec §1.6 |
| NEW BEST 角标 | sprint 结算分 > S8 存档最佳 | ux-spec §3 结算线框 |
| 音效 | 连击升档/断连 | 音频层消费 §2.5 事件；音色归音频规格 |

## 5. 数值

- **冻结引用**：§3.7（`STAR3_RATIO=0.32`、`STAR2_RATIO=0.12`、`DEMO_LEVEL_COUNT`、解锁规则；v1.10 组 C）；§3.4（sprint 梯级供料区间端点）；§3.3（sprint 梯级格数上限）；§3.8（特效红线：≤3Hz、无真屏震）。
- **提案数值**：全部在 §9 附录（C1–C8），**未冻结不得实现**。

## 6. 边界条件

**极端值**
- sprint 开局即连续超窗：streak 恒 0、倍率 ×1，仅基础分累计，无死局。
- stage 填满瞬间单局时间同时归零：**stage 优先结算再加时**——若加时后 >0 则继续爬梯，仍 ≤0 才判负（cleared 优先精神的 sprint 变体，附录 C8 已冻结 2026-09-12）。
- 连击窗口计时与 PAUSED：窗口计时随 PAUSED 冻结（复用 S5 冻结语义），恢复不追溯断连。
- `SPRINT_TIME` 覆盖值越界（<90 或 >120）：BOOT 拒绝该配置，回退默认（对齐 S5 §6 防御口径）。

**并发 / 竞态**
- `bead:placed` 与 `bead:rejected` 同帧：**不可能同帧** —— 两者都由一次落子裁决产生，而每帧最多 1 条输入指令（`input-control §2.4`）；产生哪个只取决于该次落子的颜色是否匹配。原「按到达序处理——先 placed 后 rejected…」两种分支**均不可达**，随「到达序」基准一并作废（WXG-T-061）。
- 连击窗口到期与正确落子同帧：**落子判有效**。基准改为 `core-loop §2.2.2` 帧内序——落子属**输入段**、连击窗 tick 属**连击窗段**（紧接输入段之后）⇒ 落子当帧已重置窗口，本帧窗口不会再穿透判定。结论与旧表述一致，基准由「事件到达时序」改为帧内序。
- `sprint:ended` 与 `combo:up` 同帧：`sprint:ended` 由归零判定产生（**计时段**），`combo:up` 由落子产生（**输入段**）⇒ 按帧内序 combo 先、ending 后；S1 一旦离开 PLAYING，其后到达的 combo 事件被丢弃（`core-loop §2.2.2` + §6 同款裁决）。
- stage 切换与道具请求同帧：**不可能同帧** —— 两者同属输入段且每帧最多 1 条指令。跨帧按帧内序：stage 切换（含**托盘清空**，裁定见 §2.3 / §9 C8 补记）先于其后帧的道具请求；S6 单步结算无中间态（S6 §2.3），无叠加风险。

**非法输入**
- sprint 模式收到普通模式 `level:cleared`（stage 完成不走该事件，走 `sprint:stage`）：防御忽略 + 警告。
- 倍率计算收到 streak < 0 或非整数：钳 0 + 警告。
- 普通模式误开连击计数（mode 标志丢失）：mode 从 S1 单向读取，S7 不自持模式态，杜绝漂移。

## 7. 依赖

- **上游**：S1（模式标志、状态门禁、cleared/failed）、S3（placed/rejected）、S5（sprint 倒计时真源）、S6（结算统计）、S8（最佳分读写）。
- **下游**：S8（冲刺最佳分/连胜写入——v1.1 字段已冻结 2026-09-12）、视觉/音频层（特效三档、音效）、未来排行（meta-framework 提案消费 settleScore/sprint 分）。
- **明确边界**：不修改 S5 计时真源（sprint 倒计时复用 S5，仅换 `LEVEL_TIME` 注入值与加时指令）；不改 S3/S4 任何裁决规则；普通模式局内零 HUD 计分（D6 维持）。

## 8. 验收标准（可测试硬判据，QA 直接造用例；附录 C1–C8 已于 2026-09-12 冻结（systems-index §3.10），全部判据可执行）

1. 普通模式局内全程零分数 HUD 元素；过关后 `level:cleared` payload 的 stars 与 §3.7 阈值表逐一吻合（ratio=0.32/0.319/0.12/0.119 四点采样）。
2. 普通模式结算分 = 附录 C7 公式逐项可复算（构造 stars/ratio/powerupsUsed/扩展四因子组合各 1 例）。
3. sprint 单局时长 = `SPRINT_TIME_DEFAULT`（未覆盖时），误差 ≤ ±0.5s（联合 S5§8-1）。
4. streak 达 C3 各阈值瞬间倍率切换为 ×2/×3/×5，`combo:up` 恰各 1 次；超过最高档 streak 继续增长但倍率封顶 ×5。
5. 断连三分支各 1 例：放错（rejected 后 streak=0、`combo:break{reason:'wrong'}`）、窗口超时（> `COMBO_WINDOW_S` 无落子，reason='timeout'）、无第三分支误触发。
6. stage 填满 → `sprint:stage{stageIndex}` 恰 1 次（`stageIndex` = 新 stage 索引，即已完成 stage 索引 + 1）、新图案 ≤1 帧装载、**连击跨 stage 延续**（streak 值不变）。
7. stage 完成加时后 remaining = 原值 + `STAGE_BONUS_TIME`（钳单局上限）；加时与归零同帧按 C8 裁决。
8. sprint 单局得分 = Σ(SCORE_PER_BEAD×倍率) + Σ stage 奖励，注入 20 次落子序列逐分可复算。
9. 特效档位与倍率档一一对应（×2 粒子/×3 伪震屏/×5 全屏爆发），全程无 >3Hz 闪烁、无真位移震屏（§3.8 红线，DevTools 帧检）。
10. PAUSED 期间连击窗口计时冻结（恢复后窗口从暂停值续算）；普通/冲刺模式互窜注入（sprint 中发 level:cleared）→ 防御忽略 + 警告。
11. 破纪录：sprint 结算分 > S8 最佳 → NEW BEST 显示且 S8 写入新值；≤ 最佳 → 不写不显示。（"sprint 结算分"即 `sprint:ended.settleScore`，与 §8.8 的单局分同值）
12. **取回不计分不断连（v1.1 新增）**：注入「归位 → 取回 → 归位」序列（取回间隔 > 0 且 < `COMBO_WINDOW_S`）→ streak 连续递增、`combo:up` 按阈值正常触发、`combo:break` 计数 =0；单局分只随两次 `bead:placed` 增长（`tray:stored` 前后分数差 = 0）；取回与下一归位间隔 > `COMBO_WINDOW_S` 时**仍断连**（取回不续窗）。解环器归位（`bead:placed` 无 `slot`）**正常计分**（它是一次真实落子，仅非托盘来源）。

## 9. 附录：冲刺模式新常量（❄️ 已冻结）

> **冻结状态：2026-09-12 用户拍板（WXG-T-020），全部回写 `systems-index §3.10`（唯一真源，引用以该处为准）。本附录保留作设计推导记录，数值与 §3.10 一致，不得在此单独修改。**

| # | 常量 | 推荐值 | 说明 |
|---|---|---|---|
| C1 | `SPRINT_TIME_DEFAULT` | 120 s | 单局时长，合法区间 [90, 120]（用户目标"快节奏单局"） |
| C2 | `COMBO_WINDOW_S` | 5.0 s | 连击窗口；犹豫断连阈值 |
| C3 | `COMBO_STREAK_TIERS` | [2, 4, 7] | streak 阈值 → ×2/×3/×5 |
| C4 | `SCORE_PER_BEAD` | 10 分 | 基础落子分（×倍率前） |
| C5 | `STAGE_BONUS_TIME` / `STAGE_CLEAR_BONUS` | +15 s / 200+50×stageIndex 分 | stage 完成加时与得分 |
| C6 | 梯级公式 | 色数 min(3+⌊n/2⌋, 8)；格数 min(30+10n, 156)；间隔 max(6.0−0.5n, 2.0) | n=0 起；端点对齐 §3.2/3.3/3.4 冻结区间 |
| C7 | 普通结算分 | `stars×1000 + round(ratio×1000) − powerupsUsed×50 + (未用扩展 ? 200 : 0)` | 局内不显示，供排行 |
| C8 | 三条裁决 | stage 切换不断连；stage 加时与归零同帧 stage 优先；stage 切换**托盘清空**（新图新供料，2026-09-12 补记） | 前两条已同步 core-loop §2.3 补记；第三条为 v1.9 补记，消解 §6 → C8 的悬空引用，依据见 systems-index §3.10 裁决条款 |

> **C6 设计说明（预算 vs 实际，2026-09-12 澄清，WXG-T-027）**：C6 的「色数/格数」是该 stage 的**上限预算 / 选池约束**，**不是必须精确等于的生成目标**。原因：§2.3 图案库从 levels-spec 8 关图案池循环取用，池图案的形状与格数是冻结的——若公式为精确生成目标，则"公式输出"与"池图案冻结"两条不可能同时成立（字面冲突，WXG-T-027 工程实现暴露，曾误把 C6 预算值原样塞进 `sprint:stage.nextParams`）。**实际装载 = 池图案 ∩ 预算**（池图案天然满足端点约束，预算用于选池与校验）；~~供料间隔公式为精确注入值，不受此影响~~（**⛔ v1.1：供料间隔段作废**——供料关停，stage 换图改 `swaps` 交换构造，见 `systems-index §3.10`）。因此 `sprint:stage.nextParams` 报**实际装载值**（色数/格数取实际棋盘，非 C6 公式值）——这是 `systems-index §4` 对 nextParams 的定义「换 stage 参数注入」的必然要求。

## 10. 变更记录

| 版本 | 日期 | 变更 | 任务 |
|---|---|---|---|
| v1.0 | 2026-09-12 | 建档（双模式 + 连击 + C1–C8 冻结回写 §3.10）；后续落码回写见 WXG-T-062 | WXG-T-015/020/062 |
| v1.1 | 2026-09-16 | **取回不计分不断连（WXG-T-130 第二批，用户 2026-09-16 裁定「错位归位」）**：① §2.4 新增「取回不计分不断连」条——`tray:stored` 零分数、零续窗、零断连（理由：取回是解谜周转步，续窗会被白嫖、断连错罚必要操作）；② §3 输入表加 `tray:stored` 与解环器口径（S6 不动连击、不触碰托盘）；③ §8 增判据 12（三段可测断言）；④ C6 设计说明供料间隔段作废标注。**C1–C8 冻结常量零改动**；stage 切换 = 新错位布置的连带（供料间隔公式作废）登记于 `systems-index §3.10` | WXG-T-130，用户 2026-09-16 四项裁定 |
