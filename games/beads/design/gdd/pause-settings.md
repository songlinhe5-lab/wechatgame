# GDD · S9 暂停与设置（Pause & Settings）· beads

- 项目：`games/beads` · 版本 **v1.7** · 任务号 **WXG-T-211-D**（主理人编排案批 1 节点 D，用户 2026-09-25 批准；缺口 G-3 = 收 `ux-spec` v1.15 头注所列「下游欠账①」）（**v1.7 = 暂停面板升至「五行」**（上位裁 = **Q2 甲「扩行」**）：§2.2 **行4 =「珠子风格 / 豆子尺寸」两钮**（`ux-spec` v1.15 的**规格先行面转正** ⇒ 面板内容单源回到 S9）、**行5 =「性能信息 / 回主菜单」两格**（v1.6 刚冻结的两格组合与左右位序**保留不动**，仅行号随扩行下移 ⇒ **漂移单向收口**：S9 改口对齐 ux §3.3 线框既有「行5」编号，不回改 ux）；旧行位口径 `~~行 4~~` **逐字划线留档、不静默删**（K-053）；`PAUSE_PANEL_H` **扩容值本文不钉** `[待定]`（呈现层常量住 `tuning.ts`、**非 `systems-index §3` 冻结项** ⇒ 归落码单实测回填，沿用 ux v1.15 口径）；**Q1 = 甲**：本批可即刻落码 = **复刻·四棱刻面（默认）/ 13 双色对角 / 18 线稿描边** 三套，**06 珐琅包边只写「出池条件」（§12.5-④ 需在四棱新基线重推系数后才出池）、不出任何数值**；钮文案规则背 **U16 = 甲**（本文 §2.2「珠子风格钮文案规则」条）；连带复审 = §2.2 末「遮罩前提」条（U17 连带面）；**§3 数值零改动**；上一版 v1.6 任务号 = **无 WXG-T 号（debug 工具批，主理人直派，同 QA 真机测量单先例）**，其 **v1.6 = 「性能信息」DEBUG 覆层开关行**：§2.2 行 4 左格新增「性能信息」，**唯一持久化的 debug 项**（`settings.debugInfo` 逐字段降级 false，复用 v1→v2 判例）；view 在主循环之上叠画 fps/帧耗时/相位/关卡·格距/LOD 只读半透明窄面板（**非全屏 scrim**——全屏 α 覆盖会改变 fill 负载，D2 测量失真）；暂停面板行 4 由 v1.5「回主菜单单钮居中」改回**两格**；**§3 数值零改动**；v1.5 = 「去冲刺（冲刺入口）」行隐藏（用户 2026-09-19 裁定「去冲刺按钮先隐藏，咱不需要这个功能」）：§2.2 该行**删划线留档**（K-053），暂停面板行 4 只余「回主菜单」并**居中**；`startSprint()` 与冲刺模式实现**保留不动**，入口收敛后**暂时不可达**（仅测试 / harness 注入）；**§3 数值零改动**；同源回写见 `ux-spec` v1.12 §3.3 图 / `systems-index` v1.32 §1 S7 行；v1.4 = **回主菜单语义反转**（真机首验用户裁定）：「回主菜单」弃本局棋盘、关卡解锁进度留、「开始游戏」恒为全新开当前关并扣 1 心（原 v1.3「保留进度不惩罚/在途续进不重复扣心」已废）；v1.3 = 回主菜单次钮 + 震动开关行（`VIBRATE_DEFAULT`、仅 isMiniGame 显示行）八项拍板；v1.2 及以前见 §9）
- 数值纪律：冻结常量只引用 `systems-index §3`（§3.1 布局带/避让、§3.5 重置与告急、§3.8 热区）；面板动效毫秒归 `ux/ux-spec.md`，本篇不自写。
- 依赖锚点：`core-loop.md` §2.1（PAUSED 状态与「重玩本关」）、`timer-gameover.md` §2.3（冻结语义）、`tray-spawner.md` §6（心跳冻结）、`architecture-beads.md` §2（bgm/sfx 双通道 setMuted）。

---

## 1. 目标

提供"随时可走、一键暂停"的唯一通道（左上设置齿轮），冻结全部关内计时与供料；承载音乐/音效开关与「重玩本关」，并保证恢复后世界从暂停值无缝续算。

## 2. 机制

### 2.1 入口（单通道原则）

- **唯一入口（局内）**：`HUD_BAND` 左侧设置齿轮（§3.1 布局带；热区 88×88，`TOUCH_MIN` UI 控件口径 §3.8）。**无独立设置页**；**v1.3 口径澄清（WXG-T-164）**：入口**随屏单通道**——局内 = 本齿轮、主菜单 = 菜单齿轮（同 S9 内容 overlay，ux-spec v1.7），无第三入口（避免双入口状态不一致）。
- 齿轮点击 → S2 路由优先级 1（input-control §2.1 路由表最高位）→ S1 切 `PAUSED`。
- 可暂停状态：仅 `PLAYING`。其余状态点齿轮 = 忽略（PAUSED 面板本身只响应面板按钮）。
- 双模式一致：普通/冲刺共用同一齿轮、同一面板（冲刺模式下「重玩本关」文案变「重新冲刺」，行为 = 整关重置语义；P1 已冻结：2026-09-12 用户拍板，重置连带冲刺连击态归零）。

### 2.2 暂停面板（内容清单，冻结范围）

| 元素 | 行为 | 备注 |
|---|---|---|
| 继续 | `game:resumed` → S1 回 PLAYING | 主按钮 |
| 重玩本关 | 整关重置五项（§3.5，S5 §2.4）→ PLAYING | 沿 core-loop §2.1 面板能力 |
| 音乐开关 | `settings.bgmMuted` 取反 + 即写 S8 | S9 设置直连 |
| 音效开关 | `settings.sfxMuted` 取反 + 即写 S8 | 双通道独立（architecture §2） |
| 减弱动效（D1） | `settings.reduceMotion` 取反 + 即写 S8 | **副按钮样式**，与「去冲刺」共用面板行3（WXG-T-088）；仅写设置不切相位；开关经 snapshot 回显给 view，恢复后即时生效（关停清单见 accessibility §2 D1） |
| 大字号（E2） | `settings.largeText` 取反 + 即写 S8 | **副按钮样式**；仅放大正文/说明类字号（×1.25），数字/标题/按钮不变；弹窗自适应延后 v1.1（accessibility §5.2 用户拍板基础版） |
| 震动开关（v1.3） | `settings.vibrate` 取反 + 即写 S8 + `settings:vibrate` 事件 | **仅 `platform.isMiniGame` 显示该行**（不伪造能力；§3.8 `VIBRATE_DEFAULT=ON` 可关）；触觉桥归 S10（weapp `wx.vibrateShort`）；与屏震禁令无关（§3.8 语义钉死） |
| **珠子风格选择钮（v1.7 行4 左格）** | 点按 = **池序循环切下一档**（到末档回第一档）→ `settings.beadStyle` **写入循环后的新值、即时落盘**（「即写 S8」判例族，本文**不新造事件名**） | **行4 左格**（v1.7）·**选择器钮、非开关**（钮面 = 当前值字面 + `▸`）；作用域 = 珠体 + 凹槽（B1）+ 托盘珠，**B0 目标色底图全风格共享几何**（§12 S4）；生效 = **下一帧、零过渡动画**（§12 S6 / 12.4）；**本行不写具体 styleId 清单、不写款数** ⇒ 循环与文案口径见下方「珠子风格钮文案规则」条（**U16 = 甲**）；**本文格只定行为与存档口径，不声称已落码**（现状无 registry，见本节末「规格先行与落码现状」条） |
| **豆子尺寸选择钮（v1.7 行4 右格）** | 点按 = **满豆 ⇄ 小豆** 两档循环 → 豆径档字段即时落盘（同上口径） | **行4 右格**（v1.7）；玩家侧文案「标准 / 小巧」（内部正名满豆/小豆，ux §3.3 文案表）；作用域 = **仅网格珠珠体（含已填态）**，托盘珠尺寸**恒满幅不随档**（§12 S5 / 12.4）；**孔随档分化**：满豆恒画孔 / 小豆不画孔（§12 S9）；**命中判据零变**（§3.8 `BEAD_HIT_PAD` 名义外扩，与珠体**视觉**尺寸无关）；档间 inset 系数住 `tuning.ts`、**本文不钉值** `[待真机 A/B]`（§12 C5 / §12.8-⑨） |
| 性能信息开关（v1.6 DEBUG） | `settings.debugInfo` 取反 + 即写 S8 | ~~**行 4 左格**~~ ⇒ **行5 左格**（v1.7 行号随扩行下移，**两格组合与左右位序零改动**）（debug 工具批，主理人直派）：真机 QA 无 console/query，唯一**可点击**的性能诊断入口；开关**持久化**（同 v1→v2 逐字段降级判例，无需版本 bump）；经 snapshot 回显，视图在主循环之上叠画 fps/帧耗时 EMA/相位/关卡·格距/LOD 只读半透明窄面板；不切相位（与音频开关同纪律）；**dev/QA 工具行**，非玩法承诺 |
| 回主菜单次钮（v1.4） | shell 路由 → 主菜单屏（**弃本局棋盘**，关卡解锁进度保留） | **行5 右格**（v1.7；~~行 4 右格~~）；**副按钮样式**不抢「继续」主钮位；双模式共用；菜单「开始游戏」= **全新开当前关、扣 1 心**（原「在途续进不重复扣心」已废，§3.14 `STAMINA_START_COST` 仅规定新局开局扣 1，本行为不变） |
| ~~去冲刺（冲刺入口）~~ | ~~切到冲刺模式（接 `startSprint()`）~~ | **⛔ 隐藏（WXG-T-177，用户 2026-09-19「去冲刺按钮先隐藏，不需要这个功能」）**：暂停面板行 4 只余「回主菜单」并**居中**；旧口径 ~~副按钮样式、不抢「继续」主位、冲刺模式下隐藏；依据 `ux-spec §8` U1（2026-09-12 冻结：三处都放）~~ 删划线留档（K-053）。`startSprint()` 与冲刺模式实现保留，入口收敛后**暂时不可达**；v1.6 起行 4 改为「性能信息 / 回主菜单」两格（见上），单钮居中口径随之作废；**v1.7 起「行 4」这个格位改指「珠子风格 / 豆子尺寸」，性能信息 / 回主菜单 两格下移为行5**（两格内容与左右位序不动，仅编号收口） |
| （冲刺模式）重新冲刺 | 同「重玩本关」 | **P1 已冻结**（2026-09-12 用户拍板） |

- **行位（v1.7 冻结为五行；上位裁 Q2 = 甲「扩行」；行号口径与 ux §3.3 线框一致：标题「暂停」不占行号）**：**行1** 主钮「继续」（240×88）→ **行2**「重玩本关 / 音乐 / 音效」→ **行3**「减弱动效 / 大字号 / 震动」——**以上三行不随本批变**；本批只重排尾部：**行4（新）=「珠子风格 / 豆子尺寸」两格均分**（每格宽 ≈280 = 560/2，与行3 同一算法、**非新冻结值**；高 ≥ `TOUCH_MIN` 88，§3.8）与 **行5 =「性能信息 / 回主菜单」两格**。行3 震动行仍**仅 `platform.isMiniGame` 显示**（非小游戏退两格，v1.3 口径不变）。
  - ~~旧行位口径（v1.5–v1.6 档案，逐字留档不静默删，K-053）：v1.5「行 4 只余『回主菜单』单钮居中」→ v1.6「行 4 = 性能信息（左）/ 回主菜单（右）两格」~~ ⇒ v1.7 后该两格落 **行5**；**面板总行由四升五需的 `PAUSE_PANEL_H` 扩容值本文不钉** `[待定]`（住 `tuning.ts`、非 §3 冻结 ⇒ 落码单按行高地板 `TOUCH_MIN` 实测回填；共用底板 `PANEL_SIZE` 560×480 不受扰动，见 ux §3.3「几何」条）。
  - **不建二级页 / 不做滚动区 / 不加「确认」钮**（选择即确认、写档一次；关闭仍只靠「继续」）；**不加第三入口**（入口随屏单通道：局内暂停齿轮 / 菜单齿轮，同一 overlay、内容单源 S9，v1.3 口径）。两钮均**不切相位**（与四开关同纪律）。
- **珠子风格钮文案规则（v1.7 钉死；上位裁 = ux §8 U16 **甲**，正文锚 `bead-visual-style-spec §12.8-⑩`，K-059）**：① 钮的循环序**只遍历 style registry 已注册的 `styleId`**，未交付档不出现、不置灰（无「点了没反应」的空档）；② **一切文案不得写死「共 N 款」**（含钮面、面板标题、帮助类字面），因可切档数 = 运行时注册数；③ §12 S3 **目标态池序仅供对账、不得当写死清单**；④ **池扩容 = 循环变长，零改框**（不需重规行4）。按 **Q1 = 甲** 本批可即刻落码三套：**复刻·四棱刻面（默认）/ 13 双色对角 / 18 线稿描边**；**06 珐琅包边 不列数值**，其**出池条件** = 在四棱新基线上重新 A/B 推系数（§12.5-④），旧十层系数不得直接沿用；完成前它**不进 registry ⇒ 不出现在循环里**（由① 自动兜住）。
- **规格先行与落码现状（诚实口径，不得当已成立用，K-035）**：行4 两钮自 v1.7 起**由「UX 侧先行」转为「S9 内容单源」**，但**不等于已落码**：仓内现状 = **无 style registry / 无 `settings.beadStyle` 存档字段 / 无豆径档字段 / 渲染仅经典四棱一套**（`ux-spec` v1.19 头注③ 实测；本文核仓同结论）⇒ **行4 两钮本批不呈现（不得造假 UI），其落码须与 registry 同批**（§12.9 步 3 ⇒ 步 5）。本批只**定行为与判据**，不声称屏幕上有钮。
- 面板背后**半透明遮罩**盖住棋盘与托盘（防误触 + 防偷看，遮罩参数归 ux-spec §3）。
- **遮罩前提复审（v1.7 新增；U17 连带面；任务单要求的一段复审结论）**：
  - **被复审前提**：§2.3「表现层不冻结（脉冲动画帧可续跑，但**被遮罩遮挡不产生误导**）」。原措辞把「遮挡」当唯一依据。
  - **触发面**：`ux-spec` v1.16 将 U14 裁为 **丙**（上位 = `bead-visual-style-spec §12` **S10**）⇒ **含行4 的设置态期间背景遮罩 α 下调**（承载位 `tuning.ts::PANEL_SCRIM_ALPHA`，**现值 0.5**，目标值 `[待定]`、由落码单实测回填、**下调不得为 0**）；U17 点名本前提需重跑。
  - **实测事实（本文核仓，非推测）**：暂停面板遮罩 = **全屏 rect（0,0,DESIGN_W,DESIGN_H）**，即 HUD 与棋盘/托盘同层被压 ⇒ α 下调后 HUD **倒计时读数只会更清晰**、不更差。
  - **复审结论（三段，逐段定性）**：① **「被遮罩遮挡」这一依据在设置态不再成立**（玩家可清晰观察盘面，属 S10-② 已接受代价的一部分，不重辩）。② **但「不产生误导」的结论仍成立，承重件需改述**：从「遮住看不见」换成「**因果链已断 + 冻结读数可对照**」三条——S5 计时冻结（告急判定不再新起）、S4 供料心跳冻结（不追溯补发）、HUD 倒计时停在暂停值且遮罩变浅后更易读。玩家若把「脉冲仍在跑」误读为「计时仍在走」，反证通道（冻结的数字）比改前**更强**。③ 因此本前提的**正确表述 = 「表现层继跑，但玩法因果链已断且倒计时读数恒冻，故不产生误导」**；「遮挡」降为充分而非必要修饰。§2.3 已随本批加引注（原句不删，K-053）。
  - **`[待真机]` 面如实标注**：上述② 是**推理结论、非观感实测**——「α 下调后告急脉冲（图标+颜色+脉冲，§3.8）是否仍让部分玩家误判为计时未冻」属屏幕层判断，仓内从无真机走查数据 ⇒ 标 `[待真机]`，**本文不写「已成立」**；取证归 QA Playtest 计划（非本文自验）。
  - **射程未裁（不代判）**：本复审只覆盖「设置态 α 下调」分支。若 U17 最终采**甲（全局下调）**，结算/失败面板态的同前提一并失效，结论射程需重跑；若采**乙（设置态专用值）**，则本文结论直接可用；若采**丙（降为落码单前置注）**，本条作为背景保留。**三选一属 ux/主理人侧，本文不自决。**
- 面板**不含**：退出游戏（交微信胶囊/系统返回——有意为之非遗漏）、排行榜/签到等 meta 入口（meta 入口归主菜单聚合，v1.3：见 `proposals/meta-framework.md` v1.1 / systems-index S10；「回主菜单」次钮属**导航**非 meta 入口，不受本行限制）。

### 2.3 冻结语义（复用 ADR-0007 单点裁决）

- 进入 PAUSED：S5 计时冻结（含告急判定与连击窗口的 sprint 扩展）、S4 供料心跳冻结；**表现层不冻结**（脉冲动画帧可续跑，但被遮罩遮挡不产生误导——timer-gameover §2.3 同款口径）。（**v1.7 复审注**：上句「被遮罩遮挡」作为唯一依据**已随 U14 丙案（设置态遮罩 α 下调）失效**；本条结论不变、**依据改述为「因果链已断 + 倒计时读数恒冻」**，全文见 §2.2「遮罩前提复审」条。原句字面按 K-053 **不删**。）
- 恢复：所有冻结计时从**暂停保存值**续算（S5 §8-5 判据）；不追溯、不补发暂停期间跳过的供料（S4 §6）。
- 同帧裁决：暂停请求与归零/落子同帧 → **门禁先切 PAUSED**，玩法事件作废（input-control §6 同款；**实现口径见 §6 帧内序定义**——本文 §6 与 S5 `timer-gameover.md` §6 曾表述不一致，**已于 WXG-T-031 统一：timer-gameover §6 已对齐本文 §6 帧内序**（输入 → 连击窗 → 供料 → 计时））。

## 3. 输入

| 来源 | 内容 |
|---|---|
| 玩家（经 S2 优先级 1） | 齿轮点击 → 暂停请求 |
| 玩家（PAUSED 中，面板热区） | 继续 / 重玩本关 / 音乐 / 音效 / 震动（v1.3，仅 isMiniGame 显示行）/ **珠子风格（v1.7 行4）/ 豆子尺寸（v1.7 行4）** / 性能信息（v1.6）/ 回主菜单（v1.3）/（重新冲刺）——**行4 两钮属规格先行面，registry 未落码前不呈现（见 §2.2）** |
| S1 | 当前状态（门禁）；PAUSED 进入/退出回执 |
| S8 | 设置读档初值（BOOT 回显） |

## 4. 输出与反馈

| 事件/表现 | 条件 | 对齐 |
|---|---|---|
| `game:paused` / `game:resumed` | 齿轮/继续按钮（§4 既有事件，S9 是唯一发布方之一） | systems-index §4 |
| 面板入/出场动效 | 进入 PAUSED / 恢复 | `art-bible.md` §7「面板入 200 / 出 150」行（引用不自写） |
| 开关即时回显 | 开关切换 | 视觉态立即翻转，不等重启 |
| 「重玩本关」路径 | 点击 → 面板出 → 五项重置 → PLAYING | S5 §2.4 清单 |
| 音效反馈 | 面板按钮 | 音频层；音效被静音时仅视觉双通道（ux-spec §5 红线） |

## 5. 数值

- 引用 §3.1：`HUD_BAND`（齿轮带位）、`CAPSULE_AVOID`（面板右侧避让——面板宽不得侵入胶囊区）。
- 引用 §3.8：齿轮热区 88×88（`TOUCH_MIN` UI 控件口径）；面板按钮热区同口径。
- 引用 §3.5：「重玩本关」= 整关重置五项。
- 面板尺寸/遮罩透明度/动效毫秒：归 `ux/ux-spec.md`（本篇零自定义）。
- **v1.7 登记·本篇不钉的呈现层值（三项，均非 `systems-index §3` 冻结项）**：① 五行所需的 `PAUSE_PANEL_H` 扩容值 `[待定]`；② 设置态专用遮罩 α（`PANEL_SCRIM_ALPHA` 现值 0.5，目标值 `[待定]`，**下调不得为 0**）；③ 小豆档珠体 inset 系数 `[待真机 A/B]`。三项一律住 `src/config/tuning.ts`，由落码单按行高地板 `TOUCH_MIN` 88（§3.8）与真机 A/B 实测回填；**本文不预设数字**（沿用 `ux-spec` v1.15 口径与其 §3.3「值待回填」同纪律）。

## 6. 边界条件

**极端值**
- PAUSED 中再次点齿轮区：被遮罩拦截，零响应（面板按钮是唯一出口）。
- 告急脉冲激活时暂停：danger 态保留在 HUD，恢复后按剩余时间重新判定是否仍告急（不强制常亮）。
- 重玩本关时扩展已解锁 / 道具已用 / 连击在冲刺模式进行中：全部随五项重置清零（S7 连击态一并归零；P1 已冻结，2026-09-12 用户拍板）。
- 面板打开瞬间 `level:cleared` 同帧：**同帧暂停优先**（判定基准 = 输入段内序「状态指令 → 玩法事件」，与 §2.3、§8-6 同基准）——暂停生效，`level:cleared` **缓存至恢复后处理**；若 `level:cleared` 已在**此前帧**生效（已进 LEVEL_CLEAR），则齿轮请求走门禁忽略（非 PLAYING 入口）。依据：① 齿轮属状态指令、`level:cleared` 属玩法事件，段内序已定前者先；② 与 §2.3「暂停 vs 落子同帧 → 暂停优先」自洽；③ 与 WXG-T-030 实现一致（齿轮路由前置于 phase 路由）；④ 该 outcome 对玩家**零损失**——通关只是延后展示，不丢。
  - **注**：core-loop §6「cleared 优先」判例的适用范围是 `level:cleared` vs `level:failed`，**不含** cleared vs 暂停，本条与其**不构成冲突**（S1 最终裁决归属不变）。

**并发 / 竞态**
- 齿轮点击 vs 归零同帧：**同帧暂停优先**（2026-09-12 裁定，WXG-T-030 实现确认）。**帧内序固定为：输入（段内序：状态指令 → 玩法事件）→ 连击窗 → 供料 → 计时**——由该序推出两条 outcome：（⚠️ WXG-T-061 起，本序的**规范定义归口 `core-loop §2.2.2`**，本条改为对齐引用——两处各自复述正是此前漂移的成因）
  - 归零**已生效**（此前帧已借此切进 GAME_OVER）之后到达的齿轮请求 → GAME_OVER 生效、**无暂停**（非 PLAYING 状态入口走门禁忽略，见「非法输入」第一条）；
  - 与归零**同帧**到达的齿轮请求 → 输入在计时之前处理 ⇒ **暂停生效、本帧不判负**（计时冻结、dt 不累计、归零不触发）。
  - 裁定理由：① 单线程固定步长下"同帧到达序"不可定义（一帧内没有先后事件，只有代码执行序），唯一可实现的确定性解是显式帧内序；② §2.3 是更具体的规则，优先于本节原先的抽象表述；③ "偏向玩家"与 core-loop §6「cleared 优先」的通关 generosity 精神同向。两条路径均无"暂停后又失败"的叠加态。
- 开关切换 vs 结算写档同帧：合并一次整档写（S8 §2.5 幂等）。
- 连续快速 齿轮→继续→齿轮：每帧至多 1 条状态指令（S2 §6 指令洪泛上限），状态机单向流转无抖动。

**非法输入**
- 非 PLAYING 状态收到暂停请求：忽略（BOOT/LEVEL_CLEAR/GAME_OVER/FINISH 无可暂停物）。
- PAUSED 中收到玩法事件（落子/供料/道具）：源系统已被冻结，防御层再拦（零副作用）。
- 重复 `game:paused`（已暂停再暂停）：幂等，不重置 S5 保存值（S5 §6）。
- **行4 两钮（v1.7）**：① **registry 注册数 ≤ 1** ⇒ 「珠子风格」钮**不呈现**（不置灰、不做无档可切的占位钮；U16 = 甲）⇒ 行4 退化为单格或整行不渲染，**不得因此抽动面板行高布局**（具体退化形式归落码，行高地板 `TOUCH_MIN` 不变）；② 存档含**未注册 / 越界 `styleId`、非枚举豆径档值、类型错** ⇒ **不属面板职责**，在 BOOT 读档阶段逐字段降级为默认（复刻·四棱刻面 + 满豆），面板永远只从 snapshot 读到合法值（判据 = §8-18，真源 = S8 §2.4 / `ux §4` BOOT 行）；③ 两钮**不切相位**：PAUSED 中无论连点多少次，S5/S4 仍冻结、不发 `game:resumed`。

## 7. 依赖

- **上游**：S1（状态机 PAUSED 节点）、S2（齿轮与面板按钮路由）、S8（设置读写）。
- **下游**：S5（计时冻结）、S4（心跳冻结）、S7（连击窗口冻结，提案事件消费）、音频层（bgm/sfx 通道静音）、视觉层（遮罩与面板）。
- **明确边界**：不新增状态（PAUSED 复用 core-loop §2.1 既有节点）；不做退出/账号/meta 入口；双通道音频用框架现成 `setMuted`（architecture §2，无框架改动）。

## 8. 验收标准（可测试硬判据，QA 直接造用例）

1. PLAYING 点齿轮 → `game:paused` 恰 1 次、S1=PAUSED、面板可见、遮罩覆盖棋盘与托盘；PAUSED 中点击遮罩/棋盘/托盘/道具卡全部零响应。
2. PAUSED 300s 后继续 → remaining 与暂停前一致（≤1 帧 dt），首个供料不早于"暂停剩余间隔 +1 帧"（联合 S5§8-5 / S4§8-8）。
3. 「重玩本关」→ 五项重置逐一断言（倒计时回满/图案清空/托盘清空/扩展重置/道具次数回初值），S1 直接回 PLAYING 无 GAME_OVER 中转。
4. 音乐/音效开关各切 2 次：`settings.*` 即档、重启回显一致、BGM 与 SFX 互不影响（关音乐仍有音效，反之亦然）。
5. 齿轮热区 ≥88×88 且不侵入 `CAPSULE_AVOID`；面板任何元素不与胶囊重叠（布局断言）。
6. 归零 vs 齿轮两组用例（判定基准 = **帧内序：输入（段内序：状态指令 → 玩法事件）→ 连击窗 → 供料 → 计时**，齿轮属输入、先于计时处理）：**同帧**（归零与齿轮同帧到达）→ 暂停生效、本帧 dt 不再累计、恢复前不判负；**跨帧**（此前帧已借归零切进 GAME_OVER）→ GAME_OVER 生效、无暂停。注：「事件到达序」在单线程固定步长循环内不可观测，判据基准以帧内序为准——本条结论与改前一一对应，**仅换判定基准、非改判据**。
7. 非 PLAYING 五状态（BOOT/LEVEL_CLEAR/GAME_OVER/FINISH/PAUSED）注入齿轮请求 → 零事件零状态变化。
8. 重复暂停幂等：PAUSED 中注入 `game:paused` → S5 保存值不变（恢复后 remaining 正确）。
9. sprint 模式暂停：连击窗口计时冻结（恢复后从暂停值续算，不追溯断连）。
10. 面板**入** ≤ 200ms / **出** ≤ 150ms 量级（动效参数以 ux-spec §5 为准做帧检，红线 ≤3Hz 闪烁）。
11. **回主菜单次钮（v1.4 反转）** → shell 切主菜单屏；菜单主钮「开始游戏」= **全新开当前关**（`goToLevel(levelIndex)` 棋盘复位）、**扣 1 心**（非回到在途局）；关卡解锁进度与当前关指针不变（0 心留在菜单走回满广告）。——原 v1.3「同一在途局 PAUSED→PLAYING、不重复扣心、不重置」作废。
12. **震动行（v1.3）**：harness（非 isMiniGame）不渲染该行；weapp 渲染且默认 ON （`VIBRATE_DEFAULT` 回显），切换即写 S8 并发 `settings:vibrate`，重启回显一致。
13. **性能信息行（v1.6 DEBUG）**：默认 OFF（含新档首启与旧档升档）；~~面板行 4 左格~~ ⇒ **行5 左格（v1.7）**切换即写 `settings.debugInfo`、snapshot 同帧回显、不切相位（PAUSED 保持）；开时 `perfFrameMs` 为实测帧耗时 EMA（首帧 ≤ 一帧时长）、关恒 0；同存储重启回显一致（v1→v2 逐字段降级判例，旧档无字段不丟档）。
14. **行4「珠子风格」钮——循环与即写档（v1.7，Q2 甲）**：钮存在时，每次点按恰切换到**注册序的下一档**（到末档回第一档）、**恰写 `settings.beadStyle` 1 次**（无「保存」步、无撤销、只存末值）；**不切相位**（PAUSED 保持、不发 `game:resumed`）、**不推 S5/S4 任何计时**。
15. **行4 连点幂等**：连点 N 次（N = 当前注册档数）⇒ 回到起始档且**观感与初始逐帧一致**（循环幂等，QA 回归锚）；写档次数 = N 次合并后的**末值一次**（与 §6「同帧多次写合并」/ S8 §2.5 幂等同口径）；**不得出现历史栈**。
16. **行4 双轴正交**：「珠子风格」×「豆子尺寸」交替连点 ⇒ 两键**各写各的存档字段、互不锁、互不重置**（§12 S6）；生效面 = 各自作用域并集；任意（注册档 × 两档）组合恒可渲染且**零棋局状态写入**（铁律 L5：呈现层不持有游戏状态）。
17. **下一帧生效、零过渡（v1.7）**：切档后**恰好一帧**内珠体族图元集发生变化、**B0 目标色底图输出逐字段不变**（§12 C1/C8/S4 夹具判据）；钮面值翻转无动效、**零新增毫秒值**（§5 不落新行）；面板入/出时长仍按 §8-10 引用值（不因本批改变）。
18. **BOOT 非法值降级 = 默认档（v1.7）**：存档含未注册/越界 `styleId`、非枚举豆径档值、或字段类型错 ⇒ **逐字段**回落默认 = **复刻·四棱刻面 + 满豆档**；**不报错、不中断启动、不弃整档**，其余字段（进度/星级/四开关/震动/debugInfo）**无损**（沿 S8 §2.4 逐字段降级判例与 `ux §4` BOOT 行；属**已有降级口径的顺延适用，非新决议、非 §3 变更**）。
19. **注册数 = 1 时钮不呈现（v1.7；U16 = 甲 + `ux-spec` v1.19 头注③）**：style registry 已注册 `styleId` 数 ≤ 1 时，行4 **不渲染「珠子风格」钮**（**不得呈现点下去无档可切的占位钮、不得置灰**）；注册数 ≥ 2 时钮出现且循环长度 = **运行时注册数**；**任何文案（钮面/标题/帮助字面）写死「共 N 款」即判红**。

> 第 3 条可测性注记（2026-09-12，WXG-T-030）：五项重置中的**第五项「道具免费次数回初值（`POWERUP_FREE_USES`）」依赖 S6 道具系统**——S6 落地前仓库无道具状态，该项**不可测**，用例应注记为"待 S6"的真空项，**不得发明伪状态充数**；S6 落地后自动转为可断言。前四项不受此限。
>
> 第 **14–19** 条可测性注记（v1.7，WXG-T-211-D）：六条**全为待实现判据**，不得当已达标引用（K-035）。实测前置三件：① style registry 落地（§12.9 步 3）；② `settings.beadStyle` + 豆径档字段入档（S8 v1.1 §8-11～13）；③ 行4 两钮与 registry **同批**落码（`ux-spec` v1.19 头注③）。**仓内现状**：三件均不存在、渲染仅经典四棱一套 ⇒ 14–19 现在**只能拿红基线**；QA 建例按「未落码 ⇒ SKIP/红」记录，**禁止以「文档已写」当「已实现」**。两条例外：**18 号部分可立即测**（向存档写入非法 `styleId` 字符串即验降级路径，无需 registry）；**19 号在当前（零注册 / 单套）状态即可验「不得呈现假 UI」**。

---

## 9. 变更记录

| 版本 | 日期 | 变更 | 依据 |
|---|---|---|---|
| v1.0 | WXG-T-015 交付 | 初版八节定稿（含 P1 冻结：暂停面板能力 / 双模式共用齿轮 / 重玩本关五项重置） | WXG-T-015 |
| v1.1 | 2026-09-12 | **S9 实现暴露回写（零数值变更）**：① §2.2 补「去冲刺（冲刺入口）」行——副按钮样式不抢主按钮位、冲刺模式下隐藏，依据 `ux-spec §8` U1；② §6「齿轮点击 vs 归零同帧」确立**帧内序 = 输入 → 连击窗 → 供料 → 计时**并裁定**同帧暂停优先**，消解原 §2.3 / §6 自相矛盾；③ §8-10 笔误「面板出场」→「面板**入** ≤200ms / **出** ≤150ms」（数值零改动，权威 ux-spec §5）；④ §8 末补第 3 条可测性注记（第五项依赖 S6，S6 落地前不可测） | WXG-T-030 实现暴露 + 主理人裁定 |
| v1.2 | 2026-09-12 | §8-6 判定基准由不可观测的「事件到达序」改为**帧内序**（同帧 / 跨帧两条 outcome 与改前一一对应，**非改判据、仅换基准**）；§2.3 交叉引用注记更新为「**已于 WXG-T-031 统一**，timer-gameover §6 已对齐本文 §6 帧内序」；⑤ §6 帧内序补**输入段内序（状态指令 → 玩法事件）**并把「面板打开瞬间 `level:cleared` 同帧」条目改为**同帧暂停优先**（cleared 缓存至恢复后处理；此前帧已进 LEVEL_CLEAR 则门禁忽略）——消解 §2.3「暂停优先」与 §6 原到达序表述的残留打架，并注明 core-loop §6「cleared 优先」适用范围为 cleared vs failed、与本条不冲突 | WXG-T-031 主理人裁定（单线程固定步长下"到达序"不可观测、不可测） |
| v1.3 | 2026-09-18 | **回主菜单次钮 + 震动开关行（WXG-T-164，用户八项拍板）**：§2.2 增两行（次钮 = shell 路由回主菜单、玩法保持 PAUSED、保留进度不惩罚、在途续进不重复扣心；震动行 = `settings.vibrate` 即写 S8 + `settings:vibrate`、**仅 isMiniGame 显示行**）；§2.1 单通道口径改「随屏单通道」（局内齿轮 / 菜单齿轮，内容单源 S9）；§2.2「面板不含」注记更新（meta 入口归主菜单聚合）；§3/§8 同步（§8 增 11/12 判据）。冻结值真源 = systems-index v1.28 §3.8 `VIBRATE_DEFAULT` / §3.14 `STAMINA_START_COST`；**§3 数值零改动** | WXG-T-164，用户 2026-09-18 八项拍板 |
| v1.4 | 2026-09-18 | **回主菜单语义反转（真机首验用户裁定）**：「回主菜单」由「保留进度不惩罚」改为**弃本局棋盘**（关卡解锁进度/当前关指针仍保留）；§2.2 行与 §8-11 同步——菜单「开始游戏」恒 `goToLevel` 复位当前关 + 扣 1 心（不再「在途 PAUSED 续进」）；连带删 `beads-game.resumeFromPause()`（无调用者）与 shell 续进分支。§3.14 `STAMINA_START_COST=1` 数值**不变**（仅「新局」集合扩大），无 §3 数值漂移 | WXG-T-165，用户真机反馈「回主菜单不保留进度」 |
| v1.5 | 2026-09-19 | **「去冲刺（冲刺入口）」行隐藏（WXG-T-177，用户 2026-09-19 裁定「去冲刺按钮先隐藏，咱不需要这个功能」）**：§2.2 该行删划线留档（原口径「副按钮样式、不抢『继续』主位、冲刺模式下隐藏、依据 `ux-spec §8` U1 三处都放」整体作废，K-053）；暂停面板行 4 由「start-sprint / go-menu」两格改为**仅「回主菜单」居中**（落位表达式与 `sprintLayout` 同源）；`startSprint()` / `sprint.ts` / `sprint-settle.ts` 与 `systems-index §3.10` 全部冻结常量（C1–C8）**保留不动**，入口收敛后冲刺模式**暂时不可达**。**§3 数值零改动、§8 判据主体零改动**（§8 未含「去冲刺」判据，无需迁移）；入口实现面改动见 `ux-spec` v1.12 / `systems-index` v1.32 §1 S7 行 | WXG-T-177，用户 2026-09-19 裁定（原 U1 冻结于 2026-09-12 / WXG-T-020） |
| v1.6 | 2026-09-25 | **「性能信息」DEBUG 覆层开关行（无 WXG-T 号，debug 工具批，主理人直派，同 QA 真机测量单先例）**：§2.2 新增「性能信息开关」行——暂停面板行 4 由 v1.5「回主菜单」单钮居中改回**两格**（左格 = 新钮，右格 = 回主菜单）；`settings.debugInfo` 取反 + 即写 S8，**唯一持久化的 debug 项**（逐字段降级 false，复用 v1→v2 判例，**无需版本 bump**，save-schema 未动）；真机 QA 无 console/query，故必须走 UI 入口；view 在主循环之上叠画 fps/帧耗时 EMA/相位/关卡·格距/LOD 只读半透明窄面板（**非全屏 scrim**，不污染 D2 的 fill 测量）；不切相位；关→开时帧耗时 EMA 归零重起步。**§3 数值零改动、§8 增 13 判据**；sprint 布局同步（两模式行 4 同构，`sprintLayout` 退化纯过滤） | 无号，主理人直派（服务 QA 真机测量单 D1/D2 现场读数） |
| v1.7 | 2026-09-25 | **暂停面板升至五行：行4「珠子风格 / 豆子尺寸」转正（WXG-T-211-D，缺口 G-3，收 `ux-spec` v1.15 下游欠账①）**。上位裁写死：**Q2 = 甲（扩行）** ⇒ 行4 = 两钮（选择器钮、非开关）、**行5 =「性能信息 / 回主菜单」**（v1.6 刚冻结的两格组合与左右位序**保留不动**，仅行号下移 ⇒ **漂移单向收口**，S9 对齐 ux §3.3 既有行5 编号、不回改 ux）；旧行位口径 `~~行 4~~` **逐字划线留档**（K-053，正文四处：§2.2 性能信息行 / §2.2 回主菜单行 / §2.2 表后「旧行位口径」整句 / §8-13）。**Q1 = 甲** ⇒ 可即刻落码三套（复刻四棱默认 / 13 双色对角 / 18 线稿描边），**06 珐琅只写出池条件、零数值**。新落点：§2.2 新增两行 + 行位总览（含 `PAUSE_PANEL_H` 扩容值 **本文不钉** `[待定]`）+ **钮文案规则条（U16 = 甲：只遍历 registry、文案禁写死款数）** + **规格先行与落码现状条**（无 registry / 无存档字段 ⇒ 不呈现钮、不造假 UI，K-035）+ **遮罩前提复审条**（U17 连带：设置态 α 下调后「被遮挡」不再成立、但「不误导」结论仍以「因果链已断 + 倒计时恒冻」成立；观感面标 `[待真机]`；§2.3 同句加引注不删）。§3 输入行 / §5 增「本批不钉的呈现层值」登记条（三项：`PAUSE_PANEL_H` / 设置态遮罩 α / 小豆档 inset）/ §6 非法输入（注册数 ≤ 1 不呈现、非法 `styleId` 不属面板职责、两钮不切相位）**同步**。**§8 增 14–19 六条硬判据**（即写档 / 连点幂等 / 双轴正交 / 下一帧生效零过渡 / BOOT 非法值降级 = 复刻四棱+满豆 / 注册数=1 不呈现）+ 14–19 可测性注记；§8-13 仅改行号引用。**`systems-index §3` 零改动**（两设置属呈现层，住 `tuning.ts` + S8 存档）、**零新增毫秒值**、**不改 `src/` 与 cocos 镜像**、不 commit；继承面：`bead-visual-style-spec §12` v0.4（S3/S5/S6/S8/S9/S10/C5/C9）**不重开任何已裁项**；同步下游 = S8 `save-progress.md` v1.1 §8-11～13 | WXG-T-211-D（主理人编排案批 1 节点 D，用户 2026-09-25 批准）；判例 K-035 / K-053 / K-059 |
