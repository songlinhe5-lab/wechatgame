# 2026-09-17

## WXG-T-138：beads v1.23「大胆重制」提案落盘（WorkBuddy 主理人会话）

- **接续**：昨日 grill-me 两轮 16 问全按推荐拍板（提案正本 `games/beads/design/proposals/v1.23-bold-remake.md`）；本单 = 提案 → 冻结文档串行落盘。
- **领号**：T-138（头注 137→139 已核工作树：design/ 无并发未提交文件，T-137 程基岩施工中但其单禁改 design/**，文件级无碰撞）。
- **落盘五文**：① `systems-index.md` v1.23 七处——§3.4 托盘 12→6 / 扩展 12→6（A1 二次变更）、§3.5 区间 [120,420] + `LEVEL_TIME_OVERRIDE=clamp(k×45s,120,420)`、§3.7 主题解锁语义行（★货币化）、§3.10 `SPRINT_K_CURVE` [待 playtest]、§3.13 `cycleProfile` 环长分布、§3.8 幽灵符号作废态→设置开关转正（默认关）；② changelog v1.23 行；③ `input-control.md` v2.1——swap 格间直换由 Should 改**裁定永不实现**；④ `concept.md` v1.2——支柱 2 改写「托盘工作台的手感与环谜题」、MDA/§6 去供料化、§7 范围分层（MVP +归位连击 +幽灵开关）、D11/D12、附录 A 历史轨迹声明；⑤ 台账两件（T-138 行 + 详情小节）。
- **本单不改**（后继 backlog）：src/**（不与 T-137 竞写）、levels JSON 逐关 time 复核、score-combo 归位连击节、save-schema 幽灵开关字段、art 主题材质与托盘面板收窄（T-131 域）、生成器工具链化。
- **诚实修正存档**（提案 §5，防后人再犯）：托盘 2 槽即可滚窗解任意置换 ⇒ 托盘容量是手感旋钮非难度闸门；难度全押 k × 环长分布。
- **未 commit**：等用户指示（含 ctx 索引面文件时按 MEMORY「先 --staged-blobs 连跑再提交」口径）。

## 美术风格草稿（v1.23 后续，2026-09-17 上午）
- 用户要看美术草稿确认整体风格。按 art-bible v1.5-r2 + `src/view/palette.ts` 程序化生成三张 SVG 草稿（零新增 hex）：
  ① 整屏玩法样张 750×1334（冷底+暖光 band+十层珠+凹坑+6 槽托盘+解环器道具卡）；② 凹凸对比+十色符号特写；③ 材质三方向（糖果/玻璃/哑光，同板不同光影配方，对应 Q13 换肤）。
- 交付：`games/beads/art/style-drafts-20260917.html`（合成预览页）；生成器 `/tmp/beads-style-drafts.cjs`（含珠十层卡/凹坑四层/符号 path 的可复用绘制函数，确认后可迁入工具链）。
- 待用户反馈：整体风格是否 OK；错位珠标注方式（蓝虚线环仅草稿用）；材质主题方向取舍。

## 谜面可读性澄清（用户问：底色被盖住怎么辨错位？）
- 确认：珠50=格50、同圆角 ⇒ 凹坑底色**零露出**，filled 格无错位标记（`accessibility.md §5.4` = 美术侧 v1.5-r2 已登记的 P0 待审批项）。`bead-grid.md` L74「珠色≠底色即可辨」系循环论证，assets-spec L945 已自认。
- 当前谜面唯一可读通道 = **图案语境色块冲突**；「放错」不会发生（归位裁决拒异色），错位只存在于开局 swaps。
- 已出对比草稿④（A 现状 / B 目标色环（美术推荐，+156 层最坏，零新 hex）/ C 只标错位（泄底，≤+16 层）；(a) 缩珠径已驳回因 BEAD=50 冻结）。**待用户裁定 B/C/A**，裁定后落 §5.4 + assets-spec + 图元基线重算。
- 预览页已更新至四节：`games/beads/art/style-drafts-20260917.html`。

## WXG-T-140：谜面可读性裁定 (b)「L11 目标色环」已落盘（用户拍板）
- **裁定**：每颗棋盘 filled 珠沿格缘加 2px 环，ink = 该格 pattern 目标色暗一档 `mix(target,#000,0.30)`（复用 §1.9.2 端点 ⇒ 零新 hex）；环≠珠即错位。托盘珠/locked/empty 不带环；环归属「格」不随选中上移、落座缩放。
- **拒指纹**：(a) 缩珠径（BEAD=50 冻结）驳回；(c) 只标错位珠（泄底）不采纳。已接受副作用：156 格环的视觉噪声；回退第一顺位 = 砍 L11。
- **图元**：新增基线 ④ = 1672 +156 = **1828**（可达真值非松上界）；`h` 不变量破例（每手持珠 −1）。
- **落盘四文档**：`accessibility.md §5.4` 关项、`assets-spec.md` §1.1 L11 / §1.2 filled / §1.8 基线④、`bead-grid.md` L74 循环论证改正、`style-drafts-20260917.html` 草稿同步。**零 §3 变更**；`art-bible.md` 未改（其 §7.4 已声明绝对值以 assets-spec 为唯一真源）。
- **后继**：渲染层核实 view-model 是否对 filled 格携带 pattern colorIdx + tuning 加环宽常量 + 真机噪声验收（均需另立单）。

## 参考录像 UI/UX 分析（用户提供第三方拼豆游戏录屏 372s/220×480）
- 产出：`games/beads/design/references/ref-video-2026-09-17-ui-ux-analysis.md`（九节 + 关键帧 9 张存 `design/references/frames-20260917/`）。
- 方法：ffprobe/ffmpeg（`/opt/homebrew/bin/ffmpeg`，**不在 PATH**，用绝对路径）均匀抽帧 37 张 + `select='gt(scene,0.35)'` 场景检测 + crop/neighbor 像素级放大。
- **最重要发现**：参考游戏把「目标色」做成**每格始终可见的淡色垫**（空槽=目标色浅版；已放珠格周围留 ~1px 淡色缝）⇒ 玩家靠「珠色 vs 周围淡色」辨错位。**这印证了 T-140 裁定 (b) 的方向，但载体是「垫色显缝」而非「描边环」** ⇒ 已提实现变体 **B′**（同价 +156 图元，不触碰「must not read as a bead」、噪声更低、与 empty 坑语言同源），待用户拍板是否切换实现（语义与裁定不变）。
- 其它可借鉴：结算屏「成品图展示」（缎带 + 白板 + 金币 + 主页/下一关/分享）、托盘扩展胶囊「▶ 增加2行」、道具「圆盘+绿+徽章」语汇、HUD 右上「重开/更多」双胶囊、开场揭示动画。
- 不照抄：33 槽大托盘（与 v1.23 6 槽冲突）、低对比淡紫文字、多处广告位、清盘式开局、珠面点状纹理。
- 待确认 5 项（左侧竖向滑块用途、结算期红色徽章组合、托盘行数关系、珠面纹理实现、结算期倒计时用途）。

## 高清参考录像复核（同一条录屏 1284×2778 @3x 版，用户提供）
- 产出：`design/references/ref-video-2026-09-17-ui-ux-analysis.md` **追加 §10 高清复核**（§0–§9 保留低清原版，§10 优先）；关键帧 12 张存 `design/references/frames-hd-20260917/`。
- 素材参数：1284×2778 @3x ⇒ **逻辑分辨率 428×926 pt**（上一版按 220×480 推的绝对尺寸全部作废）；120fps/16970 帧。
- 方法：ffprobe + scene 检测 18 个切换点 + 22 张全景 + 8 组 neighbor 10–16× 裁切 + **raw RGB24 逐像素 run-length 扫描**（脚本 `/tmp/refhd/scan.py`）。
- **★★★ 最重要结论（实证）**：参考游戏的谜面可读性 = **每格铺「目标色垫」（浅版目标色、铺满整格、平底无锥面）+ 珠子比格子小（珠 44px / 格 64px / 缝 20px ≈1/3 格）⇒ 四周露垫色；判据 = 珠色与垫色同色系即正确、异色系即错位**。取证：绿珠配黄绿垫（正确）／粉珠配米白垫（错位）／黄珠配珊瑚红垫（错位）／青珠配米白垫（错位）。
  ⇒ **T-140 裁定 (b) 的语义获行业实证，但载体应换成 B′（垫色显缝）**；另：开局揭示动画里"珠子淡出后留下的正是色垫" ⇒ 色垫是独立渲染层，进一步支持 B′。**待用户拍板是否切 B′**。
- **4 处修正**：① 托盘实为 **3 实+1 虚 × 8 列 = 24+8 槽**（原误记 33）；② 托盘空槽是**「素坯拼豆」造型**（米白珠状）非凹坑；③ 扩展按钮 = **1 行淡紫虚线槽 + 居中叠压白胶囊「▷ 增加2行」**；④ 开局动画 = **图案以珠子+色垫逐层显现/淡出**（非"满盘飞离"）；另"珠子点状纹理"作废 → 实为**中央四棱锥（方钻）锥面**。
- **新增（上版完全漏掉）失败页**：HUD 转结算态（金币/红徽章/倒计时/绿+）+「关卡已完成 73%」+「就差一点」+ 沙漏 +180s 胶囊 + 「▷ +180s」+「按住屏幕查看关卡」。**续时量级 180s ⇒ 提示我方 v1.23 续时预算可能偏小**，值得复核。
- **新发现机制**：托盘珠**按色批量发放**（第3关 t=182 全黄珠、第4关 t=120 全深棕 24 颗、开局为空）⇒ 参考是"按色供给源"，与我方"取回错位珠暂存区"语义冲突，**不照抄**。
- **仍未确证 6 项**（左侧放大镜+竖滑轨、HUD 右上「手柄+」「•••|◎」、红徽章 8/9、结算期倒计时递减用途、托盘行数、锥面是否程序化）。

## WXG-T-141：参考竞品四项用户裁定落盘（托盘 6→24 槽等）
- 用户对参考竞品待确证项拍板：①缩放只做双指捏合手势（无滑轨控件，Should）；②HUD 不设「更多游戏」；③失败页 = 归零→看广告续时或退出（与现稿一致零变更）；④**托盘定案 24 槽（固定 2×12）+ 广告扩展 24（+2 行）——推翻 v1.23 Q9 的 6 槽**。
- 落盘：systems-index **v1.24**（§3.4 两常量 + 4 行态几何重验注记）、tray-spawner v2.1、concept v1.3（D13）、input-control v2.2、ux-spec **v1.6**（补记 v1.5 漏升版漂移）、参考文档 §8 四项回填闭环。零 §3.5/§3.11 变更。
- 诚实记录：容量是手感旋钮非难度闸门（2 槽即可滚窗解任意置换），v1.23「小托盘滚窗」论述作废；24 槽 = 从容批量取回，难度仍全押 k × 环长分布。
- 遗留：①4 行态带高/btn_expand 热区重验（art/ux 新单）；②**L11 载体 B′（垫色显缝）vs (b) 描边环仍待用户拍板**；③REVIVE_BONUS_SEC 60s vs 竞品 180s 待 playtest。

## WXG-T-142：L11 载体切换 B′「目标色垫·垫色显缝」（用户拍板）
- 语义不变（珠色≠垫色即错位），载体由 (b) 描边环改为：珠层下全格垫 roundRect（ink 与 empty 坑 S1 同源 `mix(target,#000,0.30)`，零新 hex）+ **珠视觉内缩 `BEAD_DRAW_INSET=2`**（视觉 46/50 露 2px 缝；`BEAD`=50 格/热区不动 ⇒ 绕开而非推翻 (a) 的冻结驳回，已诚实记录）。
- 落盘：assets-spec **v1.5-r5**（§1.1/§1.2/§1.8）、accessibility §5.4（(b) 留档划线 + B′ 终裁条）、bead-grid L74、草稿 HTML ① 重出（B′ 样张 + 24 槽托盘）。基线 1828 不变；缝宽 8% vs 竞品 31% 待 playtest。
- 后继：渲染层实现单（view-model pattern colorIdx 核实 + tuning 常量）。

## WXG-T-145：G1 `vfx_fill_pop` 落座回弹落码（T-128 动态质感章 落码①·首张代码单）
- 触发：`src/` 内 `FILL_POP` grep 零命中 ⇒ 用户原始要求「有质感 + **及时的互动**」仍全在纸面。用户**两次取消我对 art-director 的派单** ⇒ 解读为「别再 spawn 美术、直接推进」，本单由我亲自落码。
- 落码 5 文件 + 1 测试（详见 `TASKS-DETAIL.md ## WXG-T-145`）：`tuning.ts`（9 常量，表现层 ⇒ 不进 §3）/ `state.ts`（快照 3 单调标量）/ `bead-render.ts`（`FilledBeadOptions` +4 字段、`fillPopEnvelope()` 纯函数包络、D1 分支）/ `view-model.ts`（循环外 `pop` 槽 + `isPop`）/ `beads-game.ts`（`_placeFx` 骨架 1:1 照 `_wrongFx`，三处 `bead:placed` 后 arm）。
- **最大陷阱（v1.5-r6 层序死结论）**：`outer` 同时驱动 L11 垫 ⇒ `scale` 只能乘珠体 `(outer − 2×INSET)`，**严禁乘 `outer`**，否则 120ms 内自己抹除目标色谜面。测试里把「垫恒 50×50 不随 scale」写成硬断言钉住。
- 自纠 3 处：① `const lerp = (a,b)=>…` 局部闭包 = 逐帧分配 ⇒ 改模块级 `lerpMix`；② TS2540 ×5（options 全 `readonly`）⇒ 改三元构造字面量，其间一次 SearchReplace 因 original==new 成 no-op 并把 `}` 挤行，随即修正；③ 我自己的单调性用例首帧误判（`last = -Infinity` 让 p=0 必判「上升」）⇒ 是**断言初始化错**，非实现错，加「首帧跳过方向判定」修净。
- **A/B 对照归因法（本单最值钱的流程产物）**：中途全量跑出 **66 红**，我先按失败签名（`placeColor` 返回 false / `fillBoard`=0）推断属并发会话的 `tests/helpers.ts` 空盘迁移，但**不接受推断当证据** —— 备份 5 文件 → `git checkout HEAD --` → 跑基线 **315 绿** → 接回改动 → **328 绿（+13 零回归）** ⇒ 坐实「66 红 = 并发会话提交前中间态」。判例：**多会话同仓时，红的归属必须 A/B 实测，签名吻合只是线索**。
- 提交归属异常：我的改动**第二次被并发会话连带提交**（本次进 `dbb3c1c`「WXG-T-139 补遗」，消息未提 G1）。处置 = 按台账注 2 判例：不回改已入 HEAD 的提交信息，以台账 `WXG-T-145` 行为准；本会话至今零提交。
- 派单前自查再次必要：我上段拟派的 v1.5-r6 两件事（24 槽几何重验 / B′×§1.6 交叉重算）**已被并发会话做完**，且我的「4 行态 `panelH 234 > 220` 溢出 14px、与 `btn_expand` 热区重叠 88px」验算被 v1.5-r6 §1.3.2 逐位采纳（V1–V4 候选，V1 = 2 实行 + 按钮收起 + 瞬时切换，**待用户拍板**）。
- 遗留：① G2′「解环器逐颗 80ms 错开」< G1 重启门 120ms ⇒ G2/G2′ 单必须单独处理（已写进 `beads-game.ts` 注释）；② `[待真机]` 观感与帧开销（Cocos 构建未接入）；③ `[待 playtest]` 谷 0.96→0.92 或 `INSET 2→3` 两候选，本单按现行冻结规格实现未改幅度；④ 剩余落码单：②G2/G3/G4（含面板延迟 800ms + `lodLayers=6`）、③G7 轻压 + `sfx_denied` **音频原子批**（§3.12 19→20 五项同批）、④G6 彩带、⑤G5 = **T-132**（跨 framework，前置 = `FIXED_WIDTH` 下整屏 1.015 是否露黑边）。

## WXG-T-146：T-128 落码② G3 扫光 + G4 过关波浪（含裁定 1 面板延迟门）

- 范围：G3 `vfx_powerup_sweep`（§1.6.3）+ G4 `vfx_complete_wave`（§1.6.4）+ G4 LOD 降档通道 + **裁定 1 的结算面板延迟门**；本单零新造时长、零 §3 变更（毫秒真源仍 ux-spec §5）。
- **本单最值钱的流程产物：单常量因果实验推了自己的预判**。开工时我判「10 红大概率是面板延迟 800ms 撞既有测试」——**方向对、机制错**。两跳 A/B（换回 HEAD 版 `view-model.ts` 仍 10 红 ⇒ 与我无关）后，把 `CLEAR_PANEL_DELAY_MS` 改成 0 再跑：只救回 **1** 例 ⇒ 真机制是「面板不再与 `LEVEL_CLEAR` **同帧**可见」（`open()` 从同步 `onEnter` 移到 `_stepLevelClear` ⇒ 延迟 0 也要 ≥1 个 tick），影响四类读数：可见性 / 按钮命中 / `sfx_panel_in` 时机 / 绘制命令。判例：**红的机制假设要用最小变量实验定，不要用版本回退碰运气**。
- ⚠️ **P0 陷阱 #2 从「规格警告」变「已修缺陷」**：§1.6.1（v1.5-r6）点名 `bead-render.ts` 的 L11 垫 rect 用 `y = cy + lift` ⇒ **`lift` 会带动垫**，旧护栏是「view-model 不传 lift ⇒ 零实害」。我的波浪（`lift = dy`）与并发会话 T-148 锚组（`lift = -6`）**双双打破**该前提 ⇒ 垫改锁格心 `cy`。修后既有套件仍全绿 ⇒ **335 例对此零覆盖**（真覆盖缺口，已由 `scene-vfx.test.ts` 补上硬断言：同 `lift` 下垫 y/h 一字不动、珠体确实上移）。
- 「**0 = 未激活**」约定（与 G1 `placeProgress` 同族）：`sweepProgress`/`waveProgress` 上弦**当帧**读 0，视图守卫 `<= 0` 跳过首帧 ⇒ 代价 = 一帧 16.7ms（此时斜带主体仍在屏外），**非缺陷**。我自己的测试先把它当 bug 报，查后改为**修断言到真实语义并写明代价**，没去改实现迁就测试。
- 派生常量不自造字面：`SWEEP_Y_MAX = HUD_BAND.yMin`、`CLEAR_PANEL_DELAY_MS = WAVE_MS`（均为派生）；齿轮坐标正本 = `GEAR_HIT_SIZE / 2` + `(HUD_BAND.yMin + yMax)/2`（我先误写了道具带的 195）；`RectCommand` 字段是 `x,y,w,h`（**不是** `width/height`）。
- 判据迁移入口单一真源：`tests/helpers.ts::advancePastClearWave(harness)`（内部 `CLEAR_PANEL_DELAY_MS / 1000 + 0.1`）⇒ **测试里零字面秒数**；例外 = `audio-dispatch` 需帧末 flush，直接 `tick(c, …)` 不走 helper。意图零软化、旁路例零删除；最终 **350/350**（+15）、tsc 0 错。
- **跨会话三项事实（均已登记）**：① 我的 G3/G4 六件 src 被并发会话 `3359c25`（T-147）**连带提交**且消息未提（第三例；`667d2c5` 连我的 T-145 台账也扫走）；② 那笔 HEAD 当时 **10 例红**，而 T-147 详情记「全包 335/335」⇒ **假绿登记**，纪律回写：「全包绿」必须在**提交后的 blob** 上跑（`git stash` / worktree 隔离）；③ `667d2c5` 用了 **T-148** 号但主表/头注/详情三处均无 ⇒ 已由本单代登记（`check:tasks` 的行/节配对门禁把这个遗漏**正确报错**了，门禁有效）。
- 台账双绿：`check:tasks` OK（14 行 / 14 节，含本会话代补的 T-148）、`check:links` OK。本会话仍**零提交**（用户纪律：不擅自 commit）；工作树内同时存在并发会话的在途改动（`save-schema.ts` v3 `onboarded` / `REVIVE_BONUS_SEC 180`），**不得被我扫走**；收口时连跑全量从 350 绿演变到 **353 passed / 1 failed**，唯一红据实码定到其 `beads-game.ts` 新增的「每次 `placed` 都 `save()`」打掉 S8 零写隔离——**共享工作树里的红必须当场定归属并写进台账，不要口头说「与我无关」**。
- 遗留：**G2′ 相 A 未落码** —— `slot_border` 状态环 × T-148 `selectableRing` 恒亮白环**同格双环**（solver 目标必为错位珠），三个候选（互斥 / 合并 / 改墨）待用户或美术拍板；另 `vfx_clear_dissolve`（G2）自 v1.22 道具反转起为**失效规格**（已在 TC-PER-11 标注）；G5 = T-132、G6 彩带、G7 音频原子批各自另单。`[Probe]`/`[Cocos]`/真机 全部未跑（K-037：不得引用为屏幕层 PASS 证据）。

## 「开始任务」= 渲染层实现单核验（2026-09-17 晚）
- 上会话承诺的「B′ 渲染层实现单」经查**已由并发会话 WXG-T-143 完成落码**：`tuning.ts`（BEAD_DRAW_INSET=2、TRAY_BASE/EXPAND=24、TRAY_BAND v1.25 yMin 216）、`bead-render.ts`（padColorIdx：垫走 outer 不参与 scale/lift，内缩仅有垫时）、`view-model.ts`（filled 格 padColorIdx=cell.colorIdx + T-148 白环/组抬起）、`beadEndpoints.edge = mix(base,-0.3)` = 规格 ink。托盘不带垫 ✓。配套提案 `design/proposals/tray-24-layout-derivation.md`。
- 本会话做了规格符合性核验（零代码改动）：B′ 实现与 T-142 规格逐条一致；渲染测试（bead-render/view-model/scene-vfx/fill-pop）23+ 全过。
- ⚠️ 全量测试 15 红/339 绿：失败全部是**托盘 12→24 槽适配的旧判据连带**（满槽禁取、btn_expand 几何 132×88、solver 行主序、restart 重置项、满槽描边几何/呼吸）——属 T-143 在途施工的过渡态，判据待按新几何更新，**非 B′ 渲染回归**。worktree 未提交改动（tuning/beads-game/state/scene-vfx/view-model）= 并发会话在途产物，未触碰。
- 后继：T-143 会话需把 15 个旧判据更新到 24 槽/新面板几何口径后跑绿再提交。

## WXG-T-150：G2′ `vfx_solver_restore` 落码（T-128 落码③·裁定「甲」两则）（Qoder 主理人会话 · 晚）
- **相 A 是玩法提交时刻，不是观感时长**：用户拍板「归位延后到 200ms 后」⇒ `SOLVER_HINT_MS` 决定 `bead:placed` / 过关判定 / 托盘可取状态的**实际发生帧**。判例：这类常量**不能**挂到 `_update` 尾部「不被 PAUSED 冻结」那一串表现层槽上，必须走 `playing` 相位守卫（`paused` 冻结不作废、其他相位作废、`_setupLevel` 作废、**过关判定排序列末**）。三处（tuning 头注 / game 字段注 / assets-spec 落码注）都写了这句话，防后人当纯表现层常量改。
- **80ms vs 120ms 门冲突靠「不走同一条通道」消解**：相 B 若复用 G1 单槽 `_armPlaceFx` 就会撞 `FILL_POP_RESTART_GATE_MS`（逐颗 80ms 错开 < 单颗 120ms 包络）。正解 = 独立逐颗队列 `_noteSolverLand`，每颗一份完整包络、交换一步的两格**共享同一 step**；G1 那个门**一字未动**。
- **实测推翻规格的「净 +3」**：§1.6.2a 立项时前提是「错位珠无恒亮标记」，T-148 上线白环后点名格变成**白环 ↔ 相 A 环 1:1 互换** ⇒ `countRects(snap) === countRects(off)` 差分钉死**净 +0** ⇒ §1.8 包体基线零变动。**图元增量类规格值必须差分复算，不能沿用立项推算**。
- **缩进 churn 自查法**（本会话自己造的坑）：`scene-vfx.ts` 被我整文件写成 4 空格（`.editorconfig` 正本 `indent_size = 2`）⇒ 看 `git diff --stat` 与 `git diff -w --numstat` 的差值；两者差距大 = 纯空白污染，`git checkout --` 复原后按该文件既有缩进重贴。`tests/scene-vfx.test.ts` 193/193 同法判定为纯 churn，零内容损失。
- **误 checkout 的复原路径**：`games/beads/cocos/assets/scripts/**` 是 `pnpm run framework:sync` 的**单向产物**，被误 `git checkout` 后跑一次 sync 即复原（本会话在同一条命令里多带了镜像文件，代价 = 重跑 sync）。纪律：**复原类命令一次只点一个确定属于自己的路径**。
- **红点归属第四次实测（共享工作树）**：本段起手 9 红，收口时只剩 **3 红**（并发会话 T-143 在此期间自己推进了判据迁移）⇒ 报数前**必重跑**，不要引用上一段的数。3 红逐条读断言确认全属托盘 24 槽在途（`btn_expand` 热区 275 vs 304、`tray.capacity` 12 vs 24），本单 hunk 零涉及 `TRAY_*`。
- **提交边界**：`tuning.ts` / `beads-game.ts` / `view-model.ts` / `memory` 本会话与 T-143 **同文件交叉** ⇒ 整文件 `git add` 必扫走他人未收口改动；本会话**零提交**，等用户发话并逐 hunk 核对。
