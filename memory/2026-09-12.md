# 2026-09-12

## 专家团 vs my-agents 跨平台复用评估（调研结论）
- 三 IDE 均原生支持项目级 subagent 目录：CodeBuddy `.codebuddy/agents/`（项目级优先级最高，字段 name/description/tools/model/permissionMode/skills/color）、Cursor `.cursor/agents/`（字段 name/description/model(fast|inherit)/readonly/is_background）、Qoder `.qoder/agents/`（name/description + 正文为系统提示；优先级 Built-in<User<Project<Plugin<--agents）。
- my-agents 现有 7 个 agent frontmatter 为 name/description/model: inherit/readonly，三平台均可识别；但缺 `tools` 白名单（Qoder 靠 tools 限权，readonly 未必生效）、CodeBuddy 的 `permissionMode`、以及 `skills:` 自动加载字段（目前只在正文写必读 skill）。
- 关键差异：插件专家团的 lead SOP 是"系统级注入主对话"；my-agents 的 studio-orchestrator 是"可被 spawn 的 subagent"。无插件环境下复现需把主理人纪律写进 AGENTS.md / alwaysApply 规则，否则主对话不会自动按九阶段 SOP 走。
- 待实测：四 IDE 对 `agents/` 内**符号链接**的识别率（Qoder 可 `qodercli agents list` 验证）；不识别则改为复制 + 同步脚本。

## 实施纠偏（核对后落地）
- **不做**：往共享 frontmatter 塞 `tools:` / `permissionMode:` / `skills:`（Cursor 官方不认；其它家方言未 CLI 实测）。
- **已做**：`AGENTS.md` §2「主理人触发条件」为**条件式**（跨域/新游戏/发布 → 阶段 0 或 `@studio-orchestrator`；单域小改直调），四 IDE rules 摘要同步；非无条件注入主理人人格。
- **已做**：`check:links` 改为扫描 `my-agents`/`my-skills` 真源；验 frontmatter（name/description/必 readonly）、INDEX 表、编排路由表、四 IDE 链接。
- **符号链接**：Cursor 委派链路已实测跟随 symlink；CodeBuddy/WorkBuddy/Qoder 仍待各家 CLI 验证——不默认改复制。
- description 去重：文策渊偏 UX flow；林绘澄偏视觉 a11y / art bible。

## Rules 合并到 my-rules
- 正本：`my-rules/agents-md.md`（frontmatter 并集：`alwaysApply` + `enabled` + `trigger: always_on`）。
- 四 IDE 原各自一份摘要 → 改为相对符号链接；改摘要只动 `my-rules/`。
- `check:links` 已验规则链接指向 `my-rules/agents-md.md`。

## Review 通过并落地（用户自改，主理人 review + 提交）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-12-s04.md`（5166 tok｜原 L21–61｜隶属标记见该件 H1）。
## PR #1 合并闭环完成（13:50）
- PR #1（develop→master）已于 12:58Z 确认 MERGED，16 项检查全绿（含 Headless PR Review 3m24s 通过）。
- 评审门链路修复链最终态：CURSOR_API_KEY 配置 → pr-review.yml 全量 fetch（弃 depth=1）→ ci-pr-review.sh merge-base 两点直比兜底 → --trust 显式信任（3e1fa57 补回）→ ctx:build 刷新索引（5e93439）→ 并行会话 ARG_MAX 修复（15d039a）后全绿。
- develop 分支后续工作继续走 develop→PR→master；下一步候选：D-03 口径决策、冲刺模式工程实现派发。

## 冲刺模式工程实现派发（21:10）
- 关键事实确认：`games/beads/src` **零代码**（architecture-beads §9 记为「⬜ 未开始」），`games/breakout/src` 为成熟参考实现（同框架/同 harness/同目录风格），`packages/framework` 已冻结（226 单测）。
- QA 侧对冲刺模式零覆盖（grep `sprint|冲刺|combo:|连击` 在 production/qa/beads/ 无命中）。
- 台账领号：WXG-T-027（工程实现）+ WXG-T-028（QA 用例扩展）已写入 production/TASKS.md，下一可用号 → WXG-T-029。
- 并行派发（文件集不重叠，安全）：
  - engineering-lead-3 → WXG-T-027：beads 冲刺模式垂直切片（包脚手架 + config 三件套 + S1 FSM + S3 网格/落子 + S4 托盘/供料 + S2 输入 + S5 计时 + S7 冲刺连击/爬梯/计分 + Game 装配 + view-model + harness + vitest）。范围排除：S6 道具、S9 面板、Cocos 工程、元游戏。里程碑（普通模式最小闭环）回报一次。不提交，等用户批准。
  - quality-lead-2 → WXG-T-028：test-cases.md v1.2 §B 冲刺 11 条判据 1:1 映射（TC-SPRINT-01..11）+ smoke-tests 冲刺段。docs-only，仅动 production/qa/beads/**。
- 决策依据：A+ 路线把冲刺模式定为主线钩子（快节奏单局 120s），故首个实现冲刺必须同时落地 P0 骨架（否则无珠可落、无法成局）；判断为「冲刺模式垂直切片」而非「仅计分内核」或「先普通模式 13 Story」。

## WXG-T-028 交付 + 冲刺事件 payload 真源裁定（21:25）
- 严守真（quality-lead-2）交付 T-028：test-cases.md v1.2（新增 §C 11 条 TC-SPRINT-01..11，判据 50→61，用例 73：Node 65/DevTools 6/Device 2）+ smoke-tests.md v1.1（SC-SP-01 五步冲刺冒烟）。docs-only，§A 既有 50 条零改动。
- **发现真源分歧（已裁定）**：score-combo.md §2.5 的冲刺事件 payload 与 systems-index.md §4 登记定稿不一致三处。裁定 = **事件 payload 以 §4 为唯一真源**：
  - `combo:up {streak, multiplier, tier}`（§2.5 原写 {mult, streak}）
  - `sprint:stage {stageIndex, nextParams}`（原写 {index}；stageIndex = 新 stage 索引）
  - `sprint:ended {score, bestStage, settleScore}`（原写含 reason；**去 reason**——sprint 结束唯一原因=倒计时归零，无可分辨性）
- 两路同步：engineering-lead-3 收到 S7 断言口径（按 §4）；design-strategist 收到 score-combo §2.5/§8.6/§8.11/头部回写任务（机械一致性修订，非数值变更，不涉 §6 变更记录）。
- 其余两条 QA 发现处理：① 新增节命名 §C（v1.1 的 §B 已被派生用例占用）——认可；② test-cases v1.1 过期标注（TC-CONST-05「无 S7 GDD」）按「追加不覆盖」原则加 v1.2 注登记——认可。
- 关键方法论：**跨文档分歧不自行改真源，上报主理人裁定后再机械回写**——本轮 QA 侧正确执行，未擅改。

## score-combo 事件 payload 回写完成 + 独立核验（21:40）
- design-strategist 完成回写（7 处，仅 score-combo.md；未 commit）：①§2.5 四事件 payload 对齐 §4；②§8.6 `sprint:stage{index+1}`→`{stageIndex}`；③§8.11 消歧 settleScore；④头部清「待主理人回写 §1」悬置；⑤⑥⑦ 联带清理「不进 §4 / 待登记 / 提案事件」陈旧措辞。
- 独立核验（非只信汇报）：§2.5(54–55 行) 与 systems-index §4(199–202 行) payload **逐字符一致**；残留词计数 0；git status 仅 score-combo.md 一个设计文件被改，systems-index 零改动（§3.10 C1–C8 完好）。
- §6 变更记录判定：**无需登记**（消费侧文档向既成真源对齐，真源未变；数值零变更）。采纳。
- 方法沉淀：跨文档真源分歧 → 裁定后**机械回写消费侧**，不产生 §6 噪声；验收必须独立核验（grep/读原文）而非采信汇报。

## WXG-T-027 里程碑 + 3 项实现裁决（21:55）
- 工程侧（engineering-lead-3）里程碑：step 1–6 完成（包脚手架三件套 + config 三件套 + entities/grid+tray + systems/placement+spawner+timer+sprint + game/state+save-schema+beads-game + view/palette+view-model）。15 源文件 / ~2977 行。
- **独立核验**：`games/beads/node_modules/.bin/tsc --noEmit` exit=0；目录结构与 architecture-beads §3 一致。
- 三项裁决：
  1. **推翻**「C6 cells 预算值原样进 nextParams」→ 裁定 `nextParams` 必须报**实际装载值**（cells=池图案 fillable、colors=重映射后实际、spawnInterval=C6 公式值）。理由：§4 定义 nextParams 为「换 stage 参数注入」，报预算值等于 payload 说谎。C6 `min(30+10n,156)` 语义裁定为**上限预算/选池约束**（与 §2.3「图案池循环取用」字面冲突，二者不可能同时精确成立）。
  1b. 建议（成本自裁）池图案选择改「色数适配优先」——避免前中段多色并图压扁（阶 1–5 色数预算仅 3–5，池内 L5=6色/L6=8色/L7=8色/L8=8色）。
  2. **确认** `settleScore` = 单局分本身（beads-game.ts:684 `settleScore: score`）。
  3. **采纳** stage 切换托盘清空——noop 死珠 + MVP 无弃珠机制（S6 不在本轮）会永久占槽。同时暴露真源缺陷：score-combo §6 称"托盘是否清空见附录 C8"，但 **C8 无此规定（悬空引用）**。
- **两项待回写设计项（收尾统一派设计侧，不阻塞工程）**：
  - D-1：score-combo §2.3/§9 C6 —— 写明"cells 为上限预算，实际取池图案可填格数"。
  - D-2：score-combo §6/§2.3 —— 明确"stage 切换托盘清空"（消解悬空引用 C8）。
- 下一步：工程侧 step 7–9（tests → harness → 全量 test/typecheck/check:arch）。

## WXG-T-027 完成 + 全量独立验证（22:20）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-12-s10-t027.md`（605 tok｜原 L105–118｜隶属标记见该件 H1）。
## T-027 提交推送 + 环境事故修复 + WXG-T-030 派发（23:15）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-12-s11-t030.md`（706 tok｜原 L119–130｜隶属标记见该件 H1）。
## WXG-T-030 完成与裁决（23:50）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-12-s12-t030.md`（706 tok｜原 L131–142｜隶属标记见该件 H1）。
## WXG-T-030 提交与 T-031 真源对齐（23:56）
- **提交 `62ad9c5`**（第一笔·工程侧，已 push develop）：8 文件 +1195。pre-commit `check:links` OK、commitlint 通过。
  暂存用精确 pathspec 排除并行会话未提交的 `ctx/*`、`knowledge/*` 与 `.codebuddy/plans/`。
- **文档侧发现真源硬伤（已领号 WXG-T-031 派 design-strategist-2）**：`timer-gameover.md:73` 仍写「同帧先结算归零」，
  与已裁定的帧内序（输入 → 连击窗 → 供料 → 计时）互斥。成员进一步论证「事件到达序」在单线程固定步长循环内
  **不可观测**（一帧内没有先后事件，只有代码执行序）→ 凡以「谁先到」为基准的判据都不可测。
  裁定：全部立项，timer-gameover §6 对齐 + pause-settings §8-6 换基准为「同帧/跨帧」+ §2.3 注记更新。
- 提交节奏（用户拍板）：先提代码 → 文档等 T-031 落地后一笔 → memory 日志单独一笔。
  **进度：1/3 已推送；文档与日志待 T-031。**
