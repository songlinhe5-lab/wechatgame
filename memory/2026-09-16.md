# 2026-09-16

## WXG-T-121：装置自测分档挂门禁（BD-39 · BD-40，Qoder 主理人会话）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-16-s01-t121.md`（764 tok｜原 L3–10｜隶属标记见该件 H1）。
## 主理人复核 WXG-T-119（并发会话施工 · 只读）+ BD-40 撞号消歧

- **为什么不接施工**：主表那行仍写 📋，但工作树实况是并发会话已跑到终轮（探针 1148 行未跟踪、报告已追加 §25 + §25.12 + §25.13、
  `TASKS-DETAIL.md` 最后写入 07:44:49 = 我动手前 27 秒）⇒ **不重做、不代提交、不跑会覆盖 evidence 的探针**（其 `--log` 默认名正是被覆盖的那个）。
- **只读复算结论（三条）**：① 官方轮 `evidence/beads-browser-probe-t119.log` 汇总块逐条数出 **PASS 12 ｜ PASS* 10 ｜ FAIL 1（CLK-01）｜ ⛔ 4 ｜ ⊘ 2（P4S-01b、P4S-03）= 29 条**，
  与 §25.13 自报计数**逐项一致**；② 「两轮判定串逐字一致」亦成立 —— 与 `-rerun.log` 对比 29 条中**只差 1 行**（`P4S-01b` ⊘↔PASS），与其声明相同；
  ③ **两点存疑被证据推翻**（如实撤回，不当发现报）：我以为跨轮 stdout 不可复核（其实按轮另存 `-t119.log` / `-rerun.log`）、以为 §25.13 引用的 20:42Z 轮缺失（那正是保留的默认名 log，且它是以**脚本级 TypeError 崩溃**收尾的中间轮，非正证）。
- **一条卫生项**：`evidence/beads-browser-probe.log`（04:42 · 尾部 `[脚本级错误] TypeError … supportMs`）与官方轮同名前缀混放 ⇒ 后人极易误读为正证；
  建议成员侧改名 `…-crashed-204208Z.log` 或删除，`evidence/diag-t119-a05.mjs`（诊断脚本混在证据目录）同理。**未代改**。
- **撞号消歧（本笔唯一落盘动作）**：成员把 CLK-01 发现记作 **BD-40**，而 BD-40 已由 `WXG-T-112` 入库为「`ctx:check` 未挂 verify」⇒ 以 blob 法补登 **BD-41** 为正本行并在行内写明消歧（`d549afc`）。
  **连带后果已成形**：并发会话据此写了 `WXG-T-122`「宿主驱动权互斥（BD-40 修复）」任务书，其背景段称「`WXG-T-121` 已把 BD-40 挂上门禁」——
  **此句失真**（T-121 挂的是装置门，与双驱动无关）⇒ 该单动工前须由主理人/其会话订正。沉淀 **K-050**。

### 追记（同日 08:14 复核，上面两条判断已被后续事实推翻，据实修正）

- **卫生项作废**：`evidence/beads-browser-probe.log` 于 **07:43:48 被并发会话复跑覆盖** ⇒ 那个 04:42 崩溃轮已不在盘上（我引用的对象消失，非我读错）；
  现在这个默认名 log 是**修复后**的正证轮（`CLK-01 = 0.999`、计数 PASS 13 ｜ PASS* 10 ｜ FAIL 0 ｜ ⛔ 4 ｜ ⊘ 2 = 29）。`diag-t119-a05.mjs` 仍在证据目录，未代改。
- **「动工前订正」窗口已过**：并发会话已把 `WXG-T-122` 施工到自证完成（`App.startHostDriven()` + 三道互斥守卫）并另开 `WXG-T-123`（QA 复测 `CLK-01` + 勘误 §25.13）。
- **消歧尚未闭环**（这才是留给后续的事实）：§WXG-T-122 背景段「`WXG-T-121` 只把 BD-40 挂上了检测门禁」仍在（`TASKS-DETAIL.md:1452`），
  探针 `CLK-01` 明细行也仍写「登记 **BD-40**」⇒ 正本号是 **BD-41**（`d549afc`）。台账文件此刻仍在热写（`TASKS.md` mtime 08:09:10）⇒ **不代改**，只在此留待订正清单。
- **清单已登为可领待办 BD-42**（用户拍板「甲」）：一行涵盖三处订正（背景段失真句 / 探针旧号 / `evidence/diag-t119-a05.mjs` 移出证据目录），
  提交 `757f07a`。施加时 `--worktree` + `--stage` 两模式双跑：blob 入索引只提交这一行增量，工作树同步插一行 ⇒ 对方后续整文件 `add` 不会冲掉本行（K-045 正解，上一笔 `d549afc`
  因提交时带了 pathspec 而把对方工作树内容捎入，已核实无源码文件受波及，但口径以此笔为准：共享台账提交 **不带 pathspec**）。

## WXG-T-124：beads 美术 v1.3「丙案·双色温对撞」风格单（Qoder 主理人 + 林绘澄 art）

- **用户裁定链（原话权威）**：起因「review 美术设计，对标行业领先，需独立风格与质感」。主理人三方对账（规格×代码×真机帧）得 F1–F8 后，用户拍：
  风格=**丙**（双色温对撞：冷紫灰底 × 暖光 band）；落地=**丙**（派林绘澄 art-director 出 v1.3 草案 → 主理人复核 → 用户拍板再落码）；F5=**甲**（幽灵符号可见性立独立冻结变更单，与风格单解耦）。
  四项执行拍板全选**甲**：① 落码节奏=先落不撞车两文件（palette/bead-render）；② 现在提交 art 三件套；③ F5 现在领号立项（=T-125）；④ 截图移 evidence/ 留档。
- **主理人复核 PASS（非橡皮图章）**：独立复算 a11y 对比度 #6E7288/白=**4.74:1**、#2A2E43/白=**13.4:1**（诚实，非再度假绿）；冻结面全守住；渲染原语仅 rect/circle/line/text/polygon（软高光/暖光 band 靠叠层递减 α 模拟，无渐变/贴图）。
- **本轮实际落地**：F1（palette 冷底 7 值）、F4（珠子六→十层卡：L0a 接触阴影/L3b rim/L4a-c 软高光）、F7④ 半边（textDim #8B8578→#6E7288 严格更暗，闭合白底标签 a11y）。三笔提交：`adc1520`（台账 T-124/T-125 领号，blob 法双施加不带 pathspec）、`b8e1e62`（art 三件套 v1.3）、`0c4f855`（palette+bead-render+test+2 cocos 镜像）。typecheck 0 + beads 全量 251 测试绿 + framework:sync 镜像一致。
- **延后到 view-model 阶段（因 view-model.ts/ux-spec.md 正被并发会话热写，本轮不撞车）**：F6 textAccent 逐处路由（view-model 消费 22×，语义混杂：选中点/连击光/星/按钮各应不同色，**盲改会误染 ⇒ 本轮 textAccent 值不动**）、F2/F3 容器板+暖光 band 绘制、F7 HUD 白胶囊/8齿齿轮/图标 re-route、F8 背景层次叠层、ux-spec §3 HUD 条款对齐（文策渊）。
- **踩坑（commitlint scope-enum）**：台账提交初写 `docs(production):` 被 commit-msg 钩子拒——**无 production scope**；台账类正本 scope 是 `docs(ctx)`（参照 BD-42 `757f07a` 惯例）。改 scope 后无 pathspec 重提即过。允许 scope 见 commitlint.config.mjs scope-enum。
- **三未决（待用户/后续）**：① success #3FBF6B 与珠色4同值是否走冻结变更；② 十层卡真机性能回退阀（合并 L4a+b 降 8 层，单帧最坏 +624 图元）是否预授权；③ T-125 F5 单需用户确认改值（EMPTY_GHOST_ALPHA 0.20→0.32 / EMPTY_TINT_MIX 0.35→0.42）后主理人串行落 systems-index §3.8，避免与并发竞写。
