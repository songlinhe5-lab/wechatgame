# 2026-09-16

## WXG-T-121：装置自测分档挂门禁（BD-39 · BD-40，Qoder 主理人会话）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-16-s01-t121.md`（764 tok｜原 L3–10｜隶属标记见该件 H1）。
## 主理人复核 WXG-T-119（并发会话施工 · 只读）+ BD-40 撞号消歧

- **为什么不接施工**：主表那行仍写 📋，但工作树实况是并发会话已跑到终轮（探针 1148 行未跟踪、报告已追加 §25 + §25.12 + §25.13、
  `TASKS-DETAIL.md` 最后写入 07:44:49 = 我动手前 27 秒）⇒ **不重做、不代提交、不跑会覆盖 evidence 的探针**（其 `--log` 默认名正是被覆盖的那个）。
- **只读复算结论（三条）**：① 官方轮 `evidence/beads-browser-probe-t119.log` 汇总块逐条数出 **PASS 12 ｜ PASS* 10 ｜ FAIL 1（CLK-01）｜ ⛔ 4 ｜ ⊘ 2（P4S-01b、P4S-03）= 29 条**，
  与 §25.13 自报计数**逐项一致**；② 「两轮判定串逐字一致」亦成立 —— 与 `-rerun.log` 对比 29 条中**只差 1 行**（`P4S-01b` ⊘↔PASS），与其声明相同；
  ③ **两点存疑被证据推翻**（如实撤回，不当发现报）：我以为跨轮 stdout 不可复核（其实按轮另存 `-t119.log` / `-rerun.log`）、以为 §25.13 引用的 20:42Z 轮缺失（那正是保留的默认名 log，且它是以**脚本级 TypeError 崩溃**收尾的中间轮，非正证）。
- **一条卫生项**：`evidence/beads-browser-probe.log`（04:42 · 尾部 `[脚本级错误] TypeError … supportMs`）与官方轮同名前缀混放 ⇒ 后人极易误读为正证；
  建议成员侧改名 `…-crashed-204208Z.log` 或删除，`evidence/diag-t119-a05.mjs`（诊断脚本混在证据目录）同理。**未代改**。
- **撞号消歧（本笔唯一落盘动作）**：成员把 CLK-01 发现记作 **BD-40**，而 BD-40 已由 `WXG-T-112` 入库为「`ctx:check` 未挂 verify」⇒ 以 blob 法补登 **BD-41** 为正本行并在行内写明消歧（`d549afc`）。
  **连带后果已成形**：并发会话据此写了 `WXG-T-122`「宿主驱动权互斥（BD-40 修复）」任务书，其背景段称「`WXG-T-121` 已把 BD-40 挂上门禁」——
  **此句失真**（T-121 挂的是装置门，与双驱动无关）⇒ 该单动工前须由主理人/其会话订正。沉淀 **K-050**。
