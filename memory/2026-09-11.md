# 2026-09-11 — 完成 Breakout 浏览器验证器 + 运行时预览/视频

## 完成的工作
- 修复 `Canvas2DLike.fillStyle` / `strokeStyle` 类型过窄导致真实 `CanvasRenderingContext2D` 不可赋值的问题（`packages/framework/src/adapters/canvas2d/canvas2d-renderer.ts`）。
- 为 harness tsconfig 补充 `DOM.Iterable`，解决 `NodeListOf` 无法 `for...of` 的编译错误。
- 实现零依赖浏览器 dev harness：
  - `tools/scripts/serve-harness.mjs`：用仓库自带 `tsc` 编译产物 + 120 行静态服务器。
  - `dev/harness/index.html` + `main.ts`：通过 `importmap` 映射 `@wxgame/framework`。
  - 已验证 HTTP 200、产物无裸标识符泄漏、浏览器模块图可解析。
- 实现运行时冒烟测试 `tools/scripts/smoke-harness.mjs` + 共享 `tools/scripts/lib/harness-runtime.mjs`：
  - 把浏览器将要加载的 `dev/harness/dist` 产物在 Node 里执行，用 DOM/Canvas 桩验证整条图能启动、能渲染、有真实绘制调用。
  - 当前输出：`boot phase = ready, level 1, board has 30 bricks, 80 draw commands`。
- 修复 view-model HUD bug：`BEST` 文字原来放在 y=1200，与砖墙顶行重叠；改为 `HUD.barY - 24 = 1222`，位于进度条和砖墙之间。
- 新增脚本：
  - `render-harness-frame.mjs`：从真实渲染模型导出 5 个关卡的 SVG 预览。
  - `render-harness-clip.mjs`：自动游玩并用 `@resvg/resvg-js` + `ffmpeg` 生成 MP4 游戏实录（已生成 L1/L3 预览视频）。
- 根 `package.json` 修正：删除指向不存在脚本的 `build:wx` / `check:size`，新增 `test`、`typecheck`、`harness`、`harness:smoke`、`preview:frames`、`preview:clip`、`verify` 等真实可用脚本。
- `pnpm run verify` 全绿：架构守卫、关卡同步、类型检查、425 个测试、harness 编译 + 运行时冒烟全部通过。

## 新增/修改文件
- `packages/framework/src/adapters/canvas2d/canvas2d-renderer.ts`
- `tsconfig.harness.json`
- `dev/harness/main.ts`
- `dev/harness/index.html`
- `dev/harness/README.md`
- `tools/scripts/serve-harness.mjs`
- `tools/scripts/smoke-harness.mjs`
- `tools/scripts/lib/harness-runtime.mjs`
- `tools/scripts/lib/render-model-svg.mjs`
- `tools/scripts/render-harness-frame.mjs`
- `tools/scripts/render-harness-clip.mjs`
- `games/breakout/src/view/view-model.ts`
- `package.json`
- `.gitignore`
- `dev/harness/preview/*.{svg,mp4}`

## 遗留 / 待后续
- 微信小游戏真机构建（`build:wx`）仍是占位，需要 Cocos Creator 项目配置或 CLI 接入后才能落地。
- 用户提到“游戏开发工作室专家团能否用到 codebuddy、模型选择受限”的产品级问题，尚未在代码层面回答，需在回复中说明。

# 2026-09-11（下午） — 跨 IDE Skill 沉淀方案 + WorkBuddy 模型配置修复

## 跨 IDE skill 沉淀结论
- Agent Skills（agentskills.io）开放标准已获 Cursor 2.4+/Codex/Copilot/Windsurf 等采纳，SKILL.md（frontmatter 仅 name+description）是跨 IDE 通用格式。
- 推荐架构：建 git 仓库 `~/Development/skills-repo` 作为唯一事实源，符号链接到 `~/.workbuddy/skills/`、`~/.codebuddy/skills/`、`~/.cursor/skills/`；其他 76 个 Agent 可用 `npx skills` 分发。
- 专家团插件格式不可移植；可复用的是其中程序性知识（GDD 八节模板、QA 门控、发布清单等），应随手沉淀为标准 SKILL.md。
- 现有 13 个用户 skill：game-studio / game-ui-voice-pack / indie-game-ost-pack / game-dev-tool-free 仅标准 frontmatter，可直接链接；a-stock-data、tdx-stock-analysis 含绝对路径引用，移植前需改相对路径。

## WorkBuddy 模型配置修复（已完成）
- 根因：WorkBuddy 读 `~/.workbuddy/models.json`（原为空 `[]`），不是 `~/.codebuddy/models.json`；且无 `subagents`/`variantModels`/`CODEBUDDY_CODE_SUBAGENT_MODEL` 配置，所有子代理回落免费档 hy3/hy3-x（429 根因，日志确认）。
- 已把 9 个自定义模型从 `~/.codebuddy/models.json` 迁入 `~/.workbuddy/models.json`，并修正 4 个缺 `/chat/completions` 的 URL（stealth/ox-alpha→openrouter.ai/api/v1/chat/completions、Qwen3.8-27B-FP8→model.inferx.net/endpoints/v1/chat/completions、DeepSeek-V4-Flash 与 Qwen3.8-Flash-Next→developer.amd.com.cn/radeon/api/v1/chat/completions）。
- `~/.workbuddy/settings.json` 新增：`env.CODEBUDDY_CODE_SUBAGENT_MODEL=glm-5.3-flash`（子代理全局默认，走用户自己的 b.ai key）；`subagents.agents` 给 7 个专家团成员 + general-purpose 显式映射 glm-5.3-flash，Explore→lite、Plan→reasoning；`variantModels.lite=qwen3.8-flash`、`variantModels.reasoning=deepseek-v4-flash`；glm-5.3-flash 条目加 relatedModels。
- models.json 权限已设 600。JSON 校验通过。
- 注意：settings.json 的 env/subagents 变更需重启 WorkBuddy 会话才生效；models.json 支持热重载。
- `~/.codebuddy/models.json` 里同样的 4 个坏 URL 尚未修（用户本次只要求修 WorkBuddy）。

## Game skill 迁移到项目级（用户改向：不要放全局）
- 用户拍板：game 相关 skill 放**项目级**目录，各 IDE 引用，不进全局，且要 git 提交。
- 发现 wechatgame 工作区此前**从未 git init**（之前的"提交"都是计划）。已 `git init`。
- 最终布局：正本 `wechatgame/.workbuddy/skills/{game-studio,game-ui-voice-pack,indie-game-ost-pack,game-dev-tool-free}`；`.codebuddy/skills/` 与 `.cursor/skills/` 为**相对符号链接** `../../.workbuddy/skills/<name>`（第一版写成 `../` 链接解析错误，已修）。
- 全局清理：已删除 `~/.workbuddy/skills/`、`~/.codebuddy/skills/` 的 4 个全局符号链接；`~/.cursor/skills/` 整个移除（含 4 个 0 字节沙箱残留文件）。原全局 skillhub 目录备份在 `~/.workbuddy/skills/.backup-20260911/`。
- git 提交 `8263e58`（范围限定：仅 skill 路径），项目其余文件（packages/games/docs 等）保持未跟踪，等用户指令再做基线提交。
- `~/Development/skills-repo` 仓库已被项目级方案取代，暂保留未删，用户可自行决定去留。
- 注意：symlink 方案在 Windows clone 上需开启开发者模式/符号链接权限。
- 追加 Qoder IDE 接入：官方文档确认 `.qoder/skills/{skill-name}/SKILL.md`（项目级）支持标准 Agent Skills 格式；已建 `.qoder/skills/` 相对符号链接并提交 `9af1dbb`。现共 4 个 IDE 读取同一份正本。
- 应用户要求正本再迁到项目根 `my-skills/`（git mv 保留历史），`.workbuddy/.codebuddy/.cursor/.qoder` 四处 skills 目录统一为 `../../my-skills/<name>` 相对符号链接，提交 `305367a`。后续新增 skill 的位置以 MEMORY.md 项目约定为准。
- 沉淀专家团方法论为 5 个新 skill（正本 my-skills/，提交 e890866 + 修复 cc1be69）：wxgame-gdd-writer（概念+冻结常量+八节 GDD+十查评审）、wxgame-qa-gates（测试五件套，判据从 GDD §8 导出）、wxgame-release-checklist（六阶段+上架+版本+回滚）、wxgame-art-spec-programmatic（程序化美术三件套）、wxgame-adr-arch（ADR 五节+控制清单）。模板已通用化去掉 Breakout 专属数值。
- 踩坑记录：本机 shell 是 zsh，`for x in $VAR` 不做分词（与 bash 不同），整串变成一个符号链接名；必须 `for x in a b c` 逐名列举或 `${=VAR}` 强制分词。
- 按用户确认的 review 结论补齐缺口（提交 f10a9de）：新增 wxgame-orchestration（七阶段 SOP/spawn 七要素/PASS-CONCERNS-FAIL 质量门/七条铁律）与 wxgame-ux-spec（ASCII 线框/状态×输入矩阵/动效毫秒表/微信首 10 秒留存）。my-skills 现 11 个 skill。
- WXG-T-006 已派 engineering-lead：产出 production/epics/epics-breakout.md（Epic/Story 拆分，验收标准只准引用 GDD §8），回传后由主理人提炼为 wxgame-epic-split skill。音频方法论 skill 决定延后到首款游戏实做音频时再提炼。
- WXG-T-006 交付验收通过：10 Epic / 40 Story / 316 行，验收引用约定 S<n>§8-<k>，第一冲刺 12 Story 最小垂直切片（vitest 可验证），EP-10 三 Story 标 [Blocked: 待编辑器]。方法论已沉淀为 wxgame-epic-split（提交 b298e95）。my-skills 现 12 个 skill，专家团七域覆盖 6 个。注意：production/epics/epics-breakout.md 本身属项目基线，随基线提交待用户指令。
- 用户要求补音频框架：新增 wxgame-audio-spec（框架版，提交 6455aae）——音频事件表与 UX §5 动效表逐行对齐、双通道红线联动、混音 Bus 框架、包体预算对齐 systems-index §3.8；项目专属数值全标 [TODO]，实做时填充回写。至此 my-skills 13 个 skill，专家团七职责域全部有对应 skill。
- 全量二次 review（19:23）：13 skill × 4 IDE 链接 52/52 可读；frontmatter 全合规（name 格式/与目录一致/desc 均为触发式 ≤1024 字）；无绝对路径引用（仅 2 个 _skillhub_meta.json 的 iconLocalPath，属元数据非正文）；跨 skill 引用全部有效；git 干净。唯一发现：game-dev-tool-free 带 10 个非标 frontmatter 字段（含 tools/tags，个别宿主可能误读），已报告用户待其决定是否清洗。
- 用户确认清洗：game-dev-tool-free frontmatter 已裁到只剩 name+description（提交 7cd0476），description 重写为触发式并保留原关键词。全库 13/13 复检通过，review 全部闭环。
- 用户指出触发冲突（skill 库最大误触发源）：game-dev-tool-free 与 game-studio 的泛化关键词（全流程/GDD/测试/发布）会抢走 wxgame-* 的匹配。已按用户方案二降级（提交 40d887c）：两者 description 改为「仅明确点名时使用；本 monorepo 默认走 wxgame-*」，弃用宿主专属的 disable-model-invocation 以保跨 IDE 一致。game-ui-voice-pack / indie-game-ost-pack 无冲突保持原样。
- 用户指出 orchestration 缺「成员→skill」路由表（提交 af111f8）：已补七成员×职责×必读 skill 硬表（含 indie/voice pack 执行层），spawn 七要素模板增第 8 要素「必读 skill 路径」；纪律：prompt 必附 SKILL.md 相对路径、未加载方法即产出=格式漂移打回、编排者下场前查表 spawn（铁律 1 落地开关）。表中 10 个引用路径全部验证存在。
- 用户 review 第 3、4 项（提交 578074a）：① orchestration「七阶段」实为 0–8 九段，已改称九阶段（0–8）并在表头注明 0/8 主理人独占、1–7 专业段对应旧"七阶段"口径；② game-dev-tool-free（488 行市场 boilerplate，泛用 GDD/Steam 发布与八节 GDD/微信红线冲突）已移除 4 个 IDE 链接、my-skills 留档。现各 IDE 实际加载 12 skill × 4 = 48/48 全通过。
- 用户 review 第 5–8 项（提交 bb48704）：① game-studio 归档（移除 4 IDE 链接）——73/10 声明不符、假 slash command、Godot/Unity/Unreal 倾向与 Cocos/微信冲突、references 渐进披露失效；② 新增 my-skills/INDEX.md 家族级约定（现役 11/存档 2 清单、全 SOP 豁免条件、优先级链 orchestration>域skill>执行pack>外来通用、路径约定、systems-index §3 冲突裁决链）；③ description 互斥收窄：gdd-writer 限八节体系、audio-spec 声明框架版 TODO 语义并与 ost/voice pack 互斥、qa-gates 声明 PASS/FAIL 裁定归 orchestration、voice-pack 补 WHEN 句式；④ 8 处开头"提炼自 Breakout"出处句收敛为指向 INDEX.md。复检：13 frontmatter 全标准、现役 11×4=44 链接全通过、INDEX 15 引用全有效。
- 用户 review 第 9–11 项（提交 05cad10 + 30d7da3）：⑨ Beatra 两包——SKILL 开头加本仓运行纪律（默认 update --auto off、不路由第三方包如 short-video-bgm-studio、共享层说明）、卖家→用户×6 / seller→user×3、7 份逐字一致的 references 抽到共享层 my-skills/_beatra-runtime/references/（两包内相对符号链接；workflow.md 业务差异与 scripts 安装常量各自保留）；⑩ epic-split 五段→六段(0–5)、qa-gates 补 G1–G4 门编号表（G1静态/G2单测/G3冒烟/G4硬判据，忠实搬运 test-plan.md）、adr-arch 补 ≥3 条 ADR 回链、ux/art 加"由编排者 spawn 勿跳过上游"句、frontmatter 全库去引号；⑪ game-studio 加 references 按需索引（单文件单节读取）。
- 踩坑记录 2：python 非原始字符串里写 \` 产生反斜杠残留（SyntaxWarning 不可忽略）；frontmatter 重写时 group(1) 不含末尾换行导致闭合 --- 粘连——正则解析会"匹配到更后面的分隔符"而假报 OK，复检必须同时查解析+引号+转义三样。
- 用户给出目标架构终稿（提交 0775d28）：game-studio 恢复四 IDE 链接为「点名兜底」层（description 降权禁自动触发 + references 按需索引），game-dev-tool-free 维持存档不挂载；ost-pack description 补"属 audio-spec 执行层、生成前查音频事件表"；INDEX 现役 12/存档 1。最终路由：全流程→orchestration（spawn 强制附带域 skill）；单域→wxgame-*；生成音频→ost/voice pack（经 audio-spec 事件表）；点名→game-studio。唯一遗留：音频实做后回写 wxgame-audio-spec 的 [TODO]。
- **新游戏立项：拼豆填色消除 games/beads**（21:52）。用户拍板：忠实复刻参考玩法 + 关卡倒计时 + 目录 games/beads；引擎 Cocos 3.8 / 750×1334 FIXED_WIDTH 沿用。参考图为 3D 立体珠子像素猫（路径在 QQ 图片缓存），要素：倒计时 04:55、底部槽位托盘（虚线扩展行+扩展按钮）、三道具（区域消除/槽位清空/随机消除，均带激励视频角标）。已并行下发 WXG-T-007（design-strategist：concept+systems-index+core-loop，强制先读 wxgame-gdd-writer skill）与 WXG-T-008（art-director：美术三件套，强制先读 wxgame-art-spec-programmatic skill）。这是 wxgame-* skill 家族首次实战复用。

## 拼豆游戏（games/beads）阶段 1：美术线验收 + 两笔提交
- WXG-T-008（林绘澄）美术三件套验讫 PASS（提交 b0f8543）：assets-spec 六层珠子参数卡（L0 投影→L5 符号，BEAD=64 示例演算）、10 珠色×唯一矢量符号、6 状态；accessibility 目标 Standard（17 项矩阵）。主理人一致性修正 1 处：ad_badge 角标 art-bible「右下」×2 对齐 assets-spec 权威「右上」。
- 用户拍板：H1 纯色模式不提供（三重编码始终显示）；E2 大字号基础版即可；分两笔提交。
- 用户框架构建入库（提交 6c02732）：my-skills 13 文件增强（orchestration SubAgent 路由+G1-G4 挂钩、game-studio disable-model-invocation 瘦身 284→64 行）、my-agents/+四 IDE agents/rules/memory/hooks+AGENTS.md、pnpm workspace+packages/framework+tools/dev/harness+.github/.githooks（pre-commit check:links 已验证生效）、games/breakout 等既有内容首次入库。
- 坑：沙盒里 grep 链式命令多次空输出退出码 1（结果实际存在），改用 Read 工具直读文件核验。
- 待办：WXG-T-007（文策渊 concept/systems-index/core-loop）进行中，回传后做跨成员一致性检查（concept↔art-bible 视觉锚点、§3 与资产规格对齐）。

## 拼豆阶段 2：设计线验收 + 数值全冻结
- WXG-T-007（文策渊）三件套验讫 PASS（提交 feat(beads)）：concept 十节 + systems-index（9 系统/拓扑/§3 冻结令）+ core-loop（六状态机/10 硬判据/cleared 同帧优先）。
- 跨成员一致性回写 9 处：art-bible §4.3 布局带（HUD/拼图/托盘/道具四带）、BEAD=50/pitch 52/13×12、托盘 TRAY_SLOT=48/GAP=6/COLS=12、告急 10s 关闭 accessibility 待批项。原 art 64 演算示例保留标注按公式换算。
- A1–A8 全拍板（systems-index v1.1）：A1 扩展不跨关/A5 每道具 1 次免费/A6 三道具仅作用托盘（用户）；A2/A3/A4/A7/A8 主理人依支柱裁定。§3 数值正式冻结，待确认标记清零。
- 遗留：WXG-T-002-9（validateLevel 补列号+maxRows）建议随 S3 GDD 一并补完；激励视频拉起逻辑 MVP 占位（AD_PLACEMENTS=4）；[待工程对齐] platform/weapp 是否暴露 createRewardedVideoAd。
- 下一步：阶段 2 续（S2–S5 P0 GDD）→ 阶段 3 程基岩架构。

## 拼豆阶段 2 全部闭环（WXG-T-009）
- 文策渊交付 S3 bead-grid/S4 tray-spawner/S2 input-control/S5 timer-gameover 四份八节 GDD（各 10 条硬判据，共 40 条；13 个事件名全量核对零自造）。
- 主理人裁决 S2 热区：维持棋盘珠 66²（外扩 8px），systems-index 升 v1.2 澄清 TOUCH_MIN 语义（88px 限 UI 控件）——对齐 accessibility F2，防 52px pitch 下 88px 热区 69% 重叠。
- breakout 遗留 WXG-T-002-9 补完：levels.schema.ts（BRICK_MAX_ROWS=6+列号错误信息）、levels-spec §5.1 校验分工。
- 阶段 2 完成（S1–S5 P0 GDD 齐）。下一步阶段 3：程基岩架构+≥3 ADR+Epic 拆分+确认 createRewardedVideoAd；可并行 spawn 严守真 QA 五件套（40 条判据已备）。
