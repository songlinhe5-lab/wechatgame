# 2026-09-14

## Cocos 预览全链路验收通过（08:31，主理会话，WXG-T-042 收口）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-14-s01-t042.md`（943 tok｜原 L3–16｜隶属标记见该件 H1）。
## 构建链前置补洞 + 多游戏粒度修正（WXG-T-047 / T-048，CodeBuddy 会话）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-14-s02-t047.md`（1041 tok｜原 L17–27｜隶属标记见该件 H1）。
## 构建自动化能力修正：**MCP 不可，但编辑器 CLI 可**（WXG-T-049）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-14-s03-t049.md`（737 tok｜原 L28–40｜隶属标记见该件 H1）。
## 里程碑：用户确认「已基本完成 breakout」→ 验收回填（WXG-T-050）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-14-s04-t050.md`（722 tok｜原 L41–55｜隶属标记见该件 H1）。
## 引擎功能裁剪：主包 3008 → 1815 KB，内部目标首次达标（WXG-T-051）

- **证据前置（先证"真没用到"再裁）**：全量 **`cc` 导入符号实测只有 7 个** —— `Color` / `Component` / `Graphics` / `Label` / `Node` / `UITransform` / `_decorator`；待裁模块引用数**全为 0**。
  **一个假命中值得记**：`Animation` 的 4 处匹配实为 **`requestAnimationFrame`**（浏览器/微信 API），**不是**引擎 `Animation` 模块——**按关键词裁剪必须先看命中上下文**。
- **改动**：`cocos/settings/v2/packages/engine.json` 的 `includeModules` **22 → 10**，关闭 14 个 checkbox（spine/dragon-bones/tiled-map/video/webview/particle-2d/physics-2d-box2d/mask/rich-text/animation/audio/tween…）。改写走 **parse→set→serialize + 解析校验**，非手改文本。
- **效果（release 同口径）**：主包 **3008.3 → 1815.1 KB（−39.7%）**；`cocos-js/` 引擎 **2560 → 1356 KB（−47%）**；gzip 809 → 490 KB；**§3.8② 内部目标 2000 KB 首次达标**，引擎亦回到 1800 KB 预算内；红线余量升至 55.7%。`cocos:check` 类型检查通过。
- **方法教训**：裁剪前先做「**导入符号全集**」取证，比逐模块猜引用可靠得多——`perl -0777` 处理多行 import 是必需的（`import {` 常跨行，单行 grep 会漏）。
- **⚠️ 仍未闭环**：**运行时实玩验收**。类型检查只覆盖编译期，禁模块的运行时行为抓不到。诊断法：浏览器 console 的缺失类名**直接指向被裁模块**，回加该模块重测即可。


## S6 道具系统落码：beads 最后一个未实现玩法系统归零（WXG-T-060）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-14-s06-t060.md`（779 tok｜原 L66–91｜隶属标记见该件 H1）。
## 「到达序」判据清理：把不可测的「先到为准」统一换成帧内执行序（WXG-T-061）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-14-s07-t061.md`（1155 tok｜原 L92–127｜隶属标记见该件 H1）。
## 三项待裁定落地（WXG-T-062，用户「按推荐」）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-14-s08-t062.md`（601 tok｜原 L128–147｜隶属标记见该件 H1）。
## EP-07 结算·过关面板落地（WXG-T-063，EP-07 未完）

- **发现的是"占位 vs 规格"错位**：`ux-spec §4` 流转表写的是
  `LEVEL_CLEAR → 下一关 / 去冲刺*(U1) → 玩法·n+1 / 玩法·冲刺`（**等按钮**），
  而实现从第一天起是「`LEVEL_CLEAR_DELAY_S = 1.4s` 后自动进下一关」——S7 的评分函数
  （`computeClearStars` / `normalSettleScore`）早就落地且有测试，**缺的只是"把这堆数据画出来并等玩家点"**。
- **新增** `systems/clear-panel.ts`：同 PausePanel 范式（纯布局 + 命中 + 入 200/出 150ms），
  外加**星入场逐颗 150ms**（ux-spec §5）与 0→1.2→1 弹跳；布局按 `lastLevel` 缓存两份。
  视图按 §3.4 画遮罩 + `panel_dialog` + 金色缎带标题 + 逐颗星 + 「剩余 mm:ss ｜ 道具 n/3」+ 主/副双钮。
- **占位退休**：`LEVEL_CLEAR_DELAY_S` 与 `tuning.levelClearDelay` 一并删除（留墓碑注以便追溯），
  `level-clear` 的 onUpdate 改为只驱动「每星一次 `sfx_star`」；面板开时压掉相位横幅（否则双重标题）。
- **两条按钮出口**：主钮（末关文案切「查看结果」）→ 下一关 / FINISH；副钮「▶ 去冲刺」= U1 第二处入口。
- **测试**：新增 `tests/clear-panel.test.ts` 7 条，其中最关键的是**回归闸门**——「过关后 advance(3)
  仍在 level-clear 且 levelIndex 不变」，直接钉死"不许退回自动翻页"。另改两处依赖旧占位的用例
  （`pause-settings` 的 FINISH 构造、`powerups §8-9`）为**点面板主钮**；beads 150 → **157**。
- **顺手校正一处我自己的测试假设**：星入场第 1 颗在 t=0（逐颗 150ms ⇒ 三颗共 450ms），
  我最初按"t=0 第 0 颗"写了步进期望，实测差一格——**规格的"逐颗"从第一颗即刻开始**。
- **EP-07 仍未闭环**（登记 Epic §0 声明 6）：冲刺结算面板（§3.5 左列 + NEW BEST + §8-11）、
  FINISH 通关画面（§3.6）、连击特效三档（§2.5，DevTools 判据）。

## 台账「标题制 + 详情分片」：治随任务数线性膨胀（WXG-T-064）

- **用户反馈点是准的，而我上轮结项是误判**：T-053 的「体积驱动归档」只清**已完成**行，
  而膨胀源是**在办行的详情** + **每个新任务的详情注入** ⇒ 必然反复撞 `ctx:check` B 项 8000。
- **实测根因**：`TASKS.md` **81% 体积是任务行详情**（16 行 ≈ 5334 tok，中位 389、最重 613；
  非任务行仅 ≈1207）。
- **最有价值的发现**：**装置早就有这个能力**——`ctx/index.json` 已按**小节**输出
  `anchor/level/startLine/endLine/tokens/summary/keywords`。所以「命中标题后再读详情」不需要造机器，
  需要的只是**改台账格式**：详情从**表格单元格**搬到**小节**，索引器立刻就能按节寻址。
  **教训**：遇到"上下文膨胀"先看装置已有什么粒度，别急着设计新机制。
- **落地**：主表标题制（≤60 字符，机械强制）；正文迁 `TASKS-DETAIL.md`（16 节原样搬）；
  新增 `check:tasks` 四类 FAIL（名称超限/行不连续/行⇔小节不配对/残留已归档 id）并带 `--prune`；
  协议进 `ROUTES`（台账成本 1439 → 2220 修正 + 详情入口）与主表头注。
- **效果**：`TASKS.md` **7224 → 2220 tok（−69%）**，单任务详情按需 ≈ **290 tok**。
- **顺带修掉两处我自己造的格式缺陷**：① 前几轮往表里插任务行时**带进空行，把 Markdown 表格
  从 T-060 起截断**（难怪读起来别扭）；② 表内夹着 3 条「注」——已移到表后。
- **未闭环**：归档器**成对搬运**（行 + 小节）还没机械进 `archive-tasks.mjs`，现由 D 项 + `--prune` 半自动兜住。

## 归档器「成对搬运」机械化（WXG-T-065，闭环 T-064 的未闭环项）

- **为什么要机械化**：T-064 把详情拆到 `TASKS-DETAIL.md`（一任务一节），但归档器**只搬行** ✗
  ⇒ 详情文件**只增不减**，膨胀只是被搬到了另一个文件。半自动的 `--prune` 不够——它要人手记得跑。
- **落地**：① 共享解析库 `tools/scripts/lib/tasks-detail.mjs`（**单一真源**：归档器与门禁都走它；
  两边各写一套切分逻辑必然漂移，而漂移的后果是**静默丢任务正文**）② `archive-tasks.mjs` 成对搬运：
  行 → `archive/TASKS-archive.md`、节 → `archive/TASKS-DETAIL-archive.md`，**节数 == 行数**
  （不等即 fail loud，且**两个文件都不写**）③ `check-tasks.mjs` 加 **E 项**
  （详情归档有节 ⇒ 行归档必须有行 = 成对脱钩检测）。
- **顺带纠正一处危险默认**：`check-tasks --prune` 原本是**删除**残留小节 ✗ —— 那会直接丢正文，
  与成对搬运语义正好相反。已改为**搬入**详情归档。
- **三条安全边界**：**宁漏勿错**（主表有行但详情无小节 ⇒ 跳过该行 + 告警，搬了会丢正文）｜
  **退化**（详情文件不存在 ⇒ 只搬行并明示；老仓库与桩自测不受影响）｜**幂等**（重跑四文件字节不变）。
- **证据（三条，都不是"我觉得"）**：① 桩自测新增 [7] 组 ⇒ **PASS 41 → 67**；
  ② 解析库对**真实** `TASKS-DETAIL.md` 实测 `serialize(parse(x)) === x`（幂等 ⇒ 写盘不 churn 文件）；
  ③ 真实仓库 dry-run 报「计划归档 **14 行 + 详情节 14 节**」（数量守恒）且未落盘。
- **未执行**：真实 `--write`（会搬走 14 行、含 T-060…T-062）—— 属治理动作，等发令。

## EP-07 通关画面（FINISH）落地（WXG-T-066）

- **做了什么**：`ux-spec §3.6` 的全屏庆祝 + 星级总览 + 双钮（去冲刺 / 重玩第 1 关），接上
  `core-loop §8-8` 三段判据。新增 `systems/finish-panel.ts`（纯逻辑，镜像 `clear-panel.ts` 纪律）；
  `addState('finish')` 此前是**空壳**、`case 'finish'` 把**任意点击**都当「重玩第 1 关」✗ ⇒ 现在只认两个按钮。
- **顺带修一处真缺陷**：末关进 FINISH 时**结算面板不退场** ⇒ 它 150ms 淡出会从通关画面后面透出来
  （两个面板都画全屏遮罩）。已于交接处 `close()` 并有断言钉住。
- **实测踩到一条过期文档**：`epics-beads.md` EP07-S1 写「STAR3_RATIO 0.40 / STAR2_RATIO 0.20」，
  而 §3 冻结值是 **0.32 / 0.12**（T-054 重冻结）⇒ 我第一版测试按过期值把 ratio 0.15 当成 1★ ✗，
  实测是 2★ ✓。按该文档自身的裁定纪律「以 §3 为准，回写本文档」已更正，测试改为从冻结常量取值。
  **教训**：文档里的数值不是真值，`tuning.ts` 与 §3 才是；判据测试不得写死阈值。
- **派生项（无明文，已登记）**：① §3.6 未给几何 ⇒ 沿用结算面板常量族；② 「逐颗 150ms」改**逐关**
  150ms（24 颗逐颗要 3.6s ✗）；③ 总览取每关**历史最好**。
- **未闭环**：冲刺结算面板、连击特效三档、**星级表持久化**（存档无字段 ⇒ 总览现为局内累计，
  冷启动清空；已进 backlog）。
- **证据**：新测试 10 例 ⇒ beads **157 → 167 全过**；拷贝件同步（game 22 → 23）。

## EP-07 冲刺结算面板（WXG-T-067）

- **判据 `score-combo §8-11` 的「数据半边」早就实现了**（`_recordSprintEnd` 已算 NEW BEST 并写盘 ✓），
  缺的只是**展示 + 出口**：冲刺 `GAME_OVER` 此前「任意点击即重开本局」✗（与 FINISH 的旧毛病同型）。
- **两处受契约约束、不能用「加个字段」解决的**：
  ① 「最高连击」**不能**进 `sprint:ended` payload —— 该事件字段集合受 `systems-index §6` 变更流程约束，
     而它是**展示量** ⇒ 落到 `SprintTracker.bestStreak`（`streak` 断连归零，必须单独记最大值）；
  ② 崩溃快照是**白名单 + 缺字段即丢** ⇒ 我为 `bestStreak` 开了**例外**（缺省 ⇒ 0，不弃整份快照），
     并在提案 §4 写明理由：为一行结算文案丢掉玩家整局恢复档不划算。
     **教训**：加字段前先看那个契约是「白名单」还是「可缺省」——两者的默认行为完全相反。
- **改了一条既有断言**：`revive.test.ts` 里 `…still retries on any tap` 是**在钉住旧行为** ✗。
  改名 + 改为「面板外零响应 / 只有双钮能离开」——**改测试必须让覆盖更严，不是更松**。
- **证据**：新测试 9 例（含用**共享 storage 的两个 harness** 造真「重启」语义验证 §8-11 的「≤ 最佳不写」）
  ⇒ beads **167 → 176 全过**；拷贝件同步 game 23 → 24。

## memory 日志分级加载：摘要层 INDEX.md（WXG-T-068）

- **问题**：`memory/` 4 篇日记合计 **31,690 tok**（单篇 5.6k–10.1k）⇒ 任何会话都读不起 ⇒
  「知识库很大但没人敢读」＝事实上的不可用。而 `knowledge/` 早就是正确形状（INDEX 摘要 + 详情层），
  `memory/` 缺这一层；ROUTES 也只登记了 `MEMORY.md` 的小节，**4 篇日记没有任何入口**。
- **做法**：`memory/INDEX.md`（生成物，标记块 `<!-- memory:index:start -->`）＝「文件 → `##` 主题」的
  行区间 + 体量 + 摘要素描。**摘要不写第二套算法**：取 `ctx/index.json` 里已有的 `firstSentence()`——
  两套必然漂移，而漂移的后果是索引**指错行**（比没有索引更坏）。由 `ctx:build` 生成并纳入不动点收敛。
- **口径诚实**（写进产物与文档）：摘要是**节首句摘取**，**不是**人工提要 ⇒ 只用来判断「要不要读这一节」。
- **门禁**：`ctx:check` 新增 C 子项（现算文本 vs 磁盘逐字节 + 蒸馏天数与 `distill` 默认值对账）；
  **双向实测**——移走产物报红、移回转绿（不是只看它绿）。pre-commit 的产物暂存行补了它。
- **收益**：入口 **31,690 → 3,756 tok（8.4×）**，且行区间可直接喂 `read_file` ⇒ 真正「分级加载」。
- **踩到两个门禁细节（照规矩办，没绕）**：① 新文件 + ROUTES 引用 ⇒ 默认模式的 D 项必红
  （索引描述 HEAD）⇒ 用 `--working-tree` 安全阀自证；② 整文件引用必须**显式**标 `<!-- no-anchor -->`，
  门禁不接受「默默放过」——这一条值得记：本仓的门禁宁可要一行显式豁免，也不要一处隐式宽容。

## 两库的使用触发协议入档（WXG-T-069）

- 用户问「每次新窗口加载 knowledge，那 memory 什么时候用？」——**前提与实际相反**：按 `AGENTS.md:118`，
  knowledge 是「**同域任务开工前**」读（非每窗口无条件）；而**挂进四 IDE** 的是 `memory/MEMORY.md`。
  真正的问题是 `my-rules/agents-md.md:13` 把 memory 写成含糊的「需要活上下文时 Read」。
- **协议定稿**：`MEMORY.md` 可常读；日记**只在四触发下查**——改旧政前 / 接续未完 / 追溯用户裁定 /
  排障找当初的确诊法。查法 = grep `memory/INDEX.md` 定位到节 → 只读那一节（几百 tok 而非 3.2 万）。
- **落点按预算分层**：常驻层（`AGENTS.md`、`my-rules`）只放**触发词**（每个字每次会话都付费 ✗），
  完整协议进按需层（`memory/INDEX.md §1`）。这是本任务真正花心思的地方：**协议该多详细，取决于它躺在哪一层**。
- 顺带实测：改完 §1 重跑 `ctx:build` ⇒ 手写前言保住、生成块完好 ⇒ T-068 的「手写在外、生成在内」成立。

## 头注静默落后：把「下次注意」换成机械门禁（WXG-T-070）

- **抓到自己一个真错**：T-067…T-069 三笔登记里我用 `str.replace` 改台账头注，**静默没匹配上**
  （真实文案不含我假设的「（已完成）」；而那三处我**漏了 `assert`** ✗，别处却用了 ✓）⇒
  头注停在 T-066，而表内已到 T-069。唯一会告警的 `tasks:archive` **平时不跑** ⇒ 差点带着
  「已释放的号」继续领号（并行会话重号的经典成因）。
- **修法不是「下次注意」，是 F 项门禁**：`check:tasks` 断言头注号 ≥ 主表∪归档全局最大号；
  只判**落后**为负（领先合法——archive 本身就允许「头注只进不退」）。双向实测过。
- **顺带**：`--until-under=2500` 归档 5 行 + 5 节，详情 8214 → 6592 解 B 项；头注被工具校准到 T-069。
- **两条沉淀**：① 脚本化改文档时 **`replace` 必须配 `assert`** —— 静默 no-op 是这类工具最贵的失败模式；
  ② 同一类错犯到第二次，就该把它从「纪律」升级成「门禁」。

## 星级表持久化：按 GDD 已冻结字段落码（WXG-T-071）

- **侦察纠正了我自己登记的第一步**：backlog 写「先回写 save-progress GDD 增字段」✗，实测该 GDD
  **§2.2 早就冻结**了 `stars`（每关历史最高星）、§2.4/§6 给了降级与小数口径、§8-2/§8-3 给了判据 ⇒
  **不需要回写 GDD**，缺的是**实现**（「文档有、代码没有」，与之前抓到的 A4 道具图标同一类）。
  ⇒ **教训：动「新增字段」前，先 grep 一遍 GDD 的字段表**——它可能早就定了，只是没落码。
- **落码**：schema 做钳制（长度不符→全 0、小数下取整、钳 [0,3]）、**不做 max**；`max(旧,新)` 归游戏侧
  过关时取（§8-2），并专门写了一条「schema 不发明 max」的用例防两处各取一次。
- **判据实测**：§8-2「重启保留」用**共享 storage 的两个 harness**（真重启语义）；179/179 全过。
- **一致性**：新增 `STAR_MAX` 到 tuning，顺便把 `finish-panel` 里那个重复的 3 收敛掉 —— 同一事实只留一个常量。

## ctx:build 写入顺序闭环：把「每次都要重试」的假失败根治（WXG-T-072）

- **症状**：当天**连续四笔**提交都被 ctx 兜底门拦下（同一条笼统信息），每次都要手工 `ctx:build` + 重暂存才过 ✗。
  前三次我按"常规收敛"处理 ✓；**第四次不再当偶发**，去读生成器代码。
- **真因**：我在 T-068 把 `memory/INDEX.md` 的最终写入排在 `serializeIndex()` **之后** ⇒ index.json 记的是
  **上一轮**的 INDEX.md 哈希 ⇒ `C(--staged)` 必判不一致。
- **修法**：① 先落 INDEX.md ② 再建索引把它纳入 ③ 最后序列化 —— 三步同源，不再依赖重试。
- **教训**：**同类拦截到第二次就该去读代码，而不是把它当"已知的常规收敛"**——"每次都要重试"本身就是缺陷签名 ✗。
  另外如实记下不确定性：手工复现仍能造出一次不一致，可能还有第二因子，未穷尽。

## 归档器「详情侧」旋钮：把「凭感觉挑阈值」换成表达意图的开关（WXG-T-073）

- **同一错位当天咬人两次**：`--until-under` 量的是**台账**（标题制后仅 ~2.4k），而 B 项压力在
  **详情文件**（8274）⇒ 第一次（T-065）我手挑了个 2400 ✗；这次 `--until-under=2500` 直接**空转** ✗。
  于是给归档器加 `--detail-until-under=<N>`：直接量详情文件，意图与手段一致。
- **抽出一个口径**：三条策略原本各自内联「可归档行」筛选 ⇒ 我把新旋钮接到**已被年龄筛过**的
  `candidates` 上 ⇒ **静默什么都不搬** ✗。抽成 `eligibleRows()` 单一出处后，策略只管「选多少」。
- **同日第二次同类顺序缺陷**：候选重算必须早于 `toArchive` 派生，否则"报告列了候选、计划 0 行"
  （与 T-072 的 `ctx:build` 写入顺序同型 ✗）。两处都已写进代码注释。
- **当日共性沉淀**：**"顺序"是我今天最贵的错误类别**（写入顺序、派生顺序各一次）。
  它们的共同症状都是**静默无害的样子**（空转 / 少了哈希），门禁才把它们变可见 ⇒ 值得记进 knowledge。

## EP-07 最后一项：连击特效三档（WXG-T-074）

- **侦察**：`combo:up` 事件早已在发 ✗ 却**没有任何消费方** —— 「事件在发」≠「已实现」。
  形态归 §2.5、**毫秒值归 ux-spec §5**（200/150/350，§2.5 明写不自写）。
- **两条红线用结构落实**：Lv2 的呈现类型**只有 `screenScale`、没有 dx/dy**（位移震屏在类型层写不出来）；
  三档都是**单次循环**（播完即清、不自重复）⇒ ≤3Hz 闪烁的来源不存在。
- **平台缺口（不假造）**：Lv2 需要整屏 scale，但渲染管线没有全局变换（`_commands` 私有）⇒
  视图不为它画替代物，只把**值**算对测对，等宿主提供变换。教训：**假造一个"看着像"的实现，
  比一个如实登记的缺口更糟**。
- **派生项**：粒子数取 4（§2.5 是 3–5 区间、无 RNG）；多档重叠只播最高档。
- **证据**：新用例 5 条 ⇒ beads **179 → 184 全过**；EP-07 四块全部闭环（帧检半边与特效音效留真机/后续）。

## 领号只读头注：把协议写在成本所在的地方（WXG-T-075）

- 旧读法「只读主表拿号」（≈2.4k）里，**拿号只需要头注那一行**（≈30 tok）——每次领号多付约 80 倍 ✗。
- 已写进 `ctx/ROUTES.md` 领号行与 `TASKS-DETAIL.md` 前言：**领号 = `grep -n '当前已分配至'` 读那一行**；
  「看全部状态」才读主表；详情照旧按行区间读节。
- 顺带一个一致性的小心思：头注**行号会漂**，所以协议里写的是 **grep 配方**而不是行号——
  与 `memory/INDEX.md` 的「grep 定位」同判例（定位靠内容锚，不靠会漂移的行号）。

## 台账 backlog 卫生门 + 行数哨（WXG-T-076）

- 用户问「分级读取后还需要 archive 吗」⇒ 按段量化回答：**头注 485 有界 / 主表 526（行数无门）/
  backlog 1,399 占 57% 无任何门** ✗，其中一条**已结项**删除线占位行占 383 tok（16%）。
- **结论**：分级读取解决「读不起」，但 archive 管的是另外三件事——**摘要层成本守恒**（省下的
  转移到索引/摘要层）、**工作集 vs 历史** 的语义分界、**写路径健康**（并行冲突/diff 噪声）。
  B 项「单文件 8000」对这类文件的原初理由确实变弱 ⇒ 该降级时降级门，不退回归档（独立决定）。
- **落地三闸**：结项即移除（纪律写进 backlog 段头）/ backlog 行 ≤160（G 项，双向实测）/
  主表行数 >20 观察哨（H 项，report-only，对齐「结构硬行为软」）。
- 顺带修一处**坏表**：backlog 表头后缺 `|---|` 分隔 ⇒ 拆成了两张断开的表 ✗（压缩时一并修）。

## beads 工程接入 Cocos MCP（WXG-T-079）

- 用户要求给 beads 装 Cocos MCP ⇒ 核查发现**链路早已就位**（编辑器/扩展/IDE 配置/工程），
  真缺口只有 beads 工程缺 `mcp-server.json` + `tool-manager.json`（§13.3 B2 漏做）。
- **autoStart 用 true**（breakout 是 false）：实测语义 = 重启编辑器免手点 Start。
- **拦下一个回归**：编辑器删了 beads `project.json` 的 `fitHeight:true`——框架代码不自设
  分辨率 ⇒ 该键是 Fit Height 唯一落点，提交即静默破坏竖屏 ✗。已恢复，留观。
- 剩一步 GUI：重启编辑器或面板点 Start；探活 `/health`（`/mcp` GET 恒 404 会误判）。
