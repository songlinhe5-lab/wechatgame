# 2026-09-14

## Cocos 预览全链路验收通过（08:31，主理会话，WXG-T-042 收口）

- **游戏在 Cocos 预览完整可玩**：发球 ✓、挡板跟手 ✓、撞砖得分 ✓（实测 score=300，砖墙被打掉多块）、life-lost 状态机流转 ✓。三项验收全绿：`screenW=750`（画布空间对齐）、信箱区拖挡板（画布相对 495 → 设计 608，与 letterbox 公式精确吻合）、按住 300ms 点击发球（justDown 竞态修复生效）。
- **用户报的 start_scene 错误已消**：「无法查到当前场景 JSON 数据(start_scene)=current_scene」= 预览设置取「当前场景」而编辑器当时没有打开场景（叠加此前 bindings 语法错误导致编译失败）。配置本身没问题（`settings/v2/packages/scene.json` 的 current-scene 指向正确 uuid，library 缓存完好）。若再遇：编辑器里双击打开 `assets/Main.scene` 并保存即可；或项目设置→预览→起始场景固定选 Main.scene。
- **编译不通过的根因（第 5+1 个 bug）**：我此前一次 Edit 把 `launch()` 的闭合花括号吃掉，`_fitToGameCanvas` 被嵌套进方法内 → 编辑器编译期 SyntaxError（`Unexpected reserved word 'private'`）→ SystemJS 整个模块图崩、预览白屏。**verify 没拦住的原因：Cocos 拷贝件从未被任何门禁检查。**
- **门禁补洞**：新增 `tools/scripts/check-cocos-scripts.mjs`（`pnpm run cocos:check`）——用编辑器生成的 `temp/tsconfig.cocos.json` 对 `games/*/cocos/assets/scripts` 跑 tsc；干净检出（CI 无编辑器产物）自动跳过。已接入 `verify`（同时补了 `framework:sync:check`）。本地实测：修前 TS1128 命中、修后零错误。
- 顺带：`cc.game` 在编辑器生成声明里类型异常 → 画布尺寸改从 DOM `#GameCanvas` 读取（微信端无 document 自动降级为平台整屏，双端一致）。
- 提交：`90052df`（花括号+DOM）、`979d75e`（cocos:check 门禁），均已推送。
- **教训沉淀**：① 注释里不能出现 `*/` 字面量（`games/*/cocos` 写进块注释直接炸语法）；② 编辑器拥有的状态文件（tool-manager/mcp-server.json）入库前必须按 JSON 解析核对语义；③ 验收手法：playwright eval 直查 `_machine.current/ball.launched/_pointerX/paddle.x` + wrap `input.push` 追数据流。
- **下一站**：微信构建 + 包体校验（`node tools/scripts/check-bundle-size.mjs`，脚本可能缺失需按 README §3 步骤 7 补）；G3 Label 锚点 / G5 dt 单位实测时顺带核。

## 构建链前置补洞 + 多游戏粒度修正（WXG-T-047 / T-048，CodeBuddy 会话）

- **T-047 补两个缺失脚本**：`check-bundle-size.mjs`（阈值真源 `systems-index §3.8` 双口径：红线 4096/30720 KB、内部目标 2000 KB，**分列不混用**；分包三级回退 `game.json`→`subpackages/`→全计入主包；干净检出自动跳过）+ `check-secrets.mjs`（把 `control-manifest §10` / `ADR-0010 §4.2-5` 两条**只写在文档里、脚本却不存在**的红线机械化。关键设计：扫描面 = `git ls-files --cached --others --exclude-standard`，即「**会不会入仓**」而非「磁盘上有没有」；故不需要 `--staged` 模式。接 pre-commit **⓪ 步** / CI `arch-guard` / `verify`；`.gitignore` 补密钥段）。自测 **13 + 18** 全绿。
- **实测推翻 `ADR-0009 §3.2 P2`**：`cocos-mcp-server` **v1.5.4 不能触发构建**——`project_build_system` 仅 3 个只读 action；唯一含 `build` 的 `project_manage` 被白名单禁用，且其实现只 `Editor.message('builder','open')`，源码注释原文 *Building requires manual interaction through the build panel*。⇒ 微信构建**结构上不可自动化**，G7 只能靠「人工构建 + 回读日志 + `check:size`」。已按 ADR 惯例**不改写原行**、另加偏差注。
- **多游戏粒度修正（T-048）**：① `build:cocos` 由"根层裸命令"改为 **per-package 声明 + 根 `pnpm -r` 聚合**——`games/<game>/cocos/` 每款游戏都有，根层裸命令**说不清构建哪一款**；② `sync-levels-data.mjs` 原**硬编码 breakout**，导致 beads **有真源、有产物却零门禁且静默报绿**（K-031 族）→ 改「**每游戏 header 模板 + 通用遍历**」+ **覆盖面断言**，新游戏零改共享脚本。
- **门禁补齐后首跑即抓到 beads「漂移」——但定性后是格式漂移，不是数据漂移**：beads 的产物由**另一个渲染器**产出（单引号 / 紧凑数组 / `6.0`），其**数据与真源结构等价**（用 sortDeep 逐字段证明相同）。**教训：门禁报「不一致」时必须先定性再覆盖**——否则会把"格式差异"误报成"数据事故"，反向也可能抹掉手工修正。
- **新脚本首版漏「仅直接调用时执行」守卫**：`sync-levels-data.mjs` 顶层有写盘副作用，而它要导出 `buildModuleSource()` 供测试复用 ⇒ **一次 `import` 就改写了 beads 产物**。已补守卫并实测「import 不写盘 = true」。**教训：任何被测试 import 的脚本，写盘必须收进 `main()` + `import.meta.url === process.argv[1]` 守卫。**
- **文档回填 8 处**：`architecture.md §5`「gzip 后统计」措辞（实现是 **raw 判定**；raw ≥ gzip，按 raw 卡严格更安全）、`cocos/README §2.1` 与 `VERSION.md G1` 的 **strip-suffix 反向漂移**（文档写"拷贝件保留 `.js` + 备有 `--strip-suffix`"，实现是 `stripSuffix = !includes('--keep-suffix')` 即**默认剥除**；实测拷贝件确无后缀 ⇒ 该残余风险对本工程已不适用）、`ADR-0009 §3.2` P2 偏差、`test-plan Q9`、`cocos-setup §12`。
- **顺手抓到 3 处**：`my-mcp/README.md` **两个 `## 5.`**（T-046 插节未重编号）；`settings/mcp-server.json` **已提交版本键名错**（`debugLog` vs 扩展认的 `enableDebugLog`）⇒ 该开关一直失效；未跟踪目录 `games/breakout/cocos/preview-template/` **未被 gitignore**（`git add -A` 会静默入库，**待决**）。
- **未决**：`preview-template/` 入库还是忽略；A 档 `web-mobile` 构建（人工 GUI，零 AppID）尚未跑。

## 构建自动化能力修正：**MCP 不可，但编辑器 CLI 可**（WXG-T-049）

- **本会话上轮结论被自己推翻**：我曾判定「微信构建**结构上不可自动化**」。那只对 **MCP 通道**成立。实测 **Cocos Creator 自带 CLI 可以构建**：
  `CocosCreator --project <proj> --build "platform=<p>;debug=true"`（官方手册《命令行发布项目》）+ 本机 `app-asar` 内含 `--project`/`--build`/`buildConfig`。
  实测 `platform=web-mobile` **3.9s 构建成功**，产物含 `index.html`/`cocos-js/`/`assets/`，日志 `build Task (web-mobile) Finished in (3 s)`，**编辑器实例同时开着也不冲突**。
  **教训：登记「能力不行」必须写明「失败的是哪条通道、哪一层」**——"MCP 不可"写成"构建不可"，会让 `build:wx` 类缺口看起来无解。
- **G1 语法那一半在 web-mobile 上关闭**：产物 `assets/main/index.js` 内命中 `BreakoutBootstrap`/`BreakoutGame`（我们的代码确实被编进去），日志**无** `Unexpected token`、无后缀解析报错 ⇒ `export * from …` 这类 ESM 语法被 Cocos 构建管线接受。wechatgame 平台未测（同一套 packer-driver，风险已大幅下降）。
- **构建产物落点与门禁静默跳过**：Cocos CLI 默认把产物写在**工程内** `games/<game>/cocos/build/<platform>/`，而 `architecture.md §1/§5` 约定的是 `games/<game>/build/`。`check:size` 原先只认后者 ⇒ **产物在、门禁说没有**（K-031 族）。已改为**两处都扫**。
- **落地**：`build-cocos.mjs` = 前置检查 + CLI 构建 + 产物校验；`pnpm run build:cocos`（wechatgame，需 AppID）/ `build:cocos:web`（web-mobile，免 AppID）。实测聚合调用 **12.5s 成功**。
- **我另一处操作失误**：第一次构建被我加的 `perl -e 'alarm 300'` 超时保护 SIGTERM 掉，产出"半成品产物"（只有 `assets/`+`src/`，缺 `index.html`+`cocos-js/`）。**构建类长任务不该套 alarm**；改用 `nohup … &` 后成功。注意：SIGTERM 在**成功的**构建日志里也出现（builder worker 收尾），**不可当失败信号**。
- **A 档 404 根因**：用户在 `cocos/extensions/` 目录下用相对路径 `serve` → 目录不存在 → 全 404。**手机与网络正常**（192.168.1.251 → Mac 192.168.1.248 已连通）。用绝对路径 `serve` 即解决。
- **仍待做**：`preview-template/` 已裁定**忽略**（已加 gitignore 规则）；手机端实玩验收；`build:cocos`（wechatgame）待 AppID 后首跑取**主包空体积基线**（G7）。

## 里程碑：用户确认「已基本完成 breakout」→ 验收回填（WXG-T-050）

- **触发**：用户报「已经完成基本 breakout 游戏」。按本仓纪律，里程碑必须落到证据而非自报 ⇒ 先取已有实证，再补差额。
- **核心动作**：把此前**只活在会话里**的验收结论落盘到 `VERSION.md`（此前 `§6` 明写「编辑器里发现不符必须回来更新本文件」，而 G2–G5 一条结论都没回填，下个会话会误判为"从未验证"）。
- **证据分层法（本次最有复用价值的方法论）**：
  1. **能由官方声明回答的 → 给行号引用**。真身不在工程里：`cocos/temp/declarations/cc.d.ts` 是**壳文件**（只有 `/// <reference>`），实际声明在
     `/Applications/Cocos/Creator/3.8.8/…/3d/engine/bin/.declarations/cc.d.ts`（含 JSDoc，3.3 MB）。
  2. **声明回答不了的 → 如实标注"声明未明说"，改由实测闭环**，不猜。
  3. **真未闭环的 → 单独登记，不得记为已关闭**。
- **本次靠声明拿到的硬结论**：`Graphics.rect` 的 `x,y` 是**左上角**（`top left point`）⇒ 左下设计空间**确实需要 y 翻转**（现有实现已被实测证明正确）；`roundRect` **存在**（可去掉降级分支）；`strokeColor/fillColor` getter 返回 **`Readonly<Color>` ⇒ 只能整体赋值**；对齐枚举真名是 `HorizontalTextAlignment`/`VerticalTextAlignment`（**原文误写成不存在的 `Label.HorizontalAlign`**，这正是代码里留"空实现占位"的原因）；`EventType.TOUCH_START = "touch-start"`（代码字符串与官方常量值**逐字一致**）；`getID(): number | null`（**可为 null**）；`schedule` 的 `delay ... Unit: s`。
- **靠实测闭环的**：Label 锚点默认值、`getUILocation` 的原点语义、`schedule(cb,0)` 每帧 + `dt` 按秒（反证：若毫秒球速会快 1000 倍）。
- **真未闭环（登记待办）**：`_anchorForText()` 仍用 `字符数×字号×0.55` **估算**宽度（`cocos-renderer.ts:171`）——文档要求"**必须**用真实 `Label` 尺寸替换"，尚未执行。当前目视通过，但换字号/文本长度会跑偏。
- **G1 关闭**：wechatgame release 构建成功 + 产物命中 `BreakoutBootstrap`。
- **仍差（未做）**：G8 `wx` 适配真机行为、微信基础库版本选定、真机帧率/draw call —— 全部卡在 **AppID**；包体内部目标（3008 → 2000 KB 需先做引擎裁剪）。

## 引擎功能裁剪：主包 3008 → 1815 KB，内部目标首次达标（WXG-T-051）

- **证据前置（先证"真没用到"再裁）**：全量 **`cc` 导入符号实测只有 7 个** —— `Color` / `Component` / `Graphics` / `Label` / `Node` / `UITransform` / `_decorator`；待裁模块引用数**全为 0**。
  **一个假命中值得记**：`Animation` 的 4 处匹配实为 **`requestAnimationFrame`**（浏览器/微信 API），**不是**引擎 `Animation` 模块——**按关键词裁剪必须先看命中上下文**。
- **改动**：`cocos/settings/v2/packages/engine.json` 的 `includeModules` **22 → 10**，关闭 14 个 checkbox（spine/dragon-bones/tiled-map/video/webview/particle-2d/physics-2d-box2d/mask/rich-text/animation/audio/tween…）。改写走 **parse→set→serialize + 解析校验**，非手改文本。
- **效果（release 同口径）**：主包 **3008.3 → 1815.1 KB（−39.7%）**；`cocos-js/` 引擎 **2560 → 1356 KB（−47%）**；gzip 809 → 490 KB；**§3.8② 内部目标 2000 KB 首次达标**，引擎亦回到 1800 KB 预算内；红线余量升至 55.7%。`cocos:check` 类型检查通过。
- **方法教训**：裁剪前先做「**导入符号全集**」取证，比逐模块猜引用可靠得多——`perl -0777` 处理多行 import 是必需的（`import {` 常跨行，单行 grep 会漏）。
- **⚠️ 仍未闭环**：**运行时实玩验收**。类型检查只覆盖编译期，禁模块的运行时行为抓不到。诊断法：浏览器 console 的缺失类名**直接指向被裁模块**，回加该模块重测即可。

