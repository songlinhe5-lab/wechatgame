# 2026-09-15

## 波次3：G4 复验 + 文档漂移回写（WXG-T-092 / T-093，Qoder 主理人会话）

- **用户拍板「甲」两度**：① 阶段0 诊断后选「先复验拿真相比继续堆码重要」；② 复验入库后续推推荐路线。原话只有一字「甲」，语义按 `production/TASKS.md` 注 5/注 6 的拍板谱系追认。
- **T-092（严守真）**：新探针 `g4-probe-v1.1.mjs`（26 组，预期值全取 T-091 回写后的 §8 现文）。改判 **PASS 13 / PASS\* 8 / FAIL 4 / ⛔ 1**（v1.0 为 2/5/11/1）；**4 项 FAIL 与基线严格同集 ⇒ 零新回归**。波次2 修复首次拿到他证：首供第 1 帧、空槽 22/22、告急 1Hz 三通道、A′ 不变量 7933 采样死珠 0、χ²=5.32、harness 往返误差 ≤2.3e-13、§8-9 156/156 写档 0/1。新登 **BD-27..32**；探针自身缺陷再自查 **13 处**（会造成 10 处假 FAIL + 1 处假 PASS + 1 处双向 + 1 处判据误读）。
- **T-093**：`ux-spec §8` U8 行「⏳ 待拍板」→「✅ 已拍板 A′+D」；`accessibility §2` B3 行「脉冲未实现」→「✅ 落地（T-087 三通道，`view-model.ts` L418–424）」。两处同属 K-035 病根家族（状态列 ≠ 实现事实）。
- **提交**：`dafef9a`（T-093 立项+回写）、`5a4f8ba`（T-092 交付入库，含 `production/qa/beads/evidence/` —— 该目录**未被 gitignore**，v1.0 轮证据同批补入库）。

## 主理人裁决：G4 = **CONCERNS**（WXG-T-094，报告 §18.5）

- **不判 FAIL**：v1.0 的 8 项 P0 全关或附条件关、零新回归、主链路在 Node 下全程可跑且 8 关可达。**不判 PASS** 的五条限制：① 只证到指令流层（§G 3 条仍 ⛔）；② beads 主包红线无数据（只有 web-mobile `du -sk` 代理值 1968 KB / 内部目标 2000 KB，两口径禁混用）；③ **门禁自身不可信**（BD-17 `verify` `&&` 短路 + BD-18 `check:size` 缺产物静默 ✅）；④ a11y A2b/A3/C1 与满槽告警通道未兑现；⑤ BD-27/28/29 未裁 ⇒ **BD-25 不关单**。
- **放行范围**：波次4 全部单 + M2 降级 Playtest（音频维度记 BLOCKED）+ 波次2/3 修复不回炉。**不放行**：阶段7 发布、每日挑战评审（既定「G4 **PASS** 前不评」，CONCERNS 不算 PASS）、对「解压/治愈」下任何 PASS。
- **⚠️ 确诊法与事实修正（值得记住）**：QA §18.2 B2 写「解除条件 = 主理人授权代跑 `build:cocos`」——**不成立**。`tools/scripts/build-cocos.mjs` 头注（09-14 实测）明载：`--platform=web-mobile` 可 CLI 自动化（3.9s），但**默认 `wechatgame` 平台需有效 AppID 才构建成功**。⇒ 包体红线实测**挂在用户侧 B4 之下**，本环境唯一能做的是把「缺产物」从静默 ✅ 改成显式 SKIP（T-095）。**教训：登记「解除条件」前必须去脚本头注/实测面核一次，别把「需要授权」当成实际阻塞点。**
- 沉淀：`knowledge/lessons.md` 新增 **K-036**（串联门禁短路 + 缺对象报绿 ⇒ ✅ 不构成证据）、**K-037**（指令流层可证 ≠ 屏幕层可证，综合结论取最弱道次），经 `kb:sync` 入 ledger。

## 波次4 立项（一次占位 T-095~099，防跨 IDE 撞号）

- **顺序**：T-095 门禁可信度（BD-17/18，本环境可全验，成本最低收益最大）→（**T-096 音频后端** ∥ **T-098 B7 裁定包**）→ T-097 P1 反馈缺口（BD-15/16/04 余类/10 半边/BD-32 引导判定）→ T-099 取证通路（像素/色盲滤镜 + `preview:frames` 支持 beads + §H 缺口进探针）→ Playtest M2 降级轮（领号待 T-096 后）。
- **T-096 现状锚点**：`platform/web.ts:57` 与 `weapp.ts:136` 均 `return new NullAudioBackend()`；`games/beads/src` 只有 3 个 clip 常量。规格面已齐备：`audio-spec §6.2` 后端需求单 6 项 + `audio-events §1/§4` A05-01..27 + §3 `AUDIO_*`（v1.16 冻结）。
- **升 G4 PASS 最小集**：T-095 + T-098 + T-099 + AppID 到位后的 `wechatgame` 包体实测。
- **未决（待用户）**：~~波次4 施工起点~~ → 用户第三度拍板「**甲**」＝先 T-095 再接 T-096（见下节）；微信 AppID / 真机窗口何时可给（决定 B2/B4 与阶段7 全部道次）；`develop` 领先 `origin/develop` 的归属与推送源（本会话仍未 push）。

## WXG-T-095：门禁自身可信度修复（BD-17 + BD-18）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-15-s04-t095.md`（685 tok｜原 L24–33｜隶属标记见该件 H1）。
## WXG-T-096：beads 音频后端落码（BD-05 / BD-05b，路线 ADR-0013）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-15-s05-t096.md`（1623 tok｜原 L34–47｜隶属标记见该件 H1）。
## WXG-T-100 占位 + WXG-T-101（pre-commit ①⁷⁄₈ 镜像守卫）

- **用户第四度拍板（三项）**：BD-34 **只立 WXG-T-100 占位不动码**（改的是玩法输入，须单独一轮：先加真链红例 → 再上提输入读取）；镜像漂移守卫 **加**（→ WXG-T-101）；波次4 接续 = **先 spawn T-098 裁定包，再接 T-097**。
- **T-101 落法**：`.githooks/pre-commit` 步骤 ①⁷⁄₈ —— `git diff --cached --name-only | grep '^packages/framework/src/.*\.ts$'` 命中才跑 `sync-framework-to-cocos.mjs --check`。**只拦不写**的关键理由（值得记住）：`git commit` 提交的是**索引**，钩子改写工作区不会进本次提交 ⇒ 自动 sync 反而造出「钩子绿了但镜像仍漏在 HEAD 之外」。
- **验收法（红→绿双向，可复现）**：`printf '\n' >> packages/framework/src/core/audio/audio.ts && git add <该文件>` → `sh .githooks/pre-commit` EXIT=1；再 `framework:sync` + 暂存镜像 → EXIT=0；不含框架源码的提交该步整体不打印。测完 `git checkout` 复位。
- **⚠️ 仓库事实（本会话未 push，但远端已前进）**：`git reflog show origin/develop` 显示 `update by push` 且已指到本会话最后一提交 ⇒ 推送由 IDE/用户侧发生，不是智能体所为；后续引用「领先几个提交」前**先核 `origin/develop` 实际位置**。
- **另一处工作区异常**：`packages/framework/src/platform/node.ts` 第 87 行在 14:58 被改回 `void { }`（本会话未编辑该文件），后果 = `framework:sync:check` 反绿（源码 `{ }` vs 镜像 `{}`）。已 `git checkout` 复位到已提交版本。若要保持 `{ }` 写法，正确做法是**改源码后跑 `pnpm run framework:sync` 让镜像跟随**，而不是手改镜像（§15 绝不手改）。

## WXG-T-098 / T-103：B7 裁定包回写 + QA 改判（波次4 第三、四轮）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-15-s07-t098.md`（1866 tok｜原 L56–72｜隶属标记见该件 H1）。
## WXG-T-097（BD-16 / BD-15）：扩展入口落位与一处测试假绿（波次4 第五轮）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-15-s08-t097.md`（1203 tok｜原 L73–82｜隶属标记见该件 H1）。
## WXG-T-097（BD-10 满槽告警视觉通道）：结论摘要（波次4 第六轮）

> 本节按「摘要 + 指针」写（单日期文件超 ctx 8000 tok 上限，不靠豁免硬扛）。细节正本：
> `production/TASKS-DETAIL.md` §WXG-T-097「进度（2026-09-15·BD-10）」三段；可复用口径已入 `knowledge/lessons.md` **K-043/K-044/K-045**。

- **做了什么**：BD-10「满槽告警只有音、无视觉通道」这半边关闭。`83be050` design → `1654bdc` feat → `4efe5a6` test → `c39ebac` + `cc8065e` test(qa)。**核心玩法区零改动**（延续用户约束「不影响核心区域玩法」）。
- **为何 §3 冻结常量零改动**：动效周期真源在 `ux-spec §5`「满槽告警」行现文（与 `DANGER_PULSE_MS`/`HINT_PULSE_MS` 同族先例一致，均不属 §3）；α 幅度 0.6↔1.0 明写「沿用现有实现值，不构成判据」。
- **两项实测收获**：`timer-gameover §8-10` 由⛔转可测（按 `ux-spec §5:174`「闪烁 = 同一区域内 α 往复」分区读：托盘 2.00Hz / HUD 1.00Hz 各自合规，跨区域合成仅披露不判）；**P7 维持 PASS\* 未升 PASS**（卡点从「缺通道」换成 **BD-35 判据缺 α 幅度**）。
- **不据此宣称全绿**：`verify` = 12P｜1SKIP｜**1FAIL** —— 唯一红是**他人未跟踪文件**`cocos-touch-origin-contract.test.ts`（T-104 阶段 A 自标「今日必红」），未动不删；本单自证 = typecheck 净 + beads **238 passed** + `framework:sync:check` ✅。探针 54 组 = **PASS 29 / PASS\* 14 / FAIL 2 / ⛔ 9**；**G4 仍为 CONCERNS**。
- **本轮三个过程坑的确诊法**（已沉淀，不在此展开）：装载顺序假 FAIL（K-043）、判定上限越界升格（K-044）、并发会话文件级 `add`（K-045）；另 `tray:full` 在**下一次供料尝试**才发 ⇒ 夹具基线必须等到首次广播。证据：`production/qa/beads/evidence/g4-probe-v1.4-t097bd10.log`。

## WXG-T-106：memory 二级详情层落地（波次4 第七轮 · 用户两项拍板）

> **正文已外移**（WXG-T-106，逐字节）→ `memory/details/2026-09-15-s10-t106.md`（1022 tok｜原 L55–66｜隶属标记见该件 H1）。
## WXG-T-111：knowledge 沉淀库按标签分片落地（波次4 第八轮 · 用户裁定「乙：本轮就地做」）

> 「摘要 + 指针」写法同 §WXG-T-097（BD-10）；细节正本 = `production/TASKS-DETAIL.md` §WXG-T-111 完成记录。

- **为何现在做**：`knowledge/lessons.md` 实测 **10768 tok**，越 WXG-T-098 豁免硬到期条（基线 9021 + 800）已 **+1747**；协议禁续期 ⇒ 只能结构性拆分。用户两项拍板：粒度 = **按行内标签切 6 片**、旧路径 = **保留 `lessons.md` 作指针页**。
- **结论口径（今后所有会话照此）**：条目正文在 `knowledge/lessons/<标签>.md`，引用**只写 K-0NN**（路径会跟布局漂）；ID→分片查 `knowledge/INDEX.md` 活跃表「分片」列；`ACTIVE_FILES` 动态枚举 ⇒ **新标签建片即被采集、无需改码**；各片共用单份归档、同受 B 门、**不得加豁免**。本轮撤 1 条到期豁免、**零新增**。
- **不靠猜的实测依据**：`##` 小节与行内标签**不一致**（「跨 IDE」节下挂 5 条 `[工具链]`）⇒ 按小节切不干净，故认行内标签。迁移逐字节：分片前 30 块 ↔ 现 32 块**差异 0**（对照 `dd02a6f` 源复跑）。
- **本轮自己撞出的三条**（都已修，判例入 K-046/K-047）：① 领号只读已提交头注 ⇒ 会撞并发会话占的号；② 一次性工具没有「已完成」判定 ⇒ 二跑把指针页排版行报成「缺 [K-0NN]」假故障；③ 注释引用的 lib 符号名不存在（`LESSONS_TAG_ORDER` ≠ 真实 `LESSONS_SHARD_ORDER`）。另既存缺陷：`knowledge-selftest.sh` **无 pnpm 入口、不在 verify** ⇒ 4 条断言烂了很久没人看见（已补入口，未挂 verify 的债登 **BD-39**）。
- **代价实测（比预估大，已记）**：结构开销 = 指针页 350 + 6 片头注（791 → 收紧后 **545**）；单片最大 4925 tok，对 8000 余量仍足。门禁：`kb:check` 八重 ✅、`kb:selftest` **190/0**、`knowledge:split:selftest` **45/0**、`verify` **PASS 14｜SKIP 1（check:size）｜FAIL 0**。

## WXG-T-112：装置自指类门禁缺陷评估（波次4 第九轮 · BD-38 关单）

**结论**：BD-38 与 `ctx/BUDGET.md` / `ctx/hot-files.md` 是**同一根因**——`ctx:build` 自己写盘的 .md 产物，
在提交索引里取的却是**改写前的旧字节**（`resolveContent` 的 committed / staged-blobs 分支：dirty → HEAD blob、
已暂存 → 暂存 blob），而钩子随后 `git add` 把新字节推进暂存区 ⇒ `ctx:check --staged` 必红 ⇒ 每次涉及它的提交
都要提交两遍。`memory/INDEX.md` 只是**漏登记**进 `lib/context-index.mjs::WORKTREE_AUTHORITATIVE`。

**取证（隔离 `git worktree`，基线 = `3fa3be4`，全程不动在飞工作树）**：改日记一行 → 只 `add` 该日记 → 跑真
`pre-commit`：**基线三种起手（A 只暂存自有改动 / B 手跑 build 并 add 四产物 / C 手跑 build 不 add 产物）第一遍
全部 exit 1，诊断只点名 `memory/INDEX.md`；把该路径加进集合后三起手第一遍全部 exit 0**。三方 sha 对账直接坐实：
`idx=e6c82d12 == head`，而 `staged == worktree == 694e63d8`（另两个产物 `idx == staged == worktree`）。

**两条推翻既有认知的实测**：① 我上一轮记的「不手跑 `ctx:build`、让钩子自建即一次通过」是**错的**——那次一次通过
是因为前两遍失败时钩子已经把产物 add 过了；干净提交照样第一遍红。② WXG-T-072 的「先写产物、后建索引」三步同源
注释在 committed / staged-blobs 模式下**并不成立**，顺序只是必要条件，取源分支才是决定项。

**三类形态判定（Deliverable ①）**：类①（`memory/INDEX.md` ↔ `ctx/BUDGET.md` / `hot-files.md`）同根因 ⇒ 收口；
类③（任务书假设 WXG-T-111 的 `knowledge/lessons.md` 指针页 + `knowledge/INDEX.md` 生成块属同一形态）**不成立**：
前者的写盘方是 `kb:sync`、后者的指针页出自一次性分片器，都不在 `ctx:build` 的写盘链内，其一致性由 `kb:check` ④
守护 ⇒ 不并入本收口（诚实订正任务书假设，不扩面）。

**落码**：`WORKTREE_AUTHORITATIVE` 补 `memory/INDEX.md` 并导出（连带 `BUDGET_MD_REL` / `HOT_FILES_MD_REL`，消掉
`check-context-budget.mjs` 里的重复字面量）；`ctx:check` 新增**装置自指对账** `C:` 级项（生成物 ⊆ 集合，漏登记
当场红并给出修法位置；实测撤掉登记即 `exit 1` 点名）；新增 `tools/scripts/worktree-authoritative-selftest.sh`
（`pnpm run ctx:selftest`，9/0：静态对账 + 三类 gate 取源 + **判别力对照**（普通 dirty → `head`、已暂存 → `staged`，
防空转）+ worktree 端到端红绿双向）；回写 `.githooks/pre-commit` ② 段与 `docs/agent/hooks-best-practices.md §7`。
沉淀 **K-048**。

**两处自查（自己犯的）**：① 注释里写「本门跑在 verify / CI / pre-commit 三处」——**verify 的 15 项里根本没有
`ctx:check`**（grep 零命中），真实覆盖 = CI（`ci.yml` 的 ctx:check 步）+ 本钩子兜底两处 ⇒ 三处措辞已全部订正并
注明本地需显式 `pnpm run ctx:check`（BD-39 同族）。② 自测初版有两处假绿风险：红测那遍漏了重新施加「缺项」源码
（`run_hook_once` 每遍从工作树拷回正确版就把补丁冲掉了）、`String.replace` 的替换串里以 `$` 开头的序列会被按
特殊含义解释 ⇒ 改函数型替换 + 红测显式传入缺项源。

**代价（如实）**：集合内产物的索引记录跟随工作树 ⇒ 钩子 `git add <产物>` 会把工作树里未暂存的产物改动一并纳入
本次提交（三产物同语义、且是既存行为）；`memory/INDEX.md` 标记块**外**允许手写协议正文 ⇒ 手写后须与改动同次暂存。
未削弱任何既有守卫（手改 + add 在基线与修法下都为绿，实测对照过）。
