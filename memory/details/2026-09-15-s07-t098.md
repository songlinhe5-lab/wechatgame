# 隶属 · 2026-09-15 · §WXG-T-098 / T-103：B7 裁定包回写 + QA 改判（波次4 第三、四轮）

> 本节正文自 `memory/2026-09-15.md` **逐字节**外移（WXG-T-106）；日记原位只余节标题 + 指向本件的指针。
> 隶属：`2026-09-15` §WXG-T-098 / T-103：B7 裁定包回写 + QA 改判（波次4 第三、四轮）（原 L56–72）。下方即原节全文（含 `##` 标题行）。

## WXG-T-098 / T-103：B7 裁定包回写 + QA 改判（波次4 第三、四轮）

- **用户拍板「先 spawn T-098 裁定包，再接 T-097」** ⇒ 并行 spawn 文策渊（BD-27/28/29/30）+ 林绘澄（BD-31），**要求成员直接落盘**（历史教训：spawn 回传正文会被系统清空），回传只当摘要用，正文以 `git diff` 为准——本轮两人回传都完整且与落盘一致。
- **容差裁定的可复用形式（K-041）**：`tray-spawner §8-1` = **闭区间 `t∈[0,60]` + 次数轴按「调度名义时刻」计数（±0 不放宽）+ 时刻轴单列 `0 ≤ 滞后 ≤ 2 帧`（非负硬要求）**。2 帧不是手感：= 首供分支不累计 `_acc` 的 1 帧 + `_acc≥interval` 向上取整 1 帧。
- **成员拟改 §3 被否的判例**：文策渊呈请新增 `SIM_FIXED_STEP=1/60s` + `SPAWN_WINDOW_SLACK=2帧` ⇒ **不采**——时基属框架 `GameLoop` 实现事实，进每款游戏的 §3 必造两处漂移；改为在 `systems-index §3` 前言「使用约定」**第 4 条划界定义**（§3 数值零改动，随 **v1.19** 登记）。“要写进真源的到底是值，还是值的口径”——后者优先。
- **作废 ≠ 平凡真（K-040）**：`§8-3`（3:1 抽色）在 `DECOY_COLORS_MAX=0` 下前置不可构造 ⇒ 采**作废（⛔ 不产绿）**，不采「decoys 恒空⇒平凡真」（比例无从观测，记 PASS = 伪绿）；同时写**复活条件**。QA 另开负向用例 P26-N（decoys 非空→BOOT 拒收）但**不得回填成 §8 的绿**。
- **BD-29 的择案与后果链**：>3Hz 闪烁红线（WCAG 2.3.1）**不为一次描边开豁免** ⇒ 改视觉列为**单次脉冲（淡入 60/保持 80/淡出 60）+ 连续拒绝 500ms 重启门**（= §3.8 冻结的 ≤2 次/秒，与 `AUDIO_REJECT_MIN_INTERVAL` 同拍）。**规格冲突一消解，原本“忠实于旧表”的实现立刻转为实现落差 ⇒ P4 诚实记 FAIL**，改码单 **WXG-T-102** 同批立项——否则“文档已改”会被读成“已修”。
- **改判轮的取证法**：主对话**自己重跑探针**而不是只读成员报告（`node production/qa/beads/g4-probe-v1.1.mjs`）⇒ 54 组计数逐条复现（PASS 27 / PASS\* 14 / FAIL 4 / ⛔ 9）。严守真交来的证据里有 **mtime 硬自证**（设计回写 16:17 < 预期值修改 16:24 < 实跑 16:38）——这是“先改预期再跑”惯例的可验做法，下轮沿用。
- **主理人自己的坑（认错记录）**：§19（T-096 轮）建议第 4 条已写明「C1/C3 随 T-098 一次性打包给文策渊」，我写任务书时**漏列** ⇒ 另号 **WXG-T-103** 补做（不把范围 retroactively 扣到 T-098 头上）。C1 = `A05-09` 把伪震屏挂在 `sfx_combo_t3`（实为 tier=2）；C3 = 时长列 7 处分流（5 甲 / 2 乙）。**A05-09 现仍 ⛔**：判据改了但探针未同步（成员无 `production/**` 写权限），已登 backlog。**教训：spawn 前把 QA 报告里“建议随本单一并处理”的行逐条抄进任务书。**
- **顺手清掉的三处文档漂移**（均属“判据侧滞后残留”同族）：`systems-index.md:3` 版本行停在 v1.12（实 v1.18）；`levels-spec.md:23/:42` 仍写 `DECOY_COLORS_MAX=2`（v1.17 已冻为 0 ⇒ 关卡作者会被误导写 decoys）且文首引用 §3 v1.5。新号：**BD-35**（告急行缺 α 幅度→T-102 前置）、**BD-36**（包体分项 1952 vs 实测 1968，Δ16 KB，登 backlog）。
- **工作区又出现外部产物**：`games/beads/cocos/assets/scripts/framework/platform/audio-synth.ts.meta` 与 `game/config/audio-voices.ts.meta` 两个未跟踪文件在本轮中途出现（本会话未创建，疑编辑器侧生成）⇒ **不入提交、不伪造**，与 BD-33 同族处理。
- **T-101 守卫本身有个漏报面（本轮发现并已修）**：上面记的 `grep '^packages/framework/src/.*\.ts$'` **不够**——`sync-framework-to-cocos.mjs` 同时镜像 `games/<game>/src/**`（日志里的「framework 39 + game 26」就是证据），所以只改玩法源码的提交以前能绕过核镜像。已改为 `grep -E '^packages/framework/src/.*\.ts$|^games/[^/]+/src/.*\.ts$'`。**可复用口径：条件守卫的触发面必须等于被守护工具的实际作面集合**，而“实际集合”要从工具输出取证，不能从脚本名推断（`framework:sync` 这个名字本身就在误导）。上方 51/52 两条为当时快照，修正见本条与 TASKS-DETAIL WXG-T-101「追加修正」。
- **上下文预算越界的处理方式（本轮首例 ux-spec）**：§5 两条新注把 `ux-spec.md` 顶到 8683 tok > 8000 ⇒ pre-commit 的 `ctx:check` 直接拦下。选择：**登记豁免 + 写明到期条件**（`ctx/budget-exempt.json` 新增条，reason 记“§4 矩阵与 §5 动效表互为镜像不可割”，note 记“下次单次增幅 >500 tok 先评 §3 线框外移”），取向对齐 WXG-T-041 对 memory 豁免加的到期条件。**判据：豁免可以给，但必须同时留下可执行的拆分手案与触发条，否则豁免只会静默地越积越厚。**
- **同批第二个预算越界＝`knowledge/lessons.md` 9021 tok**：`kb:audit` 无归档候选（41 条全 ≤90 天，沉淀面太年轻），故只能登记豁免——但给了**硬到期条**（2026-10-15 或再增 >800 tok 先到 ⇒ 必须评估「按标签分片 `lessons/<tag>.md` + 同步 `tools/scripts/lib` 的 `ACTIVE_FILES` 与 ctx 锚点」，禁止续期豁免、禁止删正文凑体积）。**口径：豁免只买时间，必须同时写下到期条件与拆分手案；连续两个文件撞同一上限，说明上限该被结构性回应而不是逐文件放行。**
- **证据日志不入库（仓库既有口径）**：`production/qa/beads/evidence/*.log` 被 `.gitignore:55` 的 `*.log` 排除，只跟踪 `diag-*.mjs` 夹具 ⇒ 复验日志以**路径 + mtime**形式被报告引用，不进提交（不是遗漏，别去找它为什么没入库）。
- **门禁**：`check:tasks`（35 行/35 节配对）与 `check:links` 均绿；`pnpm run verify` = **PASS 13 ｜ SKIP 1（check:size）｜ FAIL 0**（含一处纯注释改动的 `src/config/audio-voices.ts` 已重镜像）。沉淀 **K-040 / K-041**，K-039 的“待拍板”子句已改为“已落地（T-101）”。

