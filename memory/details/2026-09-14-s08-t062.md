# 隶属 · 2026-09-14 · §三项待裁定落地（WXG-T-062，用户「按推荐」）

> 本节正文自 `memory/2026-09-14.md` **逐字节**外移（WXG-T-106）；日记原位只余节标题 + 指向本件的指针。
> 隶属：`2026-09-14` §三项待裁定落地（WXG-T-062，用户「按推荐」）（原 L128–147）。下方即原节全文（含 `##` 标题行）。

## 三项待裁定落地（WXG-T-062，用户「按推荐」）

- **① 卡几何：改卡高、不动带位**。`assets-spec §1.4`「卡 176×150 + 卡下方标签 28」与
  `systems-index §3.1` 的 `POWERUP_BAND` 高 **152** 无法同时成立（150+4+28=182 > 152）。
  裁定取 **176×116**（+间隔 4 +标签 28 = 148 ≤ 152）⇒ `§3.1` 与**底部留白 48 一字不动**。
  视图同时补齐 §1.4 的其它漂移：角标 28×28 贴右上内缩 (8,8) + 白 ▶ 边 10、圆角 20、1px 描边、
  投影 α0.10、卡下方 28px 标签（`FONT.sub` 恰为 28）。几何仍只此一份：`powerupCardRects()` /
  `powerupLabelY()` 同时供渲染与 S2 命中测试。
- **A4 转真**：此前 ✅ 是**假绿**（空白卡），WXG-T-060 补形状、本轮补文字标签 ⇒ 两半都在，
  落地计数 12 → **13 项**。新增 `view-model.test.ts` 的 A4 判据：标签文案逐字出现、卡与标签
  都落在带内、卡高 ≥ `TOUCH_MIN`、**三张卡内图元组合互不相同**（「以形状为唯一识别」可测化）。
- **② §8-4 改卡方检验**：原「各槽偏差 ≤ ±20%」在 n=200/12 槽下**约一半概率误报**（12 槽族
  极大偏差期望 ≈20–24%）⇒ 改 χ²（df=11、α=0.01、临界 24.725），**与 seed 无关**、误报率恒定 1%。
- **③ `region` 偶数窗口偏向升格**：由 §6「实现约定」升为 §2.2 表格内明文（含锚点、左 2 右 3），
  §6 改为指引 —— 免得日后两处各说一套（这正是 WXG-T-031 踩过的坑）。
- **顺手清了两笔账**：① backlog 里「到达序判据清理」**被记了两遍**（原行 + 我 T-060 新增的行），
  本轮连同另三项已结条目一并删除，backlog 6 条；② 台账头注被 `tasks:archive` 改写过（去掉了
  「（已完成）」后缀）导致我的锚点失配 —— 用正则替换才改对，**注意归档后头注文本会变**。
- **门禁**：`verify` 退出码 0；framework 234 / breakout 239 / beads 150；拷贝件已同步（写入 4）。

