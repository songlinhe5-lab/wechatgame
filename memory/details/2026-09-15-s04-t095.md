# 隶属 · 2026-09-15 · §WXG-T-095：门禁自身可信度修复（BD-17 + BD-18）

> 本节正文自 `memory/2026-09-15.md` **逐字节**外移（WXG-T-106）；日记原位只余节标题 + 指向本件的指针。
> 隶属：`2026-09-15` §WXG-T-095：门禁自身可信度修复（BD-17 + BD-18）（原 L24–33）。下方即原节全文（含 `##` 标题行）。

## WXG-T-095：门禁自身可信度修复（BD-17 + BD-18）

- **用户拍板「甲」（第三度）**：先做 T-095（纯脚本、本环境可全验，“之后所有的绿才可信”），紧接 T-096 音频后端。
- **BD-17 修法**：新建 `tools/scripts/verify-all.mjs`——14 项（顺序同旧 `&&` 链）**逐项执行永不短路**，子进程 stdout 回放后取**末次** `STATUS: OK\|SKIP\|FAIL` 定性，尾打 `PASS n ｜ SKIP n ｜ FAIL n` 汇总 + 未通过清单，**退出码由汇总定**（0 / 1 / 2=用法错）。两道防回退锁：`--validate`（步骤表须全在 `package.json`，且 `verify` 必等于本聚合器）+ 不短路实测入 `verify:selftest`。
- **BD-18 修法**：`check-bundle-size.mjs` 新增覆盖面计算（`games/*` 逐游戏）+ 导出纯函数 `overallStatus({anyFail, missing})`；缺产物不再打 ✅，而是 `⚠️ 包体校验 **SKIP**（未全覆盖）—— 已测：breakout／未覆盖（1 款，**不是通过**）：beads` + `STATUS: SKIP`；`--strict` 判 exit=1。真跑输出已逐项写入报告 §14 BD-18 行。
- **验收数据**：`check:size --selftest` **19/19**（含 §C1–C6 覆盖面例）、`verify-all --selftest` **9/9**、`bash verify-all-selftest.sh` 五段全绿、全量 `pnpm run verify` = **PASS 13 ｜ SKIP 1（check:size） ｜ FAIL 0**。
- **踩坑（K-030 同族，自测自己也会假绿）**：① 合成步骤 argv 写成嵌套数组 `['node', ['-e', code]]` ⇒ `node: bad option: -e,...`；② 断言 5/6/7 复用含 FAIL 那一轮的结果判「应退出 0」⇒ 假红（需按子集重新构造 OK/BAD/AFTER/SKP 四常量）；③ `verify:selftest` 首版写成内联 `&&` 串接——**与本单精神直接相悖**，改为 `bash tools/scripts/verify-all-selftest.sh`。
- **口径变更（影响以后所有会话）**：“`verify` 绿”从此等价于「14 项都跑过且无 FAIL」；但 **SKIP 仍不等于 PASS**，引用结论时必须逐条列出。回写：`docs/agent/commands.md`（verify 行改写 + 新增 `check:size` 行）、报告 §14 BD-17/18 + §18.2 B3 + §18.5 追加「裁决后续更新」、`test-plan.md` R8、`lessons.md` K-036 补「落地」行。
- **不因本单改判 G4**：限制声明②（beads 主包红线无数据）**依旧成立**——修的是“静默假绿→显式 SKIP”，不是替 beads 测出体积。G4 仍 CONCERNS。

