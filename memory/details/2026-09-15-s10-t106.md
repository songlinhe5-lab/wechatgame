# 隶属 · 2026-09-15 · §WXG-T-106：memory 二级详情层落地（波次4 第七轮 · 用户两项拍板）

> 本节正文自 `memory/2026-09-15.md` **逐字节**外移（WXG-T-106）；日记原位只余节标题 + 指向本件的指针。
> 隶属：`2026-09-15` §WXG-T-106：memory 二级详情层落地（波次4 第七轮 · 用户两项拍板）（原 L55–66）。下方即原节全文（含 `##` 标题行）。

## WXG-T-106：memory 二级详情层落地（波次4 第七轮 · 用户两项拍板）

- **用户拍板（一句内两项）**：「布局取 `memory/details/` 单层」+「批次2 本轮一起做完并撤销那两条豁免」⇒ 不建多级目录、09-12/09-14 不延到下轮、WXG-T-030/065 两条 budget-exempt 直接删。**本轮零新增豁免**。
- **要解决的雷（结构性，不是本轮写不下）**：日记是追加式的 ⇒ 单日期文件必反复撞 `ctx:check` B 门（8000 tok）；09-15 已真被 pre-commit 拦过（8693 tok）。上轮把当日段「压成摘要 + 指针」降到 7876，**余量仅 124 tok** ⇒ 压缩不是可持续解；压摘要＝丢细节，加豁免＝违 T-041 的裁决原文。
- **四层结构**：`MEMORY.md`（常驻摘要）→ `YYYY-MM-DD.md`（**当日骨架**：标题 + 指针）→ `details/<日期>-<slug>.md`（**正文层**）→ `INDEX.md`（入口）。详情件天然进索引面 ⇒ **机制自带防再膨胀**。
- **四件交付**：① 生成器 `lib/memory-index.mjs` 新增「详情」列（由详情件 **H1 隶属标记**派生，只读 `index.sections` 不读正文）；② `ctx:check` 新增 **C-③** 双向硬拦（孤儿 + 断链，push 入 `failures` 不降级 note）；③ `memory:distill` **连座归档**（该天详情件同进同退，组内任一重名则整组跳过；全量 read → 全量 write+回读校验 → 统一 rm）；④ 可复用外移器 `split-memory-detail.mjs`（**节解析复用门禁侧 `makeFileRecord`**，杜绝两套口径漂移）。
- **迁移与自证**：15 节逐字节外移——09-12 10141→3132、09-14 12586→6911、09-15 7876→2674 tok。**还原性自证**：一次性脚本把「骨架 + 详情件正文」回拼与 `git show HEAD:` 比对，三篇**全部逐字节一致**（不拿「看起来没丢」交差）。桩自测：split **28 PASS**、distill **56 PASS**（含反向造孤儿，证明硬拦不是纸面硬拦）。
- **两个当场发现并修的设计缺陷**：① 已外移行的「摘要」列只会抽出骨架指针句并被截成半截括号 ⇒ 改取详情件里的**原节首句**（否则索引自毁导航价值）；② `split-memory-detail.mjs` 头注写“永不覆盖、整节跳过告警”而实现是 **fail loud exit 1** ⇒ 订正头注（文档↔实现不得两套）。
- **跨会话撞号（本号未改，但根因已抓）**：并发会话 T-104 收尾裁定文内写「④ 取证固化 = **WXG-T-106**」，而其派单**实际落在 108** ⇒ 旧字面未订正，不构成本号冲突（本号已随 `1f73e2b` 入库）。根因：**领号只读了已提交头注（当时最大 103），而对方占的 105–108 全在工作树未提交** ⇒ 领号必以**工作树全文**全局最大号为准（K-046，与 K-045 同族）。
- **门禁复验成对口径（可复用）**：新建未跟踪件后必 `ctx:build --working-tree && ctx:check --working-tree`（默认 committed 模式会 skip 未跟踪件）。结果：C 项「✅ 15 个详情文件，隶属/指针双向可解析」；B 门已无 09-12/09-14 两行。**唯一红 = `ADR-0011` 8446 tok**，属并发会话 T-104 未提交改动，**不代拆、不为其新增豁免**。
- **代价诚实记录**：`memory/INDEX.md` 本体 5513→**6606 tok**（+20%，协议文字 + 新列），仍 <8000；若日报继续追加，索引层自己也会需要拆（已登记，不属本单）。

