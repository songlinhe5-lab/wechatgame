# 隶属 · 2026-09-14 · §里程碑：用户确认「已基本完成 breakout」→ 验收回填（WXG-T-050）

> 本节正文自 `memory/2026-09-14.md` **逐字节**外移（WXG-T-106）；日记原位只余节标题 + 指向本件的指针。
> 隶属：`2026-09-14` §里程碑：用户确认「已基本完成 breakout」→ 验收回填（WXG-T-050）（原 L41–55）。下方即原节全文（含 `##` 标题行）。

## 里程碑：用户确认「已基本完成 breakout」→ 验收回填（WXG-T-050）

- **触发**：用户报「已经完成基本 breakout 游戏」。按本仓纪律，里程碑必须落到证据而非自报 ⇒ 先取已有实证，再补差额。
- **核心动作**：把此前**只活在会话里**的验收结论落盘到 `VERSION.md`（此前 `§6` 明写「编辑器里发现不符必须回来更新本文件」，而 G2–G5 一条结论都没回填，下个会话会误判为"从未验证"）。
- **证据分层法（本次最有复用价值的方法论）**：
  1. **能由官方声明回答的 → 给行号引用**。真身不在工程里：`cocos/temp/declarations/cc.d.ts` 是**壳文件**（只有 `/// <reference>`），实际声明在
     `/Applications/Cocos/Creator/3.8.8/…/3d/engine/bin/.declarations/cc.d.ts`（含 JSDoc，3.3 MB）。
  2. **声明回答不了的 → 如实标注"声明未明说"，改由实测闭环**，不猜。
  3. **真未闭环的 → 单独登记，不得记为已关闭**。
- **本次靠声明拿到的硬结论**：`Graphics.rect` 的 `x,y` 是**左上角**（`top left point`）⇒ 左下设计空间**确实需要 y 翻转**（现有实现已被实测证明正确）；`roundRect` **存在**（可去掉降级分支）；`strokeColor/fillColor` getter 返回 **`Readonly<Color>` ⇒ 只能整体赋值**；对齐枚举真名是 `HorizontalTextAlignment`/`VerticalTextAlignment`（**原文误写成不存在的 `Label.HorizontalAlign`**，这正是代码里留"空实现占位"的原因）；`EventType.TOUCH_START = "touch-start"`（代码字符串与官方常量值**逐字一致**）；`getID(): number | null`（**可为 null**）；`schedule` 的 `delay ... Unit: s`。
- **靠实测闭环的**：Label 锚点默认值、`getUILocation` 的原点语义、`schedule(cb,0)` 每帧 + `dt` 按秒（反证：若毫秒球速会快 1000 倍）。
- **真未闭环（登记待办）**：`_anchorForText()` 仍用 `字符数×字号×0.55` **估算**宽度（`cocos-renderer.ts:171`）——文档要求"**必须**用真实 `Label` 尺寸替换"，尚未执行。当前目视通过，但换字号/文本长度会跑偏。
- **G1 关闭**：wechatgame release 构建成功 + 产物命中 `BreakoutBootstrap`。
- **仍差（未做）**：G8 `wx` 适配真机行为、微信基础库版本选定、真机帧率/draw call —— 全部卡在 **AppID**；包体内部目标（3008 → 2000 KB 需先做引擎裁剪）。

