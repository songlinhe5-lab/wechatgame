# 隶属 · 2026-09-15 · §WXG-T-097（BD-16 / BD-15）：扩展入口落位与一处测试假绿（波次4 第五轮）

> 本节正文自 `memory/2026-09-15.md` **逐字节**外移（WXG-T-106）；日记原位只余节标题 + 指向本件的指针。
> 隶属：`2026-09-15` §WXG-T-097（BD-16 / BD-15）：扩展入口落位与一处测试假绿（波次4 第五轮）（原 L73–82）。下方即原节全文（含 `##` 标题行）。

## WXG-T-097（BD-16 / BD-15）：扩展入口落位与一处测试假绿（波次4 第五轮）

- **用户约束：“不要影响核心区域玩法”** ⇒ BD-15 三个候选里取 **丙-A**（`TRAY_BAND` 上沿 420→450、托盘面板由「带内居中」改「贴带上沿」）：`PUZZLE_BAND` / `gridLayoutFor()` / 关卡数据 / 托盘容量**零改动**。**甲**（把按钮挤到带上沿间隙）与 12 行关最底行格热区只剩 **3px** 刀锋净空，否；**丙-B**（`TRAY_COLS` 12→8）会改基础容量 12→8，属玩法数值，否。另记：**带加高本身不解决重叠**——面板若仍居中，每侧净空 ≥88 需要带高 ≥292 ⇒ 上沿反而撞 12 行格热区（481），“改尺寸”不等于“改锚定”。
- **判据冲突新号 BD-37**：`input-control §8-1` 字面「点扩展→S4 收请求」在 `powerups §2.6` 布局 A（MVP 无解锁路径）下**不可达** ⇒ 实装为「命中即吞 + 一次性占位轻提示（零事件/零槽变化/零扣次/不调 wx）」，该腿 QA 记 **⛔ 不可验**，**不得因占位实装打 PASS**（探针 P8 因此至多 PASS\*）。已登 backlog（随解锁上线同批改 §8-1 + `ux-spec §4`）。
- **本轮最有价值的一条：测试假绿（K-042）**——`pause-settings.test.ts::trayPoint()` 自己重推带中线公式，v1.20 后整组脱靶 ⇒「PAUSED 点托盘零响应」退化为**永真断言**。修法定三件事：改 import `trayLayout()`、补**正向对照**（PLAYING 点未选中槽必生 1 次 `tray:selected`）、几何净空另立不变量断言（**M3 变异在此转红**：`expected 309 to be greater than 318`）。对照首跑即失败又送一个口径：`tapDesign()` 返回 `相位变化 or _consumedTap`，选中分支**不置旗** ⇒ 真阳性只能以**事件**为准，别拿返回值当“状态变了”。
- **探针侧也能造出假 FAIL（P10）**：原判据只查旧字段 `powerupHint/failHint/banner`，且 `sig()` 为了防 mm:ss 伪满足**刻意剔除 text 指令**，而 BD-16 轻提示正走 text 通道 ⇒ 已实现反被判 FAIL。补 `textSig` 配对差分（同 seed 同帧号，点 vs 不点）+ 锚点 `{anchor,row,col}` 校验后转 PASS。**口径：取证工具的通道必须覆盖被测行为的载体**（与假绿同害，且更难发现，因为它“看起来在严格”）。
- **主对话自己的坑（认错记录）**：因文件名无版本号，我把 `slotXY` 改动错落到了 `production/qa/beads/g4-probe.mjs`（波次 1 v1.0 历史夹具）上，而不是现役的 `g4-probe-v1.1.mjs`。**确诊法**：跑完看汇总只有 19 组（现役 **54 组**）且 FAIL 理由引用已删字段与旧行号 ⇒ 必是旧夹具。处置：**全量回退旧文件**，只加 6 行「已冻结」声明（禁止逐条改预期，否则造出半新半旧的证据链），改动移植到 v1.1。**可复用口径**：改“看起来像是现役”的取证文件前，先核它是否被引用（本轮 grep 确认 CI/verify/hooks **均不引用任何探针**，探针是人工证据），并看其输出量级与最新报告是否一致。
- **提交细粒度（5 笔，均含 WXG-T-097）**：`ffb8bd3` design（§3.4 v1.20）→ `88bd091` feat（代码）→ `2acafc9` test → `61af856` docs（三文档回写）→ `66c0216` test(qa)（探针改判）。先文档后代码的序仍成立。**一次危险动作已避免**：`git add games/beads/cocos/assets/scripts/game` 把 stray `audio-voices.ts.meta` 一并扫进暂存区，提交前 `git diff --cached --name-only` 才发现并 `git restore --staged`——镜像目录做 add 目标时必逐次复核暂存列表。
- **当前基线**：beads vitest **233 通过（22 文件）**；`verify` = **PASS 13 ｜ SKIP 1（check:size）｜ FAIL 0**；探针 v1.1 = **PASS 28 / PASS\* 15 / FAIL 2 / ⛔ 9（54 组）**，剩余 FAIL = P4（→T-102）与 P17（BD-12）。**G4 仍为 CONCERNS**，局部转绿不升格。T-097 余项：BD-10 满槽告警多通道半边（P7 现 PASS\*）、BD-32 引导判定、BD-04 余类 VFX。

