# knowledge/CHANGELOG.md — 知识库变更日志

> **本文件由 `pnpm run kb:sync` 生成，请勿手改**（手改会被下次 sync 覆盖）。
> 统计口径：`added` 新增条目 ／ `updated` 修改条目（正文变化，按 `contentHash` 判定）
> ／ `reactivated` 从归档重新激活 ／ `archived` 归档。
> 数据源 = `knowledge/ledger.json` 的 `events`（**append-only** 追加日志）+ 条目台账（标题取自台账）；
> 日期倒序，同一日期内按 Task ID 分组。

## 2026-09-26

### （无任务号）

- **新增**（added）：K-078「跨域共用批次号（`v1.5-rN` 族）取号必须全仓扫占用，「本文件已见最大号」推不出空号」、K-079「「参数还在、通道已死」：换肤/转正会让表现层入参失去承载体，改值前先验消费者」 —— K-078 / K-079 属 beads 选中抬起立体感批（未挂 Task 号）；原记 WXG-T-169 = 并发会话 kb:sync 串号，本行为订正（条目正文来源行与 sourceTask 自始为「未挂 Task 号」/ null）
- **新增**（added）：K-080「`kb:sync` 的「谁先跑谁归因」：无主条目会被恰好先运行的那次 sync 记进别人的 Task 号」 —— beads 立体感批 · 并发 sync 串号坑入账
- **修改**（updated）：K-078「跨域共用批次号（`v1.5-rN` 族）取号必须全仓扫占用，「本文件已见最大号」推不出空号」、K-079「「参数还在、通道已死」：换肤/转正会让表现层入参失去承载体，改值前先验消费者」、K-080「`kb:sync` 的「谁先跑谁归因」：无主条目会被恰好先运行的那次 sync 记进别人的 Task 号」

### WXG-T-169

- **新增**（added）：K-076「脏工作树里「这条测试红是不是我引入的」必须用 stash 隔离实测判定，不得凭直觉归因」、K-077「给既有渲染路径加缩放/档位能力时，必须盘点该路径所有「绝对像素量」——凡与比例制几何同屏对照者要一起归一」
- **修改**（updated）：K-045「同一台账文件被并发会话同时写时，`git add <整文件>` 会把别人的 WIP 捎进本单提交」
- **修改**（updated）：K-045「同一台账文件被并发会话同时写时，`git add <整文件>` 会把别人的 WIP 捎进本单提交」、K-076「脏工作树里「这条测试红是不是我引入的」必须用 stash 隔离实测判定，不得凭直觉归因」、K-077「给既有渲染路径加缩放/档位能力时，必须盘点该路径所有「绝对像素量」——凡与比例制几何同屏对照者要一起归一」

### WXG-T-214

- **新增**（added）：K-081「台账详情节的节标题只认「整行形状」：`## WXG-T-0NN` 后带文字 = 节静默丢失」、K-082「珠体族视觉改动 ⇒ 封箱对照腿必然红是预期行为，唯一合法通道 = 复评归因（禁自更新基准）」、K-083「「面积占比 ≥X%」是代理指标不是感知标准；感知判据 = 对比度（3:1）+ 样本视角（≥1°），且面积账必须含圆角修正」
- **修改**（updated）：K-081「台账详情节的节标题只认「整行形状」：`## WXG-T-0NN` 后带文字 = 节静默丢失」、K-082「珠体族视觉改动 ⇒ 封箱对照腿必然红是预期行为，唯一合法通道 = 复评归因（禁自更新基准）」、K-083「「面积占比 ≥X%」是代理指标不是感知标准；感知判据 = 对比度（3:1）+ 样本视角（≥1°），且面积账必须含圆角修正」

## 2026-09-24

### WXG-T-208

- **新增**（added）：K-073「外部 skill 批次接入：上游正文零改写 + 本地差异集中登记到 INDEX 批次节」、K-074「接外部能力前先机械验源：「仓库存在」≠「有可转换的 SKILL.md」」
- **修改**（updated）：K-073「外部 skill 批次接入：上游正文零改写 + 本地差异集中登记到 INDEX 批次节」、K-074「接外部能力前先机械验源：「仓库存在」≠「有可转换的 SKILL.md」」

### WXG-T-209

- **新增**（added）：K-075「外部决策模型（Jev/System One 类）接入前先过四条件滤网；beads-studio 用例已证伪并拆除，候选场景留档待立项」

## 2026-09-22

### WXG-T-186

- **新增**（added）：K-067「「就近」排序的度量必须与集合的连通定义同度量，且多消费口共用一份序」
- **修改**（updated）：K-067「「就近」排序的度量必须与集合的连通定义同度量，且多消费口共用一份序」

### WXG-T-203

- **新增**（added）：K-068「关卡生成链四段连锁模式」、K-069「部分错位「整区域就位 + 子集置换」构造模式」
- **修改**（updated）：K-068「关卡生成链四段连锁模式」、K-069「部分错位「整区域就位 + 子集置换」构造模式」

## 2026-09-20

### WXG-T-179

- **修改**（updated）：K-045「同一台账文件被并发会话同时写时，`git add <整文件>` 会把别人的 WIP 捎进本单提交」、K-056「渲染里「未解锁/扩展」的视觉语言必须随容量语义常量派生：裸行号判定会把已开放行画成未开放，并反噬成「假容量」缺陷报告」
- **修改**（updated）：K-036「多项串联的门禁若「失败即短路」且「缺对象仍报绿」，它的 ✅ 不构成任何证据」、K-045「同一台账文件被并发会话同时写时，`git add <整文件>` 会把别人的 WIP 捎进本单提交」
- **新增**（added）：K-064「跨工具链的「草案自报元数据」必须被消费端复核归一：手工合规草案的单测永远抓不到生产端的字段说谎」
- **修改**（updated）：K-064「跨工具链的「草案自报元数据」必须被消费端复核归一：手工合规草案的单测永远抓不到生产端的字段说谎」
- **新增**（added）：K-065「国内云 VPS 上 Docker 部署必踩的两道坎：镜像仓库不可达 + 云侧安全组（与主机防火墙是两层）」
- **新增**（added）：K-066「长期分支间用 squash 同步 ⇒ 下次同步「全域冲突」，且 3-way 会静默产出重复定义」
- **修改**（updated）：K-066「长期分支间用 squash 同步 ⇒ 下次同步「全域冲突」，且 3-way 会静默产出重复定义」
- **修改**（updated）：K-066「长期分支间用 squash 同步 ⇒ 下次同步「全域冲突」，且 3-way 会静默产出重复定义」

### WXG-T-183

- **新增**（added）：K-063「会「静默转移」的锚不能当距离排序基准：消费序基准与展示坐标必须分离」

## 2026-09-19

### WXG-T-128

- **新增**（added）：K-060「变异自检里断言「方向」选错会留下伪绿：被测标量在变异下「冻结在旧非零值」而非归零时，存在性断言无判别力」、K-061「`noAssemble` 下手工铺盘 ≠ 装配：子系统内部索引未建，表现为「点名了但不动手」，极易误判为业务缺陷」
- **修改**（updated）：K-045「同一台账文件被并发会话同时写时，`git add <整文件>` 会把别人的 WIP 捎进本单提交」
- **修改**（updated）：K-060「变异自检里断言「方向」选错会留下伪绿：被测标量在变异下「冻结在旧非零值」而非归零时，存在性断言无判别力」

### WXG-T-164

- **修改**（updated）：K-053「冻结「语义/行为」反转（数值不变）走「反转批」固定顺序，防漏回写散落的旧裁定」

### WXG-T-166

- **新增**（added）：K-057「「立项单」与「ADR 状态行」都不会被实施它们的另一张单自动收口，核销时要成对扫」

### WXG-T-167

- **新增**（added）：K-059「用「甲/乙/丙」转述既有候选清单时必须带正文锚——字母会换义」

### WXG-T-169

- **修改**（updated）：K-042「几何单一真源化后测试失去自证能力，须另配不依赖该真源的不变量守卫」

### WXG-T-176

- **新增**（added）：K-058「「已写进 .gitignore」不等于「已排除出索引产物」——枚举型走盘生成器只认自己的硬编码跳过集」

### WXG-T-178

- **新增**（added）：K-062「事件驱动宿主的输入位移必须进单测：prev 在帧首结算会把帧间到达的位移吞成 0」

## 2026-09-18

### WXG-T-165

- **新增**（added）：K-053「冻结「语义/行为」反转（数值不变）走「反转批」固定顺序，防漏回写散落的旧裁定」、K-054「「未登记」不等于「已证伪」：纸面缺账不能推翻真机实证，回滚前必须分清这两种缺失」

### WXG-T-168

- **新增**（added）：K-055「「连通块」API 的返回序是行主序、不是距锚序：「就近/最近」语义取前 N 前必须显式重排」、K-056「渲染里「未解锁/扩展」的视觉语言必须随容量语义常量派生：裸行号判定会把已开放行画成未开放，并反噬成「假容量」缺陷报告」
- **修改**（updated）：K-046「领号必须读「工作树全文」的全局最大号，只读已提交的头注必撞号」

## 2026-09-17

### （无任务号）

- **新增**（added）：K-051「规格里的「图元/包体增量」数值是立项时点的推论，落码后必须差分复算、不得沿用」、K-052「动效时长常量一旦决定「事件发生在哪一帧」，它就是玩法时序而非表现层参数，判据与守卫都得换档」
- **修改**（updated）：K-051「规格里的「图元/包体增量」数值是立项时点的推论，落码后必须差分复算、不得沿用」

## 2026-09-16

### WXG-T-119

- **新增**（added）：K-050「缺陷号（`BD-NN`）与任务号同规：领号须读工作树全文，且「已登记 BD-xx」的声明必须回查台账是否真有正本行」
- **修改**（updated）：K-050「缺陷号（`BD-NN`）与任务号同规：领号须读工作树全文，且「已登记 BD-xx」的声明必须回查台账是否真有正本行」
- **修改**（updated）：K-050「缺陷号（`BD-NN`）与任务号同规：领号须读工作树全文，且「已登记 BD-xx」的声明必须回查台账是否真有正本行」

### WXG-T-121

- **新增**（added）：K-049「自测夹具必须由被测生成器播种、依赖按整目录拷贝，否则门禁一扩就整片假红」

## 2026-09-15

### （无任务号）

- **新增**（added）：K-035「文档状态列混淆「规格已写」与「代码已实现」= 可访问性假绿的温床」
- **新增**（added）：K-036「多项串联的门禁若「失败即短路」且「缺对象仍报绿」，它的 ✅ 不构成任何证据」、K-037「「指令流层可证」≠「屏幕层可证」：Node 探针全绿不得抬升表现层结论」
- **修改**（updated）：K-036「多项串联的门禁若「失败即短路」且「缺对象仍报绿」，它的 ✅ 不构成任何证据」
- **新增**（added）：K-038「自动化统一走「便捷旁路 API」，真输入链可以整段断链而全绿」、K-039「报绿之后又动了源码：交付自述必须与最终状态同一时点」
- **新增**（added）：K-040「判据回写轮的三个读数陷阱：改严≠回归、作废≠平凡真、文档一致≠代码一致」、K-041「容差写法：不得吞掉被测变更，帧量化只进时刻轴」
- **修改**（updated）：K-039「报绿之后又动了源码：交付自述必须与最终状态同一时点」
- **修改**（updated）：K-039「报绿之后又动了源码：交付自述必须与最终状态同一时点」
- **新增**（added）：K-042「几何单一真源化后测试失去自证能力，须另配不依赖该真源的不变量守卫」

### WXG-T-097

- **新增**（added）：K-043「取证脚本里「装载动作」必须写在模块 import 之前，否则拿到上一轮旧产物造出假 FAIL」、K-044「判定上限由「判据完备性」决定，不由实现进度决定；且不得把「缺通道」当预期写进 PASS 条件」
- **修改**（updated）：K-043「取证脚本里「装载动作」必须写在模块 import 之前，否则拿到上一轮旧产物造出假 FAIL」、K-044「判定上限由「判据完备性」决定，不由实现进度决定；且不得把「缺通道」当预期写进 PASS 条件」
- **新增**（added）：K-045「同一台账文件被并发会话同时写时，`git add <整文件>` 会把别人的 WIP 捎进本单提交」
- **修改**（updated）：K-045「同一台账文件被并发会话同时写时，`git add <整文件>` 会把别人的 WIP 捎进本单提交」

### WXG-T-111

- **新增**（added）：K-046「领号必须读「工作树全文」的全局最大号，只读已提交的头注必撞号」、K-047「幂等判据必锁「自己写出的产物字形」，写方与读方不同源时假绿与自毁同时存在」
- **修改**（updated）：K-047「幂等判据必锁「自己写出的产物字形」，写方与读方不同源时假绿与自毁同时存在」

### WXG-T-112

- **新增**（added）：K-048「装置自指文件必须让索引取工作树字节，否则重建-add-校单遍不收敛」

## 2026-09-13

### （无任务号）

- **新增**（added）：K-031「多产物生成器新增产物时，必须同任务登记 pre-commit 的 `git add` 清单」
- **修改**（updated）：K-004「改过被索引的 `.md` 后必须重跑 `ctx:build`，否则 C 门必红」
- **新增**（added）：K-032「「逃生阀」两侧都支持时，文档必须写成」
- **新增**（added）：K-033「能否用符号链接做「一处正本」，判据是」

### WXG-T-044

- **新增**（added）：K-034「协议第二跳的准入条件不能沿用「热度 + 体积」——引用面覆盖率必须独立成硬门（D2）」

## 2026-09-12

### WXG-T-029

- **新增**（added）：K-029「自增 ID 的下界必须取自既有数据，不能只看计数器」、K-030「产物进索引前先确认「新鲜度契约」——脚本自测会假绿」
