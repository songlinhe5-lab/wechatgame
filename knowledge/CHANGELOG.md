# knowledge/CHANGELOG.md — 知识库变更日志

> **本文件由 `pnpm run kb:sync` 生成，请勿手改**（手改会被下次 sync 覆盖）。
> 统计口径：`added` 新增条目 ／ `updated` 修改条目（正文变化，按 `contentHash` 判定）
> ／ `reactivated` 从归档重新激活 ／ `archived` 归档。
> 数据源 = `knowledge/ledger.json` 的 `events`（**append-only** 追加日志）+ 条目台账（标题取自台账）；
> 日期倒序，同一日期内按 Task ID 分组。

## 2026-09-13

### （无任务号）

- **新增**（added）：K-031「多产物生成器新增产物时，必须同任务登记 pre-commit 的 `git add` 清单」
- **修改**（updated）：K-004「改过被索引的 `.md` 后必须重跑 `ctx:build`，否则 C 门必红」
- **新增**（added）：K-032「「逃生阀」两侧都支持时，文档必须写成」
- **新增**（added）：K-033「能否用符号链接做「一处正本」，判据是」

### WXG-T-044

- **新增**（added）：K-034「协议第二跳的准入条件不能沿用「热度 + 体积」——引用面覆盖率必须独立成硬门（D2）」

## 2026-09-12

### WXG-T-029

- **新增**（added）：K-029「自增 ID 的下界必须取自既有数据，不能只看计数器」、K-030「产物进索引前先确认「新鲜度契约」——脚本自测会假绿」
