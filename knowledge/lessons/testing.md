# lessons · `testing` 分片（标签 `测试`）

> 由 `knowledge/lessons.md`（WXG-T-111 按行内标签分片）逐字节搬运而来；本片条目**按 ID 升序**。
> 引用只写 **K-0NN**；口径正本（B 门不豁免 / 新标签 = 新片）见 `knowledge/INDEX.md` §1–§4。

## 测试

- **[测试][K-038] 自动化统一走「便捷旁路 API」，真输入链可以整段断链而全绿**（来源 WXG-T-096 附带发现 BD-34，2026-09-15）
  现象：beads 所有面板用例与 QA 探针 P22 都用 `game.tapDesign()` 驱动 ⇒ 225 例连绿；但 `_readInput()` 的唯一调用点在 `_stepPlaying` 内，`App.tick` 又是 `beginFrame → game.update → endFrame`（一次性标志每帧被清），于是 PAUSED / LEVEL_CLEAR / GAME_OVER / FINISH 四相位在**真链**（harness 鼠标、Cocos touch → `InputManager.push`）上收不到任何点击——面板按钮全绿地不存在。
  根因：测试入口与被测系统的入口不是同一条；旁路 API 为了便利绕过了状态机里唯一读输入的那一步，而没有任何断言约束「旁路与真链必须等效」。
  代价：这是用户最初「完全没法玩」里最后一块 P0/P1，靠 Node 侧回归三轮都没测到；若进真机才被发现，成本是整轮返工。
  规避：① 关键交互必须各有一条**经 `InputManager` 真链**的用例（旁路只用于装配前置），二者断言同一结果；② 给「相位 × 输入」建矩阵守卫（`input-control §2.3` 已有表，可机检：非 playing 相位也必须能命中面板按钮）；③ 评审时追问「这条绿走的是哪条入口」，凡便捷 API 都要问它是否等价于宿主接线。
  判例引用：`production/qa/beads/g4-regression-report.md` §15.2 BD-34、evidence/diag-p5-fixtures.mjs（同坐标同帧序：真链无派发 / 旁路生效）；同族 K-035、K-037。

- **[测试][K-042] 几何单一真源化后测试失去自证能力，须另配不依赖该真源的不变量守卫**（来源 WXG-T-097/BD-15，2026-09-15）
  现象：把渲染与命中都改接同一份 `trayLayout()` 后做变异检验：把锚定改回旧行为（带内居中），整套测试仍然**全绿**；同时另一条自己重推「带中线」公式的测试在带加高后整组脱靶，变成永真断言（点了个空处，当然零响应）。
  根因：单一真源只保证**一处改、处处一致**，不保证那一处**对**；而任何在测试/探针里重推公式的地方都是静默漂移温床，一旦与真源分叉，断言对象从「不该有反应」退化为「什么都没点」。
  代价：错锚定可以无阻力入仓；真正靠它才成立的结论（可访问性热区、重叠仲裁）看起来已验收，实则从未被测定。
  规避：① 测试与探针内**禁止重推几何公式**，一律 import 真源；② 必须另写一条**不变量断言**（净空 / 不重叠 / 上下界关系），其预期值来自**规格字面值或另一条独立推导**而不共用于被测真源，让错锚定必红；③ 每条「零响应 / 零事件」类断言都要配一条**正向对照**（同坐标在应响应的相位下确实产生事件），否则永真无从发现；④ 真阳性判据选**事件**而不选函数返回值——返回值常只表「吞掉」分支，与「状态是否改变」不等价。
  判例引用：`games/beads/tests/feedback-vfx.test.ts` BD-15 几何净空条（M3 变异在此转红：`expected 309 to be greater than 318`）、`games/beads/tests/pause-settings.test.ts` 正向对照；同族 K-035（假绿谱系）。
  追记（2026-09-19，WXG-T-169 收口批·§A4c.2 落点补齐时重现）：规避③「零响应类断言必须配正向对照」**不只在旧测试里失效，新写的用例同样会当场中招**。本批新写「drag 途中跨格 ⇒ 零落子零选中」例，第一版把**抬起点选在拖拽终点**（该格与已备珠颜色不同 ⇒ 落子本就不合法）⇒ 对「阈值被改到 `drag` 永不成立」的变异**不红**：绿得完全没有信息量。把抬起点改回**已备珠的那个格心**（同点在单指 tap 下确实会落子 = 正向对照同位）后，同一变异立即转红，例子才真正锁住 drag 判定。**做法补一条**：写完零事件例子先不要登记「已验」，而是问「**同坐标同帧形下，它该红的那次会红吗**」——若答不出，就是把断言域选在了非法/无效位置上；先找共位正向对照，再跑变异。

- **[测试][K-060] 变异自检里断言「方向」选错会留下伪绿：被测标量在变异下「冻结在旧非零值」而非归零时，存在性断言无判别力**（来源 WXG-T-128 / commit 61d2911，2026-09-19）
  现象：为「扫光不因状态切换中断」写判据，首版断言「过关帧 `sweepProgress > 0`」。按 K-036 做变异自检——给 `_stepSweepFx` 加 `playing` 门（**正是该断言要否定的那个实现**）——**判据仍然全绿**：变异下扫光并未归零，而是**冻结在 0.79**（离开 `playing` 后步进被 `return` 掉，旧值原样留着）⇒ `> 0` 恒真。改成「过关后**仍在推进**（`sweepProgress > pAtClear`）+ 越过窗口后归零」两条后，同一变异立即红于 `expected 0.79 to be greater than 0.79`。
  根因：变异自检只回答「**断言红了没有**」，不回答「**断言凭什么红**」。当被测量是**单调推进的时间/进度标量**时，`> 0` / `!= null` / `is defined` 这类**存在性断言**与「暂停 / 冻结 / 短路」型缺陷正交 —— 缺陷让标量**停在中间值**，存在性依然成立。判别力只能落在**差值或终态**上，不能落在「有没有值」。
  规避/做法：① 时间/进度类标量一律断言**单调增量**（下一帧 > 本帧）或**终态**（窗口过后归零 / 复位），存在性断言只作前置；② 选「**必然让被测行为偏离**」的注入点，并在跑之前先预测「这条断言会怎么红」——预测不出红法即说明断言形态选错；③ 变异后**看红在哪一条**：若红在无关的收尾断言、核心断言仍绿，等于没测（本例首版即「红的在④归零、②仍绿」）；④ 把「断言形态 + 变异实况」写进用例注释，防后人把弱断言当主体系。
  判例引用：`games/beads/tests/scene-vfx.test.ts`「裁定『恒播满』…」例（注释含两次失败记录）；同族 K-042 追记（断言域选错 ⇒ 变异不红）、K-036（变异自检方法论）、K-037（可证层级）。

- **[测试][K-061] `noAssemble` 下手工铺盘 ≠ 装配：子系统内部索引未建，表现为「点名了但不动手」，极易误判为业务缺陷**（来源 WXG-T-128 / commit 61d2911，2026-09-19）
  现象：为构造「solver 归位致过关」局面，用 `createBeadsHarness({ noAssemble: true })` + 手工 `grid.fill()` 铺满盘、再把一格染色 ⇒ `misplacedCount === 1`（**读面正确**），`usePowerup('solver')` 返回 `true`、`solverProgress` 从 0 走到 1 再归零 —— 但**棋盘纹丝不动**（`misplaced` 恒 1）、`phase` 恒 `playing`、`level:cleared` 计数 0。第一反应是「solver 有业务缺陷」。
  根因：`noAssemble` 只跳过关卡**装配**，而装配期同时建立了子系统（解环器点名队列等）依赖的**内部索引 / 缓存**；手工 `fill` 只改格子读面（`cell.state` / `isMisplaced`），不重建那些派生结构 ⇒ 点名清单为空、相 A 空转、相 B 无到点。**读面正确 ≠ 内部结构正确**。
  规避/做法：① 造局面优先「**默认装配 + 官方构造器**」（先装配出就位盘，再用异色对调 / `setBead` 造受控错位），不要为省事用 `noAssemble` + 手铺；② 断言「**行为已发生**」而非「状态读面正确」——本例若只断言 `misplacedCount` 与 `usePowerup` 返回值，会得到一个**自洽但完全空转**的绿；③ 排查「点名了但不动手」先怀疑装配面、再怀疑业务面（临时打印点名清单长度即可分辨）；④ 定位这类**静默空转**最快的办法 = 临时探针用例逐帧打印 `phase` / 进度标量 / 棋盘计数（用完即删，勿留在测试面）。
  判例引用：`games/beads/tests/scene-vfx.test.ts` 恒播满例（首版失败记录）；同族 K-038（测试入口 ≠ 真实入口）、K-042（测试失去自证能力）。

- **[测试][K-062] 事件驱动宿主的输入位移必须进单测：prev 在帧首结算会把帧间到达的位移吞成 0**（来源 WXG-T-178 / 真机反馈「能缩放不能拖动」，2026-09-19）
  现象：Cocos/微信真机上双指捏合正常缩放，但单指拖拽**完全不平移**；同一份代码在 Node 单测（501 例）与浏览器 harness 里全绿。插桩读数：拖拽分支每帧都跑（`panFrames=69`），但喂进去的位移 `panDxSum=0`，而指针坐标确实在变。
  根因：`InputManager.beginFrame()` 里做 `_prevX = _x`，而事件驱动宿主（Cocos）的原生 `touch-move` 到达在**两个固定步之间**（上一次 `endFrame` 之后、下一次 `beginFrame` 之前）⇒ 拷贝时 `_x` 已是新值，`dx = _x - _prevX` 恒 0。位移判定用的是「当前位置 − 按下位置」，所以「进了拖动态」看起来一切正常，只有真正要增量的地方拿到零 —— **症状与被吞的量正好错开一层**。
  规避：① 采样器的 prev 推进放在**帧末**（`endFrame`，一固定步 = 一输入帧），使事件驱动宿主与同步宿主同一语义；② 单测必须有一条**按宿主真实顺序**写的用例：`endFrame → push(事件) → beginFrame → 读 snapshot`，断言位移非零、且无新事件的下一帧位移归零；只在 `beginFrame` 之后 push 的同步形**永远测不出这类缺陷**；③ 通用形态：**「坐标对但行为不跟随」先查增量型量（dx/dy/dt/count delta）是否被采样时序吞掉**，别去查几何或语义；④ 修法要覆盖**所有走该通道的游戏**（本次两游戏共用 core，需同时验证另一游戏不读该量 → `grep .dx` 全仓确认影响面）。
  判例引用：K-037（指令流可证 ≠ 宿主可证）、K-036（全绿不构成证据）、K-042（断言不得与真源共用同一个错）、K-054（未登记 ≠ 已证伪）。

- **[测试][K-076] 脏工作树里「这条测试红是不是我引入的」必须用 stash 隔离实测判定，不得凭直觉归因**（来源 WXG-T-169 / commit c7d157d，2026-09-26）
  现象：本单改动落码后全量测试剩 1 条红（`bead-style-seal` 腿 4 整帧命令数对不上已登记基线），而它与本单同族（都是「渲染」），极易被误读为本单引入 ⇒ 下一步就是去「更新基准让它绿」（封箱类基准的红正好是被禁止那样做的）。
  根因：门禁红是**时点性**证据，工作树上同时存在他人未提交改动时，「红的集合归属」不可从「我改了什么」反推；本次真因是并发会话在 `view-model.ts` 新增缩放控件，使整帧 +3 rect / +3 text / +1 circle。
  规避：① **只 stash 自己的文件集**做对照：`git stash push -- <本单文件>` → 复跑该测 → 仍红即证与本单无关 → `git stash pop`（整树 stash 会吞掉并发会话的工作，禁用）；② 结论写进提交信息（「唯一红 = …，经 stash 隔离证明非本批引入」），否则下一个读者得重做同一实验；③ 封箱/基准类判据被他人红时，**不得为绿改基准**（K-051/K-053 口径），交回改动的那一单重封；④ 并发改动还在实时流入时（看 mtime / `git status` 新增脏文件），红灯集合本身会变，本判定只在**当时有效**，提交前重做一次；⑤ 尾报可能遮真因（本例 ctx 拦截报「索引不一致」，实为另一道预算门）——拦截后先看完整输出再下结论。
  判例引用：同族 K-040（读数变脸须逐条归因）、K-036（全绿不构成证据）、K-037（指令流可证 ≠ 宿主可证）、K-045（并发工作树的暂存原子性）、K-010（假阴性输出不得当证据）。
